{
  "service": "orbit-gateway",
  "listen": { "addr": ":8443", "tls_min_version": "1.3" },
  "auth": {
    "audience": "orbit-gateway",
    "jwt_token_leeway_seconds": 30,
    "public_key_refresh_seconds": 300,
    "bcrypt_cost": 12
  },
  "cache": {
    "enabled": true,
    "max_entries": 50000,
    "default_ttl_seconds": 120,
    "single_flight": true,
    "note": "bounded LRU cache; capacity eviction plus lazy TTL eviction on read"
  },
  "rate_limit": {
    "strategy": "sliding_window",
    "limit": 600,
    "window_seconds": 60,
    "note": "per-client sliding window rate limiter keyed on the jwt token subject"
  },
  "connection_pool": {
    "pool_max": 64,
    "acquire_timeout_ms": 1500,
    "health_check_interval_ms": 5000,
    "note": "per-upstream connection pool; acquire timeout surfaces exhaustion as 503"
  },
  "shutdown": {
    "grace_seconds": 20,
    "note": "graceful shutdown drains in-flight requests before exit"
  },
  "observability": {
    "metrics_path": "/metrics",
    "health_path": "/healthz",
    "scrape_interval_seconds": 15
  }
}
