# session-46 — v0.16 구현·검증·릴리스 (워크플로 max + Codex 교차가 회귀를 잡았다)

## 1. 세션 정보

- **세션**: 46 (직전 = session-45, v0.16 설계·계획)
- **기간**: 2026-07-30 ~ 2026-07-31
- **모델/노력**: Claude Opus 5 (1M context) / max effort, ultrathink
- **성격**: v0.16 구현 11태스크 → 태스크별 리뷰 → 최종 브랜치 리뷰 → 릴리스 커밋 → PR #29
  → **`/code-review max`(워크플로 37 에이전트) + Codex 2패스가 배송 차단 회귀를 찾아 머지 중단**
  → 회귀 고침 둘 → 머지·태그.

## 2. 시작 프롬프트

사용자가 붙여넣은 것은 **session-44 §7의 프롬프트**였다(한 세션 뒤짐). 그 첫 지시가
"`docs/prompts` 최신 기록을 읽고 재개"였고 최신은 session-45였으므로 그쪽 §7을 따랐다.
불일치를 즉시 표면화한 것이 옳았다 — 붙여넣은 프롬프트의 BASE(`8bc660d`)와 "첫 작업 = 범위 결정"이
모두 이미 지난 상태였다.

## 3. 무엇을 했나

**커밋 17개.** merge-base `1cbb426` → HEAD `9ea3437`(+ 머지·태그).

### 3.1 구현 11태스크 (subagent-driven-development)

계획의 실행 순서대로 **T9를 가장 먼저** 돌렸다. 각 태스크마다 서브 구현 + 서브 리뷰,
findings는 fix 라운드 + scoped re-review로 닫았다.

| 태스크 | 커밋 | 성격 |
|---|---|---|
| T9 `t.Run` 위생 | `7db1081` | 뒤 태스크의 유도 FAIL이 케이스 경계를 남기게 |
| T1 사유 열거형 (X3) | `8ae534f` | `dup bool` → `anomaly codexAnomaly` |
| T2 D87 (C2) | `b95ce42` + `a38c7e7` | **무효 TOML 기록 차단**, fix 1라운드 |
| T3 사유 문면 | `9da2efc` | `[16]`·설치기가 사유를 인쇄 |
| T4 D86 (X8) | `8cdfd68` | **사용자 설정 보존** |
| T5 판정원 단일화 | `a00577c` | 요청 조립까지 한 함수에 |
| T6 `[20]` 라벨·경고 | `6b71962` | **감지·고침 등가 완성**(15상태 표) |
| T7 D88 계약 문면 | `55c97e4` | 코드 줄 변경 0 |
| T8 X6 배선 테스트 | `415abc5` | **첫 실행에서 PASS** — 실사용 위험 해소 |
| T10 CHANGELOG 전수 | `800e4ed` | 32커밋 훑어 10항목 보완 |
| T11 인용 정리 (X4) | `b993ff9` | 전수 10건 중 6건 |

### 3.2 최종 브랜치 리뷰 → fix wave 둘

서브(opus) + **Codex 1차 교차**(`task --background`). critical 0 / important 0 / minor 5 +
이월 필수 1건. fix wave(`7e524cc`) 후 scoped re-review가 **살아 있는 계약 문서에 같은 부정확이
남았다**고 NOT ADDRESSED 판정 → 전수 정정(`bbcebda`).

### 3.3 릴리스 커밋과 PR

`25ef5c9` — `productVersion` 0.15.0 → **0.16.0** · CHANGELOG 절 날짜 확정 · 참조 링크 정의 둘.
푸시 후 **PR #29**. CI 전 레그 초록(**windows-latest 포함**).

### 3.4 ★ `/code-review max` + Codex 2패스 — 머지 중단

사용자 지시로 PR을 재검증했다. 워크플로 **37 에이전트**(후보 36 → 검증 29 통과·7 기각 → **15 결함**),
대부분 격리 모듈이나 `go test -overlay`로 **실제 실행해 재현**했다.

**앞선 리뷰 전부(태스크별 11회 + 전체 브랜치 + Codex 1차)가 놓친 것들이었다.**

Codex 2패스가 회귀 다섯을 독립 판정했다(1차 시도는 컨트롤러 프롬프트 결함으로 실패 — "셸 금지 +
파일을 직접 읽어라"가 이 호스트에서 모순. **코드를 프롬프트에 인라인으로 담아 재시도해 성공**).

| | 후보 | Codex | 원인 |
|---|---|---|---|
| R1 | uninstall이 성공 문면 내며 등록 남김 | **REGRESSION** | 구현 선택 |
| R2 | `--fix`가 도구 0개 allowlist 보존 | 회귀 아님 | **D86 결과** |
| R3 | `[20]`이 command 수정본에 `--fix` 권함 | **REGRESSION** | 구현 선택 |
| R4 | `표식없음` + "이미 현재 버전" 모순 | **REGRESSION** | 구현 선택 |
| R5 | `--fix` 뒤 뒤처진 도구 목록 | 회귀 아님 | **D86 결과** |

**`BLOCKING: R1, R3, R4`** — 사용자가 "그 셋을 고치고 머지"를 결정했다.

### 3.5 회귀 고침 둘

- `f55b97a` — R1·R3·R4 + 회귀 테스트 넷. R1은 **인쇄만**(exit code 계약 불변 — Codex의
  "실패를 반환하라" 제안을 컨트롤러가 좁혔다), R3은 **문면만**, R4는 값 토큰 **치환**.
- `9ea3437` — **직전 fix가 들인 신규 파손 차단.** R4 고침이 `inlineMarkerSpan`을 비문자열 값에도
  구간을 돌려주게 만들었는데 그 함수의 바깥 루프가 문자열 비인식이라 **사용자 값 안의 텍스트가
  치환 대상이 됐다**(TOML 파스 불가). 바깥 루프를 같은 파일의 기존 기준으로 전진시켜 닫았고,
  **종전부터 있던 홑따옴표 변형의 조용한 값 변조도 함께 닫혔다.**

## 4. 현재 저장소 상태

- **`main`** = **`9ea3437`** = origin. **fast-forward 머지**(`1cbb426..9ea3437`, 17커밋,
  13파일 +1545/-160) — v0.15와 같은 형태로 머지 커밋이 없다.
- **태그 `v0.16.0`**(annotated) = `9ea3437`, 푸시 완료. **PR #29 = MERGED.**
- `productVersion` = **0.16.0**. CHANGELOG `[0.16.0] — 2026-07-31`.
- 열린 PR 없음. 브랜치 `feat/v0.16-d85-d88`는 origin에 남아 있다.
- 작업 트리: 추적하지 않는 `.claude/`·`.codex/`만.
- **CI 전 레그 초록**(`9ea3437` 기준): crossbuild · lint · test macos/ubuntu/**windows** ·
  test-node-leg. `mergeState=CLEAN`이었다.
- **로컬 red 9건은 기준선**(`internal/exec`의 `powershell` 레그). **CI의 windows-latest가
  통과한 것이 그 진단을 확정한다** — 같은 코드가 CI Windows에서 초록이므로 원인은 코드가 아니라
  로컬 호스트의 실행 정책이다(닫힌 BaseEnv 표 자식에서 `PSExecutionPolicyPreference`가 상속되지
  않아 PS 5.1이 실효 `Restricted`).
- **다음 작업의 BASE = `9ea3437`**(또는 그 뒤에 이 핸드오프 커밋이 생기면 그 값 — P0에서
  `git rev-parse HEAD`로 읽는다).

## 5. 이월 — v0.17에서 해결할 것

### 5.1 D-번호가 필요한 설계 항목 (우선순위 순)

1. **★ 판독기 셋의 문자열·주석 인지 통일** — 이 릴리스의 결함 대부분이 여기서 나왔다.
   - `tomlInlineValue`(**읽기 경로**) — 주석·문자열 비인식. **R4 모순의 진짜 뿌리**이고,
     그것을 고치지 않으면 `inlineMarkerSpan`에 `#` 이탈을 넣을 수 없다(넣으면 R4가 재현된다).
     D82 소유 판정의 의미를 건드리므로 D-번호가 필요하다.
   - `codexEscapedKeyInSpans` — D87 오탐 둘(충돌 불가 키 동결 + 수행 불가능한 안내 · 문자열 안
     쉼표 오인). 리뷰어가 구체적 고침을 제시했다: 문자열 건너뛰기 한 번으로 대체하면
     `stripTrailingComment` 23줄이 통째로 없어지고 브랜치가 추가한 15 픽스처의 판정이 전부 동일하다.
   - `inlineMarkerSpan` — 문자열은 `9ea3437`이 닫았고 **주석은 남았다**(TOML 파스는 안 깨진다).
   **셋을 함께 닫아야 판독 기준이 갈리지 않는다.**

2. **config.toml 파손 둘** (워크플로가 CONFIRMED로 재현)
   - **점 표기 `env.X` 키**: `envMissing`이 `[mcp_servers.ctr.env]` 헤더와 인라인 `env = { … }`만
     인식해, 관리 테이블 안의 `env.CTR_MANAGED = …`를 무관한 보존 라인으로 보고 env 헤더를 append한다
     → **같은 테이블이 두 번 정의된 무효 TOML**. Codex가 파일 전체를 파스 못 해 사용자가 그 파일의
     모든 MCP 서버와 설정을 잃는다. `view.inlineEnv` 가드가 인라인 표기에 대해, D87이 이스케이프
     표기에 대해 막는 것과 **같은 결과이고 점 표기가 남은 구멍**이다.
   - **이스케이프를 담은 헤더 이름**: TOML이 `mcp_servers.ctr`로 읽는 표기를 `codexManagedSpans`가
     구간으로 못 잡고 `ctrKeySignal`도 리터럴 매치라 충돌로 서지 않는다 → install이 append해
     같은 테이블이 두 번 정의된다. (따옴표만 있고 이스케이프가 없는 형태는 `mcpConflict`로 정상 차단.)

3. **D86 인수 비용 둘을 다시 열지 여부** — Codex가 **회귀가 아니라 계약의 결과**로 판정했다.
   - **R2**: 구 `# BEGIN/END` 블록 안 남의 테이블을 `--fix`가 인수하면서 `enabled_tools = ["x"]`를
     보존해 **도구 0개 allowlist**가 되고, 2회차 `doctor`는 정상으로 보고한다.
     `hook install --codex`는 같은 입력에 올바른 allowlist를 쓰므로 **doctor가 권한 경로만
     파손 상태로 수렴한다.**
   - **R5**: `--fix` 뒤 뒤처진 도구 목록이 "현재"로 보고된다. **CHANGELOG에 이미 문서화됐다**
     (T10이 "`--fix`는 도구 목록을 늘리지도 않는다 — 그 반영은 `hook install --codex`가 한다"를 넣었다).
   - 질문: D84 위치 기반 인수 경로에서는 `MarkerOnly`를 끄는 것이 맞는가?

4. **`hook uninstall --codex`의 잔여** — `# END context-router` 마커가 구 블록이
   `[mcp_servers.ctr.env]`까지 감쌌을 때 마이그레이션 뒤 남는다(설계서 §5.5가 비범위로 선언한 배치).
   짝 없는 마커가 남으면 이후 `classifyMarkers`가 `classAnomaly`를 반환해 self-heal이 막힌다.

### 5.2 문면·감시선 (v0.16 이월 Minor에서 승격 후보)

- `[16]`의 `mcpExistingHeader` 갈래에 **지시가 없다** — `--fix` 거절 문면이 그쪽으로 안내하는데
  `[16]`은 `테이블=존재`만 인쇄한다. 두 줄이 표면상 어긋난다(`존재` vs `미등록`).
- `[20]` 경고가 **없는 파일의 수리 동작까지 서술**한다(config.toml이 없는 프로젝트에 백업을 약속).
- `.mcp.json` 은퇴 정리 **예고와 경고 술어가 어긋난다** — 경고는 표식 버전 드리프트만 보는데
  `--fix`의 기입 술어는 `retireSupersededMCPServers`를 포함한다.
- `[20]` 사유 줄 감시선이 **접두만 본다** — 사유가 뒤바뀌는 회귀를 잡지 못한다(스펙 §2-7이 요구하는
  세 표면 중 `[20]` 다리만 무감시).
- `codexMarkerLabel`이 `markerState` 클로저의 라벨 문법을 **복제**한다 — 한 줄에 두 라벨을 인쇄하는데
  그 문법의 소유자가 둘이다.

### 5.3 위생

- `mcp_install.go:21`의 "ctr-exec의 8도구" 한 줄 확인(스테일이 아닐 공산이 크다 — 기본 프로필
  `ingest,net` = 6+1+1 = 8).
- `internal/exec`의 줄 번호 인용 재발(3/3 표본) — v0.15가 그 패키지를 정리했는데 다시 생겼다.
- `TestDoctorVersionlessHookMarker`의 호스트 의존(`CODEX_HOME` 미설정).
- `TestDoctorWarningAnnouncesCommandRewrite`는 문면 결속이라 리워딩에 취약(정상 성질이나 인지 필요).

### 5.4 v0.16 이월 Minor 48건

`.superpowers/sdd/2026-07-30-v0.16-d85-d88/progress.md`에 태스크별로 `file:line`과 함께 기록돼
있었다. **그 디렉터리는 gitignore되므로 머지 후 사라진다** — 위 5.1~5.3이 그중 승격분이고,
나머지는 다듬기·주석·커버리지 수준이다.

## 6. 이 릴리스에서 드러난 것 — 다음 세션이 반복하지 말 것

1. **★ 판정 함수의 실패 모드를 바꾸면, 그 함수를 공유하는 다른 경로의 무해했던 오탐이 유해해진다.**
   D87이 `codexManagedSpans`의 무변경 집합을 넓히자 `uninstall`이 조용히 실패했고(R1),
   R4 고침이 `inlineMarkerSpan`의 반환 조건을 넓히자 오탐이 사용자 값을 파괴했다.
   **둘 다 D85가 만든 "판정원 하나"의 이면이다 — 판정원을 묶으면 결함도 함께 전파된다.**
   공유 판정을 넓힐 때는 **그것을 소비하는 모든 경로의 실패 갈래**를 함께 세어야 한다.
2. **`/code-review max`(워크플로)가 앞선 리뷰 전부가 놓친 것을 찾았다.** 태스크별 리뷰 11회 +
   전체 브랜치 리뷰 + Codex 1차가 critical 0 / important 0으로 판정한 브랜치에서 **회귀 다섯과
   파손 경로 둘**이 나왔다. 차이는 **에이전트 수와 실행 검증**이다 — 37 에이전트가 격리 모듈과
   `go test -overlay`로 실제로 돌렸다. **릴리스 전 한 번은 그 밀도로 봐야 한다.**
3. **"세는 방식이 누락을 남긴다"가 네 번 반복됐다** — X7의 원래 결함 · T10의 전수 훑기가 리뷰
   지목분 외 여섯을 더 찾음 · 잔여 문면이 네 곳 중 한 곳만 고쳐짐 · 인용 정리에서 실측이 브리프의
   넷보다 둘 많음. **네 번 다 전수로 돌았을 때만 닫혔다.**
4. **계획이 축자로 준 것에도 결함이 있다(여섯).** 유도 FAIL Expected 셋(도달 불가 관문 · 다른
   단정이 묾 · 아예 무감시) · 커밋 메시지가 코드와 반대 · 브리프 코드가 앞 태스크 작업을 되돌림 ·
   `MarkerOnly` "두 곳"(실측 한 곳). **실측을 요구하는 dispatch 규율이 전부 잡았다.**
5. **Codex는 이 호스트에서 `task --background`로 쓰되 파일 읽기를 전제하지 마라.** "셸 금지 +
   파일을 직접 읽어라"가 모순이라 한 번 실패했다. **코드를 프롬프트에 인라인으로 담는 것이
   확실하다**(18KB로 다섯 판정을 받았다). 1차 패스가 성공한 것은 운이었다.
6. **fix가 새 파손을 만들 수 있다 — 두 번 났다.** 매 fix 라운드의 scoped re-review에
   **"이번 고침이 또 새 파손을 들이지 않았는가"**를 핵심 질문으로 넣어라.
7. **서브의 자체 측정이 틀릴 수 있다.** T11 구현자가 "네 파일 커밋 blob이 100% CRLF"라고 보고했으나
   실측은 CR=0이었다. **컨트롤러가 직접 재는 것이 판정이다.**
8. **태스크 알림에 붙어 오는 diagnostics는 stale 스냅샷일 수 있다** — RED 단계의 컴파일 에러나
   이미 지워진 프로브 파일이 보인다. DONE 보고와 모순되면 **직접 확인하고 그 결과를 따른다.**

## 7. 상시 프로토콜

- **막히면** 협업: Codex(`task --background`, 코드 인라인) / 웹·context7 / 서브. 블라인드 재시도 금지.
- **리뷰 체크포인트**: 서브 + Codex. 재리뷰는 서브 단독(Codex는 체크포인트당 1패스).
  **릴리스 전 한 번은 `/code-review max`**(워크플로) — 이번에 그것만이 회귀를 잡았다.
- **실행**: superpowers `subagent-driven-development`. BASE는 원장에서, `HEAD~1` 금지.
- **서브 위생**: 전체 파일 재작성 금지, 긴 테스트 데이터는 `strings.Repeat`/testdata, 밀집 태스크 분할.
  Go 테스트는 `-p 1`, 메모리 캡 규칙 준수. **임시 프로브는 반드시 지운다.**
- **커밋**: `git add -A` 금지, 인덱스 열 확인. 트레일러
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
- **파일 출력**: UTF-8(BOM 없음)·LF. **판정 지점은 커밋 blob.** 역슬래시는 방향별로 센다.
  **Bash 도구가 `\\`를 셸에 닿기 전에 `\` 하나로 붕괴시킨다** — Grep 도구나 quoted heredoc을 쓴다.
- **머지·태그·푸시는 사용자 명시 승인.**
- 문서 계층: `docs/*.md`(살아 있는 계약, 제자리 개정) / `docs/superpowers/{specs,plans}`(날짜 붙은
  불변 기록) / `docs/prompts`(핸드오프, append-only).

## 8. 다음 세션 시작 프롬프트

> `docs/prompts` 최신 기록(session-46)을 읽고 재개. **v0.16.0 릴리스가 완료됐다** — 구현 11태스크 ·
> 태스크별 리뷰 · 최종 브랜치 리뷰(서브 + Codex) · PR #29 · **`/code-review max`(워크플로 37
> 에이전트)와 Codex 2패스가 배송 차단 회귀 셋을 잡아 머지를 중단시켰고** 그것을 고친 뒤 머지·태그까지
> 닫았다. `main` = v0.16.0, 태그 `v0.16.0`, `productVersion` 0.16.0, 열린 PR 없음.
> 브랜치 `feat/v0.16-d85-d88`는 origin에 남아 있다. 작업 트리에는 추적하지 않는 `.claude/`·`.codex/`만.
>
> **첫 작업 = v0.17 범위 결정.** session-46 §5가 이월의 진실 소스다(`.superpowers/sdd/`는
> gitignore되므로 그 원장은 이미 사라졌다). 무게순:
>
> 1. **판독기 셋의 문자열·주석 인지 통일**(`tomlInlineValue` 읽기 경로 · `codexEscapedKeyInSpans` ·
>    `inlineMarkerSpan`의 주석). **이 릴리스 결함 대부분의 뿌리**이고, 읽기 경로를 고치지 않으면
>    `inlineMarkerSpan`에 `#` 이탈을 넣을 수 없다(넣으면 R4 모순이 재현된다). D82 소유 판정의
>    의미를 건드리므로 **D-번호가 필요하다.**
> 2. **config.toml 파손 둘**(점 표기 `env.X` 키 · 이스케이프 헤더 이름) — 둘 다 같은 논리 테이블을
>    두 번 정의해 **Codex가 파일 전체를 파스하지 못한다.** D87이 키 표기에서 닫은 것과 같은 부류.
> 3. **D86 인수 비용 둘(R2·R5)을 다시 열지 여부** — Codex가 회귀가 아니라 계약의 결과로 판정했다.
>    D84 위치 기반 인수 경로에서 `MarkerOnly`를 끄는 것이 맞는지가 설계 질문이다.
> 4. 문면·감시선 다섯 · 위생 넷.
>
> **§6의 여덟 교훈을 읽어라 — 특히 첫째다**: 판정 함수의 실패 모드를 바꾸면 그 함수를 공유하는
> 다른 경로의 무해했던 오탐이 유해해진다. 이 릴리스에서 **두 번** 났다(D87 → uninstall 조용한 실패,
> R4 고침 → 사용자 값 파괴). **D85가 만든 "판정원 하나"의 이면**이므로 v0.17이 판독기를 통일할 때
> 같은 함정을 반드시 지나간다 — 공유 판정을 넓힐 때 **그것을 소비하는 모든 경로의 실패 갈래**를
> 함께 세어라.
>
> **릴리스 전 한 번은 `/code-review max`를 돌려라.** 태스크별 리뷰 11회 + 전체 브랜치 리뷰 +
> Codex 1차가 critical 0으로 판정한 브랜치에서 그것만이 회귀 다섯과 파손 둘을 찾았다.
>
> **Codex는 `task --background`에 코드를 인라인으로 담아 쓴다** — "셸 금지 + 파일을 읽어라"는
> 이 호스트에서 모순이고 한 번 실패했다.
>
> 막히면 웹+서브+Codex 협업, `go test -p 1`, `git add -A` 금지, 인덱스 상태를 보고 커밋,
> 머지·태그·푸시는 사용자 명시 승인. ultrathink
