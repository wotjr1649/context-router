# Session 36 — v0.12 머지 게이트(코드리뷰 2종) + v0.12.0 릴리스 (2026-07-26)

1. **Session/span/model**: session-36, 2026-07-26 단일 일자. Opus 5(1M, max effort) 단일 모델.
   리뷰·fix 서브에이전트 전부 general-purpose + `model:"opus"` 명시.

2. **Starting prompt (verbatim)** — session-35 부록 B로 부팅:
   > docs/prompts 최신 기록(session-35)을 읽고 재개 — 특히 부록 B(머지 게이트)와 부록 C(PowerShell).
   > v0.12는 PR #25로 열려 있고 CI 전부 green(브랜치 feat/v0.12-d63-d67 @ fd4363d, 46커밋,
   > base main=ec69381). 머지는 보류 중이다.
   >
   > 첫 작업 = 상세 코드리뷰 두 개를 통과시키는 것이고, 통과해야 머지한다:
   > ① /code-review max
   > ② Codex 크로스모델 리뷰 — docs/codex-secure-review.md 프로토콜대로
   >    review --background --base ec69381, 결과는 리뷰 서브가 자기 컨텍스트에서 읽어
   >    고정 JSON findings로 통합(컨트롤러는 저밀도 JSON만 소비)
   >
   > 부록 B.2의 "이미 검토된 경계" 표를 먼저 보고 중복을 피할 것 — 76f3c13..780d5fa(CI 조사)는
   > 전 브랜치 관점에서 한 번도 검토되지 않았다. 초점: exec.go의 PSModulePath 3항목 주입(D65 계약을
   > 바꿨고 로컬 재현 없이 CI green으로만 검증됨, MkdirAll 실패가 경고로 남은 계약), 남긴 계측 497행의
   > 적정 크기, ci.yml의 -count=1과 setup-node/bun/python, 설계서 D65·§2 개정.
   >
   > findings → fix → 스코프 재리뷰. 통과 후 사용자 승인으로만 머지 → v0.12.0 태그 → 태그 CI의
   > tag-version으로 D56 실검증. 이월 minor 약 47건은 로컬 원장
   > .superpowers/sdd/2026-07-25-v0.12-d63-d67/progress.md 참조. CI가 초록이어도 자동 머지 금지.
   > ultrathink

3. **What was done**

   **머지 게이트를 리뷰 3라운드 · fix 3 wave로 통과시키고 v0.12.0을 릴리스했다.**

   | 라운드 | 판정 | 내용 |
   | --- | --- | --- |
   | ① `/code-review max` + ② Codex(`review --background --base ec69381`) 통합 | needs_fixes | critical 0 · important 10 · minor 10 |
   | 재리뷰 2 (스코프 `fd4363d..ea600da`) | needs_fixes | G1~G21 중 **ADDRESSED 16 · PARTIAL 0**, 신규 important 1(R1) |
   | 재리뷰 3 (델타 `ea600da..4b8f50f`) | **approved** | 신규 결함 **0건** |

   fix 커밋 6개: `45719d4`(G5~G11) · `adf3470`(G12·G14~G17) · `9ad8538`(G1) ·
   `c54540f`(G2) · `ea600da`(설계서·주석) · `4b8f50f`(R1·R2·R4).
   findings 파일 3개는 로컬 원장에 있다(`merge-gate-findings.json` ·
   `merge-gate-integrated.json` · `merge-gate-rereview.json` · `-rereview-2.json`).

   **이번 게이트의 최대 수확 — 재리뷰가 fix가 만든 회귀를 잡았다(R1).** G8이 uninstall에서
   빈 승인 배열을 남기게 바꿨는데(하위 스코프 목록이 살아나는 것을 막기 위해), 그 빈 배열이
   `enabledServersScope`에서 "정의됨"으로 세어져 **같은 프로젝트의 재설치가 승인 이름을 되돌리지
   않았다** — `.mcp.json` 등록만 복구되고 자동 승인은 빠진 채 "설치 완료"만 남는다. CI 초록으로는
   잡히지 않는 형태이고, 재리뷰가 없었으면 그대로 머지됐다. 설계 D64가 이미 수정 방향을
   지시하고 있었다("사용 중인 최고 우선순위 스코프에 쓴다").

   **Codex의 유일한 critical 주장은 기각했고 근거를 코드에서 확인했다.** 주장은 "나이 필터가
   한 media 행만 오래되면 hash를 골라 보존창 내 다른 행·소스·청크까지 삭제한다"였다. 도달 불가다:
   `mediaType`은 `isMarkdownExt(path)` 기반이고 hook ingest는 path를 `""`로 넘기므로(shadow.go:82)
   항상 `text/plain`, `UNIQUE(content_hash, media_type)`(store.go:189)가 hash당 hook 행을 1개로
   묶는다. 두 `NOT EXISTS`는 비-hook 소스가 하나라도 있으면 hash 전체를 제외한다.
   **그런데 `/code-review`가 같은 줄(store.go:1055)을 다른 기전으로 지목한 것은 진짜였다** —
   두 리뷰가 같은 자리를 다른 이유로 가리킨 셈이다(아래 G1).

   **`plan_conflict` 4건은 사용자 판단으로 확정했다.**

   - **G1 — shadow 보존 나이 기준을 마지막 포착으로**(계획 T11의 `artifacts.created_at` 문면 대체).
     프로덕션에 `UPDATE artifacts SET created_at`이 없어(테스트 헬퍼만) 동일 바이트 재포착 시
     `created_at`이 첫 포착에 고정된다 → 방금 재포착한 콘텐츠가 지워지고 초 단위 전에 모델에 넘긴
     ref가 `ctr_fetch`에서 안 풀린다. 형제 경로 `PurgeOlderThan`은 이미 `indexed_at`을 쓴다.
     교체 형태: `AND NOT EXISTS(SELECT 1 FROM artifacts a4 JOIN sources s4 … WHERE
     a4.content_hash = a.content_hash AND s4.indexed_at >= ?)` — hash 단위, 범위 축소 전용.
   - **G2 — `runner.extra`를 fail-closed로**(`([]string, error)`). 판정 기준 하나:
     "부재가 호스트 무관 기본으로 안전하게 퇴화하는가". 시행점 = pip.conf · nuget.config ·
     psmodules · GOTMPDIR · macOS Application Support · NuGet sentinel. 이중 안전장치 =
     npmrc · 스크래치 홈 · pycache · go-env.
   - **G3(종료코드) · G4(unix shell 격리) — 설계서 문면만**, 구현은 v0.13.

   **설계서에서 열린 항목 하나가 닫혔다** — D65 "쓰기 실패 계약"이 "러너 공통으로 정한다"로
   미결이었는데 G2 결정이 그 답이었다. `go-env`는 **경계 사례로 검토해 승격하지 않았다**(부재가
   호스트 파일이 아니라 go 내장 기본으로 가므로 기준상 benign, 하드닝 상실을 근거로 한 곳만
   올리면 기준이 둘이 된다) — 설계서에 "빠뜨린 것이 아니라 결정한 것"으로 남겼다.

   **G13은 리뷰어 수치를 인용하지 않고 내가 확인한 구조적 사실만 적었다.** 스키마에
   `CREATE INDEX`가 한 줄도 없어(암묵 인덱스는 `UNIQUE` 둘) 술어의 `sources.raw_blob_hash`·
   `s2.artifact_id` 조회가 후보 행마다 `sources`를 훑는다 → `LIMIT`은 반환 행만 묶고 술어 평가는
   못 묶는다. 리뷰어가 보고한 타이밍(2천→2만에서 253ms→73s)은 재현하지 않았으므로 적지 않았다.

   **R3는 고치지 않기로 판정했다** — 별표가 서버 세그먼트에 온 규칙에 대해, 호스트가 그 위치의
   별표를 와일드카드로 해석하는 한 그 규칙은 실제로 그 도구를 덮으므로 현재 보고가 **참 양성**이고
   제안된 수정이 그것을 거짓 clean으로 되돌린다. 재리뷰 3이 독립적으로 같은 결론에 도달했다.

   **검증**: `go test -p 1 -count=1 ./...` 전수 green **3회**(14패키지, 캐시 아님) +
   build·vet·gofumpt clean 3회. CI 3-OS는 `ea600da`·`4b8f50f`에서 push·PR 런 모두 초록.
   이 브랜치에서 처음 unix를 탄 두 가지가 검증됐다 — `extra` fail-closed 픽스처(파일 자리에
   디렉터리를 만들어 실제 OS 오류를 내는 방식)와 `sources.indexed_at` 기반 술어.

   **릴리스**: PR #25 머지(`4c73244`, 2026-07-26T11:10:04Z) → `v0.12.0` annotated 태그 →
   **태그 CI success, `tag-version` 포함 — D56 실검증 완료**.

4. **Repo state**

   - **main HEAD = `4c73244`**(`Merge pull request #25 from wotjr1649/feat/v0.12-d63-d67`),
     `ec69381..4c73244` = 53커밋. tag **`v0.12.0`** 최신.
   - `productVersion` = **0.12.0**(`internal/buildinfo/buildinfo.go:9`).
   - 원격 브랜치 `feat/v0.12-d63-d67` **삭제하지 않았다**(머지 시 `--delete-branch` 미사용).
   - working tree: untracked 로컬 설정 2개(`.claude/`·`.codex/`)만 — `git add -A` 금지 규율 유지.
   - 로컬 원장(gitignored): `.superpowers/sdd/2026-07-25-v0.12-d63-d67/` — findings JSON 4개와
     이번 라운드의 diff 패키지(`review-76f3c13..fd4363d.diff`, `review-ec69381..fd4363d.diff`,
     `review-fd4363d..ea600da.diff`, `review-ea600da..HEAD.diff`).

5. **정정 (과거 기록 수정 금지 규약에 따라 여기에)**

   **`4b8f50f` 커밋 메시지의 R3 부수 문장이 과장이다.** "제안된 수정은 allow 쪽에만 적용돼
   비대칭 복원"이라고 썼지만, 그 수정은 `ruleToolSet` 한 곳이고 그 함수는 `r`·`a` 양쪽에
   호출되므로 대칭 적용된다. 재리뷰 3이 지적했고 확인했다. **R3를 고치지 않는 결정 자체는
   주 논거(참 양성)만으로 성립하며 코드에 영향 없다.**

6. **Carryovers**

   - **사용자 액션 — `hook install` 재실행**: 로컬 `.claude/settings.json`·`.codex/hooks.json`
     마커가 `0.11.1`이라 doctor `[9]`·`[16]`·`[17]`이 불일치를 보고한다. 이번 릴리스부터
     `hook install`이 `.mcp.json`·승인 키까지 다루므로 `--enable-exec` 포함 실사용으로 확인할
     기회다. **이번 세션이 그 경로를 크게 손댔으므로 도그푸딩 값이 특히 높다** — R1(재설치가
     승인 이름을 되돌린다) · R2(uninstall이 은퇴 이름도 지운다) · G9(은퇴 항목 프로필 이월) ·
     G11(명시 `alwaysLoad:false` 보존)이 전부 이 경로다.
   - **릴리스 노트 한 줄이 아직 없다**: `PYTHONNOUSERSITE=1`로 호스트 `pip install --user`
     패키지가 python 러너에서 보이지 않게 된 것은 사용자 관측 가능한 동작 변경인데 출하 기록이
     어디에도 없다. **확인한 사실**: 저장소에 `CHANGELOG.md`가 없고 `gh release list`도 비어 있다
     (태그만 씀), README에 러너 env 격리 언급이 0건. 이번 릴리스에는 `extra` fail-closed도
     같은 부류로 추가됐다(격리 준비 실패가 이제 실행 거부다).
   - **v0.13 후보** — 이번 게이트가 남긴 것:
     ① unix shell 러너 툴체인 구성 격리(설계 §1 D65 명시적 제외 · §4 이월. 재지정 대상이 열린
     집합이라 여섯 번째 복사본이 아니라 공유 지점 필요 — D13)
     ② PowerShell 네이티브 종료 상태 후행 전파(§1 D66 · §4. 한 줄로 닫히지 않는 이유는 마지막
     문장이 cmdlet일 때 남아 있는 `$LASTEXITCODE`)
     ③ shadow 술어 스캔 경계 + 색인(§4. **나이 기준 교정이 색인 없는 하위 조회를 하나 더
     더했으므로 우선순위가 올라갔다**)
     ④ G18 ci.yml — 액션 8개 전부 SHA 핀 + workflow `permissions: contents: read`(하나만
     바꾸면 불일치라 전부 함께)
     ⑤ G19 기동 purge 고루틴 취소(`lockStore`가 `context.Background()` 고정, blob 루프가 ctx
     미확인 → 종료가 최대 5s 지연되고 D67 임계값 튜닝이 읽는 `deferred` 수를 부풀린다)
     ⑥ G20 `buildinfo.go` 주석-값 불일치 — 주석은 dev 사이클 `-dev` 접미를 처방하는데 최근 세
     릴리스가 모두 따르지 않았다. 주석을 실무에 맞추거나 실무를 주석에 맞추는 프로세스 결정.
     ⑦ windows 파일 lint 부재(session-35 A.7) — 미검출 errcheck 13건 + ubuntu 잡에
     `GOOS=windows golangci-lint run ./...` 한 스텝
     ⑧ node 인터프리터 레그 CI 커버리지(bun 설치로 `detectJS`가 항상 bun을 고름)
     ⑨ `docs/prompts` 파일명 규약(session-35 기록이 종료일로 명명됨 — 이 기록은 시작일 규약을
     따랐다) ⑩ 나머지 이월 minor 약 47건은 로컬 원장 `progress.md`
   - **관측 기준선은 session-35에서 리셋됐다**(shadow 1254→20 아티팩트). D67 임계값 관측과
     D63 상시 로드 전후 비교는 그 시점부터 새로 쌓는다(설계서 §3에 기록).

7. **Standing protocols** (유지 + 이번 세션 실증)

   - **fix는 회귀를 만든다 — 재리뷰를 생략하지 말 것.** 이번 R1이 그 실증이다. 1라운드 fix가
     심은 회귀를 2라운드가 잡았고, 3라운드는 신규 0건으로 닫혔다. **CI 초록은 이 층을 보지 못한다.**
   - **크로스모델 리뷰 = 중립 프레이밍 + JSON findings**(`docs/codex-secure-review.md`) 무사고 유지.
     Codex 1패스/체크포인트 규율 지켰다(재리뷰 3회 모두 서브에이전트 단독).
   - **서브에이전트 보고는 검증 대상이다.** 이번에도 성립했다 — Codex critical 기각 근거를
     코드에서 재확인, `/code-review`의 "superseded 정리가 마커를 요구하지 않는다"는 절반이
     부정확(`:126`도 명령 일치만으로 통과), 리뷰어 타이밍 수치는 미재현이라 문서에 넣지 않음,
     R3는 두 리뷰어의 제안을 기각.
   - **컨트롤러가 설계 문면을 직접 쓴 것이 값이 컸다** — 두 fix 서브가 같은 설계서를 동시에
     쓰면 편집이 유실되고, 과대주장 여부는 이 저장소가 이미 물린 지점이다(session-35 §2 축소).
   - **"거짓 clean"은 important**: 이번 라운드의 G5(진단이 두 형태를 못 봄)·G6(판정 실패를
     "정의 없음"으로)·G10(하지 않은 제거를 완료라고)·G12(정지가 실측된 인터프리터를 아무 테스트도
     안 지남)가 모두 그 부류였다.
   - **테스트가 옛 계약에만 의존했는지 확인할 것**: G1 교정 후 `TestPurgeHookOnlyOlderThan`이
     `created_at`만 늙혀서 실패했다 — 그것이 옛 기준 의존의 증거다. 나이·건수 예산을 **함께**
     쓰는 테스트가 없어 두 위치 인자가 뒤바뀌어도 통과할 수 있었던 것도 같은 라운드에서 닫혔다.
   - 유지: `go test -p 1` / gofumpt / **`git add -A` 금지** / BASE 원장 기록 / 스펙(설계서) 승 /
     보안 서술 최소화 / CI 3-OS가 unix 실검증 권위 / 머지는 사용자 명시 승인.

8. **도그푸딩 결과 (같은 세션, 릴리스 직후 — 6번 첫 항목을 여기서 소진했다)**

   `go install ./cmd/context-router`로 바이너리를 0.11.1 → **0.12.0**으로 올린 뒤 실행했다.
   **`hook install`은 한 번이 아니라 세 번이다** — `--codex`가 배타 플래그이고 `--user`가 또
   갈라진다(`codexHooksPath(user, projectRoot)`):

   1. `hook install` → Claude 프로젝트 훅 6개 + `.mcp.json` + 승인 키
   2. `hook install --codex` → Codex 프로젝트 훅 3개
   3. `hook install --codex --user` → Codex 사용자 훅 3개 (**이것을 빼면 `[16]`이 계속 불일치**)

   **결과 — 마커 전부 `0.12.0`, `[9]`·`[16]`·`[17]` 불일치 해소.**

   - `[16] codex: … project=등록됨(3개, marker 0.12.0) user=등록됨(3개, marker 0.12.0)`
   - `.mcp.json`의 `ctr-exec`: **`args`가 보존됐다**(`["--enable","exec"]` 그대로 — 플래그 없는
     재설치가 프로필을 끄지 않는 규칙이 실사용에서 성립). `alwaysLoad: true`와
     `__ctrManaged: context-router/0.12.0`이 새로 붙었다. 설치 전 항목에는 마커도 `alwaysLoad`도
     없었다(v0.11.1엔 `.mcp.json` 처리가 없어 사용자가 손으로 넣은 항목이고, 소유 관문은
     명령 일치로 통과했다).
   - 승인 키: 어느 스코프도 정의하지 않은 상태였으므로 프로젝트 `.claude/settings.json`에
     `["ctr-exec"]`로 기록됐다.
   - **재설치 멱등 실측**: 2회차 `hook install` 후 `.mcp.json`·`settings.json` **바이트 동일**.
     2회차는 **쓰기 갈래를 탔다**("기록했습니다") — 승인 키가 이미 project에 정의됐으므로
     `winner == path`이고, R1이 고친 그 분기다. 다만 **R1의 실제 버그 케이스(빈 배열)는 이
     도그푸딩이 재현하지 않는다**(목록에 이름이 이미 있어 보고 갈래여도 무해했을 상태다) —
     그쪽은 유닛 테스트가 덮는다.
   - `[19] permissions: ask/allow 충돌 없음` — **거짓 clean이 아니다**: `settings.local.json`의
     ask가 0건이라 겹칠 대상 자체가 없다(allow 1건).
   - `[17] build: 0.12.0 (… dirty)` — `dirty`는 Go의 `vcs.modified`이고 porcelain에는 untracked
     `.claude/`·`.codex/` 둘뿐이다. 추적 소스에 미커밋 변경은 없다.

   **하지 않은 것**: `hook uninstall` → 재설치 왕복. R1의 빈 배열 케이스와 R2(은퇴 이름 정리)를
   실사용으로 보려면 그 왕복이 필요하지만, 세션 중 라이브 MCP 등록을 끊는 위험이 있어 돌리지
   않았다. 유닛 테스트가 두 경로를 덮고 있고, 필요하면 의도적으로 따로 하면 된다.
   설치 전 네 파일(`.mcp.json`·`.claude/settings.json`·`.claude/settings.local.json`·
   `~/.codex/hooks.json`)은 세션 scratchpad에 백업했다(세션 종료 시 사라진다).

   **호스트 재시작이 필요하다**: `alwaysLoad: true`는 Claude Code 세션 재시작 시 반영되고
   (v2.1.121 이상에서만 동작), Codex는 재시작 + `/hooks`로 훅 리뷰·재신뢰가 필요하다
   (설치기가 그렇게 안내한다). 이 세션의 `ctr-exec`는 여전히 0.11.1 프로세스다.

   **doctor가 함께 보고한, 이 릴리스와 무관한 관측 3건**:
   `[14]` `file=216248320B > 임계 104857600B` — D67이 "상시 초과라 신호로서 죽어 있다"고 적은
   그 상태 그대로다. `[15]` shadow 귀속이 **hashes=353 · 12.8MB** — session-35 리셋(18해시)에서
   다시 쌓였다. 3일 보존 자동 purge가 이제 활성이고 나이 기준은 마지막 포착이다(G1).
   `[12]` `drops: worktree=399(unknown-session=399)` — 소스 355건 대비 적지 않은 수치다.
   원인은 확인하지 않았고 이 릴리스가 만든 것도 아니다. **v0.13 후보로 볼 값이 있다.**

9. **Next-session starting prompt**:
   > docs/prompts 최신 기록(session-36)을 읽고 재개. **v0.12.0 릴리스 완료** — main=`4c73244`
   > (`Merge pull request #25`), tag `v0.12.0`, **태그 CI success로 `tag-version`까지 통과해 D56이
   > 실검증됐다**. 머지 게이트는 코드리뷰 3라운드·fix 3 wave로 통과했고 마지막 재리뷰는 신규 0건
   > approved였다. **첫 작업 = 도그푸딩**: `hook install` 재실행(marker `0.11.1`→`0.12.0`, doctor
   > `[9]`·`[16]`·`[17]` 불일치 해소, `--enable-exec` 포함). **이번 세션이 그 경로를 크게 손댔으므로
   > 도그푸딩 값이 특히 높다** — 재설치가 승인 이름을 되돌리는지(R1), uninstall이 은퇴 이름도
   > 지우는지(R2), 은퇴 항목 프로필이 이월되는지(G9), 명시 `alwaysLoad:false`가 보존되는지(G11)를
   > 실사용으로 확인한다. 그다음 **릴리스 노트/문서 한 줄**: `PYTHONNOUSERSITE=1`(호스트
   > `pip install --user` 패키지가 python 러너에서 안 보임)과 `extra` fail-closed(격리 준비 실패가
   > 실행 거부)는 사용자 관측 가능한 동작 변경인데 출하 기록이 없다 — 저장소에 `CHANGELOG.md`도
   > GitHub Release도 없고 README에 러너 env 격리 언급이 0건이므로 **어디에 쓸지부터 결정**이
   > 필요하다. **v0.13 스코프 결정**은 5번·6번 Carryovers의 10항목 참조(shell 격리, 후행 종료코드
   > 전파, shadow 술어 색인 — 나이 기준 교정으로 우선순위 상승, ci.yml 액션 핀 8개+permissions,
   > purge 고루틴 취소, buildinfo `-dev` 프로세스, windows 파일 lint 13건, node 레그 커버리지,
   > 파일명 규약, 원장 minor 약 47건). 크로스모델 리뷰=중립 프레이밍+JSON findings
   > (`docs/codex-secure-review.md`, Codex 1패스/체크포인트·재리뷰는 서브 단독), 막히면 웹+Codex+서브
   > 협업, CI 3-OS가 unix 실검증 권위, `git add -A` 금지(로컬 설정 untracked), 머지는 사용자 명시
   > 승인으로만. ultrathink
