# Session 59 — 확인 셋이 336h를 잡아냈고, 두 시계가 갈렸다

## 1. 세션 정보

- **번호**: 59 (앞: session-58 = 측정 구간 개시)
- **기간**: 2026-08-12
- **모델/노력**: Opus 5 (1M context), max, ultrathink
- **결과**: **확인 셋 2번 실패.** 이 호스트가 `CTR_SHADOW_RETENTION=336h`를 못 받은 채
  떠 있었고, 그 서버는 기본 **72h**로 기동 퍼지를 돌렸다. 처방(설정 파일 `env`)을 넣었으나
  **발효는 호스트 재시작부터**다. (A) 회수 검증 네 경로 **전부 통과**, (B) D104 결함 **12건
  중 10건** 닫음.
- **성격**: 첫 일이 게이트였고 그 게이트가 실제로 걸렸다. 나머지는 그 판정 위에서 돌았다.

## 2. 시작 프롬프트 (원문)

```
docs/prompts/2026-08-11-session-58-measurement-kickoff.md 와
docs/superpowers/specs/2026-08-11-measurement-kickoff-design.md 를 읽고 이어서 작업한다.

(중략 — 지금 상태 · 확인 셋 셋 · 통과 시 푸시/PR · (A) 회수 검증 · (B) D104 결함 12건 ·
밟으면 안 되는 자리 · 배경. 전문은 session-58 기록 §7의 "다음 세션 시작 프롬프트"에 있고,
이 세션이 받은 것은 거기에 실행 세부를 덧댄 판이다.)

문제 발견되면 fix 혹은 브레인스토밍하여 사용자에게 질문하고, 문제 없으면 진행한다.
ultrathink
```

### 2.1 소유자 판정 셋 (`AskUserQuestion`, 한 번에)

| # | 물은 것 | 답 |
|---|---|---|
| 1 | 336h 전파 복구 방법 | **`settings.json`의 `env`에 한 줄** |
| 2 | 14일 시계 기준 | **계측 t0 유지 + 창 발효를 별도 기록** |
| 3 | 이번 세션 작업 | **(B)와 (A) 둘 다** |

## 3. 한 일

### 3.1 ★★★ 확인 셋 — 2번이 걸렸다

| # | 항목 | 결과 |
|---|---|---|
| 1 | 서버 기동 | ✅ PID 19032, 13:25:03 KST, 설치본 0.20.0, **1개뿐**(옛 서버 없음) |
| 2 | 이 호스트의 `336h` | ❌ **비어 있었다** |
| 3 | `legacy_after_migrate` | ✅ **0** — 회수 줄 여덟 칸이 t0와 글자 그대로 동일 |

**2번의 정체**: 레지스트리 User 스코프에는 `336h`가 **살아 있었다**
(`[Environment]::GetEnvironmentVariable(...,'User')` = `336h`). 못 받은 것은 **이 호스트
프로세스의 환경 블록**이다(`Process=[]`, `CTR_*` 하나도 없음). Windows에서 User 스코프 값은
**이미 떠 있는 프로세스에 전파되지 않으므로**, 값을 건 것보다 먼저 열려 있던 셸에서 시작한
호스트는 그 값 없이 돈다. MCP 서버는 매니페스트에 `env` 블록이 없어 호스트의 환경 블록을
그대로 상속하므로, **그 서버는 `store.DefaultShadowRetention` = 72h로 기동 퍼지를 돌렸다**
(`main.go`의 `store.ShadowRetention(os.Getenv)` → `store.go`).

★ **세션 58의 확인 셋 자체는 옳았다.** 이 실패를 잡으라고 있는 게이트였고 실제로 잡았다.
틀린 것은 "User 스코프에 걸었으니 다음 호스트가 받는다"는 전제였다.

### 3.2 손실을 재고 경계를 확정했다

**먼저 퍼지 경로를 확정했다** `[실측 — 호출부 grep]`: `PurgeHookOnlyOlderThan`을 부르는 것은
**`serve` 기동 시 1회뿐**이고(`startupPurgeMaxHashes` = 100, `startupPurgeBudget` = 60초),
**훅도 CLI(`stats`·`doctor`)도 퍼지를 부르지 않는다.** 그래서 이 세션이 도구를 쓰는 것으로는
추가 손실이 없다는 것이 먼저 확정됐다.

**프로젝트 스토어 blob 나이 분포** `[실측 — blob mtime, 13:31 UTC 기준]`:

| 포착일(UTC) | 07-21 | 08-08 | 08-09 | 08-10 | 08-11 | 08-12 | 계 |
|---|---|---|---|---|---|---|---|
| 개수 | 116 | 25 | 212 | 368 | 31 | 6 | **758** |

- **72h 초과 = 191개(10.6 MiB)** ← 기동 1회가 최대 100개를 삼킨다
- **336h 초과 = 116개(6 MiB)** ← 올바른 336h 퍼지가 삼킬 몫
- **차이 75개**가 "336h면 살고 72h면 죽는" 몫

**측정 구간 데이터 손실은 0이다** — 이미 지워진 몫은 전부 t0 이전 포착분이고, t0 이후
포착분(8/12의 6개)은 최대 세 시간 나이라 72h 컷오프 밖이다.

★★ **진짜 노출은 앞이다**: 8/10의 **368개가 8/13에 72h를 넘는다.** 336h가 발효되지 않은 채
그 뒤에 서버가 뜨면 기동마다 100개씩 사라지고, 그것이 "72시간이 짧은가"에 구조적으로 답할 수
없게 만드는 바로 그 손실이다.

**세션 끝 재측정**: blob **785개**(>72h **193개**) — **늘었다.** 이 세션 동안 추가 퍼지 손실은
없었다.

### 3.3 처방 — 실증한 뒤에 넣었다

`~/.claude/settings.json`의 `env` 블록이 자식 프로세스에 전파되는 것을 **먼저 실증했다**
`[실측 — 그 블록의 `CLAUDE_CODE_USE_POWERSHELL_TOOL=1`·`MAX_MCP_OUTPUT_TOKENS=16000`·
`BASH_MAX_OUTPUT_LENGTH=80000`이 PowerShell 자식에서 그대로 읽혔고, 그 블록에 없던
`CTR_SHADOW_RETENTION`만 비어 있었다]`. 거기에 `"CTR_SHADOW_RETENTION": "336h"` 한 줄을
넣었다(기존 env 7개·deny 273개 보존, JSON 유효성 확인).

**이 파일은 git 밖이다** — 커밋에 없고, 머신을 옮기면 따라가지 않는다.

★★ **재시작 없이 발효됐다** `[실측 — 처방 직후 PowerShell과 Bash 두 자식 셸이 모두 `336h`를
읽는다]`. Claude Code가 설정 변경을 런타임에 반영해 **호스트 환경 블록**에 넣으므로, 그 뒤
호스트가 스폰하는 훅과 **다음에 뜨는 MCP 서버**가 그 값을 받는다. 이 기록의 초안은 이 자리를
*"발효는 호스트 재시작부터"*로 적었고 **그 전제가 틀렸다** — 세션 58의 §2 ★("설치 뒤 첫 훅이
이미 이관했을 것")와 같은 부류의 오판이고, 같은 방법(실행하고 재기)으로 잡혔다.

★ **다만 현재 서버(PID 19032)는 여전히 72h 환경이다** — 세션 시작 시 스폰됐고 프로세스의 환경
블록은 나중에 바뀌지 않는다. **그래도 앞으로 영향이 없다**: 기동 퍼지는 프로세스당 1회이고 그
1회는 이미 돌았다(§3.2). 그 서버는 다시 퍼지하지 않는다.

★ **그래서 8/13 시한이 해소됐다.** 실효 창이 336h가 되는 첫 시점은 **다음 서버 기동**이고, 그
기동 전까지 포착된 것은 어느 창으로도 지워지지 않는다.

★ **Codex 호스트에는 이 처방이 닿지 않는다** — 세션 끝에 `context-router` 프로세스 7개가
떠 있었고 `[실측 — Win32_Process 부모 추적]` 그중 **6개의 부모가 `codex`(PID 6944)**,
1개가 이 호스트의 `claude`였다. 세션 58이 관측한 "단조 증가"가 그대로 재현됐다.

### 3.4 (A) 회수 검증 — 네 경로 전부 통과

**판정 대상이 아닌 `dl` 프로젝트**(`ProjectID=dl-b3dca29ae1e0`)에서 했다. 그 원장은 시작
시점에 `미이관`이었고 아티팩트 26개가 **전부 shadow 귀속**(`cx:` = Codex 훅 유래)이었다.

★ **퍼지를 건너뛰게 하고 이관만 시켰다**: `CTR_SHADOW_RETENTION=1000000h`를 주면
`ShadowCutoff`가 0 이하가 되어 기동 퍼지를 **건너뛴다**(설계서가 문서화한 경로,
`TestShadowCutoffBoundary`가 고정). 그러지 않았으면 336h 컷오프가 7/29 이전 포착분을 지워
①의 대상이 사라질 뻔했다. **이관 뒤 `hashes=26` 그대로.**

★★ **CLI에는 회수 경로가 없다** `[실측 — `recordFetch*`·`LedgerAppendFetch`가
`internal/mcp`·`internal/store`에만 있다]`. 그래서 **MCP stdio를 직접 몰았다.** 파이프로
요청을 한꺼번에 밀면 서버가 응답을 쓰기 전에 EOF를 본다(PowerShell·Bash 둘 다 stdout 0줄) —
이 저장소 E2E 스모크의 `stdioClient`처럼 **요청-응답 교대**가 필요하다. 표준 라이브러리만 쓰는
70줄짜리 클라이언트를 scratchpad에 만들어 썼다(세션 디렉터리라 자동 정리).

| # | 대상 | 기대 | 실측 |
|---|---|---|---|
| ① | shadow 귀속 (artifact 26) | `resolved`·`resolved_artifacts`·`shadow_rows`·`shadow_artifacts` 각 +1, `age_s` 숫자 | ✅ 넷 다 0→1, **`age_s` = 1,205,800초(≈14일)** |
| ② | 없는 id 999999 **+ chunk 선택자** | `missed` +1, `resolved` 불변 | ✅ `missed` 0→1, `resolved` 1 유지 |
| ③ | explicit (artifact 27, `source=inline:`) | `resolved`·`resolved_artifacts` +1, **`shadow_*` 불변** | ✅ `resolved` 1→2, **`shadow_*` 1 유지, `age_s` 불변** |
| ④ | 있는 artifact 26 + 없는 chunk 999999 | `missed`·`resolved` 둘 다 불변 | ✅ 둘 다 불변, **`calls`조차 안 늚** |

- **①이 프로덕션에서 D103 계측이 회수를 기록한 첫 사례다.** 그전까지는 테스트로만 검증됐다.
- **③이 가장 값지다**: D103 계약 2 ★★의 귀속 제한이 실제로 작동한다 — explicit 소스는 채택
  문턱에는 들고 창 모집단에는 안 든다. 넷을 몰아서 쟀으면 이 배제를 원리적으로 못 봤다.
- **④에서 `calls`도 안 늘었다**(예상 밖의 정밀도): 잘못된 chunk id는 원장에 행을 아예 안
  남긴다 — D103 계약 3이 `ReadRange` 이전에 끊는다.
- ③의 대상은 `ctr_index`로 새로 만들었다. 판정 대상이 아닌 원장이라 안전하다(스펙 ★의 금지는
  판정 원장 한정).

★★★ **판정 대상 원장은 한 행도 받지 않았다** `[실측 — 검증 전후 회수 줄이 t0와 글자 그대로
동일]`. 스펙 §4.2의 전제("원장은 프로젝트 단위로 갈리고 그 ID를 정하는 것은 cwd다")가 실증됐다.

### 3.5 (B) D104 결정표 — 12건 중 10건 (커밋 `8b3e269`)

**(a)의 범위가 스펙이 적은 것보다 넓었다**: 행 2의 옛 문면("채택의 문제")이 설계서 세 곳·
CHANGELOG 한 곳 말고 **코드 주석 다섯 곳**에도 남아 있었다(`store.go`·`store_test.go`·
`cli.go`·`cli_test.go`·`mcp_test.go`). 문서만 고치면 desync가 남으므로 함께 고쳤다 — 순수
주석이고(비주석 변경 줄 0으로 기계 검증) **게이트 다섯을 돌렸다.**

구조 결함 넷을 새 ★로 닫았다:

- **★T** — 회수 줄의 수는 전부 **원장 수명 누적**이다(`ts` 술어 없음 · 삭제 경로 없음).
  `age_s max`는 평생 최댓값이라 한 번 72h를 넘으면 **행 3이 구조적으로 소멸**하고, `missed`도
  지난 구간 몫이 행 4를 채운다. 두 축을 `ledger.db`의 `ts`로 **구간 한정 질의**해 읽도록 못
  박았다. ★C1의 "그 구간에"와 ★C2의 "그 사이"도 이 절차를 가리킨다(B6·T3이 한 문단으로 닫힘).
- **★T1** — 행 0b의 관측이 절대값(`≥1`)인데 **처방 안에는 이미 "늘어나는지로 본다"가 적혀
  있었다.** 표는 관측 칸으로 걷으므로 한 번 오염된 원장이 영구히 걸린다. **기준선 비교**로.
- **★T2** — `missed` 5~9 띠에서 행 2가 행 4를 삼킨다. ★C1과 같은 이월 처방. 과대 계수 논증도
  그 띠를 빠뜨리고 있었다(B5, 같은 자리에서 고침).
- **★C2 정밀도 3점** — "재기동이 있었다"는 "지워졌을 것이다"를 **함의하지 않는다**(상한·예산에
  밀린 몫 · 사용자 `purge` 경로 · "그 사이"의 구간 한정). §3.2의 191개가 그 직접 실측이다.

그 밖 B2·B4·B7·표기 셋. **열둘 전부 닫혔다.**

★ **"측정 출처 누락 3"은 나중에 따로 찾아 닫았다** — 스펙이 위치를 적지 않아 D104 절을 통째로
훑어 열거했다. 셋은 ① `알고 받는 대가`의 `170,790,912 B`(정본은 같은 문서 §"근거가 된 관측"의
`+VACUUM` 행) · ② 세는 방식 ★★의 `16 KiB` 기본 예산과 `164 KiB → 열 개` 산술 · ③ 세는 방식
★의 `16 KiB` 재진술이다. **D103의 같은 자리에는 이미 `[실측 — `ctr_fetch`의 기본 예산]`이
붙어 있었고 D104 두 곳에만 빠져 있었다.** 예산 값은 이 세션이 `tools/list`로 직접 받은
스키마로 확인했다(`max_return_bytes` 기본 16,384 · 최대 65,536).

### 3.6 ★★★ 다음 릴리스 1순위가 실측으로 기각됐다

채택 레버 브레인스토밍을 시작하자마자 전제가 무너졌다. 세션 58이 1순위로 올린 *"훅이
`updatedToolOutput`으로 큰 출력을 요약+참조로 대체한다"*의 근거는 **이슈
`anthropics/claude-code#32105`가 COMPLETED로 닫혔다**는 사실 하나였다.

**증거 셋이 반대를 가리켰다**:

1. hooks reference와 hooks-guide **두 문서 모두** 그 필드가 없다. `PostToolUse`의 결정 제어는
   top-level `decision: "block"`뿐이고, `hookSpecificOutput`에 문서화된 것은
   `additionalContext` · `permissionDecision` · `updatedInput`(PreToolUse 전용)이다.
2. 그 이슈는 제목부터 **요청**이다 — `allow updatedToolOutput for built-in tools`.
3. 잠기기 직전 코멘트가 *"`updatedToolOutput` in PostToolUse **remains the only viable path**"* ·
   *"**+1 for extending** `updatedToolOutput` to built-in tools"* — 아직 없다는 뜻이다.

**그리고 직접 쟀다** `[실측 — 프로젝트 스코프 PostToolUse 훅]`: 고정 마커를 `updatedToolOutput`
으로 출력하는 훅을 `Glob`에 걸고 그 도구를 불렀다. **훅은 발화했는데**(sentinel 파일에
타임스탬프) **결과는 원본 그대로** 왔다. 발화와 무시가 함께 관측되므로 "훅이 안 떴다"는 대안
설명이 배제된다. 프로브는 즉시 걷었다(`.claude/settings.local.json`, gitignore 안, sentinel이
그 뒤로 안 늘어난 것으로 확인).

★ **계약에 박았다** — v0.19 설계서 D100 결정문 뒤 ★. 이 자리는 두 번 뒤집혔으므로(초안이
"호스트 스펙상 불가능"으로 기각 → 세션 58이 문서·이슈로 "그것은 틀렸다"고 뒤집음 → 이 실측이
원래 기각을 복원), **세 번째로 뒤집으려면 문서나 이슈 상태가 아니라 같은 형태의 실측을 가져와야
한다**고 함께 적었다.

★ **부수 소득**: 같은 이슈 코멘트가 `PreToolUse` `updatedInput`의 화이트리스트를 실측해 뒀다 —
`file_path`(Read/Edit/Write) · `command`(Bash)만 적용되고 `limit` · `head_limit`은 **조용히
버려진다.** 스펙 §6이 적은 그 부분은 맞다.

### 3.7 F10 — 문서보다 심각했고, 워터마크로 닫았다

세션 57·58이 *"퍼지 뒤 id 재발급으로 `ctr_fetch`가 무관한 내용을 오류 없이 돌려주는 선재
결함"*으로 이월해 둔 것이다. **재현부터 했다** `[실측 — `internal/store` 임시 테스트]`:

    퍼지: sources=1 artifacts=1
    idA=1 idB=1  재사용=true
      line               → 통과, "SECOND-artifact-body-completely-unrelated\n"
      byte               → 통과, "SECON"
      chunk(옛 chunk id) → 통과, "SECOND-artifact-body-completely-unrelated\n"

★ **세 선택자 전부 뚫렸다 — 설계서가 적은 것보다 심각하다.** D104의 그 ★는 영향을 "distinct
계수 과소 계상"으로만 적었는데, 실제 결과는 **회수가 무관한 내용을 오류 없이 돌려주는 것**이다.
그리고 내 예상("chunk 선택자는 소속 검증에 걸린다")도 **틀렸다** — `chunks.id`가 함께 재발급되어
옛 chunk id가 새 artifact의 chunk를 정확히 가리킨다. 같은 날 (A) 검증의 ④가 NOT_FOUND를 낸 것은
chunk `999999`가 **아예 없어서**였지 소속 검증 때문이 아니었다.

★★ **그래도 고치는 자리는 한 곳이다.** `ReadRange`의 첫 조회가 `SELECT … FROM artifacts WHERE
id=?`이므로 **`artifacts.id` 재사용만 막으면 chunk 경로까지 닫힌다**(없는 id는 그 앞에서
NOT_FOUND가 된다). 계측의 두 distinct 계수도 같은 축이다.

**처방**(소유자 판정): 워터마크. `AUTOINCREMENT`가 `sqlite_sequence`로 하는 일을 `id_watermark`
한 행으로 직접 한다. `AUTOINCREMENT`를 붙이려면 `ALTER TABLE`이 안 돼 `artifacts`를 통째로
재생성해야 하는데 `sources`가 FK로, `chunks`가 FTS5 외부 콘텐츠와 트리거 셋으로 묶여 있다 —
사용자 스토어에 아티팩트 678개가 있는 상태에서 감당할 위험이 아니었다.

- 표 생성은 **버전 스위치 밖**이다(D73 색인과 같은 이유) — 이미 v1인 저장소가 첫 Open에서 닿는다.
- **마이그레이션 단계가 없다**: 워터마크가 없던 DB는 `max(id)`에서 이어 발급한다(lazy 초기화).
- **fail-open**: 표가 없으면 `max(id)+1`로 되돌아가고 저장은 계속된다 — 경고만 남는다. 그
  폴백을 실제로 관측했다(테스트가 표를 지우고 등록에 성공한다).
- 회귀 둘: `TestArtifactIDNotReusedAfterPurge`(재사용 금지 **+ 옛 참조 셋 다 NOT_FOUND**) ·
  `TestRegisterSurvivesMissingIDWatermark`(폴백).

**이번 측정 구간에는 영향이 없다** — t0의 distinct 계수가 전부 0이라 오염될 과거분이 없다.
이미 재사용된 id를 되돌리지는 않는다.

## 4. 저장소 현재 상태

- **`main`**: 세션 시작 시 `eda87ac`(v0.20.0). 이 세션의 작업은 **PR #44로 머지**했다 —
  브랜치 `docs/session-58-measurement-kickoff`, **커밋 열**(세션 58의 여섯 + `8b3e269` D104 ·
  `5fd0046` 핸드오프 · `73be9de` 전파 정정 · `7169e58` D100 기각). windows CI는 재실행에서
  **두 run 모두 pass**해 `internal/exec` flake 판정이 확정됐다.
  그 뒤 `main`에 직접 둘(핸드오프 상태 정정 · D104 "측정 출처 누락 3"), 그리고 **PR #45**로
  F10 수정(§3.7)까지 들어갔다 — **`main` = `639049c`**. 이 세션이 남긴 코드 변경은 F10 하나이고
  나머지는 전부 문서다.
- **설치본**: 0.20.0. `.old`는 0.19.1(롤백은 **편도**다).
- **`CTR_SHADOW_RETENTION`**: User 스코프 `336h` + `~/.claude/settings.json`의 `env`에 `336h`
  (**이 세션이 추가, git 밖**). **호스트 환경 블록에 발효됨** `[실측 — 자식 셸 둘이 읽는다]`.
- **계측 t0** = `2026-08-12T01:22:18Z` — **유효.** 회수 줄 여덟 칸이 t0와 동일.
- **336h 창 실효 시작** = **다음 서버 기동.** 현재 서버(PID 19032)는 72h 환경으로 떴으나 그
  기동 퍼지는 이미 돌았고 프로세스당 1회라 추가 손실 경로가 없다.
- **프로세스**: `context-router` 7개(claude 1 · **codex 6**). 내가 띄운 것이 아니라 죽이지
  않았다.
- **켜진 채 남긴 것**: 위 프로세스 6개(내 것 아님) · `settings.json` 변경(의도).
  백그라운드 작업 없음, 워킹트리 clean.

## 5. 이월 사항

### 5.1 다음 세션의 첫 일 — 확인 셋을 **그대로 다시**

특히 **2번**. `$env:CTR_SHADOW_RETENTION`이 그 세션 안에서 `336h`로 읽혀야 한다. 비어 있으면
`settings.json` 처방이 **지워진** 것이고 그 원인부터 봐야 한다 — 어댑터가 그 파일을 "seed, not
a source"라고 적는 이유가 이것이다(Claude Code는 설정 변경·플러그인 설치·모델 변경에 그 파일을
재작성한다).

**8/13 시한은 해소됐다**(§3.3): 8/10 포착분 368개가 그날 72h를 넘지만, 그때 뜨는 서버는 336h를
받는다. 확인 셋 2번은 그것이 실제로 그런지 보는 게이트로 남는다.

### 5.2 두 시계를 따로 적는다 (소유자 판정)

- **계측 t0** = `2026-08-12T01:22:18Z` — 채택 문턱이 읽는 칸의 기준선. 유지.
- **336h 창 발효 시각** — 재시작 뒤 확인 셋 2번을 통과한 시점을 여기 적는다. 나이 분포는 그
  시점 이후 구간으로만 해석한다.

### 5.3 D104 결정표 — 남은 것 없음

열둘 전부 닫혔다: 열은 `8b3e269`, "측정 출처 누락 3"은 그 뒤 별도 커밋(§3.5 ★).

### 5.4 Codex 호스트의 창

`settings.json` 처방은 Claude Code 전용이다. Codex 호스트가 이 프로젝트를 cwd로 서버를 띄우면서
336h를 못 받으면 그 기동이 72h 퍼지를 돈다. 이 세션에는 그 손실이 관측되지 않았지만(blob이
늘었다) **닫힌 구멍은 아니다.**

### 5.5 그 밖 (세션 58에서 이월, 그대로 열림)

- ~~다음 릴리스 후보 1순위: 훅이 `updatedToolOutput`으로 큰 출력을 요약+참조로 대체~~ —
  **실측으로 기각됐다(§3.6).** 다음 후보는 D100이 열거한 셋 안에서 찾는다.
- ~~F10 `AUTOINCREMENT` — 퍼지 뒤 id 재발급~~ — **닫혔다**(§3.7). 워터마크 발급으로 막았고,
  재현이 문서보다 심각했다(세 선택자 전부 뚫렸다). **이번 측정 구간에는 영향이 없다** — t0의
  distinct 계수가 전부 0이라 오염될 과거분이 없다.
- 세션 57의 F12·F13, 트라이그램 축, 워크트리 `v0.20-d103-fetch-instrumentation` 정리.

## 6. 상시 프로토콜 (이번 세션이 낸 것)

- **★★★ "설정했다"와 "그 프로세스가 받았다"는 다른 명제다.** User 스코프 환경 변수는 이미 떠
  있는 프로세스에 전파되지 않는다. 검증은 **그 값을 쓸 프로세스와 같은 환경 블록에서** 읽는
  것이고, 별도로 띄운 CLI가 찍는 값은 증거가 되지 않는다. 세션 58의 확인 셋 2번이 정확히 이
  형태로 쓰여 있었고, 그래서 잡혔다. **그리고 경로마다 전파 시점이 다르다**: OS의 User 스코프는
  새 부모를 요구했는데 호스트 설정 파일의 `env`는 재시작 없이 그다음 자식부터 반영됐다
  `[실측]`. 어느 쪽이든 **그 자리에서 읽어 보는 것** 말고 아는 방법이 없다 — 이 세션은 처방을
  넣은 뒤 "재시작해야 발효된다"고 적었다가 확인 한 번에 그 진술을 정정했다.
- **★★★ 이슈가 COMPLETED로 닫혔다는 것은 그 기능이 존재한다는 뜻이 아니다.** 세션 58이 그 한
  걸음으로 다음 릴리스 1순위를 세웠는데, 문서 두 곳과 **그 이슈의 코멘트 자체**가 모두 반대를
  가리켰고 15분짜리 실측이 확정했다. **기능의 존재는 그것을 써 보는 것으로만 확정된다** — 훅이면
  훅을 걸고 도구를 부르는 것이다. 그리고 그 실측에는 **"안 떴다"와 "떴는데 무시됐다"를 가르는
  sentinel이 반드시 함께 있어야 한다**; 없으면 음성 결과가 무효다.
- **★★ 처방을 넣기 전에 그 처방의 기제를 실증하라.** `settings.json`의 `env`가 자식에게
  전파되는지를 **그 블록의 기존 값 셋으로** 먼저 확인했다. 확인 비용은 한 번의 셸 호출이었다.
- **★★ 되돌릴 수 없는 손실을 다룰 때는 "이미 난 손실"과 "앞으로 날 손실"을 먼저 가르라.**
  전자는 t0 이전 재고라 측정 가치가 낮았고, 후자는 8/13부터 구간 데이터를 지운다. 급한 것은
  사후 측정이 아니라 사전 차단이었다.
- **★★ 검증을 몰아서 재면 배제 조건을 원리적으로 못 본다.** ③(explicit이 `shadow_*`에 안
  드는 것)은 ①과 따로 재야만 보인다. 스펙이 그렇게 적었고 그 지시가 옳았다.
- **★ 스펙이 적은 결함 범위가 실제 범위와 같다고 전제하지 마라.** (a)는 "문서 셋 + CHANGELOG
  둘"로 적혀 있었는데 코드 주석 다섯이 더 있었다. 같은 문자열을 저장소 전체로 grep하는 것이
  그 확인이다.
- **★ 파이프는 대화가 아니다.** stdio 서버에 요청을 한꺼번에 밀면 응답 전에 EOF를 본다.
  요청-응답 교대가 필요하고, 이 저장소에는 그 패턴이 E2E 스모크에 이미 있었다.
- **★ 퍼지를 피해야 할 때는 창을 크게 주는 것이 아니라 `cutoff <= 0`으로 건너뛰게 한다.**
  `1000000h`. 설계서가 그 경계를 이미 문서화하고 테스트가 고정한다.

## 7. 다음 세션 시작 프롬프트

```
docs/prompts/2026-08-12-session-59-retention-propagation-and-d104.md 를 읽고 이어서 작업한다.

## 지금 상태

- **계측 t0 = 2026-08-12T01:22:18Z 는 유효하다** — 회수 줄 여덟 칸이 t0와 동일.
- **336h가 호스트 환경에 발효돼 있다.** 세션 59가 ~/.claude/settings.json 의 env 에
  "CTR_SHADOW_RETENTION": "336h" 를 넣었고(git 밖), 재시작 없이 자식 프로세스에 반영됐다.
  실효 창이 336h가 되는 것은 **다음 서버 기동**부터다.
- 세션 58·59의 작업은 전부 main 에 들어갔다 — PR #44(문서), PR #45(F10 수정), 그리고 main
  직접 커밋 둘. main = 639049c, 브랜치 정리됨, 워킹트리 clean.

## 첫 일 — 확인 셋을 그대로 다시. 특히 2번.

1. Get-Process context-router | Select Id,StartTime  — 서버가 떴는가
2. echo "[$env:CTR_SHADOW_RETENTION]"  — **이 호스트 안에서** 336h 인가.
   비어 있으면 settings.json 처방이 **지워진** 것이다(Claude Code는 설정 변경·플러그인
   설치·모델 변경에 그 파일을 재작성한다). 그 원인부터 본다. 그대로 두면 그 세션에서
   뜬 서버의 기동 퍼지가 72h로 돈다.
3. context-router stats  — legacy_after_migrate 가 t0의 0보다 늘었는가

2번을 통과하면 **그 세션 서버의 기동 시각을 "336h 창 실효 시작"으로 기록한다** —
계측 t0와 별개 시계다. 그 기동의 퍼지가 336h 초과분(7/21 포착 116개)을 걷는데,
그것은 정책이 지우기로 한 몫이라 정상이다.

## 그다음

- PR #44·#45 둘 다 머지됐다. windows CI 는 #44 에서 한 번 빨갰는데 internal/exec flake였고
  재실행 두 run 모두 pass로 확정됐다(그 패키지에 그 변경의 파일이 0개, main 도 같은 워크플로에서
  이미 빨갰다). #45 는 windows 를 포함해 처음부터 전부 green이었다.
- 주 1회 회수 줄 스냅샷을 남긴다(stats 는 read-only라 비용이 없다).
- **채택 레버 1순위(updatedToolOutput)는 실측으로 기각됐다** — v0.19 설계서 D100 결정문 뒤
  ★를 먼저 읽어라. 다음 후보는 D100 이 열거한 셋 안에서 찾는다: (a) 가시성 (b) 훅의 좁은
  강제(쓸 수 있는 것은 거부와 PreToolUse updatedInput 화이트리스트뿐) (c) 수동 포착.
- D104 결정표 결함 열둘은 **전부 닫혔다** — 남은 항목 없음.
- F10 AUTOINCREMENT 는 닫혔다(워터마크 발급). 남은 코드 항목은 세션 57의 F12·F13 과
  트라이그램 축 유지 여부다.
- Codex 호스트의 창은 settings.json 처방이 닿지 않는다 — 열린 구멍이다.

## 밟으면 안 되는 자리

- **336h를 못 받은 채 서버를 띄우지 마라.** 확인 셋 2번이 그 게이트다.
- **CTR_SHADOW_RETENTION 에 14d 를 쓰지 마라** — ParseDuration에 d 가 없어 조용히 72h로
  떨어진다. 336h 만 유효하다.
- **검증 회수를 판정 대상 원장에서 하지 마라.** 세션 59는 dl 프로젝트에서 했고 판정 원장은
  한 행도 받지 않았다.
- **판정 대상 원장을 미리 정하지 마라** — 14일 뒤 문턱에 닿은 원장에서 판정한다.
- stats·doctor 는 퍼지를 돌리지 않는다(serve 기동만 돈다). 훅도 안 돈다.
- go test 에 -p 1 필수. 게이트 다섯을 && 로 잇지 마라 — internal/exec 가 이 머신의
  PowerShell 실행 정책 때문에 빨갛다(환경 조건이지 회귀가 아니다).
- 행 번호를 주석·문서에 적지 마라 — 심볼 이름을 적어라.
- **context-router 프로세스 수는 단조 증가한다** — 설치나 재기동 직전에 다시 세라.
  세션 59 끝에 7개였고 6개가 codex 유래였다.

문제 발견되면 fix 혹은 브레인스토밍하여 사용자에게 질문하고, 문제 없으면 진행한다.
ultrathink
```
