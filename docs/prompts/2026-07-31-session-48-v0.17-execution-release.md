# Session 48 — v0.17 실행 · 릴리스

## 1. 세션 정보

- **번호**: 48 (앞: session-47 = v0.17 설계·스펙·계획)
- **기간**: 2026-07-31 → 2026-08-01 (자정 넘김)
- **모델/노력**: Opus 5 (1M context), ultrathink
- **결과**: **v0.17.0 릴리스 완료** (PR #30 머지, 태그 `v0.17.0`)

## 2. 시작 프롬프트 (원문)

> 계획대로 실행하고 문제 발견되면 fix 혹은 브레인스토밍하여 사용자에게 질문하고 문제 없으면 최종 릴리즈까지 진행 ultrathink

중간 지시 둘:

> /code-review xhigh와 CODEX로 상세 검증을 하고 문제 발견되면 fix 혹은 브레인스토밍하여 사용자에게 질문하고 문제 없으면 CI 결과 확인하고 머지·태그까지 진행해줘. changeLog 날짜는 현재 날짜가 2026-08-01로 변경이 되었으니 참고하라. ultrathink

> PR 부터 생성하고 코드리뷰 시도한다. ultrathink

## 3. 한 일

### 3.1 계획 실행 (12태스크, subagent-driven-development)

`docs/superpowers/plans/2026-07-31-v0.17-d89-d91.md` 12태스크를 순서대로 실행.
태스크마다 **fresh 구현 서브 → 리뷰 서브(spec+quality) → fix 라운드 → 범위 한정 재리뷰**.

- 태스크별 리뷰 **12회**, fix 재리뷰 **8회**, 전체 브랜치 리뷰 **1회**
- fix 라운드 발생: T1·T4·T5·T6·T7(반입 1 + 2라운드)·T9·T11·T12
- 브랜치 `feat/v0.17-d89-d91`, 22커밋 → 최종 28커밋

### 3.2 계획 자체의 결함 (구현자들이 발견·정정)

**전 태스크에서 하나 이상씩** 나왔다. 종류:

- **존재하지 않는 테스트 이름의 `-run` 정규식 셋** — `TestCodexUninstall`(매치 0, 실제는
  `TestUninstallCodexConfigBlock`) · `TestCodexInstall`이 `TestInstallCodexConfigBlock`을 놓침 ·
  `TestDoctor`가 `TestRunDoctor_*` 9개를 놓침. 축자로 따랐으면 그만큼 회귀 감시가 조용히 빠졌다.
- **컴파일이 깨지는 코드 둘** — T5의 `bytes` 임포트 누락(계획이 "이미 있다"고 단정), T10의 지역변수
  `label`이 명명 반환값과 충돌.
- **코드에 대한 사실 주장 오류** — 매 태스크. 예: T11의 "`(0,0)`이 구간 첫 줄을 지운다"(도달 불가),
  T8의 "테이블 부재는 그대로 `anomalyNone`"(실제로는 `sp.anomaly` 전파).

### 3.3 ★ 릴리스 차단 둘 — CI 게이트가 계획 검증 절차에 없었다

1. **`golangci-lint` 적색 2건** (T6이 들인 코드, T7 구현자가 우연히 발견). 계획의 검증 절차가
   `go build`·`go vet`·`go test`만 돌리는데 CI는 lint를 게이트로 돌린다. 리뷰어들은 diff만 읽으므로
   **구조적으로 못 잡는 축**이었다.
2. **`gofumpt` 적색** (T12가 발견). `golangci-lint`는 포맷 린터가 꺼져 있어 `0 issues`와 **공존한다** —
   두 게이트가 서로를 대신하지 않는다. 원인은 D91 진단 필드가 복합 리터럴을 여러 줄로 늘린 것.

교훈: **CI가 게이트로 돌리는 것을 계획의 검증 절차가 빠뜨리면 그 축은 아무도 안 본다.**

### 3.4 ★★ `/code-review xhigh` — 정오류 여덟, 다섯이 한 뿌리

태스크별 리뷰 12 + fix 재리뷰 8 + 전체 브랜치 리뷰가 **전부 통과한 뒤** 실행. 워크플로 40 에이전트
(finder 6 → candidate 37 → verifier 31 → 32 verified: 20 kept / 12 refuted).

**뿌리**: D90이 `codexReadTable`에게 점 표기 `env.CTR_MANAGED`를 가르쳤으나 **판정 함수 넷**
(`codexMarkerValue`→`codexOwnership`·`codexConfigMarker`·라벨)은 눈이 먼 채 남았다. 동시에 이 릴리스가
(a) 그 상태를 스스로 풀던 `envMissing` 경로를 제거하고 (b) 그 상태에서 발화하는 **새 사용자 지시 문면 둘**을 더했다.

각 태스크는 자기 diff만 옳았다. **여러 태스크가 각각 옳게 더한 것이 합쳐져** 결함이 됐다.

확정 결함(검증기들이 **빌드한 바이너리로 재현**):

| 자리 | 증상 | main과 비교 |
|---|---|---|
| `cli.go` `[16]` | 사용자에게 **자기 정상 등록물을 지우라** 지시 | 이 릴리스 신설 |
| `uninstallCodexConfigBlock` | 우리 표식 박힌 등록물에 "우리 것이 아니니 직접 정리" | main은 무성 |
| `installCodexConfigBlock` | 표식을 **영영 기입 못 하면서** 매번 "기입 완료" | — |
| `doctor [20]` | 정상 현재 등록물에 `표식없음`, 어떤 명령으로도 안 지워짐 | — |
| 다음 릴리스 | `테이블=이상`으로 굳어 해소 불가 | — |

**이 상태는 엣지 케이스가 아니다** — Codex 자신이 재직렬화하며 `[mcp_servers.ctr.env]`를 점 표기로
접는다(D80 전제). 정상 설치 → 정상 사용으로 도달한다.

그 밖의 확정 셋:

- **BOM이 D89 게이트를 무력화** — go-toml이 선두 UTF-8 BOM을 거부해 `InputParses=false` → 게이트 조기 반환.
  Windows 편집기(PowerShell 5.1 `Out-File -Encoding utf8`) 산출물이 그 형태. **이 릴리스가 존재하는 이유인
  되돌림이 하필 그 파일에서만 꺼졌다.** 동시에 멀쩡한 파일에 `[16]` "파스되지 않습니다" 오보.
- **`[20]` 예고와 `--fix` 행동 불일치** — 절 조건은 `mcpDrift`인데 `--fix`는 더 넓은 조건에서 은퇴 항목을
  삭제 → **예고 없이 `.mcp.json` 항목 소실**. main은 무조건 예고했다(T9 회귀).
- **주석 처리된 값이 활성으로 계수** — `enabled_tools` 배열 안 `# "ctr_index",`가 수확돼 실제 부족이
  충족으로 읽힘(D91 무력화). 후행 주석이 **표식 값을 위조**해 D90 이탈을 억제하는 것도 같은 부류.

### 3.5 수정 셋 + 문서 정합

**사용자 결정**: 판독기를 통일한다(`codexMarkerValue`가 점 표기를 읽는다). 설계서 D90의
"표식 판독은 손대지 않았다"를 개정. 동작 변화: uninstall이 점 표기 등록물을 실제로 제거. **기입 형태는 불변.**

| 커밋 | 내용 |
|---|---|
| `365887c` | `[20]` 절이 `--fix`의 기입 조건을 쓴다 — 조건을 옮겨 적지 않고 **계산을 `mcpFixMerge`로 통합** |
| `852a00d` | 선두 BOM을 떼고 판정 — **실제 Codex CLI 0.146.0으로 대조**(BOM 파일을 Codex는 정상으로 읽는다) |
| `31dd9b3` | 판독기 통일 + 값 판독 주석 인식 + 설계서 D90 개정 |
| `9851950` | 수정 셋이 뒤집은 문서 주장 13곳을 코드에 맞춤 |

각각 범위 한정 재리뷰 통과. 결함 5(표식이 **현재 값이 아닌** 점 표기 파일)는 **의도된 PARTIAL** —
자동 해소 후보가 "점 표기 줄의 값만 제자리 갱신"뿐인데 그 산출물은 우리가 기입한 점 표기 표식이라
사용자 결정과 정면으로 걸린다. 파괴적 지시는 수행 가능한 안내로 바뀌었다.

### 3.6 Codex 교차 검증

`task-ms943j6d-1qpvg0` → 통합 서브가 검증: **confirmed 2 · plausible 1 · rejected 1, 전부 minor.**
Codex 주장 하나는 **설계서 §1.2가 이미 "알려진 잔여"로 기록하고 v0.18 이월을 결정한 항목**이었고
그 코드는 이 브랜치가 손대지도 않았다. 통합 서브가 걸러냈다.

## 4. 저장소 현재 상태

- **`main` HEAD**: `9851950` `docs(v0.17.0): 수정 셋이 뒤집은 문서 주장을 코드에 맞춘다`
- **`productVersion`**: `0.17.0` (`internal/buildinfo/buildinfo.go`)
- **태그**: `v0.17.0` (annotated) → `9851950`
- **열린 PR**: 없음. PR #30 MERGED (rebase — main의 선형 이력 관례 유지)
- **브랜치**: `feat/v0.17-d89-d91` 원격에 남아 있음(삭제하지 않음)
- **CI**: PR 런·push 런 **3-OS 전부 초록**(lint · crossbuild · test-node-leg · ubuntu/macos/windows).
  태그 런 `30664588409`·main 런 `30664531507`은 세션 종료 시점 진행 중 — **다음 세션이 확인할 것.**
- **다음 작업의 base**: `main` (`9851950` 또는 그 이후)

## 5. 이월 사항

### 5.1 ★ 최우선 — 미검증 후보 5건

`/code-review xhigh` 워크플로가 **주간 한도로 끊겨** verify 5건과 synthesis가 실행되지 않았다.
**이 리뷰는 완결이 아니다.** sweep 단계 후보 5건이 아무도 검증하지 않은 채 남아 있다. 그중 하나는
결과에 노출됨: `cli_test.go`의 D91 표가 `enabled_tools = []`와 **키 부재를 구별하지 않아**
`ToolsPresent: len(view.toolsLines) > 0`이 falsify 불가 — 빈 배열이 D91이 근거로 든 "도구 0개 allowlist"
바로 그 파일이다.

워크플로 산출: `C:\Users\js\.claude\projects\...\workflows\scripts\code-review-wf_0d1c352c-f3d.js`
(resumeFromRunId: `wf_0d1c352c-f3d`) — 한도가 풀리면 resume로 캐시 재사용 가능.

### 5.2 v0.18 (D92·D93) 대상

이미 설계서에 있던 것:

- **D92**: 판독기·판정의 한 패스 열거형 전면 통일. (이번에 표식 판독기 통일과 공유 값 판독 정규화는
  들어갔다 — 설계서 §1.2 범위 문장이 갱신됨.)
- **D93**: 이스케이프 표기 마디(`"env".X`류). 이번 릴리스는 다루지 않고 게이트가 막는다.

이번에 새로 발견한 잔여:

- **BOM이 우리 테이블 헤더 줄에 붙으면** 헤더로 인식되지 않아 `mcpConflict`로 막힌다(손실 없음,
  재등록이 막히는 막다른 갈래). `hook install --codex`가 만드는 파일의 첫 줄이 실제로 그 헤더라
  **도달 가능**하다. 닫는 방법은 정해져 있다 — `docs/context-router-design-v0.14-ko.md`가 exec 스니펫
  미니 파서에 이미 "선두 U+FEFF 먼저 제거"를 요구한다. 같은 규칙을 `stripLine`/`tomlLineScanner`에.
  **연쇄는 판독으로만 확인했고 실행하지 않았다 — 회귀 테스트가 필요하다.**
- **재직렬화된 점 표기 파일에서 표식이 옛 버전이면 자동 해소가 없다**(수동 이전 안내). **버전을 올릴
  때마다 도달한다.** "무변경 + 수동 이전"으로 굳힐지, D93이 기입 형태를 열지가 결정 사항.
- **`.mcp.json`에 백업 조항이 없다** — `config.toml`과 달리 `atomicWriteFile` 직접 덮어쓰기. 실행 전
  예고는 생겼으나 실행 후 복구 수단이 없다. v0.16 스펙 §1.2가 D84 개정을 요구한다.
- **`--fix`의 예고와 기입이 계산은 공유하지만 가드 술어는 여전히 두 자리에 미러링**(현재 동치).
- **`blockOwns`의 후행 주석 미인식** — 구 BEGIN/END 블록 헤더에 주석이 붙은 파일은 마이그레이션이
  닫힌다(fail-safe 방향).
- **`internal/hook`의 `TestShadowRefHashMatches`가 Windows에서 플레이크** — `sql: no rows in result set`.
  이 릴리스와 무관(그 패키지 미변경)하나 저장소 건강 이슈. 같은 커밋이 한 런에서 실패하고 다른 런에서 통과했다.

### 5.3 이월된 minor (전체 목록)

`.superpowers/sdd/2026-07-31-v0.17-d89-d91/deferred-minors.md`에 26건. 최종 브랜치 리뷰가 triage했고
"머지 전 수정" 셋은 반영됨. 나머지는 대부분 주석·커버리지 계열.

## 6. 상시 프로토콜 (이번 세션에서 확인·정정된 것)

- **Codex**: `review --base <ref>`는 이 호스트에서 **49분 블록**(ctr-exec 경유 pwsh 실패 반복). `task`는
  즉시 job id를 반환하지만 **로컬 파일 읽기 도구가 없다** — 브랜치를 **푸시한 뒤 GitHub ref로 읽게 해야** 한다.
  session-47 §6-6의 `--prompt-file`은 **v1.0.6에 없는 플래그**(프롬프트는 위치 인자, 명령줄 한도 ~32KB).
- **컴패니언 `cancel`은 Git Bash에서 실패** — MSYS가 `taskkill /PID`의 `/PID`를 경로로 변환. PowerShell로
  돌려야 하고, 그마저도 **작업 기록만 지우고 프로세스는 살려 둔다** → `taskkill /PID <pid> /T /F`로 마무리.
- **`gofumpt -w`가 성공을 보고하고도 쓰지 않은 적이 있다** — 항상 `-l`로 다시 재라.
- **`grep -c $'\r'`이 이 셸에서 거짓값을 낸다** — `git cat-file blob` + `od`로 재라.
- **PowerShell 도구가 출력 없이 exit 1을 내는 경우가 있다** — Bash로 대체.
- **커밋 메시지에 PowerShell here-string(`@'...'@`)을 Bash로 넘기면 `@`가 섞인다** — 세 구현자가 각자
  같은 사고를 냈다. Bash에서는 quoted heredoc.
- 리뷰 프로토콜·SDD 실행·서브 위생은 `CLAUDE.md` 참조.

## 7. 다음 세션 시작 프롬프트

```
docs/prompts/2026-07-31-session-48-v0.17-execution-release.md 를 읽고 이어서 작업한다.

v0.17.0은 릴리스 완료(main=9851950, 태그 v0.17.0, PR #30 머지). 먼저 확인할 것:
태그 CI 런 30664588409와 main CI 런 30664531507이 초록으로 끝났는지. 실패했으면
그것부터 처리한다.

그 다음 최우선은 **미검증 후보 5건**이다. v0.17의 /code-review xhigh 워크플로가
주간 한도로 끊겨 verify 5건과 synthesis가 실행되지 않았다 — 그 리뷰는 완결이
아니다. 워크플로 스크립트와 runId가 §5.1에 있으니 resume하거나, 안 되면 sweep
후보 5건을 새로 검증한다. 노출된 하나: cli_test.go의 D91 표가 enabled_tools = []와
키 부재를 구별하지 않아 ToolsPresent가 falsify 불가하다.

그 뒤 v0.18 설계(D92·D93 + §5.2의 새 잔여 여섯)로 넘어간다. 특히 결정이 필요한 것:
재직렬화된 점 표기 파일에서 표식이 옛 버전일 때 자동 해소를 열 것인가(D93이 기입
형태를 여는가), 아니면 "무변경 + 수동 이전 안내"로 굳힐 것인가. 버전을 올릴 때마다
도달하는 상태다.

작업 전 docs/context-router-design-v0.17-ko.md(§1.2 알려진 잔여, §3 표2)와
CLAUDE.md의 상시 프로토콜을 훑는다.
```
