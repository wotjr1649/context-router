# Session 08 — v0.1.0 real-host smoke GREEN, NOTICE oracle resolved, v0.2 scope brainstorm

- **Span:** 2026-07-20
- **Model:** Fable 5 (ultrathink). No subagents — direct execution (T1-scale ops +
  scope brainstorm; no code changes, so no implementer/reviewer loop needed).
  Codex usage: 1 light `codex exec` as MCP *host* for the smoke (not a review pass).
- **One line:** main CI GREEN ×2 confirmed, ctr.exe rebuilt at v0.1.0, 6-tool
  surface smoked GREEN over both real-host registration paths (stdio JSON-RPC +
  codex exec), NOTICE oracle copyright confirmed byte-identical upstream (no edit
  needed), v0.2 scope brainstorm produced 3 options with recommendation A.

## 1. Starting prompt (verbatim — original Korean, do not translate)

```
context-router 후속 작업을 진행한다. 먼저 docs/prompts/2026-07-19-session-07-v0.1-session-events-complete.md(최신 기록)를 정독한다. 상태: v0.1 머지 완료(main 972ad10, PR #9, 태그 v0.1.0) — main 머지 CI GREEN 먼저 확인.
1. 실호스트 스모크: ctr.exe를 v0.1.0으로 재빌드 후 .mcp.json/codex 등록 경로로 6-도구 표면 수동 스모크(세션 3종 round-trip + scope=events + doctor). NOTICE oracle 저작권자 확인(세션-02 이월)도 처리.
2. 여유가 되면 v0.2 스코프 브레인스토밍(설계 §10 로드맵) — 기록 §4.2의 v0.2 이월 벌크(Summarize fan-out 상한 등 10건)를 입력으로.
3. 세션 종료 시 docs/prompts에 session-08 기록 작성·push.
메모리 캡 테스트 규칙(-p 1) 준수. Fable 유지, 보안 서술 최소화(fable-security-prose-minimization) 준수. ultrathink
```

## 2. What was done

**CI:** main merge run (972ad10) + session-07 docs run (334442b) both GREEN.

**Rebuild:** `go build -o ctr.exe ./cmd/context-router` at HEAD 334442b
(= v0.1.0 code + docs commit). Version is a source const (`main.go:29`), no
ldflags. Stale pre-v0.0.2 server process (this session's own MCP spawn) killed,
exe swapped in place — killing it dropped the session's `mcp__ctr__*` tools
(host does not auto-reconnect; smoke pivoted to direct stdio + codex exec).

**Real-host smoke — path 1, stdio JSON-RPC against the `.mcp.json`-registered
exe (scripts in session scratchpad, newline-delimited MCP):**
- `tools/list`: serverInfo `context-router/0.1.0`, exactly the 6-tool surface.
- Session round-trip GREEN: `ctr_record_event` (decision, attrs) → id/ts back →
  `ctr_session_summary` (grouped decision + session_start, `untrusted:true`) →
  `ctr_export_events` (EventV1 camelCase wire: schemaVersion 1.0, producer
  context-router/0.1.0, privacyLabel internal, RFC3339 timestamp, session_start
  carries retention_sec/worktree_root attrs) → `ctr_search scope=events` FTS hit
  on the recorded summary.
- `ctr_transform`: top-level `emit()` convention → `"smoke-transform-ok:3"`.
  (First attempt returned `result:""` — script defined `def run()` and emitted
  nothing; correct behavior, convention documented in transform_test.go.)
- `ctr_fetch` error paths: schema validation (artifact_id is **integer**),
  `[INVALID_ARGUMENT] 선택자는 정확히 1개` (selector rule), `[NOT_FOUND]` with a
  byte selector on a nonexistent id. Happy path NOT exercised — content.db is
  empty (probe queries hit 0) because ingest tools are profile-gated off by
  design (`mcp.go:512,667`); deferred, see §4.
- `ctr_search scope=content`: surface OK (empty store, hits 0, untrusted).
- doctor before smoke: session 3 items "not initialized"; after smoke:
  `session.db quick_check=ok`, `session.lock shared 획득 가능`,
  `recover-pending 없음`. content.db quick_check=ok, fts5 가능.

**Real-host smoke — path 2, codex registration:** `~/.codex/config.toml`
`enabled_tools` updated 3→6 (matches doctor §9 snippet). `codex exec` called
`ctr_session_summary` through its own spawned ctr.exe: returned the decision
event recorded by path 1 — cross-process read-back doubles as a real-host
shared-lease check (multiple session_start rows from each smoke process,
grouped correctly).

**NOTICE oracle (session-02 carryover) — RESOLVED, no edit:** upstream
`mksglu/context-mode` LICENSE (main & master identical) reads exactly
"Elastic License 2.0 (ELv2) / Copyright 2026 Mert Koseoglu"; the ctxscribe fork
retains the same notice. NOTICE lines 8–17 already match. Closed by
verification.

**Smoke findings (new, minor):** doctor prints items as [3]→[5]→[4] (ledger
before fts5) — cosmetic ordering nit, queued for v0.2 piggyback.

## 3. Current repo state

- `main` @ 334442b (+ this record's commit). Tag v0.1.0 on 972ad10. No branches,
  no open PRs, working tree clean (ctr.exe/ctr-new.exe are untracked local
  smoke artifacts; `.mcp.json` unchanged; `~/.codex/config.toml` now lists 6
  enabled ctr tools).
- repo-root ctr.exe = **fresh v0.1.0** (rebuild no longer pending).
- This session's Claude Code host lost its live `ctr` MCP connection (stale
  server killed); next session's spawn picks up the new exe automatically.

## 4. Carryovers / next work

1. **v0.2 scope decision (user)** — see §5 options; recommendation A.
2. **fetch happy-path smoke** — needs content: run once with `--profile ingest`
   (ctr_index a file, search → fetch byte-exact). Natural first step of the
   v0.2 onboarding work; not worth a standalone session.
3. **v0.2 deferred bulk (10, unchanged from session-07 §4.2)** + new doctor
   ordering nit (§2). All non-blocking; fold into v0.2 tasks per §5.
4. Session-02 NOTICE item: closed (this session).

## 5. v0.2 scope brainstorm (autonomous — user decision pending)

Ran as a scope brainstorm (superpowers:brainstorming, compressed for an
unattended session: context + options + recommendation recorded here; the
interactive question/approval gates run next session with the user).

**Inputs triangulated:** vision §7 roadmap (canonical v0.2 = "훅·플러그인
패키징 — 패시브 인덱싱(Shadow Recall)·large-read guard 강제 채널 + exec 3종",
gate: provider-usage 무작위 A/B; thesis: MCP-only voluntary adoption caps at
3.82%, ADR-0008), v0.1 design §11 (spill journal, 훅·패시브 수집 + 자동 계측
이벤트 9종, repository{}, invalidates, payload field query via virtual
generated column, title dedup), v0.0.1 design §14 (exec 3종 상세 계약, Shadow
Recall 훅 설계, OTel·Codex usage 어댑터, 무작위 A/B 하네스, semantic 보강은
recall@k 기준선 후, go-trafilatura는 게이트 9 실패 시), session-07 §4.2 debt
×10, session-08 doctor nit. Key fact: ctr_index/ctr_fetch_and_index already
implemented behind `--profile ingest`/`net` — onboarding is registration/hook
work, not new tool code.

**Option A — "강제 채널" (vision-canonical, RECOMMENDED):** host hook/plugin
packaging (Claude Code + Codex), passive indexing (Shadow Recall), large-read
guard, auto-instrumented events (§11's 9종 land in the v0.1 session DB — the
natural consumer of what v0.1 built), usage adapter + A/B harness as the exit
gate. exec 3종 explicitly split out (own track later; isolation design is
heavy and orthogonal). Debt items ride along: session-adjacent ones (Summarize
fan-out cap, summary budget, EventV1 omitempty re-check, byte-exact golden)
fold into instrumentation tasks; standalone ones (filterShortTokenCandidates
dedup, bak ts-orphan cleanup, corruption fixtures, migrateBusyRetry tests,
float64 caveat, recover UX nit, doctor ordering) as an opening cleanup task.
Pros: directly attacks the 3.82% thesis; v0.1 session DB gets its real
producer; ingest/net onboarding falls out naturally. Cons: host hook-contract
dependency (external API risk); largest design surface.

**Option B — "exec 3종 먼저":** OS-isolation exec contract (§14/§8.3, D10).
Independent of host hooks, but per the vision thesis real savings come from
forced channels, and exec isolation (Job Object/landlock/sandbox-exec) is the
highest-difficulty item in the backlog. Defers the product's core bet.

**Option C — "소형 릴리스":** debt ×10 + title dedup + payload query + doctor
nit as v0.1.1. Lowest risk, no roadmap progress; debt can just ride Option A's
opening task instead.

**Open questions for the user (next session, one at a time):**
1. Scope: A as recommended? exec 3종 split confirmed out?
2. Hook packaging first target: Claude Code first, or Claude Code + Codex
   simultaneously?
3. Is the provider-usage 무작위 A/B harness in-scope as v0.2 exit gate, or
   measured manually first?
4. 자동 계측 이벤트 9종 — reconcile list against 설계 기준서 §26 event kinds.
5. spill journal (§6.4): include (passive capture raises event volume — the
   coupling argument) or defer to v0.3?

## 6. Standing protocols (delta only — rest as session-07 §5)

- **Real-host smoke pattern:** killing the registered exe's process drops this
  session's own MCP tools without auto-reconnect. Smoke via direct stdio
  JSON-RPC (newline-delimited, initialize → tools/list → tools/call) plus one
  light `codex exec` covers both registration paths without needing a host
  restart. Scripts live in the session scratchpad; ~15 lines of harness.
- **transform scripts are top-level + `emit()`** — `def`-only scripts silently
  return empty; check transform_test.go for conventions before smoke-scripting.
- Codex `exec` as MCP host is cheap (~17K tokens) and does not count against
  the one-review-pass-per-checkpoint guard (it is not a review).

## 7. Next-session starting prompt (paste-ready)

```
context-router v0.2 준비를 진행한다. 먼저 docs/prompts/2026-07-20-session-08-v0.1.0-smoke-v0.2-scope.md(최신 기록)를 정독한다. 상태: v0.1.0 실호스트 스모크 GREEN(stdio+codex 경로), NOTICE oracle 해소, ctr.exe fresh.
1. 기록 §5의 v0.2 스코프 브레인스토밍을 이어받아 superpowers:brainstorming으로 마무리한다 — §5 미결 질문 5개를 한 번에 하나씩 사용자에게 묻고(추천: 옵션 A "강제 채널", exec 3종 분리), 스코프 확정 후 설계 문서(D21~)를 작성한다.
2. 설계 승인 후 writing-plans로 구현 계획을 만든다(부채 10건+doctor 순서 nit의 편승 배치 포함). 필요 시 plan 체크포인트에서 Codex adversarial-review 1패스(in-diff focus).
3. 여유가 되면 --profile ingest로 fetch 정상 경로 스모크 1회(§4.2).
4. 세션 종료 시 docs/prompts에 session-09 기록 작성·push.
메모리 캡 테스트 규칙(-p 1) 준수. Fable 유지, 보안 서술 최소화(fable-security-prose-minimization) 준수. ultrathink
```
