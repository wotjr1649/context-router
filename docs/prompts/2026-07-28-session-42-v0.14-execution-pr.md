# Session 42 — v0.14(D77~D79) 구현 · 리뷰 · PR (2026-07-28~29)

1. **Session/span/model**: session-42, 2026-07-28 저녁 ~ 07-29 새벽. 컨트롤러는 Opus 5(1M,
   max effort). 서브에이전트 **19**(전부 opus, 마지막 한 토큰 수정만 sonnet): 구현 7 · 태스크
   리뷰 7 · scoped 재리뷰 3 · Codex 병합 1 · 최종 whole-branch 리뷰 1 · 최종 fix wave 1 ·
   fix wave 재리뷰 1. Codex 8패스(태스크 7 + 최종 1) 중 **6패스 회수 성공**.
   **구현·리뷰·PR까지 완료. 머지·태그는 미수행**(사용자 승인 대기).

2. **Starting prompt (verbatim)** — session-40 §7의 부팅 프롬프트가 그대로 전달됐다. 앞선
   슬래시 커맨드는 `/plugin`·`/skills`·`/model`·`/andrej-karpathy-skills:karpathy-guidelines`
   ("카파시 스킬 호출만 해줘").

   > `docs/prompts` 최신 기록(session-40)을 읽고 재개. **v0.14는 설계·계획까지 끝났고
   > 구현은 착수하지 않았다.** main의 산출 커밋은 **`5b3dc0b`**(설계서 + 계획)이고 그 위에
   > 핸드오프 문서 커밋이 얹혀 있다 — BASE는 `main`의 최신을 쓴다. 최신 태그는 `v0.13.0`,
   > `productVersion` 0.13.0, 열린 PR 없음 · **v0.14 작업용 브랜치 없음**(과거 완료
   > 작업의 미정리 브랜치가 로컬 9 · 원격 7개 남아 있지만 v0.14와 무관하다 — §4).
   > 작업 트리에는 추적하지 않는 로컬 설정(`.claude/`·`.codex/`)만 있다 — 다만 **그
   > 안의 `settings.json`·`hooks.json`은 이제 빈 `{}`**이고 훅·MCP 등록은 사용자(전역)
   > 스코프(`~/.claude`·`~/.codex`)로 옮겨졌다(§5).
   >
   > **선행 도구는 이미 설치돼 있다**(session-41) — `gofumpt v0.10.0` ·
   > `golangci-lint v2.12.2`, CI(`ci.yml`)가 쓰는 것과 같은 버전이고 `GOPATH/bin`
   > (`C:\Users\js\go\bin`, PATH 등록됨)에 있다. **다시 설치하지 마라.**
   >
   > **계획서는 session-41에 두 곳 수정됐다** — ① 커밋 트레일러를 실행 모델명
   > 플레이스홀더로 규칙화 ② Task 7에 호스트 등록물 self-heal 스텝 추가(Codex
   > `config.toml` 마커 선행 확인 포함). **설계 결정은 하나도 바뀌지 않았다.**
   >
   > **첫 작업 = v0.14 구현.** 계획
   > `docs/superpowers/plans/2026-07-28-v0.14-d77-d79.md`(7태스크)를 **superpowers
   > `subagent-driven-development`**로 돈다 — 태스크별 fresh 서브에이전트, 태스크
   > 리뷰(스펙+품질), fix → 재리뷰. BASE는 원장에 기록하고 `HEAD~1`을 쓰지 않는다.
   > **코드 변경이 생기므로 브랜치를 먼저 만든다**(`feat/v0.14-d77-d79`).
   >
   > **스펙은 5라운드, 계획은 1라운드 교차 검수를 받았고 findings가 전부 반영됐다** —
   > 설계 결정을 다시 열지 마라. 특히 되돌리면 안 되는 것 넷: ① `try/finally`(문 종료
   > 오류 뒤 나머지 스니펫을 건너뛴다) ② dot-source(`exit` 종료 코드 오염) ③ 등록 문을
   > 앞 줄에 두기(모든 오류의 줄 번호 +1) ④ `-SupportEvent` 없이 등록(`Get-Job |
   > Wait-Job` 미종료 + stdout 오염). 기각된 둘(`OpenContext` 취소 전파 · windows
   > dotnet 억제)도 §4의 실측을 뒤집기 전에는 다시 올리지 마라.
   >
   > **계획 Task 2 Step 5를 건너뛰지 마라** — 기존 `TestShellNativeExitAugmentation`
   > (`exec_test.go:372-520`)의 세 서브테스트는 D78이 닫는 D76 공백을 단정으로 굳혀
   > 놓아 **정상 구현에서 반드시 FAIL한다**. 그 FAIL을 보고 등록 문을 되돌리면 D78
   > 자체가 사라진다. 계획이 각 서브테스트의 새 계약을 표로 준다.
   >
   > **유도 FAIL 절차 넷은 그대로 밟아 출력을 태스크 보고에 남겨라**(Task 3 Step 2 ·
   > Task 4 Step 3 · Task 5 Step 3·4). "검사를 추가했다"와 "그 검사가 물린다"는 다르다 —
   > 이 저장소가 다섯 번 실증한 지배적 결함 부류다.
   >
   > **리뷰 체크포인트**: 프로젝트 규약대로 서브에이전트 + Codex(1패스/체크포인트,
   > 재리뷰는 서브 단독). Codex는 job id 없이 백그라운드 출력 파일에 결과를 남기므로
   > 컨트롤러가 그 경로를 서브에게 `SendMessage`로 넘긴다. `adversarial-review`는 쓰지
   > 말고 중립 `review` + 서브 프롬프트에 적대성 부여.
   >
   > 막히면 웹+Codex+서브 협업, `go test -p 1`, `git add -A` 금지, 머지·태그는 사용자
   > 명시 승인. ultrathink

   그 밖에 `AskUserQuestion` **5문**으로 계획 충돌과 통합 방식을 결정했다(§3).

3. **What was done**

   브랜치 `feat/v0.14-d77-d79`에 **커밋 16개**. main은 무변경(`1f33d31`).
   **PR #27** — https://github.com/wotjr1649/context-router/pull/27

   | 태스크 | 커밋 | 결과 |
   | --- | --- | --- |
   | 1 미니 파서 | `57e9476` | approved (minor 2) |
   | 2 `snippetContent` 교체 | `d453275`·`ed50d62` | approved (minor 4) |
   | 3 실행 계약 | `c249f5b`·`e9a6cf3` | fix 1회 → clean |
   | 4 부작용·폴백·한계 | `89d0b39` | approved (minor 2) |
   | 5 D77 게이트 | `b70eba0`·`3dea4d7`·`b55ae94` | fix 1회 → clean |
   | 6 D79 lint 축 | `3660226`·`39896f4` | fix 1회 → clean |
   | 7 릴리스 마감 | `a1e6e41` | approved (minor 3) |
   | 최종 fix wave | `a9a379a`·`d55a229`·`2e31aa7` | 13항목 전부 ADDRESSED |
   | 스테일 인용 정정 | `0d432b6` | — |

   **사람 판정이 필요했던 계획 충돌 넷 — 전부 "수정 적용"**. 넷 다 **계획이 축자로 지정한
   코드의 공백**이었고, 넷 다 저장소에 이미 선례가 있거나 설계 의도와 어긋나 있었다:

   | # | 무엇 | 왜 계획이 틀렸나 |
   | --- | --- | --- |
   | T3 F1 | 실행 헬퍼가 `cmd.Env`를 nil로 둠 | 제품은 닫힌 allowlist `sandbox.BaseEnv()`를 쓰는데 테스트가 호스트 환경 전체를 상속 → 5.1 레그가 **실행 세션 의존**, D65가 배제한 `PSModulePath`가 스니펫에 도달 |
   | T5 F1 | 게이트가 `FailedFiles`를 안 봄 | rename/unlink 실패로 20해시 중 일부만 회수돼도 통과 — **축소된 작업량을 재고 넉넉한 여유를 보고** |
   | T6 F1 | perf 축 1단계가 `go build -tags perf` | `go build`는 `_test.go`를 컴파일하지 않고 perf 태그 파일이 하나뿐 → **항상 통과**. 실증: `perf_test.go`를 깨뜨려도 exit 0 |
   | T6 F2 | `go list` 템플릿의 두 join이 구분자 없음 | 외부 테스트 파일이 생기면 조용히 붙는다(현재 무해) |

   **"검사가 물린다"를 실증한 유도 FAIL이 잡아낸 것** — 이번에도 계획이 준 픽스처에서:

   - Task 4: 한계 픽스처 둘이 **선두 `[`의 과잉 폴백**에 걸려 등록 문이 아예 안 붙었다.
     하나는 통과 불가능, 하나는 **거짓 초록**. 계획 검수 6라운드가 놓친 자리다 —
     미니 파서의 과잉 폴백 규칙과 한계 픽스처가 서로 다른 라운드에서 확정돼 교차 검증되지
     않았다.
   - Task 5: 나이 게이트를 단락시키면 게이트가 틀린 것을 재는 데 그치지 않고 **더 빨리**
     끝난다(0.04s vs 0.06s) — 경로 단정이 없었다면 종이 게이트가 **더 좋은 수치를 보이며**
     조용히 초록이었다.
   - 최종 wave: `paramx-identifier` 케이스가 선두 `$` 때문에 자기 목적에 대해 공허했다.
     접두어 구현으로 바꿔도 **옛 케이스는 PASS하고 새 케이스만 FAIL**한다 — 그 짝이 공허함의
     증명이다. 브랜치 전체에 "접두 vs 전체 토큰"을 구별하는 케이스가 하나도 없었다.

   **측정으로 정정된 설계·계획 서술 넷**:
   - D78이 닫는 공백 ②가 **"인수 없는 `exit`"보다 넓다** — `exec.go:189`의 조건이
     `exit_code == 0`이므로 **`exit 0`도 같은 자리**다. CHANGELOG·설계 §0·§2.2를 넓혔고
     `exit-zero` 감시선을 신설했다.
   - ConstrainedLanguage 모드가 설계 §3의 "미측정" 목록에서 빠졌다 — 핸들러 스크립트블록이
     FullLanguage에서 생성돼 언어 모드 강등 뒤에도 기록에 성공한다.
   - D77 재측정: 20해시 · **16.7ms** · 해시당 ~1.0ms · `ReclaimedB=370B` · `DeferredFiles=0` ·
     `FailedFiles=0` · 임계 1000ms 대비 **여유 약 50배**(대역 13.0~23.7ms). 계획 검수의
     44.1ms를 알려진 상단 관측으로 병기했다.
   - 계획 Task 2 Step 2가 "3 FAIL"을 기대했으나 실제로는 1 FAIL이었고(나머지 둘은 무회귀
     잠금이라 구현 전에도 성립), Step 4의 `TestSnippetPS1BOM`은 PASS가 아니라 FAIL이었다
     (D76 형태를 굳힌 단정 — Step 5와 같은 부류로 판정해 단정 하나 갱신).

   **Task 7 Step 5(호스트 등록물 self-heal)는 실행하지 않았다** — 사용자 결정으로 머지·태그
   후로 미뤘다. 계획이 "0.14.0 바이너리를 PATH에 올린 뒤에만 의미가 있다"고 적었는데 그
   설치 스텝이 계획에 없었고, 브랜치가 머지되지 않으면 전역 환경만 앞서간다.

4. **Repo state**

   - `main` = **`1f33d31`** (무변경). 최신 태그 여전히 **`v0.13.0`**.
   - 브랜치 `feat/v0.14-d77-d79` = **`0d432b6`**, origin에 푸시됨(upstream 설정).
     main 대비 커밋 16개. `productVersion`은 브랜치에서 **0.14.0**, main에서는 0.13.0.
   - **PR #27 열림** (base `main`). `main`이 브랜치 시작 이후 무이동이라 **fast-forward 가능**.
   - 작업 트리: 추적하지 않는 `.claude/`·`.codex/`만. **미추적이지 무시는 아니다.**
   - SDD workspace `.superpowers/sdd/2026-07-28-v0.14-d77-d79/` **보존**(Option 2 규약 —
     PR 피드백을 여기서 처리). 원장 `progress.md`에 모든 판정·이월이 있다.
   - **CI**: PR 생성 시점에 push·pull_request 두 잡이 시작됐다. **결과는 다음 세션이 확인한다**
     (§5의 첫 항목).

   **로컬에서 red인 테스트 9건 — 전부 `internal/exec`의 `powershell` 레그**:
   `TestRunShellPS51ProviderWriteReturns`(v0.14 이전부터) ·
   `TestD78SignalOnExit/powershell/`(explicit-exit·bare-exit·**exit-zero**·throw·normal, 5/5) ·
   `TestD78NoObservableSideEffects/powershell/`(3/3).
   원인: 제품의 닫힌 allowlist에 `PSExecutionPolicyPreference`가 없고 이 호스트의 5.1은
   레지스트리 정책이 unset(클라이언트 SKU 기본 Restricted)이라 `.ps1`을 로드하지 못한다.
   **`normal`·`exit-zero`가 함께 실패하는 것이 정책 차단 시그니처**이며, D78 부재였다면
   `normal`은 통과한다. `pwsh` 레그 실패 0, 나머지 13개 패키지 전부 통과.

5. **Carryovers**

   - **CI 결과 확인이 최우선이다.** PR #27의 첫 런이 정산할 것 셋:
     ① **5.1 레그 9건이 windows-latest에서 초록인지** — 이 브랜치의 5.1 갈래 권위는 전적으로
     그 런에 있다. ② **lint 잡 실소요** — `ci.yml:84`의 `timeout-minutes: 15` 아래에서
     golangci-lint가 명시 4회 + 액션 1회 돌고 `.golangci.yml:4`의 `timeout: 5m`은 **호출당**
     상한이라 산술 최악이 예산을 넘는다(실측은 축당 10~13s라 실제 위험은 낮다).
     ③ **세 새 축의 실제 exit 0** — 로컬 검증은 Windows 호스트에서 했다.
   - **머지·태그 미수행** — 사용자 명시 승인 대기. 머지 후 순서: 태그 `v0.14.0` →
     `go install ./cmd/context-router` → **Task 7 Step 5(호스트 등록물 self-heal)**.
     그 스텝의 **선행 확인을 건너뛰면 사용자 설정이 소실된다**: Codex `config.toml`의
     `# BEGIN/END context-router` 마커 쌍 **사이에 `[mcp_servers.ctr]` 외 테이블이 있는지**
     먼저 본다(경로 해석은 `codexConfigPath`와 같은 규칙 — `CODEX_HOME` 우선).
   - **v0.15+ 후보(이번 세션이 추가)**:
     · **설계 문서가 `exec.go`를 줄 번호로 가리키는 관례 자체가 드리프트 재발원이다.**
       이번 브랜치에서만 두 번 밀렸다(주석 한 줄 추가로 `:187`→`:189` 등). 심볼 이름 인용으로
       바꾸면 이 부류가 사라진다 — `exec_test.go`의 M1 주석이 이미 그 방식을 쓴다.
     · darwin/netbsd 축의 1단계가 `_test.go`를 컴파일하지 않는다(golangci-lint는 린트한다).
       설계 §4에 의도적 이월로 기록 — 대체 수단이 전부 신호를 흐리거나 스텝을 부풀린다.
     · `TestD78Limits`의 `fallback-throw-no-signal` 다리는 `exitCode != 1`로 파싱 실패를
       가르지 못한다(PowerShell이 throw와 ParserError 모두 1). 본문이 `throw` 전에 표지를
       stdout에 내고 그것을 단정하면 닫힌다.
     · here-string 폴백 tail의 런타임 커버리지 상실(바이트 골든이 기전은 잠근다).
     · `exec_test.go:451`·`:461`의 `t.Fatalf` 문면에 남은 "tail" 표현(코드 0줄 계약 때문에 유예).
   - **설계 §4의 기존 이월분은 그대로** — 기각한 둘(`OpenContext` 취소 전파 · windows dotnet
     억제)은 §3 표3의 실측을 뒤집기 전에 다시 올리지 말 것 · 기각한 승격 수단 넷 ·
     `[Environment]::Exit` 경로 · 폴백 경로 승격 · 첫 줄 오류 열 번호 오염 · 조작 경로 방어 ·
     `stderr == ""` 보강 조건 · `worker_unix.go` GOOS 이식성 · 100해시 절대 상한 ·
     `startupPurgeMaxHashes` 재조정 · **Codex `config.toml` 마커 방식 교체** · v0.13 §4 이월분.
   - **ctxscribe → context-router 전환 로드맵**(session-40 조사, 변동 없음): 막는 것 여섯 중
     **B2 압축 복원**이 완전 종료의 게이트이고 ctr에 대응 코드가 한 줄도 없다.
     **B1 ingest·net 프로필 미등록**은 여전히 유효 — 전역 스코프 등록도 `--enable exec`만 준다.

6. **Standing protocols** (유지 + 이번 세션 실증)

   - **계획이 축자로 지정한 코드에도 공백이 있다** — 이번 세션의 plan_conflict 넷이 전부 그
     부류였고, 넷 다 저장소에 이미 선례가 있거나(같은 파일의 기존 관용구) 제품 코드와
     어긋나 있었다. **계획을 따르되 그것이 무엇을 검증하는지 매번 확인하라.**
   - **크로스모델이 서브 리뷰어가 못 보는 것을 본다** — Codex가 Task 5의 `FailedFiles`
     공백과 Task 6의 perf 축 공허함을 냈다. 둘 다 **diff 밖의 코드와 대조해야** 보이는
     것이었고, diff만 보는 리뷰로는 구조적으로 안 잡힌다.
   - **Codex 회수 타이밍** — diff가 커지면 Codex가 리뷰 서브보다 늦게 끝난다(11KB는 먼저,
     25KB는 나중). 리뷰 서브를 지연시키면 체크포인트가 직렬화되므로, **미완이면
     `codex_available=false`로 확정하고 컨트롤러가 뒤늦은 결과를 병합 서브로 소비**한다.
     최종 리뷰 패스는 세 번 읽어도 노이즈만 있었다(회수 실패 2/8).
   - **편집 도구 이스케이프 오염이 양방향으로 실재한다** — 이번 세션에서 **네 번**.
     `\uFEFF` 여섯 글자가 실제 U+FEFF 바이트로, 그리고 한국어 주석이 리터럴 `\uXXXX`
     텍스트로. **한 번은 컴파일도 테스트도 통과하는 무증상 오염이었고 바이트 검사만이
     잡았다.** 모든 dispatch에 바이트 검사를 요구하고 그 출력을 보고에 남기게 하라.
     autocrlf 호스트에서는 **작업 트리가 아니라 커밋 blob**이 정확한 판정 지점이다.
   - **테스트가 제품 환경을 재현하지 않으면 결과가 세션 의존이 된다** — 실행 헬퍼가
     `cmd.Env`를 nil로 두면 자식이 호스트 환경 전체를 상속하고, 결과가 "테스트를 띄운 세션이
     무엇을 상속했는가"의 함수가 된다. 닫으면 로컬이 red가 될 수 있고 **그것이 정직한 상태다**.
   - **전제를 검증 없이 dispatch에 넣지 마라** — 컨트롤러가 "이 호스트 5.1은 `.ps1`을 못
     돌린다"를 조건으로 넘겼는데 절반만 맞았다(환경 변수 상속 여부에 달려 있었다). 구현자가
     실측으로 반박하고 제안된 Skip 가드가 통과하던 레그를 4 SKIP으로 만든다는 것까지 보였다.
   - 유지: `go test -p 1` / 메모리 캡 / `gofumpt -l .` 무출력 / **`git add -A` 금지** /
     BASE를 원장에 기록 / 스펙 승 / 보안 서술 최소화 + 중립 프레이밍 + JSON findings
     (`docs/codex-secure-review.md`) / 리뷰 체크포인트 = 서브 + Codex 1패스, 재리뷰는 서브
     단독 / CI 3-OS가 unix·5.1 실검증 권위 / **머지·태그는 사용자 명시 승인** /
     `docs/superpowers/plans/`는 immutable, 정정은 설계서에.

7. **Next-session starting prompt** (이 프롬프트로 부팅한다)

   > `docs/prompts` 최신 기록(session-42)을 읽고 재개. **v0.14는 구현·리뷰·PR까지 끝났고
   > 머지·태그는 하지 않았다.** 브랜치 `feat/v0.14-d77-d79`(= `0d432b6`, origin 푸시됨)가
   > **PR #27**로 열려 있고 base는 `main`(= `1f33d31`, 무변경)이다. `main`이 브랜치 시작
   > 이후 움직이지 않아 **fast-forward가 가능**하다. 최신 태그는 여전히 `v0.13.0`이고
   > `productVersion`은 브랜치에서만 0.14.0이다. 작업 트리에는 추적하지 않는
   > `.claude/`·`.codex/`만 있다.
   >
   > **첫 작업 = PR #27의 CI 결과 확인.** `gh run list --branch feat/v0.14-d77-d79`로
   > 시작한다. 정산할 것 셋이고, 이 브랜치를 로컬 머지가 아니라 PR로 연 이유가 그것이다:
   > ① **`internal/exec`의 `powershell` 레그 9건이 windows-latest에서 초록인지** — 개발기는
   > 정책상 5.1로 `.ps1`을 로드하지 못해 그 9건이 red이고, **5.1 갈래의 권위는 전적으로 이
   > CI 런에 있다.** ② **lint 잡 실소요** — `timeout-minutes: 15` 아래에서 golangci-lint가
   > 명시 4회 + 액션 1회 돌고 `.golangci.yml`의 `timeout: 5m`은 호출당 상한이라 산술 최악이
   > 예산을 넘는다(실측 축당 10~13s). ③ **세 새 lint 축의 실제 exit 0**(로컬 검증은 Windows
   > 호스트에서 했다).
   >
   > **CI가 초록이면** 머지 방식을 사용자에게 확인하고, 승인 후 태그 `v0.14.0` →
   > `go install ./cmd/context-router` → **Task 7 Step 5(호스트 등록물 self-heal)** 순으로
   > 진행한다. 그 스텝의 **선행 확인을 절대 건너뛰지 마라** — Codex `config.toml`의
   > `# BEGIN/END context-router` 마커 쌍 사이에 `[mcp_servers.ctr]` 외 테이블이 있으면
   > `hook install --codex`가 그것을 통째로 지운다(2026-07-28에 24개 테이블이 그 안에 들어간
   > 것을 실측했다). 경로는 `codexConfigPath`와 같은 규칙으로 푼다(`CODEX_HOME` 우선).
   >
   > **CI가 빨간불이면** 원인을 §5의 셋과 대조해 분류한다. 5.1 레그 실패라면 그것이 이
   > 브랜치가 답을 기다리던 질문이므로 결과에 따라 재설계가 필요할 수 있다 — 임의로
   > 테스트를 약화시키거나 Skip을 넣지 마라(사람이 그 대가를 명시적으로 받아들였다).
   >
   > **설계 결정을 다시 열지 마라.** 스펙이 5라운드, 계획이 1라운드 검수를 받았고 구현 중
   > 사람 판정 넷이 추가됐다. 되돌리면 안 되는 넷: `try/finally` · dot-source · 등록 문을
   > 앞 줄에 두기 · `-SupportEvent` 없이 등록. 기각된 둘(`OpenContext` 취소 전파 · windows
   > dotnet 억제)도 설계 §3 표3의 실측을 뒤집기 전에는 올리지 마라.
   >
   > **이 저장소의 지배적 결함은 "공허하게 통과하는 검사"다** — 이번 브랜치에서만 네 건을
   > 잡았고(한계 픽스처 둘의 거짓 초록 · perf 축 빌드 확인 · D77 게이트의 부분 회수 은폐 ·
   > `paramx` 케이스), **넷 다 계획이 축자로 지정한 코드에 있었다.** 감시선을 손볼 때마다
   > 회귀를 유도해 FAIL을 캡처하고 원복해 PASS를 캡처하라.
   >
   > **편집 도구 이스케이프 오염이 양방향으로 실재한다**(이번 세션 네 번, 한 번은 컴파일·
   > 테스트를 모두 통과하는 무증상). 파일을 쓴 뒤 **커밋 blob 기준으로** BOM·NUL·CR·의도치
   > 않은 `\u` 텍스트를 확인하라.
   >
   > SDD workspace `.superpowers/sdd/2026-07-28-v0.14-d77-d79/`가 보존돼 있다 — 원장
   > `progress.md`에 모든 판정·parked 항목·이월이 있다. 머지가 끝나면 지운다.
   >
   > 막히면 웹+Codex+서브 협업, `go test -p 1`, `git add -A` 금지, 머지·태그는 사용자
   > 명시 승인. ultrathink
