# Session 07 — v0.1 (session events) tasks 3–11 complete, PR #9 merged, v0.1.0 tagged

- **Span:** 2026-07-19 ~ 2026-07-20
- **Model:** Fable 5 (ultrathink). Execution: superpowers:subagent-driven-development
  (fresh implementer per task, sonnet; task reviewers sonnet/opus by risk; final
  whole-branch reviewer + re-verification fable; final fix wave opus). Codex
  `review --base main` ×1 (final checkpoint, merged with fable review).
- **One line:** v0.1 tasks 3–11 executed (9 split into 9a/9b), one task-level
  Critical (T9b recover publish-crash resume) fixed in-loop, final dual review
  merged 15 must-fix items (fable C1+I6 / Codex P1×5+P2×7) into a single 6-commit
  fix wave — PR #9 merged, main GREEN, **v0.1.0 tagged**.

## 1. Starting prompt (verbatim — original Korean, do not translate)

```
context-router v0.1(session events) 실행을 계속한다. 먼저 docs/prompts/2026-07-19-session-06-v0.0.2-merge-v0.1-start.md(최신 기록)를 정독한다. 상태: v0.0.2 머지 완료(main 27c8ed8, CI GREEN), feat/v0.1-session-events @ 41f4914(태스크 1~2/11 완료, push·브랜치 CI GREEN 확인됨).
1. superpowers:subagent-driven-development로 docs/superpowers/plans/2026-07-19-context-router-v0.1-session-events.md의 태스크 3부터 이어서 실행한다(태스크당 fresh 서브에이전트 + 태스크 리뷰, BASE는 원장 기준).
2. T2 이관 계약 주의(기록 §4.3): Event Redaction 필드·redaction 컬럼 기록은 T3 소관, ErrLeaseHeld가 init/migrate 유계 소진도 wrap — toToolError→STORAGE_UNAVAILABLE은 sentinel 3종 전부.
3. 통합·최종 체크포인트는 서브에이전트 + Codex review --base 병렬 1패스(§5 wedge 대응 유지, 재리뷰는 서브에이전트만). 게이트 G1~G9는 각 태스크 테스트가 실체.
4. 세션 종료 시 docs/prompts에 session-07 기록 작성·push.
메모리 캡 테스트 규칙(-p 1) 준수. 임시 등록(.mcp.json, ~/.codex/config.toml)·ctr.exe 유지(pre-v0.0.2 stale — 실호스트 스모크 전 재빌드 필수). Fable 유지, 보안 서술 최소화(fable-security-prose-minimization) 준수. ultrathink
```

## 2. What was done

**Tasks 3–10 (each: fresh implementer + task review; fix rounds where noted):**
- T3 (74c6468..8e2fba2): ctr_record_event — caps ×6, redaction wiring (Event.Redaction
  carried contract), toToolError 3-sentinel→STORAGE_UNAVAILABLE, canonical artifact URI.
  Attributes pivoted to map[string]any (go-sdk jsonschema []byte misinference, measured).
- T4 (01c7e93): ctr_session_summary — checkpoint 3 rules, missing hint (D15),
  ArtifactHashExists placed in search (only legal owner per §8), mcp_session.go split.
- T5 (66a4caf): ctr_export_events + EventV1 (13-field camelCase wire, 1 mapping point),
  RowID `json:"-"` internal cursor, golden G6.
- T6 (ad37943): event FTS porter+trigram (summary only), ad-trigger homomorphic to
  content.db, search.QueryEvents (reader arg, superseded via surviving-rows EXISTS).
- T7 (90f0513..13677fa, 1 fix round): ctr_search scope(content|events|all), fail-closed
  events without session, fetch phrasing; **gate 11 re-measured on the 6-tool surface
  10024B/12029B** (controller reversed the 3-tool-only interpretation).
- T8 (2cb2796): retention sweep (per-session correlated cutoff, unexpressed sessions
  untouchable — M-4), purge --sessions whitelist(3 files), --retention-events helpers
  (call-site deferred to T10 by design).
- T9 split 9a/9b (density rule): 9a (98fa792) session export JSONL(EventV1 reuse,
  OpenReadOnly — no session.Open pollution), worktree contract, doctor 3 items.
  9b (c4c9ca5..68057af, **1 Critical fix round**): recover 7-step (marker, rowid-preserving
  salvage, publish). Critical: crash between publish renames wedged recovery and destroyed
  the healthy tmp → resume branch (resumePublishOnly / restoreLatestBackup) + step-injection
  regression tests.
- T10 (8710656): main wiring (openSessionDB single-point, sentinel→nil+stderr), retention
  sweep call-site, E2E ×4 (incl. real 2-process shared-lease losslessness), docs/gates-v0.1-ko.md.

**T11 + final checkpoint:**
- Docs/NOTICE/version (e054acb, 05aab61): architecture doc 4 revisions, NOTICE uuid
  (pre-existing omission drift fixed, byte-compare match), version 0.1.0.
- **Final dual review** (base 27c8ed8): fable Critical 1 (recover CLI guard blocked the
  §6.3 ⑦ resume path — T9b fix was CLI-unreachable dead code) + Important 6; Codex P1×5
  (incl. quick_check indeterminate-as-healthy, purge unlink without lease) + P2×7.
  Convergences: CLI resume block, restore partial-family, version unification.
  Controller adjudications: full-purge-includes-sessions kept as designed (notice text
  only); Summarize type fan-out deferred to v0.2; supersedes error code follows design
  §3.1 INVALID_ARGUMENT (plan text corrected — conflict rule applied).
- **Fix wave** (opus, 6 commits b92f61b..0832562): recover resume completeness (CLI
  delegation via HasRecoverArtifacts, tmp-first adjudication, restore reverse order
  "-wal","-shm","", strict quick_check for recover paths only), purge lease guard,
  error contracts (supersedes INVALID_ARGUMENT, ClassifyStorageErr → runtime
  STORAGE_UNAVAILABLE for summary/export/search-events/append, events-scope budget,
  export serialized-size budget, record 8KB +119B×refs), version/hostSnippet/
  Options.WorktreeRoot(§2.2 fix)/sub-second retention rejection, gofumpt (the only CI
  failure all session) + doc piggybacks.
- **Re-verification** (fable, subagent-only): 15/15 checklist resolved, crash-state
  table (marker/db/tmp/bak 8 rows) independently re-verified, Ready to merge Yes.
  4 of 5 new minors piggybacked (tmp single-file invariant, tmp-check-first ordering,
  Append classify, plan text fix).
- **PR #9 MERGED** → main 972ad10, all checks GREEN ×2 runs. **Tag v0.1.0 pushed.**

## 3. Current repo state

- `main` @ **972ad10** (v0.1 merged; PR #9; tag **v0.1.0**) + this record's commit.
  Merge-triggered main CI was in flight at record time — verify GREEN next session.
- Branch feat/v0.1-session-events deleted (remote+local). No open PRs.
- Smoke registrations intact: `.mcp.json`, `~/.codex/config.toml`, repo-root `ctr.exe`
  (**stale: pre-v0.0.2 — rebuild mandatory before any real-host smoke**).
- Codex passes this session: `review --base main` ×1 (final checkpoint; spent).

## 4. Carryovers / next work

1. **Real-host smoke** (from session-02, still pending): rebuild ctr.exe from v0.1.0,
   then manual smoke of the 6-tool surface in a real host (.mcp.json / codex config).
   NOTICE oracle upstream copyright-holder confirmation (session-02 item) still open.
2. **v0.2 deferred bulk** (ledgered, all triaged non-blocking): Summarize type fan-out
   cap (Codex P2-4), summary budget 정밀화(len(summary)-only 관례), byte-exact golden,
   EventV1 omitempty vs §26 consumer re-check, filterShortTokenCandidates 파라미터화
   (~45-line dup), bak family ts-orphan cleanup, modernc query-plan-dependent corruption
   fixtures, migrateBusyRetry branch unit tests, attributes float64 캐비앗 문구,
   recover worktree-absent error-message UX nit.
3. v0.2 scope planning (design doc §10 roadmap next milestone).

## 5. Standing protocols (delta only — rest as session-06 §5)

- **T9b→final-review chain lesson**: a task-level fix can be correct yet CLI-unreachable —
  integration-level resume paths need a CLI-through test, not only package tests. The
  final whole-branch review caught what both T9a and T9b task reviews (each correct in
  scope) structurally could not.
- **Merged-verdict fix wave works**: one opus fixer with a pre-adjudicated 15-item list
  (each with reviewer-specified fix direction) landed 6 clean commits in one round;
  re-verification found zero new Critical/Important.
- **Cross-model value #5**: Codex P1 quick_check-indeterminate and purge-unlink-lease
  were invisible to all Claude reviews; conversely fable's CLI-guard Critical was not
  in Codex's list. Keep both lenses at the final gate.
- Dense-task split (9a/9b) again avoided 32K deaths; recover (9b) alone consumed a full
  subagent context.

## 6. Next-session starting prompt (paste-ready)

```
context-router 후속 작업을 진행한다. 먼저 docs/prompts/2026-07-19-session-07-v0.1-session-events-complete.md(최신 기록)를 정독한다. 상태: v0.1 머지 완료(main 972ad10, PR #9, 태그 v0.1.0) — main 머지 CI GREEN 먼저 확인.
1. 실호스트 스모크: ctr.exe를 v0.1.0으로 재빌드 후 .mcp.json/codex 등록 경로로 6-도구 표면 수동 스모크(세션 3종 round-trip + scope=events + doctor). NOTICE oracle 저작권자 확인(세션-02 이월)도 처리.
2. 여유가 되면 v0.2 스코프 브레인스토밍(설계 §10 로드맵) — 기록 §4.2의 v0.2 이월 벌크(Summarize fan-out 상한 등 10건)를 입력으로.
3. 세션 종료 시 docs/prompts에 session-08 기록 작성·push.
메모리 캡 테스트 규칙(-p 1) 준수. Fable 유지, 보안 서술 최소화(fable-security-prose-minimization) 준수. ultrathink
```
