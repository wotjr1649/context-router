# 세션 66 — 축 C 를 구현해 머지했다. 그리고 계획 자신의 결함 여덟이 실행 중에 드러났다

## 1. 세션 정보

- **세션 66**, `2026-08-15T03:45Z ~ 08:35Z`. **UTC 로 08-15 다** — 세션 63~65 의 08-14 연속이 끊겼다.
- 시작 `main` = `5c879ab`. **마감 `main` = `c5e7c66`**(PR #48 머지 커밋). 워킹트리 clean, origin 과 동기.
- **세션 65 마감(18:56Z)과 이 세션 시작(03:45Z) 사이는 약 9시간이다.**
- 네 국면: ① 확인 여섯 ② 축 C 구현(`subagent-driven-development`, 태스크 다섯) ③ whole-branch
  리뷰 → fix wave 둘 ④ PR #48 → CI → 머지.
- **설치 0. 저장소 밖 설정 변경 0. `ctr_*` 호출 0. Codex 호출 0. 백그라운드 작업 전부 회수.**
- **★ 이 세션은 코드를 많이 바꿨다** — 브랜치 13커밋, 최종 diff 는 `main` 대비 코드 5파일 + 문서 3파일.

## 2. 시작 프롬프트 (원문)

> docs/prompts/2026-08-14-session-65-review-ladder-and-spec-approval.md 를 읽고 이어서 작업한다.
> 이 레코드가 최신이다. **세션 64 레코드는 §3.8(포착 구성비)·§3.11(분모의 소속)만 필요할 때 보면
> 되고, 세션 63 이전 레코드의 포착 수치·08-13/08-14 칸은 인용하지 마라** — 계상과 값이 다르다.
>
> ## 지금 상태
>
> - **계측 t0 = 2026-08-12T01:22:18Z** — 유효. 판정 가능 시점은 2026-08-26T01:22:18Z 이고
>   그전에는 결정표 행 1("14일이 안 지났다")이다.
> - **336h 창 실효 시작 = 2026-08-12T18:19:19Z**.
> - **★★★★ 손실이 다시 났다 — 세션 65 마감에 100개다**(§3.6.3). **세션 62 이후 서버 기동 열한 번
>   연속 삭제 0 이었고 열두 번째가 그것을 끊었다.**
>   **원인은 그 세션이 띄운 Codex 다**: `context-router.exe ← codex.exe ← bash.exe ← bash.exe ←
>   node.exe` 계보의 서버가 `18:07:00Z` 에 뜨면서 **336h 를 못 받아 72h 로 돌았다.** 지워진 범위가
>   72h 창과 정확히 맞고(08-11 일부까지, 08-12·08-13 무사) `startupPurgeMaxHashes`(100)까지 돌았다.
>   **회수 줄 여덟 칸은 t0 와 같다 — 판정 지표는 안 오염됐다.**
> - **★★★★ `[소유자 결정]` 판정일까지 Codex 를 쓰지 마라.** 크로스모델 패스도, `codex:rescue` 도,
>   companion 직접 실행도 전부다. **그 진입점이 336h 를 못 받는다는 것이 실측이고 어느 고리에서
>   갈리는지는 아직 안 쟀다.** 서브에이전트 리뷰(로컬)는 무관하니 그대로 쓴다.
> - **★★ `bash.exe` 계보가 무사하다는 옛 관측을 믿지 마라** — 세션 61~64 의 그 네 번과 이번은
>   **진입점이 다르다.** 관측이었지 보증이 아니었다. **계통 이름이 아니라 그 기동이 실제로 무엇을
>   받았는지가 판정 근거다.**
> - **구멍은 `claude.exe` 계보에만 닫혀 있다** — 그 셋은 336h 를 받았고 이번 사고에도 무사했다.
>   지운 전력이 있는 쪽은 이제 **`ChatGPT.exe` 계보와 `codex.exe` 계보 둘**이다.
> - **훅 중복 없음** — 도구 출력 1건당 원장 1행이다. **모델 변경 경로는 두 번 시험돼 두 번 다
>   견뎠다**(mtime 만 갱신, sha256 `DCC1B40B…AC5DF6` 불변). **플러그인 설치 경로는 아직 미시험.**
> - **★ 판정 분모가 아니라 처방의 전제가 오염돼 있다** — 포착의 **78.5%건 / 87.6%B 가 Edit 응답**
>   이고 지금은 끌 수 없다. **판정일 규칙은 아래에 그대로 실어 뒀다. 그날 정하지 마라.**
> - **★★★ 열린 작업은 하나다 — 축 C 구현이 착수 대기이고, 계획은 이미 검토를 통과했다.**
>   계획 `docs/superpowers/plans/2026-08-14-purge-audit-log.md`(다섯 태스크) · 스펙
>   `docs/superpowers/specs/2026-08-14-purge-audit-log-design.md` — **둘 다 읽어라**(계획이
>   스펙에서 논증한다). 세션 65 가 **Codex 한 패스 + 서브에이전트 리뷰**를 돌려 나온 **16건을
>   전부 반영했다**(critical 1 · important 4 · minor 11, 커밋 `972e401`).
>   `[소유자 결정]` **실행 방식은 `subagent-driven-development`** — 태스크당 fresh 서브에이전트 +
>   태스크 리뷰. **다시 묻지 않아도 된다.**
> - **★★★ "판정까지 감시만"은 틀린 전제다**(세션 65 §3.6). 계측이 막는 것은 셋뿐이다:
>   ① 이 프로젝트에서 `ctr_*` 호출 ② **설치** ③ 채택 레버 스펙 읽기.
>   **코드 변경·커밋·PR·머지·게이트·설계·계획은 전부 무관하다** — 세션 65 가 PR 하나를 리뷰
>   래더까지 태워 머지했는데 회수 줄 여덟 칸이 t0 와 글자 그대로 같다. **진짜로 기다려야 하는
>   것은 채택 레버 하나뿐**이고 그것도 판정 결과가 설계의 입력이기 때문이다.
> - PR #47 은 세션 65 가 닫았다(머지 커밋 `833bc4b`, CI 두 run 전부 초록 — windows flake 없음).
>   **★ 그 수정은 아직 안 돈다** — 설치본이 `077b4acc8fa1` 그대로라 `doctor [9]` 의 마커 사각은
>   **로컬 CLI 에서만** 닫혔다. 훅 중복 감시는 계속 **확인 3** 이 한다.
> - **`CHANGELOG.md` 의 `[Unreleased]` 에 둘이 쌓여 있다** — F10(artifact id 재사용)과 PR #47.
>   **새 기능을 머지하면 그 절에 함께 적어라** — 세션 65 가 그것을 빠뜨렸다가 뒤늦게 잡았다.
> - 설치본은 그대로 0.20.0 @ 077b4acc8fa1. **판정일까지 설치하지 마라.**
>
> ## 첫 일 — 확인 여섯
>
> 1. Get-CimInstance Win32_Process -Filter "Name='context-router.exe'"  — 서버와 그 부모.
>    **계보를 끝까지 봐라.**
>    **★★ 옛 레코드의 *"`bash.exe` 계보는 네 세션 연속 무사했다"* 를 인용하지 마라 — 세션 65 가
>    반증했다**(§3.6.3). **그 계보가 이번에 100개를 지웠다.** 진입점이 달랐을 뿐이다. **계통
>    이름이 아니라 그 기동이 336h 를 받았는지가 판정 근거이고, 그것은 프로세스 목록으로는
>    안 보인다.** 지운 전력이 있는 쪽은 이제 `ChatGPT.exe` 와 `codex.exe` 둘이다.
>    **★★★ `18:07:00Z` 에 뜬 PID 17224(부모 `codex.exe`)가 그 서버다 — 살아 있으면 죽이지 마라.**
>    그 기동의 퍼지는 이미 돌았으므로 살아 있는 것 자체는 무해하고, **죽이면 다음에 다시 뜨면서
>    남은 236 개 중 또 100 개를 집는다.**
> 2. "UTCNOW=$([DateTime]::UtcNow.ToString('yyyy-MM-ddTHH:mm:ssZ')) RET=[$env:CTR_SHADOW_RETENTION] SRC=[$env:CTR_RETENTION_SOURCE]"
>    — 게이트. **RET 이 336h 여야 한다.** 비어 있으면 즉시 재처방이다. SRC=pwsh-profile 은 정상이고,
>    **INH 는 이 셸에서 상속 경로의 증거가 아니다.**
>    **★ UTC 날짜를 여기서 받아라 — 확인 5 의 08-14 칸 판정이 그것에 걸린다.**
> 3. **훅 중복 재발 검사** — Claude Code 는 플러그인 설치·모델 변경·설정 변경 때 settings.json 을
>    다시 쓴다:
>    Select-String -Path "$env:USERPROFILE\.claude\settings.json" -Pattern 'context-router hook' -SimpleMatch
>    **0건이어야 한다.** 나오면 다시 두 벌이고 그때부터의 포착 수치는 또 2배다.
> 4. context-router stats  — 회수 줄 여덟 칸이 t0 와 같은가.
>    기준선: calls=49 legacy=49 나머지 여섯 칸 0, age_s 전부 없음.
>    **주 1회 스냅샷은 ~08-21 이 다음이다.**
> 5. **손실 감시 — doctor [15] hashes · [14] sources · CAS blob 일별 분포를 함께 본다.**
>    **★ 세션 65 마감에 손실 100개가 났다(§3.6.3) — 고정 칸 값이 바뀌었다. 587 을 인용하지 마라.**
>    **고정값은 07-21~08-13 일곱 칸이고 그 합은 이제 487 이다. 2026-08-14T18:33:24Z 기준:**
>    07-21 **116**(고아) · 08-08 **15** · 08-09 **82** · 08-10 **130** · 08-11 **9** ·
>    08-12 **86** · 08-13 **49** · **08-14 266**.
>    **하한: blob ≥753 · hashes ≥637 · sources ≥639.** 크면 정상이고,
>    **크다는 것 자체는 아무 문제도 아니다.**
>    **★★ 08-08~08-11 네 칸은 아직 72h 대상이고 배치가 남았다** — 그 기동이 상한(100)까지 돌았다.
>    **336h 를 못 받은 서버가 또 뜨면 남은 236 개 중 100 개가 더 빠진다.**
>
>    **★★★ 아래 판정 규칙 넷은 그대로지만, 첫째 행(삭제)에는 이제 원인 후보가 붙는다:**
>    **336h 를 못 받은 진입점**(세션 65 는 Codex 였다). 확인 1 의 계보를 보되 **계통 이름으로
>    판정하지 마라** — 더 빠른 증거는 **삭제 범위와 창의 비교**다. 줄어든 칸이 `오늘 − 72h`
>    이전에만 몰려 있고 그 뒤 칸이 무사하면 **72h 로 돈 기동**이다(세션 65 가 그렇게 읽어 냈다).
>    **★ 그리고 세 축 정합 등식은 손실에도 안 깨진다** — 그 등식은 *"내부 정합"* 을 볼 뿐
>    *"얼마나 남았는지"* 를 안 본다. **손실 판정은 고정 칸이 하고, 등식은 계상 오류가 한다.**
>
>    **★★★ 08-14 칸의 성격은 확인 2 가 받은 UTC 날짜가 정한다 — 먼저 그것부터 보라:**
>    - **UTC 로 아직 08-14 면** → 08-14 는 **오늘 칸**이다. 하한 266, 늘기만 한다.
>    - **UTC 로 08-15 이후면** → 08-14 는 **옛 칸이 되지만 고정값이 아니다.** 그 칸에 세션 64·65 의
>      레코드·스펙·계획과 그 세션들이 읽은 소스의 blob 이 **266개 이상**(하한 — 마감 검토 중에도
>      계속 늘었다) 들어 있고, 네가 그중 아무거나 다시 읽으면
>      재포착되어 **08-14 가 줄고 오늘 칸이 그만큼 는다.** 이것이 **판정표 둘째 행(재포착 mtime
>      이동)이고 정상이다 — 조치하지 마라.** 세션 64 가 08-13 칸에서 이미 관측했다.
>      **손실 판정은 07-21~08-13 일곱 칸(합 487)으로만 하라.**
>
>    분포는 doctor 가 안 낸다. 직접 센다:
>    Get-ChildItem 'C:\Users\js\AppData\Local\context-router\projects\context-router-4d307dacd75b\artifacts' -Recurse -File
>    을 LastWriteTimeUtc 의 MM-dd 로 Group-Object 한다.
>    **판정은 수가 아니라 이 규칙으로 한다:**
>    - 오늘 이전 칸이 X 줄고 **총계 세 축도 함께 줄었다** → **삭제.** 확인 1 의 계보를 본다.
>    - 오늘 이전 칸이 X 줄었는데 **총계는 불변/증가이고 오늘 칸이 X 늘었다** →
>      **재포착 mtime 이동이다. 정상, 조치 없음.**
>    - 오늘 이전 칸 불변, 총계 증가 → 순수 신규. 정상.
>    - **★ 08-22 부터**: 줄어든 칸이 **오늘로부터 14일 이상 된 날짜**면 → **정상 336h 만료다.
>      조치 없음.** 그보다 최근 칸이 줄었을 때만 사고다.
>    정합 검산도 같이 한다: **blob 총수 − 116(고아) = hashes = 일별 칸 합**, sources = hashes + 2.
>    **이 등식은 총계가 얼마든 성립해야 한다 — 깨지면 그때가 사고다.**
> 6. context-router doctor 의 [17] commit — 077b4acc8fa1 인가 (설치본이 안 갈렸는가).
>
> ## 포착 수치를 읽는 법
>
> - **출발점은 655**(2026-08-14T18:33:24Z, hook:shadow 655회 37,204,838 B, span 끝 18:33:14Z
>   — **하한이다**). **증가분은 1:1 이다** — 세션 64 가 독립 확증했고 세션 65 가 여러 구간에서
>   재확인했다. **★ 다만 손실 사고 구간(§3.6.3)에서는 blob 이 줄었으므로 그 구간에 1:1 을
>   적용하지 마라** — 포착은 늘고 삭제가 그보다 컸다.
> - **★ 이 수의 78.5%는 Edit 응답이다.** 판정에 쓸 때 아래 「판정일 규칙」의 걸음 4를 반드시 밟아라.
> - **★ 이 수가 빨리 는다고 놀라지 마라.** 16 KiB 넘는 도구 응답은 전부 포착된다 — 대형 파일 Read
>   뿐 아니라 **대형 파일 Edit 도** 매번 파일 크기만큼 올린다. **구현 세션은 특히 빠르게 는다.**
>   판정 지표는 이 수가 아니라 **회수 줄**이다.
> - **세션 62 이전 수치와 직접 비교하지 마라** — 2026-08-14T09:10Z 를 경계로 계상이 바뀌었다.
>   세션 60·61·62 의 172·388·476 은 실제 ~86·~194·~238 이다.
>
> ## 그다음 — 축 C 구현 착수
>
> **확인 여섯이 초록이면 바로 구현으로 간다.** 계획이 다섯 태스크로 나뉘어 있고 각각 독립
> 테스트 가능하다: store 쓰기 → cli 파서 → 기동 배선(**0건 분기 신설**) → CLI 세 경로 배선 →
> `doctor [21]`.
>
> **★★★ 확인 5 가 또 손실을 내면 구현보다 그것이 먼저다.** 배치가 남아 있어(236개, 기동당 상한
> 100) **336h 를 못 받은 기동이 한 번만 더 있으면 100개가 또 빠진다.** 그때는 구현을 멈추고
> ① 어느 진입점이었는지 ② **그 진입점을 이 세션이 직접 불렀는지**를 확정해 소유자에게 보고해라 —
> 세션 65 의 원인은 **자기가 부른 검토 도구**였고, 그것이 가장 놓치기 쉬운 자리다.
>
> 1. **계획과 스펙을 둘 다 읽는다** — 계획이 스펙에서 논증하므로 스펙 없이는 근거를 못 본다.
> 2. **`subagent-driven-development` 로 간다** — 소유자가 이미 지정했다(태스크당 fresh
>    서브에이전트 + 태스크 리뷰). **계획 검토는 세션 65 가 이미 돌렸다** — 같은 것을 다시
>    돌리지 마라(Codex 는 체크포인트당 한 패스다).
> 3. **태스크마다 게이트 다섯을 직접 돌린다** — 옮겨 적지 마라.
> 4. **태스크 체크포인트마다 서브에이전트 리뷰.** 크로스모델 패스는 외부 전송이라 **현재 요청이
>    있을 때만** 돈다.
> 5. **머지까지 간다. 설치는 판정일(2026-08-26) 뒤다** — 그 구분이 이 계획의 전제다.
> 6. **CHANGELOG `[Unreleased]` 에 적는다**(계획 Task 5 Step 6). 세션 65 가 PR #47 에서 그것을
>    빠뜨렸다가 뒤늦게 잡았다.
>
> **★ 계획대로 안 되는 것이 나오면 계획을 고쳐라.** 세션 65 가 계획을 쓰며 **승인된 스펙의 오류
> 둘**을 잡았다(§3.6.1) — 둘 다 실제 호출부를 열어 봐야만 보였다. 구현이 스펙과 부딪히면
> **코드를 비틀지 말고 스펙을 코드로 확인해라.** 승인 도장이 찍힌 문서라도 코드와 대조하기
> 전에는 미검증 주장이다.
>
> - 훅 진단의 새 이월 넷(세션 65 §5)은 **판정과 무관하지만 이번 세션 범위 밖**이다. 축 C 를
>   끝내고 여유가 있으면 소유자에게 물어라.
> - **소유자가 다른 축을 지시하면 그것이 이긴다.**
>
> ## ★★★ 판정일 규칙 — 세션 64 가 미리 확정했다. **그날 정하지 마라.**
>
> **왜 미리 박는가**: 축 A 가 포착의 **78.5%건 / 87.6%B 가 Edit 응답**임을 밝혔고, 그 사실은
> *"소비 0"* 의 **해석**을 바꿀 수 있다. 규칙을 판정일에 정하면 수치를 보고 규칙을 맞추게 된다.
>
> **판정일에 이 다섯을 이 순서로 하고, 순서를 바꾸지도 줄이지도 마라:**
>
> 1. **회수 줄 여덟 칸을 그대로 적는다.** D104 가 읽는 것은 이 칸들이고 **착수 조건의 분모는
>    `resolved_artifacts + missed` 다**. **★ 포착 수로 그 분모를 갈아 끼우지 마라** — 포착 수가
>    답하는 것은 *"얼마나 포착되는가"* 이고 창의 길이가 묻는 것은 *"포착된 것이 회수되는가"* 다.
>    **다른 질문의 분모다.**
> 2. **포착의 구성비를 판정일 시점으로 다시 계산해 옆에 적는다.** 방법:
>    `context-router session export --project context-router-4d307dacd75b --worktree <각각>` →
>    `tool_result_summary` 의 `<도구명>: <크기>B` 를 도구별 집계. **CLI 라서 `ctr_*` 원장을
>    안 건드린다.** worktree 는 7개이고 주 worktree 가 압도적이다. 세션 64 값은
>    Edit 78.5%건/87.6%B · Read 13.5%/7.6% 였다(t0 이후 520건).
> 3. **결정표에서 행을 고른다.** 여기까지는 규칙대로다 — 해석을 섞지 마라.
> 4. **★ 그 행의 처방을 채택하기 전에 전제를 한 번 검사하고, 결과를 레코드에 적는다:**
>    *"이 처방은 「모델이 회수를 안 한다」를 전제하는가? 그렇다면 포착의 78%가 Edit 응답이라는
>    사실이 그 전제를 약화시키는가?"* — **답이 무엇이든 적는다.**
> 5. **그 뒤에** 채택 레버 스펙을 읽는다. 먼저 읽으면 그 문면이 `ctr_fetch` 를 권해 판정 지표를
>    오염시킨다. **판정 전에는 이 저장소에서 그 스펙을 열지 마라.**
>
> **4를 건너뛰면 *"채택 미달"* 과 *"채택 대상이 애초에 희소"* 가 같은 수로 보인다 — 처방이
> 정반대다.** 전자는 레버를 넣고, 후자는 **포착을 줄인다**(Edit 매처 제외 등).
>
> ## 그 밖의 상태
>
> - **설치는 하지 마라.** 판정일(2026-08-26) 전에 채택 레버가 살아나면 판정 지표가 오염된다.
> - **★★★ 336h 대상이 0개인 것은 2026-08-22 까지다.** 08-08→08-22 · 08-09→08-23 · 08-10→08-24 ·
>   08-11→08-25 · 08-12→**판정일** · 08-13→08-27 · 08-14→08-28.
>   **★ 판정일의 336h 대상은 약 236개다**(검산: 15+82+130+9 — 세션 65 의 손실 100개가 그만큼
>   줄였다, §3.6.3). **옛 레코드의 "약 336개"를 인용하지 마라.** 그 삭제는 **정상 정책 동작이지
>   사고가 아니다.**
>   **★ 창은 336h 그대로 둔다 — 소유자 판정 완료. 이 결정은 닫혔다.**
> - **★★ 72h 대상은 236개이고 배치가 남아 있다** — 세션 65 의 그 기동이 상한(`startupPurgeMaxHashes`
>   = 100)까지 돌았다. **336h 를 못 받은 서버가 또 뜨면 100개가 더 빠진다.** 이걸 재려고 테스트를
>   만들지 마라 — 분포와 창 시작 시각의 비교가 답한다.
> - **소비는 여전히 0** (`ctr_fetch`·`ctr_search`·`ctr_session_summary` 전부 t0 이후 0회,
>   여섯 세션째). 결정표 행 2 로 갈 공산이 크다 — 판정일에 회수 줄을 인위적으로 올리지 마라.
> - **프로브 blob 둘**(`0fc3bc88…`·`3884b8a0…`, 각 16,461 B, 08-14 칸)은 세션 63 계측의 산물이다.
>   **지우지 마라** — CAS 파일만 지우면 DB 행이 dangling 이 된다. **2026-08-28 에 정상 만료로 빠진다.**
> - **코드베이스 전반은 세션 64 가 읽고 소유자에게 보고했다. 같은 것을 다시 읽지 마라** — 재고만 는다.
> - F12·F13·트라이그램 축·고아 blob 116개·두 계상 이력은 그대로다. **이 머신에 `sqlite3` 가 없다.**
> - **훅 진단의 새 이월 넷**(세션 65 §5): `bannedHortatoryVocab` 이 수동 힌트를 안 스캔한다 ·
>   `heldGroupHint` 단정형은 미룬 선택이다 · `scanRegisteredHooks` 와 `hook uninstall` 의 훑는
>   범위가 다르다(선존, F3 계열) · `n==0` 선두 토큰의 "없음 + N그룹" 충돌은 계약 변경이다.
>   **전부 PR #47 밖이고 판정 뒤 후보다.**
> - **마감 직전에 프로세스와 재고를 다시 세고 레코드에는 그 수를 적어라.** 세션 61·62·64 가 마감
>   직전에 새 서버를 잡았다 — 세션 63·65 가 안 잡혔다고 이 프로토콜을 놓으면 못 본다.
>   **그리고 마감은 커밋으로 끝나지 않는다** — 커밋 뒤에 재고를 움직이는 작업을 하면 다시 세고
>   레코드를 갱신해 두 번째 커밋을 남겨라.
> - **이번 세션은 짧게 닫는 세션이 아니다** — 확인 여섯은 게이트일 뿐이고 본 일은 축 C 구현이다.
>   **다만 마감 프로토콜은 그대로다**: 마감 직전 재계수 → 레코드 → 커밋. **구현 세션은 재고를
>   많이 움직이므로** 그 재계수가 특히 중요하다.
>
> ## 밟으면 안 되는 자리
>
> - **336h 를 못 받은 채 서버를 띄우지 마라.** 확인 2 가 그 게이트다.
> - **★ 카운터 하나로 계상 배율을 판정하지 마라** — 독립적으로 세는 다른 카운터 옆에 세워라.
>   `원장 2행 : blob 1개` = 배율 2 · `원장 2행 : blob 2개` = 배율 1. 비율이 반복해서 정확히 딱
>   떨어지면(1/2, 1/3, 2:1) 현상보다 **계상**을 먼저 의심하라.
> - **★ 계측기가 세는 *단위*를 확인했으면 그 수의 *구성비*도, 그 분모가 *어느 질문의* 분모인지도
>   확인해라.** 세 층이다: 단위 → 구성비 → 분모의 소속.
> - **★★★★ 같은 계통도 진입점이 다르면 다른 답을 낸다** — 아래 *"계통 하나를 재고 일반화하지
>   마라"* 의 다음 층이다. 세션 61~64 가 **네 번 무사히 관측한 `bash.exe` 계보**가 세션 65 에서
>   100개를 지웠다(§3.6.3). **계통 이름은 식별자가 아니라 라벨이고, 판정 근거는 그 기동이 실제로
>   336h 를 받았는지다.** 그리고 그것은 프로세스 목록으로 안 보인다 — **삭제 범위와 창의 비교**가
>   유일한 사후 증거다.
> - **★★★★ 검토를 위해 부르는 외부 도구가 서버를 띄울 수 있다** — 그것이 세션 65 의 손실
>   원인이다. **egress 판단만으로 부족하다**: *"무엇이 전송되는가"* 뿐 아니라 *"이 호출이 무엇을
>   실행하고 무엇을 띄우는가"* 를 함께 세라. 계측이 도는 저장소에서 **읽기 전용이라는 가정이
>   틀리는 자리**가 거기다.
> - **★ 테스트가 통과한다고 그것이 무언가를 잠근다고 읽지 마라**(세션 65 §6). 결정을 잡는다고
>   주장하는 단정은 **한 번 뒤집어 빨개지는지 재라.** Edit 둘과 테스트 한 번이면 관측이 된다.
> - **★ 리뷰 findings 를 기각할 때 결론만 지키지 말고 기각 근거를 리뷰어에게 되돌려라** — 세션 65
>   는 다섯 기각 중 하나에서 **근거가 틀렸다는 정정**을 받았다(결론은 유지). **코드에 남는 것은
>   근거이고, 다음 사람은 그 문장에 기대 판단한다.**
> - **★ 진단 문면이 조건을 단정하기 전에 그 조건을 볼 수단이 코드에 있는지 확인해라.** 없으면 그
>   단정은 오보를 권하는 안내다. **조건형으로 고칠 때는 인과절만이 아니라 지시도 함께 걸어라.**
> - **★ 감시 세션에서 대량 소스를 읽었으면 기준선을 그 *뒤*에 다시 잡아라.** doctor·stats·작은
>   출력은 재고를 안 늘리지만 **16 KiB 넘는 소스 여러 개는 다르다.**
> - **★ `doctor [9]` 의 초록을 "훅 중복 없음"으로 읽지 마라** — 그 검사는 마커를 첫 관문으로
>   요구한다(PR #47 이 그 구멍을 고쳤지만 **설치 전이라 아직 안 돈다**). 중복은 확인 3 이 본다.
> - **재고 감시를 회수 줄로 하지 마라** — 판정 지표가 t0 와 글자 그대로 같은 채로 재고의 3분의 1이
>   사라질 수 있다. 세션 58~60 이 그래서 200개를 못 봤다.
> - **감시 칸을 날짜로 고정하지 마라** — "오늘 이전"이 규칙이다. **그리고 "오늘 이전 칸이 줄면
>   퍼지"로 단정하지 마라** — 재포착 mtime 이동과 08-22 이후의 정상 만료가 같은 모양으로 보인다.
> - **순감과 삭제량을 같은 수로 쓰지 마라** — 견고한 것은 순감뿐이고 삭제량은 하한이다.
> - **삭제를 로그로 찾지 마라** — MCP 서버 stderr 는 어디에도 안 남는다. **삭제 시각을 파일시스템
>   자국으로 역산하지도 마라** — 그 자국은 생성과 삭제를 못 가른다. 손실이 났으면 **어느 계통이
>   336h 를 못 받았는지**부터 봐라.
> - **INH 를 상속 경로의 증거로 읽지 마라** — 그 표식을 설정하지 않는 부모에서 잰 것만 관측이다.
> - **계통 하나를 재고 일반화하지 마라** — 세 계통을 각각 재고 나서야 "구멍이 없다"를 말할 수 있었다.
> - **CTR_SHADOW_RETENTION 에 14d 를 쓰지 마라** — ParseDuration 에 d 가 없어 조용히 72h 다.
> - **회수 줄을 인위적으로 올리지 마라.** 검증 회수는 판정 대상이 아닌 프로젝트에서 한다.
> - **판정 대상이 될 수 있는 프로젝트에서 채택 레버의 스펙·계획을 읽지 마라** — 그 문면이
>   ctr_fetch 를 권한다.
> - **침묵을 단정하는 테스트를 stdout == "" 하나로 쓰지 마라** — 긍정 케이스와 한 축만 다른
>   픽스처를 쓰고 부작용 축을 함께 봐라.
> - **외부 검토 초점에 의역을 싣지 마라** — 별칭화는 식별자에 거는 것이지 검토 대상에 거는 것이 아니다.
> - **게이트 결과를 옮겨 적지 말고 직접 돌려라.** 완료 알림의 진단이 보고와 모순되면 직접 확인.
> - **가드를 만나면 우회하지 마라** — 그 한 칸은 사람에게 넘겨라. `Grep head_limit=0` 은 실제로
>   거부된다(D54 가드) — `head_limit` 을 지정해 다시 물어라.
> - stats·doctor 는 퍼지를 돌리지 않는다(serve 기동만 돈다). 훅도 안 돈다.
>   **그리고 16 KiB 미만 도구 출력은 포착되지 않는다** — 감시 명령 한 바퀴가 재고를 안 늘린다.
> - go test 에 -p 1 필수. 게이트 다섯을 && 로 잇지 마라 — internal/exec 는 이 머신의 PowerShell
>   실행 정책 때문에 빨갛다(환경 조건이지 회귀가 아니다). 관측된 이름:
>   TestNpmrcScratchConfigWins · TestRunShellPS51ProviderWriteReturns · TestD78SignalOnExit ·
>   TestD78NoObservableSideEffects.
> - 행 번호를 주석·문서에 적지 마라 — 심볼 이름을 적어라.
> - **프로세스 목록은 그 순간의 생존자만 보여준다** — 지나간 기동은 안 잡히고 다가올 기동도 안
>   잡힌다. 마감 직전에 그 자리에서 다시 세라.
>
> 문제 발견되면 fix 혹은 브레인스토밍하여 사용자에게 질문하고, 문제 없으면 진행한다.
> ultrathink

## 3. 한 일

### 3.1 확인 여섯 전부 초록 — 그리고 이번엔 계보가 아니라 삭제 범위로 읽었다

`[실측 03:45:09Z]`. **확인 5(분포)를 대량 읽기 *전에* 잡았다** — 세션 65 §7 의 *"감시 세션에서
대량 소스를 읽었으면 기준선을 그 뒤에 다시 잡아라"* 를 뒤집어, 아예 앞에서 잡았다.

| # | 항목 | 결과 |
|---|---|---|
| 1 | 프로세스 | 3개, **전부 `claude.exe` 계보**. 세션 65 의 PID 17224(codex 부모)는 죽었다. 오늘 신규 둘(`03:43:01Z`·`03:44:09Z`) |
| 2 | 게이트 | `UTCNOW=2026-08-15T03:45:09Z` · `RET=[336h]` · `SRC=[pwsh-profile]` ✅ |
| 3 | 훅 중복 | `settings.json` 매치 **0건** ✅ |
| 4 | 회수 줄 | `calls=49 legacy=49` 나머지 여섯 0, age_s 전부 없음 — **t0 와 글자 그대로 같다** ✅ |
| 5 | 손실 | 고정 일곱 칸 합 **487** — 손실 0 ✅ |
| 6 | 설치본 | `[17] 0.20.0 commit=077b4acc8fa1` ✅ |

정합: blob 777 − 116 = **661 = `[15] hashes`** = 일별 칸 합, `[14] sources` 663 = 661+2.

**★ 오늘 뜬 서버 둘이 336h 를 받았다는 판정을 계통 이름이 아니라 삭제 범위로 했다**: `03:43Z`
기동이 72h 로 돌았다면 창 시작이 `08-12T03:43Z` 라 08-08~08-11 네 칸(236개)이 상한까지 깎였을
텐데 **네 칸 전부 무사**하다. 세션 65 §7 이 지시한 방법 그대로다.

### 3.2 축 C 구현 — 다섯 태스크, 서브에이전트 실행, 게이트는 컨트롤러가 직접

`subagent-driven-development` 로 돌렸다. **worktree 는 만들지 않았다**(§4 Ruling 1).

| 태스크 | 커밋 | 수정 라운드 |
|---|---|---|
| 1 `internal/store` 형식·쓰기·분류 | `ec887c8` | 0 |
| 2 `internal/cli` 파서 | `ae24f28` → `3c770aa` | 1 |
| 3 기동 배선(0건 분기 신설) | `4a34577` → `488eaa8` | 1 |
| 4 CLI 세 경로 배선 | `3a47002` → `bbc625f` | 1 |
| 5 `doctor [21]` + CHANGELOG | `175dc1d` | 0 |

**게이트 다섯을 커밋마다 컨트롤러가 직접 돌렸다** — 총 아홉 번. 매번 build 0 · vet 0 ·
**12/13 패키지** · gofumpt 무출력 · lint **0 issues**. 빨간 `internal/exec` 는 매번 문서화된 네
이름 그대로였다.

**★ 그중 한 번은 그 규율이 실제로 값을 했다**: Task 1 완료 알림에 `undefined: AppendPurgeLog` 진단이
붙어 보고(DONE)와 모순됐다. 직접 확인하니 커밋에 세 파일이 다 있고 build·vet 초록 — **RED 국면의
낡은 스냅샷**이었다. 세션 65 §7 의 *"완료 알림의 진단이 보고와 모순되면 직접 확인"* 이 그 자리다.

### 3.3 ★★★ 프리플라이트 스캔이 계획 내부 모순 다섯을 잡았다 — 구현 전에

`subagent-driven-development` 는 Task 1 디스패치 전에 계획 전체를 훑어 **태스크 쌍마다 한 행,
태스크마다 한 행**의 표를 쓰라고 한다. 그 표가 다섯을 냈다:

- **`PurgeStatus` 본문이 어디에도 없었다.** Task 1 의 Produces 이고 그 우선순위 표 테스트도
  Task 1 소관인데, 구현 코드 블록에서 빠져 있고 본문은 Task 3 에 *"참고로"* 실려 있었다.
- **"미러 상수" 문장 둘**(Task 1 의 `PurgeLogName` 주석 · Task 2 의 Consumes 줄)이 소유자 결정
  F8 **이전** 초안의 잔재였다.
- Task 1 테스트가 `errors.New` 를 쓰는데 **import 에 `errors` 가 없다**.
- Task 1 의 `-run 'PurgeLog|PurgePolicy'` 가 `TestPurgeStatusPriority` 를 못 잡는다.
- Task 4 의 `-run '…RunPurgeHookOnlyLogsPolicy'` 가 실제 이름 `…LogsNoPolicy` 와 안 맞는다.

**전부 판정해 디스패치에 실어 보냈다.** 실행 중에 터졌으면 각각 한 라운드씩 썼을 것들이다.

### 3.4 ★ 계획이 `[실측]` 이라 적은 것을 코드로 대조했고, 하나는 틀렸다

디스패치 전에 컨트롤러가 직접 읽었다. 계획이 맞힌 것: `retention` 이 지역 변수가 아니라는 것
(`cutoff := store.ShadowCutoff(time.Now(), store.ShadowRetention(os.Getenv))` 한 줄) · `d` 가
`if selective` 안에 갇혀 있다는 것 · `PurgeOlderThan` 이 두 반환값을 버린다는 것.

**틀린 것 하나** — `--gc` 배선의 *"가장 싼 해소"*. 계획은 선택 분기를 `runGCOrphan` 으로
라우팅하라 했는데, 그러면 ① 살아 있는 writable `st` 옆에 두 번째 store 핸들이 열리고
② **`purgeErr` 흐름이 끊긴다** — 인라인 형태는 `purgeErr` 에 대입돼 뒤따르는 `*vacuum`/`MergeFTS`
블록이 읽는데 `runGCOrphan` 은 즉시 `return` 이라 `--all` 순회가 중단된다. 계획이 적어 둔
fallback(두 자리 + 공유 헬퍼)으로 갔다.

**계획이 걱정한 것 중 이미 있던 것 둘**: `closeAndWait`(정상 종료) 와 `hookContentDir`(프로젝트
디렉터리). Task 3 구현자가 후자를 컨트롤러의 제안(디렉터리 스캔)보다 낫다고 판단해 갈아 끼웠고
리뷰어가 동의했다.

### 3.5 ★★★★ 리뷰가 값을 한 자리 넷 — 전부 "테스트가 무언가를 잠근다"는 거짓 주장

세션 65 §6 이 낸 프로토콜(*"결정을 잡는다고 주장하는 단정은 한 번 뒤집어 재라"*)이 이 세션에서
**네 번** 물었다.

**① Task 2 — "F3 의 잠금"이라 이름 붙은 테스트가 아무것도 안 잠갔다.** 픽스처
`…\t1755180899\tcli-gc\t…` 의 count 칸이 `cli-gc` 라 `ParseInt` 가 먼저 거른다. **닫힌 집합
검사를 전부 지워도 통과한다.** status 검사 하나만이 거르는 픽스처로 바꾸고 뮤테이션으로
실증했다(`purgeStatusSet` 항 제거 → `unparsed=0 entries=1` 로 빨개짐 → 복원 초록).

**② Task 2 — 파서 주석이 세션 65 에서 Codex 가 반증한 그 문장을 담고 있었다.** *"잘린 append 는
9필드보다 짧게 나오므로 이 조건이 이미 거른다"* — **스펙 §4 는 그것을 "거짓이다"라고 명시**하는데
계획의 코드 블록은 안 고쳐져 구현자가 축자 전사했다. 세션 65 의 16건 반영이 스펙만 고치고 계획을
놓친 자리다. 재리뷰어가 스플라이스 필드 수 범위를 `[9,17]` 로 독립 유도해 새 주석까지 검증했다.

**③ Task 3 — 셧다운 레이스를 논증으로 닫으려 했다.** 구현자의 논거 둘 중 하나(늦은 취소는 무해 —
모든 non-`ok` 분기가 `purgeErr != nil` 을 요구한다)는 맞았지만, `handshake` 는 **배리어가 아니라
경과 시간**이다. `AppendPurgeLog` 이 고루틴의 **마지막 문장**이라 행 파일의 등장이 정확한 완료
신호가 되고, 8줄 폴이 논증을 관측으로 바꿨다. **부수 효과로 `cmd/context-router` 가 64.6s →
47.3s 로 줄었다** — 결정적으로 만드니 대기가 사라진 것이고, 그것 자체가 레이스가 실재했다는
증거다.

**④ Task 4 — `Count` 가 세 경로 중 둘에서 안 잠겨 있었다.** 테스트 셋이 전부 0건 퍼지를 기록했고,
살아남는 뮤테이션 하나가 **`sources` 를 `artifacts` 대신 기록하는 배선**(컨트롤러 결정이 콕 집었던
그 자리)이었다. **구현자가 한 층 더 내려갔다**: 기본 픽스처가 2 sources / 2 artifacts 라
**`Count` 단정을 넣어도 그 swap 이 통과한다.** 같은 바이트를 다른 URI 로 한 번 더 Register 해
(`Register` 가 `(content_hash, media_type)` 로 키를 잡아 artifact 행이 재사용된다) 3/2 로 갈라
놓고 그 전제를 명시로 단정했다 — 픽스처가 드리프트하면 조용히 무력해지는 대신 요란하게 실패한다.

**★ 그리고 브리프의 테스트만으로는 반쪽 배선이 출하됐을 것이다**: 인라인 `cli-gc` append 를 지워도
브리프가 지정한 `TestRunPurgeLogsGC` 는 초록이었고, 구현자가 추가한 `TestRunPurgeLogsSelectiveGC`
만 그것을 잡았다.

### 3.6 whole-branch 리뷰 — "메커니즘이 아니라 그 주변의 기록"이 막았다

`[소유자 결정 없음 — 델리게이션은 SDD 절차의 일부]` opus, 10커밋 105 KB. **Critical 0 ·
Important 4.** 리뷰어가 다섯 호출부를 스펙 §2.0 의미표와 대조하고, writer/parser/renderer 가 닫힌
집합 둘에 합치하는 것을 확인하고, `PurgeOlderThan` 이 커밋 후 실패에서 실제로 count 를 낸다는 것
(그래서 `failed`+`count>0` 계약이 허구가 아니라는 것)을 확인했다.

**★ 그리고 원래 사건을 새 코드로 재생했다**:

```
<ts>  startup-shadow  72h0m0s/-  <cutoff>  capped  100  <bytes>  <deferred>  <failed>
```

`72h0m0s/-` 가 추론을 관측으로 바꾸고, **`capped 100` 이 스펙이 주장하지 않은 것까지 답한다** —
배치가 상한에서 잘렸으므로 출혈이 기동마다 이어지고 있었다는 것. 성공 기준
`grep '72h0m0s/'` 는 그대로 성립한다.

**막은 넷은 전부 "기록"이다**:

| # | 무엇 |
|---|---|
| I1 | `main.go` 의 `&rep.Field` 주석이 이 브랜치가 이미 반증한 사실을 단정 — `cli.go` 의 쌍둥이는 Task 4 가 고쳤다 |
| I2 | CHANGELOG 의 `핵심 칸은 policy` 가 네 경로 중 하나에만 참인데 보편으로 읽힌다 |
| I3 | 파서가 **중단된 읽기를 한 줄 흠집으로 보고** — "최근 N건"이 잘린 앞부분에서 나오는데 `파싱 실패 1줄` 로 나간다 |
| I4 | `policy` 가 검증도 이스케이프도 없는 유일한 렌더 필드 — 로그에 append 할 수 있는 자가 운영자 터미널에 ANSI 를 몰 수 있다 |

**I3·I4 는 감사 로그의 *읽는 쪽*이 감사 로그가 없애려는 실패 모드를 재현하는 자리다.**

리뷰어는 flake 위험을 **패키지 기준선보다 낮다**고 판정했다: `cmd/context-router` 에
`t.Parallel()` 이 **0개**라 `t.Setenv` 가 샐 수 없고, `488eaa8` 이 경과 시간을 진짜 완료 신호로
바꿨다. 추가 비용 ≈ 5–12s.

### 3.6.1 ★★★ fix wave 가 자기 수정으로 여섯 번째 거짓 주석을 만들었다

fix wave(`4ad55b3`)가 열한 건을 전부 닫았지만 **판단 하나가 틀렸다.** 경고 셋에 `err` 를 실으며
주석에 *"경로 금지(§5.5 canary)는 **반환 오류** 규칙이며 sanitizeIOErr 가 그 자리를 맡는다"* 고
적었다. 재리뷰어가 원문을 짚었고 **컨트롤러가 직접 확인했다**:

`docs/context-router-design-v0.0.1-ko.md` §5.5, 축자:
> slog(stderr): `ts, level, tool, duration_ms, bytes_in, bytes_out, error_code`만.
> 원문·쿼리 본문·**절대경로**·env 미기록.

**§5.5 는 첫 두 단어가 slog 이고 절대경로를 그 금지 목록에 넣는다.** v0.2 §125 가 *"stderr slog,
비밀·절대경로 미포함"* 으로 승계한다. 그리고 세 오류는 전부 `*os.PathError` 라 스토어 절대경로와
프로젝트 id 를 물고 온다.

**옳게 하는 비용이 0 이었다** — `sanitizeIOErr` 가 같은 패키지에 unexported 로 있고,
`MergeFTSIfDue` 가 같은 모양에서 이미 그렇게 한다(`"error", sanitizeIOErr("merge stamp create", err)`).
그 함수 **자신의 주석이 "§5.5: 오류에 절대경로 금지"** 라고 적고 있다.

`[컨트롤러 판정]` 스킬은 fix wave 를 한 번으로 제한하지만 *"load-bearing 잔여는 판정해서
처리하라"* 고도 한다. **문서화된 제품 계약 위반 + 그 계약을 잘못 인용해 정당화한 거짓 주석**은
load-bearing 이다. 라운드 2(`25f2a9a`)가 셋을 `sanitizeIOErr` 로 감싸고 주석을 참으로 고쳤다.

구현자의 자기 진단이 정확했다: *"주장의 그림자가 초록인지를 본 것이지 주장이 참인지를 본 것이
아니다."*

### 3.7 ★★★ codex 계보 서버가 세션 중에 떴다 — 그러나 이 세션이 부른 것이 아니다

`[실측 08:18:42Z]` 마감 재계수에서 네 번째 서버를 잡았다: PID 17432, `04:07:12Z`, 계보
`context-router.exe ← codex.exe ← bash.exe ← bash.exe ← node.exe` — **세션 65 에서 100개를 지운
것과 정확히 같은 모양.** 소유자 결정은 판정일까지 Codex 금지이고, **이 세션은 Codex 를 한 번도
부르지 않았다.**

조상들의 커맨드라인이 답했다:

```
node.exe … app-server-broker.mjs serve --endpoint pipe:\\.\pipe\cxc-… --cwd D:/AI_DEV/codex-conductor-cc
  └ bash -c "codex app-server"  └ codex.exe app-server  └ context-router.exe
```

**다른 저장소(`D:/AI_DEV/codex-conductor-cc`)에서 도는 다른 Claude Code 세션**이 `openai-codex`
플러그인을 불렀고, 그 Codex 가 context-router 를 플러그인 MCP 서버로 물고 왔다. 진입점도 세션 65
와 다르다 — 거기는 `codex-companion.mjs task --background`, 여기는 `app-server-broker.mjs serve`.

**★★ "손실 0 이니 그 기동이 336h 를 받았다" 로 읽으면 안 된다.** 기동 퍼지는 **프로젝트
범위**이고 그 서버의 프로젝트는 `codex-conductor-cc` 다 — 어떤 보존값을 받았든 이 저장소를 안
건드린다. **여기서 삭제가 없는 것은 범위가 설명하지 정책이 설명하지 않는다.** 세션 65 의 결론은
**반증도 확증도 안 됐다.**

날카로워진 것은 하나다: 위험은 *"이 머신에서 Codex 가 돌았다"* 가 아니라 **"Codex 가 이 저장소를
cwd 로 돌았다"** 이고, **방아쇠가 이 세션일 필요도 없다** — 이 머신의 어느 Claude Code 창이든
띄울 수 있다.

**★★★ 그리고 이 모호함이 정확히 축 C 가 없애는 것이다.** 설치돼 있었다면 그 기동이 자기 프로젝트에
`336h0m0s/pwsh-profile` 또는 `72h0m0s/-` 한 줄을 남겼을 것이고, 질문은 범위에 대한 추론이 아니라
한 줄 조회였다. 설치는 의도적으로 안 했다.

## 4. `[컨트롤러 판정]` 목록 — 소유자가 뒤집을 수 있게 전부 적는다

1. **worktree 를 만들지 않고 주 디렉터리 브랜치로 갔다.** 새 git worktree 는 shadow store 의
   worktree 계정을 8개로 늘려 판정일 걸음 2(구성비 집계, *"worktree 는 7개"*)를 복잡하게 한다.
   *대가*: 파일시스템 격리 없음 — 트리가 clean 이고 태스크마다 커밋해 완화.
2. **스펙 §7·헤더의 *"구현은 판정일 이후"* 를 먼저 고쳤다**(`4df7763`). 구속 문서가 자기가 승인한
   작업을 금지하는 상태였다. *대가*: 문단 하나 되돌리기.
3. **프리플라이트 다섯을 전부 판정해 디스패치에 실었다**(R1–R6, §3.3).
4. **`sanPurgeField` 중복(Important, plan-mandated)을 park 했다.** 해소가 `internal/hook` 을
   건드려 이 계획의 File Structure 밖이고, 세션 65 §5 가 이미 별건 이월로 기록했다.
   *대가*: 두 사본이 갈린다 — 둘 다 진단용 사이드카라 국소적이고 주석이 remedy 를 가리킨다.
5. **룬 경계 절단(Minor)을 여기서 안 고쳤다** — 그 수정이 정확히 4번이 경고한 갈림을 만든다.
6. **Task 2 의 Important 둘을 고쳤다**(§3.5 ①②). 스펙이 구속 authority 이고 §4 가 그 문장을
   명시적으로 거짓이라 부른다.
7. **실행된 계획 절을 코드에 맞춰 고치지 않았다.** `docs/superpowers/plans/` 는 규약상 불변
   process record 이고, 계획의 문면이 결함을 심었다는 사실 자체가 정보다. 대신 맨 앞에
   **"★ 실행 중 정정"** 한 절을 얹어 살아 있는 거짓만 죽였다(`8af4720`·`afad336`).
8. **Task 3 의 셧다운 레이스를 배리어로 고쳤다**(§3.5 ③). 논증이 아니라 관측으로.
9. **Task 3 의 Minor(거짓 주석)를 라운드에 접었다** — 스킬은 Minor 를 루프에 안 넣지만 이
   프로젝트는 거짓 주석을 실제 결함으로 친다.
10. **Task 4 의 `--gc` 배선에서 계획을 뒤집었다**(§3.4).
11. **Task 4 의 세 지역 변수는 유지하고 주석만 고쳤다** — 계획이 그 구조를 명시하고, 결함은
    *근거*이지 구조가 아니었다.
12. **fix wave 뒤 라운드 2 를 돌렸다**(§3.6.1) — 스킬의 한 번 제한을 넘지만 load-bearing 이다.
13. **라운드 2 는 네 번째 리뷰를 안 걸고 컨트롤러가 직접 검증했다** — ~20줄이고 인용 정확성은
    이미 원문에서 확인했다. 새 주석의 사실 주장 넷을 각각 대조했다.
14. **SDD 워크스페이스를 지우지 않았다.** 스킬은 지우라 하지만 이 저장소는 형제 11개를 전부
    남겨 뒀고, gitignore 된 스크래치라 비용이 0이다.

## 5. 저장소 현재 상태

- **`main` = `c5e7c66`** — PR #48 머지 커밋. 워킹트리 clean, origin 과 동기.
- **★ PR #48 머지 완료** `[2026-08-15T08:29:10Z]` — 브랜치 `feat/purge-audit-log` **13커밋**
  (`4df7763` … `afad336`). **CI 두 run 전부 초록** `[실측]`: `test (windows-latest)` **pass ×2**
  (7m35s·7m4s) · ubuntu ×2(10m43s·9m41s) · macos ×2 · lint ×2 · crossbuild ×2 · test-node-leg ×2.
  **windows flake 안 났다** — 이 브랜치가 그 flake 군집에 서브프로세스 e2e 셋을 더했는데도.
- **설치본**: **0.20.0 @ `077b4acc8fa1`** — 안 갈았다. **판정일까지 설치하지 않는다.**
- 새 파일 넷: `internal/store/purgelog{,_test}.go` · `internal/cli/purgelog{,_test}.go`.
  수정: `internal/store/store.go`(`ProjectDir()` 4줄) · `cmd/context-router/main{,_test}.go` ·
  `internal/cli/cli{,_test}.go` · `CHANGELOG.md` · 계획 · 스펙.
- **`CHANGELOG.md` 의 `[Unreleased]` 에 셋이 쌓였다** — F10(artifact id 재사용) · PR #47 ·
  **`### Added` 의 퍼지 감사 로그**(이 세션이 만든 절).
- **계측 t0** = `2026-08-12T01:22:18Z` — **유효.** **336h 창 실효 시작** = `2026-08-12T18:19:19Z`.
- **`CTR_SHADOW_RETENTION`**: HKCU `336h` + `~/.claude/settings.json` 의 `env` `336h` +
  `$PROFILE.CurrentUserAllHosts`(git 밖).
- **훅 등록**: 플러그인 한 벌만. `settings.json` sha256 =
  `DCC1B40BC4B320B188374B13A6005FD7BC589DF071AA8DAF1153BE9480AC5DF6` — **세션 63 제거 이후 불변**.
- **★★★★ 마감 재계수** `[2026-08-15T08:18:42Z]`:
  - **손실 0 — 세션 내내.** 고정 일곱 칸 `116 · 15 · 82 · 130 · 9 · 86 · 49` = **487**,
    세션 시작 값과 동일. **08-14 칸은 290 에서 안 움직였다**(그 칸의 blob 을 아무도 다시 안 읽었다).
  - CAS blob **930** · `[15] hashes` **814** · `[14] sources` **816**.
    정합 `930 − 116 = 814` = 일별 칸 합(`15+82+130+9+86+49+290+153`), `816 = 814 + 2`. ✅
  - `hook:shadow` **832회 54,267,709 B**, span 끝 `08:13:10Z`(세션 시작 679 → **+153**).
    **오늘 칸이 정확히 153 — 배율 1.** 훅 중복 부재가 두 카운터로 확인된다.
  - **`fetch calls=49 legacy=49 legacy_after_migrate=0 resolved=0 resolved_artifacts=0 missed=0
    shadow_rows=0 shadow_artifacts=0 age_s 전부 없음` — t0 와 글자 그대로 같다. 소비 0, 일곱 세션째.**
  - `doctor [12]` drops **431 불변 — 이 세션 drop 0** · `[6]` sessions=248(empty 78) ·
    `[17]` commit `077b4acc8fa1` · 훅 중복 **0건** · `RET=[336h] SRC=[pwsh-profile]`.
  - **`[21]` 은 doctor 출력에 없다** — 설치본이 이 브랜치 이전이라 정상이다.
- **★ 재고 기준선** — 다음 세션의 손실 감시용. CAS blob **930**:

  | 07-21 | 08-08 | 08-09 | 08-10 | 08-11 | 08-12 | 08-13 | 08-14 | 08-15 |
  |---:|---:|---:|---:|---:|---:|---:|---:|---:|
  | 116 (고아) | 15 | 82 | 130 | 9 | 86 | 49 | 290 | **153** |

  **고정값은 07-21~08-13 일곱 칸이고 그 합은 487 이다.** **08-14·08-15 칸과 총계 셋은 하한이다** —
  이 레코드를 쓰는 행위 자체가 포착을 만든다.
- **★★★ 프로세스** `[08:18:42Z]`: **4개** — 16148(`08-14T17:46:44Z`) · 19976(`08-15T03:43:01Z`) ·
  15928(`08-15T03:44:09Z`) 전부 `claude.exe ← pwsh ← WindowsTerminal 5796`,
  **그리고 17432(`08-15T04:07:12Z`, `codex.exe` 부모 — §3.7). 죽이지 마라.**
- **★★★★ 진짜 최종 재계수** `[2026-08-15T08:37:52Z — 이 레코드를 쓰고 커밋한 뒤. 이것이
  인계값이다]`: CAS blob **932** · `[15] hashes` **816** · `[14] sources` **818** ·
  `hook:shadow` **834**(span 끝 `08:36:54Z`) · `[12]` drops **431** · `[17]` commit
  `077b4acc8fa1` · 훅 중복 **0건** · 회수 줄 여덟 칸 **t0 동일** · **프로세스 4개 — 마감 직전
  신규 기동 없음**.
  **고정 일곱 칸 487 불변**, 08-14 **290**, 08-15 칸 **155**.
  정합 `932 − 116 = 816` = 일별 칸 합(`15+82+130+9+86+49+290+155`), `818 = 816 + 2`.
  **배율도 1 그대로**(원장 +2 : blob +2). **§8 기준선은 이 수로 인계한다** — 위 블록은 첫 커밋
  시점 값으로 남긴다(하한 방식이 재편집을 요구하지 않는다는 세션 64 §3.7.3 설계 그대로다).
- **켠 채 남긴 것**: 없다. 게이트·리뷰의 백그라운드 작업은 전부 완료 회수했고 서브에이전트의
  뮤테이션 프로브는 전부 원복돼 `git diff` 로 확인됐다. **설치 0 · 저장소 밖 파일 변경 0 ·
  일회성 테스트 0 · Codex 호출 0 · `ctr_*` 호출 0.**

## 6. 이월 사항

- ~~**축 C 구현이 착수 대기**~~ — **닫혔다.** PR #48 머지(`c5e7c66`).
- **★★★ 설치는 판정일(2026-08-26) 뒤다.** 이 코드는 설치 전에는 **한 줄도 안 돈다** — 그래서
  §3.7 의 codex 기동도 여전히 기록을 안 남겼다. **판정 당일 설치 한 번으로 끝난다.**
- **★★ 판정 뒤 이 스펙을 다음 설계 버전에 D-number 로 흡수한다** — 스펙 §7 이 그렇게 적혀 있다.
  계획 파일은 그 결정의 유래 기록으로 남는다.
- **★ 전체 검토가 남긴 defer 아홉** — 전부 판정 무관, 판정 뒤 후보:
  `sanPurgeField` 의 바이트 경계 절단(**`san` 을 store 로 통합하는 별건과 함께 고쳐야 한다** —
  여기서만 고치면 두 사본이 갈린다) · `isDashOrInt` 가 부호를 받는다 · `PurgeStatus` 의 인접 bool
  셋에 호출부 인자 순서 테스트가 없다(**params 구조체는 만들지 마라** — 네 자리 중 셋이
  `false, false` 를 넘기고 swap 은 같은 "중단" 사실의 두 라벨을 바꿀 뿐이다) · 도달 불가능한
  `cancelled && budget && hashes==0` 행 · `purgeEntry.Cutoff` 가 `-` 를 `0` 으로 뭉갠다 ·
  `fmt.Fprint` vs `WriteString` · 미측정 바이트의 `-B` 표기 · 쓰는 자리의 `os.Getenv` 호이스트 ·
  `buildCtrBinary` 를 `sync.OnceValue` 로(호출부 열둘 전부에 이득).
- **★ 선존 관측 둘**(이 브랜치 밖): `dropsByReason` 은 `sc.Err()` 를 **아예 안 읽는다** — 이번에
  `purge.log` 쪽에서 닫은 I3 와 같은 실패 모드가 그 형제 리더에 그대로 있다. 그리고
  `internal/cli` 의 `compare.go`·`cli.go` 가 `sql.Rows` 를 `rows.Err()` 없이 순회하고
  `hook_install.go` 가 `sc.Err()` 없이 스캔한다.
- **훅 진단의 이월 넷**(세션 65 §5)은 그대로다. **전부 판정 뒤 후보다.**
- **★ 판정 분모가 아니라 처방의 전제가 오염돼 있다** — 포착의 78.5%건 / 87.6%B 가 Edit 응답.
  **판정일 규칙은 세션 64 가 확정했다. 그날 정하지 마라.** 전문은 §8 에 실어 인계한다.
- F12·F13·트라이그램 축·고아 blob 116개·두 계상 이력(`09:10Z` 경계)은 그대로다.
  **이 머신에 `sqlite3` 가 없다.**
- **판정까지** — `2026-08-26T01:22:18Z`. **11일 남았다.**
- **08-22 부터 336h 정상 만료** — **창은 336h 그대로. 소유자 판정 완료, 닫힌 결정이다.**

## 7. 상시 프로토콜 (이번 세션이 낸 것)

- **★★★★ 계획을 실행하기 *전에* 계획을 훑는 걸음이 라운드를 산다.** §3.3 의 프리플라이트 스캔이
  다섯을 잡았고 그중 하나(`PurgeStatus` 본문 부재)는 Task 3 에서야 컴파일 에러로 터졌을 것이다.
  값은 표의 형식에서 온다 — *"태스크 쌍마다 한 행, 태스크마다 한 행"* 을 실제로 채우면
  *"계획이 자기와 어긋나는가"* 가 훑기가 아니라 **열거**가 된다. "스캔은 깨끗했다"를 그 행들 없이
  적으면 그것은 돌린 스캔이 아니다.
- **★★★★ 문서가 고쳐졌다고 그 문서를 인용한 코드 블록이 고쳐진 것은 아니다.** §3.5 ②: 세션 65 가
  Codex 반증을 받아 **스펙 §4 를 고쳤는데 계획의 코드 블록은 안 고쳤고**, 구현자가 그 블록을 축자
  전사해 거짓 문장이 코드까지 갔다. **정정을 반영할 때 그 주장이 *복사돼 있는 자리*를 함께 세라** —
  스펙·계획·코드·CHANGELOG 는 같은 문장을 네 벌 든다.
- **★★★ 픽스처의 *대칭*이 단정을 무력화한다.** §3.5 ④: `Count` 단정을 넣어도 기본 픽스처가
  2 sources / 2 artifacts 라 두 값을 뒤바꾼 배선이 통과했다. **두 후보 값이 같은 픽스처에서는
  어느 쪽을 읽는지 물을 수 없다.** 단정을 쓰기 전에 *"내가 구분하려는 두 값이 이 픽스처에서
  다른가"* 를 묻고, 다르게 만들었으면 **그 전제 자체를 단정해라** — 픽스처가 드리프트하면 조용히
  무력해지는 대신 요란하게 실패한다.
- **★★★ 배리어를 만들 수 있으면 논증하지 마라.** §3.5 ③: 셧다운 레이스를 확률 논증으로 닫으려
  했고 절반이 틀렸다. **완료를 관측 가능하게 만드는 자국이 이미 있으면**(여기서는 고루틴의 마지막
  문장이 쓰는 파일) 그것을 기다리는 것이 논증보다 싸고, **부수적으로 빨라졌다**(64.6s → 47.3s).
  논증은 그 자국이 없을 때의 차선이다.
- **★★★ 자기 수정이 자기가 고친 부류의 결함을 새로 만든다.** §3.6.1: 다섯 번째 거짓 주석을 닫은
  fix wave 가 여섯 번째를 만들었다 — **계약을 인용해 정당화하면서 그 계약을 안 읽었다.**
  구현자의 자기 진단이 정확하다: *"주장의 그림자가 초록인지를 봤지 주장이 참인지를 안 봤다."*
  **인용은 원문을 열어야 인용이다.** 컨트롤러도 같은 규율을 밟았다 — 리뷰어의 인용을 받고 §5.5 를
  직접 grep 해 확인한 뒤에야 라운드 2 를 걸었다.
- **★★ 서브에이전트가 컨트롤러의 지도를 반증하면 그것이 성과다.** Task 3 이 `hookContentDir` 를
  찾아 컨트롤러의 디렉터리 스캔 제안을 갈아 끼웠고, Task 5 가 브리프와 컨트롤러 디스패치가 공유한
  거짓 주장(*"중간 삽입은 `assertDoctorAscending` 을 깬다"*)을 **측정으로** 정정했다. 디스패치에
  *"문서와 코드가 부딪히면 코드를 비틀지 말고 보고해라"* 를 넣은 값이 여기서 나온다.
- **★★ 프로세스 계보는 라벨이고 커맨드라인이 식별자다.** §3.7: 계보만 보면 세션 65 의 사고와
  구별할 수 없었다. `--cwd` 한 칸이 *"다른 저장소의 다른 세션"* 을 답했다. **계보를 봤으면
  조상들의 커맨드라인까지 봐라** — 그것이 "누가 띄웠나"의 유일한 답이다.
- **★★ "영향 없음"의 *이유*를 확정하기 전에 안심하지 마라.** §3.7 에서 손실 0 을 보고
  *"그 기동이 336h 를 받았다"* 로 읽을 뻔했다. 실제 이유는 **프로젝트 범위**였고, 그러면 그
  기동은 보존값에 대해 **아무것도 말하지 않는다.** 같은 관측이 두 설명을 가지면 그 관측은
  둘 중 어느 것의 증거도 아니다.

## 8. 다음 세션 시작 프롬프트

> docs/prompts/2026-08-15-session-66-purge-audit-log-merged.md 를 읽고 이어서 작업한다.
> 이 레코드가 최신이다. **세션 65 레코드는 §3.6.3(손실 사고의 계보)만 필요할 때 보면 되고,
> 세션 64 이전 레코드의 포착 수치·재고 칸은 인용하지 마라** — 계상과 값이 다르다.
>
> ## 지금 상태
>
> - **계측 t0 = 2026-08-12T01:22:18Z** — 유효. 판정 가능 시점은 **2026-08-26T01:22:18Z** 이고
>   그전에는 결정표 행 1("14일이 안 지났다")이다.
> - **336h 창 실효 시작 = 2026-08-12T18:19:19Z**.
> - **★★★ 축 C 는 닫혔다 — PR #48 이 머지됐다**(`main` = `c5e7c66`, `2026-08-15T08:29:10Z`).
>   CI 두 run 전부 초록이었고 **windows flake 도 안 났다**. 브랜치 13커밋.
>   **★ 그런데 그 코드는 아직 한 줄도 안 돈다** — 설치본이 `077b4acc8fa1` 그대로다.
>   `doctor` 에 `[21]` 이 없는 것이 정상이다.
> - **★★★ 열린 작업이 없다.** 백로그는 판정 뒤 후보뿐이다(§6). **소유자가 축을 지시하지 않으면
>   이 세션은 감시로 짧게 닫는다.**
> - **손실 0 — 세션 66 내내.** 고정 일곱 칸 합 **487** 불변. **세션 65 의 100개 이후 추가 손실은
>   없다.**
> - **★★★ codex 계보 서버가 08-15 에 다시 떴는데 이 저장소를 안 건드렸다**(§3.7). 원인은
>   **다른 저장소에서 도는 다른 Claude Code 세션**이 `openai-codex` 플러그인을 부른 것이고,
>   커맨드라인의 `--cwd D:/AI_DEV/codex-conductor-cc` 가 그것을 답했다.
>   **★★ "손실 0 이니 336h 를 받았다"로 읽지 마라** — 기동 퍼지는 프로젝트 범위라 그 서버는
>   애초에 이 저장소를 안 본다. **세션 65 의 결론은 반증도 확증도 안 됐다.**
>   **위험은 "이 머신에서 Codex 가 돌았다"가 아니라 "Codex 가 이 저장소를 cwd 로 돌았다"이고,
>   방아쇠가 이 세션일 필요도 없다.**
> - **★★★★ `[소유자 결정]` 판정일까지 Codex 를 쓰지 마라** — 크로스모델 패스·`codex:rescue`·
>   companion 직접 실행 전부. 서브에이전트 리뷰(로컬)는 무관하니 그대로 쓴다.
> - **훅 중복 없음** — 도구 출력 1건당 원장 1행(세션 66 이 +153 : +153 로 재확인).
>   sha256 `DCC1B40B…AC5DF6` 불변. **플러그인 설치 경로는 아직 미시험.**
> - 설치본은 그대로 0.20.0 @ 077b4acc8fa1. **판정일까지 설치하지 마라.**
>
> ## 첫 일 — 확인 여섯
>
> 1. Get-CimInstance Win32_Process -Filter "Name='context-router.exe'" — 서버와 그 부모.
>    **계보를 끝까지 보고, codex/ChatGPT 계보가 나오면 조상들의 커맨드라인까지 봐라**:
>    Get-CimInstance Win32_Process -Filter "ProcessId=<부모 pid>" 의 CommandLine.
>    **`--cwd` 가 이 저장소가 아니면 이 저장소의 재고와 무관하다**(세션 66 §3.7).
>    **★ 계통 이름으로 판정하지 마라 — 삭제 범위와 창의 비교가 더 빠른 증거다.**
>    **★★ `04:07:12Z` 의 PID 17432(부모 `codex.exe`)가 살아 있으면 죽이지 마라.**
> 2. "UTCNOW=$([DateTime]::UtcNow.ToString('yyyy-MM-ddTHH:mm:ssZ')) RET=[$env:CTR_SHADOW_RETENTION] SRC=[$env:CTR_RETENTION_SOURCE]"
>    — 게이트. **RET 이 336h 여야 한다.** 비어 있으면 즉시 재처방이다. SRC=pwsh-profile 은 정상.
>    **★ UTC 날짜를 여기서 받아라 — 확인 5 의 오늘 칸 판정이 그것에 걸린다.**
> 3. **훅 중복 재발 검사**:
>    Select-String -Path "$env:USERPROFILE\.claude\settings.json" -Pattern 'context-router hook' -SimpleMatch
>    **0건이어야 한다.**
> 4. context-router stats — 회수 줄 여덟 칸이 t0 와 같은가.
>    기준선: calls=49 legacy=49 나머지 여섯 칸 0, age_s 전부 없음.
> 5. **손실 감시 — doctor [15] hashes · [14] sources · CAS blob 일별 분포를 함께 본다.**
>    **고정값은 07-21~08-13 일곱 칸이고 그 합은 487 이다. 2026-08-15T08:37:52Z 기준:**
>    07-21 **116**(고아) · 08-08 **15** · 08-09 **82** · 08-10 **130** · 08-11 **9** ·
>    08-12 **86** · 08-13 **49** · 08-14 **290** · **08-15 155**.
>    **하한: blob ≥932 · hashes ≥816 · sources ≥818.** 크면 정상이다.
>    **★ 08-14·08-15 칸은 고정값이 아니다** — 그 칸의 blob 을 다시 읽으면 재포착돼 오늘 칸으로
>    옮겨 간다(판정표 둘째 행, 정상). **손실 판정은 일곱 칸(487)으로만 하라.**
>    **★★ 08-08~08-11 네 칸(236개)은 아직 72h 대상이고 배치가 남았다** — 336h 를 못 받은 기동이
>    **이 저장소를 cwd 로** 한 번만 더 있으면 100개가 또 빠진다.
>
>    분포는 doctor 가 안 낸다. 직접 센다:
>    Get-ChildItem 'C:\Users\js\AppData\Local\context-router\projects\context-router-4d307dacd75b\artifacts' -Recurse -File
>    을 LastWriteTimeUtc 의 MM-dd 로 Group-Object 한다.
>    **판정 규칙 넷:**
>    - 오늘 이전 칸이 X 줄고 **총계 세 축도 함께 줄었다** → **삭제.** 확인 1 의 계보와 커맨드라인.
>    - 오늘 이전 칸이 X 줄었는데 **총계 불변/증가이고 오늘 칸이 X 늘었다** → **재포착 mtime
>      이동. 정상, 조치 없음.**
>    - 오늘 이전 칸 불변, 총계 증가 → 순수 신규. 정상.
>    - **★ 08-22 부터**: 줄어든 칸이 **오늘로부터 14일 이상 된 날짜**면 → **정상 336h 만료다.
>      조치 없음.** 그보다 최근 칸이 줄었을 때만 사고다.
>    정합 검산: **blob 총수 − 116(고아) = hashes = 일별 칸 합**, sources = hashes + 2.
>    **이 등식은 총계가 얼마든 성립해야 한다 — 깨지면 그때가 사고다.**
>    **★ 등식은 손실에도 안 깨진다** — 손실 판정은 고정 칸이 하고, 등식은 계상 오류가 한다.
> 6. context-router doctor 의 [17] commit — 077b4acc8fa1 인가.
>    **★ `[21] purges` 가 없는 것이 정상이다** — 설치 전이다. **나오면 누가 설치한 것이고 계측이
>    오염됐다. 즉시 소유자에게 보고해라.**
>
> ## 포착 수치를 읽는 법
>
> - **출발점은 834**(2026-08-15T08:37:52Z, hook:shadow 834회 54,403,355 B, span 끝 08:36:54Z
>   — **하한이다**). **증가분은 1:1 이다** — 세션 64 가 독립 확증했고 65·66 이 재확인했다
>   (세션 66: 원장 +155 : blob +155).
> - **★ 이 수의 78.5%는 Edit 응답이다.** 판정에 쓸 때 아래 「판정일 규칙」의 걸음 4를 반드시 밟아라.
> - **★ 이 수가 빨리 는다고 놀라지 마라.** 16 KiB 넘는 도구 응답은 전부 포착된다 — Read 뿐 아니라
>   **Edit 도** 매번 파일 크기만큼 올린다. **판정 지표는 이 수가 아니라 회수 줄이다.**
> - **세션 62 이전 수치와 직접 비교하지 마라** — 2026-08-14T09:10Z 를 경계로 계상이 바뀌었다.
>
> ## 그다음 — 열린 작업이 없다
>
> **확인 여섯이 초록이고 소유자가 축을 지시하지 않으면 재계수 → 레코드로 짧게 닫는다.**
> 백로그는 전부 판정 뒤 후보다(세션 66 §6): 전체 검토의 defer 아홉 · 훅 진단 이월 넷 ·
> 선존 관측 둘(`dropsByReason` 이 `sc.Err()` 를 안 읽는다, `sql.Rows`/`Scanner` 의 `Err()` 누락).
> **여유가 있으면 소유자에게 물어라 — 스스로 열지 마라.**
>
> **★★★ 확인 5 가 손실을 내면 그것이 최우선이다.** 그때는 ① 어느 진입점이었는지 ② **그 진입점의
> `--cwd` 가 이 저장소였는지** ③ 이 세션이 직접 불렀는지를 확정해 소유자에게 보고해라.
>
> ## ★★★ 판정일 규칙 — 세션 64 가 미리 확정했다. **그날 정하지 마라.**
>
> **왜 미리 박는가**: 축 A 가 포착의 **78.5%건 / 87.6%B 가 Edit 응답**임을 밝혔고, 그 사실은
> *"소비 0"* 의 **해석**을 바꿀 수 있다. 규칙을 판정일에 정하면 수치를 보고 규칙을 맞추게 된다.
>
> **판정일에 이 다섯을 이 순서로 하고, 순서를 바꾸지도 줄이지도 마라:**
>
> 1. **회수 줄 여덟 칸을 그대로 적는다.** D104 가 읽는 것은 이 칸들이고 **착수 조건의 분모는
>    `resolved_artifacts + missed` 다**. **★ 포착 수로 그 분모를 갈아 끼우지 마라** — 포착 수가
>    답하는 것은 *"얼마나 포착되는가"* 이고 창의 길이가 묻는 것은 *"포착된 것이 회수되는가"* 다.
>    **다른 질문의 분모다.**
> 2. **포착의 구성비를 판정일 시점으로 다시 계산해 옆에 적는다.** 방법:
>    `context-router session export --project context-router-4d307dacd75b --worktree <각각>` →
>    `tool_result_summary` 의 `<도구명>: <크기>B` 를 도구별 집계. **CLI 라서 `ctr_*` 원장을
>    안 건드린다.** worktree 는 7개이고 주 worktree 가 압도적이다. 세션 64 값은
>    Edit 78.5%건/87.6%B · Read 13.5%/7.6% 였다(t0 이후 520건).
> 3. **결정표에서 행을 고른다.** 여기까지는 규칙대로다 — 해석을 섞지 마라.
> 4. **★ 그 행의 처방을 채택하기 전에 전제를 한 번 검사하고, 결과를 레코드에 적는다:**
>    *"이 처방은 「모델이 회수를 안 한다」를 전제하는가? 그렇다면 포착의 78%가 Edit 응답이라는
>    사실이 그 전제를 약화시키는가?"* — **답이 무엇이든 적는다.**
> 5. **그 뒤에** 채택 레버 스펙을 읽는다. 먼저 읽으면 그 문면이 `ctr_fetch` 를 권해 판정 지표를
>    오염시킨다. **판정 전에는 이 저장소에서 그 스펙을 열지 마라.**
>
> **4를 건너뛰면 *"채택 미달"* 과 *"채택 대상이 애초에 희소"* 가 같은 수로 보인다 — 처방이
> 정반대다.** 전자는 레버를 넣고, 후자는 **포착을 줄인다**(Edit 매처 제외 등).
>
> ## 판정일에 할 일 — 그 순서 뒤에
>
> - **★★ 설치한다.** 축 C 가 머지된 채 대기 중이고, 그날 설치 한 번으로 `purge.log` 가 살아난다.
>   그때부터 *"어느 기동이 336h 를 못 받았나"* 가 `grep '72h0m0s/'` 한 줄이 된다.
> - **★ 스펙을 다음 설계 버전에 D-number 로 흡수한다**(스펙 §7). 계획 파일은 유래 기록으로 남는다.
> - **★ 설치 직후 `doctor [21]` 을 한 번 본다** — 그 기동 자신의 행이 첫 줄로 나와야 한다.
>   안 나오면 배선이 안 도는 것이고, 그것이 이 기능의 스모크 테스트다.
>
> ## 그 밖의 상태
>
> - **★★★ 336h 대상이 0개인 것은 2026-08-22 까지다.** 08-08→08-22 · 08-09→08-23 · 08-10→08-24 ·
>   08-11→08-25 · 08-12→**판정일** · 08-13→08-27 · 08-14→08-28 · 08-15→08-29.
>   **★ 판정일의 336h 대상은 약 236개다**(검산: 15+82+130+9). 그 삭제는 **정상 정책 동작이지
>   사고가 아니다.** **창은 336h 그대로 — 소유자 판정 완료, 닫힌 결정이다.**
> - **소비는 여전히 0**(`ctr_fetch`·`ctr_search`·`ctr_session_summary` 전부 t0 이후 0회,
>   일곱 세션째). 결정표 행 2 로 갈 공산이 크다 — 판정일에 회수 줄을 인위적으로 올리지 마라.
> - **프로브 blob 둘**(`0fc3bc88…`·`3884b8a0…`, 각 16,461 B, 08-14 칸)은 세션 63 계측의 산물이다.
>   **지우지 마라** — CAS 파일만 지우면 DB 행이 dangling 이 된다. **2026-08-28 에 정상 만료.**
> - **`CHANGELOG.md` 의 `[Unreleased]` 에 셋이 쌓였다** — F10 · PR #47 · 퍼지 감사 로그.
>   **새 기능을 머지하면 그 절에 함께 적어라.**
> - **코드베이스 전반은 세션 64 가 읽고 보고했다. 축 C 코드는 세션 66 이 다섯 리뷰로 훑었다.
>   같은 것을 다시 읽지 마라** — 재고만 는다.
> - F12·F13·트라이그램 축·고아 blob 116개·두 계상 이력은 그대로다. **이 머신에 `sqlite3` 가 없다.**
> - **마감 직전에 프로세스와 재고를 다시 세고 레코드에는 그 수를 적어라.** 세션 61·62·64·66 이
>   마감 직전에 새 서버를 잡았다. **그리고 마감은 커밋으로 끝나지 않는다** — 커밋 뒤에 재고를
>   움직이면 다시 세고 두 번째 커밋을 남겨라.
>
> ## 밟으면 안 되는 자리
>
> - **336h 를 못 받은 채 서버를 띄우지 마라.** 확인 2 가 그 게이트다.
> - **★★★ 계보는 라벨이고 커맨드라인이 식별자다**(세션 66 §3.7). codex/ChatGPT 계보를 보면
>   조상들의 `CommandLine` 까지 봐라 — `--cwd` 가 "누가 띄웠고 어느 프로젝트인가"를 답한다.
> - **★★★ "영향 없음"의 *이유*를 확정하기 전에 안심하지 마라.** 같은 관측(손실 0)이 두 설명
>   (336h 를 받았다 / 애초에 이 프로젝트를 안 본다)을 가지면 **그 관측은 둘 중 어느 것의 증거도
>   아니다.**
> - **★★★★ 문서를 고쳤다고 그 문장이 복사된 자리가 고쳐진 것은 아니다**(세션 66 §3.5 ②).
>   스펙·계획·코드·CHANGELOG 는 같은 주장을 네 벌 든다. 정정할 때 네 벌을 다 세라.
> - **★★★ 픽스처의 대칭이 단정을 무력화한다**(§3.5 ④). 구분하려는 두 값이 픽스처에서 같으면
>   그 단정은 아무것도 못 묻는다. **다르게 만들고 그 전제 자체를 단정해라.**
> - **★★★ 배리어를 만들 수 있으면 논증하지 마라**(§3.5 ③). 완료를 관측 가능하게 만드는 자국이
>   있으면 그것을 기다려라 — 논증보다 싸고 여기서는 더 빨랐다.
> - **★★ 인용은 원문을 열어야 인용이다**(§3.6.1). 계약을 근거로 대려면 그 절을 읽어라. 테스트가
>   초록인 것은 주장의 그림자이지 주장이 아니다.
> - **★ 테스트가 통과한다고 그것이 무언가를 잠근다고 읽지 마라.** 한 번 뒤집어 빨개지는지 재라.
>   Edit 둘과 테스트 한 번이면 관측이 된다. **이 세션에 네 번 물었다.**
> - **★ 완료 알림의 진단이 보고와 모순되면 직접 확인해라** — 세션 66 에서 한 번은 RED 국면의
>   낡은 스냅샷이었고 한 번은 알림 둘이 서로 모순됐다(사라진 중간 커밋).
> - **★ 게이트 결과를 옮겨 적지 말고 직접 돌려라.** 세션 66 은 커밋마다 아홉 번 직접 돌렸다.
> - **재고 감시를 회수 줄로 하지 마라** — 판정 지표가 t0 와 글자 그대로 같은 채로 재고의 3분의 1이
>   사라질 수 있다. 세션 58~60 이 그래서 200개를 못 봤다.
> - **감시 칸을 날짜로 고정하지 마라** — "오늘 이전"이 규칙이다. **그리고 "오늘 이전 칸이 줄면
>   퍼지"로 단정하지 마라** — 재포착 mtime 이동과 08-22 이후의 정상 만료가 같은 모양으로 보인다.
> - **순감과 삭제량을 같은 수로 쓰지 마라** — 견고한 것은 순감뿐이고 삭제량은 하한이다.
> - **삭제를 로그로 찾지 마라** — MCP 서버 stderr 는 어디에도 안 남는다(**설치 뒤에는
>   `purge.log` 가 답한다**). **삭제 시각을 파일시스템 자국으로 역산하지도 마라.**
> - **INH 를 상속 경로의 증거로 읽지 마라** — 그 표식을 설정하지 않는 부모에서 잰 것만 관측이다.
> - **CTR_SHADOW_RETENTION 에 14d 를 쓰지 마라** — ParseDuration 에 d 가 없어 조용히 72h 다.
> - **회수 줄을 인위적으로 올리지 마라.** 검증 회수는 판정 대상이 아닌 프로젝트에서 한다.
> - **판정 대상이 될 수 있는 프로젝트에서 채택 레버의 스펙·계획을 읽지 마라** — 그 문면이
>   ctr_fetch 를 권한다.
> - **가드를 만나면 우회하지 마라** — 그 한 칸은 사람에게 넘겨라. `Grep head_limit=0` 은 거부된다
>   (D54 가드) — `head_limit` 을 지정해 다시 물어라.
> - stats·doctor 는 퍼지를 돌리지 않는다(serve 기동만 돈다). 훅도 안 돈다.
>   **16 KiB 미만 도구 출력은 포착되지 않는다** — 감시 명령 한 바퀴가 재고를 안 늘린다.
> - go test 에 -p 1 필수. 게이트 다섯을 && 로 잇지 마라 — internal/exec 는 이 머신의 PowerShell
>   실행 정책 때문에 빨갛다(환경 조건이지 회귀가 아니다). 관측된 이름:
>   TestNpmrcScratchConfigWins · TestRunShellPS51ProviderWriteReturns · TestD78SignalOnExit ·
>   TestD78NoObservableSideEffects.
> - 행 번호를 주석·문서에 적지 마라 — 심볼 이름을 적어라.
> - **프로세스 목록은 그 순간의 생존자만 보여준다** — 지나간 기동은 안 잡히고 다가올 기동도 안
>   잡힌다. 마감 직전에 그 자리에서 다시 세라.
>
> 문제 발견되면 fix 혹은 브레인스토밍하여 사용자에게 질문하고, 문제 없으면 진행한다.
> ultrathink
