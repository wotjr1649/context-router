# Session 39 — v0.13.0(D73~D76) 구현 실행 · 최종 리뷰 · 머지·태그 (2026-07-27~28)

1. **Session/span/model**: session-39, 2026-07-27 시작 → 07-28 마감. 컨트롤러는 Opus 5(1M,
   max effort). 서브에이전트: 구현 sonnet 6개(태스크 1·2·2b·3·4·5) + 최종 fix sonnet 2개,
   태스크 리뷰 opus 4개 + 스코프 재리뷰 sonnet 4개, 최종 통합 리뷰 opus 1 + 최종 게이트 opus 1
   + 확인 재리뷰 sonnet 1. Codex 크로스모델 6패스(4 integrated · 1 unavailable · 1 재시도 성공).

2. **Starting prompt (verbatim)** — session-38 §8을 그대로 받았다. 세션 중 사용자 입력은
   `/model`·`/andrej-karpathy-skills:karpathy-guidelines` 슬래시 커맨드와 아래 둘,
   그리고 `AskUserQuestion` 6문(§3 참조)이었다.

   > `docs/prompts` 최신 기록(session-38)을 읽고 재개. **v0.12.1은 완전히 닫혔다** — 구현
   > 6태스크, 태스크별 리뷰, 최종 전체 브랜치 리뷰, PR 다각도 리뷰(서브 5렌즈 + Codex 2패스),
   > CI 3-OS, 머지, 태그까지 전부 끝났다. **main = `16e7643`, tag `v0.12.1`,
   > `productVersion` 0.12.1, PR #26 MERGED.** 작업 트리에는 추적하지 않는 로컬 설정
   > (`.claude/`·`.codex/`)만 있다.
   >
   > **첫 작업 = v0.13 실행**: `docs/superpowers/plans/2026-07-27-v0.13-d73-d76.md`
   > (6태스크 45스텝)를 **superpowers `subagent-driven-development`**로 돈다 — 태스크별 fresh
   > 서브에이전트, 태스크 리뷰(스펙+품질), fix → 재리뷰. BASE는 원장에 기록하고 `HEAD~1`을
   > 쓰지 않는다. **코드 변경이 생기므로 브랜치를 먼저 만든다.** 계획이 남긴 미확정 둘을 그
   > 태스크 안에서 닫는다(Task 2b 측정 수치는 §3에 실측 기록, Task 3 Step 8c는 먼저
   > `exec_test.go:890`을 읽고 그 A/B 형태를 따른다).
   >
   > **확정된 v0.13 설계 결정 네 개를 되돌리지 말 것**: ① 배송 2단 ② D73 `SchemaVersion`
   > **1 유지** + 색인은 버전 스위치 밖 ③ D76 응답 필드 없이 `stderr` 한 줄(실측 13,008B가
   > 게이트 11의 13,000B를 넘긴다) ④ D75 `exec` 계층 공유 헬퍼.
   >
   > (이하 크로스모델 주의 ⓐⓑ·감시선 증거 요구·리뷰 체크포인트 규약 — §5 정정과 §7 참조)

   세션 중 사용자 입력 둘:

   > 해당 에이전트만 실행하고 대기한다. 다시 "continue ultrathink" 로 재개하라고 신호를 주겠다.

   > continue ultrathink

3. **What was done**

   브랜치 `feat/v0.13-d73-d76` → **15커밋** `d510bbb`..`30fc556` → main에 **fast-forward 병합**
   → tag **`v0.13.0`** → 푸시. PR 없이 직접 병합(사용자 승인).

   - **Task 1 (D74)** 기동 purge 파일 회수 루프의 취소 전파: `d510bbb` + 리뷰 fix `678f522`
     (F1-F4 — 예산-소진 분류·잠금 대기 취소 전파 포함).
   - **Task 2 (D73)** shadow 보존 술어에 복합 색인 3개 + doctor `[3]`에 `indexes=<n>/3` 병기:
     `395399f` + fix `c79b054`(F1·F4). **`SchemaVersion`은 1 유지**, 색인은 버전 스위치 밖에서
     매 writable Open에 적용 — 이전 버전 바이너리도 이 스토어를 계속 연다.
   - **Task 2b (D73 §2⑦⑧)** 기동 예산 게이트 + 술어 전후 벤치: `0b9d507` + fix `6d63eb6`(F1-F6)
     + `78d828e`(§3 백분율 정정, 컨트롤러 직접).
   - **Task 3 (D75)** 홈 유도 격리 공유 헬퍼 + unix shell 적용: `689294f`·`cbc2314`(자체 리뷰)
     + fix `0934913`(F1-F5 — unix shell에 dotnet first-run 억제를 `csEnv`와 공유).
   - **Task 4 (D76)** PowerShell 네이티브 종료를 `stderr` 한 줄로: `c77d6b5` + fix `e97dc6a`
     (F1-F3 — doctor 안내 정정·tail try/catch 가드·사이드파일 마커).
   - **Task 5** CHANGELOG `[0.13.0]` 절 + `productVersion` 0.13.0 + **설계 §4에 v0.14 후보 4건**:
     `887c477`.
   - **최종 통합 리뷰**(main..`887c477`, 13커밋) → approved · release_ready · minor 2 →
     `386667c`(D76 안내 조사 오류 + 주석 줄 포인터 26곳).
   - **최종 게이트**(fix 검증 + Codex 통합) → **important 1 + minor 1 · release_ready false** →
     `30fc556`(G1 사이드파일 읽기 상한 30B, G2 기동 회수 취소 분류 순서) → 확인 재리뷰 clean.

   **실측 (설계 §3에 기록)**:

   | 항목 | 값 |
   | --- | --- |
   | 기동 예산(색인 생성) | 2,000행 7.21/8.08ms · 20,000행 29.54/29.70ms — 5s의 0.14~0.59%(독립 재실행 0.26~1.04%) |
   | 술어 2,000행 | 색인 없음 202.9ms → 있음 4.88ms (**×41.5**) |
   | 술어 20,000행 | 색인 없음 25.58s → 있음 48.62ms (**×526**) |
   | 10배 행수 스케일링 | 색인 없음 ×126.0(거의 제곱) · 있음 ×10.0(거의 선형) |

   측정 조건·미측정 축은 §3에 함께 적혀 있다(전부 단일 프로세스, 격리된 `SELECT`만,
   artifact당 sources 5개 균일 픽스처).

   **사용자 결정 6건(`AskUserQuestion`)**: Task 2 F1 = 단정 강화 · Task 2 F2 = park 후 2b 판정 ·
   Task 2b F1 = 픽스처에 artifact 차원 추가 후 재측정 · Task 2b F6 = 게이트 범위를 §3에 명시
   (하네스 확장 안 함) · Task 3 F1 = dotnet 설정을 shell 전용 `extra`에 두고 `csEnv`와 공유 ·
   릴리스 마감 = main 병합 + 태그 + 푸시.

4. **Repo state**

   - `main` = **`30fc556`**, tag **`v0.13.0`**(annotated, 같은 커밋), `productVersion` **0.13.0**.
   - 열린 PR 없음. 활성 브랜치 없음(`feat/v0.13-d73-d76`는 main에 흡수됨, 로컬에 남아 있음).
   - 작업 트리: 추적하지 않는 로컬 설정 `.claude/`·`.codex/`만.
   - CI: main·tag 두 run이 `30fc556`에서 시작됨(id 30280575960 · 30280573984). **결과는 §5 정정
     또는 다음 기록에서 확인할 것** — 이 기록 작성 시점에 in_progress였다.
   - 다음 작업 BASE = `main`(`30fc556`).

5. **정정 — session-38 크로스모델 관측 갱신** (과거 기록 수정 금지 규약에 따라 여기에)

   - session-38 §8 ⓐ는 "`review --background`가 잡 레지스트리에 항목을 만들지 않는다 → 서브에게
     `status`/`result`를 시키지 말고 컨트롤러가 출력 파일 경로를 직접 넘겨라"였다. **이번 세션에서는
     레지스트리에 항목이 생겼고 6패스 중 4패스를 서브가 정상 회수했다.** 다만 `result <id>`가
     "No job found"를 내는 경우가 있었고, 그때는 **레지스트리 저장 레코드 또는 job의 `logFile`을
     직접 읽는 fallback**이 통했다. → 절대 규칙이 아니라 **fallback을 함께 지시하는 것**이 정답이다.
   - **실패한 Codex 패스는 "체크포인트당 1회"로 세지 않는다.** 최종 통합 리뷰에서 `unavailable`로
     빠진 job이 실제로 important(G1 무제한 읽기)를 담고 있었고, 재시도가 그것을 찾았다. 즉 그
     누락은 실질적 손실이었다.
   - Task 2 1차 Codex 패스도 결과를 못 냈으나 **같은 형태로 재시도해 성공**했다(차이는
     `run_in_background` 하나뿐 — 원인 특정은 하지 못했고 관측으로만 남긴다).

6. **Carryovers**

   - **설계 §4 v0.14 후보 4건(이번 세션 추가)**:
     ① windows shell 갈래의 cold `USERPROFILE` — dotnet first-run 억제 없음, `827fe15`(v0.12 D65)
     발원이고 D75는 unix 갈래만 다뤘다.
     ② 색인 DDL의 non-context `Exec`과 잠금 경합 — §3 ⑦은 단일 프로세스만 쟀다.
     `migrate`/`applySchemaV1`도 `tx.Exec`(non-context)이라 **둘을 함께 결정한다**.
     ③ `PurgeHookOnlyOlderThan`의 실 잠금 보유 시간 재측정 — `main.go`의 632·633ms는 색인 도입
     **이전** 스토어의 값이다.
     ④ D76 사이드파일 마커 위조 — `ctr-exit-code:`가 고정 리터럴. 새 권한은 아니고 `exit_code`도
     불변이라 정보 정확성 문제다.
   - **CI 3-OS가 unix/macOS 경로의 유일한 검증 권위** — D75 테스트 3개와 D76 stdout 단정이
     Windows에서 skip된다. 이 릴리스에서 그 축은 로컬 미검증으로 배송됐다(§3에 명시).
   - **G2에 자동 감시선이 없다** — 기동 회수 취소 분류 switch가 익명 goroutine 안에 인라인이고
     실제 store 타이밍에 결합돼 있으며, 저장소 원칙("zero self-defined interfaces")상 주입 가능한
     fake가 없다. 결정적 테스트는 flaky 레이스 또는 순수 분류 함수 추출(범위 초과)을 요구한다.
     재리뷰가 "실용적으로 불가"로 판정했고 정적 switch 순서 추적으로 대체했다.

7. **Standing protocols** (유지 + 이번 세션 실증)

   - **감시선이 "실제로 물리는 것"을 보여주는 증거를 요구하라** — session-38이 남긴 이 지침이
     이번에 **네 번** 값을 만들었다: Task 1 F1(첫 실패 정지 회귀를 잡지 못하는 테스트) ·
     Task 2 F1(같은 결함의 재발) · Task 2b F2(벤치 두 arm이 자기 색인 상태를 단정하지 않아
     §3 수치의 근거가 없던 것) · Task 4(감시선을 갱신하면서 약화시키지 않게 정확한 문면 고정).
     **"검사를 추가했다"와 "그 검사가 물린다"는 다르다**가 이 릴리스의 지배적 결함 부류였다.
   - **계획이 지정한 것도 틀릴 수 있다** — Task 2b의 벤치 픽스처는 계획이 verbatim 지정했고,
     그대로 두면 "색인을 넣었는데 유일한 측정이 더 느렸다"는 상태로 배송될 뻔했다. 픽스처를
     현실적 형태로 고치자 ×41.5~×526이 나왔다. **측정이 전제를 반증하면 픽스처를 의심하되,
     유리한 결과가 나올 때까지 만지지 말고 BLOCKED로 올려라**(그렇게 지시했고 발동하지 않았다).
   - **brief와 코드 주석의 줄 참조는 드리프트한다** — 매 태스크 dispatch 전에 검증했고 매번
     정정이 필요했다(Task 4에서 `cli.go:1463`→`1483` 등). 코드 주석도 같은 문제였고 최종 리뷰가
     26곳을 잡았다. **리뷰어가 준 정정 번호도 검증하라** — 이번에 2건이 틀렸다.
   - **컨트롤러는 fix를 직접 하지 않는다.** 이번 예외 1건: 설계 §3 백분율 한 줄 정정(`78d828e`) —
     검산을 이미 마쳤고 재실행할 테스트가 없었고 구현자 재개 비용이 276K였다. 그 외 모든 코드
     변경은 서브 위임.
   - **서브가 API 오류로 죽거나 자기 background 작업을 기다리며 턴을 끝낼 수 있다** — 둘 다
     겪었다. read-only 진단(HEAD·`git status`·해당 패키지 테스트)으로 실제 상태를 확인하고
     확증한 사실을 전달해 재개시키면 재검증 비용이 사라진다. `SendMessage` 결과가 `resumed`인지
     `queued`인지가 **에이전트 생존 여부의 단서**다.
   - 유지: `go test -p 1` / 메모리 캡 / `gofumpt -l .` 무출력 / **`git add -A` 금지** /
     BASE를 원장에 기록(`HEAD~1` 금지) / 스펙(설계서) 승 / 보안 서술 최소화 + 중립 프레이밍 +
     JSON findings(`docs/codex-secure-review.md`) / 리뷰 체크포인트 = 서브 + Codex 1패스,
     재리뷰는 서브 단독 / CI 3-OS가 unix 실검증 권위 / **머지·태그는 사용자 명시 승인**.

8. **Next-session starting prompt** (이 프롬프트로 부팅한다)

   > `docs/prompts` 최신 기록(session-39)을 읽고 재개. **v0.13.0은 머지·태그까지 닫혔다** —
   > 구현 6태스크(2b 포함), 태스크별 리뷰 + fix + 재리뷰, 최종 전체 브랜치 리뷰, 최종 게이트
   > (Codex 통합), 확인 재리뷰, main ff 병합, tag `v0.13.0`, 푸시 완료. **main = `30fc556`,
   > tag `v0.13.0`, `productVersion` 0.13.0, 열린 PR 없음.** 작업 트리에는 추적하지 않는 로컬
   > 설정(`.claude/`·`.codex/`)만 있다.
   >
   > **첫 작업 = CI 결과 확인.** `gh run list`로 `30fc556`의 두 run(main push · tag push)
   > 결론을 확인하라. **이 릴리스에서 unix/macOS 경로는 로컬 미검증으로 배송됐다** — D75 테스트
   > 3개와 D76 stdout 단정이 Windows에서 skip되므로 CI 3-OS가 그 축의 유일한 검증이다. 실패가
   > 있으면 그것이 최우선 작업이고, 초록이면 세션-39 기록에 결론을 추가 커밋하라.
   >
   > **그다음 = v0.14 범위 결정.** 설계 `docs/context-router-design-v0.13-ko.md` §4에 이번
   > 릴리스가 남긴 후보 4건이 파일·심볼 참조와 함께 기록돼 있다: ① windows shell cold
   > `USERPROFILE`(`827fe15`/v0.12 D65 발원) ② 색인 DDL non-context `Exec`의 잠금 경합 축
   > (`migrate`/`applySchemaV1`도 동형이므로 **함께** 결정) ③ `PurgeHookOnlyOlderThan` 실
   > 보유 시간 재측정(`main.go`의 632·633ms는 색인 이전 값) ④ D76 마커 위조(정보 정확성).
   > §4의 기존 6항목도 함께 읽고 브레인스토밍 → 설계 → 계획 순으로 간다.
   >
   > **v0.13 설계 결정을 되돌리지 말 것**: D73 `SchemaVersion` **1 유지**(색인은 가산적
   > 변경이므로 이전 바이너리도 스토어를 연다) · D76은 응답 필드 없이 `stderr` 한 줄(게이트 11
   > 여유 86B) · D75 홈 앵커 3개는 `homeIsolationEnv`만 소유하고 러너 고유 키는 각 러너가 유지 ·
   > D76 사이드파일 읽기 상한 30B(마커+16, 여유를 좁게 둔 이유가 상한 회귀 테스트가 `Atoi`
   > 오버플로 대신 상한에 막히도록 하는 것이다 — `exec.go`의 해당 상수 주석 참조).
   >
   > **이 세션이 네 번 실증한 실패 형태를 기억할 것**: 검사를 "추가했다"는 것과 그 검사가
   > "실제로 물린다"는 것은 다르다. 감시선·게이트·벤치 arm을 만드는 태스크에는 **그것이 깨지는
   > 것을 한 번 보여주는 증거**(회귀를 유도해 FAIL을 캡처하고 원복해 PASS를 캡처)를 요구하라.
   > 그리고 **계획이 지정한 픽스처·인터페이스도 틀릴 수 있다** — Task 2b의 벤치 픽스처가 그랬고,
   > 고치지 않았으면 "색인을 넣었는데 측정은 더 느려졌다"로 배송될 뻔했다.
   >
   > **크로스모델 리뷰**: 레지스트리 회수가 대체로 되지만(6패스 중 4) `result <id>`가 "No job
   > found"를 낼 수 있다 — 서브에게 **`logFile` 직접 읽기 fallback**을 함께 지시하라. **결과를
   > 못 낸 패스는 1회로 세지 말고 재시도하라**: 이번에 최종 게이트에서 그 재시도가 important를
   > 찾았다.
   >
   > 리뷰 체크포인트는 프로젝트 규약대로 서브에이전트 + Codex(1패스/체크포인트, 재리뷰는 서브
   > 단독). 막히면 웹+Codex+서브 협업, `go test -p 1`, `git add -A` 금지, 머지·태그는 사용자
   > 명시 승인. ultrathink
