# Session 09 — v0.2 scope fixed, design (D21–D28) + implementation plan, double adversarial review rounds

- **Span:** 2026-07-20
- **Model:** Fable 5 (ultrathink). Subagents: web-research (opus, docs-layout
  conventions), 2× adversarial reviewers (opus — design doc, then plan).
  Codex: 2 `adversarial-review` passes (design checkpoint + plan checkpoint —
  one per checkpoint, guard respected). Both codex runs exceeded 300s and ran
  as background tasks (fine for in-repo focus; see §6).
- **One line:** v0.2 scope decided (Option A "강제 채널", exec split out),
  design doc v0.2 written and hardened through a 17-item adversarial round,
  implementation plan (T0–T11) written and hardened through a second 23-item
  round, docs directory convention formalized, all committed to main.

## 1. Starting prompt (verbatim — original Korean, do not translate)

```
context-router v0.2 준비를 진행한다. 먼저 docs/prompts/2026-07-20-session-08-v0.1.0-smoke-v0.2-scope.md(최신 기록)를 정독한다. 상태: v0.1.0 실호스트 스모크 GREEN(stdio+codex 경로), NOTICE oracle 해소, ctr.exe fresh.
1. 기록 §5의 v0.2 스코프 브레인스토밍을 이어받아 superpowers:brainstorming으로 마무리한다 — §5 미결 질문 5개를 한 번에 하나씩 사용자에게 묻고(추천: 옵션 A "강제 채널", exec 3종 분리), 스코프 확정 후 설계 문서(D21~)를 작성한다.
2. 설계 승인 후 writing-plans로 구현 계획을 만든다(부채 10건+doctor 순서 nit의 편승 배치 포함). 필요 시 plan 체크포인트에서 Codex adversarial-review 1패스(in-diff focus).
3. 여유가 되면 --profile ingest로 fetch 정상 경로 스모크 1회(§4.2).
4. 세션 종료 시 docs/prompts에 session-09 기록 작성·push.
메모리 캡 테스트 규칙(-p 1) 준수. Fable 유지, 보안 서술 최소화(fable-security-prose-minimization) 준수. ultrathink
```

Mid-session user directives: (a) explain/justify spec location, research
directory management via web+subagent, then review; (b) adversarial-review the
design with codex+subagent, fix, then proceed to writing-plans; (c) same
adversarial treatment for the plan, then wrap the session (chosen over
starting execution).

## 2. What was done

**Scope (5 answers, one at a time):** ① Option A + exec 3종 분리(roadmap §7
개정) ② Claude Code 먼저 ③ 수동 측정 먼저(무작위 A/B 하네스·OTel 후속) ④ 계측
9종 전부 — 기준서 §6.2의 15종 − 모델 기록 6종 = tool_call, tool_result_summary,
artifact_created, file_edit, git_diff, build_run, test_run, error, warning
("9종"의 열거 원본은 없었음; 이 파생이 정본이 됨) ⑤ spill journal v0.3 이월 +
v0.2는 fail-open drop 카운터.

**Design doc:** `docs/context-router-design-v0.2-ko.md` (D21–D28, §1–§13) —
훅 아키텍처(`context-router hook` 단명 프로세스), cc: 세션 식별(§2.4 개정),
fail-open+deadline, 매핑표, large-read guard, Shadow Recall, `ctr usage`,
`hook install`, 부채 편승 배치, 게이트. vision §7 roadmap 개정(exec 3종·Codex
훅 → v0.3+, gate 문면 완화).

**Docs layout (user challenge → research → fix):** why-docs/ explained
(precedent + skill override clause + approved path); web research (superpowers
author intent, spec-kit, ADR/KEP/Go/Rust conventions, Diátaxis, feral-specs)
confirmed the 2-tier split is canonical. Applied: `docs/superpowers/Specs` →
lowercase `specs` (git mv), CLAUDE.md "Docs layout" section added (living
canonical `docs/*.md` vs dated immutable `docs/superpowers/specs|plans/` vs
`docs/prompts/`).

**Adversarial round 1 (design checkpoint):** subagent 12 + codex 5 findings,
merged, all load-bearing claims verified against code before accepting.
Key confirmed defects fixed: `session.Open()` unusable for hooks (no external
id, plain INSERT, auto session_start) → hook-only append API; `runInline` has
no caps/denylist → Shadow Recall self-defends (`CTR_SHADOW_MAX`, denylist
consult); wait-sum can exceed hook timeout → deadline budget contract; guard
4-condition predicate (`Indexed==1`, out-of-boundary/partial-read pass);
summary allowlist assembly; SessionStart-only session creation + unknown-drop;
settings merge atomicity; `context-router` (not `ctr`) is the binary;
store-root env contract; hook-session retention 30d. Rejected with recorded
rationale (§13): IPC/capability source auth, full path/command stripping,
shadow default-off.

**Plan:** `docs/superpowers/plans/2026-07-20-context-router-v0.2-forced-channel.md`
— T0 [plan 검증]+fixtures, T1/T2 debt(6), T3 session `OpenAppend`+
`ValidateEvent`, T4 hook skeleton, T5 mapping+allowlist, T6 Shadow Recall,
T7 guard, T8 install/doctor, T9 usage, T10 integration gates, T11 real-host
smoke(+`--enable ingest` fetch smoke + manual A/B). Self-review caught 3 gaps
(계측 흡수 부채 4건 → T10 Step 3 등).

**Adversarial round 2 (plan checkpoint):** subagent 11 + codex 12 findings.
Confirmed and fixed: E2E harness (`buildCtrBinary`/`spawnCtr`) is package-main
private → T10 moved to `cmd/context-router/main_test.go` + one-shot exec
helper; deadline injection swapped (exclusive lease → nonblocking instant
fail; now `BEGIN IMMEDIATE`/init-lock/open-lock occupancy); **Append has no
limit validation** (mcp-only) → `session.ValidateEvent` 이관; EnsureSession
single-tx atomicity; `ingest.Report.Hash` (URI 조립 불가였음);
`ingest.DeniedFilename` already public (DeniedPath alias dropped); T9 reuses
`readTranscriptLine`/`runStatsProvider`; dispatchCLI tests per task;
`--store-root` explicitness via main→cli signature extension; uninstall exact
token+marker match; `CTR_HOOKS_OFF` stdin drain; T0 settings.local.json probe
removed (→T11 real-host assumption); ctx-aware waits for init/open locks.
Design back-propagation ×2: guard boundary = projectRoot-only for v0.2
(amended §4), smoke command `--enable ingest` (not `--profile ingest`).

**Commits (main):** f1b7b66 design+roadmap → e4bdd00 docs convention →
d4f45ab design review round → 9948640 plan → 0fed0a4 plan review round
(+ this record).

## 3. Current repo state

- `main` @ 0fed0a4 (+ this record's commit), pushed. Tag v0.1.0 unchanged.
  No branches, no open PRs. Untracked local: ctr.exe, ctr-new.exe (smoke
  artifacts).
- Design v0.2 + plan are review-hardened and ready for execution. Codex ran
  `go test -p 1` across packages during review: all GREEN on Windows at 0fed0a4.

## 4. Carryovers / next work

1. **Execute the v0.2 plan** (T0→T11) via superpowers:subagent-driven-development
   on branch `feat/v0.2-forced-channel`. T0 first (hook API doc verification
   → fixtures). Codex usage: task reviews are subagent-only; integration/final
   checkpoints get subagent+Codex parallel (1 pass each).
2. fetch happy-path smoke + tool_response truncation 실측은 plan T11에 폴딩
   (session task로는 종결).
3. Manual A/B protocol: first measurement attempt in T11; ongoing measurement
   is post-v0.2 routine.

## 5. Standing protocols (delta only — rest as session-08 §6)

- **Adversarial review rounds (this session's pattern, worked well):** per
  checkpoint — launch opus subagent reviewer (background Agent) + codex
  `adversarial-review` (background Bash; in-repo focus still takes 5–10min,
  >300s timeout is normal — start `run_in_background` from the outset).
  Merge both, **verify every load-bearing claim against actual code before
  accepting** (multiple review claims each round needed code-level
  confirmation; a few were partially wrong — e.g. exclusive-lease injection
  idea came from one reviewer and was refuted by the other's nonblocking-lock
  observation). Record adopted/rejected-with-rationale in the design doc §13.
- **Codex verdicts are advisory**: both rounds returned NO-SHIP verdicts whose
  blockers were real but fixable in-document; fix and move on, no re-review
  loop (usage guard).
- Plan-location convention now codified in project CLAUDE.md "Docs layout" —
  canonical design docs live in `docs/`, dated process records in
  `docs/superpowers/specs|plans/` (lowercase).

## 6. Next-session starting prompt (paste-ready)

```
context-router v0.2 구현을 실행한다. 먼저 docs/prompts/2026-07-20-session-09-v0.2-design-plan.md(최신 기록)를 정독한다. 상태: 설계 v0.2(D21~D28)·구현 계획 모두 이중 적대 검토 반영 완료, main 0fed0a4+ GREEN.
1. superpowers:subagent-driven-development로 docs/superpowers/plans/2026-07-20-context-router-v0.2-forced-channel.md를 실행한다 — 브랜치 feat/v0.2-forced-channel, T0(훅 API 문서 검증+픽스처)부터 순서대로, 태스크당 신규 구현 서브에이전트 + 태스크 리뷰. BASE는 SDD 원장 기준.
2. 통합 체크포인트(T10 Step 4)에서 서브에이전트+Codex review --base 병렬 1패스. T0에서 훅 API 사실이 계획 가정과 다르면 중단하고 보고.
3. 진행 한도까지 실행 후 docs/prompts에 session-10 기록 작성·push.
메모리 캡 테스트 규칙(-p 1) 준수. Fable 유지, 보안 서술 최소화(fable-security-prose-minimization) 준수. ultrathink
```
