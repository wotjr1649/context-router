# Session 40 — v0.14(D77~D79) 범위 결정 · 설계 · 구현 계획 (2026-07-28)

1. **Session/span/model**: session-40, 2026-07-28 하루. 컨트롤러는 Opus 5(1M, max
   effort). 서브에이전트 7(전부 opus): ctxscribe 전환 격차 분석 1 · 설계 검수 5라운드 ·
   계획 검수 1. Codex 3패스(설계 1R·5R, 계획 1R — 전부 결과 회수 성공).
   **구현은 착수하지 않았다**(사용자 지시로 다음 세션).

2. **Starting prompt (verbatim)** — **이번 세션은 session-39 §8의 부팅 프롬프트로
   시작하지 않았다.** 사용자 입력은 슬래시 커맨드 둘(`/model`,
   `/andrej-karpathy-skills:karpathy-guidelines ultrathink`)이었고, 컨트롤러가 프로젝트
   `CLAUDE.md`의 "Session handoff — do this first" 규약에 따라 `docs/prompts`의 최신
   기록(session-39)을 읽고 그 §8을 스스로 적용해 재개했다. **다음 세션이 "§8 프롬프트가
   전달되어 작동했다"고 오독하지 않도록 이 사실을 남긴다** — 규약이 프롬프트 없이도
   재개를 성립시켰다는 관측이다.

   세션 중 사용자 입력 셋(원문 그대로):

   > 코덱스와 서브에이전트로 적대적 검수, 검토, 자문, 리뷰를 한뒤 문제 발견되면 fix혹은 브레인스토밍하여 사용자에게 질문하고 문제 없으면 진행한다. 언제쯤 Ctxscript에서 context-router로 전환할 계획과 완성도가 갖춰지는지와 남은 계획들도 설명 요청한다. ultrathink

   > 스펙을 코덱스와 서브에이전트로 적대적 검수, 검토, 자문, 리뷰를 한뒤 문제 발견되면 fix혹은 브레인스토밍하여 사용자에게 질문하고 문제 없으면 writing-plans로 구현 계획 작성에 들어간다. ultrathink

   > subagent-driven-development로 구현은 다음 세션에서 진행한다. 이번 세션은 계획 설계후 핸드오프까지 작성까지 이다. ultrathink

   그 밖에 `AskUserQuestion` **17문**으로 범위·설계·실행 방식을 결정했다(§3).

3. **What was done**

   커밋 **1개**: `5b3dc0b` (main에 직접, 문서 전용). 브랜치·PR 없음.

   - **v0.14 범위 결정** — 설계 v0.13 §4의 미결 10건에서 시작해 다섯을 골랐고,
     검수가 둘을 기각해 **셋으로 확정**했다.
   - **설계서** `docs/context-router-design-v0.14-ko.md`(563행) — D77 purge 실 잠금
     보유 시간 재측정+게이트 · D78 `.ps1` 종료 신호 `Register-EngineEvent` 승격+미니
     파서 폴백 · D79 lint 미검출 세 축(darwin·**netbsd**·perf).
   - **구현 계획** `docs/superpowers/plans/2026-07-28-v0.14-d77-d79.md`(1234행, 7태스크).
   - **ctxscribe → context-router 전환 격차 분석**(§5에 요약).

   **검수가 설계를 어떻게 바꿨나** — critical 추세 5 → 2 → 1 → 1 → 1:

   | 라운드 | 무너진 전제 | 결과 |
   | --- | --- | --- |
   | 1 | 취소 전파의 근거 둘 · dotnet 배너 미재현 · `param` 정규식 미탐 · "tail이 항상 덮는다" | 결정 둘 기각, 정규식 → 미니 파서 |
   | 2 | "purge는 advisory lock을 쥐지 않는다"(컨트롤러 오독) · `try/finally`가 문 종료 오류 흐름을 바꿈 | 승격 수단 교체 |
   | 3 | `Register-EngineEvent`가 작업표를 오염시켜 `Wait-Job` 미종료 · 줄 번호 +1 · `dynamicparam` 누락 | `-SupportEvent` + 인라인 배치 |
   | 4 | 인라인의 줄 번호 보존은 **2행 이상 오류에서만** · `-Force` 없는 `Unregister-Event`는 실패 | 한계 명시, 단정 두 갈래 |
   | 5 | **`GOOS=freebsd`는 빌드 자체가 깨짐** · D77 픽스처가 유예 경로만 재는 종이 게이트 · 임계 여유 1.58배 | netbsd로 교체, 경로+규모 조건 |

   **기각한 둘**(§4에 근거와 함께): ① `OpenContext` 취소 전파 — 기존 색인의
   `CREATE INDEX IF NOT EXISTS`는 쓰기 락 경합에서도 **14ms/nil**이고, 200ms 데드라인
   ctx로 불러도 반환까지 5.03s로 **오류 분류만** 바뀐다(busy handler가 interrupt를
   무시한다). ② windows dotnet first-run 억제 — `USERPROFILE`만 스크래치로 준 실행에서
   **배너가 나지 않았다**. v0.13 §4의 서술은 코드 이력을 동작 근거로 오인한 것이었다.

   **D78의 승격 수단은 세 번 바뀌었다**: `try/finally`(문 종료 오류 뒤 나머지 스니펫을
   건너뛴다 — 종료 코드가 같아 검출되지 않는다) → `Register-EngineEvent` →
   `-SupportEvent`(없으면 `Get-Job | Wait-Job` 미종료) → 인라인 `; ` 결합(앞 줄에 두면
   모든 오류의 줄 번호가 +1).

   **계획 검수(1라운드)가 잡은 것** — critical 4·important 7·minor 4, 전부 반영:
   기존 `TestShellNativeExitAugmentation`의 세 단정이 **D78 정상 구현에서 반드시
   깨지는데** 계획이 "PASS여야 한다"고 적어 구현자를 D78 무효화로 보냈다(P1) ·
   perf lint 스텝의 `run:` 값이 따옴표로 시작해 **ci.yml 전체가 파싱 오류**(P2) ·
   `runPS1`이 호출마다 새 TempDir을 만들어 stderr 대조가 **정상 구현에서도 무조건
   FAIL**(P3) · `git stash` 유도 FAIL이 성립하지 않고 무관한 stash를 푼다(P4) ·
   타임아웃 부재로 회귀 유도 시 10분 매달림(P5) · 미니 파서가 FF·VT·NBSP와 단독 CR을
   놓쳐 미탐(P13).

   **사용자 결정 17건**(`AskUserQuestion`): v0.14 축 = v0.13 잔여 비대칭 마감 · 범위 =
   후보 다섯 전부 · ⑧ 전파 = `OpenContext` 안 전부 · ⑧ ctx 취소 = Open 실패 · ⑨ = 수치 +
   자동 게이트 · ⑦ = `dotnetFirstRunEnv`만 · ⑩ = nonce → **try/finally**(실측 후 재결정) ·
   ① = 세 축 모두 · **D77·D79 초안 기각 승인** · D80 폴백 = 미니 파서 · **D78 방식 =
   `Register-EngineEvent`** · D77 flaky = 픽스처 축소 · 실행 = 서브에이전트 구동 ·
   계획 검수 = 서브+Codex · 커밋 위치 = main에 문서만 먼저.

4. **Repo state**

   - `main`의 산출 커밋은 **`5b3dc0b`**(설계서 + 구현 계획) 하나이고, 그 위에 이
     핸드오프 문서 커밋들이 얹힌다 — 해시를 여기 고정하면 정정 커밋이 다시 HEAD를
     옮겨 순환하므로 **다음 작업 BASE는 `main`의 최신**으로 읽는다(`git rev-parse main`).
     최신 태그는 여전히 **`v0.13.0`**(`30fc556`).
     `productVersion`은 **0.13.0**이다 — v0.14 릴리스는 계획 Task 7에서 올린다.
   - 열린 PR 없음. **v0.14 작업용 브랜치 없음**(구현 브랜치는 다음 세션이 만든다).
     — session-41(2026-07-28) 갱신: 원문의 "활성 브랜치 없음"은 의도는 맞았으나 표현이
     부정확했다. 실측하면 로컬에 `main` 외 **9개**(`feat/v0.0.1-core` ·
     `feat/v0.0.1-global-cli` · `feat/v0.11-d58-d62` · `feat/v0.12-d63-d67` ·
     `feat/v0.13-d73-d76` · `fix/ci-race-default-timeout` ·
     `fix/default-timeout-upper-bound` · `fix/v0.11.1-exec-runner-hardening` ·
     `fix/v0.12.1-d68-d72`), 원격에 `origin/main` 외 **7개**가 남아 있다(로컬 전용은
     `feat/v0.0.1-core`·`feat/v0.13-d73-d76`). 전부 **과거 완료 작업의 미정리
     브랜치**이고 v0.14와 무관하다 — `feat/v0.14-d77-d79` 생성에는 지장이 없다.
   - 작업 트리: 추적하지 않는 로컬 설정 `.claude/`·`.codex/`만. **미추적이지 무시는
     아니다** — `git add -A` 금지 규약이 그래서 중요하다.
   - **CI는 이 커밋에 대해 확인하지 않았다**(문서 전용 커밋, 푸시 시점에 돈다).
   - 다음 작업 BASE = **`main`의 최신**(설계·계획 `5b3dc0b` + 핸드오프 문서 커밋들).

5. **Carryovers**

   - **v0.14 구현 미착수** — 계획 7태스크가 그대로 남아 있다. §7이 부팅 프롬프트다
     (원문의 "§8"은 이 문서에 없는 절을 가리켰다 — session-41 정정).
   - **ctxscribe → context-router 전환 로드맵**(이번 세션 조사, v0.15+ 범위 결정의 입력):
     코어 5축(저장·검색·회수·변환·격리 실행)은 **동급 이상**이고 `ctr_fetch`(바이트 정확)·
     `ctr_transform`(헤르메틱)은 ctr에만 있다. 배경 훅 캡처도 SubagentStart/Stop·
     PostToolUseFailure까지 잡아 앞선다. 막는 것은 여섯:
     · **B2 압축 복원(대)** — `UserPromptSubmit`·`PreCompact`·`Stop` 훅이 없고
     `SessionStart`가 주입하지 않는다(`hook.go:134-143`). **ctr에 대응 코드가 한 줄도
     없는 유일한 축**이고 완전 종료의 게이트다.
     · B6 검색 품질 3종(중) — proximity rerank·오타 교정·stale 자동 재색인.
     · B5 크로스 세션·프로젝트 회수(중) · B3 `ctr_batch_execute`(중, 3연속 이월).
     · B4 `stats`/`doctor`/`purge`가 CLI 전용(소~중) — 세션 중 모델이 자체 진단 불가.
     · **B1 ingest·net 프로필 미등록(소, 하루치)** — `.mcp.json`이 `--enable exec`만 줘
     `ctr_index`·`ctr_fetch_and_index`가 아예 로드되지 않는다.
     (— session-41 갱신: 등록을 전역 스코프로 옮긴 뒤에도 `--enable exec`만 주었으므로
     **여전히 유효한 carryover**다.)
     비전 D57이 대체 게이트로 지정한 "exec 3종 이식"은 이미 통과했으나, 그 게이트는
     ctxscribe의 가치를 think-in-code 샌드박스로만 좁게 규정해 **압축 복원·batch·검색
     품질을 계산에 넣지 않았다** — 필요조건이었지 충분조건이 아니었다.
   - **로컬 설치 산출물의 버전 드리프트**(설계 §4에도 기록): `.mcp.json`과
     `.claude/settings.json`의 `__ctrManaged`가 `context-router/0.12.0`인데
     `buildinfo.go:11`은 0.13.0이다. **릴리스 후 `hook install` 재실행이 절차에 없다** —
     D64가 self-heal 대상으로 지목한 상태가 실제로 발생해 있다.
     — session-41(2026-07-28) 갱신: **드리프트 상태 자체는 해소**했다. 프로젝트 스코프의
     훅과 `.mcp.json` 등록을 걷어내고 **사용자(전역) 스코프에 0.13.0으로 재설치**했다 —
     Claude는 `~/.claude/settings.json` 훅 6개 + `~/.claude.json`의 MCP 서버
     `ctr-exec`(`--enable exec`, `alwaysLoad`), Codex는 `~/.codex/hooks.json` 훅 3개 +
     `config.toml`의 `[mcp_servers.ctr]`. 저장소 안의 `.claude/settings.json` ·
     `.mcp.json` · `.codex/hooks.json`은 이제 **빈 `{}`**다(미추적 상태는 그대로).
     **그러나 근본 갭은 남아 있다** — "릴리스 후 `hook install` 재실행이 절차에 없다"는
     지적 자체는 그대로 유효하고, Task 7에서 `productVersion`을 0.14.0으로 올리면 같은
     드리프트가 이번엔 **전역 스코프에서** 재발한다. 그래서 계획
     `docs/superpowers/plans/2026-07-28-v0.14-d77-d79.md`의 Task 7에 self-heal 스텝을
     추가했다(session-41).
   - **Codex `config.toml` 마커 밀림**(session-41 추가, 재발형 위험): Codex에서 `/hooks`
     신뢰 승인을 하면 Codex가 `~/.codex/config.toml`을 TOML 파서로 재직렬화하면서
     `# END context-router` 주석을 **파일 최하단으로 민다**. 그러면 BEGIN~END 사이에 그
     뒤의 사용자 테이블이 전부 들어간다. `internal/cli/codex_toml.go`의
     `classifyMarkers`가 BEGIN/END 1쌍을 `classReplace`로 판정하므로, 그 상태에서
     `hook install --codex` 또는 `hook uninstall --codex`를 실행하면 **그 사이가 통째로
     교체·삭제**된다. 2026-07-28 실측: **24개 테이블**(`[mcp_servers.node_repl]` ·
     `[desktop]` · `[marketplaces.*]` · `[plugins.*]` ·
     `[shell_environment_policy.set]` · `[tui.*]` · `[hooks.state]`의 신뢰 해시 13개)이
     관리 블록 안에 들어갔다. `# ingest/net ...` + `# END context-router` 두 줄을
     `[mcp_servers.ctr]` 직후로 되돌려 복구했고, 신뢰 해시 13개와 나머지 테이블은 전부
     보존됐다. 주석 기반 마커는 호스트가 파일을 다시 쓸 때마다 밀리므로 **일회성 사고가
     아니다** → **v0.15+ 후보**로 올린다.
   - **설계 §4의 v0.15+ 후보**: 기각한 둘(근거 없이 다시 올리지 말 것) · 기각한 승격
     수단 넷 · `[Environment]::Exit` 경로 · 폴백 경로 승격 · 첫 줄 오류 열 번호 오염 ·
     조작 경로 방어 · `stderr == ""` 보강 조건 · `worker_unix.go`의 GOOS 이식성 ·
     100해시 절대 상한 검증 · `startupPurgeMaxHashes` 재조정 · v0.13 §4 이월분 ·
     **Codex `config.toml` 마커 방식 교체**(주석 마커 → 호스트 재직렬화에 안전한 표현.
     session-41 추가, 위 항목 참조).

6. **Standing protocols** (유지 + 이번 세션 실증)

   - **검수 초점이 한 곳에 쏠리면 다른 곳이 빈다** — 1~4라운드가 전부 D78(PowerShell)에
     쏠렸고, 5라운드에서 초점을 D77·D79로 **옮기자마자** 구현 차단 문제 둘이 나왔다
     (freebsd 빌드 실패, 종이 게이트). 라운드를 더 도는 것보다 **초점을 옮기는 것**이
     값을 냈다. 다음 검수에서도 "지난 라운드가 안 본 축"을 명시적으로 지정하라.
   - **기존 테스트가 "닫으려는 공백"을 단정으로 굳혀 놓을 수 있다**(새 실패 형태) —
     `TestShellNativeExitAugmentation`이 D76 공백을 세 군데서 단정하고 있어 D78 정상
     구현에서 반드시 깨진다. 계획이 그것을 "무회귀 확인"으로 적으면 구현자가 정상
     구현을 결함으로 오진한다. **공백을 닫는 변경에서는 그 공백을 단정하는 기존
     테스트를 먼저 찾아라.**
   - **코드 이력은 동작 근거가 아니다** — v0.13 §4의 dotnet 항목은 `git log -S`로
     "D65가 `USERPROFILE`을 재지정했고 D75는 unix만 다뤘다"를 확인한 것이었지 배너가
     실제로 난다는 관측이 아니었다. 실측하니 나지 않았다.
   - **계획도 검수 대상이다** — 1라운드가 구현 첫날에 시간을 태웠을 넷을 잡았다
     (기존 테스트 단정 충돌 · ci.yml 전체 파싱 오류 · 정상 구현에서도 실패하는 대조 ·
     성립하지 않는 유도 FAIL). 스펙만 검수하고 계획을 건너뛰지 마라.
   - **"검사를 추가했다"와 "그 검사가 물린다"는 다르다**(session-39 지침의 연장) —
     이번엔 그것을 **계획 단계에서** 요구했고, 유도 FAIL 절차 넷 중 하나가 성립하지
     않음을 검수가 잡았다. 절차를 적는 것으로 끝내지 말고 **그 절차가 정말 FAIL을
     만드는지**를 검수 초점에 넣어라.
   - **Codex 회수**: 세 패스 모두 job id가 발급되지 않았고 결과가 백그라운드 태스크의
     **출력 파일에 직접** 있었다. `status`/`result`로 찾게 하지 말고 컨트롤러가 그 경로를
     `SendMessage`로 넘기는 것이 확실하다. `adversarial-review --help`는 응답 없이
     매달렸다 — 중립 `review`를 쓰고 적대성은 서브에이전트 프롬프트에 부여하라.
   - 유지: `go test -p 1` / 메모리 캡 / `gofumpt -l .` 무출력 / **`git add -A` 금지** /
     BASE를 원장에 기록 / 스펙 승 / 보안 서술 최소화 + 중립 프레이밍 + JSON findings
     (`docs/codex-secure-review.md`) / 리뷰 체크포인트 = 서브 + Codex 1패스, 재리뷰는
     서브 단독 / CI 3-OS가 unix 실검증 권위 / **머지·태그는 사용자 명시 승인**.

7. **Next-session starting prompt** (이 프롬프트로 부팅한다)

   > `docs/prompts` 최신 기록(session-40)을 읽고 재개. **v0.14는 설계·계획까지 끝났고
   > 구현은 착수하지 않았다.** main의 산출 커밋은 **`5b3dc0b`**(설계서 + 계획)이고 그 위에
   > 핸드오프 문서 커밋이 얹혀 있다 — BASE는 `main`의 최신을 쓴다. 최신 태그는 `v0.13.0`,
   > `productVersion` 0.13.0, 열린 PR 없음 · **v0.14 작업용 브랜치 없음**(과거 완료
   > 작업의 미정리 브랜치가 로컬 9 · 원격 7개 남아 있지만 v0.14와 무관하다 — §4).
   > 작업 트리에는 추적하지 않는 로컬 설정(`.claude/`·`.codex/`)만 있다 — 다만 **그
   > 안의 `settings.json`·`hooks.json`은 이제 빈 `{}`**이고 훅·MCP 등록은 사용자(전역)
   > 스코프(`~/.claude`·`~/.codex`)로 옮겨졌다(§5).
   >
   > **선행 도구는 이미 설치돼 있다**(session-41) — `gofumpt v0.10.0` ·
   > `golangci-lint v2.12.2`, CI(`ci.yml`)가 쓰는 것과 같은 버전이고 `GOPATH/bin`
   > (`C:\Users\js\go\bin`, PATH 등록됨)에 있다. **다시 설치하지 마라.**
   >
   > **계획서는 session-41에 두 곳 수정됐다** — ① 커밋 트레일러를 실행 모델명
   > 플레이스홀더로 규칙화 ② Task 7에 호스트 등록물 self-heal 스텝 추가(Codex
   > `config.toml` 마커 선행 확인 포함). **설계 결정은 하나도 바뀌지 않았다.**
   >
   > **첫 작업 = v0.14 구현.** 계획
   > `docs/superpowers/plans/2026-07-28-v0.14-d77-d79.md`(7태스크)를 **superpowers
   > `subagent-driven-development`**로 돈다 — 태스크별 fresh 서브에이전트, 태스크
   > 리뷰(스펙+품질), fix → 재리뷰. BASE는 원장에 기록하고 `HEAD~1`을 쓰지 않는다.
   > **코드 변경이 생기므로 브랜치를 먼저 만든다**(`feat/v0.14-d77-d79`).
   >
   > **스펙은 5라운드, 계획은 1라운드 교차 검수를 받았고 findings가 전부 반영됐다** —
   > 설계 결정을 다시 열지 마라. 특히 되돌리면 안 되는 것 넷: ① `try/finally`(문 종료
   > 오류 뒤 나머지 스니펫을 건너뛴다) ② dot-source(`exit` 종료 코드 오염) ③ 등록 문을
   > 앞 줄에 두기(모든 오류의 줄 번호 +1) ④ `-SupportEvent` 없이 등록(`Get-Job |
   > Wait-Job` 미종료 + stdout 오염). 기각된 둘(`OpenContext` 취소 전파 · windows
   > dotnet 억제)도 §4의 실측을 뒤집기 전에는 다시 올리지 마라.
   >
   > **계획 Task 2 Step 5를 건너뛰지 마라** — 기존 `TestShellNativeExitAugmentation`
   > (`exec_test.go:372-520`)의 세 서브테스트는 D78이 닫는 D76 공백을 단정으로 굳혀
   > 놓아 **정상 구현에서 반드시 FAIL한다**. 그 FAIL을 보고 등록 문을 되돌리면 D78
   > 자체가 사라진다. 계획이 각 서브테스트의 새 계약을 표로 준다.
   >
   > **유도 FAIL 절차 넷은 그대로 밟아 출력을 태스크 보고에 남겨라**(Task 3 Step 2 ·
   > Task 4 Step 3 · Task 5 Step 3·4). "검사를 추가했다"와 "그 검사가 물린다"는 다르다 —
   > 이 저장소가 다섯 번 실증한 지배적 결함 부류다.
   >
   > **리뷰 체크포인트**: 프로젝트 규약대로 서브에이전트 + Codex(1패스/체크포인트,
   > 재리뷰는 서브 단독). Codex는 job id 없이 백그라운드 출력 파일에 결과를 남기므로
   > 컨트롤러가 그 경로를 서브에게 `SendMessage`로 넘긴다. `adversarial-review`는 쓰지
   > 말고 중립 `review` + 서브 프롬프트에 적대성 부여.
   >
   > 막히면 웹+Codex+서브 협업, `go test -p 1`, `git add -A` 금지, 머지·태그는 사용자
   > 명시 승인. ultrathink
