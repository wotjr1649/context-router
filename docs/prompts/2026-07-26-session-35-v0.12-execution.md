# Session 35 — v0.12 실행(T1~T14) + fix wave, PR 대기 (2026-07-25 ~ 07-26)

1. **Session/span/model**: session-35, 2026-07-25 시작 → 07-26. Opus 5(1M, max effort) 단일 모델.
   구현·리뷰·fix 서브에이전트 전부 general-purpose + `model:"opus"` 명시.

2. **Starting prompt (verbatim)** — session-34 next-session 프롬프트로 부팅:
   > docs/prompts 최신 기록(session-34)을 읽고 재개. **v0.11.1 릴리스 완료**(main=`07a8958`,
   > tag `v0.11.1`, 태그 CI success — 도그푸딩이 잡은 csharp 호스트 NuGet 구성 상속 결함을 차단).
   > **v0.12 설계·계획 확정 완료**: 설계 `docs/context-router-design-v0.12-ko.md`(D63~D67, 열린 항목
   > 없음), 계획 `docs/superpowers/plans/2026-07-25-v0.12-d63-d67.md`(T1~T12, 2,205줄, 교차 검수
   > 15건 반영 완료). **다음 = 계획 실행**: superpowers:subagent-driven-development로 **T1부터**
   > 시작한다(태스크별 fresh 서브에이전트 general-purpose는 `model:"opus"` 명시, 태스크 리뷰=스펙
   > 준수+품질, fix→재리뷰, BASE는 원장 기록·`HEAD~1` 금지). **T1이 선행 게이트** — 설계 §1.3이
   > 계측 교정을 상시 로드(T6)보다 먼저로 못박았고, 순서가 뒤집히면 채택 전후 비교의 근거가
   > 사라진다. 실행 순서는 계획 말미 "실행 순서와 의존" 참조(T2→T3→T6, T4·T5·T7·T8·T9·T10 독립,
   > T11→T12). **사용자 액션 대기**: MCP 재기동(`.mcp.json` 단일화·settings ask 제거 반영),
   > `hook install`로 marker 0.11.1 갱신. **관측 후 결정 2건**: D63 ④(SessionStart 안내)와 D67
   > 임계값은 T6·T12 적용 후 정상상태 관측이 선행. 크로스모델 리뷰=중립 프레이밍+JSON findings
   > (`docs/codex-secure-review.md`, `review`는 focus 미지원 → 초점 검토는 `adversarial-review`를
   > in-diff로), 막히면 웹+Codex+서브 협업, CI 3-OS가 unix 실검증 권위(`gh pr checks` 전체 확인),
   > `git add -A` 금지(로컬 설정 untracked). ultrathink

3. **What was done**

   **계획 T1~T12 + 계획 밖 T13·T14 + 최종 fix wave를 전부 실행했다.** 브랜치
   `feat/v0.12-d63-d67`, 32커밋, base `main`(merge-base `ec69381`). 태스크마다 fresh 구현
   서브에이전트 → 태스크 리뷰(서브 + Codex 통합 JSON findings) → fix → 스코프 재리뷰.

   | 태스크 | 결과 | 이월 minor |
   | --- | --- | --- |
   | T1 adoption 계측(D63①) | approved | 4 |
   | T2 `.mcp.json` 병합기(D64) | fix 1회 → clean | 7 |
   | T3 스코프 판정(D64/F9) | approved | 4 |
   | T4 exec 승인 안내(D64) | fix 1회 → clean | 3 |
   | T5 doctor `[19]`(D64) | fix 1회 → clean | 6 |
   | T6 설치 원커맨드(D64) | pre-review 1회 + fix 1회 → clean | 4 |
   | T7 go `GOENV`(D65) | approved | 4 |
   | T8 node/bun·python(D65) | fix 1회 → clean | 7 |
   | T9 `PSModulePath`(D65) | approved | 4 |
   | T10 종료코드 계약(D66) | fix 1회 → clean | 3 |
   | T11 shadow purge 나이 필터(D67) | approved | 2 |
   | T12 기동 자동 purge(D67) | 재배선 + fix 1회 → clean | 4 |
   | T13 CI 툴체인(계획 밖) | approved | 2 |
   | T14 설계서 개정 + 0.12.0 | fix 1회 → clean | 1 |

   **계획·설계의 실질 결함을 실행 전/중에 잡았다 — 이번 세션의 핵심 산출물이다.**

   - **T3 스코프 우선순위가 정반대였다.** 계획이 "높은 것부터: user → project → local"로 열거했는데
     공식 문서는 **local > project > user(최저)**이고 설계 D64도 "더 좁은 스코프"로 같은 방향이었다.
     그대로 구현했다면 T6이 **무시될 스코프에 써서** T3이 막으려는 실패가 그대로 났다.
   - **T4 문면의 배타 서술이 틀렸다.** "무프롬프트 모드를 명시적으로 고른 경우에만 즉시 실행"은
     거짓 — exec를 덮는 allow 규칙이 있으면 default 모드에서도 프롬프트가 없고, 하필 D64가 실측으로
     기술한 환경(ask 2종·allow 1종 공존)이 그 경우여서 **이 변경이 스스로 만드는 상황에서 문면이
     틀렸다.**
   - **T10/D66의 안전망에 구멍이 있었다.** PowerShell에서 마지막 native 명령이 실패하면
     `exit_code` 0 + stderr **0바이트** — "두 채널을 함께 보면 판단 가능"이 성립하지 않고
     `$ErrorActionPreference='Stop'`도 그 부류를 못 덮는다. `-File` 호출형의 고정 성질이다
     (`$PSNativeCommandUseErrorActionPreference` 기본 False + `-NoProfile`).
   - **T12가 사용자 결정 하나를 실측으로 반박했다.** 300ms 예산 + 200해시는 적체 store에서
     **회수 0**이었다(200해시 배치 1.57s, 행 삭제 단일 tx라 초과분 통째 롤백). 정상상태 유지에만
     약 530ms 필요 → 어떤 상한으로도 300ms는 불가. **논블로킹 goroutine으로 이동**해 해결.
   - **T12-F1 반전**: `CTR_SHADOW_RETENTION`을 **늘리면** cutoff가 0 이하가 되고 T11이 그걸 "나이
     필터 없음"으로 읽어 **전량 삭제로 반전**된다. 보수적 설정이 최대 파괴가 되는 형태.
   - **T2·T6·T8·T12 브리프 코드의 panic·빌드 오류**: nil 맵 대입 panic 2건(`.mcp.json`이 `null`일 때,
     `mergeEnabledServers`), raw string을 종료시키는 백틱 1건.
   - **T6의 승인 키 × 소유권**: `enabledMcpjsonServers`는 **이름으로** 사전 승인하므로 남의 `ctr-exec`
     항목 때문에 병합이 실패한 상태에서 키를 쓰면 **그 남의 서버를 자동 승인**한다. 계획은 두 블록을
     독립적으로 기술했다.
   - **최종 리뷰 F1 — T4↔T6 이음새의 세 번째 층**: T6이 안내의 `mcp__ctr__` 접두가 죽었음을 잡아
     접두를 고쳤지만, 접두를 고쳐도 그 두 도구는 예시가 등록하는 `--enable exec` 프로필에 **없다**
     (`ctr_index`는 ingest 전용, `ctr_fetch_and_index`는 net 전용). 두 블록을 복사한 사용자는 무효
     규칙 2개를 얻고 ingest/net이 게이트된다고 믿었다. **태스크별 리뷰가 구조적으로 볼 수 없는
     종류였다.**

   **D65 실증이 실제 구멍을 증명했다.** 설계 §2가 "호스트에 구성 파일이 존재하는 픽스처를 만들어
   스크래치 구성이 이긴다"를 요구했는데 계획의 go 실증 테스트는 env 변수를 되읽을 뿐이었다(파일이
   비어도 통과 = 동어반복). 컨트롤러가 이를 발견해 세 러너 전부 **양방향 픽스처**로 올렸고, T8의 RED
   단계에서 **수정 전 호스트 user site 모듈이 러너 안에서 실제로 임포트됨**이 증명됐다. T7은 미격리 시
   `go: parsing $GOFLAGS: unknown flag -bogusflag`, T9는 `CANARY=HOST-MODULE-LOADED` → `IMPORT_FAILED`.

   **npm과 pip은 정반대다**(계획은 동일 취급): pip은 구성 파일이 **실재할 때만** 사용자 구성을
   건너뛰므로 사전 생성이 **격리의 시행점**이고, npm은 없으면 내장 기본으로 가므로 belt-and-braces다.

   **최종 리뷰 + fix wave(1회)**: verdict **With fixes** — critical 0 · important 5 · minor 12.
   이월 minor 41 + plan_conflict 11을 triage해 5건 승격(+컨트롤러가 1건 추가) → fix wave 5커밋
   `1e072de`..`76f3c13` → 재리뷰 **7건 전부 ADDRESSED, 신규 파손 없음, 머지 가능**.

   **`go test -p 1 -count=1 ./...` 강제 재실행 green** — 14패키지 전부 ok(캐시 아님).

4. **Repo state**

   - **main HEAD=`ec69381`**(변경 없음), tag `v0.11.1` 최신.
   - **브랜치 `feat/v0.12-d63-d67` @ `76f3c13`, 32커밋** → **PR 열림(아래 6번 확인 필요)**.
   - `productVersion` = **0.12.0**(`internal/buildinfo/buildinfo.go:9`, 단일 진실점).
   - working tree: untracked 로컬 설정 2개(`.claude/`·`.codex/`)만 — `git add -A` 금지 규율 유지.
   - 실행 원장(로컬 전용, gitignored):
     `.superpowers/sdd/2026-07-25-v0.12-d63-d67/progress.md` — 모든 결정·이탈·이월 findings가
     `minor (deferred)` 줄로 들어 있다. 태스크별 findings JSON도 같은 디렉터리.

5. **Carryovers**

   - **PR 머지 + 태그가 다음 세션의 첫 작업**이다. `gh pr checks <n>`을 **전체** 확인하고(tail 잘림
     금지) ALL GREEN 후 머지, 그다음 `v0.12.0` 태그 → 태그 CI의 tag-version이 D56을 실검증한다.
   - **CI 첫 실행이 이 브랜치에서 특별히 중요하다**: T13이 bun을 처음 도입했고, **T8의
     `TestRunJSScratchBunfigWins` 단정 절반이 unix에서 처음 실행된다**(Windows에서 벡터 재현 불가라
     "by construction"으로 닫힌 상태였다). **거기서 붉어지면 워크플로 문제가 아니라 bun 격리에 대한
     진짜 발견이다.**
   - **릴리스 후 사용자 액션**: 로컬 `.claude/settings.json`·`.codex/hooks.json` 마커가 `0.11.1`이라
     `hook install` 재실행 전까지 doctor `[9]`·`[16]`·`[17]`이 불일치를 보고한다. 이제 `hook install`이
     `.mcp.json`·승인 키까지 다루므로 그 경로를 실사용으로 확인할 기회다(`--enable-exec` 포함).
   - **릴리스 노트 한 줄 필요**: `PYTHONNOUSERSITE=1`로 호스트 `pip install --user` 패키지가 python
     러너에서 보이지 않게 된 것은 사용자 관측 가능한 동작 변경인데 현재 어디에도 출하 기록이 없다.
   - **관측 기준선이 리셋됐다.** T12 측정 과정에서 shadow 적체가 드레인됐다(1254 → **20** 아티팩트,
     `[15]` 844해시 → **18해시**, `file`은 210,399,232B 불변 = VACUUM 없음이 설계대로). 따라서 실
     store에서 `capped=true` 재현은 더 불가하고 **D67 임계값 관측·D63 상시 로드 전후 비교는 이 시점부터
     새로 쌓아야 한다**. 설계서 §3에 리셋 기록이 들어갔다.
   - **v0.13 이월(최종 재리뷰가 명시)**: ① `ruleMatches`의 allow-glob 대칭(Codex P2, 손으로 작성된
     형태에만 발생·거짓 clean이 부분적이라 이월) ② doctor `[19]` 규칙 목록에 `%q`+길이 제한(Codex P2 —
     복제 저장소가 공급한 `.claude/settings.json`의 제어문자가 터미널까지 가지만, 그 파일은 이미 신뢰돼야
     하고 임의 명령 훅을 등록할 수 있어 더 큰 노출이므로 표시 위조에 한정) ③ `main.go`의
     `if cutoff <= 0 { return }` 줄 무테스트 ④ 안내 `args` 예시 불일치 ⑤ node 인터프리터 레그의 CI
     커버리지(bun 설치로 `detectJS`가 항상 bun을 고름 — `tsRunner().argv` 유닛 테스트 2줄로 닫힌다)
     ⑥ 나머지 이월 minor 약 47건은 원장 참조.
   - **이월 Minor(v0.11)**: `.superpowers/sdd/final-review-findings.json` defer 9건 중 2건은 T1이
     처리했다. 나머지 7건은 여전히 별도 triage 대상.

6. **Standing protocols** (유지 + 이번 세션 실증)

   - **교차 검수·리뷰가 계획 자체의 결함을 잡는다 — 이번 세션이 최대 실증이다.** 계획은 session-34에서
     이미 크로스모델 적대 검수를 통과했는데도 실행 중 스코프 우선순위 역전, 문면 배타 서술 오류, 브리프
     코드의 panic 3건, 예산 산정 오류, 안전망 모델 공백이 나왔다. **실행이 검수가 못 보는 층을 본다.**
   - **컨트롤러가 dispatch 전에 브리프를 읽고 모순을 미리 잡는 것이 값이 컸다** — T5의 doctor `else`
     거짓 clean, T7의 동어반복 실증 테스트, T11의 세 사용처 불변식, T12의 데드라인 강제 범위가 모두
     dispatch 프롬프트에서 선제 지시로 들어갔다.
   - **"거짓 clean"은 이 저장소에서 important다.** v0.11.1이 테스트가 결함을 흡수해 터졌으므로, 진단·
     테스트가 확인하지 않은 것을 "문제 없음"으로 보고하는 형태는 일관되게 important로 처리했다(T5-F1,
     T10-F1, 최종 F5).
   - **서브에이전트 보고는 검증 대상이다.** CRLF 주장 2건 오판(실제는 `i/lf w/lf`), T10의 "안심시키는
     방향" 자기 평가가 리뷰어의 한 걸음 더 측정으로 뒤집혀 구현자가 보고서에서 철회, T12의 열거가
     `internal/mcp`만 훑어 `internal/hook`의 writable `Open` 4곳을 놓침.
   - **크로스모델 리뷰 = 중립 프레이밍 + JSON findings**(`docs/codex-secure-review.md`). 13회 적용
     무사고. Codex가 T2-F1(소유 관문)·T6-F1(--user 전역 승인)·T8-F1(bun bunfig)·T10-F1(PowerShell
     종료코드)·T12 2건을 잡았고, 과대 평가 1건(T6 총평)과 오탐 1건(T11 공유 해시)은 기각·근거 명시.
   - **막히면 협업**: 설계·문서 사실은 공식 문서를 직접 확인(`code.claude.com/docs`)해 `alwaysLoad`
     실재·버전 하한·5s 블로킹, 스코프 우선순위, `enabledMcpjsonServers`의 대상을 확정했다.
   - 유지: `go test -p 1` / gofumpt / **`git add -A` 금지** / BASE 원장 기록 / 스펙(설계서) 승 /
     Codex 1패스/체크포인트 / 보안 서술 최소화 / CI 3-OS가 unix 실검증 권위.

7. **Next-session starting prompt**:
   > docs/prompts 최신 기록(session-35)을 읽고 재개. **v0.12 구현 완료, PR 대기 상태**다 —
   > 브랜치 `feat/v0.12-d63-d67` @ `76f3c13`(32커밋, base `main`=`ec69381`), 계획 T1~T12 + 계획 밖
   > T13(CI 툴체인)·T14(설계서 개정+`productVersion` 0.12.0) + 최종 fix wave까지 전부 리뷰 통과,
   > `go test -p 1 -count=1 ./...` 강제 재실행 green(14패키지). **첫 작업 = `gh pr checks <n>` 전체
   > 확인 후 머지 → `v0.12.0` 태그 → 태그 CI의 tag-version으로 D56 실검증.** **CI 첫 실행이 이
   > 브랜치에서 특별히 중요하다**: T13이 bun을 처음 도입했고 T8의 `TestRunJSScratchBunfigWins` 단정
   > 절반이 unix에서 처음 실행된다 — 거기서 붉어지면 워크플로 문제가 아니라 **bun 격리에 대한 진짜
   > 발견**이다. 머지 후 **사용자 액션**: `hook install` 재실행(marker 0.11.1→0.12.0, doctor
   > `[9]`·`[16]`·`[17]` 불일치 해소, 이제 `.mcp.json`·승인 키까지 다루므로 `--enable-exec` 포함 실사용
   > 확인), 릴리스 노트에 `PYTHONNOUSERSITE=1`로 호스트 `pip install --user` 패키지가 python 러너에서
   > 안 보이게 된 것 한 줄. **관측 기준선이 리셋됐다** — T12가 shadow 적체를 드레인해(1254→20 아티팩트)
   > D67 임계값과 D63 상시 로드 전후 비교는 이 시점부터 새로 쌓아야 한다(설계서 §3에 기록). **v0.13
   > 후보**는 5번 Carryovers 참조(`ruleMatches` allow-glob 대칭, `[19]` `%q`+길이 제한, `cutoff<=0`
   > 무테스트, node 레그 CI 커버리지, 이월 minor 약 47건은 로컬 원장
   > `.superpowers/sdd/2026-07-25-v0.12-d63-d67/progress.md`). 크로스모델 리뷰=중립 프레이밍+JSON
   > findings(`docs/codex-secure-review.md`), 막히면 웹+Codex+서브 협업, CI 3-OS가 unix 실검증 권위
   > (`gh pr checks` 전체 확인), `git add -A` 금지(로컬 설정 untracked). ultrathink
