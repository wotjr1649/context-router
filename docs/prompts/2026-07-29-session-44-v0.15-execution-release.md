# session-44 — v0.15 구현·리뷰·릴리스

## 1. 세션 정보

- **세션**: 44 (직전 = session-43, 설계·계획)
- **기간**: 2026-07-29 → 2026-07-30 (로컬 자정을 넘겼다)
- **모델/노력**: Claude Opus 5 (1M context) / max effort, ultrathink
- **실행 방식**: superpowers `subagent-driven-development` — 태스크별 fresh 서브에이전트,
  태스크 리뷰(스펙+품질), fix → 재리뷰. 원장은
  `.superpowers/sdd/2026-07-29-v0.15-d80-d84/`(**gitignore됨** — 이 기록이 진실 소스다).

## 2. 시작 프롬프트

session-43 §7이 준비한 부팅 프롬프트로 시작했다. 이 세션의 초기 컨텍스트가 압축돼 사용자의
문자 그대로의 첫 메시지는 재현하지 않는다 — 아래가 그 프롬프트이며, 담긴 확정 사항 넷은
세션 전체에서 지켜졌다.

> `docs/prompts` 최신 기록(session-43)을 읽고 재개. **v0.15는 설계·계획까지 끝났고 구현은
> 착수하지 않았다.** `main` = **`f3a00aa`**이고 산출은 커밋 넷이다(`cad900d`·`391910f`·
> `03dfdb8`·`f3a00aa`). **네 커밋 모두 origin에 푸시하지 않았다.** 최신 태그는 `v0.14.0`,
> `productVersion` 0.14.0, 열린 PR 없음, v0.15 작업 브랜치 없음.
>
> **첫 작업 = v0.15 구현.** 계획 `docs/superpowers/plans/2026-07-29-v0.15-d80-d84.md`
> (4,251행, 14태스크)를 **superpowers `subagent-driven-development`**로 돈다. BASE는 원장에
> 기록하고 `HEAD~1`을 쓰지 않는다. **코드 변경이 생기므로 브랜치를 먼저 만든다.**
>
> **스펙은 검수 4라운드, 계획은 3라운드를 받았다 — 설계 결정을 다시 열지 마라.** 되돌리면
> 안 되는 것 넷: ① **등록 이름 정합 철회** ② **승인 모드 키 미기입** ③ **기본 프로필
> `ingest,net`, exec는 opt-in** ④ **`--fix`는 소유가 확인된 등록물만**.
>
> **로컬 red 9건은 기준선이다** — `internal/exec`의 `powershell` 레그. CI windows-latest에서는
> 통과한다. 새 회귀로 오인하지 마라.
>
> **계획의 유도 FAIL 절차를 건너뛰지 마라.** "검사를 추가했다"와 "그 검사가 물린다"는 다르다.
>
> **각 태스크가 끝난 시점에 패키지가 컴파일돼야 한다.**
>
> **리뷰 체크포인트**: 서브에이전트 + Codex. `adversarial-review`는 쓰지 말고 중립 `review` +
> 서브 프롬프트에 적대성 부여(`docs/codex-secure-review.md`).
>
> **편집 도구 이스케이프 오염이 양방향으로 실재한다.** **커밋 blob 기준**으로 확인하라.
>
> **전제를 검증 없이 dispatch에 넣지 마라.**
>
> **`doctor [9]`가 `project=미등록 user=미등록`으로 나온다.**
>
> 막히면 웹+Codex+서브 협업, `go test -p 1`, `git add -A` 금지, 인덱스 상태를 보고 커밋,
> 머지·태그·푸시는 사용자 명시 승인. ultrathink

**세션 중 사용자가 추가로 확정한 것**:

- Codex 사용 축소(태스크 리뷰는 서브 단독) → 이후 **"최종 리뷰 없이 머지 금지"로 강화**,
  T7-F1을 릴리스에 포함, CI 확정까지 대기, Codex 작업 종료·재시작, 문제는 서브+웹으로 근본 해결.
- 릴리스는 **PR 경유**(v0.14 선례).
- P4는 **상태 안내 한 줄 + v0.16 근본 해결**.
- `~/.codex/config.toml`: `[windows] sandbox = "elevated"` **유지**,
  `[mcp_servers.ctr-exec] default_tools_approval_mode` **삭제**.

## 3. 무엇을 했나

**v0.15.0 릴리스 완료.** 브랜치 `feat/v0.15-d80-d84`, **25커밋**(`d3283a5..8bc660d`).
PR **#28** 머지, 태그 **v0.15.0**.

### 세 계층 (D80~D84)

- **계층 1 (Task 2~5) — Codex `config.toml`을 테이블 경계로 관리 (D80).** 근본 원인: 주석 마커는
  Codex가 재직렬화하면 사라진다. `internal/cli/codex_toml.go`를 **라인 스캐너 기반 테이블 경계
  관리자**로 재작성 — 여러 줄 문자열·배열의 열림 상태를 추적해 테이블 헤더만 경계로 잡는다.
  관리 범위는 `[mcp_servers.ctr]`·`[mcp_servers.ctr.env]` **두 독립 구간**, 소유 키는
  `command`·`args`·`enabled_tools`·`env.CTR_MANAGED` 넷, 나머지는 원문 바이트 왕복 보존.
  소유 표식이 재직렬화를 견디는 `env.CTR_MANAGED`로 옮겨졌고 **게이트 ①이 실제 재직렬화 후
  생존을 실측**했다.
- **계층 2 (Task 1, 6~8) — 프로필 일반화와 무버전 마커 (D81·D82).** 설치기 기본 프로필
  `ingest,net`(세 세션 이월된 `ctr_index`·`ctr_fetch_and_index` 미등록의 원인이 기본값이었다),
  `--enable` 플래그, `args`와 `enabled_tools` 동시 산출, 훅 등록물 무버전 마커, 구 마커 하위 호환.
- **계층 3 (Task 9~11) — `doctor [20]`과 `--fix` (D83·D84).** 근본 원인: D82가 버전의 진실 소스를
  MCP 등록물로 옮겼는데 **그것을 읽는 doctor 항목이 없었다**. `--fix`는 **소유가 확인된 등록물만**
  고친다 — 파일 부재도, 등록물 부재도 만들지 않는다.

### 리뷰 — 총 7라운드

| 라운드 | 결과 |
|---|---|
| 태스크 리뷰 14회(서브 단독) | fix 라운드 6회 |
| 최종 브랜치 리뷰(opus) | important 1(X1) + minor 7, 이월 30건 triage |
| fix 웨이브 + 스코프 재리뷰 | X1·T14-F1·날짜 |
| **PR 상세 리뷰(opus)** | important 1(**P1**) + minor 3. **property 검증 30만 입력, 위반 0건** |
| **적대적 리뷰(opus)** | **blocker_found** — 반박 25건 중 2건 성공. **퍼징 34만 실행** |
| **Codex 교차 리뷰** | 주장 6건 중 4건 성립·1중복·1기각. no-ship 결론은 **기각** |
| Codex 결과 통합·검증(opus) | `ready_to_merge`. **근거 최신(stale 0건)** |

### 최종 리뷰들이 잡은 것 — 전부 고쳤다

| ID | 내용 | 커밋 |
|---|---|---|
| **T7-F1** | 공백뿐인 `--enable`이 기존 프로필을 **빈 집합으로 덮었다** | `29bc514` |
| **P1** | `hook uninstall --codex`가 `config.toml`을 **백업 없이** 썼다(D84 조항 미달) | `ec3b6cc` |
| **A1** | `tomlInlineValue` **역순 슬라이스 패닉** — `recover`가 없어 프로세스가 죽고 읽기 전용 `doctor [20]`까지 죽었다 | `4b5e9dd` |
| **A2** | EOF 스캐너 열림 미검사 → 재실행마다 테이블이 늘고(1→2→3) **D84 단일 백업 슬롯이 2회차에 원본을 잃었다** | `4b5e9dd` |
| **P4** | 빈 프로필 등록물에 명시 경로 안내(근본 해결은 v0.16) | `f08d151` |
| **C1** | 여러 줄 문자열 **내용**인 마커가 **미소유 테이블을 소유로** 만들어 install이 덮고 uninstall이 지웠다 | `8bc660d` |

**A1은 이 브랜치가 새로 들여온 코드였고, 서브 리뷰 14회·최종 리뷰·PR 상세 리뷰가 모두 놓쳤다.**
적대적 리뷰가 격리 모듈에 퍼징을 걸어 60초 안에 찾았다.

**C1은 회귀가 아니었지만**(v0.14는 같은 입력에서 오히려 무효 TOML을 냈다) **릴리스가 문서로
주장하는 "미소유 파일은 그대로 반환된다"의 반례**여서 닫았다.

### 배운 것 — 다음 세션이 반복하지 말 것

1. **이월 findings의 포인터를 다음 태스크 dispatch에 넘긴 것이 두 번 결정적이었다.** Task 2의
   이월 판정("오탐 방향 없음")이 Task 3에서 **틀렸음이 드러나 데이터 손실로 승격**됐고, Task 4의
   minor가 Task 6에서 소비자가 생기며 important로 승격됐다.
2. **계획이 축자 지정한 코드에도 결함이 있다.** 계획이 준 상수가 `unused` 린터를 깨뜨렸고
   (컨트롤러의 전역 제약에 lint가 빠져 있었다), 계획이 준 코드가 후행 주석에 속아 TOML 파스
   에러를 만들었다. **전역 제약에 lint를 반드시 넣어라.**
3. **감시선은 유도해 봐야 한다.** 게이트 11이 **물지 않는 검사**였음이 드러났다 — exec만 켠
   8도구(12,914B)를 재는데 상한 13,000이라, D81이 여는 10도구 표면(14,853B)이 상한을 1,853B
   초과해도 통과했을 것이다.
4. **퍼징·property 검증이 리뷰 읽기를 이겼다.** A1(패닉)은 34만 실행 퍼징이, 바이트 보존 성질은
   30만 입력 property 검사가 확인했다. 격리 모듈 복사(스크래치패드) 방식이 리포를 안 건드리고 통했다.
5. **백그라운드 작업의 생존은 도구의 `status`가 아니라 로그 파일 `mtime`으로 판정하라.**
   컨트롤러가 `status`의 `Elapsed 2h · running · Progress`를 live 활동으로 읽어 **2시간을 잘못
   기다렸다.** 실제로는 두 작업이 각각 1~2분 일하고 죽어 있었다.
6. **`--background` 런처를 파이프로 자르지 마라.** `| head -20`이 런처를 죽여 작업이 orphan으로
   끝났다. **파일로 리다이렉트하라.**

## 4. 현재 저장소 상태

- **`main`** = **`8bc660d`** (fast-forward 머지, origin과 동기)
- **태그** = **`v0.15.0`** (annotated, `8bc660d`) — 직전 `v0.14.0`
- **PR #28** MERGED. 열린 PR 없음.
- 브랜치 `feat/v0.15-d80-d84`는 origin에 남겨 뒀다(v0.14 선례와 동일).
- `productVersion` = `0.15.0`, 세 지점(`version` 명령·상수·CHANGELOG) 일치.
- 작업 트리: 추적하지 않는 `.claude/`·`.codex/`만.
- **다음 작업의 BASE = `8bc660d`.**

게이트: `go build ./...` OK · `golangci-lint run ./...` **0 issues** · `gofumpt -l .` 무출력 ·
`internal/cli`·`internal/mcp` 전체 초록 · `internal/exec` red는 알려진 9건뿐 · CI 3-OS 초록.

## 5. 이월

`.superpowers/sdd/`는 gitignore되므로 **아래가 이월의 진실 소스다.**

### 5.1 감지·고침 범위의 어긋남

- **X2**: codex 갈래에서 `[20]`의 감지 술어와 `--fix`의 판정이 **두 방향으로** 어긋나는데 주석은
  일치를 주장한다. 구 BEGIN/END 블록·비표준 `command`·표식 없음을 `[20]`은 "미등록"으로 보는데
  `--fix`는 재기입하고, 구간 밖 충돌에서는 `[20]`이 `--fix`를 권하는데 `--fix`가 거절한다.
  **어느 쪽도 손상 방향이 아니다.**
- **X8·P2**: `--fix`의 두 갈래 범위가 다르다 — `.mcp.json`은 `args`를 보존하지만 `config.toml`은
  프로필에서 다시 조립해 **사용자가 손으로 넓힌 `enabled_tools`가 줄어들 수 있다**. 또
  `.mcp.json` 갈래는 `install=true`라 **은퇴 이름(`ctr`) 항목을 삭제**한다. 문면은 "버전 표식
  드리프트"만 예고한다.
- **X5**: 우리 테이블의 `.env` 외 서브테이블(예: `[mcp_servers.ctr.tools.ctr_execute]`)은 구조적으로
  두 구간 밖이라 `mcpConflict`가 되어 **install·`--fix`가 영구히 기입을 생략**한다.
- **P3**: env 인라인 대입이 여러 줄로 쪼개진 파일에서 `setInlineEnvMarker`가 닫는 중괄호를 못 찾아
  `[20]`은 매 실행 "표식없음" 경고인데 `--fix`는 "무변경"을 보고한다.

### 5.2 Codex 교차 리뷰가 남긴 둘

- **C2**(minor, 손상 방향): `codexKeyName`이 TOML 이스케이프를 해석하지 않아 관리 키의 이스케이프
  표기(`"comm\u0061nd"`·`"CTR_MAN\u0041GED"`)가 사용자 키로 보존되고 정규 키도 함께 기입돼
  **같은 논리 키가 두 번 정의된다**(유효 TOML 입력 → 무효 `config.toml` 기록). `codexKeyName`은
  이 브랜치 신규라 **유효성 축의 새 노출**이나 실사용 트리거가 없다(호스트는 ASCII 식별자를
  이스케이프하지 않는다). **고침 방향**: 따옴표 키에 역슬래시가 보이면 정규화 불가로 판정해
  무변경으로 닫는다(`dup`과 같은 경로). 이스케이프 디코더를 새로 쓰는 것은 D80의 파서 비의존
  원칙과 어긋난다.
- **C3**(minor): 관리 키 넷의 물리 라인이 통째로 재생성되므로 **그 줄의 후행 주석이 사라진다.**
  D80의 왕복 보존은 **키 단위** 약속이라 계약 위반은 아니고 **미명시 손실**이다. 인라인 env
  경로는 이미 값 토큰만 치환한다(`inlineMarkerSpan`·`setInlineEnvMarker`). **고침**은 키별 값
  스팬 판독기가 필요해 범위가 크다 — 대안은 계약 문면에 "우리 키 줄의 주석은 보존 대상이 아니다"를
  명시하는 것.

### 5.3 주석·이름 표류

- **X3**: `codex_toml.go`의 두 열거형(`mcpExistingHeader`·`mcpMarkerAnomaly`,
  `classAppend`·`classAnomaly`) 주석이 후속 태스크가 의미를 바꾼 뒤로 갱신되지 않았다.
  `classAppend`·`classAnomaly`는 **비교 소비자가 없는 반환 전용 값**이다.
  **함께 정리할 것**: `codexSpans.dup`이 이제 두 사유(중복 헤더·EOF 스캐너 열림)를 담는다 —
  이름이 사유를 다 담지 못한다(`anomaly`가 정직하다).
- **X4**: `internal/mcp/mcp_test.go` 헬퍼 주석이 "8-도구 재기준화 경로"로 남아 있다(10도구로 옮겼다).
- `internal/cli/cli_test.go`의 `runDoctor(cli.go:1491)` 인용이 stale하다 — Task 11이 `:1510`으로
  옮겼다. **Task 12가 없애려던 부류가 같은 세션에 재발한 것**이고 Task 12는 `exec.go` 인용만
  범위였다.
- **X7**: CHANGELOG의 doctor 안내 항목이 Codex 절만 적으나 `hostSnippet`의 **Claude Code 절도 네 곳**
  바뀌었다. 또 `doctor`의 미지 플래그 오류 문면 변경과 **P4 안내 추가**도 CHANGELOG에 없다.

### 5.4 테스트 커버리지

- **X6**: v0.14 구 형식(BEGIN/END) 파일이 **순수 함수 수준에서만** 픽스처를 갖고
  `runHookInstallCodex`·`runDoctor --fix`가 그 형식을 읽고 쓰는 **배선은 테스트에 없다.**
  D80의 인수 경로가 실사용에서 처음 도는 릴리스이므로 값이 높다.
- **T4-F5·T5-F2·T8-F1**(동형): bare 루프 + `Fatalf`로 유도 재현 때 뒤 케이스가 가려진다.
  **셋을 묶어 한 번에 `t.Run`으로.**
- **T1-F5**: `enabledToolsForProfiles`의 정적 목록 표류 검사 부재.
- **T7-F2**: `args` null-vs-`[]` 직렬화 구분의 감시선 부재.
- **T3-oos**: 홑따옴표 키 대칭 케이스 픽스처 부재.

### 5.5 D84 v1.0 마커 청산 귀속

- **T4-F2**·**T5-F1**·**T5-F3**: 구 마커가 env 구간 안에 놓인 배치 / 부모 없이 `env`만 남은 파일 /
  `classAnomaly` 파일의 마커 주석 잔존. 셋 다 잔존물이 주석 한 줄이고 D84의 v1.0 삭제로 사라진다.
  **C1이 고친 `classifyMarkers`도 그 절에 속한다.**

### 5.6 설계 항목 (D-번호 필요)

- **P4 근본 해결**: 빈 프로필로 되읽힌 기존 등록물을 기본값으로 하강할지. 이번 릴리스는 **안내
  한 줄**로 닫았다(조용히 넓히면 쓰기·네트워크 도구를 동의 없이 노출한다). 설계서
  `docs/context-router-design-v0.15-ko.md:201-203`이 현재 동작을 **축자로 명시**한다.
- **동시성**: 읽기→변환→백업→rename이 프로세스 간 원자적이지 않다. **v0.14부터 있던 형태**이고
  v0.15 스펙에 동시성·잠금·원자성 진술이 없다. Codex가 D80의 "그 밖의 바이트 보존"(바이트 변환의
  계약)을 쓰기 직렬화의 계약으로 읽었다 — 하려면 새 결정이 필요하다.
- **`-ExecutionPolicy Bypass`**: argv 한 토큰이면 이 호스트의 5.1 레그 red 9건이 초록이 된다(실측).
  **보안 표면 변경이라 D-번호가 필요하다.**

### 5.7 환경 — Codex/호스트 (v0.16 이전에 반드시 읽을 것)

- **로컬 red 9건**은 `internal/exec`의 `powershell` 레그다. **원인이 이번에 정확히 규명됐다**:
  닫힌 BaseEnv 표 자식에서 `PSExecutionPolicyPreference`가 상속되지 않아 전 스코프 Undefined →
  **5.1은 실효 `Restricted`**(즉시 exit 1, `UnauthorizedAccess`, 0.19~0.68초). pwsh 7은 자기
  `powershell.config.json`의 `RemoteSigned`로 정상. **CI가 초록인 건 Server SKU의 Undefined
  기본이 RemoteSigned이기 때문이다.** 목록:
  `TestRunShellPS51ProviderWriteReturns` ·
  `TestD78SignalOnExit/powershell/{explicit-exit,bare-exit,exit-zero,throw,normal}` ·
  `TestD78NoObservableSideEffects/powershell/{job-and-subscriber-tables-clean,wait-job-terminates,remove-job-keeps-sidefile}`.
- **Codex 플러그인(openai-codex 1.0.6)이 이 호스트에서 셸을 못 쓴다**: 컴패니언이
  `codex.mjs:1012`에서 **`sandbox: "read-only"`를 하드코딩**해 사용자 `sandbox_mode`를 덮고,
  Windows 네이티브 샌드박스의 `CreateProcessAsUserW`가 ACCESS_DENIED(`0xC0070005`)로 실패한다.
  `[windows] sandbox = "elevated"`로 바꿔도 **효과가 없었다**(사용자가 elevated 유지 결정).
  → **`review --base <ref>`는 git이 필요해 구조적으로 쓸 수 없다.**
  **작동하는 형태는 `adversarial-review`에 "셸을 쓰지 말고 이 파일을 읽어라"를 명시한 것뿐이다**
  (실제로 그렇게 해서 교차 리뷰를 받았고, Codex는 우리 `ctr_search`/`ctr_fetch`로 저장소를 읽었다).
- **`default_tools_approval_mode = "prompt"`가 걸린 서버는 프로그램이 Codex를 몰 때 무한 대기**한다 —
  컴패니언이 서버→클라이언트 모든 요청을 `-32601`로 되쏘고(승인 핸들러 없음) `approvalPolicy:
  "never"`로 띄우므로 올 수 없는 승인을 기다린다. 대기가 **dispatch 이전**이라 우리 상한도, Codex의
  `tool_timeout_sec`도 걸리지 않는다. **사용자가 그 키를 삭제했다.** 우리 안내 두 곳
  (`hook_install.go`·`cli.go` hostSnippet)에 비대화형 경고를 넣었다(`8bc660d`).
- **컴패니언 `cancel`은 쓸 수 없다**: 기록된 PID가 이미 끝난 런처다. 별개로 Git Bash가 `taskkill`의
  `/PID`를 경로로 변환하는데, **스크립트 경로를 Windows 형식(`C:/...`)으로 주고
  `MSYS_NO_PATHCONV=1`을 함께** 쓰면 그 부분은 해결된다(둘 중 하나만 쓰면 각각 인자 훼손 /
  Node `MODULE_NOT_FOUND`).
- **컴패니언 상태 레지스트리는 `CLAUDE_PLUGIN_DATA`에 의존**하며 그 값이 **Bash 도구엔 있고
  PowerShell 도구엔 없다** → PowerShell에서는 작업이 안 보인다. **Bash 도구에서 띄워라.**
- **`doctor [9] project=미등록 user=미등록`** — Codex 훅은 user 레벨에 등록돼 있으나(`[16]` 확인)
  Claude Code 훅은 미등록이다. session-42의 self-heal이 어느 쪽이었는지 미규명.
- **`.mcp.json`에는 백업이 없다** — D84의 백업 조항이 `config.toml` 단일 슬롯만 정의한다. 넓히려면
  D84 개정이 필요하다.

### 5.8 사용자의 다음 방향 (세션 중 통보)

`D:\AI_DEV\codex-conductor-cc`를 **fork해 자체 플러그인으로 새로 개발**할 예정이다. 위 5.7의
Codex 플러그인 결함 넷(샌드박스 하드코딩·승인 게이트 무한 대기·`cancel` PID·`CLAUDE_PLUGIN_DATA`
의존)이 그 작업의 직접 입력이다.

## 6. 상시 프로토콜

- **막히면** 협업: Codex rescue / 웹·context7. 블라인드 재시도 금지.
- **리뷰 체크포인트**: 서브에이전트 + Codex. 재리뷰는 서브 단독(Codex는 체크포인트당 1패스).
  프로토콜·스키마 → `docs/codex-secure-review.md`. **이 호스트에서는 §5.7의 제약을 먼저 읽어라.**
- **실행**: superpowers `subagent-driven-development`. BASE는 원장에서, `HEAD~1` 금지.
- **서브 위생**: 전체 파일 재작성 금지, 긴 테스트 데이터는 `strings.Repeat`/testdata, 밀집 태스크는
  분할. Go 테스트는 `-p 1`, 메모리 캡 규칙 준수.
- **커밋**: `git add -A` 금지, 인덱스 열 확인. 트레일러
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.
  (프로젝트 CLAUDE.md는 Fable 5를 적지만 이 세션은 Opus 5로 돌았다.)
- **파일 출력**: UTF-8(BOM 없음)·LF. **판정 지점은 커밋 blob** — autocrlf 호스트에서 작업 트리는
  판정 지점이 아니다.
- **머지·태그·푸시는 사용자 명시 승인.**
- 문서 계층: `docs/*.md`(살아 있는 계약) / `docs/superpowers/{specs,plans}`(날짜 붙은 불변 기록) /
  `docs/prompts`(핸드오프, append-only).

## 7. 다음 세션 시작 프롬프트

> `docs/prompts` 최신 기록(session-44)을 읽고 재개. **v0.15.0 릴리스가 완료됐다** — 구현
> 14태스크 · 리뷰 7라운드(서브 + **Codex 교차 리뷰**) · PR #28(MERGED) · fast-forward 머지 ·
> annotated 태그까지 한 세션에서 닫혔다. `main` = **`8bc660d`** = origin, 태그 `v0.15.0`,
> `productVersion` 0.15.0, 열린 PR 없음. 브랜치 `feat/v0.15-d80-d84`는 origin에 남아 있다.
> 작업 트리에는 추적하지 않는 `.claude/`·`.codex/`만 있다. **다음 작업의 BASE = `8bc660d`.**
>
> **첫 작업 = 범위 결정.** session-44 §5가 이월의 진실 소스다(`.superpowers/sdd/`는 gitignore되므로
> 그 원장은 신뢰하지 마라). 사용자가 통보한 다음 방향은 **`D:\AI_DEV\codex-conductor-cc`를 fork해
> 자체 Codex 플러그인을 새로 개발**하는 것이다 — session-44 §5.7의 플러그인 결함 넷이 그 작업의
> 직접 입력이다. context-router 쪽 v0.16 후보는 무게순으로:
>
> 1. **C2 — 이스케이프 따옴표 키가 무효 `config.toml`을 기록한다.** 이 브랜치가 들여온 유효성 축의
>    새 노출이다. 고침은 "정규화 불가 → 무변경"(§5.2).
> 2. **X2·X8·P2 — `[20]`의 감지와 `--fix`의 고침 범위가 어긋난다.** 손상 방향은 아니나 `--fix`가
>    사용자가 넓힌 `enabled_tools`를 줄이고 은퇴 항목을 삭제하는데 문면이 예고하지 않는다.
>    **문면을 넓히든 판정을 맞추든 한쪽으로 정해야 한다.**
> 3. **X6 — v0.14 구 형식 마이그레이션의 배선이 테스트에 없다.** D80의 인수 경로가 실사용에서
>    처음 도는 릴리스이므로 값이 높다.
> 4. **X3 + `dup` 이름 정리 + 테스트 위생 묶음**(T4-F5·T5-F2·T8-F1을 `t.Run`으로).
> 5. **X7·C3 — CHANGELOG 누락과 주석 보존 계약 문면.**
> 6. **설계 항목(D-번호 필요)**: P4 근본 해결 · 동시성 · `-ExecutionPolicy Bypass`(§5.6).
>
> **설계 결정을 다시 열지 마라.** v0.15는 스펙 검수 4라운드·계획 검수 3라운드를 받았고, 사용자
> 확정 사항은 session-43 §3과 session-44 §2에 있다. 특히 **`--fix`는 소유가 확인된 등록물만
> 고친다**(no-create의 범위가 파일이 아니라 등록물)와 **기본 프로필 `ingest,net`, exec는 opt-in**은
> 되돌리지 마라.
>
> **로컬 red 9건은 기준선이고 원인이 규명됐다**(§5.7) — 닫힌 BaseEnv 표 자식에서
> `PSExecutionPolicyPreference`가 상속되지 않아 5.1이 실효 `Restricted`가 된다. **즉시 exit 1이며
> 무한 대기가 아니다.** CI가 초록인 건 Server SKU 기본이 RemoteSigned이기 때문이다. 새 회귀로
> 오인하지 말고 태스크마다 red 집합이 그 9건인지 확인하라.
>
> **Codex 교차 리뷰는 이 호스트에서 `review --base`로 못 한다**(§5.7) — 컴패니언이 샌드박스를
> 하드코딩해 셸이 막히고 git을 쓸 수 없다. **작동하는 형태는 `adversarial-review`에 "셸을 쓰지 말고
> 이 파일을 읽어라"를 명시하고 미리 만든 diff 파일을 가리키는 것뿐이다.** 그리고 **백그라운드
> 작업의 생존은 도구의 `status`가 아니라 로그 파일 `mtime`으로 판정하라** — session-44에서
> 컨트롤러가 stale `status`를 live로 읽어 2시간을 잘못 기다렸다. **`--background` 런처를 파이프로
> 자르지 마라**(`| head`가 작업을 죽였다) — 파일로 리다이렉트하라.
>
> **유도 FAIL 절차를 건너뛰지 마라.** v0.15에서 게이트 11이 **물지 않는 검사**였음이 드러났다.
> 그리고 **퍼징·property 검증이 리뷰 읽기를 이겼다** — 서브 리뷰 14회·최종 리뷰·PR 상세 리뷰가
> 모두 놓친 **패닉(A1)**을 격리 모듈 퍼징이 60초 안에 찾았다. 스크래치패드에 소스를 축자 복사해
> 퍼징·property를 거는 방식이 리포를 안 건드리고 통한다.
>
> **이월 findings의 포인터를 다음 태스크 dispatch에 넘겨라** — v0.15에서 두 번 결정적이었다.
> Task 2의 이월 판정이 Task 3에서 **틀렸음이 드러나 데이터 손실로 승격**됐다.
>
> **계획이 축자 지정한 코드에도 결함이 있다** — 계획이 준 상수가 `unused` 린터를 깨뜨리고 계획이
> 준 코드가 TOML 파스 에러를 만들었다. **전역 제약에 lint를 반드시 넣어라.**
>
> **편집 도구 이스케이프 오염이 양방향으로 실재한다** — v0.15 최종 fix에서도 났다(구현자가 자가
> 검출). **커밋 blob 기준**으로 확인하고 모든 dispatch에 그 검사를 요구하라.
>
> 막히면 웹+서브 협업, `go test -p 1`, `git add -A` 금지, 인덱스 상태를 보고 커밋, 머지·태그·푸시는
> 사용자 명시 승인. ultrathink
