# Session 01 — 기획~계획2 (v0.0.1 코어 + transform/net)

- **기간:** 2026-07-17 ~ 2026-07-18
- **모델:** Fable 5 (xhigh→max) → 세션 후반 사용자가 Opus 4.8(1M, max)로 전환
- **결과 한 줄:** 백지에서 시작해 설계 확정 → 계획 1(코어) main 머지 → 계획 2(transform/net) PR #1까지.

## 1. 시작 프롬프트 (verbatim)

```
docs 의 2개의 문서를 정독한다. 그래서 context-router라는 새로운 mcp, plugin을 만들것이다. 웹검색과 서브에이전트, codex 를 통해 아이디어를 얻는다. 그리고 context-router를 통해 어떤 것을 이루고자 하는지도 제안한다. C:\Users\js\Documents\ClaudeCode\context-mode 도 참고한다. ultrathink
```

(직전 `/model`로 Fable 5 xhigh 설정.)

## 2. 한 일

**기획·설계 (문서, main에 존재):**
- `docs/context-router-vision-proposal-ko.md` — 비전·목표 7개·테제 4개, 결정 D1~D13
- `docs/context-router-tool-audit-ko.md` — 62일 코퍼스 도구 감사 + 외부 증거 + 시장 검증, D10(계약 재정의)
- `docs/context-router-design-v0.0.1-ko.md` — 설계서 M0(HANDOFF 12질문 전체 답변) + 적대 검증 1라운드 반영
- `docs/context-router-code-architecture-ko.md` — 구현 규약(3채널 브레인스토밍 병합)
- 브레인스토밍·적대 검증·리서치에 서브에이전트/Codex/웹 다수 활용.

**계획 1 (v0.0.1 코어, M1~M3) — main에 머지됨:**
- Task 9개: ident·store(스키마/Register CAS/ReadRange)·ingest(redaction 2뷰/청킹/경로)·search(RRF/예산/stale)·mcp(ctr_search/ctr_fetch/ctr_index)·E2E.
- SDD(구현→태스크리뷰→수정→재리뷰) + 최종 whole-branch 이중 리뷰(최상위 모델 + Codex) → 3웨이브 19건 수정 → `--no-ff` 머지 커밋 **`4e32953`**.

**계획 2 (transform + net, M4~M5) — PR #1 (OPEN):**
- Task 6개: transform 엔진(starlark)·worker(OS 메모리 상한, 게이트8 Windows 실측 PASS)·ctr_transform 등록(**기본 3-도구 표면 완성**)·netfetch(SSRF I1~I8)·D12 파이프라인(readability→markdown)·ctr_fetch_and_index+E2E.
- 최종 이중 리뷰(max, 최상위 모델 + Codex `review --base main`) → 병합 13건 3웨이브 수정 → 재검증 Approved → **PR #1**.

**주요 결정 (전부 확정):** D1 프로필 토폴로지 A · D2 ctr_transform(starlark) · D3 exec v0.2 연기 · D4 `ctr_*` 접두사/바이너리 `context-router` · D5 Go 외부 toolchain · D6 stats CLI 전용 · D7 이름 유지+선점 · D8 스택(go-sdk v1.6.1·modernc v1.54.0·starlark·x/sys, 프레임워크 금지) · D9 PRAGMA(WAL/NORMAL/busy5000/FK/user_version) · **D10 계약 재정의**(코어 6 MCP + CLI 4, exec 3종 v0.2) · D11 SDK 현행 유지 · D12 fetch=readability→markdown · D13 반파편화.

## 3. 현재 저장소 상태

- `main` @ **95f32cb** — v0.0.1 코어(계획 1 머지) + 계획 2 설계 문서.
- **PR #1** (https://github.com/wotjr1649/context-router/pull/1) — `feat/v0.0.1-transform-net` → `main`, 14 커밋, **OPEN**(계획 2 전체). 머지 대기(사용자 결정).
- `feat/v0.0.1-core` @ 0bc1cfc — 이미 main에 머지됨(stale, 삭제 무방).
- 전체 8패키지 테스트 GREEN, 네이티브+`GOOS=linux` 빌드 clean.
- **v0.0.1 재정의 계약 진척:** 6 MCP 도구 중 5개(search/fetch/transform/index/fetch_and_index) + 0 CLI. 남은 것 = ctr_global_search + CLI 4종 = **계획 3**.

## 4. 이월 사항 (계획 3에서 처리)

**필수 (v0.0.1 태그 전):**
- `ctr_global_search` (`ctr-global` 별도 read-only 등록, `--projects` allowlist, per-call ask) — 설계 §4.6·§5.4
- CLI 4종: `doctor` / `stats`(two-ledger + Claude Code OTel 실측 대조) / `purge`(TTY+slug 2단 확인 + `--gc` orphan blob GC) / `upgrade`(최소형, 릴리스 host 컴파일타임 상수) — 설계 §6·§7
- **WAL 최초 경합 근본 수정** — 2-프로세스 동시 최초 마이그레이션 SQLITE_BUSY (Session 01 Task 9 발견, 게이트 7 심층)
- **서드파티 NOTICE 고지 절차** — 설계 §10 확장 + `LICENSE`(ELv2) + `README` + 이름 매핑표. readeck go-readability(MIT)·html-to-markdown v2(MIT)·starlark 포함. **태그/배포 전 필수**
- §14 acceptance gate 점검 → **v0.0.1 태그**

**Minor 이월 (묶어서):** transform timeout Config화 · title=readability Article.Title · DNS 다중 주소 재시도 · A3(defer 스코프)·A5(non-html src_hash 디코딩후 기준) 주석 드리프트 정정 · ident `.git` 읽기실패 err prefix · foldIndex 다중룬(ß→ss) 한계 주석 · unix RLIMIT_AS CI 실측(현 머신 CGO 없어 -race/실측 불가) · extraction!="" 좌표 테스트 · applyMemLimit 원인 err `%w` 보존.

**v0.0.1 이후 (별도 마일스톤):** v0.1 세션 이벤트(record_event/session_summary/export) · v0.2 exec 프로필 3종 + Shadow Recall 훅 + provider 무작위 A/B.

## 5. 지켜야 할 프로토콜

- **막히면 협업**: 구현자 BLOCKED/2~3회 실패/수정방향 불확실 → Codex rescue(설계·원인) / 라이브러리 사실 → 웹·context7. 맹목 재시도 금지. (프로젝트 메모리 `stuck-protocol-codex-web`)
- **이중 리뷰**: 통합(멀티커밋)·최종 whole-branch 리뷰 = 서브에이전트 리뷰어 + **Codex `review --base <ref>`** 병행 → 병합 판정 → 수정 라운드. (Session 01에서 두 모델이 서로의 사각을 메운 실증: Codex=userinfo 자격유출·특수대역 SSRF·euc-kr charset / 최상위 모델=스니펫 redaction 우회·worker 레이스.)
- **SDD 실행**: `superpowers:subagent-driven-development` — 태스크별 신규 서브에이전트(구현) + 태스크 리뷰(spec+quality) + 수정→재리뷰. BASE는 원장 기록(HEAD~1 금지).
- **인프라 주의**: 서브에이전트 32K 출력 한도 사망 3회 발생 → **응답 분할 규율**(파일 전체 재작성 금지, 단계별 소규모 Edit, 장문 데이터는 strings.Repeat/testdata) + 밀도 높은 태스크는 분할(예: 계획1 T7 → 7a/7b).
- **캐너리**: 규약 §8 런타임 분할 리터럴.
- **테스트 메모리 cap**: 전역 test-guard 규율(CLAUDE.md §1). Go 테스트는 `-p 1` 권장.
- **커밋 trailer**: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` (모델 전환과 무관하게 프로젝트 일관성 위해 유지 — 다른 트레일러 원하면 다음 세션에서 조정).

## 6. 다음 세션 시작 프롬프트 (복붙용)

```
context-router 계획 3을 시작한다. 먼저 이어받기:
1. docs/prompts/CLAUDE.md 와 docs/prompts 의 최신 세션 기록(2026-07-17-session-01-plan0-1-2.md)을 정독한다.
2. docs/context-router-design-v0.0.1-ko.md(§4.6 global, §6 stats, §7 CLI, §10 라이선스, §14 게이트)와 docs/context-router-code-architecture-ko.md(규약)를 확인한다.
3. git 상태 확인: PR #1(feat/v0.0.1-transform-net → main, 계획 2)이 머지됐는지 본다. 아직이면 사용자에게 머지 여부를 확인하고(머지가 계획 3의 base), 머지 후 main에서 새 브랜치 feat/v0.0.1-global-cli 를 딴다. 같은 머신이면 .superpowers/sdd/progress.md 도 참고.

계획 3 범위(= v0.0.1 재정의 계약 완성):
- ctr_global_search (ctr-global 별도 read-only 등록, --projects allowlist, per-call ask; 설계 §4.6·§5.4)
- CLI 4종: doctor / stats(two-ledger + Claude Code OTel 실측 대조) / purge(TTY+slug 2단 확인 + --gc orphan blob GC) / upgrade(최소형, 릴리스 host 컴파일타임 상수); 설계 §6·§7
- WAL 최초 경합 근본 수정(2-프로세스 동시 최초 마이그레이션 SQLITE_BUSY, 게이트 7 심층)
- 서드파티 NOTICE + LICENSE(ELv2) + README(이름 매핑표) — 태그 전 필수, 설계 §10 확장
- Minor 이월 묶음(session-01 기록 §4) 정리
- §14 acceptance gate 점검 → v0.0.1 태그

방식: superpowers 브레인스토밍은 이미 끝났으니 writing-plans로 계획 3 문서를 쓰고, subagent-driven-development로 실행한다(태스크별 신규 서브에이전트 + 태스크 리뷰 + 수정→재리뷰). 통합/최종 리뷰는 서브에이전트 + Codex review --base main 이중 병행. 막히면 Codex/웹 협업(맹목 재시도 금지). 서브에이전트에는 응답 분할 규율을 상속시킨다. 캐너리는 규약 §8 런타임 분할. 계획 3 완료 시 PR을 올린다. ultrathink
```
