# Session 37 — session-37 관측 · CHANGELOG 신설 · v0.13 설계 + 계획 2건 (2026-07-26~27)

1. **Session/span/model**: session-37, 2026-07-26 시작 → 2026-07-27 자정 넘김.
   Opus 5(1M, max effort) 단일 모델. 검수·통합 서브에이전트 전부 general-purpose +
   `model:"opus"` 명시.

2. **Starting prompt (verbatim)** — 이 세션은 슬래시 커맨드 둘(`/model`,
   `/andrej-karpathy-skills:karpathy-guidelines`)로만 시작해 실질 지시가 없었고,
   프로젝트 CLAUDE.md의 핸드오프 규약("작업 전 `docs/prompts` 최신 기록을 읽고 그
   next-session 프롬프트를 따른다")에 따라 **session-36의 다음 세션 프롬프트로 자동
   재개**했다. 사용자의 실질 지시는 두 번이었다(그 사이 설계 결정 6회는
   AskUserQuestion 응답):

   > 스펙을 코덱스와 서브에이전트로 적대적 검수, 검토, 자문, 리뷰를 한뒤 문제 발견되면
   > fix혹은 브레인스토밍하여 사용자에게 질문하고 문제 없으면 writing-plans로 구현 계획
   > 작성에 들어간다. ultrathink

   > 커밋하고 계획을 코덱스와 서브에이전트로 적대적 검수, 검토, 자문, 리뷰를 한뒤 문제
   > 발견되면 fix혹은 브레인스토밍하여 사용자에게 질문하고 문제 없으면 Subagent-Driven
   > (권장 — 프로젝트 표준): 태스크별 fresh 서브에이전트 + 태스크 + 웹검색으로 다음
   > 세션에서 진행할 수 있게 핸드오프 작성할 것이다. ultrathink

3. **What was done**

   **session-36 next-prompt의 첫 작업(관측)을 끝내고, CHANGELOG를 신설하고, v0.13
   설계서와 배송별 구현 계획 2건을 교차검수까지 통과시켰다. 구현은 시작하지 않았다.**

   **(1) 관측 — D63 ③의 전제가 확인된 상태가 됐다.** `ctr_*` **8도구가 지연 로드 없이
   초기 컨텍스트에 실린다**(대조군 ctxscribe는 9개 중 2개). 호스트 요건도 충족
   (Claude Code **2.1.220** ≥ 요구 2.1.121, `.mcp.json`에 `alwaysLoad: true` + 마커
   `0.12.0`). 서버 프로세스도 이 세션이 처음 0.12.0이다(python 러너에서
   `PYTHONNOUSERSITE=1` 실측). 이 세션의 `ctr_*` 호출 수는 **채택 증거로 쓰지 않았다** —
   과업 자체가 ctr 관측이라 구조적으로 과대 대표된다. 값이 있는 것은 그 구조적 사실과,
   **상시 로드 없이 진행된 session-36이 762 tool_call 중 `ctr_*` 0건**이라는 대조다.

   **(2) `[12] drops worktree=399`는 결함이 아니었다.** 399건 전부 `unknown-session`이고
   발생이 2026-07-21(216) · 07-22(108) · 07-23(75)로 끝난다 — D51(v0.9)이 닫은 문제의
   과거 이력이 append-only 로그에 남은 것이고 이후 0건이다(`hook_test.go`의 "D51 후
   미발생" 단정과 일치). `unparsed`도 0건이다(262건은 2필드 구 형식이고 `dropsByReason`이
   정상 수용한다). **남은 문제는 진단이 누적과 현재를 구분하지 못하는 것**이고 그것을
   D71로 옮겼다. session-36이 이 값을 "적지 않은 수치"로 우려한 것은 정상 상태의 오독이었다.

   **(3) 채택 계측(D63 ① 교정 후 첫 실 데이터).** 1위 버킷 `mcp`(975호출)는 파싱 실패가
   아니라 **ctxscribe의 옛 등록 이름**이었다 — "같은 도구를 다른 등록 이름으로 부른 이력이
   서버 단위로 갈린다"가 ctr/ctr-exec가 아닌 제3의 서버에서 먼저 실증됐다. `ctr-exec`
   누적은 8호출·2세션(7건이 session-34 도그푸딩)이므로 **session-35·36은 0건**이었다.

   **(4) CHANGELOG.md 신설.** 저장소에 출하 기록 채널이 없었다(`gh release list`도 비어
   있고 태그만 씀). v0.12.0부터 기록하고 그 이전은 소급하지 않는다. 최우선 항목은
   **shadow 3일 자동 삭제**다 — doctor `[14]`의 "자동 삭제 없음"이 임계 경고에 붙은 문면일
   뿐 그 보존과 별개 경로임을 함께 적었다(그 구분 없이는 사용자가 자동 삭제를 모른 채
   경고만 읽는다).

   **(5) v0.13 설계서 — 교차검수가 초안의 결론 셋을 바꿨다.** 서브 2(스펙 품질 · 코드
   대조) + Codex `review`·`adversarial-review` → 32건, 이후 재리뷰 4라운드.

   | 초안 | 검수 | 확정 |
   | --- | --- | --- |
   | `SchemaVersion`을 2로 | `case v > SchemaVersion`이 **구 바이너리의 writable Open을 영구 거부**하고, session-36 §8이 릴리스 후 구 프로세스 생존을 관측했다 | **v1 유지 + 색인은 버전 스위치 밖** |
   | 응답 필드 추가는 게이트 11에 무영향 | go-sdk가 응답 타입을 `outputSchema`로 `tools/list`에 싣는다 — 실측 12,914B + 94B = **13,008B > 13,000B** | **응답 필드 없이 `stderr` 한 줄** |
   | `HOME` 재지정은 무비용 | `jsEnv`·`csEnv`에 **이미 사본이 있고**, 빈 홈이 NuGet sentinel을 없애 sh의 dotnet을 실패로 보내며, darwin은 `CFFIXED_USER_HOME`이 필요하다 | **`exec` 계층 공유 헬퍼 + fail-closed** |

   기각 3건도 남겼다(§3 스코프 · `.claude/`는 diff 밖 · 마이그레이션 순서는 호출 그래프로
   이미 보장). 재리뷰 라운드별 critical은 **2 → 1 → 1 → 0**이었다.

   **(6) 구현 계획 2건(배송별) + 교차검수 27건 반영.** 서브 2(계획 품질 · **외부 사실
   10건**) + Codex 2패스. 계획 검수가 잡은 것 중 성격이 다른 것들:
   - **존재하지 않는 헬퍼 4개**(critical): `openStoreT`·`closeStoreT`·`seedShadowFixture`·
     `runDoctorT`를 "이 파일의 기존 헬퍼"라고 적었는데 전부 없었다. 실제는 `openAt` ·
     `s.Close()` · `regSource` 기반 신규 픽스처 · `runDoctor` 직접 호출이다.
   - **컴파일 불가·감시선 파손**(critical): `snippetContent` 3인자화가 기존 호출 4곳을
     깨고, tail이 `TestSnippetPS1BOM`의 접미 단정을 깨며, D76 보강이
     `TestShellNativeExitNotPropagated`의 "stderr 비어 있음" 감시선을 밟는다.
   - **거짓 clean 3**: EQP 판정이 별칭(`sh`·`s2`…) 때문에 색인이 없어도 통과, 게이트 11
     `-run`이 없는 이름이라 0개 실행 후 PASS(실제는 `TestSchemaTokenBudget`), 기동 예산이
     벤치 수동 판독이라 초과해도 통과.
   - **linux 실행 불가 2**: 새 테스트가 `Run(..., selfExe="")`을 부르면 `ErrSetup`이다.
   - **PowerShell 회귀 1**: tail의 `$LASTEXITCODE` 직접 참조가 strict mode에서 stderr를
     오염시킨다(내부 스크립트 경로 포함) → `Get-Variable … -ErrorAction SilentlyContinue`.
   - **외부 사실 10건은 전부 확인됐다**(반증 0). 부수로 **`GOOS=windows` lint의 실제 지적이
     13건이 아니라 4건**임을 실측했다(`realpath_windows.go:32` · `run_windows.go:140` ·
     `worker_windows.go:50·55`).

   **통합 서브가 서브 A의 수정안 하나를 실측으로 교정했다** — EQP 판정을 별칭 이름으로
   바꾸라는 제안은 그대로 쓰면 영구 실패한다(`SCAN sh USING COVERING INDEX …`가 정상
   상태인데 "SCAN"이 들어간다). 최종 판정은 ① 색인 이름 3개가 계획에 나타남 ② `USING`이
   없는 맨 `SCAN`이 없음, 둘이다.

4. **Repo state**

   - **main HEAD = `5622d7c`**. 이번 세션 커밋 5개:
     `0d4c387`(관측) · `6849080`(CHANGELOG) · `2ef4a8b`(v0.13 설계서) ·
     `8e67eec`(계획 2건) · `5622d7c`(계획 검수 27건 반영 + 설계 D76 문면).
   - **푸시하지 않았다.** `git push`가 다음 세션의 첫 액션이다.
   - `productVersion` = **0.12.0**(변경 없음). tag `v0.12.0` 최신.
   - working tree: untracked 로컬 설정 2개(`.claude/`·`.codex/`)만 — `git add -A` 금지 유지.
   - 로컬 원장(gitignored): `.superpowers/sdd/2026-07-26-v0.13-spec-review/`(설계 검수 JSON
     5개: sub-a · sub-b · integrated · rereview-1~4) ·
     `.superpowers/sdd/2026-07-27-v0.13-plan-review/`(계획 검수 JSON 3개).
   - 산출 문서: `CHANGELOG.md` · `docs/context-router-design-v0.13-ko.md` ·
     `docs/superpowers/plans/2026-07-27-v0.12.1-d68-d72.md`(6태스크 36스텝) ·
     `docs/superpowers/plans/2026-07-27-v0.13-d73-d76.md`(6태스크 45스텝).

5. **정정 (과거 기록 수정 금지 규약에 따라 여기에)**

   - **session-36이 적은 "CI 액션 8개"는 재확인에서 고유 7개 · 사용처 13곳이었다.**
     핀은 13곳 전부를 고쳐야 한다.
   - **session-35가 관측한 "미검출 errcheck 13건"은 이 세션 실측에서 4건이었다.**
     설계 D69는 그 수치를 "재현하지 않은 기대값"으로 적었고 계획은 실측값에 위임했으므로
     계약에는 영향이 없다 — 다만 다음 세션은 4건을 기대값으로 쓰면 된다.
   - **파일명 규약 이탈 1건**: session-35 기록이 종료일로 명명됐다. D72가 시작일 규약을
     확정했고 이 기록은 그것을 따랐다(세션 시작일 2026-07-26). 과거 기록은 고치지 않는다.

6. **Carryovers**

   - **첫 액션 = `git push`** (커밋 5개가 로컬에만 있다).
   - **v0.12.1 실행**: `docs/superpowers/plans/2026-07-27-v0.12.1-d68-d72.md` Task 1부터.
     D69(Task 3)가 D68(Task 5)보다 먼저라는 게이트가 있다.
   - **v0.13 실행**: `docs/superpowers/plans/2026-07-27-v0.13-d73-d76.md`. 두 배송 사이에
     순서 의존은 없다.
   - **계획이 남긴 미확정 둘**: ① Task 2b의 측정 수치(기동 예산·술어 전후 — §3에 실측으로
     기록한다) ② Task 3 Step 8c의 "격리 없는 절반" 구성 — `TestRunShellScratchModulePathWins`
     (exec_test.go:890)를 읽고 그 A/B 형태를 따라야 한다.
   - **Codex 잡 레지스트리를 서브에이전트가 조회하지 못했다.** 계획 검수의 통합 서브가
     `result <job-id>`에서 "레지스트리 항목 없음"을 받아 **Codex 두 패스 입력 없이 통합**했다
     (설계 검수 때는 정상 동작했다). Codex 요약은 컨트롤러가 `status`로만 읽어 findings와
     대조했고 겹치는 지점(컴파일 누락·거짓 clean·PowerShell 회귀)이 서브 쪽에도 있었으므로
     이번엔 손실이 크지 않았다. **원인 미확인** — 다음 크로스모델 리뷰에서 재발하면
     컨트롤러가 `status` 요약만 쓰는 형태로 프로토콜을 조정할 값이 있다.
   - **미확인 관측 1**: `session_events`에 `cc:`/`cx:` 접두가 없는 세션 ID(`019f9e…`)가
     있고 Codex가 연 스레드 ID와 형태가 같다 — Codex 훅 경로에서 온 것으로 보이지만
     확인하지 않았다. `[6]`의 `sessions=323 (empty=242)`와 같은 뿌리인지도 미확인이다.
   - **v0.14 후보**: 설계 §4 참조(lint 잔여 미검출 집합 · FTS 인덱스 구성 ·
     `indexed_at` 단독 색인 · doctor `[12]` 로테이션 · `ctr_batch_execute` · AppContainer).
     추가로 **D68의 `go install` 취득 지점을 액션 SHA 핀 + `env: GOOS`로 옮기는 안** —
     한 잡에서 액션 두 번 실행의 캐시 동작과 env 전달을 확인한 뒤에만.

7. **Standing protocols** (유지 + 이번 세션 실증)

   - **fix는 회귀를 만든다 — 재리뷰를 생략하지 말 것.** 이번 세션이 **3라운드 연속** 실증했다:
     1라운드 fix가 충족 불가능한 검증을 계약화(`exit`이면 tail이 안 도는데 "보강 줄이
     나온다"), 2라운드 fix가 코드 미확인 표를 만듦, 3라운드 fix가 한 표현을 두 곳에서
     고르지 않게 수정. **CI 초록은 이 층을 보지 못한다.**
   - **코드를 확인하지 않고 이름을 옮겨 적으면 critical이 된다.** 같은 원인이 이 세션에서
     두 번 나왔다 — 설계 S1~S3(D75 앵커 표)와 계획 F4~F6(테스트 헬퍼 4개). 문서에 식별자를
     적을 때는 그 파일을 열어 확인한다.
   - **서브에이전트 보고는 검증 대상이다.** 이번에도 성립했다 — 통합 서브가 서브 A의 EQP
     수정안을 실측으로 교정했고, 컨트롤러가 서브 B의 `CTR_SHADOW_RETENTION`·`lockStoreCtx`
     실재를 코드에서 재확인했다.
   - **컨트롤러가 설계·계획 문면을 직접 쓴다**(session-36 규율 유지). fix 서브 둘이 같은
     문서를 쓰면 편집이 유실되고, 과대주장은 이 저장소가 이미 물린 지점이다.
   - **크로스모델 리뷰 = 중립 프레이밍 + JSON findings**(`docs/codex-secure-review.md`).
     `adversarial-review`는 초점을 in-diff·중립 공학 용어로 적었고 두 번 모두 정상 종료했다
     (설계 체크포인트 D73 스키마 계약 초점, 계획 체크포인트 `migrate()` 재작성 초점).
   - 유지: `go test -p 1` / 메모리 캡 / gofumpt / **`git add -A` 금지** / BASE 원장 기록 /
     스펙(설계서) 승 / 보안 서술 최소화 / CI 3-OS가 unix 실검증 권위 / 머지·태그는 사용자
     명시 승인.

8. **Next-session starting prompt** (이 프롬프트로 부팅한다)

   > `docs/prompts` 최신 기록(session-37)을 읽고 재개. **설계와 계획은 교차검수를 통과해
   > 확정됐고 구현은 시작하지 않았다.** main=`5622d7c`(커밋 5개, **푸시 안 됨** — 첫 액션이
   > `git push`다). `productVersion`은 여전히 0.12.0이다.
   >
   > **첫 작업 = v0.12.1 실행**:
   > `docs/superpowers/plans/2026-07-27-v0.12.1-d68-d72.md`(6태스크 36스텝)를
   > **superpowers `subagent-driven-development`**로 돈다 — 태스크별 fresh 서브에이전트,
   > 태스크 리뷰(스펙+품질), fix → 재리뷰. BASE는 원장에 기록하고 `HEAD~1`을 쓰지 않는다.
   > 코드 변경이 생기므로 **브랜치를 먼저 만든다**(문서는 main 직접이 관례지만 코드는 아니다).
   > 게이트 하나: **D69(Task 3)가 D68(Task 5)보다 먼저**다. D69의 실제 errcheck는 이 세션
   > 실측에서 **4건**이다(`realpath_windows.go:32` · `run_windows.go:140` ·
   > `worker_windows.go:50·55`) — session-35의 "13건"은 부정확했다.
   >
   > 그다음 **v0.13 실행**: `docs/superpowers/plans/2026-07-27-v0.13-d73-d76.md`(6태스크
   > 45스텝). 두 배송 사이 순서 의존은 없다. 계획이 남긴 미확정 둘을 그 태스크 안에서 닫는다 —
   > Task 2b의 측정 수치(§3에 실측 기록)와 Task 3 Step 8c의 "격리 없는 절반"(먼저
   > `exec_test.go:890`을 읽고 그 A/B 형태를 따른다).
   >
   > **확정된 설계 결정 네 개를 되돌리지 말 것**(교차검수 5라운드에서 `plan_conflict` 0으로
   > 확인됐다): ① 배송 2단 ② D73 `SchemaVersion` **1 유지** + 색인은 버전 스위치 밖
   > (올리면 구 바이너리의 writable Open이 영구 거부된다) ③ D76 응답 필드 없이 `stderr`
   > 한 줄(필드 하나가 게이트 11의 13,000B를 넘긴다 — 실측 13,008B) ④ D75 `exec` 계층 공유
   > 헬퍼(`sandbox`의 닫힌 표로 올리지 않는다).
   >
   > **이 세션이 세 라운드 연속 겪은 실패 형태 둘을 기억할 것**: ⓐ fix가 회귀를 만든다 —
   > 재리뷰를 생략하지 말 것. ⓑ 코드를 열지 않고 식별자를 옮겨 적으면 critical이 된다
   > (헬퍼 이름 4개가 실재하지 않아 계획이 컴파일 불가였다).
   >
   > 리뷰 체크포인트는 프로젝트 규약대로 서브에이전트 + Codex `review --base <ref>`(1패스/
   > 체크포인트, 재리뷰는 서브 단독). **주의: 계획 검수에서 서브가 Codex 잡 레지스트리를
   > 조회하지 못했다** — 재발하면 컨트롤러가 `status` 요약만 소비하는 형태로 조정한다.
   > 막히면 웹+Codex+서브 협업, CI 3-OS가 unix 실검증 권위, `go test -p 1`,
   > `git add -A` 금지, 머지·태그는 사용자 명시 승인으로만. ultrathink
