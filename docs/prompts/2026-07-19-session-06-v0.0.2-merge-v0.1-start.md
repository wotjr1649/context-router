# Session 06 — v0.0.2 (worker robustness) merged + v0.1 tasks 1–2 complete

- **Span:** 2026-07-19
- **Model:** Fable 5 (ultrathink). Execution: superpowers:subagent-driven-development
  (fresh implementer + task reviewer per task, sonnet; T2-v0.1 reviewer opus; final
  whole-branch reviewer fable). Codex `review --base main` ×1 (final checkpoint).
- **One line:** v0.0.2 executed with a 3-round CI measurement saga (fixed RLIMIT_AS
  headroom 768→1536→8192MB all reconsidered) ending in a **Codex-P1-driven pivot to a
  dynamic limit (current VA + cap)** — PR #8 merged, main GREEN; then v0.1 tasks 1–2
  (store lock exceptions + internal/session storage layer) landed on
  `feat/v0.1-session-events` after a 3-round review convergence.

## 1. Starting prompt (verbatim — original Korean, do not translate)

```
context-router v0.0.2(worker robustness) 실행을 진행한다. 먼저 docs/prompts/2026-07-19-session-05-v0.1-planning.md(최신 기록)를 정독한다. 상태: v0.1 설계서(D14–D20)·계획 2개 커밋 완료, main(fe0a48e) — 코드 변경 없음.
1. superpowers:subagent-driven-development로 docs/superpowers/plans/2026-07-19-context-router-v0.0.2-worker-robustness.md의 태스크 1~4를 실행한다(태스크당 fresh 서브에이전트 + 태스크 리뷰, BASE는 원장 기준). 게이트 8 스위트 + churn 20회 실측(T2)·ubuntu CI 실측(T3)이 수용 기준.
2. v0.0.2 PR 머지(최종 체크포인트: 서브에이전트 + Codex review --base 병렬 1패스, §5 wedge 대응 유지) 후, 여유가 되면 v0.1 계획(2026-07-19-context-router-v0.1-session-events.md) 태스크 1~2까지 착수한다.
3. 세션 종료 시 docs/prompts에 session-06 기록 작성·push.
메모리 캡 테스트 규칙(-p 1) 준수. "worker killed" 접두사 불변(main_test.go:1026,1077 단언). 임시 등록(.mcp.json, ~/.codex/config.toml)·ctr.exe 유지(pre-P1 빌드 — 실호스트 스모크 전 재빌드). Fable 유지, 보안 서술 최소화(fable-security-prose-minimization) 준수. ultrathink
```

## 2. What was done

**v0.0.2 (PR #8 MERGED → main 27c8ed8, main CI GREEN):**
- T1 kill-reason suffixes (8dd5d79): `worker killed (cancelled|time limit|memory or
  crash|bad output)`, prefix/ErrKind invariant; later hardened to `errors.Is` +
  best-effort-classification comment. Review approved, no fixes.
- T2 GOMEMLIMIT injection (cb2451a): child env `GOMEMLIMIT=214748364` (256MB×0.8),
  single `gomemlimitBytes()` helper. Measured churn 20x windows: pre 20/20 clean
  (historical 7/20 flake did not reproduce), post 80/80 clean, no mode switch, hard
  backstop verified. Approved, no fixes.
- T3 linux RLIMIT_AS — the measurement saga:
  - 768MB headroom (2d867a8): ubuntu cancellation smoke **9/9 early deaths** (observed
    via throwaway CI branch running the smoke `-v` — skip path is t.Skipf, invisible in
    non-verbose CI).
  - ×2 → 1536MB (71a74a9): still 9/9. STOP per 2-failure rule → diagnosis v2 on the
    observe branch (worker stderr capture + /proc sampling + threshold grid):
    **worker VA baseline = VmPeak 5.75GB** — dominated by a single 4096MB `r--p` anon
    mapping from dependency init (NOT the Go runtime; session-04's "~1GB runtime
    reservation" diagnosis was incomplete). Limits up to 4.5GB all die instantly at
    `runtime.sysMap` ENOMEM; unlimited completes.
  - 8192MB (f1fe4b7): smoke 3/3 PASS, retry/skip 0. Full CI GREEN.
  - **Final checkpoint dual review**: subagent (fable) Ready-to-merge; **Codex P1** —
    fixed 8GB ceiling lets hostile live-heap scripts grow to multi-GiB (GOMEMLIMIT is
    soft; steps/timeout don't bound RSS), violating design §4.3's 256MB cap. Verified
    valid → **pivot: dynamic limit `current VA (/proc/self/statm) + cap`** (2bd09c1),
    /proc read failure = fail-closed no_isolation, darwin unchanged. Design doc §1.3
    D19-b corrected in-branch (f1878eb). Re-review approved; ubuntu re-measurement:
    smoke 3/3 PASS + gate-8 MemoryExplosion dies in 0.68s as `worker killed (memory or
    crash)` — **hard cap actually enforcing on linux for the first time**.
- T4: local full regression GREEN, 3-OS CI GREEN, PR #8 merge-commit merged, branch
  deleted, no tag. Cross-model win #4: Codex reversed the entire fixed-headroom
  approach at the final gate.

**v0.1 session events — tasks 1–2 (branch `feat/v0.1-session-events`, pushed):**
- T1 (dd99631): `store.AcquireLock(path, shared)` public non-blocking lock (flock /
  LockFileEx) + `ArtifactHashByID` (ErrNotFound reuse). Approved, no fixes. Carried
  notes: caller pre-creates parent dir; release() not idempotent.
- T2 (e790bf7 → 56a6f3b → 41f4914): `internal/session` storage layer (458+279 lines,
  schema v1 + 4 indexes + sessions table, 2-file lease shared/exclusive, conservative
  quick_check, UUIDv7, session_start auto-event, sentinels ErrLeaseHeld/
  ErrRecoverPending/ErrCorrupt; uuid → direct). Review (opus) 3 rounds:
  R1 Important — init.lock rejected instead of serializing cold-start contention →
  bounded retry + dbPath re-stat (regression test proved RED on old code, 15 reps no
  flake). R2 Important — residual window: migrate() had no BUSY retry on the
  conditional init.lock-skip paths (first WAL transition) → `migrateBusyRetry`
  chokepoint (malformed→ErrCorrupt first, busy→bounded retry, exhaustion→ErrLeaseHeld
  wrap). R3 Approved. Implementer discovery: SQLITE_NOTADB surfaces at first query
  (PRAGMA user_version), not only at quick_check — malformed classifier applied to both.

## 3. Current repo state

- `main` @ **27c8ed8** (v0.0.2 merged; PR #8; main CI GREEN) + this record's commit.
- Branch **`feat/v0.1-session-events` @ 41f4914** (pushed; tasks 1–2 of 11 done; CI
  run 29689191359 was in flight at record time — verify GREEN at next session start).
- No open PRs. SDD ledger `.superpowers/sdd/progress.md` has both plans' full history.
- Smoke registrations intact: `.mcp.json`, `~/.codex/config.toml`, repo-root `ctr.exe`
  (**stale: pre-P1 AND pre-v0.0.2** — rebuild before any real-host smoke).
- Codex passes this session: `review --base main` ×1 (v0.0.2 final checkpoint; spent).

## 4. Carryovers / next work

1. **v0.1 tasks 3–11** on `feat/v0.1-session-events` (T3 ctr_record_event → T4 summary
   → T5 export → T6 FTS → T7 search scope → T8 retention → T9 CLI → T10 fail-closed
   assembly+E2E → T11 docs/PR/tag). Integration/final checkpoints: subagent + Codex
   parallel 1-pass (task reviews subagent-only unless risky).
2. Deferred minors (ledgered): `(bad output)` branch dedicated test (needs test-only
   plumbing); migrateBusyRetry branch unit tests (indirect coverage only);
   delays[2]-unused quirk shared with quickCheck (cosmetic).
3. T2-v0.1 carried contracts for T3+: Event has no Redaction field (T3 wires
   redaction + sets the column); AcquireLock parent-dir precondition; ErrLeaseHeld
   also wraps init/migrate exhaustion (toToolError → STORAGE_UNAVAILABLE covers all
   three sentinels).

## 5. Standing protocols (delta only — rest as session-05 §5)

- **Throwaway CI observation branch** (ci/*-observe, never merged): verbose test
  steps + stderr-capturing diagnostic tests + /proc sampling turned a 2-failure
  guessing game into a measured root cause in 2 round-trips. Reuse for any
  linux-only nondeterminism. Delete the branch after.
- **Codex adversarial value compounding**: 4th session in a row it reversed a
  Claude-approved design (fixed headroom → dynamic limit). Keep feeding the final
  checkpoint the riskiest surface.
- Non-verbose CI hides t.Skipf/t.Logf — acceptance criteria that live in skip/log
  lines need a verbose observation run, not just package "ok".
- Reviewer/implementer SendMessage continuation across fix rounds again kept context
  and caught a residual race a fresh reviewer likely misses (R2's isNew-Stat path).

## 6. Next-session starting prompt (paste-ready)

```
context-router v0.1(session events) 실행을 계속한다. 먼저 docs/prompts/2026-07-19-session-06-v0.0.2-merge-v0.1-start.md(최신 기록)를 정독한다. 상태: v0.0.2 머지 완료(main 27c8ed8, CI GREEN), feat/v0.1-session-events @ 41f4914(태스크 1~2/11 완료, push됨 — 브랜치 CI GREEN 먼저 확인).
1. superpowers:subagent-driven-development로 docs/superpowers/plans/2026-07-19-context-router-v0.1-session-events.md의 태스크 3부터 이어서 실행한다(태스크당 fresh 서브에이전트 + 태스크 리뷰, BASE는 원장 기준). T2 이관계약(원장·기록 §4.3) 주의: Event Redaction 필드는 T3 소관, ErrLeaseHeld가 init/migrate 소진도 wrap.
2. 통합·최종 체크포인트에서 서브에이전트 + Codex review --base 병렬 1패스(§5 wedge 대응 유지). 게이트 G1~G9는 각 태스크 테스트가 실체.
3. 세션 종료 시 docs/prompts에 session-07 기록 작성·push.
메모리 캡 테스트 규칙(-p 1) 준수. 임시 등록 유지·ctr.exe는 pre-v0.0.2 빌드(실호스트 스모크 전 재빌드 필수). Fable 유지, 보안 서술 최소화(fable-security-prose-minimization) 준수. ultrathink
```
