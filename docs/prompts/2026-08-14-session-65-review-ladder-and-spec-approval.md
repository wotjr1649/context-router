# 세션 65 — 리뷰 래더가 브랜치를 닫았다. 스펙은 승인됐고 PR #47 이 열렸다

## 1. 세션 정보

- **세션 65**, `2026-08-14T15:43Z ~ 18:00Z`. **UTC 로 여전히 08-14 다** — 세 세션 연속이다.
- 시작 `main` = `1edb48d`. **마감 HEAD 는 `git log` 로 본다**(이 세션은 커밋을 여러 번 얹었다).
  워킹트리 clean, origin 과 동기.
- **세션 64 마감(15:12:59Z)과 이 세션 시작(15:43Z) 사이는 약 30분이다.**
- 다섯 국면: ① 확인 여섯 ② 축 C 스펙 소유자 승인 ③ 축 D 리뷰 래더 → 수정 → 재리뷰 → PR #47
  머지 ④ **"판정까지 감시만"이라는 전제의 재검토와 축 C 구현 계획**(§3.6) ⑤ 마감.
- **설치 0. 저장소 밖 설정 변경 0. 백그라운드 작업은 전부 회수했다**(게이트 실행분).
- **★ 이 세션은 코드를 바꿨고 머지까지 갔다** — 브랜치 `fix/doctor-manual-hook-groups` 에 커밋
  둘을 얹고 리뷰 래더를 마쳐 **PR #47 을 열고 머지했다**(`833bc4b`). 설치본은 안 갈렸다.

## 2. 시작 프롬프트 (원문)

> docs/prompts/2026-08-14-session-64-hook-fix-holds-and-1to1-confirmed.md 를 읽고 이어서 작업한다.
> **§3.7 이 §3.5 를 부분 반증하고 §3.11 이 §3.8 의 함의를 정정한다 — 재포착 예측이나 판정 분모를
> 인용하기 전에 그 둘부터 읽어라.** 이 레코드가 최신이고, 세션 63 레코드는 §3.4(훅 중복·배율 2)와
> §3.3(재포착 기제 확정)만 필요할 때 보면 된다.
> **세션 62 이전 레코드의 포착 수치는 인용하지 마라** — 계상이 다르다.
> **세션 63 레코드의 08-13 칸(50)·08-14 칸(101)도 인용하지 마라** — 지금은 49 와 170 이다.
>
> ## 지금 상태
>
> - **계측 t0 = 2026-08-12T01:22:18Z** — 유효. 판정 가능 시점은 2026-08-26T01:22:18Z 이고
>   그전에는 결정표 행 1("14일이 안 지났다")이다.
> - **336h 창 실효 시작 = 2026-08-12T18:19:19Z**.
> - **손실은 멎어 있다** — 세션 62·63·64 합쳐 **서버 기동 여덟 번에 삭제 0**. 총계 세 축
>   (hashes · CAS blob 파일 · sources/artifacts)은 순증이고 칸마다 서로를 확인한다.
> - **구멍은 닫혀 있다** — 살아 있는 계통이 전부 336h 를 갖는다. **codex `bash.exe` 계보도 네 세션
>   연속 무사하다**(세션 64 마감에 뜬 9108 포함, 지금은 죽었다). 지운 전력이 있는 쪽은
>   `ChatGPT.exe` 계보이고 그것은 지금 없다.
> - **훅 중복은 세션 63 이 제거했고 세션 64 가 모델 변경을 견디는 것을 확인했다** — 도구 출력
>   1건당 원장 1행이다. `~/.claude/settings.json` 에 `context-router hook` 이 하나도 없어야 정상이다.
> - **★ 판정 분모가 아니라 처방의 전제가 오염돼 있다**(§3.8·§3.11) — 포착의 **78.5%건 / 87.6%B 가
>   Edit 응답**이고 지금은 끌 수 없다. 판정일 규칙은 아래에 미리 확정해 뒀다.
> - **★ 열린 작업 둘**: 브랜치 `fix/doctor-manual-hook-groups`(`816e353`, **미푸시** — 리뷰 래더가
>   남았다) · 스펙 `docs/superpowers/specs/2026-08-14-purge-audit-log-design.md`(`857128d`,
>   **소유자 리뷰 대기**, 구현은 판정 뒤).
> - 설치본은 그대로 0.20.0 @ 077b4acc8fa1. **열린 PR 없음.** main HEAD 는 git log 로 본다.
>
> ## 첫 일 — 확인 여섯
>
> 1. Get-CimInstance Win32_Process -Filter "Name='context-router.exe'"  — 서버와 그 부모.
>    codex 계통이면 계보를 끝까지 봐라 — **`ChatGPT.exe` 계보가 지운 전력이 있고 `bash.exe` 계보는
>    네 세션 연속 무사했다.**
> 2. "UTCNOW=$([DateTime]::UtcNow.ToString('yyyy-MM-ddTHH:mm:ssZ')) INH=$env:CTR_RETENTION_INHERITED RET=[$env:CTR_SHADOW_RETENTION] SRC=[$env:CTR_RETENTION_SOURCE]"
>    — 게이트. **RET 이 336h 여야 한다.** 비어 있으면 즉시 재처방이다. SRC=pwsh-profile 은 정상이고,
>    **INH 는 이 셸에서 상속 경로의 증거가 아니다**(세션 62 §3.4).
>    **★ UTC 날짜를 여기서 받아라 — 확인 5 의 08-14 칸 판정이 그것에 걸린다.**
> 3. **훅 중복 재발 검사** — Claude Code 는 플러그인 설치·모델 변경·설정 변경 때 settings.json 을
>    다시 쓴다. 한 줄이면 된다:
>    Select-String -Path "$env:USERPROFILE\.claude\settings.json" -Pattern 'context-router hook' -SimpleMatch
>    **0건이어야 한다.** 나오면 다시 두 벌이고 그때부터의 포착 수치는 또 2배다.
>    **모델 변경만으로는 재발하지 않는다는 것이 세션 64 의 실측이다**(mtime 은 갱신되고 sha256 은
>    `DCC1B40B…AC5DF6` 그대로였다). **플러그인 설치 경로는 아직 안 시험됐다.**
> 4. context-router stats  — 회수 줄 여덟 칸이 t0 와 같은가.
>    기준선: calls=49 legacy=49 나머지 여섯 칸 0, age_s 전부 없음.
>    **주 1회 스냅샷은 ~08-21 이 다음이다.**
> 5. **손실 감시 — doctor [15] hashes · [14] sources · CAS blob 일별 분포를 함께 본다.**
>    **고정값은 07-21~08-13 일곱 칸이고 그 합은 587 이다. 2026-08-14T15:12:59Z 기준:**
>    07-21 **116**(고아) · 08-08 **20** · 08-09 **113** · 08-10 **172** · 08-11 **31** ·
>    08-12 **86** · 08-13 **49** · **08-14 170**.
>    **총계 셋과 08-14 칸은 하한이다** — 레코드를 고치는 편집마다 파일 크기(≈65 KB)만큼 포착이
>    하나씩 더 붙었다. **하한: blob ≥757 · hashes ≥641 · sources ≥643.** 크면 정상이고,
>    **크다는 것 자체는 아무 문제도 아니다.**
>
>    **★★★ 08-14 칸의 성격은 확인 2 가 받은 UTC 날짜가 정한다 — 먼저 그것부터 보라:**
>    - **UTC 로 아직 08-14 면** → 08-14 는 **오늘 칸**이다. 하한 170, 늘기만 한다.
>    - **UTC 로 08-15 이후면** → 08-14 는 **옛 칸이 되지만 고정값이 아니다.** 그 칸에 세션 64 의
>      레코드와 그 세션이 읽은 코드 파일의 blob 이 **170개** 들어 있고, 네가 그중 아무거나 다시
>      읽으면 재포착되어 **08-14 가 줄고 오늘 칸이 그만큼 는다.** 이것이 **판정표 둘째 행(재포착
>      mtime 이동)이고 정상이다 — 조치하지 마라.** 세션 64 가 08-13 칸에서 같은 것을 이미
>      관측했다. **손실 판정은 07-21~08-13 일곱 칸(합 587)으로만 하라.**
>
>    분포는 doctor 가 안 낸다. 직접 센다:
>    Get-ChildItem 'C:\Users\js\AppData\Local\context-router\projects\context-router-4d307dacd75b\artifacts' -Recurse -File
>    을 LastWriteTimeUtc 의 MM-dd 로 Group-Object 한다.
>    **판정은 수가 아니라 이 규칙으로 한다:**
>    - 오늘 이전 칸이 X 줄고 **총계 세 축도 함께 줄었다** → **삭제.** 확인 1 의 계보를 본다.
>    - 오늘 이전 칸이 X 줄었는데 **총계는 불변/증가이고 오늘 칸이 X 늘었다** →
>      **재포착 mtime 이동이다. 정상, 조치 없음.**
>    - 오늘 이전 칸 불변, 총계 증가 → 순수 신규. 정상.
>    - **★ 08-22 부터**: 줄어든 칸이 **오늘로부터 14일 이상 된 날짜**면 → **정상 336h 만료다.
>      조치 없음.** 그보다 최근 칸이 줄었을 때만 사고다.
>    정합 검산도 같이 한다: **blob 총수 − 116(고아) = hashes = 일별 칸 합**, sources = hashes + 2.
>    **이 등식은 총계가 얼마든 성립해야 한다 — 깨지면 그때가 사고다.**
> 6. context-router doctor 의 [17] commit — 077b4acc8fa1 인가 (설치본이 안 갈렸는가).
>
> ## 포착 수치를 읽는 법
>
> - **출발점은 ≥559**(2026-08-14T15:12:59Z, hook:shadow 559회 31,324,452 B, span 끝 14:53:19Z
>   — **하한이다**). **그 뒤 증가분은 1:1 이다** — 세션 64 가 새 서버·새 세션에서 독립 확증했다.
> - **★ 이 수의 78.5%는 Edit 응답이다.** 판정에 쓸 때 아래 「판정일 규칙」의 걸음 4를 반드시 밟아라.
> - **★ 이 수가 빨리 는다고 놀라지 마라.** 16 KiB 넘는 도구 응답은 전부 포착된다 — 대형 파일 Read
>   뿐 아니라 **대형 파일 Edit 도** 매번 파일 크기만큼 올린다. 판정 지표는 이 수가 아니라 **회수 줄**이다.
> - **세션 62 이전 수치와 직접 비교하지 마라** — 2026-08-14T09:10Z 를 경계로 계상이 바뀌었다
>   (세션 63 §3.8 이 적은 09:45:31Z 는 부정확하고 세션 64 §3.8.1 이 정정했다). 세션 60·61·62 의
>   172·388·476 은 실제 ~86·~194·~238 이다.
>
> ## 축 순서 — 확인 여섯 다음에 이것들이 있다
>
> 소유자가 세션 64 에서 **A → D → C** 를 승인해 셋 다 진행했다. A 는 닫혔고 D·C 는 각각 한 걸음씩
> 남았다. **둘 다 소유자 결정이 필요한 걸음이라 먼저 물어라.**
>
> 1. **D 마무리** — 브랜치 `fix/doctor-manual-hook-groups`(`816e353`)가 미푸시로 떠 있다.
>    `internal/cli` 3파일 +182/−30 이고 게이트 다섯을 통과했다(lint 0 issues, 12/13 패키지 —
>    `internal/exec` 는 이 변경이 안 건드린 패키지라 환경 조건이다).
>    남은 것은 **리뷰 래더 → PR → 머지**다. 이 저장소의 태스크 체크포인트는 *서브에이전트 리뷰 +
>    크로스모델 패스 한 번*인데 **델리게이션도 외부 전송도 현재 요청이 있어야 한다** — 소유자에게
>    물어라. **세션 64 의 권고는 서브에이전트 리뷰 하나만 돌리고 크로스모델은 생략**이다: 진단
>    표면만 늘었고 삭제 술어와 데이터 경로를 한 줄도 안 건드려 최악의 실패가 오보/미탐이지
>    파손이 아니다. **다만 한 곳은 봐야 한다** — Codex 쪽 `manualCodexGroup` 과 `heldCodexGroup` 이
>    배타적이지 않아 switch 순서로 처리했고 그 근거가 주석에만 있다.
>    **머지해도 판정과 무관하다**(설치본이 안 갈린다).
> 2. **C 스펙 리뷰** — `docs/superpowers/specs/2026-08-14-purge-audit-log-design.md` 를 소유자가
>    읽고 승인하거나 변경을 요청하는 걸음이 남았다(브레인스토밍 스킬의 User Review Gate).
>    승인 뒤 `writing-plans` 로 가는 것은 **판정 뒤**다 — 구현이 판정 뒤이므로 계획도 그때가 맞다.
>    **소유자가 볼 지점은 넷이다**: doctor 절 번호를 끝에 `[21]` 로 붙인 것 · `PurgeOlderThan` 의
>    `sources` 를 버린 것 · 9필드 고정 · `policy` 를 한 칸에 `값/출처` 로 합친 것.
>    **그리고 세션 64 가 남긴 보강 하나**: `policy` 파싱은 **첫 `/` 로만 쪼갠다**는 규칙을 스펙에
>    한 줄 적어라 — Duration 문자열에 `/` 가 없어 안전하지만, 적혀 있지 않으면 다음 사람이 출처
>    값에 든 `/` 를 보고 흔들린다.
> 3. 그 외에는 감시만. **판정까지 남은 날에 새 축을 열 이유가 없다.**
>
> ## ★★★ 판정일 규칙 — 세션 64 가 미리 확정했다. **그날 정하지 마라.**
>
> **왜 미리 박는가**: 축 A 가 포착의 **78.5%건 / 87.6%B 가 Edit 응답**임을 밝혔고, 그 사실은
> *"소비 0"* 의 **해석**을 바꿀 수 있다. 규칙을 판정일에 정하면 수치를 보고 규칙을 맞추게 된다 —
> 세션 62 가 이미 겪은 형태다(현상을 먼저 믿고 계상을 나중에 의심했다).
>
> **판정일에 이 다섯을 이 순서로 하고, 순서를 바꾸지도 줄이지도 마라:**
>
> 1. **회수 줄 여덟 칸을 그대로 적는다.** D104 가 읽는 것은 이 칸들이고 **착수 조건의 분모는
>    `resolved_artifacts + missed` 다**(세션 63 §3.4). **★ 포착 수로 그 분모를 갈아 끼우지 마라** —
>    포착 수가 답하는 것은 *"얼마나 포착되는가"* 이고 창의 길이가 묻는 것은 *"포착된 것이
>    회수되는가"* 다. **다른 질문의 분모다.**
> 2. **포착의 구성비를 판정일 시점으로 다시 계산해 옆에 적는다.** 방법:
>    `context-router session export --project context-router-4d307dacd75b --worktree <각각>` →
>    `tool_result_summary` 의 `<도구명>: <크기>B` 를 도구별 집계. **CLI 라서 `ctr_*` 원장을
>    안 건드린다.** worktree 는 7개이고 주 worktree 가 압도적이다. 세션 64 값은
>    Edit 78.5%건/87.6%B · Read 13.5%/7.6% 였다(t0 이후 520건).
> 3. **결정표에서 행을 고른다.** 여기까지는 규칙대로다 — 해석을 섞지 마라.
> 4. **★ 그 행의 처방을 채택하기 전에 전제를 한 번 검사하고, 결과를 레코드에 적는다:**
>    *"이 처방은 「모델이 회수를 안 한다」를 전제하는가? 그렇다면 포착의 78%가 Edit 응답이라는
>    사실이 그 전제를 약화시키는가?"* — **답이 무엇이든 적는다.** 검사했다는 사실 자체가 다음
>    사람에게 필요한 정보다.
> 5. **그 뒤에** 채택 레버 스펙을 읽는다. 먼저 읽으면 그 문면이 `ctr_fetch` 를 권해 판정 지표를
>    오염시킨다. **판정 전에는 이 저장소에서 그 스펙을 열지 마라.**
>
> **4를 건너뛰면 *"채택 미달"* 과 *"채택 대상이 애초에 희소"* 가 같은 수로 보인다 — 처방이
> 정반대다.** 전자는 레버를 넣고, 후자는 **포착을 줄인다**(Edit 매처 제외 등).
>
> ## 그다음
>
> - **설치는 하지 마라.** 판정일(2026-08-26) 전에 채택 레버가 살아나면 판정 지표가 오염된다.
> - **★★★ 336h 대상이 0개인 것은 2026-08-22 까지다.** 08-08→08-22 · 08-09→08-23 · 08-10→08-24 ·
>   08-11→08-25 · 08-12→**판정일** · 08-13→08-27 · 08-14→08-28. **판정일의 336h 대상은 약 336개**다
>   (검산: 20+113+172+31). 그 삭제는 **정상 정책 동작이지 사고가 아니다.**
>   **★ 창은 336h 그대로 둔다 — 소유자 판정 완료. 이 결정은 닫혔다.** 08-23~08-24 의 일시 순감
>   (−38 · −97)은 정상이다.
> - 72h 대상은 305+ 이고 날짜가 지날수록 는다 — 기동당 100개가 위험량이다. 이걸 재려고 테스트를
>   만들지 마라 — 분포와 창 시작 시각의 비교가 답한다.
> - **소비는 여전히 0** (`ctr_fetch`·`ctr_search`·`ctr_session_summary` 전부 t0 이후 0회,
>   다섯 세션째). 결정표 행 2 로 갈 공산이 크다 — 판정일에 회수 줄을 인위적으로 올리지 마라.
> - **프로브 blob 둘**(`0fc3bc88…`·`3884b8a0…`, 각 16,461 B, 08-14 칸)은 세션 63 계측의 산물이다.
>   **지우지 마라** — CAS 파일만 지우면 DB 행이 dangling 이 된다. **2026-08-28 에 정상 만료로 빠진다.**
> - **코드베이스 전반은 세션 64 가 읽고 소유자에게 보고했다** — 도구 11개의 등록 조건, 훅 넷
>   (이벤트 기록·Shadow Recall·가드 3종·압축 후 안내), 저장 3층(content.db·CAS·ledger), 보안 경계
>   (denylist·redaction·SSRF·격리), 보존·계측. **설계 문서와 어긋난 사실은 없었다.**
>   같은 것을 다시 읽지 마라 — 재고만 는다.
> - F12(조건 미충족)·F13·트라이그램 축·고아 blob 116개·두 계상 이력은 그대로다.
>   **이 머신에 `sqlite3` 가 없다** — 포착 건의 귀속은 못 재고 집계와 CAS 의 비로만 판정한다.
> - **마감 직전에 프로세스와 재고를 다시 세고 레코드에는 그 수를 적어라.** 세션 61·62·64 가 마감
>   직전에 새 서버를 잡았다 — 세션 63·64 가 잠시 안 잡혔다고 이 프로토콜을 놓으면 못 본다.
>   **그리고 마감은 커밋으로 끝나지 않는다** — 커밋 뒤에 재고를 움직이는 작업(대량 소스 읽기 등)을
>   하면 다시 세고 레코드를 갱신해 두 번째 커밋을 남겨라.
> - **확인 여섯이 초록이고 축 둘이 소유자 대기면 세션은 짧게 닫는다** — 재계수 뒤 레코드로 끝낸다.
>
> ## 밟으면 안 되는 자리
>
> - **336h 를 못 받은 채 서버를 띄우지 마라.** 확인 2 가 그 게이트다.
> - **★ 카운터 하나로 계상 배율을 판정하지 마라** — 독립적으로 세는 다른 카운터 옆에 세워라.
>   `원장 2행 : blob 1개` = 배율 2 · `원장 2행 : blob 2개` = 배율 1. 비율이 반복해서 정확히 딱
>   떨어지면(1/2, 1/3, 2:1) 현상보다 **계상**을 먼저 의심하라.
> - **★ 계측기가 세는 *단위*를 확인했으면 그 수의 *구성비*도, 그 분모가 *어느 질문의* 분모인지도
>   확인해라.** 세 층이다: 단위(세션 63) → 구성비(§3.8) → 분모의 소속(§3.11).
> - **★ 예측의 전제는 다 세되 *최소로* 줄여라 — 과소도 과다도 예측을 죽인다.** 적힌 전제마다
>   "이게 거짓이어도 발화하는 경로가 있는가"를 되물어라 — 있으면 그것은 전제가 아니라 충분조건
>   한 사례다.
> - **★ 감시 세션에서 대량 소스를 읽었으면 기준선을 그 *뒤*에 다시 잡아라.** doctor·stats·작은
>   출력은 재고를 안 늘리지만 **16 KiB 넘는 소스 여러 개는 다르다.**
> - **★ `doctor [9]` 의 초록을 "훅 중복 없음"으로 읽지 마라** — 그 검사는 마커를 첫 관문으로
>   요구한다(세션 64 §3.9 가 그 구멍을 브랜치에서 고쳤지만 설치 전이라 아직 안 돈다).
>   중복은 확인 3 이 본다.
> - **재고 감시를 회수 줄로 하지 마라** — 판정 지표가 t0 와 글자 그대로 같은 채로 재고의 3분의 1이
>   사라질 수 있다. 세션 58~60 이 그래서 200개를 못 봤다.
> - **감시 칸을 날짜로 고정하지 마라** — "오늘 이전"이 규칙이다. **그리고 "오늘 이전 칸이 줄면
>   퍼지"로 단정하지 마라** — 재포착 mtime 이동과 08-22 이후의 정상 만료가 같은 모양으로 보인다.
> - **순감과 삭제량을 같은 수로 쓰지 마라** — 견고한 것은 순감뿐이고 삭제량은 하한이다.
> - **삭제를 로그로 찾지 마라** — MCP 서버 stderr 는 어디에도 안 남는다. **삭제 시각을 파일시스템
>   자국으로 역산하지도 마라** — 그 자국은 생성과 삭제를 못 가른다. 손실이 났으면 **어느 계통이
>   336h 를 못 받았는지**부터 봐라.
> - **INH 를 상속 경로의 증거로 읽지 마라** — 그 표식을 설정하지 않는 부모에서 잰 것만 관측이다.
> - **계통 하나를 재고 일반화하지 마라** — 세 계통을 각각 재고 나서야 "구멍이 없다"를 말할 수 있었다.
> - **CTR_SHADOW_RETENTION 에 14d 를 쓰지 마라** — ParseDuration 에 d 가 없어 조용히 72h 다.
> - **회수 줄을 인위적으로 올리지 마라.** 검증 회수는 판정 대상이 아닌 프로젝트에서 한다.
> - **판정 대상이 될 수 있는 프로젝트에서 채택 레버의 스펙·계획을 읽지 마라** — 그 문면이
>   ctr_fetch 를 권한다.
> - **침묵을 단정하는 테스트를 stdout == "" 하나로 쓰지 마라** — 긍정 케이스와 한 축만 다른
>   픽스처를 쓰고 부작용 축을 함께 봐라.
> - **외부 검토 초점에 의역을 싣지 마라** — 별칭화는 식별자에 거는 것이지 검토 대상에 거는 것이 아니다.
> - **게이트 결과를 옮겨 적지 말고 직접 돌려라.** 완료 알림의 진단이 보고와 모순되면 직접 확인.
> - **가드를 만나면 우회하지 마라** — 그 한 칸은 사람에게 넘겨라. `Grep head_limit=0` 은 실제로
>   거부된다(D54 가드, 세션 64 가 겪었다) — `head_limit` 을 지정해 다시 물어라.
> - stats·doctor 는 퍼지를 돌리지 않는다(serve 기동만 돈다). 훅도 안 돈다.
>   **그리고 16 KiB 미만 도구 출력은 포착되지 않는다** — 감시 명령 한 바퀴가 재고를 안 늘린다.
> - go test 에 -p 1 필수. 게이트 다섯을 && 로 잇지 마라 — internal/exec 는 이 머신의 PowerShell
>   실행 정책 때문에 빨갛다(환경 조건이지 회귀가 아니다). 관측된 이름:
>   TestNpmrcScratchConfigWins · TestRunShellPS51ProviderWriteReturns · TestD78SignalOnExit ·
>   TestD78NoObservableSideEffects.
> - 행 번호를 주석·문서에 적지 마라 — 심볼 이름을 적어라.
> - **프로세스 목록은 그 순간의 생존자만 보여준다** — 지나간 기동은 안 잡히고 다가올 기동도 안
>   잡힌다. 마감 직전에 그 자리에서 다시 세라.
>
> 문제 발견되면 fix 혹은 브레인스토밍하여 사용자에게 질문하고, 문제 없으면 진행한다.
> ultrathink

## 3. 한 일

### 3.1 확인 여섯 전부 초록 — 아홉 번째 기동에도 삭제 0

`[실측 15:44:22Z]`. **확인 2 를 먼저 읽어 08-14 칸의 성격을 정했다** — `UTCNOW=2026-08-14T15:44:22Z`,
즉 **UTC 로 여전히 08-14 이고 그 칸은 오늘 칸이다.**

| # | 항목 | 세션 64 마감(15:12:59Z) | 지금 | 판정 |
|---|---|---|---|---|
| 1 | 프로세스 | 3개(7488·7652·9108, 5864 포함 재계수 시점) | **3개** — 7488·7652 생존, **16628 신규**(`15:43:10Z`). 5864·9108 사망. 전부 `claude.exe ← pwsh ← WindowsTerminal 5796`, **codex 유래 0** | ✅ |
| 2 | 게이트 | `RET=[336h] SRC=[pwsh-profile]` | **동일** | ✅ |
| 3 | 훅 중복 | 0건 · sha256 `DCC1B40B…AC5DF6` | **0건 · sha256 동일**(mtime 만 `15:43:18Z`) | ✅ |
| 4 | 회수 줄 8칸 | t0 동일 | **글자 그대로 동일** | ✅ t0 불변 |
| 5 | 재고 | blob ≥757 / hashes ≥641 / sources ≥643 | blob **761** / hashes **645** / sources **647** | ✅ |
| 6 | commit | `077b4acc8fa1` | `077b4acc8fa1` | ✅ 안 갈림 |

**확인 5 판정**: 고정 일곱 칸 `116·20·113·172·31·86·49` **합 587 불변**, 08-14 만 170 → **174**,
총계 셋 전부 +4. ⇒ **판정표 셋째 행, 순수 신규.** 정합 `761 − 116 = 645` = hashes = 일별 칸 합,
sources `647 = 645+2`.

**★ 이 세션 시작에 서버가 한 번 떴는데(16628, `15:43:10Z`) 삭제 0.** 그 시점 72h 대상은
305+(08-08~08-10 칸 전부)인데 재고는 오히려 +4 순증이었다 ⇒ 336h 가 이 계통에도 상속됐다.
**세션 62 부터 아홉 번 연속 삭제 0.**

**★ 배율 재확인**: `hook:shadow` 559 → **563**(+4) : blob +4 : hash +4 ⇒ **배율 1.** 훅 중복
부재가 두 카운터로 확인된다.

### 3.2 모델 변경 경로가 두 번째로 시험됐고 또 견뎠다

이 세션 시작에도 소유자가 `/model` 을 썼고 `~/.claude/settings.json` 의 mtime 이
`2026-08-14T15:43:18Z` 로 갱신됐다. **sha256 은 `DCC1B40B…AC5DF6` 그대로다** `[실측]`.

세션 64 §3.2 와 같은 결과이고 **이제 독립 관측 두 번**이다. ⇒ 모델 변경은 파일을 다시 쓰지만 훅
그룹을 되살리지 않는다. **플러그인 설치 경로는 여전히 미시험이다** — 확인 3 은 계속 돌린다.

### 3.3 축 C — 소유자가 볼 지점 넷을 승인했고, 그 걸음에서 스펙 결함 둘이 나왔다

`[소유자 결정 — AskUserQuestion 2회, 2026-08-14]`

**넷 다 유지 승인.** 각각의 근거와 대가를 압축해 올렸고 소유자가 그대로 받았다:

1. **doctor 절을 끝에 `[21]`** — 중간 삽입은 `assertDoctorAscending`·문서·**append-only 핸드오프
   레코드**의 번호 참조를 한꺼번에 깬다. 옛 레코드는 고칠 수 없으니 그 어긋남이 영구다. 대가는
   `[14]`·`[15]` 와의 주제 인접성 상실.
2. **`PurgeOlderThan` 의 `sources` 폐기** — 대가는 `sources = hashes + 2` 가 **관측이지 불변량이
   아니라는** 점이고, 그 감시는 §9 가 재고 세 축에 맡겨 뒀다.
3. **9필드 고정** — 대가는 `count` 의 뜻이 경로마다 다르다는 것(`path` 칸이 옆에 있어 읽힌다).
4. **`policy` 합침** — §1 의 판정 기준이 `grep '72h0m0s/'` **한 줄**이 된다. 쪼개면 그것을 잃는다.

**★ 스펙을 읽다가 실제 결함 둘을 찾았다** `[실측]`:

- **형식 줄의 6번째 필드가 옛 이름 `<hashes>` 로 남아 있었다.** 필드표·§2.0 의미표·§3 의
  `PurgeRecord.Count` 는 전부 `count` 다. **이름이 계약과 싸운다** — 구현자가 `hashes` 를 보면
  `cli-gc` 의 "고아 blob 파일 수"를 그 칸에 담는 것을 이상하게 여겨 스키마를 흔든다. `<count>` 로
  고쳤다.
- **파서의 엄격함과 §2.0 의 확장 약속이 충돌했다.** §2.0 은 `sources` 를 버리며 *"필요해지면 그때
  열을 늘린다"* 고 하는데 §4·§6 은 파서를 *"정확히 9필드만 수용"* 으로 못 박았다. 로그가
  append-only 라 한 파일에 9필드 줄과 10필드 줄이 섞이고 **그 순간 옛 파서가 새 줄을 전부 버린다.**
  `[소유자 결정]` **파서를 "9필드 이상 수용, 10번째부터 무시"로 완화**했다. 내주는 것은 *"필드가
  하나 더 낀 깨진 줄"* 을 거르는 능력인데, §2.4 위생이 **쓰는 쪽에서** 탭을 공백으로 바꾸므로
  사람이 파일을 손대지 않는 한 그런 줄이 안 생긴다.

**세션 64 가 남긴 보강도 반영했다**: `policy` 파싱은 **첫 `/` 로만 쪼갠다**(`SplitN(…, "/", 2)`).
앞칸은 `Duration.String()` 이라 `/` 가 없어 안전하고, 뒤칸인 출처는 사람이 임의 문자열을 넣는
자리인데 §2.4 위생이 `/` 를 안 지운다.

**부수 확인**: `HookPurgeReport` 실측 타입은 `ReclaimedB int64` · `DeferredFiles int` ·
`FailedFiles int` 이고 **스펙 §3 의 `Bytes *int64` / `Deferred, Failed *int` 와 정확히 맞는다.**

커밋 `7ab1b6f`(main). **구현은 판정일 뒤 그대로다.**

### 3.4 축 D — 리뷰 래더가 브랜치를 닫았다 (PR #47)

`[소유자 결정]` **서브에이전트 리뷰만, 크로스모델 생략.** 델리게이션 승인 · **외부 전송 0**.

**리뷰 1회차** — `b7b872f..816e353` 에 대해 **Critical 0 · Important 4 · Minor 5 · 소유자 판단 3**.
리뷰어가 요구 1·2(삭제 술어 불변)를 *"테스트가 안 깨졌다"* 보다 강한 수준으로 확인했다:
`removeHookGroups` 의 호출부가 여전히 둘뿐이고 두 술어 본문이 diff에 없으며, `manualHookGroup`·
`manualCodexGroup` 이 저장소 전체에서 자기 정의와 두 scan 함수 안에서만 참조된다 — **새 술어가
제거 경로에 닿을 경로 자체가 없다.** 부류 배타성도 여덟 형태 전수에서 성립.

**반영 넷 + Minor 하나** (커밋 `9dafa14`):

| 소견 | 검증 | 처리 |
|---|---|---|
| **I1** Codex 겹침 형태를 재는 단정이 없어 switch 순서가 주석만으로 유지 | `heldCodexGroup` 의 `foreign` 에 "표식 없는 우리 명령"이 실제로 들어간다 — A+B 형태에서 두 술어가 **둘 다 참** | `TestScanCodexRegisteredHooks` 넷째 사례 추가 |
| **I2** `manualGroupHint` 가 중복 발화를 단정형으로 | doctor 에는 플러그인 훅 탐지기가 **아예 없다**(재리뷰가 트리 전수로 확인) | 조건형으로 + **지시도 그 조건에 걸었다** |
| **I3** Codex 서브테스트 축이 Claude 형제보다 하나씩 적다 | 실측 확인 | 부작용 축 + 동거 형태의 `manual=0` 단정 |
| **I4** Claude 쪽에 한 축만 다른 음성 픽스처 없음 | `manualHookGroup` 이 `isHookCommandToken` 을 쓰지만 테스트가 그것을 안 잠근다 | `hook-wrapper` 그룹 추가 + 파일 불변 단정을 값 전체로 조임 |
| **M1** `"손으로 넣은"` 어휘의 근거 없음 | `git log -S "__ctrManaged" -- internal/cli/hook_install.go` 최초 커밋이 **`1623a1f`(hook install 첫 커밋)** | 주석 한 줄 |

**기각 다섯**(M2 스코프 · M3·M4·M5 선존 · 소유자 판단 셋)은 §5 에 이월로 적었다.

**★ I1 은 뮤테이션으로 실증했다** `[실측]`: 두 `case` 를 뒤집고 돌리자 정확히 그 단정이
`h=0 manual=1`(기대 `0/1/0`)로 빨개졌고, 즉시 원복했다. `git diff` 로 확인한 이번 라운드의
`hook_install.go` 변경은 **주석 3줄뿐이고 코드 줄은 하나도 안 움직였다.** 그 측정을 주석에 남겼다.

**재리뷰(같은 리뷰어를 이어감)** — **넷 다 "closed at the mechanism"**, 새로 들어온 문제 0,
**Ready to merge: Yes.** 리뷰어가 I1 을 독립 유도해 *"두 arm 이 다 매치하므로 실행되는 arm 은
순서만이 정하고, `h` 와 `mn` 을 **둘 다** 단정하므로 교체가 실패한다"* 를 확인했고, I4 의 조임이
장식이 아니라 **부하를 받는다**는 것도 확인했다(`"command":"context-router hook-wrapper"` 는
`"command":"context-router hook"` 을 부분 문자열로 포함하지 않는다 — 닫는 따옴표가 끊는다).

#### 3.4.1 ★ 재리뷰가 내 주석의 근거를 정정했다 — 결론은 같고 근거가 틀렸다

`9dafa14` 의 주석은 *"heldGroupHint 는 단정형인데 그쪽은 마커 자체가 우리 옛 설치기의 산물임을
증명해 「플러그인이 따로 있다」가 함의된다"* 고 적었다. **틀렸다.** 마커가 증명하는 것은 그 그룹의
**출처**뿐이고 *"지금 플러그인이 설치돼 있다"* 는 그쪽도 모른다. 즉 `heldGroupHint` 의 무조건
주장도 추론에 기대고 **잔여 실패 모양(지운다 → 포착이 조용히 멎는다 → `[9]` 초록)이
`manualGroupHint` 와 같다.**

그대로 두는 **실제 근거는 둘**이다: 그 문면이 `CHANGELOG.md` 와 `docs/context-router-design-v0.19-ko.md`
에 **축자 인용**돼 있어 고치는 것이 문서화된 계약 편집이라는 것, 그리고 코호트가 우리 설치 경로를
밟은 쪽이라 훨씬 작고 오래됐다는 것. 주석을 그렇게 고치고 **"미룬 선택이지 없는 문제가 아니다"**
를 명시했다(커밋 `e3ac98d`).

**결론은 안 바뀌었지만 코드에 남는 것은 근거다.** 결론이 맞다고 넘기면 다음 사람이 틀린 근거에
기댄다.

### 3.5 게이트 다섯 — 직접 돌렸다

`[실측 — 최종 HEAD `e3ac98d`]`: `go build` 0 · `go vet` 0 · `gofumpt -l .` 무출력 ·
`golangci-lint run` **0 issues** · `internal/cli` **ok 58.221s**.

**전체 테스트**(`-count=1 -p 1`, 앞 커밋 기준) **12/13 패키지**. 빨간 것은 `internal/exec` 하나이고
실패 넷이 **세션 프롬프트가 적은 관측 이름과 정확히 일치**한다(`TestNpmrcScratchConfigWins` ·
`TestRunShellPS51ProviderWriteReturns` · `TestD78SignalOnExit` · `TestD78NoObservableSideEffects`).
`git diff --name-only b7b872f..HEAD` 가 `internal/cli` 3파일만 내므로 **결정 규칙상 환경 조건이지
회귀가 아니다.**

### 3.6 ★★★ "판정까지 감시만"은 틀린 전제였다 — 그리고 그 프레임이 이미 대가를 냈다

소유자가 물었다: *"판정까지 기다리기엔 개발 속도 지연이 오래 될 것 같은데, 개발이 완료된 것도
아니기에 마냥 기다릴 수는 없다."* **맞는 지적이었고, 전제를 다시 세니 막혀 있는 것은 셋뿐이다:**

1. 이 프로젝트에서 `ctr_fetch`·`ctr_search`·`ctr_session_summary` 호출
2. **설치**(채택 레버가 살아나 회수가 발생한다)
3. 이 프로젝트에서 **채택 레버 스펙** 읽기(그 문면이 `ctr_fetch` 를 권한다)

**코드 변경·커밋·PR·머지·게이트·설계·계획은 전부 무관하다.** 증거는 이 세션 자체다 — PR #47 을
리뷰 래더까지 태워 머지했는데 회수 줄 여덟 칸이 t0 와 **글자 그대로 같다.**

**★ "감시만"이라는 문장의 출처는 계측 제약이 아니다.** 세션 64 가 썼고 그때 맥락은 *"열린 작업
둘이 끝나면 백로그가 빈다"* 였다. 거기에 세션 58~62 가 손실 사고를 쫓던 시기의 관성이 붙어
굳었고, **이 세션이 그것을 §7 에 그대로 옮겨 적었다.**

**그 프레임의 대가가 같은 세션 안에서 드러났다** `[실측]`: **PR #47 이 CHANGELOG 에 안
들어갔다.** `[Unreleased]` 에는 F10(artifact id 재사용)만 있었다. 이 저장소는 doctor 진단 변경을
릴리스 노트에 적는 관행이고 v0.19.0 의 `### Removed` 가 `heldGroupHint` 문면을 **축자 인용**하고
있다 — 이번 변경은 정확히 그 다음 층인데 빠져 있었다. **리뷰어도 나도 못 봤다.** 커밋 `1c76394`
가 닫았다.

**진짜로 기다려야 하는 것은 채택 레버 하나뿐**이고, 그것도 *"규칙 때문에 막혔다"* 가 아니라
**판정 결과가 그 설계의 입력**이기 때문이다 — 미리 하면 두 갈래를 설계해 하나를 버린다.

#### 3.6.1 축 C 구현 계획 — 그리고 승인된 스펙이 코드와 어긋난 곳 둘

`[소유자 결정]` 열린 백로그 중 **축 C 구현 착수**를 골랐다. 근거 셋: 스펙 승인이 끝나 준비
비용이 0에 가깝고 · 판정일에 **머지된 상태로 대기**시키면 그날 설치 한 번으로 끝나며 · 그
기능이 **정확히 이 감시 세션들의 비용을 없앤다.**

`superpowers:writing-plans` 로 `docs/superpowers/plans/2026-08-14-purge-audit-log.md` 를 썼다
(다섯 태스크, 커밋 `5ff001b`). **계획을 쓰는 과정이 스펙 결함 둘을 잡았다** `[실측]`:

- **`cli-hook-only` 의 호출 함수가 `PurgeHookOnlyOlderThan` 이 아니라 `PurgeHookOnly` 다.**
  `runPurgeHookOnly` 가 부르는 것이 그것이고 **경계 인자를 받지 않는 전량 삭제**다. 그러면 그
  경로에는 보존 창도 경계도 개입하지 않는다 — `policy`·`cutoff` 에 값을 적으면 **일어나지 않은
  정책 판정을 기록하는 것**이고, 그것이 이 로그가 막으려던 바로 그 종류의 거짓 기록이다.
  스펙 §2.0·§2.1·§2.3 을 고쳤다. **`policy` 가 실질을 갖는 경로는 `startup-shadow` 하나뿐이고,
  그것이 바로 200개가 사라진 경로다.**
- **파서 완화가 `dropsByReason` 의 *"느슨 수용 금지(설계 §5)"* 와 부딪혀 보였다.** 실제로는 다른
  문제였다 — 그쪽이 막는 것은 구형 2필드와 신형 5필드 **혼재 사이의 중간 필드 수**이고, 퍼지
  로그에서 잘린 append 는 9필드보다 **짧게** 나오므로 `>= 9` 가 이미 거른다. **선례를 어긴 것이
  아니라 같은 규칙이 다른 형상에서 다른 답을 낸 것**이고, 그 사실을 §4 에 적었다.

**계획 자체의 갭도 self-review 가 둘 잡았다**: 스펙 §6 항목 6(`status` 우선순위 표 테스트)이 어느
태스크에도 없었고(분류를 `startupPurgeStatus` 로 뽑아 표로 재게 했다 — `partial` 이
`budget`·`cancelled` 를 이기는 순서가 테스트에 걸린다), `doctorOut` 이 storeRoot 를 **매번 새
`t.TempDir()`** 로 만들어 테스트가 `purge.log` 를 놓을 자리를 못 만든다는 것이 확인됐다
(`doctorOutIn` 으로 한 겹 벗긴다).

`[소유자 결정]` **구현은 다음 세션**이다.

#### 3.6.2 ★★★ 계획 검토 — Codex 한 패스 + 서브에이전트, 16건

`[소유자 요청]` *"계획을 코덱스와 서브에이전트로 적대적 검수·검토·자문·리뷰"* — **델리게이션과
외부 전송이 둘 다 이번 요청에 있었다.** 절차는 `cross-model-review` 스킬과
`docs/codex-secure-review.md` 그대로다.

**보안 체크포인트를 먼저 진술했다**(egress 행위이므로): 신뢰 경계는 **동시 실행 중인 여러 서버
인스턴스가 append 하는 파일**이고(이 머신에 넷이 떠 있다), 신뢰할 수 없는 입력은 파일 내용
자체와 `CTR_RETENTION_SOURCE`, 구체적 오용은 **섞인 줄이 파서를 통과해 doctor 가 일어나지 않은
삭제를 보고하는 것**. 전면 보안 리뷰는 불필요(로컬 진단 파일, 권한·비밀 경로 없음)하되 오진단이
기능의 존재 이유를 무효화하므로 정확성 검토가 필요하다고 판정했다.

**Codex 는 「가장 위험한 선택 하나」로 좁혔다** — Design-ready 체크포인트의 규정이다. 별칭화된
최소 발췌만 나갔다(패키지·파일·저장소명 전부 중립화, 보낸 코드는 아직 존재하지 않는 계획의
코드). 소요 6분 30초.

**서브에이전트(opus)가 Codex 결과를 자기 컨텍스트에서 읽고 실물과 대조**해 통합 findings JSON 을
냈다 — 컨트롤러가 원문을 안 읽는 것이 이 저장소의 절차다. `verdict: needs_fixes`,
**critical 1 · important 4 · minor 11.**

**★★★ Codex 가 값을 한 지점**: `>= 9` 완화의 근거로 내가 스펙에 적은 *"잘린 append 는 줄을
짧게 만들지 길게 만들지 않는다"* 가 **거짓**이었다. append-only 파일에서는 **찢어진 조각 뒤에
다음 writer 의 레코드가 이어붙고**, 절단점이 필드 경계에 떨어지면 합쳐진 줄이 **정확히 9필드**가
되어 `ts`·`path`·`count` 가 전부 통과한다 — **조작된 수를 가진 합성 행이 유효로 수용된다.**
소유자 승인 결정(`>= 9`)은 유지하고 **닫힌 집합 둘과 나머지 네 칸의 형식 검증**을 더했다.
Codex 의 주 결론(*"각 레코드가 하나의 완전한 native append 로 성공하면 O_APPEND 만으로 충분,
advisory lock 불필요"*)은 설계를 확인해 주었고, 그 **조건절이 깨지는 경우**가 위 결함이다.

**Critical 하나는 서브에이전트가 잡았다**: `purge --gc` 는 `gcOnly` 를 세워 **`runGCOrphan`** 으로
가는데 내가 선택 분기 안에만 배선했다 — **계획 자신의 테스트가 실패하는 상태**였다.

**소유자 결정 둘**(`plan_conflict`):
- **F2** — 전체 프로젝트 삭제(`os.RemoveAll`)와 세션 파일 삭제가 세션 64 의 결정 3(*"삭제 경로
  전부"*)에서 빠져 있었다. **명시적 범위 밖으로 적기로 결정.** 근거: 이 로그가 닫는 구멍은
  *"아무도 요청 안 했는데 사라졌다"* 이고, **기록해도 그 행을 `RemoveAll` 이 함께 지운다.**
- **F8** — `store.PurgeLogName` 을 export 하면서 `internal/cli` 에 미러를 또 두는 것.
  **미러 삭제로 결정**(D13). `dropsFileName` 이 미러인 이유는 hook 쪽이 unexported 라서다.

**16건 전부 반영했다**(커밋 `972e401`). Codex 의 나머지 주장 넷은 서브에이전트가 기각했다.

#### 3.6.3 ★★★★ 그 검토가 손실 100개를 일으켰다 — codex 진입점이 336h 를 못 받는다

**`[실측 18:33:24Z]` 검토를 마치고 마감 재계수를 하니 고정 일곱 칸이 587 → 487(−100)이었고
총계 세 축도 −50 이었다. 판정표 첫째 행 — 삭제다.**

| 칸 | 17:58Z | 18:33Z |
|---|---:|---:|
| 08-08 | 20 | **15** |
| 08-09 | 113 | **82** |
| 08-10 | 172 | **130** |
| 08-11 | 31 | **9** |
| 08-12 · 08-13 · 07-21 | 86 · 49 · 116 | **불변** |
| blob / hashes / sources | 803 / 687 / 689 | **753 / 637 / 639** |
| 08-14 (오늘) | 216 | 266 |

셈이 맞는다: **삭제 100 + 신규 50 = 총계 −50.** 정합 `753 − 116 = 637` 은 그대로 성립한다.

**원인은 이 세션이 띄운 Codex 다** `[실측 — 계보]`:

```
context-router.exe 17224 (18:07:00Z) ← codex.exe 3960 ← bash.exe ← bash.exe ← node.exe
```

`18:07:00Z` 는 `codex-companion.mjs task --background` 를 실행한 시각이다. **그 체인이 서버를
새로 띄웠고 그 서버가 336h 를 못 받았다.** 지워진 범위가 08-11 일부까지인 것이 **72h 창과 정확히
맞는다**(`18:07Z − 72h = 08-11T18:07Z`, 08-12·08-13 무사). 그리고 `startupPurgeMaxHashes` 가
**100** 인데 정확히 100 이 빠졌다 — **상한까지 돌았다는 뜻이고 배치가 남았다.**

**★ Bash 툴 자체에는 336h 가 있다** `[실측: BASH_RET=[336h] BASH_SRC=[pwsh-profile]]`. 체인
어딘가 — `node.exe` → `bash.exe` → `codex.exe` 사이 — 에서 환경이 갈린다. **어느 고리인지는
안 쟀다.**

**★★ 세션 프롬프트의 *"codex `bash.exe` 계보는 네 세션 연속 무사했다"* 는 관측이었지 보증이
아니었다.** 그 네 번과 이번은 **진입점이 다르다** — 이번엔 companion 을 직접 실행했다. 세션 61
§9.4 의 *"계통 하나를 재고 일반화하지 마라"* 가 정확히 이 형태로 물었고, **물린 쪽이 그 규칙을
레코드에 적어 인계해 온 세션 자신이다.**

**판정 지표는 오염되지 않았다** — 회수 줄 여덟 칸이 t0 와 글자 그대로 같다(`calls=49 legacy=49`
나머지 0). 다만 336h 창 계산의 입력이던 재고 분포가 바뀌어 **판정일의 336h 대상이 336 → 236 개**다.

`[소유자 결정]` **판정일까지 Codex 를 쓰지 않는다.** 17224 는 **죽이지 않는다** — 그 기동의
퍼지는 이미 돌았고, 죽이면 다음에 다시 뜨면서 남은 236 개 중 또 100 개를 집는다.

**★★★★ 그리고 이 사고가 축 C 의 존재 이유를 실시간으로 증명했다.** 지금 확실한 것은 *"100개가
사라졌다"* 와 *"codex 계보 서버가 그 시각에 떴다"* 뿐이고 **그 서버가 어떤 정책으로 돌았는지는
추론이다** — 72h 창과 삭제 범위가 맞는다는 정황일 뿐 관측이 아니다. `purge.log` 가 있었다면
`72h0m0s/-` 한 줄이 그것을 **관측으로** 만들었다. 스펙 §1 의 판정 기준(*"200개가 사라진 그 기동의
행에 `72h0m0s/-` 가 적혀 있다"*)이 이 사건에 글자 그대로 적용된다 — **설계가 예측한 사건이 설계가
구현되기 전에 다시 일어났다.**

## 4. 저장소 현재 상태

- **`main` = `5ff001b`** — 이 세션이 밀어 올린 순서: `7ab1b6f`(축 C 스펙 승인) · `4b5eb46`(레코드)
  · `833bc4b`(PR #47 머지) · `54c046b`(레코드 마감 갱신) · `1c76394`(CHANGELOG) · `5ff001b`(구현
  계획 + 스펙 정정). **워킹트리 clean, origin 과 동기.**
- **★ PR #47 머지 완료** `[2026-08-14T16:33:47Z · merge commit 833bc4b]` — 브랜치
  `fix/doctor-manual-hook-groups` 3커밋(`816e353` 세션 64 · `9dafa14` 리뷰 반영 · `e3ac98d`
  근거 정정), `internal/cli` 3파일 **+238/−31**.
  **CI 는 두 run 전부 초록이었다** `[실측]`: `test (windows-latest)` **pass ×2**(7m56s·7m57s) ·
  ubuntu ×2(10m36s·10m11s) · macos ×2 · lint ×2 · crossbuild ×2 · test-node-leg ×2.
  **이번에는 windows flake 가 안 났다.** `[소유자 결정]` CI 초록 확인 뒤 머지.
  **머지는 판정과 무관하다 — 설치본이 안 갈리므로 이 수정은 아직 안 돈다.**
- **설치본**: **0.20.0 @ `077b4acc8fa1`** — 안 갈았다. **이 브랜치는 판정일까지 설치하지 않는다.**
- **`CTR_SHADOW_RETENTION`**: HKCU `336h` + `~/.claude/settings.json` 의 `env` `336h` +
  `$PROFILE.CurrentUserAllHosts`(git 밖).
- **훅 등록**: 플러그인 한 벌만. `settings.json` sha256 =
  `DCC1B40BC4B320B188374B13A6005FD7BC589DF071AA8DAF1153BE9480AC5DF6` — **세션 63 제거 이후 불변**
  (모델 변경이 mtime 만 갱신, §3.2).
- **계측 t0** = `2026-08-12T01:22:18Z` — **유효.** **336h 창 실효 시작** = `2026-08-12T18:19:19Z`.
- **★ 마감 재계수** `[2026-08-14T16:23:21Z — 최종]`:
  - `hook:shadow` **578회 32,562,911 B**, span 끝 `16:20:38Z` (세션 시작 563 → **+15**)
  - `fetch calls=49 legacy=49 legacy_after_migrate=0 resolved=0 resolved_artifacts=0 missed=0
    shadow_rows=0 shadow_artifacts=0 age_s 전부 없음` — **t0 와 글자 그대로 같다. 소비 0.**
  - `doctor [14]` sources=**662** artifacts=662 blob=53,119,101B
    file=709,922,816B(임계 초과 경고 — 의도된 상태, D104) free=519,340,032B live=190,550,016B
  - `doctor [15]` shadow-owned 46,846,083B **hashes=660**
  - `doctor [12]` drops **431 불변 — 이 세션 drop 0** · `doctor [6]` sessions=255(empty 86)
  - `doctor [17]` commit `077b4acc8fa1` 불변 · 훅 중복 **0건** · `RET=[336h] SRC=[pwsh-profile]`
  - **퍼지 대상**: 336h **0개**(08-22까지) · 72h 약 **305+** · 전량 660
- **★ 재고 기준선** — 다음 세션의 손실 감시용. CAS blob **776**:

  | 07-21 | 08-08 | 08-09 | 08-10 | 08-11 | 08-12 | 08-13 | 08-14 |
  |---:|---:|---:|---:|---:|---:|---:|---:|
  | 116 (고아) | 20 | 113 | 172 | 31 | 86 | 49 | **189** |

  **고정값은 07-21~08-13 일곱 칸이고 그 합은 587 이다** — 이 세션 내내 불변.
  정합: 776 − 116 = **660** = hashes = 일별 칸 합(20+113+172+31+86+49+189). sources 662 = hashes + 2.
  **이 등식은 총계가 얼마든 성립해야 한다 — 깨지면 그때가 사고다.**
  **08-14 칸과 총계 셋은 하한이다** — 이 레코드를 쓰는 행위 자체가 포착을 만든다(세션 64 §3.7.3).
- **★ 최종 재계수** `[2026-08-14T16:35:25Z — PR 머지와 레코드 갱신 뒤]`: blob **783** ·
  hashes **667** · sources **669** · `hook:shadow` **585**(span 끝 `16:35:05Z`) · drops **431** ·
  commit `077b4acc8fa1` · 훅 중복 **0건**(sha256 `DCC1B40B…`) · `RET=[336h] SRC=[pwsh-profile]` ·
  **프로세스 3개 — 마감 직전 신규 기동 없음**. **07-21~08-13 합 587 불변**, 08-14 칸 **196**.
  정합 `783 − 116 = 667` = 일별 칸 합(20+113+172+31+86+49+196), sources `669 = 667+2`.
  **배율도 1 그대로**(원장 +7 : blob +7 : hash +7). **§7 기준선은 이 수로 인계한다** —
  위 블록은 첫 커밋 시점 값으로 남긴다(하한 방식이 재편집을 요구하지 않는다는 세션 64 §3.7.3
  설계 그대로다).
- **★ 프로세스** `[16:23:21Z 중간 재계수]`: **3개** — 7488(`06:48:55Z`, 세션 62부터 생존) ·
  7652(`08:59:42Z`, 세션 63) · **16628(`15:43:10Z`, 이 세션)**. 전부
  `claude.exe ← pwsh ← WindowsTerminal 5796`. **codex 유래 0이고 마감 직전 신규 기동도 없었다.**
  **이 세션의 서버 기동은 한 번이고 삭제 0** — 세션 62~64 와 합쳐 **아홉 번 연속**이다.
- **★★ 중간 재계수** `[2026-08-14T17:58:02Z — 계획 작업 뒤. 손실 사고 *전* 값이다. 인계값이
  아니다 — 아래 18:33:24Z 블록이 인계값이다]`:
  blob **803** · hashes **687** · sources **689** · `hook:shadow` **605**(span 끝 `17:56:24Z`) ·
  drops **431** · commit `077b4acc8fa1` · 훅 중복 **0건**(sha256 `DCC1B40B…`) ·
  `RET=[336h] SRC=[pwsh-profile]` · 회수 줄 여덟 칸 **t0 동일**.
  **07-21~08-13 합 587 불변**, 08-14 칸 **216**. 정합 `803 − 116 = 687` = 일별 칸 합
  (20+113+172+31+86+49+216), sources `689 = 687+2`. **배율 1**(원장 +20 : blob +20 : hash +20).
  **★ 이 블록의 587·803·687·689 를 인용하지 마라** — 35분 뒤 손실 100개가 그것을 바꿨다(§3.6.3).
  여기 남겨 두는 이유는 **사고 직전 값이 있어야 삭제량을 셀 수 있기 때문**이다.
- **★★★★ 진짜 최종 재계수** `[2026-08-14T18:33:24Z — 손실 사고 뒤. 이것이 인계값이다]`:
  blob **753** · hashes **637** · sources **639** · `hook:shadow` **655**(span 끝 `18:33:14Z`) ·
  drops **431** · commit `077b4acc8fa1` · 훅 중복 **0건** · `RET=[336h] SRC=[pwsh-profile]` ·
  회수 줄 여덟 칸 **t0 동일**.
  **★ 고정 일곱 칸이 587 → 487 로 줄었다**(§3.6.3): 07-21 **116** · 08-08 **15** · 08-09 **82** ·
  08-10 **130** · 08-11 **9** · 08-12 **86** · 08-13 **49**. 08-14 칸 **266**.
  정합 `753 − 116 = 637` = 일별 칸 합(15+82+130+9+86+49+266), sources `639 = 637+2` — **등식은
  깨지지 않았다.** **§7 기준선은 이 수다. 587 을 인용하지 마라.**
- **★★★ 프로세스** `[18:33:24Z]`: **4개** — 7488 · 16628 · 16148 · **17224(`18:07:00Z`,
  `codex.exe` 부모 — §3.6.3 의 그 서버. 죽이지 않는다)**. 10104 는 죽었다.
- **★★ 프로세스** `[17:58:02Z 중간 재계수]`: **4개** — 7488(`06:48:55Z`) · 16628(`15:43:10Z`,
  이 세션 시작) · **16148(`17:46:44Z`, 신규)** · **10104(`17:47:44Z`, 신규)**. 7652 는 죽었다.
  전부 `claude.exe` 계보이고 **codex 유래 0.**
  **★ 마감 직전에 새 서버 둘을 잡았다** — 세션 61·62·64 에 이어 네 번째다. 그 두 기동 시점의
  72h 대상 305+ 가 **한 칸도 안 줄었다**(고정 일곱 칸 587 불변) ⇒ **삭제 0.**
  **이 시점까지 이 세션의 서버 기동은 셋이고 전부 삭제 0** — 세션 62~64 와 합쳐 열한 번
  연속이었다. **★ 그 연속은 9분 뒤 끊겼다**: `18:07:00Z` 의 넷째 기동(codex 계보 17224)이
  100개를 지웠다(§3.6.3). **열두 번째 기동이 첫 삭제다.**
- **켠 채 남긴 것**: 없다. 게이트·리뷰의 백그라운드 작업은 전부 완료 회수했고 뮤테이션 프로브는
  즉시 원복해 `git diff` 로 확인했다. **설치 0 · 저장소 밖 파일 변경 0 · 일회성 테스트 0.**

## 5. 이월 사항

- ~~**PR #47 — CI 확인 후 머지가 남았다**~~ — **닫혔다**(`833bc4b`, `16:33:47Z`). CI 두 run 전부
  초록이었고 windows flake 도 안 났다. **★ 설치는 판정 뒤 그대로다** — 이 수정은 설치본이
  안 갈리므로 아직 안 돈다. 즉 `doctor [9]` 의 마커 사각은 **로컬 CLI 에서만** 닫혔다.
- ~~**`main` 의 `7ab1b6f` 가 미푸시다**~~ — 밀었다(`4b5eb46` 과 함께). **마감 HEAD 는 `git log` 로
  본다** — 이 세션은 커밋을 여러 번 얹었고 마지막이 레코드 갱신이다.
- **★★★ 새 이월 — 축 C 구현이 착수 대기다. 계획은 검토를 통과했다.**
  `docs/superpowers/plans/2026-08-14-purge-audit-log.md`(다섯 태스크, `5ff001b` → 검토 반영
  `972e401`). **Codex 한 패스 + 서브에이전트 리뷰에서 나온 16건을 전부 반영했다**(§3.6.2).
  `[소유자 결정]` **실행 방식은 `subagent-driven-development`** — 태스크당 fresh 서브에이전트 +
  태스크 리뷰. **구현은 다음 세션이다.** 설치만 판정 뒤이고 구현·머지는 지금 해도 된다(§3.6).
- **★★★★ 새 이월(사고): codex 진입점이 336h 를 못 받는다**(§3.6.3). 세션 65 가 `codex-companion`
  을 직접 띄웠고 그 체인의 서버가 72h 로 돌아 **100개를 지웠다.** `[소유자 결정]` **판정일까지
  Codex 를 쓰지 마라** — 크로스모델 패스·`codex:rescue`·companion 직접 실행 전부. 서브에이전트
  리뷰(로컬)는 무관하다. **원인 규명(체인의 어느 고리에서 env 가 갈리는가)은 별건이고, 그
  조사 자체가 또 서버를 띄울 수 있으니 판정 뒤가 안전하다.**
- **★ 새 이월(별건): `san` 규칙이 두 벌이다**(검토 소견 F9). `internal/store` 의 `sanPurgeField`
  와 `internal/hook` 의 `appendDrop` 안 `san` 클로저가 같은 규칙(탭·개행·CR → 공백, 64자, 빈 값
  `-`)을 각자 든다. **두 벌이 갈리는 것을 막는 장치가 없다** — store 쪽 테스트는 자기 사본만
  잰다. 진짜 해소는 store 에서 export 하고 hook 이 그것을 부르는 것이고(import 방향이 이미
  허용한다) `internal/hook` 을 건드리므로 축 C 범위 밖이다. 계획 주석이 그 사실을 적어 뒀다.
- ~~**CHANGELOG 에 PR #47 이 없다**~~ — `1c76394` 가 닫았다. **다음 태그 전에 `[Unreleased]` 를
  다시 훑어라** — 지금 거기에는 F10(artifact id 재사용)과 PR #47 둘이 있다.
- **★ 새 이월(관측): 스펙의 함수 이름을 코드로 확인하지 않으면 구현자가 없는 함수를 찾는다.**
  §3.6.1 이 그것을 두 번 겪었다(`PurgeHookOnly` vs `PurgeHookOnlyOlderThan`, `doctorOut` 의
  storeRoot). **승인된 스펙이라도 코드와 대조하기 전에는 미검증 주장이다.**
- **★ 새 이월: `bannedHortatoryVocab` 테스트가 `manualGroupHint` 를 스캔하지 않는다**(재리뷰 관측).
  `TestDoctorOutputNoHortatoryVocabulary` 의 픽스처에 수동 그룹이 없어 그 문자열이 그 검사의
  사정권 밖이다. 지금 문면에는 트립할 ASCII 토큰이 없어 무해하지만, **다시 쓰면 아무도 안 본다.**
- **★ 새 이월: `heldGroupHint` 의 단정형은 미룬 선택이다**(§3.4.1). 잔여 실패 모양이
  `manualGroupHint` 와 같고, 그 문면이 `CHANGELOG.md`·설계 v0.19 문서에 **축자 인용**돼 있어
  고치는 것이 문서화된 계약 편집이다. **기록된 선택이지 못 본 문제가 아니다.**
- **★ 새 이월(선존, 리뷰 M5): `scanRegisteredHooks` 와 `hook uninstall` 의 훑는 범위가 다르다.**
  전자는 모든 이벤트 키를, 후자는 `hookRegistrations` 여섯만 훑는다. 마커 있는 그룹이
  `UserPromptSubmit` 같은 키에 있으면 doctor 는 제거 가능 부류로 세고 uninstall 을 안내하는데 그
  명령은 *"제거할 항목 없음"* 을 낸다 — **F3 과 같은 모양이다.** 새 manual 부류는 이 문제를 안
  겪는다(그 힌트가 uninstall 로 지워진다고 약속하지 않으므로).
- **★ 새 이월(선존, 리뷰 M3·M4)**: `codexHooksScope` 의 둘째 반환값이 양쪽에서 버려진다 ·
  세 술어를 모두 빠져나가는 그룹(객체가 아니거나 언마샬 실패)에 채널이 없다. **후자는 호스트가
  쓰는 파일에서 도달 가능한 형태가 아니라 리뷰어도 "비용을 들일 자리는 아니다"라고 봤다.**
- **★ 새 이월(리뷰 소유자 판단 1): `n == 0` 일 때 `[9]`·`[16]` 의 선두 토큰**이
  `옛 그룹 없음, …1그룹` 으로 읽혀 한 줄 안에서 충돌한다. `heldGroupHint` 선례 그대로이고 기존
  단정 넷이 그 접두에 걸려 있어 **바꾸는 것은 결함 수정이 아니라 계약 변경**이다.
- **★ 새 이월(리뷰 소유자 판단 3): `runHookUninstall` 은 보존 사실을 말하지 않는다.** doctor 를
  안 거치고 uninstall 만 돌린 사용자는 못 읽는다. 이번엔 범위로 기각했지만 **재리뷰가 "그
  선택지는 닫히지 않았다"고 봤다** — `manualGroupHint` 의 괄호가 이미 *"보존한다"* 를 *"그
  명령이 지울 수 있다"* 의 함의 없이 적는 법을 보여준다.
- **축 C 는 소유자 승인 완료**(§3.3, 커밋 `7ab1b6f`). **판정 뒤 `writing-plans`** 로 가고, 그때
  스펙을 설계 버전에 D-number 로 흡수한다.
- **★ 판정 분모가 아니라 처방의 전제가 오염돼 있다** — 포착의 78.5%건 / 87.6%B 가 Edit 응답이고
  지금은 끌 수 없다. **판정일 규칙은 세션 64 가 확정했다. 그날 정하지 마라.**
- **판정일 규칙 전문은 §7 에 그대로 실어 인계한다** — 세션 64 §3.11 이 원본이다.
- F12(조건 미충족)·F13·트라이그램 축·고아 blob 116개·두 계상 이력(`09:10Z` 경계)은 그대로다.
  **이 머신에 `sqlite3` 가 없다** — 포착 건의 귀속은 못 재고 집계와 CAS 의 비로만 판정한다.
- **판정까지** — `2026-08-26T01:22:18Z`. 그전에는 결정표 **행 1**. **12일 남았다.**
- **08-22 부터 336h 정상 만료** — **창은 336h 그대로. 소유자 판정 완료, 이 결정은 닫혔다.**

## 6. 상시 프로토콜 (이번 세션이 낸 것)

- **★★★ 테스트가 무언가를 잠근다는 것은 그 테스트의 통과로 증명되지 않는다 — 한 번 뒤집어 재라.**
  I1 이 정확히 그것이었다: 리뷰어의 지적은 *"단정이 없다"* 가 아니라 *"순서를 바꿔도 전 스위트가
  초록이다"* 였고, 새 단정을 넣고 초록인 것은 **그 단정이 존재한다**는 사실만 말한다. 한 줄
  뒤집고 한 번 돌리면 *"실제로 그것을 잡는다"* 가 추론에서 **관측**이 된다(`h=0 manual=1`).
  이 저장소가 세 층으로 쌓아 온 계열의 다음 칸이다: 검사가 **무엇을** 안 보는지(세션 63) →
  **왜** 안 보는지, 술어 어느 줄에서 탈락하는지(세션 64) → **그 검사가 실제로 잡는지**(이 세션).
  값은 싸다 — Edit 둘과 테스트 한 번이다.
- **★★★ 리뷰어의 결론이 맞아도 그 결론의 *근거*는 틀릴 수 있고, 코드에 남는 것은 근거다.**
  §3.4.1: 내가 `heldGroupHint` 를 단정형으로 두기로 한 판단은 재리뷰도 지지했지만, 내가 주석에
  적은 근거(*"마커가 플러그인의 별도 존재를 함의한다"*)는 거짓이었다. **결론이 같다고 넘기면
  다음 사람이 그 문장에 기대 판단한다.** 리뷰 findings 를 기각할 때 결론만 지키지 말고 **기각
  근거를 리뷰어에게 되돌려 반박 기회를 줘라** — 다섯 기각 중 하나에서 근거가 정정됐다.
- **★★ 문면을 조건형으로 고칠 때 *지시*도 함께 조건에 걸어라.** 리뷰어는 *"한 단어"* 라 했지만
  인과절만 조건형으로 하고 명령절(*"지우세요"*)을 단정형으로 두면 **절반만 고친 것**이다 — 그
  사용자가 실제로 하는 행동은 명령절이 정한다. 재리뷰가 이 확장을 옳다고 확인했다.
- **★★ 진단이 조건을 단정할 때, 그 조건을 *볼 수단이 코드에 있는지* 먼저 확인해라.** doctor 에는
  플러그인 훅 탐지기가 아예 없었다 — 그래서 *"겹쳐서 두 번 일어난다"* 는 관측이 아니라 추측이었다.
  **없는 관측을 문면이 단정하면 그것은 오보를 권하는 안내다.**
- **★ 리뷰 라운드 뒤 재리뷰는 같은 리뷰어를 이어가는 것이 싸고 정확할 수 있다.** 자기 지적의
  의도를 가장 잘 알고 컨텍스트를 재사용하며, **기각한 항목에 반박할 기회**가 그 자리에서 생긴다.
  대신 브리핑에 *"무엇을 어떻게 고쳤고 무엇을 왜 기각했는지"* 를 전부 적어 편향을 막아야 한다.
- **★ 승인된 걸음이 열어 준 문 밖으로 나가지 마라.** 소유자가 고른 것은 *"서브에이전트 리뷰만 →
  PR"* 이었다. 리뷰가 낸 소유자 판단 셋과 선존 Minor 셋은 **전부 기각하고 이월로 적었다** — 좋은
  지적이어도 이번 요청의 범위 밖이면 레코드가 가져갈 자리이지 이번 diff 가 가져갈 자리가 아니다.

- **★★★ 제약을 인계할 때는 그 제약이 *무엇을* 막는지까지 적어라 — 안 적으면 다음 사람이 전부
  막힌 것으로 읽는다.** §3.6: *"판정까지 감시만"* 이라는 문장이 세션 64 에서 *"열린 작업 둘이
  끝나면 백로그가 빈다"* 는 뜻으로 쓰였는데, 이 세션이 그것을 **계측 제약**으로 읽고 §7 에 그대로
  옮겨 적었다. 실제로 막힌 것은 셋뿐이고(`ctr_*` 호출 · 설치 · 채택 레버 스펙 읽기) **코드 변경과
  머지는 무관했다.** 그 프레임이 같은 세션 안에서 **CHANGELOG 누락**을 낳았다 — 릴리스 문서
  갱신이 "지금 할 일"의 시야에서 빠졌다. **제약 목록은 금지 항목이 아니라 *경계*로 적어라**:
  무엇이 막혔고 **무엇이 안 막혔는지**를 함께.
- **★★ 계획을 쓰는 것이 스펙 검증이다.** §3.6.1 이 승인된 스펙에서 코드와 어긋난 사실 둘을
  잡았는데, 둘 다 **실제 호출부를 열어 봐야만** 보였다(`runPurgeHookOnly` 가 무엇을 부르는가,
  `doctorOut` 이 storeRoot 를 어떻게 만드는가). **스펙 리뷰는 문면끼리의 정합만 잡고 코드와의
  정합은 못 잡는다** — 승인 도장이 찍힌 문서라도 구현 계획을 쓰기 전에는 미검증 주장이다.
  그리고 그 검증은 계획의 부산물이 아니라 **계획이 정확해지는 유일한 길**이다.
- **★★★★ 검토를 위한 도구가 관측 대상을 파괴할 수 있다 — egress 체크포인트에 *"이 호출이 무엇을
  띄우는가"* 를 넣어라.** §3.6.3: 보안 체크포인트를 규정대로 먼저 진술했지만 그 항목들은
  **무엇이 전송되는가**만 물었고 **무엇이 실행되는가**는 묻지 않았다. `codex-companion` 호출은
  프로세스 체인을 만들고 그 끝에서 **이 프로그램의 서버가 뜬다** — 그 서버가 336h 를 못 받아
  100개를 지웠다. **검토 도구가 읽기 전용이라는 가정이 틀린 자리가 여기다.** 계측이 도는
  저장소에서는 egress 판단만으로 부족하고 **그 명령이 부수적으로 띄우는 것**까지 세어야 한다.
  세션 61 §9.4 의 *"계통 하나를 재고 일반화하지 마라"* 와 짝이다: 그 규칙은 **무엇을 재야 하는지**
  를 말했고, 이 항목은 **언제 재야 하는지** — 외부 도구를 부르기 **전** — 를 말한다.
- **★★★ 크로스모델 패스는 「가장 위험한 선택 하나」로 좁힐 때 값을 한다 — 서베이로 보내면 안
  나온다.** §3.6.2: Codex 에 보낸 것은 계획 전문이 아니라 **동시 append 가 파서를 통과할 수
  있는가** 하나였고, 그 하나에서 **내가 스펙에 자신 있게 적은 근거가 거짓임**이 나왔다. 스킬이
  *"설계 전체 서베이가 아니라 한 선택"* 을 규정하는 이유가 이것이고, 부수 효과로 **egress 도
  최소가 된다**(별칭화된 발췌 하나면 족하다). 넓게 물었으면 그 답은 다른 열 개에 묻혔을 것이다.
- **★★★ 내가 *자신 있게* 쓴 근거일수록 검토 대상이다.** 그 문장(*"잘린 append 는 줄을 짧게
  만들지 길게 만들지 않는다"*)은 선례와의 차이까지 설명하며 **적극적으로 논증한** 자리였고,
  그래서 리뷰가 그냥 지나치기 쉬운 모양이었다. **논증이 촘촘할수록 반증 비용이 커 보이고, 그것이
  바로 외부 시선이 필요한 지점이다.** 세션 65 §6 의 *"결론이 맞아도 근거는 틀릴 수 있다"* 와 같은
  계열의 두 번째 사례다 — 이번엔 결론(`>= 9` 유지)도 살았고 근거만 갈렸다.
- **★★ self-review 를 형식으로 돌리지 마라 — 스펙의 각 항목을 태스크에 짚어 보면 실제로 빠진다.**
  이 세션의 계획은 스펙 §6 의 일곱 테스트 중 **여섯 번째(status 우선순위)를 통째로 빠뜨렸고**
  체크리스트를 실제로 훑어서야 찾았다. 그 항목이 없으면 `partial` 이 `budget`·`cancelled` 를
  이기는 순서를 아무것도 잠그지 않는다 — 세션 65 §6 첫 항목과 정확히 같은 부류의 구멍이다.

## 7. 다음 세션 시작 프롬프트

> docs/prompts/2026-08-14-session-65-review-ladder-and-spec-approval.md 를 읽고 이어서 작업한다.
> 이 레코드가 최신이다. **세션 64 레코드는 §3.8(포착 구성비)·§3.11(분모의 소속)만 필요할 때 보면
> 되고, 세션 63 이전 레코드의 포착 수치·08-13/08-14 칸은 인용하지 마라** — 계상과 값이 다르다.
>
> ## 지금 상태
>
> - **계측 t0 = 2026-08-12T01:22:18Z** — 유효. 판정 가능 시점은 2026-08-26T01:22:18Z 이고
>   그전에는 결정표 행 1("14일이 안 지났다")이다.
> - **336h 창 실효 시작 = 2026-08-12T18:19:19Z**.
> - **★★★★ 손실이 다시 났다 — 세션 65 마감에 100개다**(§3.6.3). **세션 62 이후 서버 기동 열한 번
>   연속 삭제 0 이었고 열두 번째가 그것을 끊었다.**
>   **원인은 그 세션이 띄운 Codex 다**: `context-router.exe ← codex.exe ← bash.exe ← bash.exe ←
>   node.exe` 계보의 서버가 `18:07:00Z` 에 뜨면서 **336h 를 못 받아 72h 로 돌았다.** 지워진 범위가
>   72h 창과 정확히 맞고(08-11 일부까지, 08-12·08-13 무사) `startupPurgeMaxHashes`(100)까지 돌았다.
>   **회수 줄 여덟 칸은 t0 와 같다 — 판정 지표는 안 오염됐다.**
> - **★★★★ `[소유자 결정]` 판정일까지 Codex 를 쓰지 마라.** 크로스모델 패스도, `codex:rescue` 도,
>   companion 직접 실행도 전부다. **그 진입점이 336h 를 못 받는다는 것이 실측이고 어느 고리에서
>   갈리는지는 아직 안 쟀다.** 서브에이전트 리뷰(로컬)는 무관하니 그대로 쓴다.
> - **★★ `bash.exe` 계보가 무사하다는 옛 관측을 믿지 마라** — 세션 61~64 의 그 네 번과 이번은
>   **진입점이 다르다.** 관측이었지 보증이 아니었다. **계통 이름이 아니라 그 기동이 실제로 무엇을
>   받았는지가 판정 근거다.**
> - **구멍은 `claude.exe` 계보에만 닫혀 있다** — 그 셋은 336h 를 받았고 이번 사고에도 무사했다.
>   지운 전력이 있는 쪽은 이제 **`ChatGPT.exe` 계보와 `codex.exe` 계보 둘**이다.
> - **훅 중복 없음** — 도구 출력 1건당 원장 1행이다. **모델 변경 경로는 두 번 시험돼 두 번 다
>   견뎠다**(mtime 만 갱신, sha256 `DCC1B40B…AC5DF6` 불변). **플러그인 설치 경로는 아직 미시험.**
> - **★ 판정 분모가 아니라 처방의 전제가 오염돼 있다** — 포착의 **78.5%건 / 87.6%B 가 Edit 응답**
>   이고 지금은 끌 수 없다. **판정일 규칙은 아래에 그대로 실어 뒀다. 그날 정하지 마라.**
> - **★★★ 열린 작업은 하나다 — 축 C 구현이 착수 대기이고, 계획은 이미 검토를 통과했다.**
>   계획 `docs/superpowers/plans/2026-08-14-purge-audit-log.md`(다섯 태스크) · 스펙
>   `docs/superpowers/specs/2026-08-14-purge-audit-log-design.md` — **둘 다 읽어라**(계획이
>   스펙에서 논증한다). 세션 65 가 **Codex 한 패스 + 서브에이전트 리뷰**를 돌려 나온 **16건을
>   전부 반영했다**(critical 1 · important 4 · minor 11, 커밋 `972e401`).
>   `[소유자 결정]` **실행 방식은 `subagent-driven-development`** — 태스크당 fresh 서브에이전트 +
>   태스크 리뷰. **다시 묻지 않아도 된다.**
> - **★★★ "판정까지 감시만"은 틀린 전제다**(세션 65 §3.6). 계측이 막는 것은 셋뿐이다:
>   ① 이 프로젝트에서 `ctr_*` 호출 ② **설치** ③ 채택 레버 스펙 읽기.
>   **코드 변경·커밋·PR·머지·게이트·설계·계획은 전부 무관하다** — 세션 65 가 PR 하나를 리뷰
>   래더까지 태워 머지했는데 회수 줄 여덟 칸이 t0 와 글자 그대로 같다. **진짜로 기다려야 하는
>   것은 채택 레버 하나뿐**이고 그것도 판정 결과가 설계의 입력이기 때문이다.
> - PR #47 은 세션 65 가 닫았다(머지 커밋 `833bc4b`, CI 두 run 전부 초록 — windows flake 없음).
>   **★ 그 수정은 아직 안 돈다** — 설치본이 `077b4acc8fa1` 그대로라 `doctor [9]` 의 마커 사각은
>   **로컬 CLI 에서만** 닫혔다. 훅 중복 감시는 계속 **확인 3** 이 한다.
> - **`CHANGELOG.md` 의 `[Unreleased]` 에 둘이 쌓여 있다** — F10(artifact id 재사용)과 PR #47.
>   **새 기능을 머지하면 그 절에 함께 적어라** — 세션 65 가 그것을 빠뜨렸다가 뒤늦게 잡았다.
> - 설치본은 그대로 0.20.0 @ 077b4acc8fa1. **판정일까지 설치하지 마라.**
>
> ## 첫 일 — 확인 여섯
>
> 1. Get-CimInstance Win32_Process -Filter "Name='context-router.exe'"  — 서버와 그 부모.
>    **계보를 끝까지 봐라.**
>    **★★ 옛 레코드의 *"`bash.exe` 계보는 네 세션 연속 무사했다"* 를 인용하지 마라 — 세션 65 가
>    반증했다**(§3.6.3). **그 계보가 이번에 100개를 지웠다.** 진입점이 달랐을 뿐이다. **계통
>    이름이 아니라 그 기동이 336h 를 받았는지가 판정 근거이고, 그것은 프로세스 목록으로는
>    안 보인다.** 지운 전력이 있는 쪽은 이제 `ChatGPT.exe` 와 `codex.exe` 둘이다.
>    **★★★ `18:07:00Z` 에 뜬 PID 17224(부모 `codex.exe`)가 그 서버다 — 살아 있으면 죽이지 마라.**
>    그 기동의 퍼지는 이미 돌았으므로 살아 있는 것 자체는 무해하고, **죽이면 다음에 다시 뜨면서
>    남은 236 개 중 또 100 개를 집는다.**
> 2. "UTCNOW=$([DateTime]::UtcNow.ToString('yyyy-MM-ddTHH:mm:ssZ')) RET=[$env:CTR_SHADOW_RETENTION] SRC=[$env:CTR_RETENTION_SOURCE]"
>    — 게이트. **RET 이 336h 여야 한다.** 비어 있으면 즉시 재처방이다. SRC=pwsh-profile 은 정상이고,
>    **INH 는 이 셸에서 상속 경로의 증거가 아니다.**
>    **★ UTC 날짜를 여기서 받아라 — 확인 5 의 08-14 칸 판정이 그것에 걸린다.**
> 3. **훅 중복 재발 검사** — Claude Code 는 플러그인 설치·모델 변경·설정 변경 때 settings.json 을
>    다시 쓴다:
>    Select-String -Path "$env:USERPROFILE\.claude\settings.json" -Pattern 'context-router hook' -SimpleMatch
>    **0건이어야 한다.** 나오면 다시 두 벌이고 그때부터의 포착 수치는 또 2배다.
> 4. context-router stats  — 회수 줄 여덟 칸이 t0 와 같은가.
>    기준선: calls=49 legacy=49 나머지 여섯 칸 0, age_s 전부 없음.
>    **주 1회 스냅샷은 ~08-21 이 다음이다.**
> 5. **손실 감시 — doctor [15] hashes · [14] sources · CAS blob 일별 분포를 함께 본다.**
>    **★ 세션 65 마감에 손실 100개가 났다(§3.6.3) — 고정 칸 값이 바뀌었다. 587 을 인용하지 마라.**
>    **고정값은 07-21~08-13 일곱 칸이고 그 합은 이제 487 이다. 2026-08-14T18:33:24Z 기준:**
>    07-21 **116**(고아) · 08-08 **15** · 08-09 **82** · 08-10 **130** · 08-11 **9** ·
>    08-12 **86** · 08-13 **49** · **08-14 266**.
>    **하한: blob ≥753 · hashes ≥637 · sources ≥639.** 크면 정상이고,
>    **크다는 것 자체는 아무 문제도 아니다.**
>    **★★ 08-08~08-11 네 칸은 아직 72h 대상이고 배치가 남았다** — 그 기동이 상한(100)까지 돌았다.
>    **336h 를 못 받은 서버가 또 뜨면 남은 236 개 중 100 개가 더 빠진다.**
>
>    **★★★ 아래 판정 규칙 넷은 그대로지만, 첫째 행(삭제)에는 이제 원인 후보가 붙는다:**
>    **336h 를 못 받은 진입점**(세션 65 는 Codex 였다). 확인 1 의 계보를 보되 **계통 이름으로
>    판정하지 마라** — 더 빠른 증거는 **삭제 범위와 창의 비교**다. 줄어든 칸이 `오늘 − 72h`
>    이전에만 몰려 있고 그 뒤 칸이 무사하면 **72h 로 돈 기동**이다(세션 65 가 그렇게 읽어 냈다).
>    **★ 그리고 세 축 정합 등식은 손실에도 안 깨진다** — 그 등식은 *"내부 정합"* 을 볼 뿐
>    *"얼마나 남았는지"* 를 안 본다. **손실 판정은 고정 칸이 하고, 등식은 계상 오류가 한다.**
>
>    **★★★ 08-14 칸의 성격은 확인 2 가 받은 UTC 날짜가 정한다 — 먼저 그것부터 보라:**
>    - **UTC 로 아직 08-14 면** → 08-14 는 **오늘 칸**이다. 하한 266, 늘기만 한다.
>    - **UTC 로 08-15 이후면** → 08-14 는 **옛 칸이 되지만 고정값이 아니다.** 그 칸에 세션 64·65 의
>      레코드·스펙·계획과 그 세션들이 읽은 소스의 blob 이 **266개 이상**(하한 — 마감 검토 중에도
>      계속 늘었다) 들어 있고, 네가 그중 아무거나 다시 읽으면
>      재포착되어 **08-14 가 줄고 오늘 칸이 그만큼 는다.** 이것이 **판정표 둘째 행(재포착 mtime
>      이동)이고 정상이다 — 조치하지 마라.** 세션 64 가 08-13 칸에서 이미 관측했다.
>      **손실 판정은 07-21~08-13 일곱 칸(합 487)으로만 하라.**
>
>    분포는 doctor 가 안 낸다. 직접 센다:
>    Get-ChildItem 'C:\Users\js\AppData\Local\context-router\projects\context-router-4d307dacd75b\artifacts' -Recurse -File
>    을 LastWriteTimeUtc 의 MM-dd 로 Group-Object 한다.
>    **판정은 수가 아니라 이 규칙으로 한다:**
>    - 오늘 이전 칸이 X 줄고 **총계 세 축도 함께 줄었다** → **삭제.** 확인 1 의 계보를 본다.
>    - 오늘 이전 칸이 X 줄었는데 **총계는 불변/증가이고 오늘 칸이 X 늘었다** →
>      **재포착 mtime 이동이다. 정상, 조치 없음.**
>    - 오늘 이전 칸 불변, 총계 증가 → 순수 신규. 정상.
>    - **★ 08-22 부터**: 줄어든 칸이 **오늘로부터 14일 이상 된 날짜**면 → **정상 336h 만료다.
>      조치 없음.** 그보다 최근 칸이 줄었을 때만 사고다.
>    정합 검산도 같이 한다: **blob 총수 − 116(고아) = hashes = 일별 칸 합**, sources = hashes + 2.
>    **이 등식은 총계가 얼마든 성립해야 한다 — 깨지면 그때가 사고다.**
> 6. context-router doctor 의 [17] commit — 077b4acc8fa1 인가 (설치본이 안 갈렸는가).
>
> ## 포착 수치를 읽는 법
>
> - **출발점은 655**(2026-08-14T18:33:24Z, hook:shadow 655회 37,204,838 B, span 끝 18:33:14Z
>   — **하한이다**). **증가분은 1:1 이다** — 세션 64 가 독립 확증했고 세션 65 가 여러 구간에서
>   재확인했다. **★ 다만 손실 사고 구간(§3.6.3)에서는 blob 이 줄었으므로 그 구간에 1:1 을
>   적용하지 마라** — 포착은 늘고 삭제가 그보다 컸다.
> - **★ 이 수의 78.5%는 Edit 응답이다.** 판정에 쓸 때 아래 「판정일 규칙」의 걸음 4를 반드시 밟아라.
> - **★ 이 수가 빨리 는다고 놀라지 마라.** 16 KiB 넘는 도구 응답은 전부 포착된다 — 대형 파일 Read
>   뿐 아니라 **대형 파일 Edit 도** 매번 파일 크기만큼 올린다. **구현 세션은 특히 빠르게 는다.**
>   판정 지표는 이 수가 아니라 **회수 줄**이다.
> - **세션 62 이전 수치와 직접 비교하지 마라** — 2026-08-14T09:10Z 를 경계로 계상이 바뀌었다.
>   세션 60·61·62 의 172·388·476 은 실제 ~86·~194·~238 이다.
>
> ## 그다음 — 축 C 구현 착수
>
> **확인 여섯이 초록이면 바로 구현으로 간다.** 계획이 다섯 태스크로 나뉘어 있고 각각 독립
> 테스트 가능하다: store 쓰기 → cli 파서 → 기동 배선(**0건 분기 신설**) → CLI 세 경로 배선 →
> `doctor [21]`.
>
> **★★★ 확인 5 가 또 손실을 내면 구현보다 그것이 먼저다.** 배치가 남아 있어(236개, 기동당 상한
> 100) **336h 를 못 받은 기동이 한 번만 더 있으면 100개가 또 빠진다.** 그때는 구현을 멈추고
> ① 어느 진입점이었는지 ② **그 진입점을 이 세션이 직접 불렀는지**를 확정해 소유자에게 보고해라 —
> 세션 65 의 원인은 **자기가 부른 검토 도구**였고, 그것이 가장 놓치기 쉬운 자리다.
>
> 1. **계획과 스펙을 둘 다 읽는다** — 계획이 스펙에서 논증하므로 스펙 없이는 근거를 못 본다.
> 2. **`subagent-driven-development` 로 간다** — 소유자가 이미 지정했다(태스크당 fresh
>    서브에이전트 + 태스크 리뷰). **계획 검토는 세션 65 가 이미 돌렸다** — 같은 것을 다시
>    돌리지 마라(Codex 는 체크포인트당 한 패스다).
> 3. **태스크마다 게이트 다섯을 직접 돌린다** — 옮겨 적지 마라.
> 4. **태스크 체크포인트마다 서브에이전트 리뷰.** 크로스모델 패스는 외부 전송이라 **현재 요청이
>    있을 때만** 돈다.
> 5. **머지까지 간다. 설치는 판정일(2026-08-26) 뒤다** — 그 구분이 이 계획의 전제다.
> 6. **CHANGELOG `[Unreleased]` 에 적는다**(계획 Task 5 Step 6). 세션 65 가 PR #47 에서 그것을
>    빠뜨렸다가 뒤늦게 잡았다.
>
> **★ 계획대로 안 되는 것이 나오면 계획을 고쳐라.** 세션 65 가 계획을 쓰며 **승인된 스펙의 오류
> 둘**을 잡았다(§3.6.1) — 둘 다 실제 호출부를 열어 봐야만 보였다. 구현이 스펙과 부딪히면
> **코드를 비틀지 말고 스펙을 코드로 확인해라.** 승인 도장이 찍힌 문서라도 코드와 대조하기
> 전에는 미검증 주장이다.
>
> - 훅 진단의 새 이월 넷(세션 65 §5)은 **판정과 무관하지만 이번 세션 범위 밖**이다. 축 C 를
>   끝내고 여유가 있으면 소유자에게 물어라.
> - **소유자가 다른 축을 지시하면 그것이 이긴다.**
>
> ## ★★★ 판정일 규칙 — 세션 64 가 미리 확정했다. **그날 정하지 마라.**
>
> **왜 미리 박는가**: 축 A 가 포착의 **78.5%건 / 87.6%B 가 Edit 응답**임을 밝혔고, 그 사실은
> *"소비 0"* 의 **해석**을 바꿀 수 있다. 규칙을 판정일에 정하면 수치를 보고 규칙을 맞추게 된다.
>
> **판정일에 이 다섯을 이 순서로 하고, 순서를 바꾸지도 줄이지도 마라:**
>
> 1. **회수 줄 여덟 칸을 그대로 적는다.** D104 가 읽는 것은 이 칸들이고 **착수 조건의 분모는
>    `resolved_artifacts + missed` 다**. **★ 포착 수로 그 분모를 갈아 끼우지 마라** — 포착 수가
>    답하는 것은 *"얼마나 포착되는가"* 이고 창의 길이가 묻는 것은 *"포착된 것이 회수되는가"* 다.
>    **다른 질문의 분모다.**
> 2. **포착의 구성비를 판정일 시점으로 다시 계산해 옆에 적는다.** 방법:
>    `context-router session export --project context-router-4d307dacd75b --worktree <각각>` →
>    `tool_result_summary` 의 `<도구명>: <크기>B` 를 도구별 집계. **CLI 라서 `ctr_*` 원장을
>    안 건드린다.** worktree 는 7개이고 주 worktree 가 압도적이다. 세션 64 값은
>    Edit 78.5%건/87.6%B · Read 13.5%/7.6% 였다(t0 이후 520건).
> 3. **결정표에서 행을 고른다.** 여기까지는 규칙대로다 — 해석을 섞지 마라.
> 4. **★ 그 행의 처방을 채택하기 전에 전제를 한 번 검사하고, 결과를 레코드에 적는다:**
>    *"이 처방은 「모델이 회수를 안 한다」를 전제하는가? 그렇다면 포착의 78%가 Edit 응답이라는
>    사실이 그 전제를 약화시키는가?"* — **답이 무엇이든 적는다.**
> 5. **그 뒤에** 채택 레버 스펙을 읽는다. 먼저 읽으면 그 문면이 `ctr_fetch` 를 권해 판정 지표를
>    오염시킨다. **판정 전에는 이 저장소에서 그 스펙을 열지 마라.**
>
> **4를 건너뛰면 *"채택 미달"* 과 *"채택 대상이 애초에 희소"* 가 같은 수로 보인다 — 처방이
> 정반대다.** 전자는 레버를 넣고, 후자는 **포착을 줄인다**(Edit 매처 제외 등).
>
> ## 그 밖의 상태
>
> - **설치는 하지 마라.** 판정일(2026-08-26) 전에 채택 레버가 살아나면 판정 지표가 오염된다.
> - **★★★ 336h 대상이 0개인 것은 2026-08-22 까지다.** 08-08→08-22 · 08-09→08-23 · 08-10→08-24 ·
>   08-11→08-25 · 08-12→**판정일** · 08-13→08-27 · 08-14→08-28.
>   **★ 판정일의 336h 대상은 약 236개다**(검산: 15+82+130+9 — 세션 65 의 손실 100개가 그만큼
>   줄였다, §3.6.3). **옛 레코드의 "약 336개"를 인용하지 마라.** 그 삭제는 **정상 정책 동작이지
>   사고가 아니다.**
>   **★ 창은 336h 그대로 둔다 — 소유자 판정 완료. 이 결정은 닫혔다.**
> - **★★ 72h 대상은 236개이고 배치가 남아 있다** — 세션 65 의 그 기동이 상한(`startupPurgeMaxHashes`
>   = 100)까지 돌았다. **336h 를 못 받은 서버가 또 뜨면 100개가 더 빠진다.** 이걸 재려고 테스트를
>   만들지 마라 — 분포와 창 시작 시각의 비교가 답한다.
> - **소비는 여전히 0** (`ctr_fetch`·`ctr_search`·`ctr_session_summary` 전부 t0 이후 0회,
>   여섯 세션째). 결정표 행 2 로 갈 공산이 크다 — 판정일에 회수 줄을 인위적으로 올리지 마라.
> - **프로브 blob 둘**(`0fc3bc88…`·`3884b8a0…`, 각 16,461 B, 08-14 칸)은 세션 63 계측의 산물이다.
>   **지우지 마라** — CAS 파일만 지우면 DB 행이 dangling 이 된다. **2026-08-28 에 정상 만료로 빠진다.**
> - **코드베이스 전반은 세션 64 가 읽고 소유자에게 보고했다. 같은 것을 다시 읽지 마라** — 재고만 는다.
> - F12·F13·트라이그램 축·고아 blob 116개·두 계상 이력은 그대로다. **이 머신에 `sqlite3` 가 없다.**
> - **훅 진단의 새 이월 넷**(세션 65 §5): `bannedHortatoryVocab` 이 수동 힌트를 안 스캔한다 ·
>   `heldGroupHint` 단정형은 미룬 선택이다 · `scanRegisteredHooks` 와 `hook uninstall` 의 훑는
>   범위가 다르다(선존, F3 계열) · `n==0` 선두 토큰의 "없음 + N그룹" 충돌은 계약 변경이다.
>   **전부 PR #47 밖이고 판정 뒤 후보다.**
> - **마감 직전에 프로세스와 재고를 다시 세고 레코드에는 그 수를 적어라.** 세션 61·62·64 가 마감
>   직전에 새 서버를 잡았다 — 세션 63·65 가 안 잡혔다고 이 프로토콜을 놓으면 못 본다.
>   **그리고 마감은 커밋으로 끝나지 않는다** — 커밋 뒤에 재고를 움직이는 작업을 하면 다시 세고
>   레코드를 갱신해 두 번째 커밋을 남겨라.
> - **이번 세션은 짧게 닫는 세션이 아니다** — 확인 여섯은 게이트일 뿐이고 본 일은 축 C 구현이다.
>   **다만 마감 프로토콜은 그대로다**: 마감 직전 재계수 → 레코드 → 커밋. **구현 세션은 재고를
>   많이 움직이므로** 그 재계수가 특히 중요하다.
>
> ## 밟으면 안 되는 자리
>
> - **336h 를 못 받은 채 서버를 띄우지 마라.** 확인 2 가 그 게이트다.
> - **★ 카운터 하나로 계상 배율을 판정하지 마라** — 독립적으로 세는 다른 카운터 옆에 세워라.
>   `원장 2행 : blob 1개` = 배율 2 · `원장 2행 : blob 2개` = 배율 1. 비율이 반복해서 정확히 딱
>   떨어지면(1/2, 1/3, 2:1) 현상보다 **계상**을 먼저 의심하라.
> - **★ 계측기가 세는 *단위*를 확인했으면 그 수의 *구성비*도, 그 분모가 *어느 질문의* 분모인지도
>   확인해라.** 세 층이다: 단위 → 구성비 → 분모의 소속.
> - **★★★★ 같은 계통도 진입점이 다르면 다른 답을 낸다** — 아래 *"계통 하나를 재고 일반화하지
>   마라"* 의 다음 층이다. 세션 61~64 가 **네 번 무사히 관측한 `bash.exe` 계보**가 세션 65 에서
>   100개를 지웠다(§3.6.3). **계통 이름은 식별자가 아니라 라벨이고, 판정 근거는 그 기동이 실제로
>   336h 를 받았는지다.** 그리고 그것은 프로세스 목록으로 안 보인다 — **삭제 범위와 창의 비교**가
>   유일한 사후 증거다.
> - **★★★★ 검토를 위해 부르는 외부 도구가 서버를 띄울 수 있다** — 그것이 세션 65 의 손실
>   원인이다. **egress 판단만으로 부족하다**: *"무엇이 전송되는가"* 뿐 아니라 *"이 호출이 무엇을
>   실행하고 무엇을 띄우는가"* 를 함께 세라. 계측이 도는 저장소에서 **읽기 전용이라는 가정이
>   틀리는 자리**가 거기다.
> - **★ 테스트가 통과한다고 그것이 무언가를 잠근다고 읽지 마라**(세션 65 §6). 결정을 잡는다고
>   주장하는 단정은 **한 번 뒤집어 빨개지는지 재라.** Edit 둘과 테스트 한 번이면 관측이 된다.
> - **★ 리뷰 findings 를 기각할 때 결론만 지키지 말고 기각 근거를 리뷰어에게 되돌려라** — 세션 65
>   는 다섯 기각 중 하나에서 **근거가 틀렸다는 정정**을 받았다(결론은 유지). **코드에 남는 것은
>   근거이고, 다음 사람은 그 문장에 기대 판단한다.**
> - **★ 진단 문면이 조건을 단정하기 전에 그 조건을 볼 수단이 코드에 있는지 확인해라.** 없으면 그
>   단정은 오보를 권하는 안내다. **조건형으로 고칠 때는 인과절만이 아니라 지시도 함께 걸어라.**
> - **★ 감시 세션에서 대량 소스를 읽었으면 기준선을 그 *뒤*에 다시 잡아라.** doctor·stats·작은
>   출력은 재고를 안 늘리지만 **16 KiB 넘는 소스 여러 개는 다르다.**
> - **★ `doctor [9]` 의 초록을 "훅 중복 없음"으로 읽지 마라** — 그 검사는 마커를 첫 관문으로
>   요구한다(PR #47 이 그 구멍을 고쳤지만 **설치 전이라 아직 안 돈다**). 중복은 확인 3 이 본다.
> - **재고 감시를 회수 줄로 하지 마라** — 판정 지표가 t0 와 글자 그대로 같은 채로 재고의 3분의 1이
>   사라질 수 있다. 세션 58~60 이 그래서 200개를 못 봤다.
> - **감시 칸을 날짜로 고정하지 마라** — "오늘 이전"이 규칙이다. **그리고 "오늘 이전 칸이 줄면
>   퍼지"로 단정하지 마라** — 재포착 mtime 이동과 08-22 이후의 정상 만료가 같은 모양으로 보인다.
> - **순감과 삭제량을 같은 수로 쓰지 마라** — 견고한 것은 순감뿐이고 삭제량은 하한이다.
> - **삭제를 로그로 찾지 마라** — MCP 서버 stderr 는 어디에도 안 남는다. **삭제 시각을 파일시스템
>   자국으로 역산하지도 마라** — 그 자국은 생성과 삭제를 못 가른다. 손실이 났으면 **어느 계통이
>   336h 를 못 받았는지**부터 봐라.
> - **INH 를 상속 경로의 증거로 읽지 마라** — 그 표식을 설정하지 않는 부모에서 잰 것만 관측이다.
> - **계통 하나를 재고 일반화하지 마라** — 세 계통을 각각 재고 나서야 "구멍이 없다"를 말할 수 있었다.
> - **CTR_SHADOW_RETENTION 에 14d 를 쓰지 마라** — ParseDuration 에 d 가 없어 조용히 72h 다.
> - **회수 줄을 인위적으로 올리지 마라.** 검증 회수는 판정 대상이 아닌 프로젝트에서 한다.
> - **판정 대상이 될 수 있는 프로젝트에서 채택 레버의 스펙·계획을 읽지 마라** — 그 문면이
>   ctr_fetch 를 권한다.
> - **침묵을 단정하는 테스트를 stdout == "" 하나로 쓰지 마라** — 긍정 케이스와 한 축만 다른
>   픽스처를 쓰고 부작용 축을 함께 봐라.
> - **외부 검토 초점에 의역을 싣지 마라** — 별칭화는 식별자에 거는 것이지 검토 대상에 거는 것이 아니다.
> - **게이트 결과를 옮겨 적지 말고 직접 돌려라.** 완료 알림의 진단이 보고와 모순되면 직접 확인.
> - **가드를 만나면 우회하지 마라** — 그 한 칸은 사람에게 넘겨라. `Grep head_limit=0` 은 실제로
>   거부된다(D54 가드) — `head_limit` 을 지정해 다시 물어라.
> - stats·doctor 는 퍼지를 돌리지 않는다(serve 기동만 돈다). 훅도 안 돈다.
>   **그리고 16 KiB 미만 도구 출력은 포착되지 않는다** — 감시 명령 한 바퀴가 재고를 안 늘린다.
> - go test 에 -p 1 필수. 게이트 다섯을 && 로 잇지 마라 — internal/exec 는 이 머신의 PowerShell
>   실행 정책 때문에 빨갛다(환경 조건이지 회귀가 아니다). 관측된 이름:
>   TestNpmrcScratchConfigWins · TestRunShellPS51ProviderWriteReturns · TestD78SignalOnExit ·
>   TestD78NoObservableSideEffects.
> - 행 번호를 주석·문서에 적지 마라 — 심볼 이름을 적어라.
> - **프로세스 목록은 그 순간의 생존자만 보여준다** — 지나간 기동은 안 잡히고 다가올 기동도 안
>   잡힌다. 마감 직전에 그 자리에서 다시 세라.
>
> 문제 발견되면 fix 혹은 브레인스토밍하여 사용자에게 질문하고, 문제 없으면 진행한다.
> ultrathink
