# v0.12.1 (D68–D72) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.12 머지 게이트가 남긴 저위험 항목 다섯을 닫는다 — CI 공급망 고정, 미검출 정적 오류, node 레그 커버리지, 죽은 진단 신호, 버전 표기 프로세스.

**Architecture:** 런타임 동작을 바꾸지 않는다. 코드 변경은 D71(진단 출력 한 곳)과 D69가 드러내는 errcheck 수정뿐이고, 나머지는 CI 워크플로와 주석이다. 그래서 patch 릴리스다 — 단 D69 수정이 오류 전파를 바꾸는 것으로 드러나면 그 시점에 배송 등급을 재판정한다(설계 §1.1).

**Tech Stack:** Go 1.26 · GitHub Actions · golangci-lint v2.12 · gofumpt v0.10.0

**스펙:** `docs/context-router-design-v0.13-ko.md` — D68~D72와 §2의 해당 항목이 이 계획의 계약이다. 스펙과 이 계획이 어긋나면 **스펙이 이긴다**.

## Global Constraints

- **테스트는 `go test -p 1`로 돈다** — 메모리 캡 규율(CLAUDE.md). 전체 스위트를 캡 없이 돌리지 않는다.
- **`gofumpt -l .`이 빈 출력이어야 한다.** 커밋 전 확인한다.
- **파일 출력은 UTF-8(BOM 없음) · LF.**
- **`git add -A` 금지.** 작업 트리에 추적하지 않는 로컬 설정(`.claude/`·`.codex/`)이 있다 — 항상 경로를 명시해 add한다.
- **커밋 트레일러**: `Co-Authored-By: Claude <모델명> <noreply@anthropic.com>` — 실행 세션의 실제 모델명을 쓴다.
- **작업 순서 게이트(설계 §1.3)**: D69(Task 3)가 D68(Task 5)보다 먼저다. 액션을 먼저 핀하면 CI 실패 원인이 두 갈래로 섞인다.
- **BASE는 실행 원장에 기록한다.** `HEAD~1`을 쓰지 않는다.

---

### Task 1: D71 — drops 진단에 사유별 마지막 발생 시각 병기

해결된 문제가 진단에 영구히 남아 신호가 죽는 것을 고친다. `[12]`의 `unknown-session=399`는 전부 2026-07-21~23의 이력이고 D51(v0.9) 이후 발생이 0건인데, 누적 집계만 보여 현재 발생 중인 것처럼 읽힌다.

**Files:**
- Modify: `internal/cli/hook_install.go:902-953` (`dropsByReason`, `formatDropCount`)
- Modify: `internal/cli/cli.go:1680-1686` (doctor `[12]` 호출부)
- Test: `internal/cli/hook_install_test.go` (신규 1개 + 기존 3개 갱신)

**Interfaces:**
- Produces: `dropsByReason(path string) (total int, reasons map[string]int, lastSeen map[string]int64)` — 셋째 반환값이 신규. 사유별 ts 최댓값이며, 정수 변환에 실패한 값은 키를 만들지 않는다.
- Produces: `formatDropCount(total int, reasons map[string]int, lastSeen map[string]int64) string` — 셋째 인자가 신규. `<사유>=<건수>@<YYYY-MM-DD>`(UTC), `lastSeen`에 키가 없으면 날짜를 붙이지 않는다.

- [ ] **Step 1: 실패 테스트를 쓴다**

`internal/cli/hook_install_test.go` 끝에 추가:

```go
// TestDropsLastSeenUTC — D71: 사유별 마지막 발생 시각을 UTC 날짜로 병기한다. 역순 ts가 섞여도
// 최댓값을 쓰고(로그가 시간순임을 가정하지 않는다), 정수 변환에 실패한 ts는 집계만 하고 병기를
// 생략하며, unparsed에는 ts가 없으므로 병기하지 않는다.
func TestDropsLastSeenUTC(t *testing.T) {
	p := filepath.Join(t.TempDir(), "session.drops.log")
	// a: 역순(둘째 줄이 더 과거) → 최댓값 1700000000 = 2023-11-14 UTC.
	// b: 자릿수 초과로 int64 변환 실패 → 집계는 되고 병기는 없다(isUnixTS는 통과한다).
	// 빈 줄 → unparsed.
	data := "1700000000\ta\n1600000000\ta\n" +
		"99999999999999999999\tb\n" +
		"\n"
	if err := os.WriteFile(p, []byte(data), 0o600); err != nil {
		t.Fatal(err)
	}
	total, reasons, last := dropsByReason(p)
	if total != 4 {
		t.Fatalf("total=%d want 4(빈 줄 포함 줄 수 계약)", total)
	}
	if reasons["a"] != 2 || reasons["b"] != 1 || reasons["unparsed"] != 1 {
		t.Fatalf("reasons=%v want a=2,b=1,unparsed=1", reasons)
	}
	if last["a"] != 1700000000 {
		t.Fatalf("last[a]=%d want 1700000000(최댓값 — 마지막 줄이 아니다)", last["a"])
	}
	if _, ok := last["b"]; ok {
		t.Fatalf("last[b]는 없어야 한다(int64 변환 실패): %v", last)
	}
	if _, ok := last["unparsed"]; ok {
		t.Fatalf("last[unparsed]는 없어야 한다: %v", last)
	}
	got := formatDropCount(total, reasons, last)
	want := "4(a=2@2023-11-14,b=1,unparsed=1)"
	if got != want {
		t.Fatalf("formatDropCount=%q want %q", got, want)
	}
}
```

- [ ] **Step 2: 컴파일 실패를 확인한다**

Run: `go test -p 1 ./internal/cli/ -run TestDropsLastSeenUTC`
Expected: FAIL — `assignment mismatch: 3 variables but dropsByReason returns 2 values`

- [ ] **Step 3: `dropsByReason`에 `lastSeen`을 더한다**

`internal/cli/hook_install.go:902-922`를 교체한다:

```go
func dropsByReason(path string) (int, map[string]int, map[string]int64) {
	f, err := os.Open(path)
	if err != nil {
		return 0, nil, nil
	}
	defer func() { _ = f.Close() }()
	total, reasons, lastSeen := 0, map[string]int{}, map[string]int64{}
	sc := bufio.NewScanner(f)
	sc.Buffer(make([]byte, 0, 64*1024), 1<<20) // 긴 줄에도 스캔 중단 방지(countDropsLog 관례 보존)
	for sc.Scan() {
		total++ // 빈 줄도 센다 — 기존 countDropsLog의 total 의미 보존(줄 수 계약)
		fields := strings.Split(sc.Text(), "\t")
		// D43: 정확 2필드(구) 또는 정확 5필드(신)만 수용 — 그 외 필드 수는 unparsed(느슨 수용 금지).
		if (len(fields) == 2 || len(fields) == 5) && fields[1] != "" && isUnixTS(fields[0]) {
			reasons[fields[1]]++
			// D71: 사유별 ts 최댓값. isUnixTS는 자릿수 상한을 보지 않으므로 int64 변환이 따로
			// 실패할 수 있다 — 그 줄은 집계에만 남기고 병기는 생략한다(키를 만들지 않는다).
			if ts, convErr := strconv.ParseInt(fields[0], 10, 64); convErr == nil {
				if prev, ok := lastSeen[fields[1]]; !ok || ts > prev {
					lastSeen[fields[1]] = ts
				}
			}
		} else {
			reasons["unparsed"]++ // 빈 줄·비숫자 ts·필드 수 불일치·사유 없음 전부 unparsed
		}
	}
	return total, reasons, lastSeen
}
```

같은 파일 임포트에 `strconv`를 더한다(없으면).

- [ ] **Step 4: `formatDropCount`에 병기를 더한다**

`internal/cli/hook_install.go:939-953`을 교체한다:

```go
func formatDropCount(total int, reasons map[string]int, lastSeen map[string]int64) string {
	if total == 0 {
		return "0"
	}
	keys := make([]string, 0, len(reasons))
	for k := range reasons {
		keys = append(keys, k)
	}
	sort.Strings(keys)
	parts := make([]string, 0, len(keys))
	for _, k := range keys {
		// D71: 마지막 발생 시각을 UTC 날짜로 병기한다 — 로컬 타임존이면 골든이 3-OS CI의
		// 타임존에 따라 갈린다. ts가 없는 사유(unparsed·변환 실패)는 건수만 낸다.
		if ts, ok := lastSeen[k]; ok {
			parts = append(parts, fmt.Sprintf("%s=%d@%s", k, reasons[k], time.Unix(ts, 0).UTC().Format("2006-01-02")))
			continue
		}
		parts = append(parts, fmt.Sprintf("%s=%d", k, reasons[k]))
	}
	return fmt.Sprintf("%d(%s)", total, strings.Join(parts, ","))
}
```

같은 파일 임포트에 `time`을 더한다(없으면).

- [ ] **Step 5: doctor 호출부를 맞춘다**

`internal/cli/cli.go:1680-1686`을 교체한다:

```go
	rootTotal, rootReasons, rootLast := dropsByReason(filepath.Join(storeRoot, dropsFileName))
	wtTotal, wtReasons, wtLast := 0, map[string]int(nil), map[string]int64(nil)
	if canon.ProjectID != "" && canon.WorktreeID != "" {
		wtTotal, wtReasons, wtLast = dropsByReason(filepath.Join(storeRoot, "projects", canon.ProjectID, "worktrees", canon.WorktreeID, dropsFileName))
	}
	fmt.Fprintf(w, "[12] drops: store-root=%s worktree=%s total=%d\n",
		formatDropCount(rootTotal, rootReasons, rootLast), formatDropCount(wtTotal, wtReasons, wtLast), rootTotal+wtTotal)
```

- [ ] **Step 6: 기존 테스트 셋을 갱신한다**

`internal/cli/hook_install_test.go`에서 세 곳을 고친다.

`TestDropsByReasonFiveFields`(:564):

```go
	_, got, _ := dropsByReason(p) // 실제 시그니처: (total, reasons, lastSeen)
```

`TestDropsByReason_StrictParsing`(:539):

```go
	total, reasons, _ := dropsByReason(path)
```

doctor 골든(:522) — 테스트 데이터의 ts가 1·2·3이라 UTC 날짜는 전부 1970-01-01이다:

```go
		if !strings.Contains(out, "[12] drops: store-root=2(a=1@1970-01-01,b=1@1970-01-01) worktree=3(x=1@1970-01-01,y=1@1970-01-01,z=1@1970-01-01) total=5") {
```

- [ ] **Step 7: 패키지 테스트가 통과하는지 본다**

Run: `go test -p 1 ./internal/cli/`
Expected: PASS (신규 `TestDropsLastSeenUTC` 포함, 갱신한 셋도 통과)

- [ ] **Step 8: 포맷과 전체 스위트를 확인한다**

Run: `gofumpt -l internal/cli/` → 출력 없음
Run: `go test -p 1 -count=1 ./...` → PASS

- [ ] **Step 9: 커밋한다**

```bash
git add internal/cli/hook_install.go internal/cli/cli.go internal/cli/hook_install_test.go
git commit -m "feat(v0.12.1): doctor [12]에 사유별 마지막 발생 시각 병기 (D71)"
```

---

### Task 2: D72 — buildinfo 주석을 실무에 맞춘다

주석이 dev 사이클에 `-dev` 접미를 처방하는데 v0.10·v0.11·v0.12 릴리스가 일관되게 따르지 않았다. 지키지 않는 절차를 처방하는 주석이 부정확한 쪽이므로 주석을 고친다. **코드·동작 변경은 없고 테스트를 추가하지 않는다**(설계 §2 D72).

**Files:**
- Modify: `internal/buildinfo/buildinfo.go:6-8` (주석만)

**Interfaces:**
- Consumes: 없음
- Produces: 없음 — `productVersion` 값과 `ProductVersion()` 시그니처는 그대로다.

- [ ] **Step 1: 주석을 교체한다**

`internal/buildinfo/buildinfo.go`의 6-8행을 교체한다(9행의 `var productVersion = "0.12.0"`은 건드리지 않는다):

```go
// productVersion — 릴리스 수동 지점 유일 1곳. **릴리스 커밋에서 이 값을 그 릴리스 버전으로
// 올린다** — dev 사이클에 "-dev" 접미를 두지 않는다(v0.10~v0.12의 실무가 그러했고, 지키지 않는
// 절차를 처방하는 주석이 부정확한 쪽이다: 설계 v0.13 D72). marker 등호 비교 보호와 태그 일치
// 검증은 스펙 v0.10 D56·§1.3. var(const 아님) — 향후 -ldflags -X 주입 전환 여지
// (unexported + accessor = 외부 변경 차단).
```

- [ ] **Step 2: 값이 바뀌지 않았음을 확인한다**

Run: `go run ./cmd/context-router version`
Expected: `0.12.0` (주석만 고쳤으므로 변화 없음)

- [ ] **Step 3: 빌드와 포맷을 확인한다**

Run: `go build ./... && gofumpt -l internal/buildinfo/`
Expected: 빌드 성공, gofumpt 출력 없음

- [ ] **Step 4: 커밋한다**

```bash
git add internal/buildinfo/buildinfo.go
git commit -m "docs(v0.12.1): buildinfo 주석을 실제 릴리스 절차에 맞춤 (D72)"
```

---

### Task 3: D69 — GOOS=windows lint 스텝과 드러난 오류 수정

린터는 한 GOOS의 파일 집합만 본다. windows 전용 파일 네 개(`internal/ident/realpath_windows.go` · `internal/transform/worker_windows.go` · `internal/store/store_lock_windows.go` · `internal/sandbox/run_windows.go`)가 ubuntu lint 대상 밖이었다.

**판정은 두 단계다**(설계 D69): ① 스텝이 대상 파일을 실제로 포함하는지 먼저 확인한다 ② 그 확인이 통과한 뒤의 결과를 받는다. 포함 확인 없이 "처음부터 통과"를 오구성으로 단정하면 대상이 이미 lint-clean인 정상 경우까지 오구성으로 읽혀 계약이 충족 불가능해진다.

**Files:**
- Modify: `.github/workflows/ci.yml` (`lint` 잡, 98행 `version: v2.12` 뒤에 스텝 셋 추가)
- Modify: 스텝이 드러내는 파일들 (미확정 — 아래 Step 3 참조)

**Interfaces:**
- Consumes: 없음
- Produces: 없음 — CI 스텝과 오류 수정뿐이다.

- [ ] **Step 1: 로컬에서 먼저 돌려 실제 목록을 얻는다**

```bash
go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12
GOOS=windows golangci-lint run ./... 2>&1 | tee /tmp/ctr-goos-windows-lint.txt
```

Expected: windows 전용 파일 경로가 포함된 이슈 목록. **이 출력이 Step 3의 수정 목록이다.**
session-35가 관측한 errcheck 13건은 **재현하지 않은 기대값**이므로 실제 건수는 이 출력이 정한다.

만약 이슈가 0건이면 Step 2의 포함 확인을 먼저 통과시킨 뒤 "갭 없음"으로 받는다 — 그때는 Step 3을 건너뛰고 Step 4로 간다.

- [ ] **Step 2: 대상 파일이 lint 범위에 있는지 확인한다**

```bash
for pkg in ./internal/ident ./internal/transform ./internal/store ./internal/sandbox; do
  GOOS=windows go list -f '{{join .GoFiles "\n"}}' "$pkg" | grep '_windows\.go$' || echo "MISSING in $pkg"
done
```

Expected: 네 패키지에서 각각 `*_windows.go`가 한 줄 이상. `MISSING`이 나오면 스텝 구성을 먼저 의심한다(그 상태의 lint 통과는 의미가 없다).

- [ ] **Step 3: 드러난 이슈를 저장소 관례대로 고친다**

Step 1의 출력을 항목별로 처리한다. errcheck 이슈는 이 저장소의 기존 형태를 따른다 — 의도적으로 무시하는 반환값은 `_ =`로 명시하고, 그렇지 않으면 오류를 전파한다:

```go
// 무시가 의도인 경우 (기존 형태 — internal/cli/hook_install.go:907과 동형)
defer func() { _ = f.Close() }()

// 전파가 맞는 경우
if err := os.MkdirAll(dir, 0o700); err != nil {
	return fmt.Errorf("...: %w", err)
}
```

각 수정이 **동작을 바꾸는지** 판단해 기록한다 — 오류 전파가 새로 생기면 그것은 런타임 동작 변경이므로 설계 §1.1의 배송 등급 재판정 조건에 걸린다.

- [ ] **Step 4: CI에 스텝 셋을 추가한다**

`.github/workflows/ci.yml`의 `lint` 잡에서 `version: v2.12`(98행) 다음에 이어 붙인다:

```yaml
      # D69: 기본 lint는 한 GOOS의 파일 집합만 본다 — windows 전용 파일 4개(ident/realpath_windows.go,
      # transform/worker_windows.go, store/store_lock_windows.go, sandbox/run_windows.go)가 ubuntu
      # 런에서 대상 밖이었다. 액션과 같은 버전을 직접 설치해 GOOS=windows로 한 번 더 돈다.
      - name: install golangci-lint (action과 동일 버전)
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12

      # 판정 1단계: 대상 파일이 실제로 범위에 있는지 먼저 확인한다. 이 확인 없이 통과만 보면
      # 스텝이 그 파일을 못 봐도 초록이다(설계 v0.13 D69).
      - name: verify windows-only files are in lint scope
        run: |
          set -euo pipefail
          for pkg in ./internal/ident ./internal/transform ./internal/store ./internal/sandbox; do
            if ! GOOS=windows go list -f '{{join .GoFiles "\n"}}' "$pkg" | grep -q '_windows\.go$'; then
              echo "no windows-only file in lint scope for $pkg"
              exit 1
            fi
          done

      # 판정 2단계: 포함이 확인된 뒤의 결과를 받는다.
      - name: golangci-lint (GOOS=windows)
        run: GOOS=windows golangci-lint run ./...
```

- [ ] **Step 5: 로컬에서 두 판정을 재확인한다**

Run: Step 2의 루프 → `MISSING` 없음
Run: `GOOS=windows golangci-lint run ./...` → 이슈 0건
Run: `go test -p 1 -count=1 ./...` → PASS (수정이 회귀를 만들지 않았는지)
Run: `gofumpt -l .` → 출력 없음

- [ ] **Step 6: 커밋한다**

```bash
git add .github/workflows/ci.yml
# Step 3에서 고친 파일들을 경로로 명시해 함께 add한다 (git add -A 금지)
git commit -m "ci(v0.12.1): GOOS=windows lint 스텝 + 드러난 정적 오류 수정 (D69)"
```

---

### Task 4: D70 — node 인터프리터 레그 CI 커버리지

`test` 잡이 bun을 설치하므로 `detectJS`가 항상 bun을 고른다 — node 경로가 CI에서 한 번도 실행되지 않았다. bun 없는 잡에서 js·ts 러너를 돌리고, **통과 여부가 아니라 실제로 어느 인터프리터를 탔는지**로 판정한다.

typescript도 이번 범위다 — node argv 구성과 버전 게이트가 이미 있고 CI의 node 버전(`'22'`)이 그 게이트(≥22.7)를 충족한다.

**Files:**
- Create(잡): `.github/workflows/ci.yml`의 `test-node-leg` 잡 (`crossbuild` 잡 앞에 둔다)
- Test: `internal/exec/exec_test.go` (신규 1개)

**Interfaces:**
- Consumes: `Run(ctx context.Context, scratchParent, selfExe string, req Request) (Response, error)` — `internal/exec`의 기존 진입점. `Request`의 언어 필드는 `Language`, 응답의 러너 라벨 필드는 `Runner`다(같은 파일의 기존 테스트가 쓰는 형태를 따른다).
- Produces: 없음

- [ ] **Step 1: 실패 테스트를 쓴다**

`internal/exec/exec_test.go` 끝에 추가한다:

```go
// TestRunnerLabelNodeLeg — D70: bun이 없는 환경에서 javascript·typescript가 node 레그를 탄다.
// 라벨은 실행 파일 basename이라 windows에서 ".exe"가 붙으므로 확장자를 제거하고 비교한다.
// bun이 설치된 환경에서는 detectJS가 bun을 고르므로 이 단정이 성립하지 않는다 — SKIP한다
// (거짓 실패를 만들지 않는다). CI의 bun 없는 잡이 이 테스트의 실행 지점이다.
func TestRunnerLabelNodeLeg(t *testing.T) {
	if _, err := osexec.LookPath("bun"); err == nil {
		t.Skip("bun 설치됨 — detectJS가 bun을 고르므로 node 레그가 아니다")
	}
	for _, lang := range []string{"javascript", "typescript"} {
		t.Run(lang, func(t *testing.T) {
			resp, err := Run(context.Background(), t.TempDir(), "", Request{
				Language: lang,
				Code:     "console.log(1)",
			})
			if err != nil {
				t.Fatalf("Run(%s): %v", lang, err)
			}
			// runnerLabel은 filepath.Base(bin) + (version이 있으면) " " + version이다
			// (internal/exec/exec.go:179-185) — 첫 토큰을 떼고 확장자를 제거해 비교한다.
			first := strings.Fields(resp.Runner)[0]
			base := strings.TrimSuffix(first, filepath.Ext(first))
			if base != "node" {
				t.Fatalf("runner=%q base=%q want node", resp.Runner, base)
			}
		})
	}
}
```

임포트 확인: `context` · `path/filepath` · `strings`는 이 파일에 이미 있을 것이다. `os/exec`는 같은 파일이 `exec` 패키지 안이라 이름이 겹치므로 **`osexec "os/exec"` 별칭으로** 임포트한다(파일에 이미 그런 별칭이 있으면 그 이름을 쓴다).

- [ ] **Step 2: 로컬에서 실행해 상태를 확인한다**

Run: `go test -p 1 ./internal/exec/ -run TestRunnerLabelNodeLeg -v`
Expected: 개발기에 bun이 설치돼 있으면 두 서브테스트가 **SKIP**이다. bun을 PATH에서 빼고 돌리면 PASS해야 한다 — 그것이 이 테스트의 실제 검증이다:

```bash
# bun을 임시로 가린 PATH로 한 번 돌린다(디렉터리 삭제 금지 — PATH만 바꾼다)
PATH="$(echo "$PATH" | tr ':' '\n' | grep -v bun | paste -sd:)" go test -p 1 ./internal/exec/ -run TestRunnerLabelNodeLeg -v
```

Expected: PASS, 로그에 `runner` 라벨이 node로 찍힌다. FAIL이면 `Request`/`Response` 필드명을 파일의 기존 테스트와 맞춘다.

- [ ] **Step 3: CI 잡을 추가한다**

`.github/workflows/ci.yml`의 `crossbuild:`(100행) **앞에** 넣는다:

```yaml
  # D70: test 잡은 bun을 설치하므로 detectJS가 항상 bun을 고른다 — node 레그가 CI에서 한 번도
  # 실행되지 않았다. bun을 설치하지 않는 잡에서 js·ts 러너를 돌린다. 판정은 통과 여부가 아니라
  # 응답의 runner 라벨이며(TestRunnerLabelNodeLeg), 그 단정이 없으면 bun 잡을 한 번 더 돌린
  # 것과 구분되지 않는다.
  test-node-leg:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      # test 잡과 같은 라인 — TS 게이트 ≥22.7을 충족한다. bun은 설치하지 않는다(이 잡의 존재 이유).
      - uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: go test (js·ts 러너 — node 레그)
        run: go test -p 1 -count=1 ./internal/exec/ -run 'TestRunTS|TestRunJSIsolatesUserConfig|TestRunnerLabelNodeLeg' -v
```

- [ ] **Step 4: 확인한다**

Run: `gofumpt -l internal/exec/` → 출력 없음
Run: `go test -p 1 -count=1 ./internal/exec/` → PASS (개발기에서는 새 테스트가 SKIP일 수 있다)

- [ ] **Step 5: 커밋한다**

```bash
git add .github/workflows/ci.yml internal/exec/exec_test.go
git commit -m "ci(v0.12.1): node 인터프리터 레그 커버리지 + runner 라벨 단정 (D70)"
```

---

### Task 5: D68 — CI 액션 SHA 핀 + workflow permissions

**Task 3·4가 끝난 뒤에 한다**(설계 §1.3). 액션을 먼저 핀하면 새 스텝·잡을 추가할 때 다시 핀해야 하고, CI 실패 원인이 두 갈래로 섞인다.

사용처 **전부**를 핀한다 — 일부만 핀하면 "핀된 저장소"라는 관측이 거짓이 된다. Task 3·4가 `uses:`를 더 추가했으므로 그 줄들도 포함된다.

**Files:**
- Modify: `.github/workflows/ci.yml` (파일 상단 `permissions` + 모든 `uses:` 줄)

**Interfaces:**
- Consumes: 없음
- Produces: 없음

- [ ] **Step 1: 고유 액션의 커밋 SHA를 얻는다**

```bash
for r in actions/checkout:v6 actions/setup-go:v6 actions/setup-dotnet:v4 \
         actions/setup-node:v6 oven-sh/setup-bun:v2 actions/setup-python:v6 \
         golangci/golangci-lint-action:v9; do
  repo="${r%:*}"; tag="${r#*:}"
  sha="$(gh api "repos/$repo/commits/$tag" --jq .sha)"
  printf '%s@%s # %s\n' "$repo" "$sha" "$tag"
done
```

Expected: 7줄. 이 출력이 Step 3의 치환표다.

- [ ] **Step 2: 워크플로 최상위에 `permissions`를 넣는다**

`.github/workflows/ci.yml`의 1-6행을 교체한다:

```yaml
name: ci

# D68: 기본 토큰 권한을 읽기로 좁힌다. 이 저장소는 릴리스를 태그로만 하고 워크플로가 콘텐츠를
# 쓰지 않으므로 contents: read로 충분하다 — GitHub Release를 채널로 쓰기 시작하면 이 값을
# 함께 재검토한다(설계 v0.13 D68).
permissions:
  contents: read

on:
  push:
  pull_request:
```

- [ ] **Step 3: 모든 `uses:`를 SHA로 바꾸고 태그를 주석으로 병기한다**

Step 1 출력의 SHA로 각 줄을 치환한다. 형태:

```yaml
      - uses: actions/checkout@<sha>  # v6
      - uses: actions/setup-go@<sha>  # v6
        with:
          go-version-file: go.mod
```

`with:` 블록이 있는 항목은 `uses:` 줄만 바꾼다. `golangci/golangci-lint-action`도 같은 형태다:

```yaml
      - name: "golangci-lint (v2, default: standard + misspell)"
        uses: golangci/golangci-lint-action@<sha>  # v9
        with:
          version: v2.12
```

- [ ] **Step 4: 누락이 없는지 전수 확인한다**

```bash
grep -n 'uses:' .github/workflows/ci.yml | grep -v '@[0-9a-f]\{40\}' || echo "ALL PINNED"
```

Expected: `ALL PINNED`. 한 줄이라도 남으면 부분 핀이므로 고친다.

- [ ] **Step 5: 갱신 트리거를 파일에 남긴다**

`permissions` 블록 주석 아래에 한 줄 더한다:

```yaml
# 핀 갱신은 정기 주기를 두지 않는다 — 핀된 액션의 보안 권고나 그 액션이 필요해지는 기능
# 변경이 트리거다. 주기적 갱신은 핀의 목적(의도한 시점에만 움직인다)을 되돌린다.
```

- [ ] **Step 6: 커밋하고 CI를 확인한다**

```bash
git add .github/workflows/ci.yml
git commit -m "ci(v0.12.1): 액션 참조 SHA 핀 + workflow permissions (D68)"
git push
```

Expected: 푸시 후 CI 3-OS + lint + test-node-leg + crossbuild 전부 green. 실패하면 SHA 오타를 먼저 의심한다(액션을 못 찾는 오류는 즉시 드러난다).

---

### Task 6: CHANGELOG v0.12.1 절 + 릴리스

설계 §1.1이 "두 배송 모두 CHANGELOG 절을 갖는다"고 규정한다. v0.12.1 절의 항목은 D71의 진단 출력 변경이다.

**Files:**
- Modify: `CHANGELOG.md` (`## [0.12.0]` 앞에 새 절, 파일 말미 링크 정의)
- Modify: `internal/buildinfo/buildinfo.go:9` (`productVersion`)

**Interfaces:**
- Consumes: Task 1의 출력 형식(`<사유>=<건수>@<YYYY-MM-DD>`)
- Produces: 없음

- [ ] **Step 1: CHANGELOG 절을 쓴다**

`CHANGELOG.md`의 `## [0.12.0] — 2026-07-26` **앞에** 넣는다(날짜는 릴리스 당일로):

```markdown
## [0.12.1] — <YYYY-MM-DD>

### Changed

- **doctor `[12]` drops 진단에 사유별 마지막 발생 시각이 병기된다** (D71).
  형태는 `<사유>=<건수>@<YYYY-MM-DD>`(UTC)다. 누적 집계만 보이던 이전 출력은 이미 해결된
  문제를 현재 발생 중인 것처럼 읽히게 했다 — 시각이 있으면 그 구분이 한눈에 보인다.
  정수 변환에 실패하는 타임스탬프와 `unparsed`에는 병기하지 않는다(집계는 유지된다).

### Internal

- CI 액션 참조를 커밋 SHA로 고정하고 워크플로 토큰 권한을 `contents: read`로 좁혔다 (D68).
- `GOOS=windows` lint 스텝을 추가해 windows 전용 파일의 미검출 정적 오류를 닫았다 (D69).
- bun을 설치하지 않는 잡을 추가해 node 인터프리터 레그를 CI에서 실행한다 (D70).
- `buildinfo` 주석을 실제 릴리스 절차에 맞췄다 (D72).
```

Task 3의 수정이 오류 전파를 바꿨다면 그 항목을 `### Changed`로 올린다(설계 §1.1의 배송 등급 재판정 조건).

- [ ] **Step 2: 링크 정의를 더한다**

`CHANGELOG.md` 말미의 `[0.12.0]: ...` **위에** 넣는다:

```markdown
[0.12.1]: https://github.com/wotjr1649/context-router/compare/v0.12.0...v0.12.1
```

- [ ] **Step 3: `productVersion`을 올린다**

`internal/buildinfo/buildinfo.go:9`:

```go
var productVersion = "0.12.1"
```

- [ ] **Step 4: 버전 일치를 확인한다**

Run: `go run ./cmd/context-router version`
Expected: `0.12.1`

- [ ] **Step 5: 전체 검증**

Run: `go build ./... && go vet ./... && gofumpt -l .` → 빌드·vet 성공, gofumpt 출력 없음
Run: `go test -p 1 -count=1 ./...` → PASS

- [ ] **Step 6: 커밋한다 (태그는 사용자 승인 후)**

```bash
git add CHANGELOG.md internal/buildinfo/buildinfo.go
git commit -m "release(v0.12.1): CHANGELOG 절 + productVersion 0.12.1"
```

**태그와 푸시는 사용자 명시 승인으로만 한다.** 태그 후 CI의 `tag-version` 잡이 바이너리 버전과 태그 일치를 검증한다(D56).

---

## Self-Review

**1. 스펙 커버리지** — D68(Task 5) · D69(Task 3) · D70(Task 4) · D71(Task 1) · D72(Task 2) 전부 태스크가 있다. 설계 §2의 해당 검증 항목 대응: D68 전수 확인(Task 5 Step 4) · D69 두 단계 판정(Task 3 Step 1·2·5) · D70 확장자 제거 후 `node` 단정과 bun 없는 잡 한정(Task 4 Step 1·3) · D71 역순 ts·변환 실패·unparsed 미병기·골든 갱신(Task 1 Step 1·6) · D72 테스트 없음(Task 2). §1.1의 CHANGELOG 절 규정은 Task 6이 받는다.

**2. 미확정으로 남긴 것 하나** — Task 3 Step 3의 수정 목록은 스텝을 처음 돌린 출력이 정한다. 설계 D69가 "session-35의 errcheck 13건은 재현하지 않은 기대값이며 실제 건수는 스텝을 처음 돌리는 시점에 확정한다"고 규정했으므로, 이 계획도 건수를 적지 않는다. 수정 **방법**은 저장소의 기존 형태로 명시했다.

**3. 타입 일관성** — `dropsByReason`의 3반환값과 `formatDropCount`의 3인자가 Task 1의 Step 3·4·5·6에서 같은 시그니처로 쓰인다. `lastSeen`의 타입은 전 구간 `map[string]int64`다. Task 4의 `Run`·`Request.Language`·`Response.Runner`는 `internal/exec`의 기존 진입점이며, 필드명이 파일과 다르면 Step 2가 그것을 즉시 드러낸다.

**4. 순서 의존** — Task 5가 Task 3·4 뒤라는 게이트를 Global Constraints와 Task 5 머리글에 함께 적었다. Task 6은 Task 1의 출력 형식을 인용하므로 Task 1 뒤다. Task 1·2·3·4는 서로 독립이다.
