# context-router v0.16 (D85–D88) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `doctor [20]`의 감지와 `doctor --fix`의 고침이 같은 판정원에서 나오게 만들고, `--fix`가 사용자 설정을 축소하지 않게 하며, 관리 테이블 안의 정규화 불가 키를 무변경으로 닫는다.

**Architecture:** `installCodexConfigBlock`(순수 바이트 변환)이 유일한 판정원이 된다. 읽기 전용 진단 경로가 그 함수를 **같은 요청값으로** 호출하고, 결과를 라벨·권고로 번역하는 얇은 함수 하나를 `[20]`과 `doctorFixRegistrations`가 공유한다. 구간 판정 실패는 불리언에서 사유 열거형으로 올라가 사용자 문면에 실린다.

**Tech Stack:** Go 1.x 표준 라이브러리만(신규 의존 금지 — D80의 파서 비의존 원칙). 테스트는 `testing` 표준 패키지.

**Spec:** `docs/context-router-design-v0.16-ko.md` (커밋 `34d0f64` — 크로스모델·서브 적대적 리뷰 27건 반영본). 이 계획의 모든 절 참조(§0·§1.1·§1.2·§1.3·§2·§3)는 그 문서를 가리킨다.

**BASE:** 이 계획을 담은 마지막 커밋, 즉 **브랜치를 만드는 시점의 `main` HEAD**다. P0에서
`git rev-parse HEAD`로 그 값을 읽어 원장에 적는다 — 해시를 이 문서에 박지 않는다(계획을 고쳐
커밋하면 값이 바뀐다). **`HEAD~1`을 쓰지 않는다.** 코드 변경이 생기므로 **브랜치를 먼저
만든다**: `feat/v0.16-d85-d88`.

---

## Global Constraints

모든 태스크의 요구사항에 아래가 암묵적으로 포함된다.

- **빌드**: `go build ./...` 통과. **각 태스크가 끝난 시점에 패키지가 컴파일돼야 한다.**
- **린트**: `golangci-lint run ./...` → **0 issues**. 이 항을 생략하지 않는다 — v0.15에서 계획이 축자로 준 상수가 `unused`를 깨뜨렸고 컨트롤러의 전역 제약에 lint가 빠져 있었다. **미사용 열거형 값·상수를 미리 정의하지 않는다**(그 값을 세우는 태스크에서 함께 추가한다).
- **포맷**: `gofumpt -l .` 무출력.
- **테스트**: `go test -p 1 ./internal/cli/...` 형태로 **패키지를 좁혀** 돈다. 전체 스위트는 `go test -p 1 ./...`. 메모리 캡 규칙을 준수한다(대규모 스위트를 무제한으로 돌리지 않는다).
- **로컬 red 기준선 9건**: `internal/exec`의 `powershell` 레그다. 원인은 호스트 정책 차이(닫힌 BaseEnv 표 자식에서 `PSExecutionPolicyPreference`가 상속되지 않아 5.1이 실효 `Restricted`). **즉시 exit 1이며 무한 대기가 아니다.** 목록: `TestRunShellPS51ProviderWriteReturns` · `TestD78SignalOnExit/powershell/{explicit-exit,bare-exit,exit-zero,throw,normal}` · `TestD78NoObservableSideEffects/powershell/{job-and-subscriber-tables-clean,wait-job-terminates,remove-job-keeps-sidefile}`. **태스크마다 red 집합이 그 9건인지 확인하고 새 회귀로 오인하지 않는다.**
- **유도 FAIL 필수**: 새로 추가한 검사마다 **고침을 되돌려 FAIL을 확인한 로그를 남긴다.** "검사를 추가했다"와 "그 검사가 물린다"는 다르다(§1.3 게이트 3). 픽스처가 검증 대상 상태를 실제로 만드는지도 함께 확인한다.
- **파일 출력**: UTF-8(BOM 없음)·LF.
- **이스케이프 오염 검사**: 이 릴리스는 테스트 픽스처에 이스케이프 표기를 다룬다. **역슬래시는 개수를 방향별로 따로 센다** — 합계만 보면 하나↔둘 전환이 통과하고, 그때 픽스처는 검증 대상 상태를 만들지 못하면서 테스트는 PASS한다. **판정 지점은 커밋 blob**이다(autocrlf 호스트에서 작업 트리는 판정 지점이 아니다):
  ```bash
  git cat-file blob "HEAD:<path>" > /tmp/b && \
    grep -oP '(?<!\\)\\u0061' /tmp/b | wc -l && grep -oP '\\\\u0061' /tmp/b | wc -l
  ```
- **커밋**: `git add -A` **금지**. 파일을 명시 경로로 스테이징하고 `git status --short`로 인덱스를 확인한 뒤 커밋한다. 트레일러:
  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  ```
- **주석 인용 규칙**: 코드 주석에서 다른 코드를 가리킬 때 **줄 번호를 쓰지 않고 심볼 이름을 쓴다**(T11이 기존 위반을 정리한다).
- **설계 결정을 다시 열지 않는다**: 릴리스 범위 · `--fix`가 `args`·`enabled_tools`를 보존 · 단일 판독기 + 감지 확장 · `--fix`는 소유가 확인된 등록물만 고친다 · 기본 프로필 `ingest,net`이고 exec는 opt-in.

---

## File Structure

| 파일 | 이 릴리스에서의 책임 | 태스크 |
|---|---|---|
| `internal/cli/codex_toml.go` | 순수 바이트 변환과 **유일한 판정원**. 구간 판정 사유 열거형, 정규화 불가 키 검사, `--fix` 표식 전용 갈래, 판정 결과 필드 | T1·T2·T4·T5·T7 |
| `internal/cli/cli.go` | `doctor` 출력. `[16]` 이상 사유 문면, `[20]` 라벨·경고, `doctorFixRegistrations` | T3·T4·T5·T6 |
| `internal/cli/hook_install.go` | 설치기 문면과 `ownedRegistration`. 이상 사유 안내, 주석 주장 축소 | T3·T6 |
| `internal/cli/codex_toml_test.go` | 순수 함수 단위 검증 + 표 기반 테스트의 `t.Run` 전환 | T1·T2·T4·T9 |
| `internal/cli/cli_test.go` | `[20]`·`--fix` 문면 결속과 감지·고침 등가 표, 구 형식 배선 | T5·T6·T8·T11 |
| `internal/cli/hook_install_test.go` | 설치기 문면, `runHookInstallCodex` 구 형식 배선 | T3·T8 |
| `internal/mcp/mcp_test.go` | 헬퍼 주석 정정(X4) | T11 |
| `CHANGELOG.md` | `[0.15.0]` 절 누락 보완(전수) + `[0.16.0]` 절 | T10 |
| `docs/context-router-design-v0.15-ko.md` | D80 절에 D88 예외 참조 한 줄 | T7 |

**새 파일은 만들지 않는다.** 판정을 옮기는 함수는 `codex_toml.go`(순수 변환)와 `cli.go`(출력)에 각각 자기 책임만큼 둔다 — 파일을 새로 가르면 D13 안티프래그멘테이션에 어긋난다.

---

## Preflight: 브랜치와 게이트

- [ ] **P0: 브랜치 생성**

```bash
git status --short          # 추적하지 않는 .claude/·.codex/만 있어야 한다
git log --oneline -1        # 계획 커밋인지 확인한다
git rev-parse HEAD          # **이 값이 BASE다** — SDD 원장에 적고 리뷰 diff 기준으로 쓴다
git switch -c feat/v0.16-d85-d88
```

- [ ] **P1: 기준선 red 집합 확인**

```bash
go test -p 1 ./internal/cli/... ./internal/mcp/...
```
Expected: **전부 PASS**(이 두 패키지에는 기준선 red가 없다).

```bash
go test -p 1 ./internal/exec/... 2>&1 | grep -E '^(--- )?FAIL' | sort | uniq -c
```
Expected: 위 Global Constraints의 **9건과 정확히 일치**. 다르면 멈추고 원인을 규명한다.

- [ ] **P2: 게이트 1 — `installCodexConfigBlock` 순수성 재확인 (§1.3 게이트 1)**

스펙 §3 표4가 설계 단계 판독 결과를 담는다. 현재 코드와 맞는지 확인한다:

**통과 기준은 "§1.3 호출 트리에 든 함수들 안에 IO·시간·난수 참조가 없다"다.** 두 파일에는 트리
밖 함수의 IO 참조가 있으므로 히트 자체가 실패를 뜻하지 않는다 — 각 히트가 트리 밖 함수의
것인지 확인한다.

```bash
grep -nE '\bos\.|filepath\.|time\.|rand\.' internal/cli/codex_toml.go
```
Expected: **`codexConfigPath` 안의 네 줄**(`os.Getenv` · `filepath.Join` · `os.UserHomeDir` ·
`filepath.Join` — 647·648·650·654 부근). 그 함수는 호출 트리에 없다. **다른 함수에서 히트가
나오면 멈춘다.**

```bash
grep -nE '\bos\.|filepath\.|time\.|rand\.' internal/cli/mcp_install.go
```
Expected: **`mcpConfigPath`의 `filepath.Join`**과 **트리 밖 함수의 `os.ErrNotExist` 두 줄**
(388·574 부근). 프로필 헬퍼 여섯(`isOurMarkerValue` · `canonicalProfiles` · `profilesFromArgs` ·
`mcpArgsForProfiles` · `enabledToolsForProfiles` · `enabledToolsExposeExec`) **안에는 히트가
없어야 한다** — 그것이 이 게이트가 재는 것이다.

```bash
# 두 패키지 var에 기입이 없어야 한다
grep -n 'mcpProfileNames\s*=\|defaultMCPProfiles\s*=' internal/cli/*.go
```
Expected: 선언 두 줄만(`var mcpProfileNames = ...`·`var defaultMCPProfiles = ...`).

**이 게이트가 물지 않으면**(IO가 있으면) T5를 시작하지 말고 컨트롤러에 보고한다 — 스펙이 그때는 새 판독기로 후퇴한다고 정했다.

- [ ] **P3: 게이트 2 — 문면 결속 테스트 목록화 (§1.3 게이트 2)**

```bash
grep -n 'TestDoctorMCPMarkerLine\|TestDoctorFix\|TestRunDoctorFixFlag' internal/cli/cli_test.go
grep -n 'TestDoctorVersionlessHookMarker' internal/cli/hook_install_test.go
grep -n '\[9\] hooks:\|\[16\] codex:' internal/cli/hook_install_test.go
```
Expected: 앞의 셋이 `cli_test.go`에, `TestDoctorVersionlessHookMarker`가 `hook_install_test.go`에 있고 그 안의 접두 한정이 `[9] hooks:`·`[16] codex:` **두 접두**다(`[20]`을 축자로 제외한다).

이 목록을 T6 착수 시 다시 읽는다. **기존 여섯 케이스의 결과는 바뀌지 않아야 한다**(§3 표2).

---

### Task 1: 구간 판정 사유를 열거형으로 (X3 — 필드·주석)

**Files:**
- Modify: `internal/cli/codex_toml.go` (`codexSpans` 타입, `codexManagedSpans`, 소비자 넷, 열거형 주석)
- Test: `internal/cli/codex_toml_test.go` (`TestCodexManagedSpans`)

**Interfaces:**
- Produces: `type codexAnomaly int`, 값 `anomalyNone`·`anomalyDupHeader`·`anomalyScannerOpen`. `codexSpans.anomaly codexAnomaly`가 기존 `codexSpans.dup bool`을 대체한다. **T2가 값 하나(`anomalyEscapedKey`)를 더한다** — 이 태스크에서 미리 정의하지 않는다(`unused` 린터가 깨진다).
- Consumes: 없음(첫 태스크).

**왜 지금인가**: D87이 세 번째 사유를 더하고 D85가 사유를 인쇄하므로, 필드 타입 변경과 X3의 개명을 **한 번에** 한다. 나누면 같은 필드를 두 번 만진다.

- [ ] **Step 1: 실패하는 테스트로 옮긴다**

`internal/cli/codex_toml_test.go`의 `TestCodexManagedSpans`에서 `dup`를 읽는 두 지점을 새 필드로 바꾼다.

기존 (471–473행):
```go
	if sp.dup {
		t.Errorf("dup=true인데 중복 헤더가 없다")
	}
```
새로:
```go
	if sp.anomaly != anomalyNone {
		t.Errorf("anomaly=%d인데 이상이 없다", sp.anomaly)
	}
```

기존 (480–483행):
```go
	sp3 := codexManagedSpans(splitLinesKeepEnds([]byte("[mcp_servers.ctr]\n[x]\n[mcp_servers.ctr]\n")))
	if !sp3.dup {
		t.Errorf("같은 이름 헤더 둘인데 dup=false")
	}
```
새로:
```go
	sp3 := codexManagedSpans(splitLinesKeepEnds([]byte("[mcp_servers.ctr]\n[x]\n[mcp_servers.ctr]\n")))
	if sp3.anomaly != anomalyDupHeader {
		t.Errorf("같은 이름 헤더 둘인데 anomaly=%d want %d", sp3.anomaly, anomalyDupHeader)
	}
```

그리고 EOF 스캐너 열림 사유를 **새로** 단정하는 케이스를 같은 테스트에 더한다(기존에 `sp.dup`으로만 접혀 있어 사유 구분이 없었다). 위 `sp4` 케이스 뒤에 넣는다:
```go
	// EOF에서 스캐너가 열린 파일 — 사유가 중복 헤더와 구별돼야 한다
	sp5 := codexManagedSpans(splitLinesKeepEnds([]byte("[mcp_servers.ctr]\nk = \"\"\"\nunclosed\n")))
	if sp5.anomaly != anomalyScannerOpen {
		t.Errorf("EOF 스캐너 열림 anomaly=%d want %d", sp5.anomaly, anomalyScannerOpen)
	}
```

**주의**: `codex_toml_test.go:246`의 `installFixture(dup)`은 **동명의 지역 픽스처 변수**다. 개명 대상이 아니다 — 건드리지 않는다.

- [ ] **Step 2: 컴파일 실패를 확인한다**

Run:
```bash
go test -p 1 -run TestCodexManagedSpans ./internal/cli/
```
Expected: **컴파일 에러** — `sp.anomaly undefined (type codexSpans has no field or method anomaly)`, `undefined: anomalyNone`, `undefined: anomalyDupHeader`, `undefined: anomalyScannerOpen`.

- [ ] **Step 3: 열거형과 필드를 정의한다**

`internal/cli/codex_toml.go`에서 `markerClass` 열거형 블록(현재 33–39행) **바로 뒤에** 추가한다:

```go
// codexAnomaly — 구간 판정을 신뢰할 수 없는 사유(D85·D87). 불리언이 아니라 열거형인 이유는
// 사용자에게 **사유를 알려야** 하기 때문이다: [16]의 기존 안내는 중복 헤더 정리를 지시하므로,
// 사유가 다른 파일에서 그 안내만 보이면 install이 영구히 무변경인 이유를 알 수 없다.
// 어느 사유든 결과는 같다 — 무변경 경로로 빠지고 --fix를 권하지 않는다.
type codexAnomaly int

const (
	anomalyNone        codexAnomaly = iota // 이상 없음
	anomalyDupHeader                       // 같은 이름의 관리 테이블 헤더가 둘 이상(그 자체가 TOML 중복 정의)
	anomalyScannerOpen                     // EOF에서 스캐너가 열림(닫히지 않은 문자열·배열)
)
```

`codexSpans` 타입(현재 205–209행)의 필드와 주석을 바꾼다. 기존:
```go
// dup은 "구간 판정을 신뢰할 수 없다"는 뜻이다 — 같은 이름의 헤더가 둘 이상(그 자체가 TOML
// 중복 정의)이거나, EOF에서 스캐너가 열려 있어(닫히지 않은 문자열·배열) 그 뒤 헤더가 경계로
// 잡히지 않은 경우다. 둘 다 이미 무효 TOML이며 무변경으로 뺀다.
// (이름이 두 사유를 함께 담는다 — 이 파일의 열거형 이름·주석 정리는 v0.16 X3에 묶었다.)
type codexSpans struct {
	table codexSpan
	env   codexSpan
	dup   bool
}
```
새로:
```go
// anomaly는 "구간 판정을 신뢰할 수 없다"는 뜻이며 **그 사유를 담는다**(D85가 사유를 인쇄한다).
// 어느 사유든 이미 무효 TOML이거나 우리가 다루지 않는 형태이므로 무변경으로 뺀다.
type codexSpans struct {
	table   codexSpan
	env     codexSpan
	anomaly codexAnomaly
}
```

`codexManagedSpans`의 기입 세 곳(현재 231·236·246행)을 바꾼다:
```go
		case codexManagedTable:
			if out.table.found {
				out.anomaly = anomalyDupHeader
			}
```
```go
		case codexManagedEnv:
			if out.env.found {
				out.anomaly = anomalyDupHeader
			}
```
EOF 검사(현재 245–247행)는 **이미 세워진 사유를 덮지 않는다**:
```go
	// 유효 TOML이면 EOF에서 스캐너가 닫힌다 — 열려 있으면 닫히지 않은 문자열·배열이 있어 그 뒤
	// 헤더가 경계로 잡히지 않았고 우리 구간이 EOF까지 늘어난 상태다. 그대로 기입하면 append한
	// 헤더까지 그 구간에 삼켜져 재실행마다 테이블이 하나씩 늘고, 매 실행 Changed=true라 D84
	// 단일 백업 슬롯이 2회차에 원본을 잃는다(적대적 리뷰 A2). 무변경 경로로 뺀다.
	// 중복 헤더가 이미 잡혔으면 그 사유를 유지한다 — 구조적으로 더 근본이고, 사용자가 먼저
	// 고쳐야 하는 것이 그쪽이다.
	if sc.open() && out.anomaly == anomalyNone {
		out.anomaly = anomalyScannerOpen
	}
```

- [ ] **Step 4: 소비자 넷을 바꾼다**

네 지점 모두 "이상이면 무변경"이라는 같은 의미다.

`installCodexConfigBlock`(현재 476행):
```go
	if sp.anomaly != anomalyNone {
		return codexInstallResult{Out: existing, State: mcpMarkerAnomaly}
	}
```
`probeCodexMCPBlock`(현재 580행):
```go
	if sp.anomaly != anomalyNone {
		return false, true
	}
```
`uninstallCodexConfigBlock`(현재 613행):
```go
	if sp.anomaly != anomalyNone || !sp.table.found {
		return existing, false
	}
```
`codexConfigMarker`(현재 663행):
```go
	if sp.anomaly != anomalyNone || !sp.table.found {
		return "", "", false
	}
```

- [ ] **Step 5: X3의 나머지 — 열거형 주석 정정**

`codexMCPState`(현재 26–31행)와 `markerClass`(35–39행)의 주석이 후속 태스크가 의미를 바꾼 뒤로 갱신되지 않았다. `classAppend`·`classAnomaly`는 **비교 소비자가 없는 반환 전용 값**이라는 사실을 적는다:

```go
const (
	mcpWritten        codexMCPState = iota // 블록 기입/갱신 성공 — MCP 확정
	mcpExistingHeader                      // 표식도 없고 command도 우리 것이 아닌 [mcp_servers.ctr] 실존 — 기입 생략·MCP 확정
	mcpConflict                            // 우리 두 구간 **밖**의 중복 정의 신호 — 기입 생략·MCP 미확정
	mcpMarkerAnomaly                       // 구간 판정 불가(codexAnomaly의 어느 사유든) — 무변경·MCP 미확정
)
```
```go
const (
	classAppend  markerClass = iota // 마커 0쌍 — append 후보. **반환 전용**(비교 소비자 없음)
	classReplace                    // 소유 블록 1쌍 — 교체 후보. inOldBlock 판정이 이 값을 비교한다
	classAnomaly                    // 그 외 마커 배치 — 무변경. **반환 전용**(비교 소비자 없음)
)
```

- [ ] **Step 6: 테스트가 통과하는지 확인한다**

Run:
```bash
go test -p 1 -run 'TestCodexManagedSpans|TestInstallCodexConfigBlock|TestUninstallCodexConfigBlock|TestProbeCodexMCPBlock|TestCodexTableOwnershipAndAdoption' ./internal/cli/
```
Expected: PASS.

- [ ] **Step 7: 유도 FAIL로 새 단정이 물리는지 확인한다**

`codexManagedSpans`의 EOF 검사를 일시적으로 되돌린다(`out.anomaly = anomalyDupHeader`로 바꾼다):

Run:
```bash
go test -p 1 -run TestCodexManagedSpans ./internal/cli/
```
Expected: **FAIL** — `EOF 스캐너 열림 anomaly=1 want 2`. 확인 후 고침을 복구한다.

- [ ] **Step 8: 전역 제약 확인**

Run:
```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
```
Expected: 빌드 통과 · `gofumpt` 무출력 · lint **0 issues** · 테스트 PASS.

- [ ] **Step 9: 커밋**

```bash
git add internal/cli/codex_toml.go internal/cli/codex_toml_test.go
git status --short
git commit -m "$(cat <<'EOF'
refactor(v0.16): X3 구간 판정 사유를 열거형으로 — dup을 anomaly로

D85가 사유를 인쇄하고 D87이 세 번째 사유를 더하므로 불리언을 사유 열거형으로
올린다. EOF 스캐너 열림은 이미 잡힌 중복 헤더 사유를 덮지 않는다 — 구조적으로
더 근본이고 사용자가 먼저 고쳐야 하는 것이 그쪽이다.

codexMCPState·markerClass의 표류한 주석도 함께 정정했다(classAppend·
classAnomaly는 비교 소비자가 없는 반환 전용 값이다).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: D87 — 정규화 불가 키를 무변경으로 (C2)

**Files:**
- Modify: `internal/cli/codex_toml.go` (`codexAnomaly`에 값 하나 추가, `codexManagedSpans` 반환 직전 스캔, 새 헬퍼 둘)
- Test: `internal/cli/codex_toml_test.go` (새 테스트 함수)

**Interfaces:**
- Consumes: T1의 `codexAnomaly`·`anomalyNone`·`codexSpans.anomaly`.
- Produces: `anomalyEscapedKey codexAnomaly`(열거형 넷째 값). 새 헬퍼 `codexEscapedKeyInSpans(lines [][]byte, sp codexSpans) bool`와 `tomlKeyTokenHasEscape(s string) bool`.

**왜 이 판정 지점인가**: `codexManagedSpans`를 install·uninstall·`probeCodexMCPBlock`·`codexConfigMarker` **넷이 모두** 첫 단계로 부르고 그 이상 신호를 이미 검사한다(T1이 넷을 통일했다). 한 곳이 네 경로를 닫는다.

**무엇이 문제인가**: TOML은 `command`와 유니코드 이스케이프 표기로 적은 같은 이름을 **같은 키**로 읽는다. `codexKeyName`은 이스케이프를 해석하지 않으므로 그 표기를 우리 키로 알아보지 못하고 보존 라인으로 돌리며, 정규 키를 새로 기입한다 → **같은 논리 키가 두 번 정의된다**(유효 TOML 입력 → 무효 `config.toml` 기록).

**반드시 지킬 두 경계**:
1. **키 토큰만 본다.** 라인 전체에서 역슬래시를 찾으면 **값**의 역슬래시가 걸린다 — `command = "C:\bin\ctr.exe"` 같은 Windows 경로는 흔하고, 그 사용자의 install·uninstall·`--fix`가 영구히 무변경으로 굳는다.
2. **인라인 `env` 대입 안의 키 토큰도 본다.** 그 위치의 이스케이프된 표식 키는 키 토큰이 `env`라 서브테이블 검사에 걸리지 않는데, `tomlKeyLen`이 부재로 읽어 `setInlineEnvMarker`가 같은 인라인 테이블에 표식 키를 **한 번 더** 더한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`internal/cli/codex_toml_test.go` 끝에 추가한다. **역슬래시는 Go 문자열 리터럴에서 두 개로 쓰지만 파일 바이트는 하나여야 한다** — `"\\u0061"`가 TOML 파일에서 역슬래시 **하나** + `u0061`이다(Go가 `\\`를 역슬래시 하나로 컴파일한다). 역슬래시를 둘로 만들려면 Go에 `\\\\`를 써야 하고, 그것은 TOML에서 **별개 키**라 이 검사의 대상이 아니다.

```go
// TestCodexEscapedManagedKey — D87(§2-6). 관리 키의 유니코드 이스케이프 표기는 TOML이 우리
// 키와 **같은 키**로 읽으므로, 알아보지 못한 채 정규 키를 새로 기입하면 같은 논리 키가 두 번
// 정의된다. 무변경으로 닫는다. 대상은 **키 토큰**뿐이다 — 값의 역슬래시(Windows 경로)가
// 걸리면 그 사용자의 install이 영구 동결된다.
func TestCodexEscapedManagedKey(t *testing.T) {
	cases := []struct {
		name string
		src  string
		want codexAnomaly
	}{
		{
			name: "서브테이블 키의 이스케이프 표기 — 이상",
			src:  "[mcp_servers.ctr]\n\"comm\\u0061nd\" = \"other\"\n",
			want: anomalyEscapedKey,
		},
		{
			name: "env 서브테이블 표식 키의 이스케이프 표기 — 이상",
			src:  "[mcp_servers.ctr]\ncommand = \"context-router\"\n[mcp_servers.ctr.env]\n\"CTR_MAN\\u0041GED\" = \"context-router\"\n",
			want: anomalyEscapedKey,
		},
		{
			name: "인라인 env 안의 표식 키 이스케이프 표기 — 이상",
			src:  "[mcp_servers.ctr]\ncommand = \"context-router\"\nenv = { \"CTR_MAN\\u0041GED\" = \"context-router\" }\n",
			want: anomalyEscapedKey,
		},
		{
			name: "값의 역슬래시(Windows 경로) — 이상 아님",
			src:  "[mcp_servers.ctr]\ncommand = \"C:\\\\bin\\\\context-router.exe\"\n",
			want: anomalyNone,
		},
		{
			name: "인라인 env 값의 역슬래시 — 이상 아님",
			src:  "[mcp_servers.ctr]\ncommand = \"context-router\"\nenv = { CTR_STORE_ROOT = \"C:\\\\ctr\\\\store\" }\n",
			want: anomalyNone,
		},
		{
			name: "홑따옴표 키 — 이스케이프가 없으므로 이상 아님",
			src:  "[mcp_servers.ctr]\n'command' = \"context-router\"\n",
			want: anomalyNone,
		},
		{
			name: "우리 구간 밖의 이스케이프 키 — 이상 아님",
			src:  "[other]\n\"comm\\u0061nd\" = \"x\"\n[mcp_servers.ctr]\ncommand = \"context-router\"\n",
			want: anomalyNone,
		},
		// 아래 넷은 오탐 감시선이다. 판정이 라인을 문맥 없이 훑거나 닫는 따옴표 뒤 '='를
		// 확인하지 않으면 이 파일들이 이상으로 판정되어 install·uninstall·--fix가 영구
		// 무변경으로 굳는다. 셋은 `,` 직후에 키 모양을 두었다 — 그 위치가 초안의 인라인
		// 루프가 보는 자리이므로 그렇게 두어야 감시선이 실제로 물린다.
		{
			name: "배열 원소 값의 역슬래시 — 이상 아님",
			src:  "[mcp_servers.ctr]\ncommand = \"context-router\"\nargs = [\"--store-root\", \"C:\\\\ctr\\\\store\"]\n",
			want: anomalyNone,
		},
		{
			name: "여러 줄 문자열 내용이 키 모양 — 이상 아님",
			src:  "[mcp_servers.ctr]\ncommand = \"context-router\"\nnote = \"\"\"\n\"comm\\u0061nd\" = \"x\"\n\"\"\"\n",
			want: anomalyNone,
		},
		{
			name: "후행 주석 안이 키 모양 — 이상 아님",
			src:  "[mcp_servers.ctr]\ncommand = \"context-router\"\nx = 1 # , \"comm\\u0061nd\" = 2\n",
			want: anomalyNone,
		},
		{
			name: "홑따옴표 값 내부가 키 모양 — 이상 아님",
			src:  "[mcp_servers.ctr]\ncommand = \"context-router\"\nk = ', \"comm\\u0061nd\" = 1'\n",
			want: anomalyNone,
		},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			sp := codexManagedSpans(splitLinesKeepEnds([]byte(c.src)))
			if sp.anomaly != c.want {
				t.Errorf("anomaly=%d want %d", sp.anomaly, c.want)
			}
			// 이상이면 네 경로가 모두 무변경이어야 한다.
			if c.want == anomalyNone {
				return
			}
			res := installCodexConfigBlock([]byte(c.src), codexInstallRequest{Marker: hookMarker("0.16.0")})
			if res.State != mcpMarkerAnomaly || res.Changed || !bytes.Equal(res.Out, []byte(c.src)) {
				t.Errorf("install이 무변경이 아니다: state=%d changed=%v", res.State, res.Changed)
			}
			if out, changed := uninstallCodexConfigBlock([]byte(c.src)); changed || !bytes.Equal(out, []byte(c.src)) {
				t.Errorf("uninstall이 무변경이 아니다: changed=%v", changed)
			}
			if present, anomaly := probeCodexMCPBlock([]byte(c.src)); present || !anomaly {
				t.Errorf("probe present=%v anomaly=%v want false/true", present, anomaly)
			}
			if _, _, found := codexConfigMarker([]byte(c.src)); found {
				t.Errorf("codexConfigMarker found=true — 이상 파일에서 판독이 성립했다")
			}
		})
	}
}
```

- [ ] **Step 2: 컴파일·테스트 실패를 확인한다**

Run:
```bash
go test -p 1 -run TestCodexEscapedManagedKey ./internal/cli/
```
Expected: **컴파일 에러** — `undefined: anomalyEscapedKey`.

- [ ] **Step 3: 열거형 값을 더한다**

T1이 만든 `codexAnomaly` 블록에 넷째 값을 추가한다. **이 태스크에서 추가하는 이유**: T1에서 미리 정의하면 그때는 세우는 코드가 없어 `unused` 린터가 깨진다.

```go
const (
	anomalyNone        codexAnomaly = iota // 이상 없음
	anomalyDupHeader                       // 같은 이름의 관리 테이블 헤더가 둘 이상(그 자체가 TOML 중복 정의)
	anomalyScannerOpen                     // EOF에서 스캐너가 열림(닫히지 않은 문자열·배열)
	anomalyEscapedKey                      // 우리 구간 안에 정규화 불가 키 표기(D87) — 이스케이프를 해석하지 않으므로 무변경
)
```

- [ ] **Step 4: 키 토큰 검사 헬퍼를 쓴다**

`codexKeyName` 함수 **바로 뒤에** 추가한다(같은 키 판독 계열에 둔다):

```go
// tomlKeyTokenHasEscape — 정규화 문자열 s의 **맨 앞이 따옴표 키 표기**이고 그 안에 역슬래시가
// 있는가(D87). TOML 기본 문자열 키는 이스케이프를 디코드하므로 그런 표기는 우리 키와 **같은
// 키**가 될 수 있는데 codexKeyName은 이스케이프를 해석하지 않아 알아보지 못한다.
// 홑따옴표(리터럴) 키에는 이스케이프가 없으므로 대상이 아니다.
func tomlKeyTokenHasEscape(s string) bool {
	if s == "" || s[0] != '"' {
		return false
	}
	n := basicStringLen(s)
	if n < 2 || s[n-1] != '"' {
		return false // 미종료 문자열 — 우리가 다루지 않는 형태(다른 사유가 잡는다)
	}
	// 닫는 따옴표 뒤에 '='가 와야 **키**다. 이 검사가 없으면 값이 키로 오인된다 — 배열 원소의
	// Windows 경로(args = ["--store-root", "C:\ctr"])가 대표 형태이고, 그러면 그 사용자의
	// install·uninstall·--fix가 영구 무변경으로 굳는다. 정규화(stripLine)가 공백을 지우므로
	// 키와 '=' 사이에 공백이 없다 — tomlKeyLen·tomlInlineValue가 쓰는 것과 같은 전제다.
	if n >= len(s) || s[n] != '=' {
		return false
	}
	return strings.Contains(s[1:n-1], `\`)
}

// codexEscapedKeyInSpans — 우리 두 구간 안에 정규화 불가 키 표기가 있는가(D87).
// **엔트리 단위로 본다.** codexEntries가 여러 줄 값을 한 엔트리로 묶고 그 엔트리에서 키는
// 맨 앞 토큰뿐이므로, 문자열 값 안이나 후행 주석 안의 `"이름" = 값` 모양이 키로 오인되지
// 않는다 — codexReadTable이 키를 읽는 경로와 **같은 기준**이며, 두 경로가 다른 기준을 쓰면
// 같은 파일을 두 방식으로 읽는 셈이다(라인을 문맥 없이 훑으면 여러 줄 문자열의 내용 줄·값 뒤
// 후행 주석·홑따옴표 값 내부가 모두 오탐이 된다).
// 인라인 테이블의 키 토큰은 **env 엔트리에서만** 본다 — tomlKeyLen·inlineMarkerSpan이 그 줄에만
// 적용되는 것과 같은 한정이고, 그것이 없으면 다른 키의 값 안에 든 쉼표 뒤 텍스트가 키로 잡힌다.
// **알려진 한계**: env 인라인 값 안에 따옴표·역슬래시·등호를 함께 담은 문자열은 여전히 키로
// 보인다. tomlInlineValue·inlineMarkerSpan이 이미 같은 한계를 갖는 형태이며(인라인 테이블의
// 구조를 따라가지 않는다) D80의 파서 비의존 원칙 아래에서는 그 한계가 계약이다.
func codexEscapedKeyInSpans(lines [][]byte, sp codexSpans) bool {
	for _, span := range []codexSpan{sp.table, sp.env} {
		if !span.found {
			continue
		}
		for _, e := range codexEntries(lines, span) {
			joined := ""
			for i := e[0]; i <= e[1]; i++ {
				joined += stripLine(lines[i])
			}
			if tomlKeyTokenHasEscape(joined) {
				return true
			}
			if codexKeyName(joined) != "env" {
				continue
			}
			for j := 0; j < len(joined); j++ {
				if (joined[j] == '{' || joined[j] == ',') && tomlKeyTokenHasEscape(joined[j+1:]) {
					return true
				}
			}
		}
	}
	return false
}
```

**`codexEntries`를 쓰는 것이 이 고침의 핵심이다.** 라인을 문맥 없이 훑는 초안은 네 형태에서
오탐이었다 — 배열 원소 값(F1) · 여러 줄 문자열의 내용 줄 · 값 뒤 후행 주석 · 홑따옴표 값
내부(F12). 엔트리 단위 + 첫 토큰 + `env` 한정이 그 넷을 함께 닫는다.

- [ ] **Step 5: `codexManagedSpans` 반환 직전에 검사를 넣는다**

T1이 고친 EOF 검사 **바로 뒤**, `return out` **앞**에 넣는다:

```go
	if sc.open() && out.anomaly == anomalyNone {
		out.anomaly = anomalyScannerOpen
	}
	// 우리 구간 안의 정규화 불가 키(D87) — 구간이 확정된 뒤에 본다. 앞의 두 사유가 이미
	// 잡혔으면 유지한다(그쪽이 구조적으로 더 근본이다).
	if out.anomaly == anomalyNone && codexEscapedKeyInSpans(lines, out) {
		out.anomaly = anomalyEscapedKey
	}
	return out
```

- [ ] **Step 6: 테스트가 통과하는지 확인한다**

Run:
```bash
go test -p 1 -run TestCodexEscapedManagedKey ./internal/cli/ -v
```
Expected: 일곱 서브테스트 모두 PASS.

- [ ] **Step 7: 픽스처가 의도한 바이트인지 확인한다 (전역 제약)**

Go 리터럴이 아니라 **컴파일된 문자열**이 검증 대상 상태를 만드는지 본다:

```bash
cat > /tmp/bs_check.go <<'EOF'
package main

import "fmt"

func main() {
	fmt.Printf("%q\n", "\"comm\\u0061nd\"")
	fmt.Printf("%q\n", "\"C:\\\\bin\\\\ctr.exe\"")
}
EOF
go run /tmp/bs_check.go
```
Expected 첫 줄: `"\"comm\\u0061nd\""` — 즉 실제 문자열은 역슬래시 **하나** + `u0061`.
Expected 둘째 줄: `"\"C:\\\\bin\\\\ctr.exe\""` — 실제 문자열은 역슬래시 **둘**(Windows 경로의 TOML 표기).

- [ ] **Step 8: 유도 FAIL — 다섯 방향**

각 편집 뒤 `go test -p 1 -run TestCodexEscapedManagedKey ./internal/cli/ -v`를 돌리고 **확인 후
복구한다**. 다섯 방향이 각각 다른 오탐·미탐 축을 겨눈다 — 하나만 재면 나머지 축이 열린 채
통과한다(이 계획의 초판이 그 상태였다).

① **키 토큰 한정을 되돌린다** — `tomlKeyTokenHasEscape`의 첫 검사를 지우고 라인 전체를 보게
만든다(`return strings.Contains(s, "\\")`).
Expected: **"값의 역슬래시(Windows 경로)"·"인라인 env 값의 역슬래시"·"배열 원소 값의 역슬래시"가
FAIL**.

② **닫는 따옴표 뒤 `=` 확인을 지운다** — `tomlKeyTokenHasEscape`에서 그 두 줄을 없앤다.
Expected: **"배열 원소 값의 역슬래시 — 이상 아님"이 FAIL.** 값이 키로 오인되는 축의 감시선이다.

③ **엔트리 단위를 라인 훑기로 되돌린다** — `codexEscapedKeyInSpans`를 `for i, ln := range lines`로
바꿔 `inCodexSpan`으로 필터하고 각 라인을 개별로 검사하게 만든다.
Expected: **"여러 줄 문자열 내용이 키 모양"이 FAIL**(그 내용 줄이 개별 라인으로는 키처럼 보인다).

④ **`env` 한정을 지운다** — `if codexKeyName(joined) != "env" { continue }`를 없앤다.
Expected: **"후행 주석 안이 키 모양"과 "홑따옴표 값 내부가 키 모양"이 FAIL**(둘 다 `,` 직후에 키
모양이 있다).

⑤ **인라인 검사를 지운다** — Step 4의 인라인 루프를 없앤다.
Expected: **"인라인 env 안의 표식 키 이스케이프 표기 — 이상"이 FAIL**(미탐 축의 감시선이다).

- [ ] **Step 9: 전역 제약 확인**

Run:
```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
```
Expected: 전부 통과, lint 0 issues.

- [ ] **Step 10: 커밋과 blob 검증**

```bash
git add internal/cli/codex_toml.go internal/cli/codex_toml_test.go
git status --short
git commit -m "$(cat <<'EOF'
fix(v0.16): D87 정규화 불가 키를 무변경으로 닫는다 (C2)

TOML은 관리 키의 유니코드 이스케이프 표기를 우리 키와 같은 키로 읽는데
codexKeyName이 그것을 알아보지 못해 보존 라인으로 돌리고 정규 키를 새로
기입했다 — 유효 TOML 입력에서 같은 논리 키가 두 번 정의된다.

판정은 codexManagedSpans 한 곳에 두어 install·uninstall·probe·
codexConfigMarker 네 경로가 함께 닫힌다. 대상은 **키 토큰**뿐이다 — 라인
전체를 보면 값의 Windows 경로가 걸려 그 사용자의 install이 영구 동결된다.
인라인 env 대입 안의 키 토큰도 본다(그 형태는 tomlKeyLen이 부재로 읽어
표식을 한 번 더 넣는다).

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"

# 픽스처 바이트를 커밋 blob 기준으로 확인한다. Bash의 PCRE는 \u를 거부하므로 ERE로 센다.
git cat-file blob "HEAD:internal/cli/codex_toml_test.go" > /tmp/t2blob
echo "u0061 double(정상)=$(grep -oE '\\\\u0061' /tmp/t2blob | wc -l)"
echo "u0061 quad(오염)=$(grep -oE '\\\\\\\\u0061' /tmp/t2blob | wc -l)"
echo "u0041 double(정상)=$(grep -oE '\\\\u0041' /tmp/t2blob | wc -l)"
```
Expected: `u0061 double=6`(케이스 1 · 우리 구간 밖 · 여러 줄 문자열 · 후행 주석 · 홑따옴표 —
Go 리터럴에서 역슬래시 둘이고 컴파일되면 하나), `quad=0`, `u0041 double=1`(env 서브테이블
케이스). **실측이 다르면 픽스처를 센 뒤 그 수를 적는다 — 수를 맞추려고 픽스처를 고치지 않는다.**

**T3·T6도 같은 표기를 다른 파일에 심는다**(`hook_install_test.go`·`cli_test.go`). 그 태스크의
커밋 단계에서 같은 방향별 계수를 그 파일에 대해 돌린다.

---

### Task 3: 이상 사유를 문면에 싣는다 (D85·D87)

**Files:**
- Modify: `internal/cli/codex_toml.go` (`probeCodexMCPBlock` 시그니처)
- Modify: `internal/cli/cli.go` (`[16]` 분기⑤ 문면)
- Modify: `internal/cli/hook_install.go` (`reportCodexMCPState`의 `mcpMarkerAnomaly` 갈래)
- Test: `internal/cli/codex_toml_test.go`(`TestProbeCodexMCPBlock`) · `internal/cli/cli_test.go`(`TestDoctorCodexMCPLine`) · `internal/cli/hook_install_test.go`

**Interfaces:**
- Consumes: T1·T2의 `codexAnomaly` 네 값.
- Produces: `anomalyOutsideConflict codexAnomaly`(다섯째 값 — **`probeCodexMCPBlock`만 만든다**). `probeCodexMCPBlock(existing []byte) (present bool, anomaly codexAnomaly)` — **불리언에서 사유로 바뀐다**. `func (a codexAnomaly) reason() string`이 사용자 문면용 한국어 사유를 준다. `reportCodexMCPState(stdout io.Writer, state codexMCPState, anomaly codexAnomaly)` — 인자가 하나 늘어난다.
- **반환 타입 변경의 소비자는 둘이다**: `codex_toml_test.go`의 `TestProbeCodexMCPBlock`과 **T2가 심은 `TestCodexEscapedManagedKey`**(그 테스트가 같은 반환값에 논리 부정 `!anomaly`를 쓴다). 둘을 함께 고치지 않으면 이 태스크 종료 시점에 `internal/cli`가 컴파일되지 않는다 — 전역 제약 위반이다.

**왜 필요한가**: `[16]`의 기존 안내는 "중복 헤더 정리 후 재실행하세요"를 지시한다. 사유가 EOF 스캐너 열림이나 정규화 불가 키인 사용자는 그 안내를 따라도 아무 일이 일어나지 않고, install이 영구히 무변경인 이유를 알 수 없다.

- [ ] **Step 1: 사유 문면 테스트를 쓴다**

`internal/cli/codex_toml_test.go`에 추가한다:

```go
// TestCodexAnomalyReason — 세 사유가 서로 다른 문면을 준다(§2-7). 같은 문면이면 사용자는
// install이 영구 무변경인 이유를 구별할 수 없다.
func TestCodexAnomalyReason(t *testing.T) {
	seen := map[string]codexAnomaly{}
	for _, a := range []codexAnomaly{anomalyDupHeader, anomalyScannerOpen, anomalyEscapedKey} {
		r := a.reason()
		if r == "" {
			t.Errorf("anomaly=%d의 사유 문면이 비었다", a)
		}
		if prev, dup := seen[r]; dup {
			t.Errorf("anomaly=%d와 %d의 사유 문면이 같다: %q", prev, a, r)
		}
		seen[r] = a
	}
	if anomalyNone.reason() != "" {
		t.Errorf("anomalyNone의 사유 문면이 비어 있지 않다: %q", anomalyNone.reason())
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run:
```bash
go test -p 1 -run TestCodexAnomalyReason ./internal/cli/
```
Expected: **컴파일 에러** — `a.reason undefined`.

- [ ] **Step 3: `reason()`을 구현한다**

`codexAnomaly` 열거형 블록 바로 뒤에 둔다:

```go
// reason — 사용자 문면에 실을 사유(D85). 사유마다 **다른 조치**가 필요하므로 문면이 달라야
// 한다: 중복 헤더는 사용자가 하나를 지우면 되고, 스캐너 열림은 닫히지 않은 문자열·배열을
// 닫아야 하며, 정규화 불가 키는 이스케이프 표기를 보통 표기로 바꿔야 한다.
// anomalyNone은 빈 문자열이다 — 호출자가 그 갈래에서 사유를 인쇄하지 않는다.
func (a codexAnomaly) reason() string {
	switch a {
	case anomalyDupHeader:
		return "관리 테이블 헤더가 둘 이상입니다(TOML 중복 정의) — 하나만 남기고 지우세요"
	case anomalyScannerOpen:
		return "파일 끝에서 여러 줄 문자열 또는 배열이 닫히지 않았습니다 — 닫은 뒤 재실행하세요"
	case anomalyEscapedKey:
		return "관리 테이블 안의 키가 이스케이프 표기로 적혀 있습니다 — 보통 표기(예: command)로 바꾸세요"
	case anomalyOutsideConflict:
		return "관리 테이블 밖에 ctr 관련 정의가 있습니다 — doctor 스니펫으로 수동 정리한 뒤 재실행하세요"
	}
	return ""
}
```

**다섯째 값을 이 태스크에서 더한다.** `codexSpans.anomaly`에는 들어가지 않고
`probeCodexMCPBlock`만 만드는 값이므로 열거형 블록에 그 사실을 적는다:
```go
	anomalyOutsideConflict // 우리 두 구간 **밖**의 중복 정의 신호 — 구간 판정은 성공했다.
	                       // codexSpans.anomaly에는 들어가지 않고 probeCodexMCPBlock만 만든다.
```

**이 값이 필요한 이유**: 구간 밖 충돌을 `anomalyDupHeader`로 접으면 `[16]`의 사유 줄이
"헤더가 둘 이상이니 하나만 남기고 지우세요"가 되는데, 그 상태의 파일은 **헤더가 하나뿐이다** —
존재하지 않는 조치를 지시하고, 두 원인을 함께 말하던 기존 겸용 문면보다 나쁜 회귀가 된다.

- [ ] **Step 4: `probeCodexMCPBlock`이 사유를 돌려주게 한다**

```go
// probeCodexMCPBlock — doctor [16] 존재 판별(D52 승계). install 상태기계는 "무엇을 쓸지"의
// 분류라 존재/부재 판별에 부적합하므로 같은 순수 헬퍼를 재사용해 읽기 전용으로 판정한다.
// 우선순위는 installCodexConfigBlock과 1:1이다 — 구간 판정 불가 > 구간 밖 충돌 > 테이블 존재.
// 소유 여부는 보지 않는다: 우리 이름 자리에 남의 항목이 있어도 "그 이름의 등록은 있다"가
// 맞고, install은 그 상태를 mcpExistingHeader로 따로 보고한다.
// anomaly는 불리언이 아니라 **사유**다(D85) — [16]이 사용자에게 조치를 지목해야 한다. 구간 밖
// 충돌은 사유가 아니라 별개 상태이므로 anomalyDupHeader로 접지 않고 그대로 둔다.
func probeCodexMCPBlock(existing []byte) (present bool, anomaly codexAnomaly) {
	lines := splitLinesKeepEnds(existing)
	sp := codexManagedSpans(lines)
	if sp.anomaly != anomalyNone {
		return false, sp.anomaly
	}
	if scanOutsideSpans(lines, sp) {
		return false, anomalyOutsideConflict
	}
	return sp.table.found, anomalyNone
}
```

**구간 밖 충돌은 구간 판정 사유가 아니라 별개 상태다.** 그래서 `codexSpans.anomaly`가 아니라
이 함수만 만드는 값(`anomalyOutsideConflict`)으로 돌려주고, `reason()`이 그 상태에 맞는 조치를
가리킨다. 이 구별이 없으면 `[16]`이 헤더 하나뿐인 파일에 "헤더를 하나만 남기라"고 말한다.
**어느 서브테이블이 원인인지는 여전히 알리지 않는다** — 그 지목은 X5(비범위, 스펙 §1.2)에
속하고 `scanOutsideSpans`가 걸린 줄을 돌려주도록 고쳐야 한다.

- [ ] **Step 5: 반환 타입 변경의 소비자 둘을 함께 고친다**

**① `TestProbeCodexMCPBlock`** — 표의 `anomaly bool` 필드를 `anomaly codexAnomaly`로 바꾸고
기대값을 `anomalyNone`·`anomalyDupHeader`·`anomalyOutsideConflict`로 적는다(기존 케이스가 어느
상태였는지 픽스처로 판정한다). 비교(현재 374–376행):
```go
		if p != c.present || a != c.anomaly {
			t.Errorf("%s: present=%v anomaly=%d want %v/%d", c.name, p, a, c.present, c.anomaly)
		}
```

**② T2가 심은 `TestCodexEscapedManagedKey`** — 그 테스트의 probe 단정이 논리 부정을 쓰므로
컴파일되지 않는다. 비교로 바꾼다:
```go
			if present, anomaly := probeCodexMCPBlock([]byte(c.src)); present || anomaly != anomalyEscapedKey {
				t.Errorf("probe present=%v anomaly=%v want false/escapedKey", present, anomaly)
			}
```
**이 항을 빼면 이 태스크 종료 시점에 패키지가 컴파일되지 않는다.**

- [ ] **Step 6: `[16]` 문면에 사유를 싣는다**

`internal/cli/cli.go`의 분기⑤(현재 1915–1919행):
```go
			present, anomaly := probeCodexMCPBlock(cfgData)
			switch {
			case anomaly != anomalyNone: // 분기⑤
				fmt.Fprintf(w, "[16] codex: [mcp_servers.ctr] 테이블=이상 (hooks: project=%s user=%s)\n", projScope, userScope)
				fmt.Fprintf(w, "[16] warning: %s — 수동 확인 필요(hook install --codex 안내 참조)\n", anomaly.reason())
```

- [ ] **Step 7: 설치기 문면에 사유를 싣는다**

`internal/cli/hook_install.go`의 `reportCodexMCPState`에 인자를 더한다:
```go
func reportCodexMCPState(stdout io.Writer, state codexMCPState, anomaly codexAnomaly) {
	switch state {
	case mcpWritten:
		fmt.Fprintln(stdout, "hook install (codex): MCP 관리 테이블 기입 완료 — Codex 재시작 시 반영")
	case mcpExistingHeader:
		fmt.Fprintln(stdout, "hook install (codex): 표식도 없고 command도 우리 것이 아닌 [mcp_servers.ctr] 감지 — 기입 생략(사용자 등록으로 봅니다. doctor [10]과 대조해 정리한 뒤 재실행하세요)")
	case mcpConflict:
		fmt.Fprintln(stdout, "hook install (codex): config.toml에 ctr 관련 흔적 감지 — MCP 기입·가드 등록 보류. doctor 스니펫으로 수동 등록 후 재실행하세요")
	case mcpMarkerAnomaly:
		fmt.Fprintf(stdout, "hook install (codex): config.toml 무변경·가드 등록 보류 — %s\n", anomaly.reason())
	}
}
```

호출부(`runHookInstallCodex`, 현재 743행)는 사유를 함께 넘긴다. `installCodexConfigBlock`은 상태만 돌려주므로 구간 사유를 따로 읽는다:
```go
	_, cfgAnomaly := probeCodexMCPBlock(cfgExisting)
	reportCodexMCPState(stdout, res.State, cfgAnomaly)
```

**주의**: `probeCodexMCPBlock`을 여기서 한 번 더 부르는 것은 순수 함수의 재호출이므로 부작용이 없다. `res.State`가 `mcpMarkerAnomaly`가 아닌 갈래에서는 `cfgAnomaly`가 쓰이지 않는다.

- [ ] **Step 8: 문면 테스트를 더한다**

`internal/cli/hook_install_test.go`에 추가한다(기존 Codex 설치 테스트 계열 옆):
```go
// TestHookInstallCodexAnomalyReason — 이상 사유가 설치기 안내에 실린다(§2-7). 사유가 다른
// 파일에서 중복 헤더 정리만 지시하면 사용자는 install이 무변경인 이유를 알 수 없다.
func TestHookInstallCodexAnomalyReason(t *testing.T) {
	cases := []struct {
		name string
		cfg  string
		want string
	}{
		{"중복 헤더", "[mcp_servers.ctr]\n[x]\n[mcp_servers.ctr]\n", "헤더가 둘 이상"},
		{"스캐너 열림", "[mcp_servers.ctr]\nk = \"\"\"\nunclosed\n", "닫히지 않았습니다"},
		{"이스케이프 키", "[mcp_servers.ctr]\n\"comm\\u0061nd\" = \"x\"\n", "이스케이프 표기"},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			codexHome := t.TempDir()
			t.Setenv("CODEX_HOME", codexHome)
			cfgPath := filepath.Join(codexHome, "config.toml")
			write(t, cfgPath, []byte(c.cfg))
			var out bytes.Buffer
			if err := runHookInstallCodex(true, false, false, "", t.TempDir(), "0.16.0", nil, false, &out); err != nil {
				t.Fatalf("runHookInstallCodex: %v", err)
			}
			if !strings.Contains(out.String(), c.want) {
				t.Errorf("안내에 %q 없음:\n%s", c.want, out.String())
			}
			after, rErr := os.ReadFile(cfgPath)
			if rErr != nil {
				t.Fatal(rErr)
			}
			if string(after) != c.cfg {
				t.Errorf("config.toml이 바뀌었다:\n%s", after)
			}
		})
	}
}
```

- [ ] **Step 8b: `[16]`의 사유 인쇄를 단정한다**

스펙 §0 D85는 `[16]`·`[20]`·설치기 **셋 모두**의 사유 인쇄를 요구한다. 기존
`TestDoctorCodexMCPLine`의 이상 케이스는 `"수동 확인"`만 보므로 **새 문면과 옛 문면에서 모두
통과한다** — Step 6을 건너뛰어도 전 스위트가 초록이 된다. `cli_test.go`의 그 표에 케이스를 더한다:

```go
		{
			name: "⑥ 이상 — 중복 헤더 사유",
			setup: func(t *testing.T, codexHome string) {
				write(t, filepath.Join(codexHome, "config.toml"), []byte("[mcp_servers.ctr]\n[x]\n[mcp_servers.ctr]\n"))
			},
			wantContain: []string{"테이블=이상", "헤더가 둘 이상"},
		},
		{
			name: "⑦ 이상 — 정규화 불가 키 사유",
			setup: func(t *testing.T, codexHome string) {
				write(t, filepath.Join(codexHome, "config.toml"), []byte("[mcp_servers.ctr]\n\"comm\\u0061nd\" = \"x\"\n"))
			},
			wantContain: []string{"테이블=이상", "이스케이프 표기"},
		},
		{
			name: "⑧ 구간 밖 충돌 — 그 상태에 맞는 사유",
			setup: func(t *testing.T, codexHome string) {
				write(t, filepath.Join(codexHome, "config.toml"),
					[]byte("[mcp_servers.ctr]\ncommand = \"context-router\"\n[mcp_servers.ctr.tools.ctr_execute]\napproval_mode = \"never\"\n"))
			},
			wantContain: []string{"테이블=이상", "관리 테이블 밖에"},
			wantAbsent:  []string{"헤더가 둘 이상"},
		},
```

**⑧의 `wantAbsent`가 F6의 감시선이다** — 충돌 상태에 "헤더를 하나만 남기라"고 말하면 FAIL한다.
기존 표에 `setup`의 시그니처가 다르면 그 표의 형태에 맞춘다.

- [ ] **Step 9: 테스트가 통과하는지 확인한다**

Run:
```bash
go test -p 1 -run 'TestCodexAnomalyReason|TestProbeCodexMCPBlock|TestDoctorCodexMCPLine|TestHookInstallCodexAnomalyReason' ./internal/cli/ -v
```
Expected: 전부 PASS.

- [ ] **Step 10: 유도 FAIL**

`reason()`의 `anomalyEscapedKey` 갈래를 `anomalyDupHeader`와 같은 문자열로 바꾼다.

Run:
```bash
go test -p 1 -run 'TestCodexAnomalyReason|TestHookInstallCodexAnomalyReason' ./internal/cli/
```
Expected: **FAIL** — 사유 문면이 같다는 단정과 "이스케이프 표기" 부재. 복구한다.

- [ ] **Step 11: 전역 제약 확인 후 커밋**

```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
git add internal/cli/codex_toml.go internal/cli/cli.go internal/cli/hook_install.go internal/cli/codex_toml_test.go internal/cli/hook_install_test.go
git status --short
git commit -m "$(cat <<'EOF'
feat(v0.16): 이상 사유를 [16]과 설치기 문면에 싣는다 (D85·D87)

기존 안내는 사유와 무관하게 중복 헤더 정리를 지시했으므로, 스캐너 열림이나
정규화 불가 키인 사용자는 그 안내를 따라도 아무 일이 일어나지 않고 install이
영구히 무변경인 이유를 알 수 없었다.

probeCodexMCPBlock이 불리언 대신 사유를 돌려준다. 구간 밖 충돌은
codexAnomaly의 사유가 아니어서 기존 동작 유지를 위해 중복 헤더로 접었다 —
정확한 분리는 X5(비범위)에 속하며 그 판단을 주석에 남겼다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: D86 — `--fix` 갈래를 표식·`command`로 좁힌다 (X8)

**Files:**
- Modify: `internal/cli/codex_toml.go` (`codexInstallRequest`에 필드 하나, `installCodexConfigBlock`의 `keepArgs` 갈래·`ownedSame`·`execExposed`·`Profiles`, `codexInstallResult` 주석)
- Test: `internal/cli/codex_toml_test.go` (새 테스트 함수)

**Interfaces:**
- Consumes: T1~T3의 변경(같은 파일).
- Produces: `codexInstallRequest.MarkerOnly bool`. 이 필드가 참이면 `installCodexConfigBlock`은 `args`·`enabled_tools`를 원문으로 보존하고, 결과 필드 `Profiles`는 `nil`이 된다. **`ArgsKept`의 의미는 바뀌지 않는다**(되읽기 실패 전용). T5가 `doctorFixRegistrations`에서 이 필드를 세운다 — 이 태스크에서는 순수 함수와 그 테스트만 다룬다.

**무엇이 문제인가**: `doctorFixRegistrations`의 의도는 `.mcp.json` 갈래 주석이 밝힌 "표식만 고치고 프로필은 기존 항목의 것을 그대로 둔다"인데, Codex 갈래는 `SetProfile`을 주지 않아 `profilesFromArgs`로 프로필을 되읽고 `enabled_tools`를 프로필에서 **재조립**한다 → 사용자가 손으로 넓힌 도구가 기본값으로 줄어든다.

**정확한 계약은 "표식만"이 아니다**: `codexTableBody`는 `command` 줄을 `keepArgs`와 무관하게 항상 우리 값으로 되쓴다. 그러므로 `--fix`는 **표식과 `command`를 맞추고 `args`·`enabled_tools`를 보존**한다. 기존 `ArgsKept` 안내 문면이 이미 같은 표현을 쓴다("command와 소유 표식만 갱신") — 그 표현에 맞춘다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`internal/cli/codex_toml_test.go`에 추가한다:

```go
// TestCodexInstallMarkerOnly — D86(§2-3·§2-4·§2-5·§2-8). 표식 전용 갈래는 args·enabled_tools를
// 원문으로 보존하고 표식과 command만 맞춘다. **args는 우리 형식이어야 한다** — 우리 형식이
// 아니면 profilesFromArgs가 ok=false를 내 D81의 되읽기 실패 경로가 이미 두 값을 보존하므로,
// 그 픽스처로는 이 갈래를 되돌려도 통과한다(물지 않는 검사).
func TestCodexInstallMarkerOnly(t *testing.T) {
	// 우리 형식 args + 사용자가 손으로 넓힌 enabled_tools(프로필이 켜지 않는 ctr_execute를 더했다)
	src := "[mcp_servers.ctr]\n" +
		"command = \"context-router\"\n" +
		"args = [\"--enable\", \"ingest\"]\n" +
		"enabled_tools = [\"ctr_search\", \"ctr_index\", \"ctr_execute\"]\n" +
		"[mcp_servers.ctr.env]\n" +
		codexMarkerKey + " = \"context-router/0.15.0\"\n"

	res := installCodexConfigBlock([]byte(src), codexInstallRequest{
		Marker: hookMarker("0.16.0"), MarkerOnly: true,
	})
	if res.State != mcpWritten || !res.Changed {
		t.Fatalf("state=%d changed=%v want mcpWritten/true", res.State, res.Changed)
	}
	out := string(res.Out)
	// 사용자가 넓힌 목록이 바이트 그대로 남는다
	if !strings.Contains(out, "enabled_tools = [\"ctr_search\", \"ctr_index\", \"ctr_execute\"]") {
		t.Errorf("enabled_tools가 보존되지 않았다:\n%s", out)
	}
	if !strings.Contains(out, "args = [\"--enable\", \"ingest\"]") {
		t.Errorf("args가 보존되지 않았다:\n%s", out)
	}
	// 표식은 현재 버전이 된다
	if !strings.Contains(out, codexMarkerKey+" = \""+hookMarker("0.16.0")+"\"") {
		t.Errorf("표식이 현재 버전이 아니다:\n%s", out)
	}
	// 이 갈래는 프로필을 기입하지 않으므로 "실제로 기입한 프로필"이 없다
	if res.Profiles != nil {
		t.Errorf("MarkerOnly인데 Profiles=%v", res.Profiles)
	}
	// ArgsKept는 되읽기 실패 전용이다 — 이 픽스처는 되읽기에 성공한다
	if res.ArgsKept {
		t.Errorf("ArgsKept=true — 되읽기에 성공한 픽스처인데 의미가 오염됐다")
	}
	// 산출물이 실제로 노출하는 것을 답해야 한다(보존된 목록에 exec가 있다)
	if !res.ExecExposed {
		t.Errorf("ExecExposed=false — 보존된 enabled_tools에 ctr_execute가 있다")
	}

	// 멱등: 산출물에 같은 요청을 다시 걸면 무변경이다(D84 단일 백업 슬롯의 전제)
	res2 := installCodexConfigBlock(res.Out, codexInstallRequest{
		Marker: hookMarker("0.16.0"), MarkerOnly: true,
	})
	if res2.Changed {
		t.Errorf("2회차가 무변경이 아니다:\n%s", res2.Out)
	}

	// **호스트 재직렬화 형태에서의 무변경** — 이 단정이 ownedSame의 keepArgs 절을 재는 것이다.
	// 위 멱등 단정만으로는 부족하다: 산출물은 우리 형식이라 바이트 비교로 이미 무변경이 나오므로
	// keepArgs 절을 되돌려도 통과한다(물지 않는 검사). 대입 공백이 우리 형식과 다른 파일은
	// 바이트 비교가 성립하지 않아 키 단위 동치가 판정해야 하고, 보존하는 값을 비교하면
	// enabled_tools가 프로필 조립값과 달라 매 실행 재기입된다(D84 단일 슬롯 무의미화).
	reser := "[mcp_servers.ctr]\n" +
		"command=\"context-router\"\n" +
		"args=[\"--enable\",\"ingest\"]\n" +
		"enabled_tools=[\"ctr_search\",\"ctr_index\",\"ctr_execute\"]\n" +
		"[mcp_servers.ctr.env]\n" +
		codexMarkerKey + "=\"" + hookMarker("0.16.0") + "\"\n"
	if r := installCodexConfigBlock([]byte(reser), codexInstallRequest{
		Marker: hookMarker("0.16.0"), MarkerOnly: true,
	}); r.Changed {
		t.Errorf("재직렬화 형태에서 무변경이 아니다 — ownedSame이 keepArgs를 보지 않는다:\n%s", r.Out)
	}

	// install(MarkerOnly 없음)은 종전대로 프로필에서 재조립한다 — D81 불변(§2-8)
	res3 := installCodexConfigBlock([]byte(src), codexInstallRequest{Marker: hookMarker("0.16.0")})
	if strings.Contains(string(res3.Out), "ctr_execute") {
		t.Errorf("install이 사용자 확장을 보존했다 — D81 재조립이 깨졌다:\n%s", res3.Out)
	}
}

// TestCodexInstallMarkerOnlyRewritesCommand — D86(§2-4). 표식은 우리 것이고 command가
// 절대경로인 등록물에서 command는 우리 값으로 맞춰지고 args·enabled_tools는 보존된다.
// "표식만 갱신한다"가 아니라 "표식과 command를 맞춘다"가 정확한 계약이다.
func TestCodexInstallMarkerOnlyRewritesCommand(t *testing.T) {
	src := "[mcp_servers.ctr]\n" +
		"command = \"C:\\\\bin\\\\context-router.exe\"\n" +
		"args = [\"--enable\", \"ingest,net\"]\n" +
		"enabled_tools = [\"ctr_search\"]\n" +
		"[mcp_servers.ctr.env]\n" +
		codexMarkerKey + " = \"context-router/0.15.0\"\n"

	res := installCodexConfigBlock([]byte(src), codexInstallRequest{
		Marker: hookMarker("0.16.0"), MarkerOnly: true,
	})
	if res.State != mcpWritten || !res.Changed {
		t.Fatalf("state=%d changed=%v", res.State, res.Changed)
	}
	out := string(res.Out)
	if !strings.Contains(out, "command = \"context-router\"") {
		t.Errorf("command가 우리 값으로 맞춰지지 않았다:\n%s", out)
	}
	if !strings.Contains(out, "enabled_tools = [\"ctr_search\"]") {
		t.Errorf("enabled_tools가 보존되지 않았다:\n%s", out)
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run:
```bash
go test -p 1 -run 'TestCodexInstallMarkerOnly' ./internal/cli/
```
Expected: **컴파일 에러** — `unknown field MarkerOnly in struct literal`.

- [ ] **Step 3: 요청 필드를 더한다**

`codexInstallRequest`(현재 449–453행):
```go
// codexInstallRequest — 관리 테이블 기입 요청(D80·D81·D86).
type codexInstallRequest struct {
	Profiles   []string // 명시 플래그로 정해진 프로필(SetProfile=false면 쓰이지 않는다)
	SetProfile bool     // --enable/--enable-exec 중 하나라도 있었는가
	Marker     string   // env.CTR_MANAGED 값(hookMarker(version))
	// MarkerOnly — 표식과 command만 맞추고 args·enabled_tools는 원문을 보존한다(D86).
	// doctor --fix가 세우며 install은 세우지 않는다: install은 "등록을 원하는 상태로 만든다"이고
	// --fix는 "표식을 현재 버전으로"라, 같은 함수를 쓰면서 그 차이를 인자로 표현하지 않으면
	// --fix가 사용자가 손으로 넓힌 enabled_tools를 프로필 기본값으로 되돌린다.
	// **첫 기입 경로에서는 무시된다** — 보존할 원문이 없고, --fix는 등록을 만들지 않는다.
	MarkerOnly bool
}
```

- [ ] **Step 4: `keepArgs` 갈래를 만든다**

`installCodexConfigBlock`에서 `profiles, argsKept := ...` 블록 **바로 뒤**에 넣는다:
```go
	// keepArgs — 몸통 조립에서 args·enabled_tools를 보존 라인으로 되돌리는가. 두 사유가 여기서
	// 합류한다: 되읽기 실패(D81 argsKept)와 표식 전용 요청(D86 MarkerOnly). 결과 필드 ArgsKept는
	// **앞의 것만** 담는다 — 합치면 설치기 안내가 "프로필을 해석하지 못했다"를 잘못 말한다.
	keepArgs := argsKept || req.MarkerOnly
```

`execExposed` 판정을 `keepArgs`로 바꾼다(현재 505–508행):
```go
	// execExposed — 산출물의 enabled_tools가 exec 도구를 담는가(D81, 리뷰 승격 — 이월 T4-F3의
	// 근본 픽스). **기존 테이블이 있고** 보존 갈래일 때만 view.tools가 최종값이다. 테이블이
	// 없으면 첫 기입 경로가 몸통을 profiles로 조립하므로(MarkerOnly는 그 경로에서 무시된다)
	// 보존 목록을 보면 결과 필드가 산출물과 어긋난다 — view가 비어 있어 늘 false가 된다.
	execExposed := enabledToolsExposeExec(enabledToolsForProfiles(profiles))
	if keepArgs && sp.table.found {
		execExposed = enabledToolsExposeExec(view.tools)
	}
```

`ownedSame`(현재 534–536행)도 `keepArgs`를 본다:
```go
	envMissing := !sp.env.found && view.inlineEnv < 0
	// keepArgs 갈래에서는 args·enabled_tools를 비교하지 않는다 — 보존하는 값을 비교하면 사용자가
	// 넓힌 목록이 매 실행 "다르다"로 읽혀 .bak이 매번 새로 생기고 D84 단일 슬롯 계약이
	// 무의미해진다.
	ownedSame := view.command == hookBinaryName && marker == req.Marker &&
		(keepArgs || (slices.Equal(view.args, mcpArgsForProfiles(profiles)) &&
			slices.Equal(view.tools, enabledToolsForProfiles(profiles))))
```

몸통 조립(현재 547행)에 `keepArgs`를 넘긴다:
```go
	body := codexTableBody(lines, sp.table, view, profiles, keepArgs, req.Marker, eol)
```

**첫 기입 경로(현재 514–528행)는 바꾸지 않는다** — 그 경로의 `codexTableBody(..., false, ...)`가 그대로다. 보존할 원문이 없고 `--fix`는 등록을 만들지 않으므로 이 조합은 실사용에 없다. 그 이유를 한 줄로 적는다:
```go
	if !sp.table.found { // 첫 기입 — 관리 테이블을 EOF 뒤에 잇는다
		// MarkerOnly는 여기서 무시한다 — 보존할 원문이 없고 --fix는 등록을 만들지 않는다(D86).
		body := ensureEOL(codexTableBody(lines, codexSpan{}, view, profiles, false, req.Marker, eol), eol)
```

- [ ] **Step 5: `Profiles` 결과 필드를 그 갈래에서 비운다**

`crlf := ...` 앞에 넣는다:
```go
	// resultProfiles — 설치기 안내가 읽는 "실제로 기입한 프로필". 표식 전용 갈래는 프로필을
	// 기입하지 않으므로 그 값이 없다(D86) — 되읽은 값을 실으면 그 필드의 의미가 성립하지 않는다.
	resultProfiles := profiles
	if req.MarkerOnly {
		resultProfiles = nil
	}
```

그리고 **세 반환 지점 모두** `Profiles: profiles`를 `Profiles: resultProfiles`로 바꾼다(첫 기입 반환·무변경 반환·최종 반환).

`codexInstallResult`의 `Profiles` 주석도 정정한다:
```go
	Profiles []string // 실제로 기입한 프로필(설치기 안내용) — ArgsKept면 되읽기 실패 값(nil)이고, MarkerOnly면 기입하지 않으므로 nil이다.
```

- [ ] **Step 6: 테스트가 통과하는지 확인한다**

Run:
```bash
go test -p 1 -run 'TestCodexInstallMarkerOnly' ./internal/cli/ -v
```
Expected: 두 테스트 PASS.

- [ ] **Step 7: 유도 FAIL — 세 방향**

① **`keepArgs`를 몸통에서 되돌린다**(`codexTableBody(..., argsKept, ...)`):
Expected: `enabled_tools가 보존되지 않았다` FAIL.

② **`ownedSame`을 되돌린다**(`argsKept ||`로):
Expected: 멱등 단정 `2회차가 무변경이 아니다` FAIL — 보존된 목록이 프로필 조립값과 달라 매번 재기입된다.

③ **`resultProfiles`를 되돌린다**:
Expected: `MarkerOnly인데 Profiles=...` FAIL.

각 확인 후 복구한다.

- [ ] **Step 8: 전역 제약 확인 후 커밋**

```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
git add internal/cli/codex_toml.go internal/cli/codex_toml_test.go
git status --short
git commit -m "$(cat <<'EOF'
feat(v0.16): D86 표식 전용 갈래 — args·enabled_tools를 보존한다 (X8)

--fix가 install 경로를 재사용하면서 프로필 재조립까지 물려받아 사용자가 손으로
넓힌 enabled_tools를 기본값으로 되돌렸다. 요청 필드 하나로 그 차이를 표현하고
기존 keepArgs 몸통 조립(D81 되읽기 실패 경로)을 재사용한다.

정확한 계약은 "표식만"이 아니라 "표식과 command"다 — codexTableBody가 command를
keepArgs와 무관하게 항상 되쓴다. 기존 ArgsKept 안내 문면이 이미 같은 표현을
쓴다.

ArgsKept의 의미는 오염시키지 않았고(되읽기 실패 전용), 무변경 판정은 보존하는
값을 비교하지 않으며(비교하면 .bak이 매 실행 생긴다), Profiles는 이 갈래에서
nil이다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: D85 — 판정원을 하나로 (요청 조립 + 결과 번역)

**Files:**
- Modify: `internal/cli/codex_toml.go` (`codexInstallResult`에 필드 하나)
- Modify: `internal/cli/cli.go` (새 함수 `codexRegistrationVerdict`, `doctorFixRegistrations`의 Codex 갈래)
- Test: `internal/cli/cli_test.go` (새 테스트 함수)

**Interfaces:**
- Consumes: T4의 `codexInstallRequest.MarkerOnly`.
- Produces:
  - `codexInstallResult.TableFound bool` — 관리 테이블이 구간으로 잡혔는가. `codexConfigMarker`라는 둘째 판독기를 대신한다.
  - `func codexRegistrationVerdict(data []byte, version string) codexVerdict` (in `cli.go`) —
    `type codexVerdict struct { State codexMCPState; Anomaly codexAnomaly; TableFound, Changed bool; Marker, Command string }`.
    **요청 조립이 이 함수 안에만 있다.** `[20]`(T6)과 `doctorFixRegistrations`가 이 함수만 부른다.
  - `func (v codexVerdict) shouldFix() bool` — 권고 술어. `v.State == mcpWritten && v.TableFound && v.Changed`.

**왜 이 구조인가**: "같은 인자로 부른다"를 호출자의 규율로 두면 다음 변경에서 한쪽이 빠진다. 요청 조립을 한 함수에 넣어 **인자 동일성을 구조로** 만든다. 관리 테이블 실존도 그 함수가 답한다 — 지금은 `codexConfigMarker`라는 둘째 판독기에서 오므로 판정원이 둘이다.

**이것은 "새 판독기"가 아니다**: 이 함수는 판정하지 않는다. `installCodexConfigBlock`을 부르고 그 결과를 담아 넘긴다. 판정원은 여전히 하나다.

- [ ] **Step 1: 게이트 1을 다시 통과했는지 확인한다**

P2를 다시 돌린다(T1·T2가 `codex_toml.go`를 고쳤으므로 재확인이 필요하다):
```bash
grep -nE '\bos\.|filepath\.|time\.|rand\.' internal/cli/codex_toml.go
```
Expected: `codexConfigPath` 안의 세 줄만. 새 헬퍼(`tomlKeyTokenHasEscape`·`codexEscapedKeyInSpans`)에 IO가 없어야 한다.

- [ ] **Step 1b: 순수성을 주석 계약으로 남긴다 (스펙 §1.3 게이트 1)**

읽기 전용 경로가 이 함수에 의존한다는 것을 그 자리에 적어, 나중의 IO 추가가 무엇을 깨뜨리는지
알린다. `installCodexConfigBlock`의 주석 끝에 더한다:
```go
// **읽기 전용 경로가 이 함수를 부른다**(D85 — doctor [20]의 감지원). 그러므로 파일 IO·시간·난수를
// 들이면 진단이 파일을 쓰거나 결정적이지 않게 된다. 프로필 헬퍼를 포함한 호출 트리 전체가 순수해야
// 하며, 그 조건은 스펙 v0.16 §1.3 게이트 1과 §3 표4에 있다.
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`internal/cli/cli_test.go`에 추가한다:

```go
// TestCodexRegistrationVerdict — D85(§2-2). 감지와 고침이 같은 판정원을 쓰는지 본다. 권고
// 술어는 --fix가 실제로 기입하는 조건 전체다: mcpWritten AND 테이블 실존 AND Changed.
// Changed 하나로 줄이면 "파일은 있고 관리 테이블만 없는" 상태에서 install이 append 경로로
// Changed=true를 내는데 --fix는 no-create로 거절하므로 오권고가 된다.
func TestCodexRegistrationVerdict(t *testing.T) {
	const ver = "0.16.0"
	ours := "[mcp_servers.ctr]\ncommand = \"context-router\"\nargs = [\"--enable\", \"ingest,net\"]\n" +
		"enabled_tools = [\"ctr_search\", \"ctr_index\", \"ctr_fetch_and_index\"]\n" +
		"[mcp_servers.ctr.env]\n" + codexMarkerKey + " = \"context-router/"
	cases := []struct {
		name       string
		cfg        string
		wantFix    bool
		wantState  codexMCPState
		wantTable  bool
	}{
		{
			name:      "관리 테이블 없음(파일은 있다) — 권고하지 않는다",
			cfg:       "model = \"gpt\"\n",
			wantFix:   false,
			wantState: mcpWritten,
			wantTable: false,
		},
		{
			name:      "현재 버전 — 권고하지 않는다",
			cfg:       ours + ver + "\"\n",
			wantFix:   false,
			wantState: mcpWritten,
			wantTable: true,
		},
		{
			name:      "구 버전 표식 — 권고한다",
			cfg:       ours + "0.15.0\"\n",
			wantFix:   true,
			wantState: mcpWritten,
			wantTable: true,
		},
		{
			name:      "남의 테이블 — 권고하지 않는다",
			cfg:       "[mcp_servers.ctr]\ncommand = \"other\"\n[mcp_servers.ctr.env]\n" + codexMarkerKey + " = \"other-tool/1.0\"\n",
			wantFix:   false,
			wantState: mcpExistingHeader,
			wantTable: true,
		},
		{
			// 두 케이스의 wantTable은 **true**다(실측). 구간 판정이 실패했거나 충돌이어도
			// codexManagedSpans가 헤더 자체는 찾았기 때문이다. 구속력 있는 단정은 shouldFix()가
			// 거짓이라는 것이며, 권고 술어가 State를 먼저 보므로 그 두 상태에서 TableFound가
			// 무엇이든 권고하지 않는다.
			name:      "구간 밖 충돌 — 권고하지 않는다",
			cfg:       ours + "0.15.0\"\n[mcp_servers.ctr.tools.ctr_execute]\napproval_mode = \"never\"\n",
			wantFix:   false,
			wantState: mcpConflict,
			wantTable: true,
		},
		{
			name:      "구간 판정 불가 — 권고하지 않는다",
			cfg:       "[mcp_servers.ctr]\n[x]\n[mcp_servers.ctr]\n",
			wantFix:   false,
			wantState: mcpMarkerAnomaly,
			wantTable: true,
		},
		{
			name:      "사용자가 넓힌 enabled_tools + 현재 버전 — 권고하지 않는다",
			cfg: "[mcp_servers.ctr]\ncommand = \"context-router\"\nargs = [\"--enable\", \"ingest\"]\n" +
				"enabled_tools = [\"ctr_search\", \"ctr_index\", \"ctr_execute\"]\n" +
				"[mcp_servers.ctr.env]\n" + codexMarkerKey + " = \"context-router/" + ver + "\"\n",
			wantFix:   false,
			wantState: mcpWritten,
			wantTable: true,
		},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			v := codexRegistrationVerdict([]byte(c.cfg), ver)
			if v.State != c.wantState {
				t.Errorf("State=%d want %d", v.State, c.wantState)
			}
			if v.TableFound != c.wantTable {
				t.Errorf("TableFound=%v want %v", v.TableFound, c.wantTable)
			}
			if v.shouldFix() != c.wantFix {
				t.Errorf("shouldFix=%v want %v (State=%d TableFound=%v Changed=%v)",
					v.shouldFix(), c.wantFix, v.State, v.TableFound, v.Changed)
			}
		})
	}
}
```

- [ ] **Step 3: 실패를 확인한다**

Run:
```bash
go test -p 1 -run TestCodexRegistrationVerdict ./internal/cli/
```
Expected: **컴파일 에러** — `undefined: codexRegistrationVerdict`.

- [ ] **Step 4: `TableFound`를 결과에 노출한다**

`codexInstallResult`(현재 457–469행)에 필드를 더한다:
```go
	// TableFound — 관리 테이블이 구간으로 잡혔는가(D85). doctor가 "등록물이 있는가"를 묻는
	// 판정을 이 값으로 답한다 — 별도 판독기(codexConfigMarker)에서 얻으면 판정원이 둘이 되고
	// 그 둘이 갈리는 것이 이 릴리스가 닫는 어긋남이다. mcpWritten이면서 이 값이 거짓인 상태가
	// "append하면 된다"이며, --fix는 등록을 만들지 않으므로 그 상태에서 기입하지 않는다.
	TableFound bool
```

`installCodexConfigBlock`의 **모든 반환 지점**에서 이 필드를 채운다. `sp` 판정 직후에 지역 변수를 두면 누락을 막을 수 있다:
```go
	lines := splitLinesKeepEnds(existing)
	sp := codexManagedSpans(lines)
	if sp.anomaly != anomalyNone {
		return codexInstallResult{Out: existing, State: mcpMarkerAnomaly, TableFound: sp.table.found}
	}
	if scanOutsideSpans(lines, sp) {
		return codexInstallResult{Out: existing, State: mcpConflict, TableFound: sp.table.found}
	}
```
남은 반환 넷(`mcpExistingHeader` · 첫 기입 · 무변경 · 최종)에도 `TableFound: sp.table.found`를 더한다.

**주의**: 구간 판정 불가·구간 밖 충돌에서도 `sp.table.found`는 **참이다**(실측) — 구간 판정이
실패했어도 `codexManagedSpans`가 헤더 자체는 찾았기 때문이다. 그래서 위 테스트의 두 케이스가
`wantTable: true`를 기대한다. **기대값을 실행 결과에 맞춰 고치지 않는다** — 그러면 단정이 명세가
아니라 관측 기록이 된다. 권고 술어는 `State == mcpWritten`을 먼저 보므로 그 두 상태에서
`TableFound`가 무엇이든 권고하지 않으며, 구속력 있는 단정은 `shouldFix()==false`다.

- [ ] **Step 5: 판정 함수를 쓴다**

`internal/cli/cli.go`의 `doctorFixRegistrations` **바로 앞**에 둔다:

```go
// codexVerdict — config.toml 등록물의 판정(D85). 감지(doctor [20])와 고침(doctor --fix)이 이
// 한 값을 공유한다.
type codexVerdict struct {
	State      codexMCPState
	Anomaly    codexAnomaly
	TableFound bool
	Changed    bool
	Marker     string
	Command    string
	// Out — 기입할 산출 바이트. 기입 갈래가 이 값을 쓰므로 **요청 조립 지점이 한 곳으로
	// 묶인다** — 기입 쪽에서 요청을 다시 조립하면 그 두 조립이 갈릴 수 있고, 그것이 이
	// 릴리스가 닫는 어긋남과 같은 형태다. 진단 경로는 이 필드를 쓰지 않고 무시한다.
	Out []byte
}

// codexRegistrationVerdict — 감지와 고침이 공유하는 판정(D85). **요청 조립이 이 함수 안에만
// 있다** — "같은 인자로 부른다"를 호출자의 규율로 두면 다음 변경에서 한쪽이 빠지고, 그러면
// 요청 필드 축으로 감지와 고침이 다시 갈린다(D86의 MarkerOnly가 그 축이다).
// 이 함수는 판정하지 않는다: installCodexConfigBlock을 부르고 그 결과를 담아 넘길 뿐이므로
// 판정원은 하나다. 그 함수가 순수 변환(파일 IO 없음)이라는 것이 읽기 전용 경로에서 부를 수
// 있는 근거이며, 스펙 §1.3 게이트 1이 그것을 확인한다.
func codexRegistrationVerdict(data []byte, version string) codexVerdict {
	res := installCodexConfigBlock(data, codexInstallRequest{
		Marker: hookMarker(version), MarkerOnly: true,
	})
	_, anomaly := probeCodexMCPBlock(data)
	marker, command, _ := codexConfigMarker(data)
	return codexVerdict{
		State: res.State, Anomaly: anomaly, TableFound: res.TableFound,
		Changed: res.Changed, Marker: marker, Command: command, Out: res.Out,
	}
}

// shouldFix — doctor --fix가 실제로 기입하는 조건 전체(D85). 셋의 논리곱이며 Changed 하나로
// 줄이면 "파일은 있고 관리 테이블만 없는" 상태에서 install이 append 경로로 Changed=true를
// 내는데 --fix는 등록을 만들지 않으므로 오권고가 된다 — 지금 "미등록·무경고"로 옳게 보고되는
// 가장 흔한 미설치 상태가 그것이다.
func (v codexVerdict) shouldFix() bool {
	return v.State == mcpWritten && v.TableFound && v.Changed
}
```

**`Marker`·`Command`를 함께 담는 이유**: T6의 라벨 계산이 그 값을 본다(`구형식` 구별에 `ownedRegistration`이 필요하다). 라벨용 판독을 별도 호출로 흩으면 판정원이 다시 늘어난다.

- [ ] **Step 6: `doctorFixRegistrations`가 그 함수만 부르게 한다**

Codex 갈래(현재 2062–2083행)를 바꾼다:
```go
	default:
		v := codexRegistrationVerdict(data, version)
		switch {
		case v.State != mcpWritten:
			// 구간 판정 불가·구간 밖 충돌·사용자 소유 테이블이 여기로 온다. 사유는 [16]과 [20]이
			// 라벨로 말한다.
			fmt.Fprintln(w, "[20] fix: config.toml이 기입 가능한 상태가 아닙니다 — [16] 안내를 먼저 따르세요")
		case !v.TableFound:
			// 관리 테이블 자체가 없다 — mcpWritten은 "append하면 된다"는 뜻이지만 --fix는
			// 등록을 만들지 않는다. 이 상태를 [20]도 "미등록"으로 보고하며 권고하지 않는다.
			fmt.Fprintln(w, "[20] fix: config.toml에 우리 관리 테이블이 없습니다 — 만들지 않습니다. hook install --codex로 먼저 등록하세요")
		case !v.Changed:
			fmt.Fprintln(w, "[20] fix: config.toml은 이미 현재 형식·버전입니다 — 무변경")
		default:
			if bErr := backupCodexConfig(cfgPath, data); bErr != nil {
				fmt.Fprintln(w, "[20] fix: config.toml 백업 실패 — 기입하지 않았습니다")
			} else if wErr := atomicWriteFile(cfgPath, v.Out); wErr != nil {
				fmt.Fprintln(w, "[20] fix: config.toml 기록 실패")
			} else {
				fmt.Fprintln(w, "[20] fix: config.toml 관리 테이블의 표식과 command를 다시 기입했습니다(args·enabled_tools는 원문 보존, 백업 config.toml.bak)")
			}
		}
	}
```

**기입 갈래가 `v.Out`을 쓰는 것이 이 태스크의 요점이다.** 여기서 `installCodexConfigBlock`을
다시 부르면 **요청 조립이 두 곳에 남아** 이 태스크의 인터페이스 주장("요청 조립이
`codexRegistrationVerdict` 안에만 있다")이 성립하지 않는다 — 다음 변경에서 한쪽이 빠지는 것이
바로 이 릴리스가 닫는 어긋남의 형태다. 진단 경로는 `Out`을 만들지만 쓰지 않고 무시한다.

기존의 `_, _, hasTable := codexConfigMarker(data)` 줄(현재 2064행)은 **지운다** — 둘째 판독기를 없애는 것이 이 태스크의 목적이다.

- [ ] **Step 7: 테스트가 통과하는지 확인한다**

Run:
```bash
go test -p 1 -run 'TestCodexRegistrationVerdict|TestDoctorFix' ./internal/cli/ -v
```
Expected: PASS. `TestDoctorFix`의 기존 단정도 통과해야 한다(문면 한 줄이 바뀌었으므로 그 테스트가 그 문면을 축자로 잡고 있으면 함께 고친다 — 바뀐 문면은 "표식과 command를 다시 기입했습니다"다).

- [ ] **Step 8: 유도 FAIL**

`shouldFix`에서 `v.TableFound &&`를 지운다.

Run:
```bash
go test -p 1 -run TestCodexRegistrationVerdict ./internal/cli/ -v
```
Expected: **"관리 테이블 없음(파일은 있다) — 권고하지 않는다"가 FAIL** (`shouldFix=true want false`). 이것이 F1의 오권고 회귀를 막는 감시선이다. 복구한다.

- [ ] **Step 9: 전역 제약 확인 후 커밋**

```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
git add internal/cli/codex_toml.go internal/cli/cli.go internal/cli/cli_test.go
git status --short
git commit -m "$(cat <<'EOF'
feat(v0.16): D85 감지와 고침의 판정원을 하나로

요청 조립을 codexRegistrationVerdict 안에만 두어 인자 동일성을 호출자의
규율이 아니라 구조로 만든다. 관리 테이블 실존도 그 판정이 답한다 — 지금은
codexConfigMarker라는 둘째 판독기에서 왔고 그 둘이 갈리는 것이 이 릴리스가
닫는 어긋남이다.

권고 술어는 --fix가 실제로 기입하는 조건 전체다(mcpWritten AND 테이블 실존
AND Changed). Changed 하나로 줄이면 "파일은 있고 관리 테이블만 없는" 상태가
오권고로 회귀한다.

이 함수는 판정하지 않고 installCodexConfigBlock의 결과를 번역만 하므로
판정원은 여전히 하나다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: D85 — `[20]`의 라벨·경고·보고 문면

**Files:**
- Modify: `internal/cli/cli.go` (`[20]` 절의 라벨 계산·경고 문면)
- Modify: `internal/cli/hook_install.go` (`ownedRegistration` 주석 주장 축소)
- Test: `internal/cli/cli_test.go` (`TestDoctorMCPMarkerLine`에 케이스 추가, 감지·고침 등가 표)

**Interfaces:**
- Consumes: T5의 `codexRegistrationVerdict`·`codexVerdict.shouldFix()`.
- Produces: `[20]`의 Codex 라벨 값 셋이 추가된다 — `구형식`·`충돌`·`이상`. 기존 라벨(`없음`·`읽기실패`·`확인불가`·`미등록`·`표식없음`·`marker …`)은 지우지 않는다.

- [ ] **Step 1: 게이트 2를 다시 확인한다**

P3의 명령을 다시 돌려 문면 결속 테스트 목록이 그대로인지 본다. 그리고 **기존 여섯 케이스가 이 태스크 전에 통과하는지** 먼저 확인한다:
```bash
go test -p 1 -run TestDoctorMCPMarkerLine ./internal/cli/ -v
```
Expected: 여섯 서브테스트 PASS(아직 라벨을 바꾸지 않았다).

- [ ] **Step 2: 감지·고침 등가 표 테스트를 쓴다**

`internal/cli/cli_test.go`에 추가한다. **`[20]`의 라벨·경고와 `--fix`의 행동을 같은 테스트에서** 잰다(§2-1·§2-2) — 다른 테스트에서 재면 어긋남이 다시 숨는다.

```go
// TestDoctorDetectFixEquivalence — D85(§2-1·§2-2). 감지와 고침을 한 표에서 함께 단정한다.
// 경고가 --fix를 권한 모든 상태에서 --fix가 config.toml을 바꾸고, 권하지 않은 모든 상태에서
// 바꾸지 않는다. **Codex 갈래 한정**이다 — .mcp.json 갈래는 경고 없이도 재직렬화 형식 차이·
// 은퇴 항목 정리로 파일을 다시 쓴다(스펙 §1.2 첫 항, 비범위).
func TestDoctorDetectFixEquivalence(t *testing.T) {
	const ver = "0.16.0"
	ours := "[mcp_servers.ctr]\ncommand = \"context-router\"\nargs = [\"--enable\", \"ingest,net\"]\n" +
		"enabled_tools = [\"ctr_search\", \"ctr_index\", \"ctr_fetch_and_index\"]\n" +
		"[mcp_servers.ctr.env]\n" + codexMarkerKey + " = \"context-router/"
	cases := []struct {
		name      string
		cfg       string   // "" = config.toml을 만들지 않는다
		wantLabel string   // [20] 라벨에 포함될 문자열
		wantWarn  bool     // [20] warning 유무 = --fix가 바꾸는가
	}{
		{name: "① 파일 부재", cfg: "", wantLabel: "codex=없음", wantWarn: false},
		{name: "② 현재 버전", cfg: ours + ver + "\"\n", wantLabel: "codex=marker " + ver, wantWarn: false},
		{name: "③ 구 버전 표식", cfg: ours + "0.15.0\"\n", wantLabel: "≠" + ver, wantWarn: true},
		{name: "④ 무버전 표식", cfg: "[mcp_servers.ctr]\ncommand = \"context-router\"\n[mcp_servers.ctr.env]\n" + codexMarkerKey + " = \"context-router\"\n", wantLabel: "버전미상", wantWarn: true},
		{name: "⑤ 남의 테이블", cfg: "[mcp_servers.ctr]\ncommand = \"other\"\n[mcp_servers.ctr.env]\n" + codexMarkerKey + " = \"other-tool/1.0\"\n", wantLabel: "codex=미등록", wantWarn: false},
		{name: "⑥ 표식 없는 우리 테이블", cfg: "[mcp_servers.ctr]\ncommand = \"context-router\"\nargs = []\n", wantLabel: "codex=표식없음", wantWarn: true},
		{name: "⑦ 파일 존재·관리 테이블 부재", cfg: "model = \"gpt\"\n", wantLabel: "codex=미등록", wantWarn: false},
		{name: "⑧ 구 블록 + 비표준 command", cfg: codexBlockBegin + "\n[mcp_servers.ctr]\ncommand = \"C:\\\\bin\\\\ctr.exe\"\n" + codexBlockEnd + "\n", wantLabel: "codex=구형식", wantWarn: true},
		{name: "⑨ 구간 밖 충돌", cfg: ours + "0.15.0\"\n[mcp_servers.ctr.tools.ctr_execute]\napproval_mode = \"never\"\n", wantLabel: "codex=충돌", wantWarn: false},
		{name: "⑩ 중복 헤더", cfg: "[mcp_servers.ctr]\n[x]\n[mcp_servers.ctr]\n", wantLabel: "codex=이상", wantWarn: false},
		{name: "⑪ EOF 스캐너 열림", cfg: "[mcp_servers.ctr]\nk = \"\"\"\nunclosed\n", wantLabel: "codex=이상", wantWarn: false},
		{name: "⑫ 정규화 불가 키", cfg: "[mcp_servers.ctr]\n\"comm\\u0061nd\" = \"x\"\n", wantLabel: "codex=이상", wantWarn: false},
		{
			name: "⑬ 사용자가 넓힌 enabled_tools",
			cfg: "[mcp_servers.ctr]\ncommand = \"context-router\"\nargs = [\"--enable\", \"ingest\"]\n" +
				"enabled_tools = [\"ctr_search\", \"ctr_index\", \"ctr_execute\"]\n" +
				"[mcp_servers.ctr.env]\n" + codexMarkerKey + " = \"context-router/" + ver + "\"\n",
			wantLabel: "codex=marker " + ver, wantWarn: false,
		},
		{
			// 스펙 §2-6이 정규화 불가 키의 **두 형태**를 요구한다. ⑫는 서브테이블 형태이므로
			// 인라인 형태를 배선 수준에서 함께 잰다 — 그 형태는 키 토큰이 env라 서브테이블
			// 검사에 걸리지 않는 별개 경로다.
			name:      "⑭ 정규화 불가 키(인라인 env 형태)",
			cfg:       "[mcp_servers.ctr]\ncommand = \"context-router\"\nenv = { \"CTR_MAN\\u0041GED\" = \"context-router\" }\n",
			wantLabel: "codex=이상", wantWarn: false,
		},
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			codexHome := t.TempDir()
			t.Setenv("CODEX_HOME", codexHome)
			cfgPath := filepath.Join(codexHome, "config.toml")
			if c.cfg != "" {
				write(t, cfgPath, []byte(c.cfg))
			}
			projectRoot := t.TempDir()

			// 감지 — [20] 라벨과 경고
			var buf bytes.Buffer
			if err := runDoctor(context.Background(), &buf, t.TempDir(), projectRoot, ver, false); err != nil {
				t.Fatalf("runDoctor: %v out=%s", err, buf.String())
			}
			out := buf.String()
			if !strings.Contains(out, c.wantLabel) {
				t.Errorf("라벨 %q 없음:\n%s", c.wantLabel, out)
			}
			// 경고는 Codex 갈래가 유일한 원인이어야 한다 — .mcp.json은 만들지 않았으므로 그쪽
			// 라벨은 "없음"이고 경고를 내지 않는다.
			gotWarn := strings.Contains(out, "[20] warning:")
			if gotWarn != c.wantWarn {
				t.Errorf("warning=%v want %v:\n%s", gotWarn, c.wantWarn, out)
			}
			// 이상 라벨은 세 사유를 한 값으로 묶으므로 [20]이 사유 줄을 함께 낸다(§2-7).
			// 사유가 없으면 사용자는 install이 영구히 무변경인 이유를 알 수 없다.
			if strings.Contains(c.wantLabel, "이상") && !strings.Contains(out, "[20] codex: ") {
				t.Errorf("이상 라벨인데 사유 줄이 없다:\n%s", out)
			}

			// 고침 — --fix가 파일을 바꾸는가
			if c.cfg == "" {
				return // 부재 파일은 만들지 않는다(다른 테스트가 단정한다)
			}
			var fixBuf bytes.Buffer
			if err := runDoctor(context.Background(), &fixBuf, t.TempDir(), projectRoot, ver, true); err != nil {
				t.Fatalf("runDoctor --fix: %v out=%s", err, fixBuf.String())
			}
			after, rErr := os.ReadFile(cfgPath)
			if rErr != nil {
				t.Fatal(rErr)
			}
			changed := string(after) != c.cfg
			if changed != c.wantWarn {
				t.Errorf("--fix changed=%v인데 경고는 %v였다 — 감지와 고침이 어긋난다\n%s", changed, c.wantWarn, fixBuf.String())
			}
		})
	}
}
```

- [ ] **Step 3: 실패를 확인한다**

Run:
```bash
go test -p 1 -run TestDoctorDetectFixEquivalence ./internal/cli/ -v
```
Expected: ⑦~⑫가 FAIL(새 라벨이 없다). ⑦은 `codex=미등록`을 기대하지만 현재 코드는 `codexConfigMarker`의 `found=false`로 이미 "미등록"을 내므로 통과할 수 있다 — 그 경우 경고 단정으로 갈린다.

- [ ] **Step 4: `[20]`의 Codex 라벨을 판정에서 도출한다**

`internal/cli/cli.go`의 `[20]` 절에서 Codex 갈래만 바꾼다. 기존(현재 2000–2004행):
```go
	codexLabel, codexDrift := "확인불가", false
	if cfgP, cfgErr := codexConfigPath(); cfgErr == nil {
		cfgData, cfgReadErr := os.ReadFile(cfgP)
		codexLabel, codexDrift = markerState(cfgData, cfgReadErr, codexConfigMarker)
	}
```
새로:
```go
	codexLabel, codexDrift := "확인불가", false
	codexReason := ""
	if cfgP, cfgErr := codexConfigPath(); cfgErr == nil {
		cfgData, cfgReadErr := os.ReadFile(cfgP)
		codexLabel, codexDrift, codexReason = codexMarkerLabel(cfgData, cfgReadErr, version)
	}
```

라벨 줄(현재 2005행) **바로 뒤**에 사유 줄을 낸다. 스펙 §0 D85가 `[16]`·`[20]`·설치기 **셋 모두** 사유를 인쇄하도록 정했다:
```go
	fmt.Fprintf(w, "[20] mcp markers: .mcp.json=%s codex=%s\n", mcpLabel, codexLabel)
	if codexReason != "" {
		// 이상 라벨은 세 사유를 한 값으로 묶으므로 사유를 함께 낸다 — 사유마다 필요한 조치가
		// 다르고, 그것을 알리지 않으면 사용자는 install이 영구히 무변경인 이유를 알 수 없다.
		fmt.Fprintf(w, "[20] codex: %s\n", codexReason)
	}
```

그리고 `markerState` 클로저 **뒤에** 새 함수를 파일 수준으로 둔다:
```go
// codexMarkerLabel — [20]의 Codex 라벨·권고 여부·사유(D85). **권고는 codexVerdict.shouldFix()가
// 정한다** — 라벨은 진단 정보라 자유도가 있지만 권고는 --fix의 행동과 등가여야 한다.
// 라벨 셋이 추가된다: 구간 밖 충돌은 충돌, 구간 판정 불가는 이상, 소유하지만
// ownedRegistration이 거짓인 인수 대상은 구형식이다. 마지막 술어가 여전히 필요한 이유는 그
// 거짓이 **인수 경로임을 가리키는 유일한 신호**라는 것이다 — 판정 권한을 나눠 갖는 것이
// 아니라 라벨을 구별할 뿐이다.
// 셋째 반환값은 사유이며 이상 갈래에서만 비어 있지 않다 — 그 라벨이 세 사유를 한 값으로
// 묶으므로 호출자가 별도 줄로 낸다.
func codexMarkerLabel(data []byte, readErr error, version string) (label string, fix bool, reason string) {
	switch {
	case errors.Is(readErr, os.ErrNotExist):
		return "없음", false, ""
	case readErr != nil:
		return "읽기실패", false, ""
	}
	v := codexRegistrationVerdict(data, version)
	shouldFix := v.shouldFix()
	switch v.State {
	case mcpMarkerAnomaly:
		return "이상", false, v.Anomaly.reason()
	case mcpConflict:
		return "충돌", false, ""
	case mcpExistingHeader:
		return "미등록", false, ""
	}
	if !v.TableFound {
		// **권고값을 여기서 고정하지 않는다.** shouldFix가 이미 TableFound를 보므로 거짓이
		// 나오고, false를 하드코딩하면 판정원이 둘이 되어 shouldFix의 결함이 이 갈래에서
		// 가려진다 — 그 절을 되돌리는 유도 FAIL이 물지 않게 된다.
		return "미등록", shouldFix, "" // append하면 되지만 --fix는 등록을 만들지 않는다
	}
	if !ownedRegistration(v.Marker, v.Command, true) {
		return "구형식", shouldFix, "" // D84 셋째 절이 소유로 받는 인수 대상
	}
	if !isOurMarkerValue(v.Marker) {
		return "표식없음", shouldFix, ""
	}
	// 버전 불일치 접미는 **버전 비교**로 붙인다. shouldFix로 붙이면 표식이 현재 버전인데 형식
	// 드리프트 때문에 기입이 필요한 파일(구 블록 안의 현재 버전 표식)에서 같은 버전을 좌우에 둔
	// 표기가 나온다. 형식 드리프트는 별도 접미로 구별한다.
	// early return으로 적는다 — if-else 체인은 revive의 indent-error-flow에 걸린다.
	mv := markerVersion(v.Marker)
	if mv == "" {
		return "marker 버전미상≠" + version, shouldFix, ""
	}
	if mv != version {
		return "marker " + mv + "≠" + version, shouldFix, ""
	}
	if shouldFix {
		return "marker " + mv + "(형식)", true, ""
	}
	return "marker " + mv, false, ""
}
```

**`v.Anomaly`를 여기서 쓰는 것이 그 필드의 유일한 소비자다** — 쓰지 않으면 `codexVerdict`에 죽은 필드가 남고 `unused` 계열 린터에 걸린다.

**`markerState` 클로저는 `.mcp.json` 갈래에만 남는다** — 그쪽 판정 구조는 바꾸지 않는다(스펙 §0 D85).

- [ ] **Step 5: 경고 문면을 사유 중립으로 바꾼다**

기존(현재 2006–2008행):
```go
	if mcpDrift || codexDrift {
		fmt.Fprintln(w, "[20] warning: MCP 등록물의 버전 표식이 현재 버전과 다릅니다 — doctor --fix로 다시 기입하세요(기존 파일만 고치며 config.toml은 백업을 남깁니다)")
	}
```
새로:
```go
	if mcpDrift || codexDrift {
		// 문면을 사유 중립으로 둔다 — 표식은 현재인데 형식·command가 어긋나 --fix가 파일을
		// 바꾸는 상태(구형식 포함)에서 "버전 표식이 다르다"는 사유를 잘못 말한다. 사유는
		// 라벨이 담는다. .mcp.json 갈래는 은퇴 이름 항목도 함께 정리하므로(P2) 그것을 예고한다.
		fmt.Fprintln(w, "[20] warning: 다시 기입이 필요한 MCP 등록물이 있습니다 — doctor --fix로 고치세요(기존 파일만 고치고 등록을 만들지 않습니다. config.toml은 백업을 남기고 args·enabled_tools는 보존합니다. .mcp.json은 대체된 옛 이름의 항목을 함께 정리합니다)")
	}
```

- [ ] **Step 5b: `.mcp.json` 갈래의 `--fix` 보고 문면도 맞춘다**

Step 5의 경고가 `.mcp.json`의 은퇴 이름 정리를 예고하므로 그 갈래의 보고 한 줄도 그것을 적어야
한다 — 예고만 있고 보고가 없으면 사용자는 무엇이 일어났는지 확인할 수 없다. `cli.go`의
`doctorFixRegistrations` `.mcp.json` 갈래:
```go
			} else {
				fmt.Fprintf(w, "[20] fix: .mcp.json 표식을 %s로 다시 기입했습니다(프로필은 보존, 대체된 옛 이름의 항목이 있으면 함께 정리했습니다)\n", version)
			}
```

- [ ] **Step 6: `ownedRegistration`의 주석 주장을 좁힌다**

`internal/cli/hook_install.go`의 `ownedRegistration` 주석 마지막 문장을 바꾼다. 기존:
```go
// [20]의 "미등록" 표시가 이 술어의 거짓과 같은 조건이라 감지와 고침의 대상이 일치한다.
```
새로:
```go
// **이 주장은 .mcp.json 갈래 한정이다**: 그쪽은 [20]의 "미등록"이 이 술어의 거짓과 같은
// 조건이다. Codex 갈래에서는 D84의 구 블록 절이 이 술어에 없어 거짓이어도 --fix가 인수하므로,
// 그쪽 권고는 codexVerdict.shouldFix()가 정하고 이 술어는 구형식 라벨 구별에만 쓴다(D85).
```

- [ ] **Step 7: 기존 `TestDoctorMCPMarkerLine`이 여전히 통과하는지 확인한다 (게이트 2)**

Run:
```bash
go test -p 1 -run 'TestDoctorMCPMarkerLine|TestDoctorDetectFixEquivalence|TestDoctorFix|TestRunDoctorFixFlag|TestDoctorVersionlessHookMarker' ./internal/cli/ -v
```
Expected: **전부 PASS.** 기존 여섯 케이스의 결과가 바뀌면 그것은 개선이 아니라 회귀다(스펙 §3 표2). 경고 문면을 축자로 잡은 단정이 있으면 새 문면으로 고치되 **라벨과 경고 유무는 그대로여야 한다**.

- [ ] **Step 8: 유도 FAIL — 두 방향**

① **`shouldFix()`에서 `v.TableFound &&`를 지운다**:
Expected: **⑦ "파일 존재·관리 테이블 부재"가 FAIL** — `[20]`이 경고를 내는데 `--fix`는 바꾸지
않는다. **이 유도 FAIL이 물리는 것은 `codexMarkerLabel`의 테이블 부재 갈래가 권고값을 고정하지
않고 `shouldFix`를 그대로 돌려주기 때문이다** — 거기에 `false`를 하드코딩하면 판정원이 둘이 되어
이 검사가 죽는다(계획 초판이 그 상태였다).

② **`codexMarkerLabel`의 `ownedRegistration` 갈래를 지운다**:
Expected: **⑧ "구 블록 + 비표준 command"가 FAIL** — 라벨이 `구형식`이 아니다.

③ **버전 접미를 `shouldFix`로 되돌린다**(`if mv != version` 갈래를 `if shouldFix`로):
Expected: 구 블록 안에 **현재 버전** 표식과 우리 `command`가 있는 픽스처에서 라벨이 같은 버전을
좌우에 둔 표기로 나온다. 그 픽스처가 표에 없으면 이 방향은 건너뛰고 그 사실을 적는다.

각 확인 후 복구한다.

- [ ] **Step 9: 전역 제약 확인 후 커밋**

```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
git add internal/cli/cli.go internal/cli/hook_install.go internal/cli/cli_test.go
git status --short
git commit -m "$(cat <<'EOF'
feat(v0.16): [20]의 라벨과 권고를 판정에서 도출한다 (D85)

Codex 라벨을 codexRegistrationVerdict에서 도출하고 권고를 shouldFix()에
묶는다. 라벨 셋을 더했다 — 구형식(D84 인수 대상)·충돌(구간 밖 중복 정의)·
이상(구간 판정 불가, 사유는 [16]이 인쇄한다). 기존 여섯 케이스의 결과는
바뀌지 않는다.

경고 문면을 사유 중립으로 바꿨다 — 표식은 현재인데 형식·command가 어긋나
--fix가 파일을 바꾸는 상태에서 "버전 표식이 다르다"는 사유를 잘못 말했다.
.mcp.json 갈래의 은퇴 이름 정리도 예고한다(P2).

ownedRegistration의 "감지와 고침의 대상이 일치한다" 주장을 .mcp.json 갈래
한정으로 좁혔다 — Codex 갈래에서는 D84의 구 블록 절이 그 술어에 없다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: D88 — 키 줄 후행 주석 계약 (C3)

**Files:**
- Modify: `internal/cli/codex_toml.go` (파일 헤더 주석, `codexTableBody` 주석)
- Modify: `docs/context-router-design-v0.15-ko.md` (D80 절에 예외 참조 한 줄)

**Interfaces:** 없음(계약 문면 전용, 동작 변경 없음).

**무엇을 닫는가**: D80의 왕복 보존은 **키 단위** 약속이고 관리 키 넷의 물리 라인은 통째로 재생성되므로 그 줄의 후행 주석은 사라진다. 계약 위반은 아니지만 **미명시 손실**이었다. 코드로 닫으려면 키별 값 스팬 판독기가 필요해 범위가 크고, D80의 파서 비의존 원칙 아래에서 그 판독기의 정확성이 새 위험이 된다.

- [ ] **Step 1: 파일 헤더 주석에 예외를 적는다**

`internal/cli/codex_toml.go`의 파일 헤더(현재 3–8행) 끝에 한 문단을 더한다:
```go
// D88 — 왕복 보존의 예외: **우리가 기입하는 키 줄의 후행 주석은 보존 대상이 아니다.** 보존은
// 키 단위 약속이고 관리 키 넷의 물리 라인은 통째로 재생성되므로 그 줄의 주석은 사라진다.
// 인라인 env 경로는 예외의 예외다 — inlineMarkerSpan·setInlineEnvMarker가 값 토큰만 치환하므로
// 그 줄의 주석은 남는다. 키별 값 스팬 판독기로 닫지 않는 이유는 파서 비의존 원칙 아래에서
// 그 판독기의 정확성이 새 위험이 되기 때문이다.
```

- [ ] **Step 2: `codexTableBody` 주석에 같은 사실을 적는다**

그 줄을 실제로 재생성하는 지점이므로 계약을 그 자리에 남긴다. 기존 주석 끝에 더한다:
```go
// D88 — 이 재생성이 관리 키 줄의 후행 주석을 지운다. 보존 대상이 아니라는 것이 계약이며,
// 보존 라인(keep)의 주석은 원문 그대로 옮겨진다.
```

- [ ] **Step 3: v0.15 설계서 D80 절에 참조를 남긴다**

`docs/context-router-design-v0.15-ko.md`에서 D80의 왕복 보존을 적은 항(§0 D80 안, "그 밖의 바이트 보존" 또는 "왕복 보존"을 언급하는 줄)을 찾는다:
```bash
grep -n '왕복 보존' docs/context-router-design-v0.15-ko.md
```
그 항의 끝에 한 줄을 더한다(살아 있는 계약 문서는 제자리 개정이 규칙이다):
```markdown
    (**v0.16 D88** — 이 보존의 예외: 우리가 기입하는 키 줄의 후행 주석은 보존 대상이
    아니다. 인라인 `env` 경로는 값 토큰만 치환하므로 그 줄의 주석은 남는다.)
```

- [ ] **Step 4: 동작 변경이 없음을 확인한다**

Run:
```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
```
Expected: 전부 통과(주석·문서만 바뀌었다).

- [ ] **Step 5: 커밋**

```bash
git add internal/cli/codex_toml.go docs/context-router-design-v0.15-ko.md
git status --short
git commit -m "$(cat <<'EOF'
docs(v0.16): D88 키 줄 후행 주석은 보존 대상이 아니다 (C3)

D80의 왕복 보존은 키 단위 약속이고 관리 키 넷의 물리 라인은 통째로
재생성되므로 그 줄의 후행 주석이 사라진다 — 계약 위반은 아니지만 미명시
손실이었다. 인라인 env 경로는 값 토큰만 치환하므로 그 줄의 주석은 남는다.

키별 값 스팬 판독기로 닫지 않는다: 파서 비의존 원칙 아래에서 그 판독기의
정확성이 새 위험이 된다. 계약을 명시하는 것이 정직한 닫기다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: X6 — 구 형식 마이그레이션의 배선 테스트

**Files:**
- Test: `internal/cli/hook_install_test.go` (`runHookInstallCodex` 배선)
- Test: `internal/cli/cli_test.go` (`runDoctor --fix` 배선)

**Interfaces:** 없음(테스트 전용). T1~T6의 동작을 실제 파일 경로에서 잰다.

**왜 값이 높은가**: v0.14 구 형식(BEGIN/END) 파일은 **순수 함수 수준에서만** 픽스처를 갖는다(`codex_toml_test.go`). `runHookInstallCodex`·`runDoctor --fix`가 그 형식을 읽고 쓰는 **배선은 테스트에 없고**, D80의 인수 경로가 실사용에서 처음 도는 릴리스다.

- [ ] **Step 1: `runHookInstallCodex` 배선 테스트를 쓴다**

`internal/cli/hook_install_test.go`에 추가한다:

```go
// TestRunHookInstallCodexMigratesOldFormat — X6(§2-9). v0.14 구 형식 파일을 **실제 파일로** 놓고
// 설치 배선이 1회 변환하는지, 재실행이 무변경인지, 백업이 단일 슬롯인지 본다. 지금까지 구
// 형식 픽스처는 순수 함수 테스트에만 있었고 이 배선은 테스트에 없었다.
func TestRunHookInstallCodexMigratesOldFormat(t *testing.T) {
	codexHome := t.TempDir()
	t.Setenv("CODEX_HOME", codexHome)
	cfgPath := filepath.Join(codexHome, "config.toml")
	old := codexBlockBegin + "\n" +
		"[mcp_servers.ctr]\n" +
		"command = \"context-router\"\n" +
		"args = [\"--enable\", \"ingest,net\"]\n" +
		codexBlockEnd + "\n"
	write(t, cfgPath, []byte(old))

	var out bytes.Buffer
	if err := runHookInstallCodex(true, false, false, "", t.TempDir(), "0.16.0", nil, false, &out); err != nil {
		t.Fatalf("1회차: %v out=%s", err, out.String())
	}
	after1, err := os.ReadFile(cfgPath)
	if err != nil {
		t.Fatal(err)
	}
	got := string(after1)
	// 마커 두 줄이 사라진다
	if strings.Contains(got, codexBlockBegin) || strings.Contains(got, codexBlockEnd) {
		t.Errorf("마커가 남아 있다:\n%s", got)
	}
	// 표식이 env로 옮겨진다
	if !strings.Contains(got, codexMarkerKey+" = \""+hookMarker("0.16.0")+"\"") {
		t.Errorf("표식이 기입되지 않았다:\n%s", got)
	}
	// 백업이 원본을 담는다
	bak, bErr := os.ReadFile(cfgPath + ".bak")
	if bErr != nil {
		t.Fatalf(".bak이 없다: %v", bErr)
	}
	if string(bak) != old {
		t.Errorf(".bak이 원본과 다르다:\n%s", bak)
	}

	// 2회차 — 무변경이고 .bak은 1회차 이전 원본을 그대로 담는다.
	// (backupCodexConfig는 단일 슬롯을 덮어쓰므로 ".bak이 다시 생기지 않는지"는 존재 검사로
	//  관측할 수 없다 — 바이트로 잰다. §2-5)
	var out2 bytes.Buffer
	if err := runHookInstallCodex(true, false, false, "", t.TempDir(), "0.16.0", nil, false, &out2); err != nil {
		t.Fatalf("2회차: %v", err)
	}
	after2, err := os.ReadFile(cfgPath)
	if err != nil {
		t.Fatal(err)
	}
	if string(after2) != got {
		t.Errorf("2회차가 파일을 바꿨다:\n%s", after2)
	}
	bak2, _ := os.ReadFile(cfgPath + ".bak")
	if string(bak2) != old {
		t.Errorf(".bak이 2회차에 덮였다 — 무변경이면 백업하지 않아야 한다:\n%s", bak2)
	}
}
```

- [ ] **Step 2: `runDoctor --fix` 배선 테스트를 쓴다**

`internal/cli/cli_test.go`에 추가한다:

```go
// TestDoctorFixMigratesOldFormat — X6(§2-9). --fix가 v0.14 구 형식을 실제 파일에서 1회
// 변환하는지, args가 보존되는지(D86), 재실행이 무변경이고 .bak이 원본을 유지하는지.
func TestDoctorFixMigratesOldFormat(t *testing.T) {
	codexHome := t.TempDir()
	t.Setenv("CODEX_HOME", codexHome)
	cfgPath := filepath.Join(codexHome, "config.toml")
	old := codexBlockBegin + "\n" +
		"[mcp_servers.ctr]\n" +
		"command = \"context-router\"\n" +
		"args = [\"--enable\", \"ingest\"]\n" +
		"enabled_tools = [\"ctr_search\", \"ctr_index\", \"ctr_execute\"]\n" +
		codexBlockEnd + "\n"
	write(t, cfgPath, []byte(old))
	projectRoot := t.TempDir()

	var buf bytes.Buffer
	if err := runDoctor(context.Background(), &buf, t.TempDir(), projectRoot, "0.16.0", true); err != nil {
		t.Fatalf("--fix: %v out=%s", err, buf.String())
	}
	after1, err := os.ReadFile(cfgPath)
	if err != nil {
		t.Fatal(err)
	}
	got := string(after1)
	if strings.Contains(got, codexBlockBegin) || strings.Contains(got, codexBlockEnd) {
		t.Errorf("마커가 남아 있다:\n%s", got)
	}
	if !strings.Contains(got, codexMarkerKey+" = \""+hookMarker("0.16.0")+"\"") {
		t.Errorf("표식이 기입되지 않았다:\n%s", got)
	}
	// D86 — 사용자가 넓힌 enabled_tools가 보존된다(프로필 ingest는 ctr_execute를 켜지 않는다)
	if !strings.Contains(got, "ctr_execute") {
		t.Errorf("--fix가 사용자 확장을 지웠다:\n%s", got)
	}
	if !strings.Contains(got, "args = [\"--enable\", \"ingest\"]") {
		t.Errorf("--fix가 args를 바꿨다:\n%s", got)
	}
	bak, bErr := os.ReadFile(cfgPath + ".bak")
	if bErr != nil || string(bak) != old {
		t.Errorf(".bak이 원본을 담지 않았다: err=%v\n%s", bErr, bak)
	}

	// 2회차 무변경 + .bak 유지
	var buf2 bytes.Buffer
	if err := runDoctor(context.Background(), &buf2, t.TempDir(), projectRoot, "0.16.0", true); err != nil {
		t.Fatalf("--fix 2회차: %v", err)
	}
	after2, _ := os.ReadFile(cfgPath)
	if string(after2) != got {
		t.Errorf("2회차가 파일을 바꿨다:\n%s", after2)
	}
	if !strings.Contains(buf2.String(), "이미 현재 형식·버전입니다") {
		t.Errorf("2회차가 무변경을 보고하지 않았다:\n%s", buf2.String())
	}
	// 스펙 §2-4 — 보고 문면이 무엇을 맞추고 무엇을 보존했는지 알리는지. 1회차 출력을 본다.
	for _, want := range []string{"표식과 command", "원문 보존"} {
		if !strings.Contains(buf.String(), want) {
			t.Errorf("--fix 보고에 %q 없음:\n%s", want, buf.String())
		}
	}
	bak2, _ := os.ReadFile(cfgPath + ".bak")
	if string(bak2) != old {
		t.Errorf(".bak이 2회차에 덮였다:\n%s", bak2)
	}
}
```

- [ ] **Step 3: 두 테스트를 돈다**

Run:
```bash
go test -p 1 -run 'TestRunHookInstallCodexMigratesOldFormat|TestDoctorFixMigratesOldFormat' ./internal/cli/ -v
```
Expected: PASS. **FAIL하면 그것은 테스트의 결함이 아니라 배선의 결함일 수 있다** — 순수 함수는 T1~T6에서 통과했으므로, 실패 지점이 IO·경로·순서 중 무엇인지 규명하고 컨트롤러에 보고한다.

- [ ] **Step 4: 유도 FAIL**

`doctorFixRegistrations`의 Codex 갈래에서 `MarkerOnly: true`를 지운다(두 곳: 판정 함수와 기입).

Run:
```bash
go test -p 1 -run TestDoctorFixMigratesOldFormat ./internal/cli/
```
Expected: **FAIL** — `--fix가 사용자 확장을 지웠다`. 복구한다.

- [ ] **Step 5: 전역 제약 확인 후 커밋**

```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
git add internal/cli/hook_install_test.go internal/cli/cli_test.go
git status --short
git commit -m "$(cat <<'EOF'
test(v0.16): X6 구 형식 마이그레이션의 배선을 잰다

구 형식(BEGIN/END) 픽스처가 순수 함수 테스트에만 있었고 runHookInstallCodex·
runDoctor --fix가 그 형식을 읽고 쓰는 배선은 테스트에 없었다. D80의 인수
경로가 실사용에서 처음 도는 릴리스다.

.bak 단정은 존재가 아니라 바이트로 잰다 — backupCodexConfig가 단일 슬롯을
덮어쓰므로 "다시 생기지 않는지"는 존재 검사로 관측할 수 없다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: 표 기반 테스트를 `t.Run`으로 (위생) — **T1보다 먼저 실행한다**

> **실행 순서 주의**: 번호는 9이지만 **가장 먼저 돈다.** 이 태스크의 근거가 "유도 FAIL 때 케이스
> 경계가 출력에 남는다"인데, 유도 FAIL을 쓰는 태스크가 T1~T8 전부이므로 뒤에 두면 그 이득이 이
> 릴리스에서 실현되지 않는다. 제품 코드를 건드리지 않으므로 맨 앞에 두는 데 제약이 없다.
> (번호를 바꾸지 않는 이유는 문서 안의 태스크 참조가 깨지지 않게 하려는 것뿐이다.)

**Files:**
- Modify: `internal/cli/codex_toml_test.go` (케이스 순회가 `t.Run` 없이 도는 표 기반 테스트 전체)

**Interfaces:** 없음(테스트 구조 전용, 단정 내용은 바꾸지 않는다).

**대상 기준**(스펙 §1.1 6번 행): **케이스 순회가 `t.Run` 없이 도는 표 기반 테스트**다. 실패 함수가 `Fatalf`인지 `Errorf`인지로 가르지 않는다 — 이 파일에는 `t.Run`이 하나도 없어 유도 FAIL 때 어느 쪽이든 케이스 경계가 출력에 남지 않는다.

- [ ] **Step 1: 대상을 실측으로 확정한다**

```bash
grep -nE 'for _, [a-z]+ := range|t\.Run\(' internal/cli/codex_toml_test.go
```
Expected: `t.Run(`은 이 파일에 **없다**(T9를 먼저 돌므로 T2·T3의 새 테스트도 아직 없다).
`cases` 표를 도는 곳이 네 곳이다 — `TestInstallCodexConfigBlock` · `TestUninstallCodexConfigBlock` ·
`TestProbeCodexMCPBlock` · `TestCodexTableOwnershipAndAdoption`.

**`TestCodexConfigBlockRoundTrip`은 대상에 포함한다.** 그 테스트는 `cases` 표가 아니라 문자열
슬라이스를 순회하지만 `t.Run` 없이 `t.Fatalf`를 쓰므로 같은 문제를 갖는다 — 기준은 표의 형태가
아니라 **케이스 순회가 `t.Run` 없이 돈다**는 것이다. 서브테스트 이름은 입력 문자열을 그대로 쓰지
말고 인덱스나 짧은 설명으로 붙인다(입력에 개행이 있다).

**실측 결과가 위와 다르면 그 목록을 따르고 차이를 커밋 메시지에 적는다.**

- [ ] **Step 2: 네 순회를 `t.Run`으로 감싼다**

각 테스트에서 순회 본문을 그대로 클로저 안으로 옮긴다. 패턴(예: `TestProbeCodexMCPBlock`):

기존(입력 필드 이름은 **실제 파일에서 확인한 뒤** 쓴다 — `TestProbeCodexMCPBlock`은 `in`이다):
```go
	for _, c := range cases {
		p, a := probeCodexMCPBlock([]byte(c.in))
		if p != c.present || a != c.anomaly {
			t.Errorf("%s: present=%v anomaly=%v want %v/%v", c.name, p, a, c.present, c.anomaly)
		}
	}
```
새로:
```go
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			p, a := probeCodexMCPBlock([]byte(c.in))
			if p != c.present || a != c.anomaly {
				t.Errorf("present=%v anomaly=%v want %v/%v", p, a, c.present, c.anomaly)
			}
		})
	}
```

**두 가지를 지킨다**: ① 메시지에서 `c.name` 접두를 뺀다(서브테스트 이름이 이미 담는다) ② `t.Fatalf`를 쓰던 곳은 그대로 `t.Fatalf`로 둔다 — 서브테스트 안에서는 그 케이스만 중단되므로 뒤 케이스가 가려지지 않는다(이것이 전환의 목적이다).

- [ ] **Step 3: 케이스 이름이 서브테스트 이름으로 쓸 수 있는지 확인한다**

Go는 서브테스트 이름의 공백을 밑줄로 바꾸고 `/`를 계층 구분으로 읽는다:
```bash
grep -n '{".*/.*",' internal/cli/codex_toml_test.go
```
Expected: **여섯 줄 정도가 나오지만 모두 픽스처·인자 문자열의 슬래시이고 케이스 이름에는 없다**
(예: 마커 값 `context-router/0.15.0`). **그 줄들을 고치지 않는다** — 케이스 이름 필드에 실제로
`/`가 있는 경우에만 그 이름을 `-`로 바꾼다.

- [ ] **Step 4: 전환 뒤에도 같은 것을 잰다**

Run:
```bash
go test -p 1 -run 'TestInstallCodexConfigBlock|TestUninstallCodexConfigBlock|TestProbeCodexMCPBlock|TestCodexTableOwnershipAndAdoption' ./internal/cli/ -v
```
Expected: 모든 케이스가 **서브테스트로 개별 표시**되고 전부 PASS. 케이스 수가 전환 전과 같아야 한다.

- [ ] **Step 5: 유도 FAIL — 전환의 목적을 실증한다**

`TestInstallCodexConfigBlock`의 **첫 케이스**가 실패하도록 기대값을 일시 변경한다.

Run:
```bash
go test -p 1 -run TestInstallCodexConfigBlock ./internal/cli/ -v
```
Expected: **첫 서브테스트만 FAIL하고 나머지 케이스의 PASS가 출력에 남는다.** 전환 전에는 `Fatalf`가 첫 실패에서 테스트를 끝내 뒤 케이스가 보이지 않았다. 확인 후 복구한다.

- [ ] **Step 6: 전역 제약 확인 후 커밋**

```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
git add internal/cli/codex_toml_test.go
git status --short
git commit -m "$(cat <<'EOF'
test(v0.16): codex_toml_test의 표 기반 테스트를 t.Run으로

이 파일에는 t.Run이 하나도 없어 유도 FAIL 때 첫 실패가 뒤 케이스를 가렸다.
이 릴리스의 핵심 게이트가 유도 FAIL이므로 케이스 경계가 출력에 남아야 한다.
단정 내용은 바꾸지 않았다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: X7 — `[0.15.0]` CHANGELOG 누락 전수 보완

**Files:**
- Modify: `CHANGELOG.md` (`[0.15.0]` 절 보완 + `[0.16.0]` 절 신설)

**Interfaces:** 없음(문서 전용).

**왜 전수인가**: 두 리뷰가 각각 다른 누락을 찾았다(셋 + 하나). 세는 방식이 누락을 남긴다는 것이 이 항목의 교훈이므로 **0.15.0 범위 커밋의 사용자 관측 문면 변경을 전수로 훑는다**.

- [ ] **Step 1: 0.15.0 범위의 사용자 관측 변경을 뽑는다**

```bash
git log --oneline v0.14.0..v0.15.0
```
그 범위에서 **사용자에게 보이는 문면**이 바뀐 지점을 찾는다. **출력 함수만 잡으면 안 된다** —
확인된 누락 하나(`hostSnippet`)가 원시 문자열 **상수**라 그 패턴에 걸리지 않는다. 추가된 문자열
리터럴을 전수로 본다:
```bash
# 추가된 줄 중 한글 문면을 담은 것 전체(내부 식별자·주석 제외는 눈으로 가른다)
git diff v0.14.0..v0.15.0 -- internal/ cmd/ | grep -nE '^\+' | grep -P '[가-힣]'
```
`head` 제한을 두지 않는다 — 자르면 전수성이 깨진다. 출력이 길면 파일로 받아 훑는다.

**상수 스니펫 블록은 따로 대조한다** — 그 블록은 한 줄 diff로 나오지 않는다:
```bash
git diff v0.14.0..v0.15.0 -- internal/cli/cli.go | grep -nE '^[+-].*hostSnippet' 
git diff v0.14.0..v0.15.0 -- internal/cli/cli.go | sed -n '/hostSnippet/,/^`/p' | head -80
```

각 문면 변경이 `[0.15.0]` 절에 반영됐는지 대조하고, **훑은 커밋 수와 찾은 문면 변경 수를 커밋
메시지에 적는다** — 전수를 돌았다는 것이 그 숫자로 확인된다.

- [ ] **Step 2: 확인된 누락 넷을 먼저 적는다**

리뷰가 지목한 넷은 반드시 포함한다:
1. 호스트 등록 안내 변경이 **Codex 절만** 적혀 있다 — `hostSnippet`의 **Claude Code 절도** 바뀌었다.
2. `doctor`의 미지 플래그 오류 문면이 바뀌었다(`예상치 않은 인자` → `인식할 수 없는 플래그(사용 가능: --fix)`).
3. **P4 안내** 추가(빈 프로필 등록물에 명시 경로를 알린다).
4. **비대화형 승인 프롬프트 경고** 문면 두 곳(설치기 · `doctor` 스니펫).

`[0.15.0]`의 `### Changed` 절에 항목으로 더한다. **릴리스된 절을 직접 보완한다** — Keep a Changelog는 오류 정정을 금지하지 않으며, v0.16 절에만 적으면 0.15.0을 읽는 사람이 그 사실을 못 본다.

- [ ] **Step 3: Step 1이 찾은 추가 누락을 반영한다**

전수 훑기에서 나온 것을 같은 절에 더한다. 없으면 "넷 외 추가 누락 없음"을 커밋 메시지에 적는다.

- [ ] **Step 4: `[0.16.0]` 절을 신설한다**

```markdown
## [0.16.0] — 2026-07-30

### Changed — 사용자 관측 가능한 동작 변경

- **`doctor [20]`의 경고가 `doctor --fix`의 행동과 일치한다** (D85). 경고는 `--fix`가 실제로
  파일을 바꾸는 상태에서만 나온다 — 관리 테이블이 없는 파일에는 경고하지 않고(등록을 만들지
  않으므로) 구간 밖 충돌·구간 판정 불가 상태에서도 `--fix`를 권하지 않는다.
  - **라벨 셋이 늘었다**: `구형식`(구 `# BEGIN/END` 블록 안에서 `command`를 직접 고쳐 둔
    등록물 — `--fix`가 마이그레이션한다) · `충돌`(우리 두 구간 밖의 중복 정의 신호) ·
    `이상`(구간 판정 불가). **`이상`은 사유를 함께 인쇄한다** — 중복 헤더인지, 닫히지 않은
    여러 줄 값인지, 이스케이프 표기 키인지가 `[16]`·설치기 안내에 나온다.
  - 경고 문면이 사유 중립으로 바뀌었다 — 표식은 현재인데 형식이 어긋난 상태에서 "버전 표식이
    다르다"고 말하지 않는다.
- **`doctor --fix`가 `enabled_tools`와 `args`를 보존한다** (D86). 손으로 넓힌 도구 목록이
  프로필 기본값으로 줄어들지 않는다. `--fix`가 맞추는 것은 **소유 표식과 `command`**이며,
  `command`를 절대경로나 래퍼로 고쳐 두었다면 그 값은 우리 값으로 되돌아간다.
  - `--fix`의 안내가 `.mcp.json`에서 **대체된 옛 이름의 항목을 함께 정리한다**는 사실을
    예고한다.
- **관리 테이블 안의 키가 이스케이프 표기로 적혀 있으면 `config.toml`을 손대지 않는다**
  (D87). TOML은 그 표기를 보통 표기와 같은 키로 읽는데 우리는 이스케이프를 해석하지 않으므로,
  기입하면 같은 키가 두 번 정의된 파일이 된다. `doctor`가 그 상태를 사유와 함께 알린다.
  값에 든 역슬래시(Windows 경로 등)는 이 판정의 대상이 아니다.

### Internal

- `doctor [20]`과 `doctor --fix`가 요청 조립까지 공유하는 한 판정을 쓴다 — 감지와 고침이 다른
  판독기에서 나오던 구조를 닫았다(D85).
- 구간 판정 실패가 불리언에서 사유 열거형으로 올라갔다(D85·D87, 이월 X3).
- **키 줄의 후행 주석은 왕복 보존 대상이 아니다**를 계약으로 명시했다(D88). 인라인 `env`
  경로는 값 토큰만 치환하므로 그 줄의 주석은 남는다.
- v0.14 구 형식 마이그레이션의 **배선**(`hook install --codex` · `doctor --fix`)에 테스트가
  생겼다 — 그전까지 구 형식 픽스처는 순수 함수 수준에만 있었다.
- `[0.15.0]` 절에서 빠져 있던 사용자 관측 문면 변경을 보완했다.
```

**`### Changed` 절의 D86 항목에 한 줄을 더한다** — 보존의 반대 방향을 함께 알린다:

```markdown
  - `--fix`는 등록물의 도구 목록을 **늘리지도 않는다.** 릴리스가 프로필에 새 도구를 더하면
    그 반영은 `hook install --codex`가 한다.
```

- [ ] **Step 5: 문서만 바뀌었음을 확인하고 커밋**

```bash
go build ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
git add CHANGELOG.md
git status --short
git commit -m "$(cat <<'EOF'
docs(v0.16): X7 CHANGELOG — 0.15.0 누락 보완(전수)과 0.16.0 절

0.15.0 절에서 빠진 사용자 관측 문면 변경을 보완했다. 두 리뷰가 각각 다른
누락을 찾았으므로 셋으로 닫지 않고 v0.14.0..v0.15.0 범위의 문면 변경을
전수로 훑었다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: X4와 줄 번호 인용 전수 정리

**Files:**
- Modify: `internal/mcp/mcp_test.go` (헬퍼 주석)
- Modify: `internal/cli/cli_test.go` · `internal/cli/compare.go` (그리고 Step 1이 찾는 나머지)

**Interfaces:** 없음(주석 전용).

**왜 전수인가**: v0.15가 `internal/exec`에서 같은 정리를 했는데 **다른 파일에서 재발**했다. 파일 하나가 아니라 부류를 닫는다.

- [ ] **Step 1: 어긋난 인용을 전수로 찾는다**

```bash
grep -rnoE '[a-z_]+\.go:[0-9]+' internal/cli/ internal/mcp/ --include=*.go
```
각 인용에 대해 그 줄에 실제로 그 심볼이 있는지 확인한다. **확인된 것은 아래 넷이고 실측이 더
많을 수 있다** — Step 5의 스테이징 목록을 이 실측 결과에 맞춘다(아래 목록으로 고정하지 않는다):
- `internal/cli/cli_test.go`의 `runDoctor(cli.go:1491)` — v0.15 Task 11이 옮긴 뒤 stale
- `internal/cli/cli_test.go:633`의 `cli.go:1830` — 실제 1854–1855
- `internal/cli/compare.go:57`의 `cli.go:367`
- `internal/cli/mcp_install_test.go:339`의 `hook_install.go:125`·`:129` — 실제 171·180

- [ ] **Step 2: 심볼 인용으로 바꾼다**

각 인용을 함수·상수 이름으로 바꾼다. 예:
```
- // ... runDoctor(cli.go:1491)가 ...
+ // ... runDoctor가 ...
```
줄 번호가 정보를 담고 있던 경우(예: "그 줄 위에서")는 심볼 + 관계로 바꾼다("`runDoctor`의 `[16]` 절에서").

- [ ] **Step 3: X4 — `internal/mcp/mcp_test.go` 헬퍼 주석**

```bash
grep -n '8-도구\|8도구' internal/mcp/mcp_test.go
```
"8-도구 재기준화 경로"를 현재 대상(설치기가 만들 수 있는 최대 프로필 = `ingest`·`net`·`exec` **10도구**)으로 고친다.

- [ ] **Step 4: 정리가 완결됐는지 재확인**

Step 1의 명령을 다시 돌려 남은 줄 번호 인용이 **모두 정확한지** 확인한다. 정확한 것은 그대로 둔다(이 태스크는 어긋난 것만 고친다).

- [ ] **Step 5: 전역 제약 확인 후 커밋**

```bash
go build ./... && gofumpt -l . && golangci-lint run ./... && go test -p 1 ./internal/cli/... ./internal/mcp/...
git add internal/mcp/mcp_test.go internal/cli/cli_test.go internal/cli/compare.go
git status --short
git commit -m "$(cat <<'EOF'
docs(v0.16): X4와 어긋난 줄 번호 인용을 심볼 인용으로

v0.15가 internal/exec에서 같은 정리를 했는데 다른 파일에서 재발했다 —
파일 하나가 아니라 부류를 닫는다. mcp_test의 헬퍼 주석이 8도구로 남아
있던 것도 10도구로 고쳤다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## 완료 조건

모든 태스크가 끝난 뒤 다음이 모두 성립해야 한다.

- [ ] **전역 제약 전체 통과**: `go build ./...` · `gofumpt -l .` 무출력 · `golangci-lint run ./...` **0 issues** · `go test -p 1 ./...`에서 red가 **기준선 9건뿐**.
- [ ] **감지·고침 등가**: `TestDoctorDetectFixEquivalence`의 13상태가 모두 PASS. 이것이 이 릴리스의 핵심 단정이다.
- [ ] **기존 문면 무회귀**: `TestDoctorMCPMarkerLine`의 여섯 케이스 결과가 v0.15와 같다(스펙 §3 표2).
- [ ] **유도 FAIL 로그**: 새 검사마다 고침을 되돌려 FAIL을 확인한 기록이 남아 있다. 최소 지점 — T1 EOF 사유 · T2 키 토큰 한정과 인라인 검사 · T3 사유 문면 · T4 `keepArgs`·`ownedSame`·`Profiles` · T5 `shouldFix`의 테이블 실존 절 · T6 `shouldFix`와 `구형식` 라벨 · T8 `MarkerOnly` · T9 케이스 경계.
- [ ] **커밋 blob 바이트**: 이스케이프 표기를 담은 테스트 파일에서 역슬래시 개수를 **방향별로** 확인했다.
- [ ] **스펙 대조**: `docs/context-router-design-v0.16-ko.md`의 §2 열세 항이 각각 어느 테스트로 실현됐는지 대응이 있다.
- [ ] **버전 값은 이 계획의 범위가 아니다**: `internal/buildinfo/buildinfo.go`의 `productVersion`은
  0.15.0으로 남는다. 상향과 `CHANGELOG` `[0.16.0]` 절의 날짜 확정은 **릴리스 커밋 소관**이며
  v0.15 선례(구현 → 리뷰 → PR → 머지 → 태그)와 같다. T10이 그 절을 미리 쓰므로 **릴리스 전까지
  버전 값과 절 제목이 어긋난 상태로 남는다는 것을 알고 둔다** — 릴리스 시점에 함께 맞춘다.

## 실행 순서 요약

```
P0 브랜치 → P1 기준선 → P2 게이트1 → P3 게이트2
T9  t.Run 위생          ← 번호는 9이지만 **가장 먼저**(뒤 태스크의 유도 FAIL이 그 이득을 쓴다)
T1 열거형(X3) → T2 D87 → T3 사유 문면
T4 D86 → T5 D85 판정원 → T6 D85 문면
T7 D88 → T8 X6 배선
T10 X7 CHANGELOG → T11 X4·인용
```

**BASE는 P0에서 `git rev-parse HEAD`로 읽어 원장에 적은 값이고 `HEAD~1`을 쓰지 않는다.**
리뷰 체크포인트의 diff 기준은 그 값이다. (문서 머리의 `34d0f64`는 **스펙 커밋**이며 BASE가
아니다 — 계획 리뷰 반영으로 그 뒤에 커밋이 더 있다.)

**머지·태그·푸시는 사용자 명시 승인이 필요하다.**

