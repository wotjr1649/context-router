# Context Router v0.14 (D77–D79) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `.ps1` 스니펫의 네이티브 종료 신호를 `exit`로 끝나는 경로까지 완결시키고, 기동 purge의 실 잠금 보유 시간에 자동 게이트를 세우고, lint 미검출 집합을 비운다.

**Architecture:** 세 결정은 서로 독립이다. D78은 `internal/exec/exec.go`의 스크립트 조립만 바꾼다 — 스니펫 앞에 `Register-EngineEvent` 등록 문을 **같은 줄로 이어 붙이고**, 최상단 전용 구문으로 시작하는 스니펫은 미니 파서가 걸러 기존 형태로 폴백한다. D77은 테스트만 추가한다. D79는 CI 구성만 바꾼다. **Go 제품 소스에서 동작이 바뀌는 파일은 `exec.go` 하나뿐이다.**

**Tech Stack:** Go 1.26 · PowerShell(pwsh 7 / powershell 5.1) · modernc.org/sqlite · golangci-lint v2.12.2 · GitHub Actions

## Global Constraints

스펙: `docs/context-router-design-v0.14-ko.md`. 아래는 그 문서와 저장소 규약에서 **그대로 옮긴** 값이다. 모든 태스크의 요구사항에 암묵적으로 포함된다.

- **테스트 실행은 `go test -p 1`** — 전체 스위트를 캡 없이 돌리지 않는다. 태스크마다 해당 패키지만 돌린다(`go test -p 1 ./internal/exec/ -run <name> -v`).
- **`gofumpt -l .`이 무출력**이어야 한다. 커밋 전 확인한다.
- **파일 출력은 UTF-8(BOM 없음) · LF.**
- **`git add -A` 금지.** 태스크가 만든 파일만 명시적으로 add한다. `.claude/`·`.codex/`는 미추적이며 커밋 대상이 아니다.
- **커밋 트레일러**: `Co-Authored-By: Claude <실행 모델> <noreply@anthropic.com>` — `<실행 모델>`은 **그 커밋을 만든 세션의 실제 모델명**으로 채운다(계획 작성 세션의 모델명을 그대로 옮기지 않는다). 이 계획은 다음 세션이 실행하며 그 세션의 모델이 다를 수 있어, 고정 모델명을 박아 두면 모든 커밋이 틀린 트레일러를 단다. 아래 각 태스크의 커밋 예시도 같은 자리 표시자를 쓴다.
- **무회귀 대상**(스펙 §2 말미): D73 색인 계약(`SchemaVersion` 1 유지, 색인은 버전 스위치 밖) · D76의 읽기 상한(`maxNativeExitSideFileBytes`)·마커 판별(`ctrNativeExitMarker`)·`stderr == ""` 보강 조건·`sidePath` 작은따옴표 이스케이프 · D75의 unix shell 공유 지점(`shellEnv`) · D69의 `GOOS=windows` lint 축 · `exit_code`는 스니펫의 종료 상태라는 계약.
- **D76 보강 조건은 바뀌지 않는다**: `exec.go:187`이 `.ps1` && `exit_code == 0` && `stderr == ""`일 때만 보강 줄을 낸다. **그래서 D78의 모든 단정은 보강 줄이 아니라 사이드파일 값을 본다.**
- **BLOCKED 규율**: 측정이 전제를 반증하면 픽스처를 유리한 쪽으로 고치지 말고 BLOCKED로 올린다. 스펙 §1.3이 각 결정의 선행 게이트를 정의한다.
- 새 외부 의존을 추가하지 않는다.

## File Structure

| 파일 | 책임 | 변경 |
| --- | --- | --- |
| `internal/exec/exec.go` | 스니펫 조립 · 사이드파일 읽기 | `snippetContent` 교체, `promotesNativeExitSignal`·`nativeExitRegistration` 추가 |
| `internal/exec/exec_test.go` | D78 단위·실행 계약 | 테스트 추가 **+ 기존 `TestShellNativeExitAugmentation`(`:372-520`) 갱신** — 그 세 서브테스트가 D76 공백을 단정으로 굳혀 놓아 D78 정상 구현에서 반드시 깨진다(Task 2 Step 5) |
| `internal/store/store_test.go` | D77 게이트 | 테스트 추가 |
| `cmd/context-router/main.go` | 기동 purge 주석 | 재측정 값 주석 갱신(코드 무변경) |
| `.github/workflows/ci.yml` | lint 축 | darwin·netbsd·perf 3축 + 판정 스텝 |
| `.golangci.yml` | lint 구성 | 결합 경고 주석에 새 축 반영 |
| `CHANGELOG.md` · `internal/buildinfo/buildinfo.go` | 릴리스 | `[0.14.0]` 절 · `productVersion` |
| `docs/context-router-design-v0.14-ko.md` | 스펙 §3 | 구현 단계 실측 추가 |

---

### Task 1: 미니 파서 — 최상단 전용 구문 감지

승격 여부를 판정하는 순수 함수. 실행 없이 문자열만 다루므로 테이블 테스트로 완결된다.

**Files:**
- Modify: `internal/exec/exec.go` — **`snippetContent`의 doc 주석 블록(현재 `:260`, `// snippetContent — .ps1에는 UTF-8 BOM…`) 바로 위**에 두 함수를 추가한다. `func snippetContent`(`:278`) 바로 위에 넣으면 doc 주석이 함수에서 떨어져 나가고, Task 2 Step 3의 교체 범위(주석 블록부터 함수 끝까지)에 들어가 **지워진다**.
- Test: `internal/exec/exec_test.go`

**Interfaces:**
- Produces: `func promotesNativeExitSignal(code string) bool` — `true`면 등록 문을 붙인다(승격), `false`면 붙이지 않는다(폴백). Task 2가 이 함수를 부른다.

- [ ] **Step 1: 실패하는 테이블 테스트를 쓴다**

`internal/exec/exec_test.go` 끝에 추가:

```go
// TestPromotesNativeExitSignal — D78 미니 파서. 최상단 전용 구문으로 시작하는 스니펫은
// 승격하지 않는다(설계 v0.14 D78의 4단계 절차). 미탐만이 회귀이고 과잉 폴백은 기존 동작
// 유지이므로, 판정을 넓게 잡는 쪽이 안전 방향이다.
func TestPromotesNativeExitSignal(t *testing.T) {
	fallback := []struct{ name, code string }{
		{"param", "param($x)\ncmd /c exit 74"},
		{"param-with-default", "param($x = \"dflt\")\nWrite-Output $x"},
		{"param-uppercase", "PARAM ($x)\ncmd /c exit 74"},
		{"block-comment-then-param", "<# doc #> param($x)\ncmd /c exit 74"},
		{"multiline-block-comment-then-param", "<#\n doc\n line2\n#>\nparam($x)\ncmd /c exit 74"},
		{"attribute-then-param", "[CmdletBinding()] param($x)\ncmd /c exit 74"},
		{"using", "using namespace System.Text\ncmd /c exit 78"},
		{"bom-then-param", "\uFEFFparam($x)\ncmd /c exit 74"},
		{"backtick-continuation-then-using", "`\nusing namespace System.Text\ncmd /c exit 78"},
		{"dynamicparam", "dynamicparam { }\nend { cmd /c exit 79 }"},
		{"begin", "begin { }\nprocess { }\nend { cmd /c exit 79 }"},
		{"process", "process { }\nend { cmd /c exit 79 }"},
		{"end", "end { cmd /c exit 79 }"},
		{"clean", "clean { }\nend { cmd /c exit 79 }"},
		{"type-cast-overfallback", "[int]$x = 5\ncmd /c exit 70"},
		// PowerShell은 이 줄 전체를 라인 주석으로 처리하므로 다음 줄의 param이 살아 있다.
		// 좌→우 우선순위(현재 위치 첫 문자로만 판정)를 따르면 라인 주석을 소비한 뒤
		// param을 만나 폴백한다. `<#`를 먼저 찾는 구현은 짝 없는 `#>`를 찾아 남은 입력을
		// 전부 삼키고 미탐을 낸다 — 그것을 막는 것이 이 케이스다.
		{"line-comment-containing-block-open", "# see <# note\nparam($x)\ncmd /c exit 74"},
		// PowerShell 언어 사양의 공백은 " \t\r\n" 넷보다 넓다 — form feed·vertical tab·
		// Unicode Zs(NBSP)도 공백이다. 검수 실측(pwsh 7·powershell 5.1 양쪽): 아래 셋은
		// 현재 형태에서 정상 실행(exit 0 · side :78 · stdout USING-OK)인데 승격하면
		// ParserError로 exit 1 · 사이드파일 없음 · stdout 빈 값이 된다.
		// nbsp 케이스의 선두 문자는 U+00A0이라 **화면에 보이지 않는다** — 복사·편집에서
		// 일반 공백(U+0020)으로 바뀌면 그 케이스가 무효가 되므로 바이트로 확인할 것.
		{"formfeed-then-using", "\x0Cusing namespace System.Text\nWrite-Output 'ok'\ncmd /c exit 78"},
		{"verttab-then-using", "\x0Busing namespace System.Text\nWrite-Output 'ok'\ncmd /c exit 78"},
		{"nbsp-then-using", " using namespace System.Text\nWrite-Output 'ok'\ncmd /c exit 78"},
		// PowerShell은 **단독 CR도 줄 종료**로 처리한다(실측 확인). LF만 찾는 구현은
		// 주석이 다음 LF까지 이어진다고 보고 using을 지나쳐 승격한다 — 같은 ParserError.
		{"cr-line-comment-then-using", "# note\rusing namespace System.Text\nWrite-Output 'ok'\ncmd /c exit 78"},
	}
	for _, tc := range fallback {
		t.Run("fallback/"+tc.name, func(t *testing.T) {
			if promotesNativeExitSignal(tc.code) {
				t.Fatalf("승격됐다 — 최상단 전용 구문이므로 폴백해야 한다\ncode=%q", tc.code)
			}
		})
	}

	promote := []struct{ name, code string }{
		{"plain", "cmd /c exit 71\nexit 3"},
		{"line-comment-first", "# just a comment\ncmd /c exit 82"},
		{"requires-first", "#requires -Version 5\ncmd /c exit 81"},
		{"strictmode-first", "Set-StrictMode -Version Latest\n\"hello\""},
		{"variable-first", "$x = 1\ncmd /c exit 77"},
		{"param-inside-function", "function f { param($y) }\nf\ncmd /c exit 70"},
		{"paramx-identifier", "$paramX = 1\ncmd /c exit 70"},
		{"unterminated-block-comment", "<# never closed\ncmd /c exit 70"},
		{"empty", ""},
	}
	for _, tc := range promote {
		t.Run("promote/"+tc.name, func(t *testing.T) {
			if !promotesNativeExitSignal(tc.code) {
				t.Fatalf("폴백됐다 — 승격 대상이다\ncode=%q", tc.code)
			}
		})
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 ./internal/exec/ -run TestPromotesNativeExitSignal -v`
Expected: FAIL — `undefined: promotesNativeExitSignal`

- [ ] **Step 3: 최소 구현을 쓴다**

`internal/exec/exec.go`의 `snippetContent` 정의 **바로 위**에 추가:

```go
// promotesNativeExitSignal — D78: 스니펫이 PowerShell 최상단 전용 구문으로 시작하면
// 등록 문을 붙이지 않는다(승격하지 않는다). 등록 문은 스니펫 앞의 한 문장이라 그런
// 구문의 자리를 빼앗고, 그러면 본문이 실행되지 않거나 종료 코드가 뒤집힌다.
// 적용 대상은 BOM을 붙이기 **전**의 원본 코드다 — 선두 U+FEFF를 먼저 떼지 않으면
// 공백 판정이 거기서 멈춰 모든 감지가 실패한다(설계 v0.14 D78 1단계).
//
// 과잉 폴백은 허용한다(기존 동작 유지). 미탐만이 회귀이며, 이 비대칭이 판정을 넓게
// 잡는 이유다 — `[`로 시작하는 타입 캐스트나 `end`를 명령명으로 쓰는 문장이 함께
// 걸리는 것은 무해하다.
func promotesNativeExitSignal(code string) bool {
	s := strings.TrimPrefix(code, "\uFEFF")
	// 2단계: 공백과 주석을 소비한다. 판정은 **현재 위치의 첫 문자로만** 한다 —
	// `<#`를 먼저 찾는 구현은 `# ... <#` 형태에서 짝 없는 `#>`를 찾아 남은 입력을
	// 전부 삼키고 미탐을 낸다.
	for {
		// PowerShell의 공백은 " \t\r\n" 넷보다 넓다 — form feed(U+000C) · vertical tab
		// (U+000B) · Unicode Zs(NBSP U+00A0 포함)도 공백이다. unicode.IsSpace가
		// White_Space 속성을 보므로 그 전부를 덮는다. 과잉 폴백 방향이라 "미탐만이
		// 회귀"라는 설계 §0의 비대칭 규칙에 부합한다.
		s = strings.TrimLeftFunc(s, unicode.IsSpace)
		switch {
		case strings.HasPrefix(s, "<#"):
			j := strings.Index(s[2:], "#>")
			if j < 0 {
				return true // 닫히지 않은 블록 주석 — EOF까지 소비. 어느 형태로도 ParserError라 결과가 같다.
			}
			s = s[2+j+2:]
		case strings.HasPrefix(s, "#"):
			// PowerShell은 **단독 CR도 줄 종료**로 처리한다(검수 실측). LF만 찾으면
			// `# note\rusing namespace …`에서 주석이 다음 LF까지 이어진다고 보고
			// using을 지나쳐 승격한다 — 그 스니펫은 ParserError로 통째로 사라진다.
			// CRLF에서 남는 \n은 다음 루프의 TrimLeftFunc가 소비한다.
			j := strings.IndexAny(s, "\r\n")
			if j < 0 {
				return true // 주석만 있는 스니펫
			}
			s = s[j+1:]
		default:
			return promotesFirstToken(s)
		}
	}
}

// promotesFirstToken — 주석·공백을 걷어낸 뒤의 첫 실질 토큰으로 판정한다(설계 3단계).
func promotesFirstToken(s string) bool {
	if s == "" {
		return true
	}
	// `[`는 속성 선언 시작, 백틱은 줄 연속 문자다. 백틱을 소비하지 않으면 그 뒤의
	// 최상단 전용 구문이 그대로 승격되어 스니펫이 통째로 사라진다(설계 §관측 5라운드 18).
	if s[0] == '[' || s[0] == '`' {
		return false
	}
	j := 0
	for j < len(s) {
		c := s[j]
		alpha := c == '_' || (c >= 'A' && c <= 'Z') || (c >= 'a' && c <= 'z')
		digit := c >= '0' && c <= '9'
		if alpha || (j > 0 && digit) {
			j++
			continue
		}
		break
	}
	switch strings.ToLower(s[:j]) {
	case "param", "using", "dynamicparam", "begin", "process", "end", "clean":
		return false
	}
	return true
}
```

**import**: `unicode`를 `internal/exec/exec.go`의 import 블록에 추가한다(`strings`는 이미 있다). `gofumpt`가 정렬한다.

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 ./internal/exec/ -run TestPromotesNativeExitSignal -v`
Expected: 전부 PASS(부분 테스트 29개 — fallback 20 · promote 9).

FAIL이 남으면 그 케이스의 이름이 곧 원인이다:

| FAIL 케이스 | 원인 |
| --- | --- |
| `bom-then-param` | 1단계 U+FEFF 제거 누락 |
| `backtick-continuation-then-using` | 3단계 백틱 규칙 누락 |
| `line-comment-containing-block-open` | 2단계 좌→우 우선순위 위반(`<#`를 먼저 찾는 구현) |
| `formfeed-/verttab-/nbsp-then-using` | `TrimLeft(" \t\r\n")`을 `TrimLeftFunc(unicode.IsSpace)`로 바꾸지 않음 |
| `cr-line-comment-then-using` | `IndexByte('\n')`을 `IndexAny("\r\n")`으로 바꾸지 않음 |
| `clean`·`dynamicparam` | 3단계 토큰 목록 누락 |

- [ ] **Step 5: 포맷 확인 후 커밋**

```bash
gofumpt -l .
git add internal/exec/exec.go internal/exec/exec_test.go
git commit -m "feat(v0.14): D78 미니 파서 — 최상단 전용 구문 감지

Co-Authored-By: Claude <실행 모델> <noreply@anthropic.com>"
```

---

### Task 2: `snippetContent` 승격 형태 교체

등록 문을 만들고 스니펫 앞에 **같은 줄로** 잇는다. 이 태스크는 생성 바이트만 검증한다 — 실제 실행 계약은 Task 3·4다.

**Files:**
- Modify: `internal/exec/exec.go:278-293` (`snippetContent` 전체 교체)
- Test: `internal/exec/exec_test.go`

**Interfaces:**
- Consumes: `promotesNativeExitSignal(code string) bool` (Task 1)
- Produces: `func nativeExitRegistration(sidePath string) string` — 등록 문 + `"; "`. Task 3·4의 테스트가 대조군을 조립할 때 참조한다.
- `snippetContent(fileName, code, sidePath string) []byte` — 시그니처 불변.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`internal/exec/exec_test.go`에 추가:

```go
// TestSnippetContentRegistration — D78: 승격 대상은 BOM 다음에 등록 문이 오고 그 뒤에
// 스니펫이 **같은 줄로** 이어진다. 앞 줄에 두면 모든 오류의 줄 번호가 +1 밀린다.
func TestSnippetContentRegistration(t *testing.T) {
	const side = `C:\tmp\ctr-native-exit`
	got := string(snippetContent("snippet.ps1", "cmd /c exit 71\nexit 3", side))

	if !strings.HasPrefix(got, "\xEF\xBB\xBF") {
		t.Fatal("BOM이 없다 — powershell 5.1이 비ASCII를 손상시킨다(I2 계약)")
	}
	body := strings.TrimPrefix(got, "\xEF\xBB\xBF")
	if !strings.HasPrefix(body, "Register-EngineEvent ") {
		t.Fatalf("등록 문이 선두가 아니다: %.80q", body)
	}
	if !strings.Contains(body, "-SupportEvent") {
		t.Fatal("-SupportEvent가 없다 — 세션 작업표에 PSEventJob이 남아 Get-Job | Wait-Job이 끝나지 않는다")
	}
	if !strings.Contains(body, "$LASTEXITCODE") {
		t.Fatal("$LASTEXITCODE를 읽지 않는다")
	}
	// 인라인 결합: 등록 문과 스니펫 첫 줄 사이에 개행이 없어야 한다.
	head, _, ok := strings.Cut(body, "\n")
	if !ok {
		t.Fatal("개행이 없다")
	}
	if !strings.Contains(head, "; cmd /c exit 71") {
		t.Fatalf("스니펫 첫 줄이 등록 문과 같은 줄에 있지 않다: %.200q", head)
	}
}

// TestSnippetContentEscapesSidePath — D78: sidePath의 작은따옴표는 이중화한다. 빠지면
// 사용자명에 '가 있는 호스트에서 스크립트 자체가 파싱되지 않아 모든 shell 스니펫이
// 실패한다(D76 tail이 exec.go:288에서 하던 처리와 같다).
func TestSnippetContentEscapesSidePath(t *testing.T) {
	const side = `C:\Users\o'brien\tmp\ctr-native-exit`
	got := string(snippetContent("snippet.ps1", "cmd /c exit 71", side))
	if !strings.Contains(got, `o''brien`) {
		t.Fatalf("작은따옴표가 이중화되지 않았다: %.240q", got)
	}
	if strings.Contains(got, `'C:\Users\o'brien`) {
		t.Fatal("원본 작은따옴표가 그대로 남아 문자열 리터럴이 조기 종료된다")
	}
}

// TestSnippetContentFallbackKeepsD76Form — D78: 폴백 대상은 현재 D76 형태와 **바이트
// 동일**해야 한다. §2.5·§2.6의 대조 논거 전체가 이 동일성에 걸려 있다 — runPS1의
// promote=false 대조군이 "현재 형태"라는 전제가 여기서 무너지면 이후 모든 대조가
// 조용히 무의미해진다. 그래서 부분 문자열이 아니라 골든 리터럴로 잠근다(선행 개행
// 하나, 줄 순서 하나가 달라져도 잡힌다).
func TestSnippetContentFallbackKeepsD76Form(t *testing.T) {
	const side = `C:\tmp\ctr-native-exit`
	const code = "param($x)\ncmd /c exit 74"
	// D76 tail 골든(216B). 마커를 상수 대신 리터럴로 적는 것은 의도적이다 —
	// ctrNativeExitMarker가 바뀌면 이 테스트가 먼저 깨져 D76 무회귀를 알린다.
	const wantTail = "\ntry {\n" +
		"$ctrNativeExitCode = Get-Variable -Name LASTEXITCODE -ValueOnly -ErrorAction SilentlyContinue\n" +
		"[System.IO.File]::WriteAllText('" + `C:\tmp\ctr-native-exit` + "', '" +
		"ctr-exit-code:" + "' + [string]$ctrNativeExitCode)\n" +
		"} catch {}\n"

	got := string(snippetContent("snippet.ps1", code, side))
	if strings.Contains(got, "Register-EngineEvent") {
		t.Fatal("폴백 대상에 등록 문이 붙었다")
	}
	if want := "\xEF\xBB\xBF" + code + wantTail; got != want {
		t.Fatalf("폴백 산출물이 D76 형태와 바이트 동일하지 않다\ngot  %q\nwant %q", got, want)
	}
}

// TestSnippetContentNonPS1Unchanged — .ps1이 아니면 원본 그대로다(기존 계약).
func TestSnippetContentNonPS1Unchanged(t *testing.T) {
	const code = "echo hi"
	if got := string(snippetContent("snippet.sh", code, "/tmp/x")); got != code {
		t.Fatalf("got %q want %q", got, code)
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 ./internal/exec/ -run 'TestSnippetContent' -v`
Expected: `TestSnippetContentRegistration`·`TestSnippetContentEscapesSidePath`·`TestSnippetContentFallbackKeepsD76Form` FAIL (등록 문 부재), `TestSnippetContentNonPS1Unchanged` PASS.

- [ ] **Step 3: `snippetContent`를 교체한다**

`internal/exec/exec.go`의 기존 `snippetContent`를 아래 셋으로 **통째 교체**한다. 교체 범위는 `// snippetContent — .ps1에는 UTF-8 BOM…`으로 시작하는 doc 주석부터 `func snippetContent`의 닫는 중괄호까지다 — **Task 1이 그 주석 블록 위에 추가한 두 함수는 범위 밖이다**(포함시키면 지워져 `undefined: promotesNativeExitSignal`로 컴파일이 깨진다). 기존 tail 조립 코드는 `nativeExitTail`로 옮겨 폴백 경로가 계속 쓴다.

```go
// nativeExitRegistration — D78 승격 경로. `PowerShell.Exiting` 이벤트에 기록 동작을
// 걸고 스니펫과 **같은 줄**로 잇는다("; "로 끝나는 이유가 그것이다).
//
// 세 가지가 각각 하나의 실측된 문제를 막는다(설계 v0.14 D78):
//   - 스니펫을 감싸지 않는 것 — try/finally는 문 종료 오류의 제어 흐름을 바꿔,
//     오류 뒤의 나머지 스니펫이 실행되지 않는다(종료 코드가 같아 검출되지 않는다).
//   - -SupportEvent — 없으면 세션 작업표에 PSEventJob이 남아 `Get-Job | Wait-Job`이
//     끝나지 않고 `Get-Job | Remove-Job -Force`가 사이드 파일을 지우며, 작업 표가
//     stdout에도 찍힌다.
//   - 인라인 결합 — 앞 줄에 두면 모든 오류 메시지의 줄 번호가 +1 밀린다. 인라인은
//     2행 이상 오류에서 stderr를 바이트 단위로 보존한다(1행 오류는 열 번호와 소스 줄
//     에코가 밀린다 — 설계 §1.2의 알려진 한계).
//
// $LASTEXITCODE는 global 한정자 없이 읽는다(핸들러 스코프에서 그대로 보인다).
// sidePath의 작은따옴표는 반드시 이중화한다 — 빠지면 스크립트가 파싱되지 않는다.
func nativeExitRegistration(sidePath string) string {
	return "Register-EngineEvent -SourceIdentifier PowerShell.Exiting -SupportEvent -Action { try { " +
		"[System.IO.File]::WriteAllText('" + strings.ReplaceAll(sidePath, "'", "''") + "', '" +
		ctrNativeExitMarker + "' + [string]$LASTEXITCODE) } catch {} }; "
}

// nativeExitTail — D76 폴백 경로. 최상단 전용 구문으로 시작하는 스니펫은 승격하지
// 않으므로 이 형태를 그대로 유지한다(그 경로의 알려진 공백도 함께 유지된다:
// 스니펫이 exit 또는 처리되지 않은 종료 오류로 끝나면 tail이 실행되지 않는다).
//
// tail 제약 넷은 D76 그대로다: ① 개행을 선행시킨다 ② 프로바이더 cmdlet을 쓰지 않는다
// ③ 변수명을 고유하게 둔다 ④ try/catch로 감싼다(ConstrainedLanguage·Get-Variable 재정의).
func nativeExitTail(sidePath string) string {
	return "\ntry {\n" +
		"$ctrNativeExitCode = Get-Variable -Name LASTEXITCODE -ValueOnly -ErrorAction SilentlyContinue\n" +
		"[System.IO.File]::WriteAllText('" + strings.ReplaceAll(sidePath, "'", "''") + "', '" +
		ctrNativeExitMarker + "' + [string]$ctrNativeExitCode)\n" +
		"} catch {}\n"
}

// snippetContent — .ps1에는 UTF-8 BOM(기존 계약, I2 — powershell.exe 5.1이 BOM 없는
// .ps1을 시스템 ANSI 코드페이지로 디코딩해 비ASCII를 손상시키는 것을 막는다)과 D78
// 종료 신호를 붙인다. 승격 대상이면 등록 문을 선두에 잇고, 최상단 전용 구문으로
// 시작하면 D76 tail 형태로 폴백한다(promotesNativeExitSignal).
//
// 기록값에 ctrNativeExitMarker를 접두어로 붙이는 이유는 D76 그대로다 — 스크래치가
// 스니펫의 작업 디렉터리이기도 해서 스니펫이 같은 상대 경로에 직접 쓸 수 있고,
// 마커가 없으면 그 내용이 외부 명령 종료 코드로 오인된다.
func snippetContent(fileName, code, sidePath string) []byte {
	if !strings.HasSuffix(fileName, ".ps1") {
		return []byte(code)
	}
	buf := make([]byte, 0, 3+len(code)+320)
	buf = append(buf, 0xEF, 0xBB, 0xBF)
	if promotesNativeExitSignal(code) {
		buf = append(buf, nativeExitRegistration(sidePath)...)
		return append(buf, code...)
	}
	buf = append(buf, code...)
	return append(buf, nativeExitTail(sidePath)...)
}
```

- [ ] **Step 4: 단위 테스트 통과를 확인한다**

Run: `go test -p 1 ./internal/exec/ -run 'TestSnippetContent|TestSnippetPS1BOM' -v`
Expected: 전부 PASS.

- [ ] **Step 5: `TestShellNativeExitAugmentation`을 갱신한다 (이 스텝을 건너뛰면 D78이 무효화된다)**

`internal/exec/exec_test.go:372-520`의 `TestShellNativeExitAugmentation`은 **D78이 닫으려는 D76 공백을 단정으로 굳혀 놓았다.** 정상 구현에서 세 서브테스트가 **반드시 FAIL한다** — 이것은 회귀가 아니라 의도된 계약 변경이다. **FAIL을 보고 등록 문을 되돌리면 D78 자체가 사라진다.**

Run: `go test -p 1 ./internal/exec/ -run TestShellNativeExitAugmentation -v`
Expected: 세 서브테스트 FAIL. windows 외 환경이면 Skip이므로 이 스텝은 CI windows 잡이 권위다.

검수 실측(pwsh 7 · powershell 5.1 양쪽)에 따르면 세 스니펫 모두 미니 파서 판정이 **승격**이고 결과가 사이드파일 `ctr-exit-code:5` · exit 0 · stderr 0B다 → `exec.go:187` 조건을 충족해 **보강 줄이 나간다**. 각 서브테스트를 아래 계약으로 다시 쓴다.

| 현재 서브테스트 | 왜 깨지는가 | 새 계약 |
| --- | --- | --- |
| `인수없는_exit은_보강되지_않는다`(`:402`) | 이것이 정확히 설계 §0 D78이 닫는다고 선언한 **공백 ②**다 | 단정을 뒤집는다 — 보강 줄이 **나온다**. 이름도 바꾸고 주석에 "v0.13 §1.2의 공백 ②를 D78이 닫았다"를 적는다 |
| `Get-Variable_재정의가_tail을_깨지_않는다`(`:448`) | 승격 경로에는 tail이 없다. 지키려던 회귀(F2 리뷰: tail 오류가 stderr로 새는 것)가 **대상 자체와 함께 사라졌다** | 사이드파일이 진짜 값 `:5`이고 stderr가 보강 줄 정확히 하나임을 단정한다(설계 §3 표5가 긍정 결과로 기록한 변화다) |
| `ConstrainedLanguage에서_tail이_안전하다`(`:497`) | 같은 이유 | 같은 형태로 다시 쓴다. 검수 실측에서 **핸들러 스크립트블록이 FullLanguage에서 생성돼 언어 모드 강등 뒤에도 기록에 성공했다** — 설계 §3의 "미측정: ConstrainedLanguage 모드"를 이 실측으로 옮길 수 있다(Task 5 Step 5와 같은 방식) |

- [ ] **Step 6: 패키지 전체 무회귀를 확인한다**

Run: `go test -p 1 ./internal/exec/`
Expected: PASS. 여기서 FAIL이 남으면 **Step 5에서 갱신하지 않은 다른 테스트가 D76 공백을 단정하고 있다는 뜻**이다 — 그 테스트를 같은 기준으로 판정한다(D78이 닫는 공백을 굳힌 것인가, 아니면 진짜 회귀인가). 판단이 서지 않으면 BLOCKED로 올린다.

- [ ] **Step 7: 커밋**

```bash
gofumpt -l .
git add internal/exec/exec.go internal/exec/exec_test.go
git commit -m "feat(v0.14): D78 종료 신호를 Register-EngineEvent로 승격

- snippetContent가 승격 대상에 등록 문을 인라인으로 잇는다(-SupportEvent, sidePath 이스케이프)
- 폴백 경로는 D76 형태를 바이트 그대로 유지한다(골든 단정)
- TestShellNativeExitAugmentation의 세 단정 갱신 — v0.13 §1.2의 공백 ②가 닫혔다

Co-Authored-By: Claude <실행 모델> <noreply@anthropic.com>"
```

---

### Task 3: D78 실행 계약 — 신호 생성 · 종료 코드 · 본문 완주 · stderr

여기서부터 실제 PowerShell을 돌린다. windows 전용 경로이므로 `runtime.GOOS != "windows"`면 Skip한다(CI windows 잡이 실검증 권위다).

**Files:**
- Test: `internal/exec/exec_test.go`

**Interfaces:**
- Consumes: `snippetContent`, `nativeExitTail`, `ctrNativeExitFile`, `readNativeExitCode` (Task 2 · 기존)

- [ ] **Step 1: 신호 생성 테스트를 쓴다(현재 코드에서 FAIL해야 한다)**

먼저 공용 헬퍼를 `internal/exec/exec_test.go`에 추가한다:

```go
// psBin — 셸을 고른다. name이 빈 문자열이면 러너와 같은 순서(pwsh → powershell)로
// 고른다(exec.go:301-307의 detect 순서와 일치). name을 주면 그 셸만 쓰고 부재 시 Skip한다
// — 5.1 레그를 명시적으로 밟는 관용구는 exec_test.go:1142가 선례다.
func psBin(t *testing.T, name string) string {
	t.Helper()
	if name != "" {
		p, err := exec.LookPath(name)
		if err != nil {
			t.Skipf("%s 미해석: %v", name, err)
		}
		return p
	}
	if p, err := exec.LookPath("pwsh"); err == nil {
		return p
	}
	p, err := exec.LookPath("powershell")
	if err != nil {
		t.Skipf("pwsh/powershell 미설치: %v", err)
	}
	return p
}

// runPS1Shell — .ps1 스니펫을 러너와 같은 형태로 돌리고 (종료 코드, stdout, stderr,
// 사이드파일)을 준다.
//
// **dir을 인자로 받는 이유**: PowerShell 오류 메시지에는 스크립트의 절대 경로가 박힌다.
// 승격본과 대조군을 서로 다른 `t.TempDir()`에서 돌리면 stderr가 경로에서 갈려 §2.6의
// 바이트 비교가 승격 여부와 무관하게 **항상** 실패한다(검수 실측: 같은 dir이면 바이트
// 동일, 다른 dir이면 항상 불일치).
//
// promote=false면 등록 문 없이 D76 tail만 붙인 **대조군**을 만든다 — 승격 대상은 트리 안에
// 등록 문 없는 경로가 없으므로 대조군은 테스트가 직접 조립해야 한다(§2.6).
//
// **타임아웃**: `-SupportEvent` 회귀가 나면 `Get-Job | Wait-Job`이 끝나지 않는다(설계 §3
// 표6). 그 감시선이 물리는 순간 무한 대기가 되고 `go test` 기본 10분 패닉으로만 끝난다 —
// Task 4 Step 3이 그 회귀를 의도적으로 유도하므로 구현자가 반드시 겪는다. exec.go:539-544의
// `CommandContext` + `WaitDelay` 관용구를 그대로 쓰고, 타임아웃을 일반 실패와 구분해
// 진단 가능하게 만든다. 예산 근거: 5.1 기동 실측 170~194ms, 건강한 실행은 1s 안쪽이다
// (exec_test.go:1138 선례).
func runPS1Shell(t *testing.T, shell, dir, code string, promote bool) (exitCode int, stdout, stderr, side string) {
	t.Helper()
	if runtime.GOOS != "windows" {
		t.Skip("windows shell 갈래 전용(PowerShell -File 계약)")
	}
	bin := psBin(t, shell)
	sidePath := filepath.Join(dir, ctrNativeExitFile)
	_ = os.Remove(sidePath) // 같은 dir을 재사용하는 호출(§2.6 대조)에서 이전 값이 남지 않게 한다
	var content []byte
	if promote {
		content = snippetContent("snippet.ps1", code, sidePath)
	} else {
		buf := append([]byte{0xEF, 0xBB, 0xBF}, code...)
		content = append(buf, nativeExitTail(sidePath)...)
	}
	file := filepath.Join(dir, "snippet.ps1")
	if err := os.WriteFile(file, content, 0o600); err != nil {
		t.Fatal(err)
	}
	ctx, cancel := context.WithTimeout(t.Context(), 10*time.Second)
	defer cancel()
	cmd := exec.CommandContext(ctx, bin, "-NoProfile", "-NonInteractive", "-File", file)
	cmd.Dir = dir
	cmd.WaitDelay = time.Second
	var so, se bytes.Buffer
	cmd.Stdout, cmd.Stderr = &so, &se
	err := cmd.Run()
	if ctx.Err() != nil {
		t.Fatalf("스니펫이 10s 예산 안에 끝나지 않았다 — -SupportEvent 회귀 의심(Get-Job | Wait-Job이 이벤트 작업을 기다린다): %v", ctx.Err())
	}
	if err != nil && cmd.ProcessState == nil {
		t.Fatalf("인터프리터를 시작하지 못했다: %v", err) // 비영 종료는 정상 경로이므로 err 자체는 무시한다
	}
	if b, rerr := os.ReadFile(sidePath); rerr == nil {
		side = string(b)
	}
	return cmd.ProcessState.ExitCode(), so.String(), se.String(), side
}

// runPS1 — 대부분의 케이스용 래퍼. 셸은 러너 순서로 고르고 스크래치는 새로 만든다.
// stderr를 대조하는 테스트는 이 래퍼를 쓰지 말고 runPS1Shell에 **같은 dir**을 넘긴다.
func runPS1(t *testing.T, code string, promote bool) (int, string, string, string) {
	t.Helper()
	return runPS1Shell(t, "", t.TempDir(), code, promote)
}
```

> 필요한 import: `bytes`, `context`, `os/exec`, `path/filepath`, `runtime`, `time`. `context`·`time`은 이미 `exec_test.go`에 있고 나머지도 대부분 있다 — 없는 것만 추가하고 `gofumpt`가 정렬하게 둔다.

이어서 테스트:

```go
// TestD78SignalOnExit — D78이 닫는 공백 ①②. exit로 끝나는 스니펫에서도 사이드파일이
// 생기고 마지막 외부 명령의 코드가 담긴다. **현재 코드(Task 2 이전)에서는 FAIL한다** —
// 그것이 공백이 실재했다는 증거다.
func TestD78SignalOnExit(t *testing.T) {
	cases := []struct {
		name, code, want string
	}{
		{"explicit-exit", "cmd /c exit 71\nexit 3", ctrNativeExitMarker + "71"},
		{"bare-exit", "cmd /c exit 76\nexit", ctrNativeExitMarker + "76"},
		{"throw", "cmd /c exit 73\nthrow 'boom'", ctrNativeExitMarker + "73"},
		{"normal", "cmd /c exit 72", ctrNativeExitMarker + "72"},
	}
	// 양쪽 셸을 명시적으로 돈다. runPS1은 pwsh를 먼저 잡으므로 그것만 쓰면 pwsh와 5.1이
	// 모두 있는 GitHub windows 러너에서 제품의 5.1 폴백 갈래(exec.go:305, runner 라벨
	// `5.1`)를 새 테스트가 한 번도 밟지 않는다 — D78이 바꾸는 것이 이벤트 서브시스템·
	// -SupportEvent·핸들러 실행 시점이라 셸 버전에 민감한 표면이다. 부재 셸은 psBin이
	// Skip한다(exec_test.go:1142 선례).
	for _, shell := range []string{"pwsh", "powershell"} {
		t.Run(shell, func(t *testing.T) {
			for _, tc := range cases {
				t.Run(tc.name, func(t *testing.T) {
					_, _, _, side := runPS1Shell(t, shell, t.TempDir(), tc.code, true)
					if side != tc.want {
						t.Fatalf("side=%q want %q", side, tc.want)
					}
				})
			}
		})
	}
}

// TestD78ExitCodeUnchanged — 승격 대상에서 종료 코드는 바뀌지 않는다(설계 §2.2).
func TestD78ExitCodeUnchanged(t *testing.T) {
	for _, tc := range []struct{ name, code string }{
		{"explicit-exit", "cmd /c exit 71\nexit 3"},
		{"normal", "cmd /c exit 72"},
		{"throw", "cmd /c exit 73\nthrow 'boom'"},
		{"bare-exit", "cmd /c exit 76\nexit"},
	} {
		t.Run(tc.name, func(t *testing.T) {
			base, _, _, _ := runPS1(t, tc.code, false)
			got, _, _, _ := runPS1(t, tc.code, true)
			if got != base {
				t.Fatalf("종료 코드가 바뀌었다: 승격 %d vs 대조군 %d", got, base)
			}
		})
	}
}

// TestD78BodyRunsToCompletion — 설계 §2.3. 문 종료 오류가 있어도 마지막 문장이 실행된다.
// 종료 코드는 양쪽 0이라 §2.2 단정으로는 검출되지 않는다 — 이 단정이 try/finally 회귀를
// 막는 감시선이다(래핑하면 사이드파일이 41이 된다).
func TestD78BodyRunsToCompletion(t *testing.T) {
	const code = "cmd /c exit 41\nNotACmd999\ncmd /c exit 69"
	_, _, _, side := runPS1(t, code, true)
	if want := ctrNativeExitMarker + "69"; side != want {
		t.Fatalf("side=%q want %q — 오류 뒤 마지막 문장이 실행되지 않았다(try/finally 회귀)", side, want)
	}
}

// TestD78StderrNoDrift — 설계 §2.6. 오류가 2행 이상인 스니펫에서 stderr가 대조군과
// 바이트 동일해야 한다. 등록 문을 앞 줄에 두면 여기서 줄 번호 +1 드리프트가 잡힌다.
// param을 사례로 쓰지 않는다 — 승격해도 stderr가 0B라 비교할 오류가 없다.
func TestD78StderrNoDrift(t *testing.T) {
	const code = "cmd /c exit 41\nthrow 'boom'" // 오류가 **2행**에서 난다(검수 실측 확인)
	// 같은 스크래치를 재사용한다 — 다른 TempDir이면 stderr에 박히는 스크립트 절대 경로가
	// 달라 승격 여부와 무관하게 항상 불일치하고, 이 감시선이 무조건 FAIL한다.
	dir := t.TempDir()
	_, _, baseErr, _ := runPS1Shell(t, "", dir, code, false)
	_, _, gotErr, _ := runPS1Shell(t, "", dir, code, true)
	if gotErr != baseErr {
		t.Fatalf("stderr가 달라졌다 — 등록 문을 앞 줄에 두면 여기서 줄 번호 +1 드리프트가 잡힌다\n승격: %q\n대조: %q", gotErr, baseErr)
	}
}

// TestD78QuotedSidePathRuns — 설계 §2.7의 나머지 절반. 생성 바이트에 `''`가 들어가는
// 것은 Task 2가 단정한다. 여기서는 **그 스크립트가 실제로 파싱되어 사이드파일이
// 기록되는지**를 본다 — 따옴표 짝이 어긋나거나 `"; "`가 빠지는 회귀는 바이트 단정을
// 통과하지만 이 단정은 통과하지 못한다.
func TestD78QuotedSidePathRuns(t *testing.T) {
	dir := filepath.Join(t.TempDir(), "o'brien") // 작은따옴표는 Windows 파일명에 허용된다
	if err := os.MkdirAll(dir, 0o700); err != nil {
		t.Fatal(err)
	}
	_, _, _, side := runPS1Shell(t, "", dir, "cmd /c exit 62", true)
	if want := ctrNativeExitMarker + "62"; side != want {
		t.Fatalf("side=%q want %q — 경로의 작은따옴표가 이중화되지 않으면 스크립트가 파싱되지 않는다", side, want)
	}
}

// TestD78FirstLineErrorKeepsLineNumber — 설계 §2.6. 1행 오류는 줄 번호만 단정하고
// 열 번호·소스 줄 에코 차이는 허용한다(§1.2의 알려진 한계).
func TestD78FirstLineErrorKeepsLineNumber(t *testing.T) {
	const code = "throw 'boom'"
	_, _, gotErr, _ := runPS1(t, code, true)
	if !strings.Contains(gotErr, "snippet.ps1:1") && !strings.Contains(gotErr, "snippet.ps1: 1") {
		t.Fatalf("1행 오류의 줄 번호가 1이 아니다: %q", gotErr)
	}
}

// TestD78RequiresStillApplies — 설계 §2.6. 승격돼도 #requires 지시자가 그대로 작동한다.
func TestD78RequiresStillApplies(t *testing.T) {
	const code = "#requires -Version 99\ncmd /c exit 61"
	base, _, _, _ := runPS1(t, code, false)
	got, _, _, _ := runPS1(t, code, true)
	if base == 0 {
		t.Fatalf("대조군이 이미 통과했다 — 픽스처가 무효다(요구 버전을 올려라)")
	}
	if got != base {
		t.Fatalf("승격 시 #requires가 무력화됐다: %d vs 대조군 %d", got, base)
	}
}
```

- [ ] **Step 2: 신호 생성 테스트가 D78 없이 FAIL함을 기록한다**

**`git stash push internal/exec/exec.go`를 쓰지 않는다.** Task 1·2가 그 파일을 **커밋**했으므로 저장할 변경이 없어 아무 일도 일어나지 않고(테스트는 그대로 PASS — 존재하지 않는 FAIL을 보고하게 된다), 이어지는 `git stash pop`은 스택에 남아 있던 **무관한 이전 stash**를 워킹트리에 풀어놓는다. 커밋 전에 밟아도 안 된다 — `exec_test.go`가 `nativeExitTail`·`promotesNativeExitSignal`을 참조하므로 패키지가 컴파일되지 않고, 빌드 실패는 "검사가 물린다"의 증거가 아니다.

대신 `promotesNativeExitSignal`의 **첫 줄에 `return false`를 임시로 끼워** 승격을 끈다. 그러면 `snippetContent`가 폴백 경로만 타 D76 형태를 내므로 **컴파일이 유지된 채** 공백이 그대로 재현된다:

```go
func promotesNativeExitSignal(code string) bool {
	return false // 임시 — D78 유도 FAIL 확인용. 확인 후 이 한 줄만 지운다.
	s := strings.TrimPrefix(code, "﻿")
	// … 이하 원본 그대로 …
}
```

Run: `go test -p 1 ./internal/exec/ -run TestD78SignalOnExit -v`
Expected: `explicit-exit`·`bare-exit`·`throw` FAIL(사이드파일 없음), `normal` PASS.

확인 후 그 한 줄을 **지우고** 다시 돌려 전부 PASS를 확인한다. 두 출력을 태스크 보고에 남긴다 — "검사가 물린다"의 증거다. 이 방식은 Task 4 Step 3·Task 5 Step 3·4의 "소스 한 곳 임시 변경 후 원복" 관용구와 같다. (`return false` 뒤의 도달 불가 코드는 `go test`가 기본으로 도는 vet 검사 집합에 걸리지 않는다. 임시 변경이므로 커밋하지 않는다.)

- [ ] **Step 3: 전부 통과함을 확인한다**

Run: `go test -p 1 ./internal/exec/ -run 'TestD78' -v`
Expected: 전부 PASS (windows 외 환경이면 Skip).

- [ ] **Step 4: 커밋**

```bash
gofumpt -l .
git add internal/exec/exec_test.go
git commit -m "test(v0.14): D78 실행 계약 — 신호 생성·종료 코드·본문 완주·stderr 무회귀

Co-Authored-By: Claude <실행 모델> <noreply@anthropic.com>"
```

---

### Task 4: D78 부작용 한정 · 폴백 판정 · 한계 고정

**Files:**
- Test: `internal/exec/exec_test.go`

**Interfaces:**
- Consumes: `runPS1` (Task 3), `promotesNativeExitSignal` (Task 1)

- [ ] **Step 1: 부작용·폴백·한계 테스트를 쓴다**

```go
// TestD78NoObservableSideEffects — 설계 §2.4. -SupportEvent 회귀 감시선.
// 빠지면 Get-Job이 1이 되고 Wait-Job이 끝나지 않으며 작업 표가 stdout에 찍힌다.
func TestD78NoObservableSideEffects(t *testing.T) {
	// TestD78SignalOnExit과 같은 이유로 양쪽 셸을 돈다 — 작업표·구독표는 셸 버전에
	// 민감한 표면이고, 이 테스트가 -SupportEvent 회귀의 1차 감시선이다.
	for _, shell := range []string{"pwsh", "powershell"} {
		t.Run(shell, func(t *testing.T) {
			t.Run("job-and-subscriber-tables-clean", func(t *testing.T) {
				const code = `Write-Output ("jobs=" + (Get-Job).Count + " subs=" + (Get-EventSubscriber).Count)`
				_, out, _, _ := runPS1Shell(t, shell, t.TempDir(), code, true)
				if got := strings.TrimSpace(out); got != "jobs=0 subs=0" {
					t.Fatalf("stdout=%q want \"jobs=0 subs=0\" — -SupportEvent가 빠지면 1/1이 되고 작업 표가 stdout에도 찍힌다", got)
				}
			})
			t.Run("wait-job-terminates", func(t *testing.T) {
				const code = "cmd /c exit 71\nGet-Job | Wait-Job | Out-Null"
				_, _, _, side := runPS1Shell(t, shell, t.TempDir(), code, true)
				if want := ctrNativeExitMarker + "71"; side != want {
					t.Fatalf("side=%q want %q — Wait-Job이 이벤트 작업을 기다리다 멈췄을 수 있다", side, want)
				}
			})
			t.Run("remove-job-keeps-sidefile", func(t *testing.T) {
				const code = "cmd /c exit 71\nGet-Job | Remove-Job -Force"
				_, _, _, side := runPS1Shell(t, shell, t.TempDir(), code, true)
				if want := ctrNativeExitMarker + "71"; side != want {
					t.Fatalf("side=%q want %q — 통상적인 정리 관용구가 신호를 지웠다", side, want)
				}
			})
		})
	}
}

// TestD78FallbackMatchesBaseline — 설계 §2.5. 폴백 대상은 대조군과 완전히 같아야 한다.
// 폴백이 만드는 스크립트는 D76 형태와 바이트 동일하므로 이 대조는 구성상 성립한다.
// 명명 블록은 토큰마다 따로 단정한다 — dynamicparam이 3라운드에서, clean이 5라운드에서
// 빠졌던 자리다(clean은 7.x에서만 명명 블록이라 5.1에서는 과잉 폴백으로 무해하다).
func TestD78FallbackMatchesBaseline(t *testing.T) {
	for _, tc := range []struct{ name, code string }{
		{"param", "param($x = \"dflt\")\nWrite-Output \"x=$x\"\ncmd /c exit 80"},
		{"block-comment-then-param", "<# doc #> param($x)\ncmd /c exit 74"},
		{"multiline-block-comment-then-param", "<#\n doc\n line2\n#>\nparam($x)\ncmd /c exit 74"},
		{"attribute-then-param", "[CmdletBinding()] param($x)\ncmd /c exit 74"},
		{"using", "using namespace System.Text\nWrite-Output 'ok'\ncmd /c exit 78"},
		{"backtick-then-using", "`\nusing namespace System.Text\nWrite-Output 'ok'\ncmd /c exit 78"},
		{"bom-then-param", "\uFEFFparam($x)\ncmd /c exit 74"},
		// 명명 블록은 **토큰마다 따로** 태운다 — begin/process/end를 한 스니펫에 합치면
		// begin 토큰만 판정을 거친다. dynamicparam이 3라운드에서, clean이 5라운드에서
		// 빠졌던 자리가 정확히 이 자리다.
		{"dynamicparam", "dynamicparam { }\nend { cmd /c exit 79 }"},
		{"begin", "begin { }\nend { cmd /c exit 79 }"},
		{"process", "process { }\nend { cmd /c exit 79 }"},
		{"end", "end { cmd /c exit 79 }"},
		{"clean", "clean { }\nend { cmd /c exit 79 }"},
	} {
		t.Run(tc.name, func(t *testing.T) {
			if promotesNativeExitSignal(tc.code) {
				t.Fatalf("승격 대상으로 판정됐다 — 폴백해야 한다")
			}
			bc, bo, _, bs := runPS1(t, tc.code, false)
			gc, go_, _, gs := runPS1(t, tc.code, true)
			if gc != bc || go_ != bo || gs != bs {
				t.Fatalf("폴백이 대조군과 다르다\n승격: exit=%d out=%q side=%q\n대조: exit=%d out=%q side=%q",
					gc, go_, gs, bc, bo, bs)
			}
		})
	}
}

// TestD78Limits — 설계 §2.9. 경로마다 결과가 다르므로 따로 단정한다.
func TestD78Limits(t *testing.T) {
	t.Run("environment-exit-no-signal", func(t *testing.T) {
		_, _, _, side := runPS1(t, "cmd /c exit 75\n[Environment]::Exit(5)", true)
		if side != "" {
			t.Fatalf("side=%q want 없음 — [Environment]::Exit은 이벤트를 발화시키지 않는다", side)
		}
	})
	t.Run("fallback-throw-no-signal", func(t *testing.T) {
		// 폴백 경로는 D76 tail이라 처리되지 않은 종료 오류에서 tail이 실행되지 않는다.
		_, _, _, side := runPS1(t, "param($x)\ncmd /c exit 73\nthrow 'x'", true)
		if side != "" {
			t.Fatalf("side=%q want 없음 — 폴백 경로의 알려진 공백이다", side)
		}
	})
	t.Run("fallback-normal-records", func(t *testing.T) {
		_, _, _, side := runPS1(t, "param($x)\ncmd /c exit 74", true)
		if want := ctrNativeExitMarker + "74"; side != want {
			t.Fatalf("side=%q want %q — 폴백 경로도 정상 종료에서는 기록한다", side, want)
		}
	})
	t.Run("unregister-force-wins", func(t *testing.T) {
		code := "[System.IO.File]::WriteAllText($PSScriptRoot + '\\" + ctrNativeExitFile + "', '" +
			ctrNativeExitMarker + "8888')\n" +
			"Unregister-Event -SourceIdentifier PowerShell.Exiting -Force -ErrorAction SilentlyContinue\n" +
			"cmd /c exit 45\nexit"
		_, _, _, side := runPS1(t, code, true)
		if want := ctrNativeExitMarker + "8888"; side != want {
			t.Fatalf("side=%q want %q — -Force로는 조작이 통한다(알려진 한계)", side, want)
		}
	})
	t.Run("unregister-without-force-fails", func(t *testing.T) {
		// -SupportEvent 구독은 숨김이라 -Force 없는 Unregister-Event는 찾지 못하고 실패한다.
		// 이 대비가 깨지면 -SupportEvent가 사라진 것이다.
		code := "[System.IO.File]::WriteAllText($PSScriptRoot + '\\" + ctrNativeExitFile + "', '" +
			ctrNativeExitMarker + "8888')\n" +
			"Unregister-Event -SourceIdentifier PowerShell.Exiting -ErrorAction SilentlyContinue\n" +
			"cmd /c exit 45\nexit"
		_, _, _, side := runPS1(t, code, true)
		if want := ctrNativeExitMarker + "45"; side != want {
			t.Fatalf("side=%q want %q — -Force 없는 해제가 성공했다면 -SupportEvent가 빠진 것이다", side, want)
		}
	})
	t.Run("lastexitcode-assign", func(t *testing.T) {
		// 설계 §3 표5: 스크립트 최상단 대입만으로 값이 바뀐다(global: 한정자 불필요).
		_, _, _, side := runPS1(t, "cmd /c exit 5\n$LASTEXITCODE = 7777\nexit", true)
		if want := ctrNativeExitMarker + "7777"; side != want {
			t.Fatalf("side=%q want %q", side, want)
		}
	})
	t.Run("own-handler-wins", func(t *testing.T) {
		// 설계 §3 표5: 같은 소스 식별자로 두 번 등록해도 충돌 오류 없이 공존하고
		// 스니펫 쪽 기록이 이긴다. 핸들러는 별도 스코프에서 돌므로 경로를 env로 넘긴다
		// (프로세스 전역이라 핸들러에서 보인다 — 러너의 등록 문도 같은 방식으로 읽는다).
		code := "$env:CTR_PROBE = Join-Path $PSScriptRoot '" + ctrNativeExitFile + "'\n" +
			"Register-EngineEvent -SourceIdentifier PowerShell.Exiting -SupportEvent -Action { " +
			"[System.IO.File]::WriteAllText($env:CTR_PROBE, '" + ctrNativeExitMarker + "9999') }\n" +
			"cmd /c exit 5\nexit"
		_, _, _, side := runPS1(t, code, true)
		if want := ctrNativeExitMarker + "9999"; side != want {
			t.Fatalf("side=%q want %q — 스니펫 자신의 핸들러가 이기는 것이 관측된 동작이다(위조 무력화를 주장하지 않는 근거)", side, want)
		}
	})
}
```

- [ ] **Step 2: 통과를 확인한다**

Run: `go test -p 1 ./internal/exec/ -run 'TestD78NoObservableSideEffects|TestD78FallbackMatchesBaseline|TestD78Limits' -v`
Expected: 전부 PASS (windows 외 Skip).

- [ ] **Step 3: `-SupportEvent` 감시선이 물리는지 유도 FAIL로 확인한다**

`nativeExitRegistration`에서 `-SupportEvent `를 일시 삭제하고:

Run: `go test -p 1 -timeout 90s ./internal/exec/ -run TestD78NoObservableSideEffects -v`

`-timeout 90s`를 붙이는 이유: 이 회귀는 `Get-Job | Wait-Job`을 **끝나지 않게** 만든다. `runPS1Shell`의 10s 예산이 먼저 걸려 명시적 메시지를 내지만, 예산 장치까지 함께 빠진 상태라면 기본 10분 패닉으로만 끝나 "FAIL 캡처"가 스택 덤프가 된다.

Expected: `job-and-subscriber-tables-clean`이 `jobs=1 subs=1`로 FAIL, `wait-job-terminates`가 `스니펫이 10s 예산 안에 끝나지 않았다 — -SupportEvent 회귀 의심`으로 FAIL.
확인 후 **원복**하고 다시 PASS를 확인한다. 이 두 출력을 태스크 보고에 남긴다.

- [ ] **Step 4: 패키지 전체 무회귀 확인 후 커밋**

Run: `go test -p 1 ./internal/exec/`
Expected: PASS.

```bash
gofumpt -l .
git add internal/exec/exec_test.go
git commit -m "test(v0.14): D78 부작용 한정·폴백 판정·한계 고정

Co-Authored-By: Claude <실행 모델> <noreply@anthropic.com>"
```

---

### Task 5: D77 — purge 실 잠금 보유 시간 게이트

**Files:**
- Test: `internal/store/store_test.go`
- Modify: `cmd/context-router/main.go:418-420` (주석의 재측정 값 갱신 — 코드 무변경)
- Modify: `docs/context-router-design-v0.14-ko.md` §3 (실측 추가)

**Interfaces:**
- Consumes: `openAt(t, dir)`, `regSource(t, st, content, mediaType, uri, kind)`, `hashOf(content)`, `ageBlobFile(t, st, h, delta)` — 전부 `store_test.go`의 기존 헬퍼다.

- [ ] **Step 1: 게이트 테스트를 쓴다**

`internal/store/store_test.go` 끝에 추가:

```go
// TestPurgeHookOnlyLockHoldBudget — D77: 기동 purge의 실 잠금 보유 시간이 훅 예산의
// 절반을 넘지 않는다. 형태는 D73 §2⑦(TestOpenBudgetWithIndexDDL)을 따른다 —
// elapsed >= budget이면 Fatalf이며 수동 판독이 아니다.
//
// 픽스처는 두 조건을 함께 만족해야 한다(설계 v0.14 §2.1):
//   - 경로 조건: 블롭 mtime을 gcOrphanMinAge 이전으로 소급시킨다. 그러지 않으면
//     reclaimHookBlobs의 나이 게이트가 단락되어 stillReferenced 쿼리도 os.Remove도
//     실행되지 않고, 유예 경로만 재는 종이 게이트가 된다.
//   - 규모 조건: 개발기 실측이 budget의 1/5 이하가 되는 해시 수를 쓴다. 이 게이트는
//     CI에서 3-OS와 -race로 네 번 도는데, 원 실측(100해시 632ms) 대비 여유 1.58배로는
//     러너 편차 한 번이 BLOCKED가 된다.
//
// 그래서 이 게이트가 잡는 것은 **해시당 비용의 회귀**다. startupPurgeMaxHashes(100해시)
// 배치의 절대 보유 시간은 합성 픽스처로 검증할 수 없다(실 저장소와 분포가 다르다).
func TestPurgeHookOnlyLockHoldBudget(t *testing.T) {
	const (
		budget = 1000 * time.Millisecond // 훅 총예산 2000ms의 50%(설계 v0.14 D77)
		hashes = 20                      // 규모 조건: 개발기 실측이 budget/5 이하가 되도록 고른 값
	)
	st := openAt(t, t.TempDir())
	for i := 0; i < hashes; i++ {
		c := fmt.Sprintf("lock-hold-budget-%d", i)
		regSource(t, st, c, "text/plain", fmt.Sprintf("shadow:Bash:%d", i), "hook")
		ageBlobFile(t, st, hashOf(c), -2*gcOrphanMinAge) // 경로 조건
	}

	start := time.Now()
	rep, err := st.PurgeHookOnlyOlderThan(t.Context(), 0, hashes)
	elapsed := time.Since(start)
	if err != nil {
		t.Fatalf("PurgeHookOnlyOlderThan: %v", err)
	}
	if rep.Hashes != hashes {
		t.Fatalf("rep.Hashes=%d want %d — 픽스처가 의도한 규모로 처리되지 않았다", rep.Hashes, hashes)
	}
	if rep.ReclaimedB <= 0 || rep.DeferredFiles != 0 {
		t.Fatalf("회수 경로를 타지 않았다: ReclaimedB=%d DeferredFiles=%d — 나이 게이트가 단락되면 이 게이트는 유예 경로만 재는 종이 게이트가 된다",
			rep.ReclaimedB, rep.DeferredFiles)
	}
	t.Logf("hashes=%d reclaimed=%dB deferred=%d 보유 %v (budget %v, 여유 %.1f배)",
		rep.Hashes, rep.ReclaimedB, rep.DeferredFiles, elapsed, budget, float64(budget)/float64(elapsed))
	if elapsed >= budget {
		t.Fatalf("잠금 보유가 예산을 넘었다: %v >= %v (hashes=%d reclaimed=%dB deferred=%d) — 개발기 기준값은 이 절반 이하였다",
			elapsed, budget, rep.Hashes, rep.ReclaimedB, rep.DeferredFiles)
	}
}
```

- [ ] **Step 2: 돌려서 경로 조건과 여유를 확인한다**

Run: `go test -p 1 ./internal/store/ -run TestPurgeHookOnlyLockHoldBudget -v`
Expected: PASS. `t.Logf` 출력에서 두 가지를 확인한다.
1. `reclaimed=<0보다 큼> deferred=0` — 회수 경로를 탔다.
2. `여유 <5.0배 이상>` — 규모 조건을 만족한다.

**참고값**: 계획 검수가 이 테스트를 원안 그대로 돌려 **20해시 · 44.1ms · 여유 22.7배**를 얻었다(개발기). 그 대역이면 규모 조건은 넉넉하다 — 측정이 그보다 한참 크면 픽스처나 환경 쪽을 먼저 의심한다.

**여유가 5배 미만이면** `hashes`를 줄여 다시 잰다(규모 조건은 픽스처 크기로 맞추는 것이 설계가 인가한 방법이다 — 임계 1000ms는 바꾸지 않는다).
**`deferred`가 0이 아니거나 `reclaimed`가 0이면** `ageBlobFile` 호출이 잘못된 것이다. 해시 계산(`hashOf(c)`)이 실제 블롭 경로와 맞는지 `TestGCOrphanBlobs_AgeGate`(store_test.go:1447)의 관용구와 대조한다.

- [ ] **Step 3: 나이 게이트 단락을 유도해 감시선이 물리는지 확인한다**

`ageBlobFile` 호출 줄을 임시로 주석 처리하고:

Run: `go test -p 1 ./internal/store/ -run TestPurgeHookOnlyLockHoldBudget -v`
Expected: FAIL — `회수 경로를 타지 않았다: ReclaimedB=0 DeferredFiles=20`.
확인 후 **원복**하고 PASS를 확인한다. 이 출력을 태스크 보고에 남긴다 — 종이 게이트가 실제로 막힌다는 증거다.

- [ ] **Step 4: 예산 게이트를 유도해 물리는지 확인한다**

`budget`을 `1 * time.Millisecond`로 임시 변경하고:

Run: `go test -p 1 ./internal/store/ -run TestPurgeHookOnlyLockHoldBudget -v`
Expected: FAIL — `잠금 보유가 예산을 넘었다: ...`, 그리고 메시지에 `hashes`·`reclaimed`·`deferred`가 함께 찍힌다(진단 가능성 확인).
확인 후 **원복**한다.

- [ ] **Step 5: `main.go` 주석과 설계서 §3을 실측으로 갱신한다**

`cmd/context-router/main.go`의 재측정 관련 주석(현재 `:418-420`, "그래서 위 보유 시간(632·633ms 등)은 **그 측정 당시(색인 없음, 아티팩트 1254개·210MiB)의 값 그대로**이고, D73 색인 도입 이후 실제 보유 시간을 다시 재지는 않았다")을 갱신한다. 새 문면은 이 형태로 쓴다 — `<값>`은 Step 2의 실측으로 채운다:

```
// D77(v0.14): 색인 도입 이후 합성 픽스처로 재측정했다 — <hashes>해시 <elapsed>(해시당 <x>ms),
// 회수 경로 확인(ReclaimedB>0·DeferredFiles=0). 자동 게이트는
// store_test.go의 TestPurgeHookOnlyLockHoldBudget이며 임계는 훅 예산 2000ms의 50%인
// 1000ms다. 위 632·633ms는 실 도그푸딩 저장소(아티팩트 1254개·210MiB)의 값이라
// 합성 픽스처와 분포가 달라 절대값 비교는 성립하지 않는다 — 게이트가 잡는 것은
// 해시당 비용의 회귀다.
```

이어서 `docs/context-router-design-v0.14-ko.md` §3의 "**미측정**" 문단 바로 위에 실측 문단을 추가한다:

```
**D77 재측정(구현 단계)** — `go test -p 1 ./internal/store/ -run
TestPurgeHookOnlyLockHoldBudget -v`. 픽스처는 hook-only 소스 <hashes>개이며 블롭
mtime을 `gcOrphanMinAge` 이전으로 소급시켜 회수 경로를 타게 했다(`ReclaimedB=<값>B`,
`DeferredFiles=0`). 실 보유 시간 **<elapsed>**(해시당 <x>ms)로 임계 1000ms 대비
여유 <n>배다. 원 실측 632·633ms는 실 도그푸딩 저장소의 값이라 절대값 비교 대상이
아니다.
```

- [ ] **Step 6: 커밋**

```bash
gofumpt -l .
git add internal/store/store_test.go cmd/context-router/main.go docs/context-router-design-v0.14-ko.md
git commit -m "test(v0.14): D77 purge 잠금 보유 게이트 + 재측정 기록

Co-Authored-By: Claude <실행 모델> <noreply@anthropic.com>"
```

---

### Task 6: D79 — lint 미검출 세 축

**Files:**
- Modify: `.github/workflows/ci.yml` (lint 잡, 현재 `:143` 뒤에 이어서)
- Modify: `.golangci.yml` (결합 경고 주석)

**Interfaces:** 없음(CI 구성 전용).

- [ ] **Step 1: 로컬에서 세 축이 성립함을 먼저 확인한다**

Run:
```bash
GOOS=darwin go build ./... && echo "darwin build OK"
GOOS=netbsd go build ./... && echo "netbsd build OK"
go build -tags perf ./... && echo "perf build OK"
```
Expected: 셋 다 OK. **하나라도 실패하면 그 축은 성립하지 않으므로 BLOCKED로 올린다** — 스펙 §1.3-3의 ①이다. (`freebsd`가 이 조건에서 탈락했다: `worker_unix.go:98`의 `syscall.Rlimit.Cur`가 그 GOOS에서 int64다.)

Run:
```bash
GOOS=darwin go list -f '{{join .GoFiles "\n"}}' ./internal/sandbox | grep launcher_darwin.go
GOOS=netbsd go list -f '{{join .GoFiles "\n"}}' ./internal/sandbox | grep launcher_other.go
go list -tags perf -f '{{join .TestGoFiles "\n"}}{{join .XTestGoFiles "\n"}}' ./internal/search | grep perf_test.go
```
Expected: 셋 다 매칭.

- [ ] **Step 2: `ci.yml`에 판정 1·2단계 스텝을 추가한다**

`.github/workflows/ci.yml`의 `golangci-lint (GOOS=windows)` 스텝(현재 `:142-143`) **바로 뒤**에 추가:

```yaml
      # D79(v0.14): darwin·netbsd·perf 축을 더해 미검출 집합을 비운다. D69가 GOOS=windows
      # 축을 세우고 남긴 잔여는 정확히 세 파일이었다 — internal/sandbox/launcher_darwin.go,
      # internal/sandbox/launcher_other.go, internal/search/perf_test.go.
      #
      # 판정은 **세 단계**다. D69의 두 단계 앞에 빌드 확인이 붙는다: 그 GOOS에서 전
      # 패키지가 빌드되지 않으면 golangci-lint는 typechecking error와 함께 "0 issues."를
      # 내며 종료 코드만 실패한다 — 스코프 확인(2단계)은 초록인데 lint가 **다른 패키지**에서
      # 죽는 형태다. freebsd가 정확히 그랬다(worker_unix.go의 syscall.Rlimit이 그 GOOS에서
      # int64라 !windows 갈래가 통째로 컴파일되지 않는다). netbsd는 소스 변경 0으로
      # 성립하고 같은 !windows && !linux && !darwin 갈래라 미검출 집합을 동일하게 닫는다.
      # openbsd는 syscall.RLIMIT_AS 미정의로 불가하다.
      - name: verify darwin/netbsd/perf axes build and are in lint scope
        run: |
          set -euo pipefail
          for goos in darwin netbsd; do
            if ! GOOS=$goos go build ./...; then
              echo "GOOS=$goos build failed — this lint axis does not hold (see design v0.14 D79)"
              exit 1
            fi
          done
          if ! go build -tags perf ./...; then
            echo "-tags perf build failed — this lint axis does not hold"
            exit 1
          fi
          # perf 축의 스코프 확인은 형태가 다르다 — .GoFiles는 _test.go를 포함하지 않아
          # perf_test.go를 볼 수 없다. .TestGoFiles/.XTestGoFiles를 본다.
          files="$(GOOS=darwin go list -f '{{join .GoFiles "\n"}}' ./internal/sandbox)"
          if ! grep -q 'launcher_darwin\.go$' <<<"$files"; then
            echo "launcher_darwin.go not in darwin lint scope"
            exit 1
          fi
          files="$(GOOS=netbsd go list -f '{{join .GoFiles "\n"}}' ./internal/sandbox)"
          if ! grep -q 'launcher_other\.go$' <<<"$files"; then
            echo "launcher_other.go not in netbsd lint scope"
            exit 1
          fi
          files="$(go list -tags perf -f '{{join .TestGoFiles "\n"}}{{join .XTestGoFiles "\n"}}' ./internal/search)"
          if ! grep -q 'perf_test\.go$' <<<"$files"; then
            echo "perf_test.go not in perf lint scope"
            exit 1
          fi

      - name: golangci-lint (GOOS=darwin)
        run: GOOS=darwin "$(go env GOPATH)/bin/golangci-lint" run --max-same-issues=0 --max-issues-per-linter=0 ./...

      - name: golangci-lint (GOOS=netbsd)
        run: GOOS=netbsd "$(go env GOPATH)/bin/golangci-lint" run --max-same-issues=0 --max-issues-per-linter=0 ./...

      # `run:` 값이 큰따옴표로 시작하면 YAML이 그것을 이중따옴표 스칼라로 파싱하고 닫는
      # 따옴표 뒤를 처리하지 못한다 → **ci.yml 전체가 파싱 오류**가 되어 lint 잡뿐 아니라
      # 워크플로의 모든 잡이 로드되지 않는다(검수 실측). 기존 D69 스텝과 위 두 축은 값이
      # `GOOS=…`로 시작해 평문 스칼라라 무사하고, perf 스텝만 앞에 붙는 것이 없어 따옴표가
      # 선두로 온다 — `env`를 앞세워 평문 스칼라로 만든다.
      - name: golangci-lint (--build-tags perf)
        run: env "$(go env GOPATH)/bin/golangci-lint" run --build-tags perf --max-same-issues=0 --max-issues-per-linter=0 ./...
```

- [ ] **Step 3: 로컬에서 세 lint 축을 실제로 돌린다**

Run:
```bash
go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2
GOOS=darwin "$(go env GOPATH)/bin/golangci-lint" run --max-same-issues=0 --max-issues-per-linter=0 ./...
GOOS=netbsd "$(go env GOPATH)/bin/golangci-lint" run --max-same-issues=0 --max-issues-per-linter=0 ./...
"$(go env GOPATH)/bin/golangci-lint" run --build-tags perf --max-same-issues=0 --max-issues-per-linter=0 ./...
```
Expected: 셋 다 exit 0, `0 issues.`
**issue가 나오면 그것을 고치는 것이 D79의 본래 목적이다** — 미검출이던 정적 오류가 드러난 것이므로 같은 커밋에서 수정한다. `typechecking error`가 나오면 그것은 issue가 아니라 축 실패이므로 Step 1로 돌아간다.

- [ ] **Step 4: `.golangci.yml` 결합 경고 주석을 갱신한다**

`.golangci.yml`의 `# D69 결합: ci.yml의 "verify windows-only files are in lint scope" 스텝은…` 주석에서, 스텝 이름 열거를 새 축까지 포함하도록 고친다:

```yaml
  # D69·D79 결합: ci.yml의 "verify windows-only files are in lint scope"와
  # "verify darwin/netbsd/perf axes build and are in lint scope" 두 스텝은 go list
  # 스코프만 증명한다(golangci-lint 자체의 스코프가 아니다) — 지금은 여기 경로 기반
  # 제외가 없어 둘이 일치하지만, 나중에 exclusions.paths를 추가하면 그 스텝들은 계속
  # 초록인데 golangci-lint는 그 경로를 건너뛸 수 있어 게이트가 거짓 초록이 된다.
```

- [ ] **Step 5: 워크플로가 파싱되고 기존 축이 남아 있는지 확인한다**

`run:` 값 하나가 따옴표로 시작하기만 해도 워크플로 **전체**가 로드되지 않는다 — GitHub에 푸시해야 드러나므로 로컬에서 먼저 확인한다:

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml', encoding='utf-8')); print('yaml ok')"`
Expected: `yaml ok`. python·PyYAML이 없으면 다른 YAML 파서로 대체하되 **어떤 형태로든 파싱을 한 번 통과시킨다**.

이어서 기존 축이 그대로인지 diff로 본다:

Run: `git diff .github/workflows/ci.yml`
Expected: `verify windows-only files are in lint scope`와 `golangci-lint (GOOS=windows)` 두 스텝이 **삭제되지 않고** 새 스텝이 그 뒤에 붙었다. D69의 `GOOS=windows` 축은 Global Constraints의 무회귀 대상이며, 이 diff 확인이 그 판정 지점이다.

- [ ] **Step 6: 커밋**

```bash
git add .github/workflows/ci.yml .golangci.yml
git commit -m "ci(v0.14): D79 lint 미검출 세 축(darwin·netbsd·perf) + 빌드 선행 판정

Co-Authored-By: Claude <실행 모델> <noreply@anthropic.com>"
```

---

### Task 7: 릴리스 마감

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `internal/buildinfo/buildinfo.go:11`

- [ ] **Step 1: 전체 스위트를 캡 규율로 돌린다**

Run: `go test -p 1 -count=1 ./...`
Expected: PASS. FAIL이면 릴리스하지 않는다.

**이 실행이 Global Constraints의 Go 측 무회귀 확인 지점이다** — D73 색인 계약(`SchemaVersion` 1 유지·색인은 버전 스위치 밖)과 D75의 unix shell 공유 지점(`shellEnv`)은 별도 스텝을 두지 않고 여기서 기존 테스트로 판정한다. D69의 `GOOS=windows` lint 축은 Task 6 Step 5의 diff 확인이 담당한다.

Run: `gofumpt -l .`
Expected: 무출력.

- [ ] **Step 2: `CHANGELOG.md`에 `[0.14.0]` 절을 추가한다**

기존 최상단 절 바로 위에 삽입한다. 헤더 형식은 `CHANGELOG.md:11`의 `## [0.13.0] — 2026-07-27`을 그대로 따르며 **날짜를 반드시 채운다**(아래 `<릴리스 일자>` 자리):

```markdown
## [0.14.0] — <릴리스 일자>

### Changed
- **D78** `.ps1` 스니펫의 네이티브 종료 신호를 `Register-EngineEvent PowerShell.Exiting`
  으로 승격했다. `exit`·인수 없는 `exit`·`throw`로 끝나는 스니펫에서도 마지막 외부 명령의
  종료 코드가 **사이드파일에 기록된다**. 응답의 보강 줄은 D76 조건(`exit_code == 0`이고
  `stderr == ""`)을 그대로 유지하므로 실제로 노출이 늘어나는 것은 **인수 없는 `exit`**
  경로다 — v0.13 §1.2가 남긴 공백 ②가 닫혔다. 스니펫을 감싸지 않으므로 문 종료 오류의
  제어 흐름은 그대로이며, 최상단 전용 구문(`param`·`using`·`dynamicparam`·명명 블록·
  속성·줄 연속)으로 시작하는 스니펫은 기존 형태로 폴백한다.

### Added
- **D77** 기동 purge의 실 잠금 보유 시간에 자동 게이트를 세웠다(훅 예산 2000ms의 50%).
  픽스처는 나이 게이트를 넘겨 실제 회수 경로를 재며, 그 사실을 테스트가 단정한다.
- **D79** lint에 `GOOS=darwin`·`GOOS=netbsd`·`--build-tags perf` 세 축을 더해 미검출
  집합을 비웠다. 축마다 전 패키지 빌드를 먼저 확인한다 — `freebsd`는 그 조건을 만족하지
  않아 대표에서 제외했다.
```

- [ ] **Step 3: `productVersion`을 올린다**

`internal/buildinfo/buildinfo.go:11`:

```go
var productVersion = "0.14.0"
```

- [ ] **Step 4: 커밋**

```bash
git add CHANGELOG.md internal/buildinfo/buildinfo.go
git commit -m "release(v0.14.0): CHANGELOG 절 + productVersion 0.14.0

Co-Authored-By: Claude <실행 모델> <noreply@anthropic.com>"
```

- [ ] **Step 5: 호스트 등록물 self-heal — 훅·MCP 등록을 새 버전 마커로 재기입한다**

`productVersion`만 올리면 이미 설치된 훅·MCP 등록의 `__ctrManaged`는 이전 버전으로 남아, `doctor`가 `[9]`(Claude 훅)·`[16]`(Codex)에서 `marker 0.13.0≠0.14.0 — hook install 재실행` 드리프트를 경고한다. session-40 핸드오프(`docs/prompts/2026-07-28-session-40-v0.14-design-plan.md` §5)가 **"릴리스 후 `hook install` 재실행이 절차에 없다 — D64가 self-heal 대상으로 지목한 상태가 실제로 발생해 있다"**고 지적한 갭이다. 재기입되는 마커 값은 **실행한 바이너리의 `version`**이므로, 이 스텝은 **0.14.0 바이너리를 설치해 PATH에 올린 뒤**에만 의미가 있다.

**선행 확인(반드시 먼저 — 건너뛰면 사용자 설정이 소실된다)**: Codex `config.toml`(`$CODEX_HOME/config.toml`, 미설정 시 `~/.codex/config.toml`)의 `# BEGIN context-router` / `# END context-router` 마커 쌍 **사이에 `[mcp_servers.ctr]` 외의 테이블이 있는지** 본다. 경로 해석은 `codexConfigPath`(`internal/cli/codex_toml.go:262`)와 **같은 규칙**을 쓴다 — 홈을 하드코딩하면 `CODEX_HOME`이 설정된 환경에서 설치가 건드리는 파일과 **다른 파일**을 검사하게 되어 이 확인이 무효가 된다.

Run:
```bash
sed -n '/^# BEGIN context-router$/,/^# END context-router$/p' "${CODEX_HOME:-$HOME/.codex}/config.toml" | grep '^\['
```
Expected: `[mcp_servers.ctr]` 한 줄뿐. **그 외 테이블이 하나라도 나오면 `hook install --codex`를 실행하지 말고**, `# END context-router`(와 그 앞의 `# ingest/net 활성화 시 권장:` 주석) 두 줄을 `[mcp_servers.ctr]` 블록 직후로 되돌린 뒤 다시 확인한다.

- 근거: `internal/cli/codex_toml.go`의 `classifyMarkers`는 BEGIN/END 1쌍을 `classReplace`로 판정하고, `mergeCodexConfig`·`uninstallCodexConfigBlock`이 **그 사이를 통째로 교체·삭제**한다. 마커가 밀린 상태에서 실행하면 그 사이에 들어간 사용자 테이블이 전부 소실된다.
- 실측(2026-07-28): Codex에서 `/hooks` 신뢰 승인을 하면 Codex가 `config.toml`을 TOML 파서로 재직렬화하면서 `# END context-router` 주석을 **파일 최하단으로 민다**. 그 결과 `[mcp_servers.node_repl]`·`[plugins.*]`·`[shell_environment_policy.set]`·`[hooks.state]`(신뢰 해시 13개)를 포함한 **24개 테이블**이 관리 블록 안으로 들어갔다. 주석 두 줄을 원위치로 옮겨 복구했다.
- 주석 기반 마커는 호스트가 파일을 재직렬화할 때마다 다시 밀리므로 **일회성 사고가 아니다** — 릴리스마다 이 확인을 먼저 한다.

Run:
```bash
context-router hook install --user
context-router hook install --codex --user
```
Expected: 둘 다 성공.

Run: `context-router doctor`
Expected: `[9]`와 `[16]`의 `marker`가 `0.14.0`이고, `≠`가 붙은 재실행 안내가 사라진다.

---

## Self-Review

**1. 스펙 커버리지** — §2의 열 항목을 태스크에 매핑:

| 스펙 §2 | 태스크 |
| --- | --- |
| 1 D77 게이트(경로·규모·실행 위치·진단·유도 FAIL) | Task 5 Step 1~4 |
| 2 D78 종료 코드 동일성 | Task 3 `TestD78ExitCodeUnchanged` |
| 3 D78 본문 완주 | Task 3 `TestD78BodyRunsToCompletion` |
| 4 D78 부작용 한정 | Task 4 `TestD78NoObservableSideEffects` |
| 5 D78 폴백 판정 | Task 1 테이블 + Task 4 `TestD78FallbackMatchesBaseline` |
| 6 D78 stderr 무회귀 | Task 3 `TestD78StderrNoDrift`·`TestD78FirstLineErrorKeepsLineNumber`·`TestD78RequiresStillApplies` |
| 7 D78 경로 이스케이프 | Task 2 `TestSnippetContentEscapesSidePath`(바이트) **+ Task 3 `TestD78QuotedSidePathRuns`(실행)** — 스펙이 요구하는 "그 스크립트가 정상 파싱되어 사이드파일이 기록된다"는 바이트 단정만으로는 덮이지 않는다 |
| 8 D78 신호 생성(현재 FAIL) | Task 3 `TestD78SignalOnExit` + Step 2의 **`return false` 유도**(`git stash`가 아니다) |
| 9 D78 한계 고정 | Task 4 `TestD78Limits` — `[Environment]::Exit` · 폴백 경로 둘 · 조작 경로 **셋**(`Unregister-Event -Force` · `$LASTEXITCODE` 대입 · 스니펫 자신의 핸들러) · `-Force` 없는 해제의 대비 |
| 10 D79 축 판정 | Task 6 Step 1·2 + **Step 5**(YAML 파싱 · 기존 축 diff) |

§2 항목은 아니지만 **누락하면 D78이 무효화되는 것**: 기존 `TestShellNativeExitAugmentation`의 세 단정 갱신 → Task 2 Step 5. 그 셋은 D78이 닫는 D76 공백을 굳혀 놓아 정상 구현에서 반드시 FAIL한다.

§1.3 선행 게이트 셋: 1(D77 픽스처) → Task 5 Step 2, 2(D78 폴백) → Task 1 Step 4 + Task 4, 3(D79 축) → Task 6 Step 1.

§1.1 배송 순서(exec → store → CI): Task 1·2·3·4(exec) → 5(store) → 6(CI) → 7(릴리스). 일치.

**2. 플레이스홀더** — 둘 다 그 스텝이 실측·확정으로 채우도록 지정된 자리다. Task 5 Step 5의 `<hashes>`·`<elapsed>`·`<x>`·`<n>`은 Step 2가 그 값을 만드는 명령을 주고, Task 7 Step 2의 `<릴리스 일자>`는 커밋 시점에 정해진다. 그 외 미정 항목 없음.

**3. 타입 일관성** — `promotesNativeExitSignal(code string) bool`(Task 1 정의, Task 2·4 사용) · `promotesFirstToken(s string) bool`(Task 1 내부) · `nativeExitRegistration(sidePath string) string`·`nativeExitTail(sidePath string) string`(Task 2 정의, 후자는 Task 3 대조군 조립에서 사용) · `psBin(t, name) string`·`runPS1Shell(t, shell, dir, code string, promote bool) (int, string, string, string)`·`runPS1(t, code, promote) (int, string, string, string)`(Task 3 정의, Task 4 사용). `snippetContent`의 시그니처는 불변이므로 기존 호출부(`exec.go:151`, `exec_test.go:107`·`:120`·`:1088`·`:1170`)를 고치지 않는다.

**4. 알려진 실측 의존** — Task 3·4는 windows에서만 실행된다(그 외 Skip). unix/macOS 축은 CI 3-OS 매트릭스가 권위다. Task 5는 OS 무관. Task 6은 로컬에서 세 축을 미리 돌려 CI 적색을 예방한다.

**5. 계획 검수 반영 (2026-07-28 · 서브에이전트 + Codex 1패스)** — critical 4 · important 7 · minor 4를 받아 전부 반영했다. 스펙의 결정을 바꾼 것은 없다(plan_conflict 0). 무게 순으로:

| id | 무엇이 잘못됐나 | 반영 |
| --- | --- | --- |
| P1 | 기존 `TestShellNativeExitAugmentation` 세 단정이 **D78 정상 구현에서 반드시 깨지는데** 계획은 "PASS여야 한다"고 적어 구현자를 D78 무효화로 보냈다 | Task 2 Step 5 신설 |
| P2 | perf lint 스텝의 `run:` 값이 따옴표로 시작해 **ci.yml 전체가 파싱 오류** — 모든 잡이 로드되지 않는다 | `env` 앞세움 + Step 5의 파싱 확인 |
| P3 | `runPS1`이 호출마다 새 `t.TempDir()`을 만들어 stderr 대조가 **정상 구현에서도 무조건 FAIL** | 같은 dir 재사용(`runPS1Shell`) |
| P4 | Task 3 Step 2의 `git stash`가 커밋 후에는 아무것도 저장하지 않고, `stash pop`이 **무관한 이전 stash**를 푼다 | `return false` 임시 삽입으로 교체 |
| P5 | `runPS1`에 타임아웃이 없어 `-SupportEvent` 회귀 유도 시 10분 매달린다 | `CommandContext` 10s + 진단 메시지 + `-timeout 90s` |
| P13 | 미니 파서가 FF·VT·NBSP와 **단독 CR**을 놓쳐 미탐이 생긴다(스펙이 유일한 회귀 유형으로 지목한 것) | `unicode.IsSpace` · `IndexAny("\r\n")` + 테이블 4케이스 |
| P6~P12·P14·P15 | §2.7 실행 단정 누락 · §2.9 조작 경로 셋 중 하나만 단정 · tail 바이트 동일성을 잠그는 장치 없음 · Task 1/2 삽입 위치 충돌 · 폴백 baseline 3케이스 누락 · CHANGELOG 날짜·문면 과장 · 무회귀 확인 지점 미배정 · 5.1 레그 커버리지 0 | 각각 해당 태스크에 반영 |

검수가 **확인해 준 것**: 대조군은 진짜 D76(tail 216B 바이트 동일) · `41+throw`는 진짜 2행 오류 · `hashOf`·`ageBlobFile`·`$PSScriptRoot`·심볼·import 정상 · Task 5 게이트는 원안 그대로 PASS(20해시 44.1ms 여유 22.7배) · Task 5 Step 3·4의 유도 FAIL 실측 확인(`ReclaimedB=0 DeferredFiles=20`, 보유 44ms vs 1ms) · D79 세 축 로컬 전부 초록.
