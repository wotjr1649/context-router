# v0.13 (D73–D76) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** shadow 술어의 색인 부재, 기동 purge의 취소 미전파, unix shell의 툴체인 구성 상속, PowerShell 네이티브 종료 상황의 무신호 — 네 가지를 닫는다.

**Architecture:** 스키마 **버전은 올리지 않는다**(색인은 가산적·하위 호환 변경이고 버전 상향은 구 바이너리의 쓰기 연결을 영구 거부한다). 홈 유도 격리는 러너별 사본을 만들지 않고 `exec` 계층 공유 헬퍼로 뽑는다. PowerShell 보강은 응답 필드를 추가하지 않고 `stderr` 한 줄로 낸다(go-sdk가 응답 타입을 `outputSchema`로 `tools/list`에 실어 필드 하나가 게이트 11 상한을 넘긴다).

**Tech Stack:** Go 1.26 · modernc.org/sqlite · PowerShell 7/5.1 · GitHub Actions 3-OS

**스펙:** `docs/context-router-design-v0.13-ko.md` — D73~D76과 §2의 해당 항목이 계약이다. 스펙과 이 계획이 어긋나면 **스펙이 이긴다**.

## Global Constraints

- **테스트는 `go test -p 1`로 돈다** — 메모리 캡 규율(CLAUDE.md).
- **`gofumpt -l .`이 빈 출력이어야 한다.**
- **파일 출력은 UTF-8(BOM 없음) · LF.** 예외: PowerShell 스니펫은 `snippetContent`가 의도적으로 BOM을 붙인다(기존 계약).
- **`git add -A` 금지** — 추적하지 않는 로컬 설정이 있다. 항상 경로를 명시한다.
- **커밋 트레일러**: `Co-Authored-By: Claude <모델명> <noreply@anthropic.com>` — 실행 세션의 실제 모델명.
- **`SchemaVersion`을 바꾸지 않는다.** 이 계획 어디에서도 `user_version`을 1에서 올리지 않는다 — Task 2의 테스트가 그것을 고정한다.
- **unix 실검증 권위는 CI 3-OS다.** D75·D76의 플랫폼별 동작은 개발기(windows)에서 절반만 보인다.
- **BASE는 실행 원장에 기록한다.** `HEAD~1`을 쓰지 않는다.

---

### Task 1: D74 — 기동 purge 회수 루프의 취소 전파

`reclaimHookBlobs`는 `ctx`를 받으면서도 잠금 획득에 `lockStore`(내부에서 `context.Background()` 고정)를 쓰고 파일 회수 루프가 `ctx`를 보지 않는다. 그래서 종료가 배치 크기만큼 지연되고, 남은 hash가 `deferred`로 세어져 D67 임계값 튜닝의 입력을 부풀린다.

**형태는 이미 있는 것을 쓰는 것이다** — `lockStoreCtx`가 `store.go:66`에 있다. 새 변형을 만들지 않는다.

**Files:**
- Modify: `internal/store/store.go:1186` (잠금 획득), `internal/store/store.go:1191` (루프 선두)
- Test: `internal/store/store_test.go` (신규 1개)

**Interfaces:**
- Consumes: `lockStoreCtx(ctx context.Context, dir string) (func(), error)` — `store.go:66`의 기존 함수.
- Produces: `reclaimHookBlobs`의 시그니처는 그대로다(`(ctx, hashes, rep) error`). 동작만 바뀐다 — 취소 시 `ctx.Err()`를 반환한다.

- [ ] **Step 1: 실패 테스트를 쓴다**

`internal/store/store_test.go` 끝에 추가한다. Store 생성은 **이 파일의 기존 테스트가 쓰는 형태를 그대로** 따른다(여러 예가 있다):

```go
// TestReclaimHookBlobsHonorsCancel — D74: 행 삭제 커밋 이후 파일 회수 루프가 취소를 관측한다.
// 취소된 ctx를 PurgeHookOnlyOlderThan에 처음부터 넘기는 형태로는 이 회귀를 잡을 수 없다 —
// 그 경로는 행 삭제 tx에서 먼저 실패해 빈 리포트를 반환하므로 deferred가 원래 0이다. 그래서
// 회수 단계를 직접 부른다.
func TestReclaimHookBlobsHonorsCancel(t *testing.T) {
	dir := t.TempDir()
	s := openStoreT(t, dir) // 이 파일의 기존 Store 생성 헬퍼를 쓴다
	// 회수 대상 blob 두 개를 CAS 배치대로 만든다: <dir>/artifacts/<h[:2]>/<h>
	hashes := []string{strings.Repeat("a", 64), strings.Repeat("b", 64)}
	for _, h := range hashes {
		p := filepath.Join(dir, "artifacts", h[:2], h)
		if err := os.MkdirAll(filepath.Dir(p), 0o700); err != nil {
			t.Fatal(err)
		}
		if err := os.WriteFile(p, []byte("x"), 0o600); err != nil {
			t.Fatal(err)
		}
	}
	ctx, cancel := context.WithCancel(context.Background())
	cancel() // 회수 시작 전에 취소된 상태
	var rep HookPurgeReport
	err := s.reclaimHookBlobs(ctx, hashes, &rep)
	if !errors.Is(err, context.Canceled) {
		t.Fatalf("err=%v want context.Canceled", err)
	}
	if rep.DeferredFiles != 0 {
		t.Fatalf("deferred=%d want 0 — 취소 경로가 deferred를 부풀리면 D67 임계값 입력이 오염된다", rep.DeferredFiles)
	}
	// 취소로 중단됐으므로 파일은 그대로 남는다(후속 --gc가 회수한다).
	for _, h := range hashes {
		if _, statErr := os.Stat(filepath.Join(dir, "artifacts", h[:2], h)); statErr != nil {
			t.Fatalf("blob %s 가 사라졌다: %v", h[:8], statErr)
		}
	}
}
```

임포트 확인: `context` · `errors` · `os` · `path/filepath` · `strings`.

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 ./internal/store/ -run TestReclaimHookBlobsHonorsCancel -v`
Expected: FAIL — 수정 전에는 루프가 `ctx`를 보지 않아 두 blob을 모두 처리하고 `err`가 nil이거나 `deferred`가 증가한다.

- [ ] **Step 3: 잠금 획득과 루프 선두를 고친다**

`internal/store/store.go:1186`:

```go
	release, err := lockStoreCtx(ctx, s.dir)
```

`internal/store/store.go:1191`의 루프 선두에 삽입한다:

```go
	for _, h := range hashes {
		// D74: 회수 루프는 기동 경로에서 도는데 종료 신호를 보지 않으면 종료가 배치 크기만큼
		// 지연되고, 남은 hash가 deferred로 세어져 D67 임계값 튜닝의 입력을 부풀린다. 여기서
		// 반환하면 호출부(PurgeHookOnlyOlderThan)가 "행 삭제는 커밋되어 유효, 남은 파일은
		// --gc 후속 회수"라는 기존 계약대로 처리한다.
		if err := ctx.Err(); err != nil {
			return err
		}
		p := filepath.Join(s.dir, "artifacts", h[:2], h) // blobPath 헬퍼 부재 — 인라인 관례
```

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 ./internal/store/ -run TestReclaimHookBlobsHonorsCancel -v` → PASS
Run: `go test -p 1 ./internal/store/` → PASS (기존 purge 테스트 무회귀)

- [ ] **Step 5: 커밋한다**

```bash
git add internal/store/store.go internal/store/store_test.go
git commit -m "fix(v0.13): 기동 purge 회수 루프의 취소 전파 (D74)"
```

---

### Task 2: D73 — shadow 술어 색인 (버전 스위치 밖) + doctor 병기

`sources`의 색인은 PK(`uri`) 하나뿐이라 shadow 술어의 조회가 후보 행마다 `sources`를 훑는다. **`SchemaVersion`은 1로 유지하고** 색인을 버전 스위치 밖에서 적용한다 — 버전을 올리면 `case v > SchemaVersion`이 구 바이너리의 writable Open을 영구 거부하고, 스위치 안에 두면 `case v == SchemaVersion`이 DDL 앞에서 반환해 기존 DB가 색인을 받지 못한다.

술어(`shadowOwnedHashQuery` + `shadowOwnedFilter`의 s4 절)가 쓰는 컬럼은 **4개**다: `artifact_id` · `source_kind` · `raw_blob_hash` · `indexed_at`.

**Files:**
- Modify: `internal/store/store.go:165-183` (`migrate` 말미에 색인 적용 호출)
- Modify: `internal/store/store.go` (신규 `ensureIndexes` — `applySchemaV1` 뒤에 둔다)
- Modify: `internal/cli/cli.go:1540` (doctor `[3]` 병기)
- Test: `internal/store/store_test.go` (신규 3개), `internal/cli/cli_test.go` (신규 1개)

**Interfaces:**
- Produces: `(s *Store) ensureIndexes() error` — 색인마다 개별 `CREATE INDEX IF NOT EXISTS`를 내고, 실패는 `slog.Warn` 한 줄로 남기고 계속한다. **반환 오류는 없다**(항상 nil) — 부재가 느리지만 정확한 상태로 퇴화하므로 Open을 막지 않는다.
- Produces: 색인 이름 3개(진단이 이 이름으로 실재 여부를 센다):
  `idx_sources_artifact_kind` · `idx_sources_blobhash_kind` · `idx_sources_artifact_indexed`

- [ ] **Step 1: 실패 테스트 셋을 쓴다**

`internal/store/store_test.go`에 추가한다:

```go
// shadowIndexNames — D73 대상 색인. 진단의 분자와 같은 집합이다.
var shadowIndexNames = []string{
	"idx_sources_artifact_kind",
	"idx_sources_blobhash_kind",
	"idx_sources_artifact_indexed",
}

// countShadowIndexes — sqlite_master에서 대상 색인의 실재 수를 센다. sources 전체 색인 수를
// 세면 uri TEXT PRIMARY KEY의 autoindex가 포함돼 "색인 없음"이 0으로 관측되지 않는다.
func countShadowIndexes(t *testing.T, db *sql.DB) int {
	t.Helper()
	n := 0
	for _, name := range shadowIndexNames {
		var got string
		err := db.QueryRow(`SELECT name FROM sqlite_master WHERE type='index' AND name=?`, name).Scan(&got)
		if err == nil {
			n++
			continue
		}
		if !errors.Is(err, sql.ErrNoRows) {
			t.Fatalf("index 조회(%s): %v", name, err)
		}
	}
	return n
}

// TestEnsureIndexesOnFreshDB — D73: 신규 생성 DB가 **첫 Open**에서 색인까지 도달하고
// user_version은 1로 남는다(구 바이너리 호환 회귀 방지).
func TestEnsureIndexesOnFreshDB(t *testing.T) {
	dir := t.TempDir()
	s := openStoreT(t, dir)
	if got := countShadowIndexes(t, s.Reader()); got != len(shadowIndexNames) {
		t.Fatalf("indexes=%d want %d", got, len(shadowIndexNames))
	}
	var v int
	if err := s.Reader().QueryRow("PRAGMA user_version").Scan(&v); err != nil {
		t.Fatal(err)
	}
	if v != SchemaVersion {
		t.Fatalf("user_version=%d want %d — 이 결정은 버전을 올리지 않는다", v, SchemaVersion)
	}
}

// TestEnsureIndexesOnExistingV1 — D73: 색인 없는 기존 v1 DB가 Open 후 색인을 얻는다.
// 버전 스위치 안에 두면 case v == SchemaVersion이 DDL 앞에서 반환해 이 테스트가 실패한다.
func TestEnsureIndexesOnExistingV1(t *testing.T) {
	dir := t.TempDir()
	s := openStoreT(t, dir)
	for _, name := range shadowIndexNames { // v0.12 상태(색인 없음)를 만든다
		if _, err := s.writer.Exec("DROP INDEX IF EXISTS " + name); err != nil {
			t.Fatal(err)
		}
	}
	closeStoreT(t, s) // 이 파일의 기존 종료 헬퍼
	s2 := openStoreT(t, dir)
	if got := countShadowIndexes(t, s2.Reader()); got != len(shadowIndexNames) {
		t.Fatalf("재Open 후 indexes=%d want %d", got, len(shadowIndexNames))
	}
}

// TestShadowPredicateResultUnchangedByIndexes — D73: 색인은 성능 변경이며 의미 변경이 아니다.
// 같은 데이터에서 색인 유/무의 술어 결과 집합이 같아야 한다.
func TestShadowPredicateResultUnchangedByIndexes(t *testing.T) {
	dir := t.TempDir()
	s := openStoreT(t, dir)
	seedShadowFixture(t, s) // 이 파일의 기존 shadow 픽스처 헬퍼(hook 소스 + 비-hook 소스 혼재)
	withIdx := shadowOwnedHashesT(t, s)
	for _, name := range shadowIndexNames {
		if _, err := s.writer.Exec("DROP INDEX IF EXISTS " + name); err != nil {
			t.Fatal(err)
		}
	}
	withoutIdx := shadowOwnedHashesT(t, s)
	if !reflect.DeepEqual(withIdx, withoutIdx) {
		t.Fatalf("색인이 결과를 바꿨다: with=%v without=%v", withIdx, withoutIdx)
	}
}
```

`shadowOwnedHashesT`는 `shadowOwnedFilter(0, 0)`의 질의를 실행해 hash 슬라이스를 정렬 반환하는 테스트 헬퍼다. 이 파일에 없으면 함께 만든다:

```go
func shadowOwnedHashesT(t *testing.T, s *Store) []string {
	t.Helper()
	q, args := shadowOwnedFilter(0, 0)
	rows, err := s.Reader().Query(q, args...)
	if err != nil {
		t.Fatalf("shadow 술어: %v", err)
	}
	defer func() { _ = rows.Close() }()
	var out []string
	for rows.Next() {
		var h string
		if err := rows.Scan(&h); err != nil {
			t.Fatal(err)
		}
		out = append(out, h)
	}
	if err := rows.Err(); err != nil {
		t.Fatal(err)
	}
	sort.Strings(out)
	return out
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 ./internal/store/ -run 'TestEnsureIndexes|TestShadowPredicateResult' -v`
Expected: FAIL — `ensureIndexes` 미정의, 색인 0개.

- [ ] **Step 3: `ensureIndexes`를 만든다**

`internal/store/store.go`의 `applySchemaV1` 뒤에 추가한다:

```go
// shadowIndexDDL — D73: shadow 보존 술어(shadowOwnedHashQuery + shadowOwnedFilter의 나이 절)가
// 쓰는 진입 컬럼을 선두로 둔 복합 색인. 술어 지점과의 대응:
//   · hook JOIN·첫 NOT EXISTS의 s2 — artifact_id → source_kind
//   · 둘째 NOT EXISTS의 s3        — raw_blob_hash → source_kind
//   · 나이 절의 s4                — artifact_id → indexed_at
// a2·a4·ORDER BY의 content_hash는 기존 UNIQUE(content_hash, media_type)의 선두 컬럼이 커버하므로
// 추가하지 않는다. indexed_at 단독 색인은 형제 경로 PurgeOlderThan용이며 이 결정의 범위 밖이다.
var shadowIndexDDL = []struct{ name, ddl string }{
	{"idx_sources_artifact_kind", `CREATE INDEX IF NOT EXISTS idx_sources_artifact_kind ON sources(artifact_id, source_kind)`},
	{"idx_sources_blobhash_kind", `CREATE INDEX IF NOT EXISTS idx_sources_blobhash_kind ON sources(raw_blob_hash, source_kind)`},
	{"idx_sources_artifact_indexed", `CREATE INDEX IF NOT EXISTS idx_sources_artifact_indexed ON sources(artifact_id, indexed_at)`},
}

// ensureIndexes — D73: 버전 스위치 **밖**에서 색인을 적용한다. SchemaVersion을 올리면
// case v > SchemaVersion이 구 바이너리의 writable Open을 영구 거부하고, 스위치 안에 두면
// case v == SchemaVersion이 DDL 앞에서 반환해 기존 DB가 색인을 받지 못한다. 색인 부재는
// 느리지만 정확한 상태로 퇴화하므로(D65 분류의 이중 안전장치) 실패가 Open을 막지 않는다 —
// 색인마다 개별로 내고 첫 실패에서 나머지를 중단하지 않으며, 실패한 색인마다 경고 한 줄을
// 남긴다(따라서 최대 3줄). 실제 적용 상태는 doctor [3]의 indexes 병기가 관측한다.
func (s *Store) ensureIndexes() error {
	for _, ix := range shadowIndexDDL {
		if _, err := s.writer.Exec(ix.ddl); err != nil {
			slog.Warn("store: 색인 생성 실패 — 술어는 정확하나 스캔 경계가 넓어진다", "index", ix.name, "error", err)
		}
	}
	return nil
}
```

같은 파일 임포트에 `log/slog`를 더한다. `internal/store`의 첫 로깅 호출이다 — 아키텍처 문서가 규정한 stderr slog를 그대로 쓴다.

- [ ] **Step 4: `migrate`가 모든 분기 뒤에 호출하게 한다**

`internal/store/store.go:165-183`을 교체한다:

```go
func (s *Store) migrate() error {
	var v int
	if err := s.writer.QueryRow("PRAGMA user_version").Scan(&v); err != nil {
		return fmt.Errorf("store migrate: %w", err)
	}
	switch {
	case v == 0:
		if err := s.applySchemaV1(); err != nil {
			return fmt.Errorf("store migrate: %w", err)
		}
	case v == SchemaVersion:
	case v > SchemaVersion:
		return fmt.Errorf("store migrate: db user_version=%d > 지원 %d — 비파괴 거부: %w", v, SchemaVersion, ErrUnavailable)
	default:
		return fmt.Errorf("store migrate: 알 수 없는 하위 버전 %d: %w", v, ErrUnavailable)
	}
	// D73: 색인은 버전 스위치 **밖**에서 적용한다 — 신규(v==0→v1)와 기존(v==1)이 같은 한 번의
	// Open에서 색인까지 도달해야 하고, 버전을 올리지 않아 구 바이너리도 이 DB를 계속 연다.
	return s.ensureIndexes()
}
```

**주의**: `case v == 0` 분기의 `return nil`을 제거했다(그대로 두면 신규 DB가 색인을 못 받는다). `case v == SchemaVersion`도 빈 분기로 바꿔 아래로 흐르게 한다.

- [ ] **Step 5: store 테스트가 통과하는지 본다**

Run: `go test -p 1 ./internal/store/` → PASS (신규 3개 + 기존 마이그레이션 테스트 무회귀)

`store_test.go:39`의 `"PRAGMA user_version": "1"` 단정과 `:124`·`:160`의 버전 테스트가 그대로 통과해야 한다 — 통과하지 않으면 버전을 올린 것이므로 되돌린다.

- [ ] **Step 6: doctor `[3]`에 병기를 더한다**

`internal/cli/cli.go:1540`을 교체한다:

```go
					// D73: 색인 유무를 버전이 아니라 진단으로 관측한다. 병기는 quick_check **뒤**에
					// 둔다 — 기존 테스트가 "user_version=%d quick_check=ok"를 부분문자열로 단정하므로
					// 뒤에 붙이면 골든을 갱신하지 않고 정보만 더한다. 분자는 대상 색인 3개 중 실재
					// 수다(sources 전체 색인 수를 세면 uri PK의 autoindex가 섞인다).
					fmt.Fprintf(w, "[3] content.db: user_version=%d quick_check=ok indexes=%d/%d\n",
						userVersion, countShadowIndexes(ctx, st.Reader()), len(shadowIndexNames))
					reader = st.Reader()
```

같은 패키지에 헬퍼를 더한다(`internal/cli/cli.go`의 doctor 헬퍼들 근처):

```go
// shadowIndexNames — D73 대상 색인 이름. store의 DDL과 같은 집합을 doctor가 센다. 이름을 여기에
// 복사해 두는 것은 store 내부 변수를 진단이 읽지 않기 위한 것이다(패키지 경계 유지).
var shadowIndexNames = []string{
	"idx_sources_artifact_kind",
	"idx_sources_blobhash_kind",
	"idx_sources_artifact_indexed",
}

// countShadowIndexes — read-only 프로브. 조회 실패는 0으로 센다(진단은 fail-soft).
func countShadowIndexes(ctx context.Context, db *sql.DB) int {
	n := 0
	for _, name := range shadowIndexNames {
		var got string
		if err := db.QueryRowContext(ctx, `SELECT name FROM sqlite_master WHERE type='index' AND name=?`, name).Scan(&got); err == nil {
			n++
		}
	}
	return n
}
```

- [ ] **Step 7: doctor 렌더 테스트를 쓴다**

`internal/cli/cli_test.go`에 추가한다:

```go
// TestDoctorIndexesRender — D73: 병기가 quick_check 뒤에 오고 기존 부분문자열 단정이 그대로
// 통과한다(골든 갱신 없이 정보만 더한다).
func TestDoctorIndexesRender(t *testing.T) {
	out := runDoctorT(t) // 이 파일의 기존 doctor 실행 헬퍼
	if !strings.Contains(out, "user_version=1 quick_check=ok") {
		t.Fatalf("기존 부분문자열 단정이 깨졌다:\n%s", out)
	}
	if !strings.Contains(out, "quick_check=ok indexes=3/3") {
		t.Fatalf("indexes 병기가 quick_check 뒤에 없다:\n%s", out)
	}
}
```

- [ ] **Step 8: EXPLAIN 판정 테스트를 쓴다**

```go
// TestShadowPredicateUsesIndexes — D73: 수치 기록만으로는 색인이 질의 계획에 반영되지 않아도
// 통과하므로, 계획에 sources 전체 스캔이 없음을 단정한다.
func TestShadowPredicateUsesIndexes(t *testing.T) {
	dir := t.TempDir()
	s := openStoreT(t, dir)
	seedShadowFixture(t, s)
	q, args := shadowOwnedFilter(time.Now().Unix(), 100)
	rows, err := s.Reader().Query("EXPLAIN QUERY PLAN "+q, args...)
	if err != nil {
		t.Fatal(err)
	}
	defer func() { _ = rows.Close() }()
	var plan []string
	for rows.Next() {
		var id, parent, notUsed int
		var detail string
		if err := rows.Scan(&id, &parent, &notUsed, &detail); err != nil {
			t.Fatal(err)
		}
		plan = append(plan, detail)
	}
	for _, d := range plan {
		if strings.Contains(d, "SCAN sources") {
			t.Fatalf("sources 전체 스캔이 남아 있다: %q\n전체 계획: %v", d, plan)
		}
	}
}
```

이 테스트는 `internal/store` 패키지에 둔다(`shadowOwnedFilter`가 unexported다).

- [ ] **Step 9: 색인 실패 경로를 단정한다**

```go
// TestEnsureIndexesFailureDoesNotBlockOpen — D73: 색인 DDL 실패는 Open을 막지 않고 경고로
// 남으며, 그 상태에서도 술어가 정확하고 진단의 실재 수가 3보다 작게 관측된다.
func TestEnsureIndexesFailureDoesNotBlockOpen(t *testing.T) {
	dir := t.TempDir()
	s := openStoreT(t, dir)
	seedShadowFixture(t, s)
	want := shadowOwnedHashesT(t, s)
	// 대상 이름 하나를 다른 객체가 선점하게 만들어 CREATE INDEX를 실패시킨다.
	for _, name := range shadowIndexNames {
		if _, err := s.writer.Exec("DROP INDEX IF EXISTS " + name); err != nil {
			t.Fatal(err)
		}
	}
	if _, err := s.writer.Exec(`CREATE TABLE idx_sources_artifact_kind(x)`); err != nil {
		t.Fatal(err)
	}
	closeStoreT(t, s)
	s2 := openStoreT(t, dir) // Open이 실패하지 않아야 한다
	if got := countShadowIndexes(t, s2.Reader()); got >= len(shadowIndexNames) {
		t.Fatalf("indexes=%d — 실패를 유도했는데 전부 생겼다", got)
	}
	if got := shadowOwnedHashesT(t, s2); !reflect.DeepEqual(got, want) {
		t.Fatalf("색인 부분 적용이 술어 결과를 바꿨다: got=%v want=%v", got, want)
	}
}
```

- [ ] **Step 10: 기동 예산을 측정한다**

`internal/store/store_test.go`에 벤치를 더한다:

```go
// BenchmarkOpenWithIndexDDL — D73 기동 예산: alwaysLoad는 서버 연결까지 세션 시작을 막고
// 호스트 5초 타임아웃이 상한이다(D63). v1 픽스처에서 Open→준비 구간을 잰다.
func BenchmarkOpenWithIndexDDL(b *testing.B) {
	for _, rows := range []int{2000, 20000} {
		b.Run(fmt.Sprintf("sources=%d", rows), func(b *testing.B) {
			dir := b.TempDir()
			seedSourcesN(b, dir, rows) // 신규 헬퍼: v1 스키마 + rows개 sources 행, 색인 없음
			b.ResetTimer()
			for i := 0; i < b.N; i++ {
				b.StopTimer()
				dropShadowIndexes(b, dir) // 매 반복 색인 없는 상태로 되돌린다
				b.StartTimer()
				s := openStoreB(b, dir)
				closeStoreB(b, s)
			}
		})
	}
}
```

Run: `go test -p 1 ./internal/store/ -run '^$' -bench BenchmarkOpenWithIndexDDL -benchtime 5x`
Expected: 두 규모 모두 **5초 상한 아래**. 넘으면 색인 생성을 기동 경로에서 빼는 것이 옳은 방향이며 스펙으로 되돌아간다.

- [ ] **Step 11: 술어 전후 비교 수치를 얻어 §3에 기록한다**

```bash
# 색인 있는/없는 상태의 술어 실행 시간(합성 2천·2만 행)
go test -p 1 ./internal/store/ -run '^$' -bench 'BenchmarkShadowPredicate' -benchtime 10x
```

`BenchmarkShadowPredicate`를 같은 형태로 더한다(색인 유/무 서브벤치). 결과를
`docs/context-router-design-v0.13-ko.md`의 `## 3. 관측 실측 기록`에 표로 적는다 — **측정한 값만**
적고 기대값을 쓰지 않는다.

- [ ] **Step 12: 전체 검증 후 커밋한다**

Run: `go test -p 1 -count=1 ./...` → PASS
Run: `gofumpt -l .` → 출력 없음

```bash
git add internal/store/store.go internal/store/store_test.go internal/cli/cli.go internal/cli/cli_test.go docs/context-router-design-v0.13-ko.md
git commit -m "feat(v0.13): shadow 술어 색인 + doctor indexes 병기 (D73)"
```

---

### Task 3: D75 — 홈 유도 격리 공유 헬퍼 (js · csharp · unix shell)

unix `baseKeys()`가 `HOME`을 호스트 값 그대로 통과시키고 shell 러너의 `extra`가 nil이라, sh 스니펫이 부르는 모든 툴체인이 호스트 사용자 구성을 읽는다. `jsEnv`·`csEnv`가 **이미 러너별로 `HOME`을 재지정하므로** shell에 한 줄을 더 넣으면 세 번째 사본이 된다 — 헬퍼로 뽑는다.

**헬퍼 추출이 shell 적용보다 먼저다**(설계 §1.3). 순서를 뒤집으면 중간 커밋이 D13 위반 상태로 남는다.

**Files:**
- Modify: `internal/exec/exec.go` (신규 `homeIsolationEnv`, `jsEnv:325-338` 교체, `csEnv:618-651` 교체, unix shell 갈래 `248-258`에 `extra` 추가)
- Test: `internal/exec/exec_test.go` (신규 2개)

**Interfaces:**
- Produces: `homeIsolationEnv(scratch string) ([]string, error)` — 앵커 세 개와 그 준비물을 소유한다. 준비 실패는 `sandbox.ErrSetup`을 감아 반환한다(fail-closed).
  - `HOME=<scratch>/home` — 그 디렉터리를 만든다
  - `CFFIXED_USER_HOME=<scratch>/home` — `<home>/Library/Application Support`를 만든다. **플랫폼 분기 없이 무조건**(기존 csharp 갈래가 이미 그 형태다)
  - `XDG_DATA_HOME=<scratch>/xdg` — `<xdg>/NuGet/Migrations/1` sentinel을 만든다
- Consumes: `sandbox.ErrSetup`, `sandbox.NewScratch`가 만든 스크래치 루트.

- [ ] **Step 1: 실패 테스트를 쓴다**

```go
// TestHomeIsolationEnvAnchors — D75: 헬퍼가 세 앵커와 준비물을 소유한다. 플랫폼 분기가 없다.
func TestHomeIsolationEnvAnchors(t *testing.T) {
	scratch := t.TempDir()
	env, err := homeIsolationEnv(scratch)
	if err != nil {
		t.Fatalf("homeIsolationEnv: %v", err)
	}
	home := filepath.Join(scratch, "home")
	xdg := filepath.Join(scratch, "xdg")
	for _, want := range []string{"HOME=" + home, "CFFIXED_USER_HOME=" + home, "XDG_DATA_HOME=" + xdg} {
		if !slices.Contains(env, want) {
			t.Fatalf("env에 %q 없음: %v", want, env)
		}
	}
	for _, p := range []string{home, filepath.Join(home, "Library", "Application Support"), filepath.Join(xdg, "NuGet", "Migrations")} {
		if fi, statErr := os.Stat(p); statErr != nil || !fi.IsDir() {
			t.Fatalf("준비물 디렉터리 부재: %s (%v)", p, statErr)
		}
	}
	if _, statErr := os.Stat(filepath.Join(xdg, "NuGet", "Migrations", "1")); statErr != nil {
		t.Fatalf("NuGet sentinel 부재: %v", statErr)
	}
}

// TestHomeIsolationEnvFailClosed — D75: 준비물 생성이 실패하면 실행을 거부한다. 홈이 없으면
// 재지정이 성립하지 않고 그 상태로 실행하면 호스트 홈으로 되돌아갈 수 있다.
func TestHomeIsolationEnvFailClosed(t *testing.T) {
	scratch := t.TempDir()
	// 파일 자리에 디렉터리를 만들 수 없게 해 실제 OS 오류를 낸다(v0.12의 extra fail-closed
	// 픽스처와 같은 방식).
	if err := os.WriteFile(filepath.Join(scratch, "home"), nil, 0o600); err != nil {
		t.Fatal(err)
	}
	env, err := homeIsolationEnv(scratch)
	if !errors.Is(err, sandbox.ErrSetup) {
		t.Fatalf("err=%v want sandbox.ErrSetup", err)
	}
	if env != nil {
		t.Fatalf("env=%v want nil — 격리가 빠진 env를 호출부에 넘기지 않는다", env)
	}
}
```

임포트: `errors` · `os` · `path/filepath` · `slices`.

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 ./internal/exec/ -run TestHomeIsolationEnv -v`
Expected: FAIL — `undefined: homeIsolationEnv`

- [ ] **Step 3: 헬퍼를 만든다**

`internal/exec/exec.go`의 `jsEnv` 앞에 추가한다:

```go
// homeIsolationEnv — D75: 홈 유도 사용자 구성의 **공유 지점**. unix 툴체인의 사용자 구성은 거의
// 전부 $HOME 유도이고(XDG_CONFIG_HOME은 sandbox 허용 표 밖이라 이미 차단되며 그 기본값도
// $HOME/.config다), jsEnv·csEnv가 이미 각자 HOME을 돌리고 있었다 — shell에 여섯 번째 사본을
// 만들지 않고 한 지점으로 모은다(D13). sandbox의 닫힌 표(BaseEnv)로 올리지 않는 이유는 그러면
// python·go 갈래까지 범위가 늘어나는데 그 두 러너는 다른 축으로 격리하기 때문이다.
//
// 부재가 안전하게 퇴화하지 않으므로 **시행점(fail-closed)**이다: 빈 스크래치 홈은 XDG 기본
// 경로의 NuGet Migrations sentinel을 없애 sh 스니펫의 dotnet 호출을 하드코딩 /tmp 뮤텍스 실패
// 경로로 보내고(dotnet/runtime#49822), macOS의 file-based dotnet run은 Application Support가
// 없으면 temp 경로 결정에서 죽는다. 플랫폼 분기를 두지 않는 것은 기존 csharp 갈래가 이미 그
// 형태이기 때문이다 — 분기를 두면 비darwin에서 값이 사라져 동작이 갈린다.
func homeIsolationEnv(scratch string) ([]string, error) {
	home := filepath.Join(scratch, "home")
	xdg := filepath.Join(scratch, "xdg")
	appSupport := filepath.Join(home, "Library", "Application Support")
	migrations := filepath.Join(xdg, "NuGet", "Migrations")
	for _, d := range []string{home, appSupport, migrations} {
		if err := os.MkdirAll(d, 0o700); err != nil {
			slog.Error("exec: 홈 격리 디렉터리 생성 실패", "error", err) // 경로는 로그에만
			return nil, fmt.Errorf("%w: 홈 격리 디렉터리 생성 실패", sandbox.ErrSetup)
		}
	}
	if err := os.WriteFile(filepath.Join(migrations, "1"), nil, 0o600); err != nil {
		slog.Error("exec: NuGet 마이그레이션 sentinel 기록 실패", "error", err)
		return nil, fmt.Errorf("%w: NuGet sentinel 기록 실패", sandbox.ErrSetup)
	}
	return []string{
		"HOME=" + home,
		"CFFIXED_USER_HOME=" + home,
		"XDG_DATA_HOME=" + xdg,
	}, nil
}
```

- [ ] **Step 4: `jsEnv`가 헬퍼를 쓰게 한다**

`internal/exec/exec.go:325-338`을 교체한다. `npmrc`(318-324)는 러너 고유이므로 그대로 둔다:

```go
	shared, err := homeIsolationEnv(scratch)
	if err != nil {
		return nil, err // D75: 홈 준비 실패는 이제 시행점이다(이전에는 경고 후 계속 실행했다)
	}
	// XDG_CONFIG_HOME은 러너 고유다 — bunfig 차단의 레버이고 헬퍼의 XDG_DATA_HOME과 용도·경로가
	// 다르다. 헬퍼가 만든 홈 아래 XDG 기본 배치를 그대로 쓴다.
	xdgConfig := filepath.Join(scratch, "home", ".config")
	if err := os.MkdirAll(xdgConfig, 0o700); err != nil {
		slog.Warn("exec: 스크래치 XDG_CONFIG_HOME 생성 실패 — 격리는 유지(홈 포인터가 호스트 밖을 가리킨다)", "error", err)
	}
	return append(shared,
		"NPM_CONFIG_USERCONFIG="+rc,
		"XDG_CONFIG_HOME="+xdgConfig,
	), nil
```

- [ ] **Step 5: `csEnv`가 헬퍼를 쓰게 한다**

`internal/exec/exec.go:605-660`에서 헬퍼가 소유한 것을 뺀다:
- `userHome`·`appSupport`·`xdg`·`migrations` 지역 변수와 그 `MkdirAll`·sentinel 기록을 제거한다(헬퍼가 한다).
- `MkdirAll` 루프는 dotnet 고유 넷만 남긴다: `home`(=`<scratch>/dotnet`) · `nuget` · `nugetHTTP` · `nugetPlugins`.
- 반환 슬라이스에서 `XDG_DATA_HOME` · `HOME` · `CFFIXED_USER_HOME` 세 줄을 빼고 헬퍼 결과에 append한다:

```go
	shared, err := homeIsolationEnv(scratch)
	if err != nil {
		return nil, err
	}
	return append(shared,
		"DOTNET_CLI_HOME="+home,
		"NUGET_PACKAGES="+nuget,
		"NUGET_HTTP_CACHE_PATH="+nugetHTTP,
		"NUGET_PLUGINS_CACHE_PATH="+nugetPlugins,
		"DOTNET_SKIP_WORKLOAD_INTEGRITY_CHECK=1",
		"DOTNET_GENERATE_ASPNET_CERTIFICATE=false",
		"DOTNET_NOLOGO=1",
		"DOTNET_CLI_TELEMETRY_OPTOUT=1",
		"DOTNET_CLI_DO_NOT_USE_MSBUILD_SERVER=1",
		"MSBUILDDISABLENODEREUSE=1",
		"UseSharedCompilation=false",
	), nil
```

`nuget.config` 기록(640-643)은 러너 고유 시행점이므로 그대로 둔다.

- [ ] **Step 6: unix shell 갈래에 `extra`를 붙인다**

`internal/exec/exec.go:248-258`의 unix 갈래를 교체한다:

```go
	return runner{
		file: "snippet.sh",
		detect: func() (string, string, error) {
			p, err := exec.LookPath("sh")
			if err != nil {
				return "", "", ErrToolchainMissing
			}
			return p, "", nil
		},
		argv: func(bin, file string) []string { return []string{bin, file} },
		// D75: sh 스니펫은 어떤 툴체인이든 부를 수 있어 재지정 대상이 열린 집합이다 — 공유
		// 지점(홈 유도 격리)을 그대로 쓴다. windows 갈래는 자기 PSModulePath·USERPROFILE
		// 재지정을 유지하며 이 헬퍼를 부르지 않는다.
		extra: homeIsolationEnv,
	}
```

- [ ] **Step 7: 러너별 유효 키 집합을 단정한다**

```go
// TestRunnerIsolationKeySets — D75: 통합 후 각 러너의 유효 격리 키 집합을 고정한다. js·shell이
// 얻는 키가 늘어나는 것은 의도된 변경이므로 "기존과 동일"을 요구하지 않는다 — 변화 없음을
// 단정하는 대상은 csharp(이미 세 앵커를 전부 준다)과 windows shell(헬퍼를 부르지 않는다)이다.
func TestRunnerIsolationKeySets(t *testing.T) {
	scratch := t.TempDir()
	anchors := []string{"HOME", "CFFIXED_USER_HOME", "XDG_DATA_HOME"}
	tbl := table()
	for _, lang := range []string{"javascript", "typescript", "csharp"} {
		env, err := tbl[lang].extra(scratch)
		if err != nil {
			t.Fatalf("%s extra: %v", lang, err)
		}
		for _, k := range anchors {
			if !hasEnvKeyT(env, k) {
				t.Fatalf("%s에 앵커 %s 없음", lang, k)
			}
		}
	}
	shell := tbl["shell"]
	if runtime.GOOS == "windows" {
		env, err := shell.extra(scratch)
		if err != nil {
			t.Fatalf("windows shell extra: %v", err)
		}
		if !hasEnvKeyT(env, "PSModulePath") || !hasEnvKeyT(env, "USERPROFILE") {
			t.Fatalf("windows shell 기존 2키가 유지되지 않았다: %v", env)
		}
		for _, k := range anchors {
			if hasEnvKeyT(env, k) {
				t.Fatalf("windows shell이 헬퍼 앵커 %s를 얻었다 — 호출 지점 규정 위반", k)
			}
		}
		return
	}
	env, err := shell.extra(scratch)
	if err != nil {
		t.Fatalf("unix shell extra: %v", err)
	}
	for _, k := range anchors {
		if !hasEnvKeyT(env, k) {
			t.Fatalf("unix shell에 앵커 %s 없음", k)
		}
	}
}

func hasEnvKeyT(env []string, key string) bool {
	for _, e := range env {
		if strings.HasPrefix(e, key+"=") {
			return true
		}
	}
	return false
}
```

**주의**: windows 갈래의 `extra`는 `detect`가 채우는 `psHome`·`pfDir`를 읽으므로, 이 테스트의 windows 분기는 `detect`를 먼저 부른다(`exec.go:227-230` 주석의 계약). `shell.detect()`를 `shell.extra(...)` 앞에 호출한다.

- [ ] **Step 8: 홈 유도 툴체인이 계속 동작하는지 확인한다**

```go
// TestRunShellHomeToolchainStillWorks — D75: 홈 재지정이 sh 스니펫의 홈 유도 툴체인 호출을
// 깨뜨리지 않는다. sentinel 준비가 빠지면 실패하는 경로를 덮는다.
func TestRunShellHomeToolchainStillWorks(t *testing.T) {
	if runtime.GOOS == "windows" {
		t.Skip("unix shell 갈래 전용")
	}
	if _, err := osexec.LookPath("dotnet"); err != nil {
		t.Skip("dotnet 없음")
	}
	resp, err := Run(context.Background(), t.TempDir(), "", Request{
		Language: "shell",
		Code:     "set -e\ndotnet --version\n",
	})
	if err != nil {
		t.Fatalf("Run: %v", err)
	}
	if resp.ExitCode == nil || *resp.ExitCode != 0 {
		t.Fatalf("dotnet --version 실패: exit=%v stderr=%q", resp.ExitCode, resp.Stderr)
	}
}
```

- [ ] **Step 9: 확인하고 커밋한다**

Run: `go test -p 1 -count=1 ./internal/exec/` → PASS
Run: `go test -p 1 -count=1 ./...` → PASS
Run: `gofumpt -l internal/exec/` → 출력 없음

```bash
git add internal/exec/exec.go internal/exec/exec_test.go
git commit -m "feat(v0.13): 홈 유도 격리 공유 헬퍼 + unix shell 적용 (D75)"
```

**CI 3-OS가 unix 실검증 권위다** — 푸시 후 ubuntu·macos 런의 결과를 확인한다. darwin의 `CFFIXED_USER_HOME`·Application Support 경로는 그 런에서만 실제로 검증된다.

---

### Task 4: D76 — PowerShell 네이티브 종료 상황을 `stderr` 한 줄로

`-File`은 `exit`이나 종결 오류가 없으면 0으로 끝나므로, 마지막 줄이 비 0으로 죽은 네이티브 명령이면 `exit_code` 0에 `stderr`까지 비어 실패가 두 채널 어디에도 남지 않는다.

**응답에 새 필드를 만들지 않는다** — go-sdk가 응답 타입에서 `outputSchema`를 생성해 `tools/list`에 실으므로 필드 하나가 게이트 11 상한(13,000B)을 넘긴다(실측 12,914B + 94B).

**`exit_code`의 의미는 불변이다.** tail이 읽는 `$LASTEXITCODE`도 "마지막 **네이티브** 명령"의 값이므로, 보강 줄의 의미는 "스니펫이 실패했다"가 아니라 **"마지막으로 실행된 네이티브 명령이 비 0으로 끝났다"**로 한정된다 — 문면도 그 한정을 그대로 적는다.

**Files:**
- Modify: `internal/exec/exec.go:146-149` (스니펫 기록 — 사이드 파일 경로 전달), `internal/exec/exec.go:194-199` (`snippetContent`), `internal/exec/exec.go:158-176` (응답 조립 뒤 보강)
- Test: `internal/exec/exec_test.go` (신규 1개, 서브테스트 다섯)

**Interfaces:**
- Produces: `snippetContent(fileName, code, sidePath string) []byte` — 셋째 인자가 신규. `.ps1`일 때만 tail을 붙이고 다른 확장자에는 영향이 없다.
- Produces: 보강 줄 문면 — `context-router: 마지막 네이티브 명령이 종료 코드 <n>으로 끝났습니다(exit_code는 스니펫의 종료 상태입니다).`

- [ ] **Step 1: 실패 테스트를 쓴다**

```go
// TestShellNativeExitAugmentation — D76: exit_code 0에 stderr가 빈 상황에서만 마지막 네이티브
// 명령의 종료 상황을 stderr 한 줄로 낸다. exit_code의 의미는 바뀌지 않는다.
func TestShellNativeExitAugmentation(t *testing.T) {
	if runtime.GOOS != "windows" {
		t.Skip("windows shell 갈래 전용(PowerShell -File 계약)")
	}
	run := func(t *testing.T, code string) Response {
		t.Helper()
		resp, err := Run(context.Background(), t.TempDir(), "", Request{Language: "shell", Code: code})
		if err != nil {
			t.Fatalf("Run: %v", err)
		}
		return resp
	}
	const marker = "마지막 네이티브 명령이 종료 코드"

	t.Run("마지막_줄이_비0_네이티브", func(t *testing.T) {
		resp := run(t, "cmd /c exit 5\n")
		if resp.ExitCode == nil || *resp.ExitCode != 0 {
			t.Fatalf("exit_code=%v want 0(계약 불변)", resp.ExitCode)
		}
		if !strings.Contains(resp.Stderr, marker) {
			t.Fatalf("보강 줄 없음: stderr=%q", resp.Stderr)
		}
	})

	t.Run("성공한_cmdlet_뒤_거짓실패_금지", func(t *testing.T) {
		resp := run(t, "cmd /c exit 5\nWrite-Output 'ok'\n")
		if resp.ExitCode == nil || *resp.ExitCode != 0 {
			t.Fatalf("exit_code=%v want 0 — 거짓 실패를 만들지 않는다", resp.ExitCode)
		}
		// 잠긴 값이므로 이 케이스에도 보강 줄이 나온다(의미는 마지막 네이티브 명령 한정이다).
		if !strings.Contains(resp.Stderr, marker) {
			t.Fatalf("보강 줄 없음: stderr=%q", resp.Stderr)
		}
	})

	t.Run("인수없는_exit은_보강되지_않는다", func(t *testing.T) {
		resp := run(t, "cmd /c exit 5\nexit\n")
		if strings.Contains(resp.Stderr, marker) {
			t.Fatalf("tail이 실행되지 않아야 하는데 보강 줄이 있다: %q", resp.Stderr)
		}
		// 대조: 마지막 exit만 제거하면 보강 줄이 나온다 — 이 대조가 없으면 위 단정은 tail이
		// 전혀 동작하지 않아도 통과한다.
		ctrl := run(t, "cmd /c exit 5\n")
		if !strings.Contains(ctrl.Stderr, marker) {
			t.Fatalf("대조 케이스에 보강 줄이 없다: %q", ctrl.Stderr)
		}
	})

	t.Run("코드를_명시한_exit", func(t *testing.T) {
		resp := run(t, "cmd /c exit 5\nexit 3\n")
		if resp.ExitCode == nil || *resp.ExitCode != 3 {
			t.Fatalf("exit_code=%v want 3 — 스니펫이 명시한 코드를 그대로 낸다", resp.ExitCode)
		}
	})

	t.Run("stderr가_비어있지_않으면_넣지_않는다", func(t *testing.T) {
		resp := run(t, "cmd /c exit 5\n[Console]::Error.WriteLine('boom')\n")
		if strings.Count(resp.Stderr, marker) != 0 {
			t.Fatalf("stderr가 이미 비어 있지 않은데 보강했다: %q", resp.Stderr)
		}
	})

	t.Run("여러줄_문자열로_끝나는_스니펫", func(t *testing.T) {
		// here-string으로 끝나고 마지막 개행이 없는 형태 — tail이 구문을 깨지 않아야 한다.
		resp := run(t, "cmd /c exit 5\n$x = @'\nline\n'@")
		if resp.ExitCode == nil || *resp.ExitCode != 0 {
			t.Fatalf("tail이 구문을 깼다: exit=%v stderr=%q", resp.ExitCode, resp.Stderr)
		}
		if !strings.Contains(resp.Stderr, marker) {
			t.Fatalf("보강 줄 없음: stderr=%q", resp.Stderr)
		}
	})
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 ./internal/exec/ -run TestShellNativeExitAugmentation -v`
Expected: FAIL — 보강 줄이 없다(마지막 서브테스트 둘은 통과할 수 있다).

- [ ] **Step 3: `snippetContent`에 tail을 더한다**

`internal/exec/exec.go:194-199`를 교체한다:

```go
// ctrNativeExitFile — D76 사이드 파일 이름. 런별 스크래치 절대 경로로 고정한다(스니펫이 작업
// 디렉터리를 바꾸거나 같은 상대 경로에 써도 값에 개입하지 않게).
const ctrNativeExitFile = "ctr-native-exit"

// snippetContent — .ps1에는 UTF-8 BOM(기존 계약)과 D76 tail을 붙인다.
//
// tail 제약 셋: ① 스니펫의 마지막 개행이 보장되지 않으므로 개행을 **선행**시킨다.
// ② 기록에 프로바이더 cmdlet(Set-Content·Out-File)을 쓰지 않는다 — 5.1의 무반환 정지 경로가
// 실측됐다. ③ 변수명을 스니펫과 충돌하지 않게 고유하게 둔다.
// 스니펫이 exit으로 끝나면 tail은 실행되지 않는다 — 코드를 명시한 exit이면 exit_code가 그
// 값이고, 인수 없는 exit은 0을 반환해 네이티브 실패가 가려지지만 이 보강이 덮지 못하는 알려진
// 공백이다(설계 v0.13 §1.2).
func snippetContent(fileName, code, sidePath string) []byte {
	if !strings.HasSuffix(fileName, ".ps1") {
		return []byte(code)
	}
	tail := "\n$ctrNativeExitCode = $LASTEXITCODE\n" +
		"[System.IO.File]::WriteAllText('" + strings.ReplaceAll(sidePath, "'", "''") + "', [string]$ctrNativeExitCode)\n"
	out := append([]byte{0xEF, 0xBB, 0xBF}, code...)
	return append(out, tail...)
}
```

- [ ] **Step 4: 호출부에서 사이드 파일 경로를 넘긴다**

`internal/exec/exec.go:146-149`를 교체한다:

```go
	file := filepath.Join(scratch, r.file)
	sidePath := filepath.Join(scratch, ctrNativeExitFile)
	if err := os.WriteFile(file, snippetContent(r.file, req.Code, sidePath), 0o600); err != nil {
		return Response{}, fmt.Errorf("%w: 스니펫 기록 실패", sandbox.ErrSetup)
	}
```

- [ ] **Step 5: 응답 조립 뒤에 보강을 넣는다**

`internal/exec/exec.go`의 `return resp, nil`(176행) **앞에** 넣는다:

```go
	// D76: exit_code가 0이고 stderr가 빈 상황에서만 네이티브 종료 상황을 한 줄 남긴다 — 원래
	// 비어 있던 자리에만 들어가므로 사용자 출력과 섞이지 않고, 응답 스키마도 바뀌지 않는다
	// (필드를 더하면 outputSchema가 tools/list 표면을 넘긴다). tail이 읽는 $LASTEXITCODE는
	// "마지막 네이티브 명령"의 값이므로 문면도 그 한정을 그대로 적는다.
	if resp.ExitCode != nil && *resp.ExitCode == 0 && resp.Stderr == "" {
		if n, ok := readNativeExitCode(sidePath); ok && n != 0 {
			resp.Stderr = fmt.Sprintf(
				"context-router: 마지막 네이티브 명령이 종료 코드 %d으로 끝났습니다(exit_code는 스니펫의 종료 상태입니다).\n", n)
		}
	}
	return resp, nil
}

// readNativeExitCode — D76 사이드 파일 읽기. 파일 부재(tail 미실행)·빈 값($LASTEXITCODE 미설정,
// 즉 네이티브 명령이 없었음)·파싱 실패는 모두 "보강하지 않음"이다.
func readNativeExitCode(path string) (int, bool) {
	b, err := os.ReadFile(path)
	if err != nil {
		return 0, false
	}
	s := strings.TrimSpace(string(b))
	if s == "" {
		return 0, false
	}
	n, err := strconv.Atoi(s)
	if err != nil {
		return 0, false
	}
	return n, true
}
```

같은 파일 임포트에 `strconv`를 더한다.

- [ ] **Step 6: 게이트 11 무회귀를 확인한다**

Run: `go test -p 1 ./internal/mcp/ -run 'TestToolsListSize|TestSchemaSurface'` (게이트 11 테스트 — `mcp_test.go:247`의 13,000B 상한을 단정하는 테스트)
Expected: PASS — 응답 구조체를 바꾸지 않았으므로 표면이 변하지 않는다.

- [ ] **Step 7: 전체 검증 후 커밋한다**

Run: `go test -p 1 -count=1 ./internal/exec/` → PASS
Run: `go test -p 1 -count=1 ./...` → PASS
Run: `gofumpt -l internal/exec/` → 출력 없음

```bash
git add internal/exec/exec.go internal/exec/exec_test.go
git commit -m "feat(v0.13): PowerShell 네이티브 종료 상황을 stderr 한 줄로 보강 (D76)"
```

---

### Task 5: CHANGELOG v0.13 절 + 릴리스

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `internal/buildinfo/buildinfo.go:9`

**Interfaces:**
- Consumes: Task 2~4의 사용자 관측 가능한 변경
- Produces: 없음

- [ ] **Step 1: CHANGELOG 절을 쓴다**

`CHANGELOG.md`의 가장 최근 절 **앞에** 넣는다(날짜는 릴리스 당일):

```markdown
## [0.13.0] — <YYYY-MM-DD>

### Changed — 사용자 관측 가능한 동작 변경

- **shell 러너(unix)와 javascript·typescript 러너가 스크래치 홈을 쓴다** (D75).
  sh 스니펫에서 `~`와 `$HOME`이 스크래치를 가리키므로 그 안에서 부르는 툴체인이 호스트 사용자
  구성을 읽지 않는다. **홈 유도 툴체인 루트와 캐시도 함께 이전되어 스니펫이 의존물을 다시
  받는다** — 실행 시간이 늘고 네트워크 접근이 발생할 수 있다.
- **javascript·typescript 러너에서 홈 준비 실패가 실행 거부로 바뀌었다** (D75).
  이전에는 경고만 남기고 실행했다. 홈이 없으면 재지정이 성립하지 않고 그 상태로 실행하면
  호스트 홈으로 되돌아갈 수 있다.
- **shell 러너(windows)가 마지막 네이티브 명령의 종료 상황을 `stderr`에 한 줄로 남긴다** (D76).
  `exit_code`가 0이고 `stderr`가 빈 경우에만 들어가며 `exit_code`의 의미는 바뀌지 않는다.
  이 줄은 "스니펫이 실패했다"가 아니라 "마지막으로 실행된 네이티브 명령이 비 0으로 끝났다"를
  뜻한다. 스니펫이 `exit`으로 끝나면 이 보강은 동작하지 않는다 — 종료 코드를 명시하는 것이
  그 경우의 대응이다.
- **doctor `[3]`에 대상 색인의 실재 수가 병기된다** (D73): `indexes=<실재>/3`.

### Internal

- shadow 보존 술어에 복합 색인 3개를 더했다 (D73). **스키마 버전은 그대로 1이다** — 색인은
  가산적 변경이라 이 릴리스의 스토어를 이전 버전 바이너리도 계속 열 수 있다.
- 기동 purge의 파일 회수 루프가 종료 신호를 관측한다 (D74).

[0.13.0]: https://github.com/wotjr1649/context-router/compare/v0.12.1...v0.13.0
```

링크 정의는 파일 말미의 링크 블록에 넣는다.

- [ ] **Step 2: `productVersion`을 올린다**

`internal/buildinfo/buildinfo.go:9`:

```go
var productVersion = "0.13.0"
```

- [ ] **Step 3: 검증한다**

Run: `go run ./cmd/context-router version` → `0.13.0`
Run: `go build ./... && go vet ./... && gofumpt -l .` → 성공, 출력 없음
Run: `go test -p 1 -count=1 ./...` → PASS

- [ ] **Step 4: 커밋한다 (태그는 사용자 승인 후)**

```bash
git add CHANGELOG.md internal/buildinfo/buildinfo.go
git commit -m "release(v0.13.0): CHANGELOG 절 + productVersion 0.13.0"
```

**태그와 푸시는 사용자 명시 승인으로만 한다.**

---

## Self-Review

**1. 스펙 커버리지** — D73(Task 2) · D74(Task 1) · D75(Task 3) · D76(Task 4) 전부 태스크가 있다.
§2 검증 항목 대응: D73 ①~⑩(Task 2 Step 1·7·8·9·10·11 — ④ `user_version` 무변화, ⑥ EXPLAIN, ⑦ 예산 측정, ⑨ 렌더, ⑩ DDL 실패) · D74(Task 1 Step 1, 회수 루프 중간 취소 형태) · D75 ①~⑤(Task 3 Step 1·7·8 — ③ 홈 유도 툴체인 동작, ④ 러너별 fail-closed, ⑤ 키 집합 열거와 windows shell 포함) · D76 ①~⑥(Task 4 Step 1의 서브테스트 여섯 + Step 6 게이트 재측정).

**2. 미확정으로 남긴 것** — Task 2 Step 10·11의 측정 **수치**. 설계 §3이 "측정 전이므로 기대값을 미리 적지 않는다"고 규정했고 이 계획도 상한 판정 기준(5초)만 적었다. 벤치 헬퍼(`seedSourcesN`·`dropShadowIndexes`·`openStoreB`)는 이름과 역할을 명시했고 구현은 그 태스크 안이다.

**3. 타입 일관성** — `homeIsolationEnv(scratch string) ([]string, error)`가 Task 3의 Step 3·4·5·6에서 같은 시그니처로 쓰인다(`runner.extra`의 타입과 동일하므로 unix shell에서 값으로 바로 대입한다). `snippetContent`의 3인자 형태가 Step 3·4에서 일치한다. `shadowIndexNames`는 store와 cli 두 패키지에 각각 두되 같은 세 이름이고, Task 2 Step 3·6·7이 그 집합을 공유한다.

**4. 순서 의존** — Task 1·2는 `internal/store/store.go`를 함께 만지므로 순차다(Task 1이 먼저 — 더 작고 독립적). Task 3·4는 `internal/exec/exec.go`를 함께 만지므로 순차다(Task 3이 먼저 — §1.3의 "헬퍼 추출이 shell 적용보다 먼저"를 Task 3 내부 스텝 순서로도 지킨다). Task 5는 전부 뒤다.

**5. 두 계획의 관계** — `2026-07-27-v0.12.1-d68-d72.md`와 이 계획 사이에 코드 의존은 없다. 다만 CHANGELOG를 둘 다 만지므로, v0.12.1을 먼저 배송하면 이 계획의 Task 5는 그 절 위에 새 절을 얹는다.
