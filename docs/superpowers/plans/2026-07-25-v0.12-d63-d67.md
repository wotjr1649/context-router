# Context Router v0.12 (D63–D67) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 채택 계측을 해석 가능하게 고치고, 설치·승인을 원커맨드 멱등 경로로 옮기고, 툴체인 구성 격리를 전 러너로 일반화하고, shell 종료코드 계약과 저장 보존 정책을 코드로 고정한다.

**Architecture:** 기존 CLI(`internal/cli`)·러너(`internal/exec`)·샌드박스(`internal/sandbox`)·저장소(`internal/store`)에 얹는 델타다. 새 서브시스템을 만들지 않는다. 설치 병합은 `hook_install.go`가 이미 가진 **멱등 병합 + 관리 블록 + 버전 마커** 패턴을 그대로 따르되, `.mcp.json`·승인 키를 다루는 부분은 새 파일로 분리한다(`hook_install.go`는 이미 819줄).

**Tech Stack:** Go 1.26, `database/sql` + SQLite(modernc 계열), `encoding/json`, 표준 `os/exec`. 새 의존성 없음.

## Global Constraints

설계서 `docs/context-router-design-v0.12-ko.md`가 상위 계약이다. 아래는 모든 태스크에 암묵적으로 포함된다.

- **테스트 실행**: `go test -p 1 ./...` (병렬 패키지 금지 — 메모리 상한 규율). 단일 패키지는 `go test -p 1 -count=1 ./internal/<pkg>/`.
- **포맷**: `gofumpt -l -w <touched files>`. 커밋 전 필수.
- **커밋**: `git add <명시 경로>` 만 사용한다. **`git add -A` 금지**(로컬 설정 `.claude/`·`.mcp.json`이 untracked·gitignored 상태로 존재).
- **파일 출력**: UTF-8 (BOM 없음), LF.
- **오류 메시지에 절대경로를 담지 않는다** — `os.ReadFile`의 `*PathError`를 그대로 반환하지 말고 자체 메시지로 감싼다(기존 §12 canary 규율, `mergeSettingsFile:414` 선례).
- **입력 원문 에코 금지**(I3) — 사용자 입력을 오류 메시지에 되싣지 않는다.
- **게이트 11**: `tools/list` 직렬화 ≤ **13,000B**. 현재 실측 **12,914B**로 여유 **86B**뿐이다. 도구 스키마 문면을 늘리는 변경은 이 계획에 없다. 늘려야 하면 먼저 `internal/mcp/mcp_test.go:245` 주석의 실측값을 갱신하고 상한 재기준화를 별도로 논의한다.
- **자기정의 인터페이스 금지**(D13) — 구체 타입을 그대로 쓴다. 테스트용 seam이 필요하면 함수 파라미터(예: `getenv func(string) string`)로 주입한다(`storeWarnBytes:39` 선례).
- **unix 검증 권위는 3-OS CI**다. Windows 로컬에서 컴파일되지 않는 코드(landlock/SBPL 경로)는 CI가 유일한 검증 수단이다. PR 후 `gh pr checks <n>`을 **전체** 확인한다(tail 잘림 금지).
- **BASE**: 각 태스크의 리뷰 기준 커밋은 실행 원장에 기록한다. `HEAD~1`을 쓰지 않는다.

## File Structure

| 파일 | 책임 | 태스크 |
| --- | --- | --- |
| `internal/cli/cli.go` | `runUsageAdoption`(527), `runDoctor`(1350), `runPurgeHookOnly`(895) 수정 | T1, T5, T10, T11 |
| `internal/cli/mcp_install.go` | **신규** — `.mcp.json` 병합, `enabledMcpjsonServers` 스코프 판정, 승인 진단 | T2, T3, T5 |
| `internal/cli/mcp_install_test.go` | **신규** — 위의 단위 테스트 | T2, T3, T5 |
| `internal/cli/hook_install.go` | `runHookInstall`(481)에 MCP 병합 배선, hostSnippet 문면 개정 | T4, T6 |
| `internal/exec/exec.go` | `goEnv`(348), `pyEnv`(331), `detectJS`/`tsRunner` 구성 경로 고정 | T7, T8 |
| `internal/exec/exec_test.go` | 러너 구성 격리 검증, shell 종료코드 계약 고정 | T7, T8, T10 |
| `internal/sandbox/sandbox.go` | `baseKeys`(118)의 `PSModulePath` 처리 | T9 |
| `internal/store/` | shadow purge의 나이 필터·VACUUM 분리 | T11 |
| `cmd/context-router/main.go` | 기동 경로(`SweepStale` 호출부 455)에 자동 purge 배선 | T12 |

**분리 근거**: `.mcp.json`과 승인 키는 훅 설정과 다른 파일·다른 스키마·다른 실패 모드를 갖는다. `hook_install.go`에 더 얹으면 900줄을 넘어 한 컨텍스트에서 다루기 어려워진다.

**단일 계획을 유지하는 이유**: 5개 D는 서로 독립적이지만 모두 기존 파일에 대한 작은 델타이고 하나의 릴리스 단위다. v0.11(D58–D62 → T1–T11)과 같은 형태다. 태스크 경계에서 개별 승인·거부가 가능하다.

---

### Task 1: 채택 계측을 서버 네임스페이스·세션 분모로 교정 (D63 ①)

**왜 먼저인가**: 설계서 §1.3이 이 태스크를 D63의 나머지보다 **선행**으로 못박았다. 계측을 고치기 전에 상시 로드를 켜면 전후 비교의 근거가 사라진다.

**현재 결함**: `runUsageAdoption`은 `strings.Contains(s, "ctr_")`로 분류한다. `mcp__ctr-exec__ctr_execute`에도 `ctr_`가 들어 있어 **두 서버가 한 카운터로 합쳐진다**. 또 세션 분모가 없어 "한 세션에서 몰아 쓴 것"과 "여러 세션에서 고루 쓴 것"이 구분되지 않는다.

**Files:**
- Modify: `internal/cli/cli.go:527-566` (`runUsageAdoption`)
- Test: `internal/cli/cli_test.go` (기존 파일에 추가)

**Interfaces:**
- Consumes: `openSessionDBReadOnly(storeRoot, projectRoot string) (*sql.DB, error)` — 기존 함수(521행 근처), 변경 없음.
- Produces: `mcpServerOf(summary string) (server string, ok bool)` — 이후 태스크(T5 진단)가 같은 파싱 규칙을 재사용한다.

**실데이터 형식**(계획 작성 시 실측):

```
summary 예시:  "Read" / "Bash: cd" / "mcp__plugin_ctxscribe_mcp__ctx_execute" / "mcp__ctr-exec__ctr_execute"
sessions=193, tool_call이 있는 session_id=59, mcp__ctr 계열이 있는 session=5, ctx_ 계열이 있는 session=42
```

- [ ] **Step 1: 파싱 함수의 실패 테스트를 쓴다**

`internal/cli/cli_test.go`에 추가:

```go
// TestMCPServerOf: summary에서 MCP 서버 네임스페이스를 뽑는다. 서버 이름에 밑줄이 있어도
// 첫 "__" 구분자에서 끊는다(mcp__<server>__<tool>).
func TestMCPServerOf(t *testing.T) {
	cases := []struct {
		in     string
		server string
		ok     bool
	}{
		{"mcp__ctr-exec__ctr_execute", "ctr-exec", true},
		{"mcp__ctr__ctr_search", "ctr", true},
		{"mcp__plugin_ctxscribe_mcp__ctx_execute", "plugin_ctxscribe_mcp", true},
		{"Read", "", false},
		{"Bash: cd", "", false},
		{"mcp__", "", false},
		{"mcp__onlyserver", "", false},
	}
	for _, c := range cases {
		got, ok := mcpServerOf(c.in)
		if got != c.server || ok != c.ok {
			t.Errorf("mcpServerOf(%q)=(%q,%v) want (%q,%v)", c.in, got, ok, c.server, c.ok)
		}
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run TestMCPServerOf ./internal/cli/`
Expected: FAIL — `undefined: mcpServerOf`

- [ ] **Step 3: 파싱 함수를 구현한다**

`internal/cli/cli.go`의 `runUsageAdoption` 바로 위에 추가:

```go
// mcpServerOf: "mcp__<server>__<tool>" 형태의 도구 이름에서 서버 네임스페이스를 뽑는다.
// 서버 세그먼트에는 밑줄이 들어갈 수 있으므로(예: plugin_ctxscribe_mcp) 접두 제거 후
// 첫 "__"에서 끊는다. MCP 도구가 아니면 ok=false.
func mcpServerOf(summary string) (string, bool) {
	const prefix = "mcp__"
	if !strings.HasPrefix(summary, prefix) {
		return "", false
	}
	rest := summary[len(prefix):]
	i := strings.Index(rest, "__")
	if i <= 0 || i+2 >= len(rest) {
		return "", false
	}
	return rest[:i], true
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 -run TestMCPServerOf ./internal/cli/`
Expected: PASS

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/cli/cli.go internal/cli/cli_test.go
git add internal/cli/cli.go internal/cli/cli_test.go
git commit -m "feat(v0.12): MCP 서버 네임스페이스 파싱 (D63)"
```

- [ ] **Step 6: 집계 출력의 실패 테스트를 쓴다**

`runUsageAdoption`은 세션 DB를 연다. **DDL을 새로 쓰지 않는다** — `cli_test.go:460` 근처에 이미 `session_events`에 행을 넣는 코드가 있으므로 그 삽입 패턴을 그대로 재사용한다.

경로 규칙은 `openSessionDBReadOnly`(cli.go:514-522)가 정한다:

```
<storeRoot>/projects/<ProjectID>/worktrees/<WorktreeID>/session.db
```

`ident.Canonicalize(projectRoot)`가 `ProjectID`·`WorktreeID`를 준다. 테스트는 `storeRoot`·`projectRoot` 모두 `t.TempDir()`을 쓰고, `Canonicalize` 결과로 위 디렉터리를 만든 뒤 `session.Open`으로 스키마를 생성한다(기존 세션 테스트와 같은 방식). 스키마는 이미 `sessions`와 `session_events`를 만들며 후자에 `session_id`·`event_type`·`ts`·`summary`가 있다.

```go
// TestUsageAdoptionBucketsByServer: 같은 도구 접두를 공유하는 두 서버가 분리 집계되고,
// 세션 분모가 "도구 호출이 하나라도 있는 세션 수"로 계산된다.
func TestUsageAdoptionBucketsByServer(t *testing.T) {
	// s1: ctr-exec만 사용, s2: ctxscribe만 사용, s3: 둘 다, s4: 비-MCP 도구만
	// 기대 출력:
	//   서버별 호출 수 행에 ctr-exec와 plugin_ctxscribe_mcp가 각각 나타난다
	//   세션 분모=4, ctr 계열 세션=2
	var buf bytes.Buffer
	root := newAdoptionTestDB(t, /* sessions */ nil, /* events */ nil) // 위 헬퍼로 채운다
	if err := runUsageAdoption(context.Background(), &buf, root, root, 0, time.Now()); err != nil {
		t.Fatalf("runUsageAdoption: %v", err)
	}
	out := buf.String()
	for _, want := range []string{"ctr-exec", "plugin_ctxscribe_mcp", "sessions"} {
		if !strings.Contains(out, want) {
			t.Errorf("출력에 %q 없음:\n%s", want, out)
		}
	}
	if strings.Contains(out, "\nctr\t") {
		t.Errorf("서버 구분 없는 합산 행이 남아 있다:\n%s", out)
	}
}
```

- [ ] **Step 7: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run TestUsageAdoptionBucketsByServer ./internal/cli/`
Expected: FAIL — 출력에 서버 이름이 없고 `ctr` 합산 행이 남아 있다.

- [ ] **Step 8: 집계를 교정한다**

`runUsageAdoption` 본문을 다음 계약으로 바꾼다.

1. 기존 쿼리(`SELECT summary, COUNT(*) ... GROUP BY summary`)는 유지한다.
2. 행마다 `mcpServerOf(summary)`로 서버를 뽑아 `map[string]int64`에 누적한다. MCP가 아닌 도구는 별도 `nonMCP` 카운터로 합산한다.
3. 세션 지표를 두 개 더 조회한다(같은 `days` 필터를 적용한다):

```go
// 분모: 도구 호출이 하나라도 있는 세션 수(빈 세션 제외 — sessions 테이블 전체가 아니다).
const qDenom = `SELECT COUNT(DISTINCT session_id) FROM session_events WHERE event_type='tool_call'`
// 분자: 서버별로, 그 서버 도구를 한 번이라도 부른 세션 수.
const qPerServer = `SELECT session_id, summary FROM session_events WHERE event_type='tool_call'`
```

`qPerServer` 결과를 `map[server]map[sessionID]struct{}`로 모아 서버별 고유 세션 수를 센다. 행 수가 커질 수 있으므로 `summary`만 파싱하고 문자열을 보관하지 않는다.

4. 출력을 다음 형식으로 바꾼다(탭 구분 유지 — 기존 소비자와 동형):

```go
fmt.Fprintln(w, "server\tcalls\tsessions")
// 서버별 행(호출 수 내림차순), 그다음:
fmt.Fprintf(w, "# tool_call 세션 분모: %d\n", denom)
fmt.Fprintf(w, "# 비-MCP 도구 호출: %d\n", nonMCP)
```

5. `rows.Err()`를 반드시 확인한다 — 기존 코드에 누락돼 있고 이월 Minor에도 올라 있다:

```go
if err := rows.Err(); err != nil {
	return errors.New("usage: 집계 조회 중단")
}
```

6. `days < 0`이면 오류를 반환한다(이월 Minor의 음수 days 항목):

```go
if days < 0 {
	return errors.New("usage: --days는 음수일 수 없습니다")
}
```

- [ ] **Step 9: 통과를 확인한다**

Run: `go test -p 1 -count=1 ./internal/cli/`
Expected: PASS (기존 테스트 포함 전부)

- [ ] **Step 10: 실데이터로 손검증한다**

Run: `context-router usage --adoption`
Expected: `ctr-exec`와 `plugin_ctxscribe_mcp`가 별도 행으로 나오고, 세션 분모 행이 보인다. 계획 작성 시점 실측은 분모 59 / `mcp__ctr` 계열 세션 5 / `ctx_` 계열 42였다. 값이 그 부근이면 정상이다.

- [ ] **Step 11: 커밋한다**

```bash
gofumpt -l -w internal/cli/cli.go internal/cli/cli_test.go
git add internal/cli/cli.go internal/cli/cli_test.go
git commit -m "feat(v0.12): adoption 계측을 서버 네임스페이스·세션 분모로 교정 (D63)"
```

---

### Task 2: `.mcp.json` 멱등 병합기 (D64)

**Files:**
- Create: `internal/cli/mcp_install.go`
- Create: `internal/cli/mcp_install_test.go`

**Interfaces:**
- Consumes: `atomicWriteFile(path string, data []byte) error` — `hook_install.go:372`, 그대로 사용한다.
- Produces:
  - `mergeMCPServers(existing []byte, name string, entry mcpServerEntry, install bool) ([]byte, error)`
  - `type mcpServerEntry struct { Command string; Args []string; AlwaysLoad bool }`
  - T6이 이 둘을 호출한다.

**계약**:
- 대상 파일은 프로젝트 루트의 `.mcp.json`이고 **gitignored**다(저장소에 커밋되지 않는다).
- 표준 형태는 **단일 서버**다. 설계서 D63 ②가 `ctr`·`ctr-exec` 이중 등록을 금지했다. 서버 이름은 이 태스크에서 상수로 고정한다.
- 우리 항목만 갱신하고 **다른 서버 항목은 보존**한다(`mergeHookSettings`의 소유 판정과 같은 철학).
- 멱등: 같은 입력으로 두 번 병합하면 바이트가 동일해야 한다.

- [ ] **Step 1: 실패 테스트를 쓴다**

`internal/cli/mcp_install_test.go`:

```go
package cli

import (
	"encoding/json"
	"testing"
)

// TestMergeMCPServersIdempotent: 두 번 병합해도 바이트가 같고, 남의 서버 항목은 보존된다.
func TestMergeMCPServersIdempotent(t *testing.T) {
	existing := []byte(`{"mcpServers":{"other":{"command":"x","args":[]}}}`)
	entry := mcpServerEntry{Command: "context-router", Args: []string{"--enable", "exec"}, AlwaysLoad: true}

	first, err := mergeMCPServers(existing, ctrMCPServerName, entry, true)
	if err != nil {
		t.Fatalf("merge 1: %v", err)
	}
	second, err := mergeMCPServers(first, ctrMCPServerName, entry, true)
	if err != nil {
		t.Fatalf("merge 2: %v", err)
	}
	if string(first) != string(second) {
		t.Errorf("멱등 위반:\n1: %s\n2: %s", first, second)
	}

	var got struct {
		Servers map[string]struct {
			Command    string   `json:"command"`
			Args       []string `json:"args"`
			AlwaysLoad bool     `json:"alwaysLoad"`
		} `json:"mcpServers"`
	}
	if err := json.Unmarshal(first, &got); err != nil {
		t.Fatalf("결과 파싱: %v", err)
	}
	if _, ok := got.Servers["other"]; !ok {
		t.Errorf("남의 서버 항목이 사라졌다: %s", first)
	}
	ours, ok := got.Servers[ctrMCPServerName]
	if !ok {
		t.Fatalf("우리 항목이 없다: %s", first)
	}
	if ours.Command != "context-router" || !ours.AlwaysLoad {
		t.Errorf("우리 항목 내용 불일치: %+v", ours)
	}
}

// TestMergeMCPServersUninstallKeepsOthers: install=false면 우리 항목만 지운다.
func TestMergeMCPServersUninstallKeepsOthers(t *testing.T) {
	existing := []byte(`{"mcpServers":{"other":{"command":"x"},"` + ctrMCPServerName + `":{"command":"context-router"}}}`)
	out, err := mergeMCPServers(existing, ctrMCPServerName, mcpServerEntry{}, false)
	if err != nil {
		t.Fatalf("merge: %v", err)
	}
	var got struct {
		Servers map[string]json.RawMessage `json:"mcpServers"`
	}
	if err := json.Unmarshal(out, &got); err != nil {
		t.Fatalf("파싱: %v", err)
	}
	if _, ok := got.Servers[ctrMCPServerName]; ok {
		t.Errorf("우리 항목이 남아 있다: %s", out)
	}
	if _, ok := got.Servers["other"]; !ok {
		t.Errorf("남의 항목이 지워졌다: %s", out)
	}
}

// TestMergeMCPServersRejectsMalformed: 깨진 JSON은 오류이며 메시지에 경로가 없다.
func TestMergeMCPServersRejectsMalformed(t *testing.T) {
	_, err := mergeMCPServers([]byte(`{"mcpServers":`), ctrMCPServerName, mcpServerEntry{Command: "x"}, true)
	if err == nil {
		t.Fatal("깨진 JSON에 오류가 없다")
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run TestMergeMCPServers ./internal/cli/`
Expected: FAIL — `undefined: mergeMCPServers`, `undefined: mcpServerEntry`, `undefined: ctrMCPServerName`

- [ ] **Step 3: 병합기를 구현한다**

`internal/cli/mcp_install.go`:

```go
// Package-level: .mcp.json(MCP 서버 등록) 병합. hook_install.go의 멱등 병합·소유 판정
// 철학을 따르되 대상 파일·스키마가 달라 분리한다(설계 v0.12 D64).
package cli

import (
	"encoding/json"
	"errors"
	"path/filepath"
)

// ctrMCPServerName — .mcp.json에 등록하는 서버 이름. 설계 D63 ②가 단일 서버 등록을
// 표준으로 고정했다. 이름이 바뀌면 permission 규칙 접두와 doctor 안내 문면이 함께
// 움직이므로 이 상수 한 곳에서만 정한다.
const ctrMCPServerName = "ctr-exec"

// mcpServerEntry — .mcp.json의 stdio 서버 항목. alwaysLoad는 호스트가 도구를 세션
// 시작 시 상주시키게 한다(지연 로드 면제).
type mcpServerEntry struct {
	Command    string   `json:"command"`
	Args       []string `json:"args"`
	AlwaysLoad bool     `json:"alwaysLoad,omitempty"`
}

// mcpConfigPath — 프로젝트 루트의 .mcp.json 경로.
func mcpConfigPath(projectRoot string) string {
	return filepath.Join(projectRoot, ".mcp.json")
}

// mergeMCPServers — existing(빈 슬라이스 허용)에 name 항목을 install 여부에 따라
// 반영한 JSON을 반환한다. 다른 서버 항목은 원문 그대로 보존한다(json.RawMessage).
// 키 순서가 결정적이도록 map을 그대로 마샬링한다(encoding/json이 키를 정렬한다) —
// 멱등 비교가 바이트 단위로 성립하는 근거다.
func mergeMCPServers(existing []byte, name string, entry mcpServerEntry, install bool) ([]byte, error) {
	doc := map[string]json.RawMessage{}
	if len(existing) > 0 {
		if err := json.Unmarshal(existing, &doc); err != nil {
			return nil, errors.New("mcp: 설정 파싱 실패") // 경로·원문 미포함
		}
	}
	servers := map[string]json.RawMessage{}
	if raw, ok := doc["mcpServers"]; ok {
		if err := json.Unmarshal(raw, &servers); err != nil {
			return nil, errors.New("mcp: mcpServers 파싱 실패")
		}
	}
	if install {
		b, err := json.Marshal(entry)
		if err != nil {
			return nil, errors.New("mcp: 항목 직렬화 실패")
		}
		servers[name] = b
	} else {
		delete(servers, name)
	}
	sb, err := json.Marshal(servers)
	if err != nil {
		return nil, errors.New("mcp: mcpServers 직렬화 실패")
	}
	doc["mcpServers"] = sb
	out, err := json.MarshalIndent(doc, "", "  ")
	if err != nil {
		return nil, errors.New("mcp: 설정 직렬화 실패")
	}
	return append(out, '\n'), nil
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 -run TestMergeMCPServers ./internal/cli/`
Expected: PASS (3개 테스트 전부)

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/cli/mcp_install.go internal/cli/mcp_install_test.go
git add internal/cli/mcp_install.go internal/cli/mcp_install_test.go
git commit -m "feat(v0.12): .mcp.json 멱등 병합기 (D64)"
```

---

### Task 3: `enabledMcpjsonServers` 스코프 판정 (D64, plan_conflict F9 반영)

**배경**: `.mcp.json`의 프로젝트 스코프 서버는 기본이 **승인 대기**이고 `enabledMcpjsonServers`로 자동 승인된다. 그런데 이 키는 **배열 병합 대상이 아니다** — 가장 높은 우선순위 스코프가 통째로 override 한다(병합되는 것은 permission `allow`/`ask`/`deny`뿐). 따라서 낮은 스코프에 쓰면 **조용히 무시된다**.

**Files:**
- Modify: `internal/cli/mcp_install.go`
- Modify: `internal/cli/mcp_install_test.go`

**Interfaces:**
- Consumes: `hookSettingsPath(user bool, projectRoot string) (string, error)` — `hook_install.go:400`.
- Produces: `enabledServersScope(projectRoot string, readFile func(string) ([]byte, error)) (winner string, defined []string, err error)`
  - `winner` = 그 키를 정의한 **가장 높은 우선순위** 스코프의 파일 경로(없으면 빈 문자열).
  - `defined` = 그 키를 정의한 모든 스코프 경로(진단 보고용).
  - T6이 이 결과로 "쓸지 / 보고만 할지"를 정한다.

**스코프 우선순위**(높은 것부터): user `~/.claude/settings.json` → project `.claude/settings.json` → local `.claude/settings.local.json`. 우선순위가 높은 쪽이 이긴다.

- [ ] **Step 1: 실패 테스트를 쓴다**

```go
// TestEnabledServersScopePicksHighest: 여러 스코프가 키를 정의하면 우선순위가 가장 높은
// 파일이 winner가 되고, 정의한 모든 경로가 defined에 담긴다.
func TestEnabledServersScopePicksHighest(t *testing.T) {
	files := map[string][]byte{
		"USER":    []byte(`{"enabledMcpjsonServers":["ctr-exec"]}`),
		"PROJECT": []byte(`{"enabledMcpjsonServers":["other"]}`),
		"LOCAL":   []byte(`{}`),
	}
	read := func(p string) ([]byte, error) {
		if b, ok := files[scopeKeyForTest(p)]; ok {
			return b, nil
		}
		return nil, os.ErrNotExist
	}
	winner, defined, err := enabledServersScope(t.TempDir(), read)
	if err != nil {
		t.Fatalf("scope: %v", err)
	}
	if scopeKeyForTest(winner) != "USER" {
		t.Errorf("winner=%q, user 스코프여야 한다", winner)
	}
	if len(defined) != 2 {
		t.Errorf("defined=%v, 2개여야 한다(USER·PROJECT)", defined)
	}
}

// TestEnabledServersScopeNoneDefined: 아무 스코프도 정의하지 않으면 winner가 비고
// defined도 빈다 — 이 경우 설치기가 자유롭게 쓸 수 있다.
func TestEnabledServersScopeNoneDefined(t *testing.T) {
	read := func(string) ([]byte, error) { return nil, os.ErrNotExist }
	winner, defined, err := enabledServersScope(t.TempDir(), read)
	if err != nil {
		t.Fatalf("scope: %v", err)
	}
	if winner != "" || len(defined) != 0 {
		t.Errorf("winner=%q defined=%v, 둘 다 비어야 한다", winner, defined)
	}
}
```

`scopeKeyForTest`는 테스트 파일 안에 두는 헬퍼다. 경로 끝 두 세그먼트로 스코프를 판별한다:

```go
func scopeKeyForTest(p string) string {
	switch {
	case strings.HasSuffix(filepath.ToSlash(p), "settings.local.json"):
		return "LOCAL"
	case strings.Contains(filepath.ToSlash(p), "/.claude/settings.json"):
		// user와 project를 구분하려면 홈 디렉터리 접두를 본다.
		home, _ := os.UserHomeDir()
		if home != "" && strings.HasPrefix(filepath.ToSlash(p), filepath.ToSlash(home)) {
			return "USER"
		}
		return "PROJECT"
	}
	return ""
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run TestEnabledServersScope ./internal/cli/`
Expected: FAIL — `undefined: enabledServersScope`

- [ ] **Step 3: 스코프 판정을 구현한다**

`internal/cli/mcp_install.go`에 추가:

```go
// enabledServersScope — enabledMcpjsonServers 키를 정의한 스코프를 우선순위 순으로 조사한다.
// 이 키는 permission 규칙과 달리 스코프 간 병합되지 않고 최상위 정의가 하위를 덮으므로,
// 낮은 스코프에 쓰면 조용히 무시된다(설계 D64). readFile은 테스트 주입 seam이다.
func enabledServersScope(projectRoot string, readFile func(string) ([]byte, error)) (string, []string, error) {
	userPath, err := hookSettingsPath(true, projectRoot)
	if err != nil {
		return "", nil, err
	}
	projectPath, err := hookSettingsPath(false, projectRoot)
	if err != nil {
		return "", nil, err
	}
	localPath := filepath.Join(projectRoot, ".claude", "settings.local.json")

	var defined []string
	winner := ""
	for _, p := range []string{userPath, projectPath, localPath} { // 높은 우선순위부터
		b, err := readFile(p)
		if err != nil {
			continue // 미존재·읽기 실패는 "정의 없음"으로 본다
		}
		var doc struct {
			Enabled []string `json:"enabledMcpjsonServers"`
		}
		if err := json.Unmarshal(b, &doc); err != nil {
			continue // 깨진 파일은 이 판정에서 무시한다(설치기가 건드리지 않는다)
		}
		if doc.Enabled == nil {
			continue
		}
		defined = append(defined, p)
		if winner == "" {
			winner = p
		}
	}
	return winner, defined, nil
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 -run TestEnabledServersScope ./internal/cli/`
Expected: PASS

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/cli/mcp_install.go internal/cli/mcp_install_test.go
git add internal/cli/mcp_install.go internal/cli/mcp_install_test.go
git commit -m "feat(v0.12): enabledMcpjsonServers 스코프 판정 (D64)"
```

---

### Task 4: exec 승인 정책 — 호스트 모드 존중으로 안내 개정 (D64)

**사실 확인**: `permissions.ask`는 **설치기가 쓰지 않는다**. 저장소 전체에서 그 키는 `hostSnippet` 안내 문면(`cli.go:1333`) 한 곳에만 있다. 즉 "설치기는 ask를 생성하지 않는다"는 이미 참이고, 이 태스크가 고칠 것은 **사용자를 그 방향으로 이끄는 안내 문면**이다.

**근거**: `bypassPermissions`는 프롬프트를 끄지만 **명시적 ask 규칙은 이 모드에서도 프롬프트를 강제**한다. 규칙 평가는 deny → ask → allow 순이라 **더 구체적인 allow도 ask를 이기지 못한다**. 실측에서 `.claude/settings.json`의 ask 2종이 `.claude/settings.local.json`의 allow 1종을 무력화하고 있었다.

**계획 시점 판단(설계서 D64 대비 축소)**: 설계서는 `--exec-approval=ask` 플래그를 opt-in으로 적었다. 계획에서는 **플래그를 만들지 않고 안내 문면으로 대체**한다 — 설치기가 ask를 만들지 않는 것이 기본이고, 강제 프롬프트를 원하는 사용자는 settings에 한 줄 추가하면 된다. 플래그는 코드·테스트·문서 비용에 비해 얻는 것이 없다. 이 축소가 받아들여지지 않으면 T4를 되돌리고 플래그를 추가한다.

**Files:**
- Modify: `internal/cli/cli.go:1320-1345` (`hostSnippet` 상수)
- Test: `internal/cli/cli_test.go`

**Interfaces:**
- Consumes: 없음.
- Produces: 없음(상수 문면 변경).

- [ ] **Step 1: 실패 테스트를 쓴다**

```go
// TestHostSnippetNoExecAskRule: 호스트 안내가 exec 도구를 permissions.ask에 넣게 하지
// 않는다. ask는 무프롬프트 모드에서도 프롬프트를 강제하고 allow를 이기므로, 승인 강도는
// 호스트 권한 모드가 정하게 둔다(설계 v0.12 D64).
func TestHostSnippetNoExecAskRule(t *testing.T) {
	// ask 배열 줄에 exec 도구가 있으면 안 된다.
	for _, line := range strings.Split(hostSnippet, "\n") {
		if !strings.Contains(line, `"ask"`) {
			continue
		}
		for _, tool := range []string{"ctr_execute", "ctr_execute_file"} {
			if strings.Contains(line, tool) {
				t.Errorf("ask 안내에 exec 도구가 남아 있다: %s", line)
			}
		}
	}
	// 대신 모드 기반 설명이 있어야 한다.
	if !strings.Contains(hostSnippet, "권한 모드") {
		t.Errorf("승인 강도를 호스트 권한 모드가 정한다는 안내가 없다")
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run TestHostSnippetNoExecAskRule ./internal/cli/`
Expected: FAIL — ask 줄에 `ctr_execute`가 있고 "권한 모드" 문구가 없다.

- [ ] **Step 3: 안내 문면을 개정한다**

`cli.go:1333`의 ask 줄에서 **exec 2종만 제거**한다(다른 항목 `mcp__ctr__ctr_index`·`ctr_fetch_and_index`·`mcp__ctr-global__*`는 그대로 둔다 — 그들은 exec가 아니고 이 결정의 대상이 아니다).

개정 후 그 줄과 이어지는 설명은 다음 취지를 담는다:

```
"ask": ["mcp__ctr__ctr_index", "mcp__ctr__ctr_fetch_and_index", "mcp__ctr-global__*"]
# exec 2종(ctr_execute·ctr_execute_file)은 ask에 넣지 않는다 — 승인 강도는 호스트 권한 모드가
# 정한다. default 모드에서는 MCP 기본 프롬프트가 그대로 작동하고, 사용자가 무프롬프트 모드를
# 명시적으로 고른 경우에만 즉시 실행된다. ask 규칙을 넣으면 그 모드에서도 프롬프트가 강제되고,
# 평가 순서(deny→ask→allow) 때문에 더 구체적인 allow도 ask를 이기지 못한다.
# 이중 동의는 유지된다: ① --enable exec 서버 프로필(기동 시) ② 호스트 권한 모델.
```

`cli.go:1343`의 Codex 문면(`# exec 프로필 활성 시 enabled_tools에 ... approval prompt 권장`)도 같은 취지로 맞춘다 — `enabled_tools` 추가는 유지하되 "approval prompt 권장"을 "승인 강도는 Codex 승인 모드가 정한다"로 바꾼다.

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 ./internal/cli/`
Expected: PASS. `TestDoctorShowsRunnerLine` 등 기존 doctor 테스트도 그대로 통과해야 한다.

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/cli/cli.go internal/cli/cli_test.go
git add internal/cli/cli.go internal/cli/cli_test.go
git commit -m "fix(v0.12): exec 승인 안내를 호스트 권한 모드 존중으로 개정 (D64)"
```

---

### Task 5: doctor 진단 — ask가 allow를 가리는 조합 감지 (D64)

**왜 필요한가**: 사용자가 프롬프트를 없애려고 `allow`를 추가했는데 다른 스코프의 `ask`가 그것을 무력화하는 상태가 실제로 발생했다. 규칙 평가 순서를 모르면 알아채기 어렵고, 증상은 "설정을 바꿨는데 계속 물어본다"로만 나타난다.

**Files:**
- Modify: `internal/cli/mcp_install.go`
- Modify: `internal/cli/mcp_install_test.go`
- Modify: `internal/cli/cli.go` (`runDoctor`에 진단 라인 추가)

**Interfaces:**
- Consumes: `hookSettingsPath(user bool, projectRoot string) (string, error)`.
- Produces: `askShadowedAllows(projectRoot string, readFile func(string) ([]byte, error)) ([]string, error)` — ask와 allow 양쪽에 매치되는 도구 이름들을 반환한다. doctor가 이 목록을 라인으로 출력한다.

**매칭 규칙**: 규칙 문자열은 `mcp__<server>__<tool>` 리터럴이거나 도구 위치에 glob이 올 수 있다(`mcp__server__get_*`). 서버 세그먼트에는 glob이 오지 않는다. 이 태스크는 **리터럴 완전 일치와 도구 위치 접미 glob**만 처리한다 — 그 두 형태가 실제로 쓰이는 전부다.

- [ ] **Step 1: 실패 테스트를 쓴다**

```go
// TestAskShadowedAllows: ask와 allow가 같은 도구를 가리키면 그 도구를 보고한다.
// 평가 순서가 deny→ask→allow라 이 조합에서 allow는 효력이 없다.
func TestAskShadowedAllows(t *testing.T) {
	files := map[string]string{
		"PROJECT": `{"permissions":{"ask":["mcp__ctr-exec__ctr_execute","mcp__ctr-exec__ctr_execute_file"]}}`,
		"LOCAL":   `{"permissions":{"allow":["mcp__ctr-exec__ctr_execute"]}}`,
		"USER":    `{}`,
	}
	read := func(p string) ([]byte, error) {
		if s, ok := files[scopeKeyForTest(p)]; ok {
			return []byte(s), nil
		}
		return nil, os.ErrNotExist
	}
	got, err := askShadowedAllows(t.TempDir(), read)
	if err != nil {
		t.Fatalf("askShadowedAllows: %v", err)
	}
	if len(got) != 1 || got[0] != "mcp__ctr-exec__ctr_execute" {
		t.Errorf("got=%v, [mcp__ctr-exec__ctr_execute] 여야 한다", got)
	}
}

// TestAskShadowedAllowsGlob: ask의 도구 위치 glob이 allow의 리터럴을 덮는 경우도 잡는다.
func TestAskShadowedAllowsGlob(t *testing.T) {
	files := map[string]string{
		"PROJECT": `{"permissions":{"ask":["mcp__ctr-exec__ctr_*"]}}`,
		"LOCAL":   `{"permissions":{"allow":["mcp__ctr-exec__ctr_execute"]}}`,
	}
	read := func(p string) ([]byte, error) {
		if s, ok := files[scopeKeyForTest(p)]; ok {
			return []byte(s), nil
		}
		return nil, os.ErrNotExist
	}
	got, err := askShadowedAllows(t.TempDir(), read)
	if err != nil {
		t.Fatalf("askShadowedAllows: %v", err)
	}
	if len(got) != 1 {
		t.Errorf("got=%v, glob이 리터럴을 덮는 조합을 잡아야 한다", got)
	}
}

// TestAskShadowedAllowsClean: 겹치지 않으면 빈 목록이다.
func TestAskShadowedAllowsClean(t *testing.T) {
	files := map[string]string{
		"LOCAL": `{"permissions":{"allow":["mcp__ctr-exec__ctr_execute"]}}`,
	}
	read := func(p string) ([]byte, error) {
		if s, ok := files[scopeKeyForTest(p)]; ok {
			return []byte(s), nil
		}
		return nil, os.ErrNotExist
	}
	got, err := askShadowedAllows(t.TempDir(), read)
	if err != nil {
		t.Fatalf("askShadowedAllows: %v", err)
	}
	if len(got) != 0 {
		t.Errorf("got=%v, 비어야 한다", got)
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run TestAskShadowed ./internal/cli/`
Expected: FAIL — `undefined: askShadowedAllows`

- [ ] **Step 3: 진단을 구현한다**

`internal/cli/mcp_install.go`에 추가:

```go
// permissionRules — settings 파일에서 읽는 규칙 배열.
type permissionRules struct {
	Permissions struct {
		Ask   []string `json:"ask"`
		Allow []string `json:"allow"`
	} `json:"permissions"`
}

// ruleMatches — ask 규칙 r이 allow 규칙 a가 가리키는 도구를 덮는가. 리터럴 완전 일치와
// 도구 위치 접미 glob("mcp__server__prefix_*")만 다룬다. 서버 세그먼트에는 glob이 오지 않는다.
func ruleMatches(r, a string) bool {
	if r == a {
		return true
	}
	if !strings.HasSuffix(r, "*") {
		return false
	}
	return strings.HasPrefix(a, strings.TrimSuffix(r, "*"))
}

// askShadowedAllows — 모든 스코프의 permissions를 모아, ask가 덮는 allow 항목을 보고한다.
// 규칙 평가가 deny→ask→allow 순이라 이 조합에서 allow는 효력이 없다(설계 v0.12 D64).
// permission 규칙은 스코프 간 concat+dedup으로 병합되므로 전 스코프를 합쳐 판정한다.
func askShadowedAllows(projectRoot string, readFile func(string) ([]byte, error)) ([]string, error) {
	userPath, err := hookSettingsPath(true, projectRoot)
	if err != nil {
		return nil, err
	}
	projectPath, err := hookSettingsPath(false, projectRoot)
	if err != nil {
		return nil, err
	}
	localPath := filepath.Join(projectRoot, ".claude", "settings.local.json")

	var asks, allows []string
	for _, p := range []string{userPath, projectPath, localPath} {
		b, err := readFile(p)
		if err != nil {
			continue
		}
		var doc permissionRules
		if err := json.Unmarshal(b, &doc); err != nil {
			continue
		}
		asks = append(asks, doc.Permissions.Ask...)
		allows = append(allows, doc.Permissions.Allow...)
	}
	var shadowed []string
	seen := map[string]bool{}
	for _, a := range allows {
		for _, r := range asks {
			if ruleMatches(r, a) && !seen[a] {
				seen[a] = true
				shadowed = append(shadowed, a)
				break
			}
		}
	}
	return shadowed, nil
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 -run TestAskShadowed ./internal/cli/`
Expected: PASS (3개 전부)

- [ ] **Step 5: doctor에 라인을 붙인다**

`runDoctor`(cli.go:1350)의 마지막 번호 라인 다음에 새 번호를 추가한다. 현재 마지막은 `[18] exec runners:`이므로 `[19]`가 된다. `hostSnippet` 출력(1778행) **앞**에 넣는다.

```go
// [19] 승인 규칙 정합 — ask가 allow를 덮는 조합은 "설정을 바꿨는데 계속 물어본다"로만
// 드러나 사용자가 알아채기 어렵다(설계 v0.12 D64).
if shadowed, err := askShadowedAllows(projectRoot, os.ReadFile); err == nil && len(shadowed) > 0 {
	fmt.Fprintf(w, "[19] permissions: ask가 allow를 덮는 항목 %d건 — %s (평가 순서 deny→ask→allow, ask를 지우면 호스트 권한 모드가 승인 강도를 정한다)\n",
		len(shadowed), strings.Join(shadowed, ", "))
} else {
	fmt.Fprintln(w, "[19] permissions: ask/allow 충돌 없음")
}
```

- [ ] **Step 6: doctor 라인 테스트를 추가하고 통과를 확인한다**

```go
// TestDoctorShowsPermissionLine: [19] 라인이 항상 나온다(충돌 유무와 무관).
func TestDoctorShowsPermissionLine(t *testing.T) {
	var buf bytes.Buffer
	_ = runDoctor(context.Background(), &buf, t.TempDir(), t.TempDir(), "0.12.0")
	if !strings.Contains(buf.String(), "[19] permissions:") {
		t.Fatalf("[19] 누락:\n%s", buf.String())
	}
}
```

Run: `go test -p 1 -count=1 ./internal/cli/`
Expected: PASS

- [ ] **Step 7: 실환경으로 손검증한다**

Run: `context-router doctor` — `[19]` 라인이 보여야 한다. 계획 작성 시점에는 `.claude/settings.json`의 ask를 이미 제거했으므로 "충돌 없음"이 정상이다.

- [ ] **Step 8: 커밋한다**

```bash
gofumpt -l -w internal/cli/mcp_install.go internal/cli/mcp_install_test.go internal/cli/cli.go internal/cli/cli_test.go
git add internal/cli/mcp_install.go internal/cli/mcp_install_test.go internal/cli/cli.go internal/cli/cli_test.go
git commit -m "feat(v0.12): doctor [19] ask/allow 충돌 진단 (D64)"
```

---

### Task 6: 설치 원커맨드 배선 (D64)

**Files:**
- Modify: `internal/cli/hook_install.go:481` (`runHookInstall`)
- Modify: `internal/cli/mcp_install_test.go`

**Interfaces:**
- Consumes: `mergeMCPServers`(T2), `mcpConfigPath`(T2), `ctrMCPServerName`(T2), `mcpServerEntry`(T2), `enabledServersScope`(T3), `atomicWriteFile`(hook_install.go:372).
- Produces: 없음(기존 명령의 동작 확장).

**계약**:
- `hook install`이 훅 병합에 더해 `.mcp.json`도 병합한다. 두 작업 모두 멱등이다.
- `enabledServersScope`가 **우리보다 높거나 같은 우선순위 스코프에 이미 정의가 있으면 쓰지 않고 보고**한다. 쓰면 무시될 값을 남기는 셈이기 때문이다.
- exec 활성화는 명시 opt-in을 유지한다 — `.mcp.json` 항목의 `Args`에 `--enable exec`를 넣을지는 기존 플래그가 정한다.

- [ ] **Step 1: 실패 테스트를 쓴다**

```go
// TestHookInstallWritesMCPConfig: hook install이 .mcp.json에 우리 서버를 멱등하게 쓴다.
func TestHookInstallWritesMCPConfig(t *testing.T) {
	proj := t.TempDir()
	var out bytes.Buffer
	// runHookInstall의 실제 시그니처는 hook_install.go:481을 읽고 맞춘다.
	// (args, storeRoot, storeRootRaw, storeRootExplicit, projectRoot, version, stdout)
	if err := runHookInstall(nil, t.TempDir(), "", false, proj, "0.12.0", &out); err != nil {
		t.Fatalf("install: %v", err)
	}
	b1, err := os.ReadFile(mcpConfigPath(proj))
	if err != nil {
		t.Fatalf(".mcp.json 미생성: %v", err)
	}
	if !bytes.Contains(b1, []byte(ctrMCPServerName)) {
		t.Errorf("우리 서버 항목이 없다: %s", b1)
	}
	// 두 번째 설치가 바이트를 바꾸지 않는다.
	if err := runHookInstall(nil, t.TempDir(), "", false, proj, "0.12.0", &out); err != nil {
		t.Fatalf("install 2: %v", err)
	}
	b2, _ := os.ReadFile(mcpConfigPath(proj))
	if !bytes.Equal(b1, b2) {
		t.Errorf("멱등 위반:\n1: %s\n2: %s", b1, b2)
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run TestHookInstallWritesMCPConfig ./internal/cli/`
Expected: FAIL — `.mcp.json`이 생성되지 않는다.

- [ ] **Step 3: 배선을 구현한다**

`runHookInstall`의 훅 병합이 끝난 뒤, 같은 함수 안에서:

```go
// .mcp.json 병합 — 훅과 같은 멱등 계약(설계 v0.12 D64). 실패해도 훅 설치 결과는 유지하고
// 사유만 보고한다(설치가 부분 성공임을 감춘 채 성공으로 보이지 않게 한다).
mcpPath := mcpConfigPath(projectRoot)
existing, readErr := os.ReadFile(mcpPath)
if readErr != nil && !errors.Is(readErr, os.ErrNotExist) {
	fmt.Fprintln(stdout, "mcp: 기존 설정을 읽지 못해 .mcp.json 병합을 건너뜁니다")
} else {
	entry := mcpServerEntry{Command: hookBinaryName, Args: mcpArgsForProfile(), AlwaysLoad: true}
	merged, mergeErr := mergeMCPServers(existing, ctrMCPServerName, entry, true)
	if mergeErr != nil {
		fmt.Fprintln(stdout, "mcp: .mcp.json 병합 실패 — 훅 설치는 완료되었습니다")
	} else if err := atomicWriteFile(mcpPath, merged); err != nil {
		fmt.Fprintln(stdout, "mcp: .mcp.json 기록 실패 — 훅 설치는 완료되었습니다")
	} else {
		fmt.Fprintf(stdout, "mcp: .mcp.json 병합 완료(서버 %s)\n", ctrMCPServerName)
	}
}

// 승인 키 스코프 보고 — 우리가 쓰면 무시될 자리인지 먼저 판정한다(설계 D64, F9).
if winner, defined, err := enabledServersScope(projectRoot, os.ReadFile); err == nil {
	switch {
	case len(defined) == 0:
		fmt.Fprintln(stdout, "mcp: enabledMcpjsonServers 정의 없음 — 최초 실행 시 호스트가 승인을 묻습니다")
	default:
		fmt.Fprintf(stdout, "mcp: enabledMcpjsonServers는 이미 %d개 스코프가 정의합니다(적용되는 것은 최상위 1개) — 자동 승인이 필요하면 그 파일에 %q를 추가하세요\n",
			len(defined), ctrMCPServerName)
		_ = winner
	}
}
```

`runHookInstall`의 현재 시그니처(`hook_install.go:481`)에는 exec 여부가 없다. **`--enable-exec` 불리언을 args 파싱에 추가**한다 — `--no-shadow`를 파싱하는 자리와 같은 방식이고, 설계가 요구한 "exec는 명시 opt-in"을 설치 경로에서 유지하는 지점이다. 그 값을 아래 헬퍼에 넘긴다:

```go
// mcpArgsForProfile — .mcp.json 항목의 args. exec 활성화는 명시 opt-in이므로 설치 시
// 플래그가 있을 때만 프로필을 켠다(설계 v0.12 D64). 빈 슬라이스를 반환해 JSON에 "args": []
// 가 남게 한다 — nil이면 키가 사라져 멱등 비교가 흔들린다.
func mcpArgsForProfile(enableExec bool) []string {
	if enableExec {
		return []string{"--enable", "exec"}
	}
	return []string{}
}
```

호출부는 `Args: mcpArgsForProfile(enableExec)`가 된다.

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 ./internal/cli/`
Expected: PASS

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/cli/hook_install.go internal/cli/mcp_install_test.go
git add internal/cli/hook_install.go internal/cli/mcp_install_test.go
git commit -m "feat(v0.12): hook install이 .mcp.json·승인 스코프까지 다룬다 (D64)"
```

---

### Task 7: go 러너의 `GOENV` 격리 + 적용 실증 (D65)

**결함**: `goEnv`(exec.go:348)는 `GOCACHE`·`GOPATH`·`GOTMPDIR`만 스크래치로 돌린다. `GOENV`는 그대로여서 호스트의 go env 파일(`os.UserConfigDir()/go/env`)이 `GOFLAGS`·`GOPROXY`·`GOTOOLCHAIN`을 샌드박스 빌드에 공급한다. 이 값들은 빌드 동작과 재현성을 직접 바꾼다.

**Files:**
- Modify: `internal/exec/exec.go:345-360` (`goEnv`)
- Modify: `internal/exec/exec_test.go`

**Interfaces:**
- Consumes: 없음.
- Produces: 없음(기존 `extra` 계약 유지 — `func(scratch string) []string`).

- [ ] **Step 1: 실패 테스트를 쓴다**

```go
// TestGoEnvIsolatesGOENV: go env 파일 경로가 스크래치 안으로 고정되고 그 파일이 실재한다.
// 호스트 go env 파일이 GOFLAGS·GOPROXY·GOTOOLCHAIN을 공급하지 못하게 하는 계약이다.
func TestGoEnvIsolatesGOENV(t *testing.T) {
	scratch := t.TempDir()
	m := map[string]string{}
	for _, kv := range goEnv(scratch) {
		k, v, _ := strings.Cut(kv, "=")
		m[k] = v
	}
	want := filepath.Join(scratch, "go-env")
	if got := m["GOENV"]; got != want {
		t.Errorf("GOENV=%q want %q", got, want)
	}
	// go는 GOENV 파일을 스스로 만들지 않는다 — 미존재 시 조용히 호스트 기본으로 돌아가므로
	// 빈 파일을 사전 생성해 "설정 없음"을 명시적으로 고정한다(GOTMPDIR 사전 생성과 같은 규율).
	if _, err := os.Stat(want); err != nil {
		t.Errorf("GOENV 파일 미생성: %v", err)
	}
}

// TestRunGoIgnoresHostGoEnv: 러너 안에서 관측한 GOENV가 스크래치 하위다(적용 실증).
// 파일 생성만이 아니라 go 툴체인이 실제로 그 경로를 쓰는지 확인한다.
func TestRunGoIgnoresHostGoEnv(t *testing.T) {
	requireLang(t, "go")
	resp := run(t, Request{
		Language: "go",
		Code: "package main\n\nimport (\n\t\"fmt\"\n\t\"os\"\n)\n\n" +
			"func main() { fmt.Println(\"GOENV=\" + os.Getenv(\"GOENV\")) }\n",
	})
	if resp.ExitCode == nil || *resp.ExitCode != 0 {
		t.Fatalf("exit=%v stderr=%q", resp.ExitCode, resp.Stderr)
	}
	if !strings.Contains(resp.Stdout, "go-env") {
		t.Errorf("GOENV가 스크래치 하위가 아니다: %q", resp.Stdout)
	}
}
```

`requireLang`·`run` 헬퍼는 `exec_test.go`에 이미 있다(`TestRunCS`가 같은 형태로 쓴다).

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run 'TestGoEnvIsolatesGOENV|TestRunGoIgnoresHostGoEnv' ./internal/exec/`
Expected: FAIL — `GOENV=""`

- [ ] **Step 3: 구현한다**

`goEnv`를 다음으로 바꾼다:

```go
// goEnv: 빌드 캐시·모듈·임시를 스크래치 하위로 재지정하고 그 디렉터리를 생성한다.
// GOTMPDIR은 go가 스스로 만들지 않으므로(내부적으로 그 안에 임시 하위만 생성) 사전 생성이
// 필수다 — 없으면 `go run`이 work dir 생성에 실패한다.
// GOENV는 호스트 go env 파일(os.UserConfigDir()/go/env)이 GOFLAGS·GOPROXY·GOTOOLCHAIN을
// 샌드박스 빌드에 공급하는 경로를 끊는다(D65) — env allowlist가 통과시키는 APPDATA/HOME이
// 그 파일로 가는 포인터이기 때문이다. go는 이 파일을 만들지 않으므로 빈 파일을 사전 생성해
// "설정 없음"을 명시한다.
func goEnv(scratch string) []string {
	build := filepath.Join(scratch, "go-build")
	gopath := filepath.Join(scratch, "go")
	gotmp := filepath.Join(scratch, "go-tmp")
	for _, d := range []string{build, gopath, gotmp} {
		_ = os.MkdirAll(d, 0o700)
	}
	goenv := filepath.Join(scratch, "go-env")
	if err := os.WriteFile(goenv, nil, 0o600); err != nil {
		slog.Warn("exec: go env 파일 기록 실패 — 호스트 go env가 적용될 수 있다", "error", err)
	}
	return []string{
		"GOCACHE=" + build,
		"GOPATH=" + gopath,
		"GOTMPDIR=" + gotmp,
		"GOENV=" + goenv,
	}
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 ./internal/exec/`
Expected: PASS

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/exec/exec.go internal/exec/exec_test.go
git add internal/exec/exec.go internal/exec/exec_test.go
git commit -m "feat(v0.12): go 러너 GOENV 격리 + 적용 실증 (D65)"
```

---

### Task 8: node/bun·python 러너의 사용자 구성 격리 (D65)

**결함**: `detectJS`/`tsRunner`는 `extra`가 없어 사용자 npmrc가 그대로 보이고, `pyEnv`는 `PYTHONPYCACHEPREFIX`만 돌려 user site-packages와 pip 구성이 열려 있다. 계획 작성 시점 이 머신에는 해당 파일이 없어 증상이 안 났지만, 경로는 csharp과 동일하게 열려 있다.

**Files:**
- Modify: `internal/exec/exec.go` (`table()`의 javascript/typescript 항목, `pyEnv`)
- Modify: `internal/exec/exec_test.go`

**Interfaces:**
- Consumes: 없음.
- Produces: `jsEnv(scratch string) []string` — 새 함수. `table()`의 javascript·typescript 양쪽이 쓴다.

- [ ] **Step 1: 실패 테스트를 쓴다**

```go
// TestJSEnvIsolatesUserConfig: npm 사용자 구성 경로가 스크래치로 고정된다.
func TestJSEnvIsolatesUserConfig(t *testing.T) {
	scratch := t.TempDir()
	m := map[string]string{}
	for _, kv := range jsEnv(scratch) {
		k, v, _ := strings.Cut(kv, "=")
		m[k] = v
	}
	want := filepath.Join(scratch, "npmrc")
	if got := m["NPM_CONFIG_USERCONFIG"]; got != want {
		t.Errorf("NPM_CONFIG_USERCONFIG=%q want %q", got, want)
	}
	if _, err := os.Stat(want); err != nil {
		t.Errorf("npmrc 사전 생성 안 됨: %v", err)
	}
}

// TestPyEnvIsolatesUserSite: user site-packages와 pip 구성이 차단된다.
func TestPyEnvIsolatesUserSite(t *testing.T) {
	scratch := t.TempDir()
	m := map[string]string{}
	for _, kv := range pyEnv(scratch) {
		k, v, _ := strings.Cut(kv, "=")
		m[k] = v
	}
	if m["PYTHONNOUSERSITE"] != "1" {
		t.Errorf("PYTHONNOUSERSITE 미주입")
	}
	want := filepath.Join(scratch, "pip.conf")
	if got := m["PIP_CONFIG_FILE"]; got != want {
		t.Errorf("PIP_CONFIG_FILE=%q want %q", got, want)
	}
	if got := m["PYTHONPYCACHEPREFIX"]; got != filepath.Join(scratch, "pycache") {
		t.Errorf("기존 PYTHONPYCACHEPREFIX 계약이 깨졌다: %q", got)
	}
}

// TestRunPyIgnoresUserSite: 러너 안에서 user site가 비활성임을 실증한다.
func TestRunPyIgnoresUserSite(t *testing.T) {
	requireLang(t, "python")
	resp := run(t, Request{
		Language: "python",
		Code:     "import site\nprint('USER_SITE=' + str(site.ENABLE_USER_SITE))\n",
	})
	if resp.ExitCode == nil || *resp.ExitCode != 0 {
		t.Fatalf("exit=%v stderr=%q", resp.ExitCode, resp.Stderr)
	}
	if !strings.Contains(resp.Stdout, "USER_SITE=False") {
		t.Errorf("user site가 비활성이 아니다: %q", resp.Stdout)
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run 'TestJSEnv|TestPyEnv|TestRunPyIgnoresUserSite' ./internal/exec/`
Expected: FAIL — `undefined: jsEnv`, `PYTHONNOUSERSITE` 미주입

- [ ] **Step 3: 구현한다**

`exec.go`에 추가하고 `table()`을 수정한다:

```go
// jsEnv: node/bun의 사용자 구성(npmrc)을 스크래치로 고정한다(D65). 스니펫 실행에 패키지
// 설치는 없지만 레지스트리·프록시·스크립트 설정이 호스트에서 새어 들어오는 경로를 끊는다.
// 파일을 사전 생성해 "설정 없음"을 명시한다(미존재 시 기본 탐색으로 되돌아가지 않게).
func jsEnv(scratch string) []string {
	rc := filepath.Join(scratch, "npmrc")
	if err := os.WriteFile(rc, nil, 0o600); err != nil {
		slog.Warn("exec: npmrc 기록 실패 — 호스트 npm 구성이 적용될 수 있다", "error", err)
	}
	return []string{"NPM_CONFIG_USERCONFIG=" + rc}
}
```

`pyEnv`를 확장한다:

```go
// pyEnv: 바이트코드 캐시를 스크래치로 돌리고(기존), user site-packages와 pip 사용자 구성을
// 차단한다(D65 — HOME/APPDATA가 그 경로로 가는 포인터다).
func pyEnv(scratch string) []string {
	cache := filepath.Join(scratch, "pycache")
	_ = os.MkdirAll(cache, 0o700)
	conf := filepath.Join(scratch, "pip.conf")
	if err := os.WriteFile(conf, nil, 0o600); err != nil {
		slog.Warn("exec: pip 구성 기록 실패 — 호스트 pip 구성이 적용될 수 있다", "error", err)
	}
	return []string{
		"PYTHONPYCACHEPREFIX=" + cache,
		"PYTHONNOUSERSITE=1",
		"PIP_CONFIG_FILE=" + conf,
	}
}
```

`table()`에서 javascript와 typescript에 `extra: jsEnv`를 붙인다:

```go
"javascript": {file: "snippet.js", detect: detectJS, argv: fileArgv, extra: jsEnv},
"typescript": tsRunner(), // tsRunner() 반환 구조체에 extra: jsEnv 필드를 추가한다
```

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 ./internal/exec/`
Expected: PASS

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/exec/exec.go internal/exec/exec_test.go
git add internal/exec/exec.go internal/exec/exec_test.go
git commit -m "feat(v0.12): node/bun·python 사용자 구성 격리 (D65)"
```

---

### Task 9: `PSModulePath` 호스트 상속 차단 (D65)

**결함**: `baseKeys`(sandbox.go:118-127)의 Windows allowlist가 `PSModulePath`를 호스트 값 그대로 통과시킨다. 호스트에 설치된 PowerShell 모듈이 샌드박스 스니펫에서 자동 로드 가능해진다.

**주의**: 이 키를 빼면 pwsh가 자체 기본 모듈 경로를 스스로 구성한다(PowerShell은 `PSModulePath` 미설정 시 기본값을 만든다). 그래도 **러너 왕복으로 실증**해야 한다 — 실패하면 인터프리터 설치 경로 기준으로 재구성하는 대안으로 간다.

**Files:**
- Modify: `internal/sandbox/sandbox.go:118-127` (`baseKeys`)
- Modify: `internal/sandbox/sandbox_test.go`
- Modify: `internal/exec/exec_test.go` (러너 왕복 실증)

**Interfaces:**
- Consumes: 없음.
- Produces: 없음(닫힌 표에서 항목 제거).

- [ ] **Step 1: 실패 테스트를 쓴다**

`internal/sandbox/sandbox_test.go`:

```go
// TestBaseKeysExcludesPSModulePath: 닫힌 표가 호스트 PowerShell 모듈 경로를 통과시키지
// 않는다. 통과시키면 호스트 설치 모듈이 샌드박스 스니펫에서 자동 로드된다(D65).
func TestBaseKeysExcludesPSModulePath(t *testing.T) {
	if runtime.GOOS != "windows" {
		t.Skip("Windows 전용 키")
	}
	for _, k := range baseKeys() {
		if k == "PSModulePath" {
			t.Fatalf("PSModulePath가 닫힌 표에 남아 있다")
		}
	}
}
```

`internal/exec/exec_test.go`:

```go
// TestRunShellWorksWithoutHostPSModulePath: PSModulePath를 상속하지 않아도 shell 러너가
// 기본 cmdlet을 실행한다(pwsh가 기본 모듈 경로를 스스로 구성한다는 전제의 실증).
func TestRunShellWorksWithoutHostPSModulePath(t *testing.T) {
	requireLang(t, "shell")
	code := "Get-Date -Format yyyy | Write-Output"
	if runtime.GOOS != "windows" {
		code = "date +%Y"
	}
	resp := run(t, Request{Language: "shell", Code: code})
	if resp.ExitCode == nil || *resp.ExitCode != 0 {
		t.Fatalf("exit=%v stderr=%q", resp.ExitCode, resp.Stderr)
	}
	if strings.TrimSpace(resp.Stdout) == "" {
		t.Errorf("stdout이 비었다 — 기본 cmdlet이 동작하지 않는다: stderr=%q", resp.Stderr)
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run 'TestBaseKeysExcludesPSModulePath' ./internal/sandbox/`
Expected: FAIL — `PSModulePath가 닫힌 표에 남아 있다`

- [ ] **Step 3: 닫힌 표에서 제거한다**

`sandbox.go:121-123`의 Windows 목록에서 `"PSModulePath"`를 지운다. 주석에 사유를 남긴다:

```go
// PSModulePath는 D65에서 제외했다 — 호스트 설치 모듈이 스니펫에서 자동 로드되는 경로였다.
// pwsh는 이 변수가 없으면 자체 기본 모듈 경로를 구성하므로 기본 cmdlet은 그대로 동작한다
// (TestRunShellWorksWithoutHostPSModulePath가 실증).
```

`internal/exec/exec.go`의 D59 관련 주석에 `PSModulePath`가 열거돼 있으면 그 목록에서도 지운다. 설계 문서 `docs/context-router-design-v0.11-ko.md`의 D59 열거는 **과거 기록이므로 수정하지 않는다**(델타 체인 규약 — 개정은 v0.12 문서가 담당한다).

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 ./internal/sandbox/ ./internal/exec/`
Expected: PASS. 특히 `TestRunShellWorksWithoutHostPSModulePath`가 통과해야 한다. **실패하면** 제거를 되돌리고, `shellRunner`에 `extra`를 추가해 인터프리터 설치 디렉터리 하위 `Modules`만 담은 값을 주입하는 대안으로 전환한다(그 경우 `runner.extra` 시그니처는 그대로 두고 `shellRunner()` 클로저가 `bin`을 포획한다).

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/sandbox/sandbox.go internal/sandbox/sandbox_test.go internal/exec/exec_test.go
git add internal/sandbox/sandbox.go internal/sandbox/sandbox_test.go internal/exec/exec_test.go
git commit -m "feat(v0.12): PSModulePath 호스트 상속 차단 (D65)"
```

---

### Task 10: shell 종료코드 계약 고정 (D66)

**계약**(설계서 확정): shell 계열의 `exit_code`는 **마지막 명령의 상태**이며 중간 비종결 오류를 대표하지 않는다. 러너는 엄격 모드를 주입하지 않는다. 판정은 `exit_code`와 `stderr`를 함께 본다.

이 태스크는 동작을 바꾸지 않는다. **계약을 회귀 테스트로 고정**하고 문면을 맞춘다.

**Files:**
- Modify: `internal/exec/exec_test.go`
- Modify: `internal/cli/cli.go` (`hostSnippet`에 한 줄)

**Interfaces:**
- Consumes: 없음.
- Produces: 없음.

- [ ] **Step 1: 계약 테스트를 쓴다**

```go
// TestShellExitCodeIsLastCommand: 중간 명령이 실패해도 마지막 명령이 성공하면 exit 0이고,
// 실패 흔적은 stderr에만 남는다. 러너가 엄격 모드를 주입하지 않는다는 계약의 회귀 방지다
// (설계 v0.12 D66 — 셸 일반의 정의된 동작이며 언어별로 다르게 굴지 않는다).
func TestShellExitCodeIsLastCommand(t *testing.T) {
	requireLang(t, "shell")
	var code string
	if runtime.GOOS == "windows" {
		// 존재하지 않는 경로 접근 = 비종결 오류. 그 뒤 정상 출력으로 끝난다.
		code = "Get-Content 'Z:\\ctr-no-such-file.txt'\nWrite-Output 'done'"
	} else {
		code = "cat /ctr-no-such-file.txt\necho done"
	}
	resp := run(t, Request{Language: "shell", Code: code})
	if resp.ExitCode == nil {
		t.Fatalf("ExitCode=nil (timed_out=%v)", resp.TimedOut)
	}
	if *resp.ExitCode != 0 {
		t.Fatalf("exit=%d want 0 — 러너가 엄격 모드를 주입했을 수 있다. stderr=%q", *resp.ExitCode, resp.Stderr)
	}
	if !strings.Contains(resp.Stdout, "done") {
		t.Errorf("마지막 명령이 실행되지 않았다: stdout=%q", resp.Stdout)
	}
	if strings.TrimSpace(resp.Stderr) == "" {
		t.Errorf("실패 흔적이 stderr에 없다 — 호출자가 판정할 근거가 사라진다")
	}
}
```

- [ ] **Step 2: 실행해 현재 동작이 계약과 같은지 확인한다**

Run: `go test -p 1 -count=1 -run TestShellExitCodeIsLastCommand ./internal/exec/`
Expected: **PASS** — 이 태스크는 동작 변경이 아니라 고정이므로 처음부터 통과해야 정상이다. 실패하면 러너가 이미 어딘가에서 엄격 모드를 주입하고 있다는 뜻이므로, 계약을 바꾸기 전에 그 지점을 먼저 찾는다.

- [ ] **Step 3: 안내 문면에 계약을 적는다**

`hostSnippet`(cli.go:1320)에 한 줄을 더한다. 도구 스키마가 아니라 doctor 출력이므로 게이트 11 예산과 무관하다.

```
# shell 러너: exit_code는 마지막 명령의 상태다(중간 비종결 오류는 반영되지 않는다).
# 성공 판정은 exit_code와 stderr를 함께 본다. 엄격 동작이 필요하면 스니펫 첫 줄에
# 직접 적는다: PowerShell은 $ErrorActionPreference = 'Stop', sh는 set -e.
```

- [ ] **Step 4: 전체 통과를 확인한다**

Run: `go test -p 1 -count=1 ./internal/exec/ ./internal/cli/`
Expected: PASS

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/exec/exec_test.go internal/cli/cli.go
git add internal/exec/exec_test.go internal/cli/cli.go
git commit -m "test(v0.12): shell 종료코드 계약 고정 + 안내 문면 (D66)"
```

---

### Task 11: shadow purge에 나이 필터 + VACUUM 분리 (D67)

**현재 구조**: `store.PurgeHookOnly(ctx)`(store.go:1050)는 shadow 귀속 **전부**를 지운다 — 나이 필터가 없다. 그리고 CLI 경로 `runPurgeHookOnly`(cli.go:895)는 삭제 후 **항상 VACUUM**한다(D41/D55).

**설계 요구**: 보존 3일이므로 나이 필터가 필요하고, 자동 경로에서는 VACUUM을 하지 않는다(free page 재사용으로 성장을 억제하는 것이 목표이고, 기동 시 168MiB VACUUM은 지연이 크다).

**Files:**
- Modify: `internal/store/store.go` (`PurgeHookOnly` 근처)
- Modify: `internal/store/store_test.go`

**Interfaces:**
- Consumes: 기존 `PurgeOlderThan(ctx, cutoffUnix int64) (sources, artifacts int64, err error)`(store.go:712)의 나이 조건과 `PurgeHookOnly`의 shadow 귀속 판정.
- Produces: `PurgeHookOnlyOlderThan(ctx context.Context, cutoffUnix int64) (HookPurgeReport, error)`
  - `cutoffUnix <= 0`이면 나이 필터 없이 기존 `PurgeHookOnly`와 동일하게 동작한다(호출자 단순화).
  - **VACUUM을 하지 않는다** — 호출자가 필요하면 별도로 수행한다. 기존 `PurgeHookOnly`도 VACUUM을 하지 않았다(CLI가 후행했다). 이 계약은 유지된다.
  - T12가 이 메서드를 호출한다.

- [ ] **Step 1: 실패 테스트를 쓴다**

픽스처 헬퍼는 이미 있다 — `regSource(t, st, content, mediaType, uri, kind)`(store_test.go:1717 등에서 쓰는 형태). 새 헬퍼를 만들지 않는다.

```go
// TestPurgeHookOnlyOlderThan: cutoff보다 새 shadow 아티팩트는 남고 오래된 것만 지워진다.
// explicit 소스는 나이와 무관하게 보존된다(--hook-only 계약 유지).
//
// 주의: 기존 TestPurgeHookOnlyAgeGateDefers(store_test.go:1927)의 age gate는 파일 unlink
// 유예(mtime 1시간)이고, 이 태스크가 더하는 것은 artifacts.created_at 기준 대상 선택이다.
// 서로 다른 축이므로 두 테스트가 함께 통과해야 한다.
func TestPurgeHookOnlyOlderThan(t *testing.T) {
	st := newTestStore(t) // 기존 TestPurgeHookOnly(1887행)가 쓰는 store 생성 헬퍼 이름을 그대로 쓴다
	regSource(t, st, "old-shadow", "text/plain", "shadow:Bash:old", "hook")
	regSource(t, st, "new-shadow", "text/plain", "shadow:Bash:new", "hook")
	regSource(t, st, "kept-explicit", "text/plain", "file:///kept.txt", "file")

	// regSource는 created_at을 받지 않으므로 직접 내린다. DB 핸들 접근자 이름은 기존
	// store_test.go가 UPDATE/SELECT를 직접 수행하는 테스트에서 쓰는 것을 그대로 쓴다.
	cutoff := time.Now().Add(-72 * time.Hour).Unix()
	oldTS := time.Now().Add(-96 * time.Hour).Unix()
	if _, err := st.Writer().Exec(
		`UPDATE artifacts SET created_at=? WHERE id IN (SELECT artifact_id FROM sources WHERE uri='shadow:Bash:old')`,
		oldTS); err != nil {
		t.Fatalf("나이 조정: %v", err)
	}

	rep, err := st.PurgeHookOnlyOlderThan(context.Background(), cutoff)
	if err != nil {
		t.Fatalf("PurgeHookOnlyOlderThan: %v", err)
	}
	if rep.Hashes != 1 {
		t.Fatalf("rep.Hashes=%d want 1(오래된 shadow만)", rep.Hashes)
	}
	var n int
	if err := st.Reader().QueryRow(
		`SELECT COUNT(*) FROM sources WHERE uri IN ('shadow:Bash:new','file:///kept.txt')`).Scan(&n); err != nil {
		t.Fatalf("잔존 조회: %v", err)
	}
	if n != 2 {
		t.Errorf("잔존 소스=%d want 2(새 shadow + explicit)", n)
	}
}

// TestPurgeHookOnlyOlderThanZeroMeansAll: cutoff<=0이면 나이 필터 없이 전부 지운다 —
// 호출자가 "필터 있음/없음"으로 분기하지 않게 하는 계약이다.
func TestPurgeHookOnlyOlderThanZeroMeansAll(t *testing.T) {
	st := newTestStore(t)
	regSource(t, st, "s1", "text/plain", "shadow:Bash:1", "hook")
	regSource(t, st, "s2", "text/plain", "shadow:Bash:2", "hook")

	rep, err := st.PurgeHookOnlyOlderThan(context.Background(), 0)
	if err != nil {
		t.Fatalf("PurgeHookOnlyOlderThan: %v", err)
	}
	if rep.Hashes != 2 {
		t.Errorf("rep.Hashes=%d want 2(나이 필터 없음)", rep.Hashes)
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run TestPurgeHookOnlyOlderThan ./internal/store/`
Expected: FAIL — `undefined: PurgeHookOnlyOlderThan`

- [ ] **Step 3: 구현한다**

`PurgeHookOnly`를 얇은 래퍼로 바꾸고 나이 필터 버전을 본체로 만든다:

```go
// PurgeHookOnly — 나이 제한 없이 shadow 귀속 아티팩트를 지운다(기존 계약 유지).
func (s *Store) PurgeHookOnly(ctx context.Context) (HookPurgeReport, error) {
	return s.PurgeHookOnlyOlderThan(ctx, 0)
}

// PurgeHookOnlyOlderThan — shadow 귀속(그 hash를 참조하는 소스가 전부 hook) 아티팩트 중
// created_at이 cutoffUnix보다 오래된 것만 지운다. cutoffUnix<=0이면 나이 필터가 없다.
// VACUUM은 하지 않는다 — 자동 회수 경로는 free page 재사용으로 성장을 억제하고, 파일 축소가
// 필요할 때만 호출자가 별도로 수행한다(설계 v0.12 D67).
func (s *Store) PurgeHookOnlyOlderThan(ctx context.Context, cutoffUnix int64) (HookPurgeReport, error) {
	// 기존 PurgeHookOnly 본문을 옮기고, shadow 귀속 hash를 고르는 조회에
	// cutoffUnix > 0 일 때만 "AND a.created_at < ?" 조건을 덧붙인다.
	// artifacts.created_at은 unix 초다(session_events.ts와 같은 단위).
	// 나머지 흐름(참조 판정 → 삭제 → HookPurgeReport 집계)은 그대로 둔다.
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 ./internal/store/`
Expected: PASS — 기존 `PurgeHookOnly` 테스트도 래퍼를 통해 그대로 통과해야 한다.

- [ ] **Step 5: 커밋한다**

```bash
gofumpt -l -w internal/store/store.go internal/store/store_test.go
git add internal/store/store.go internal/store/store_test.go
git commit -m "feat(v0.12): shadow purge 나이 필터 (D67)"
```

---

### Task 12: 기동 시 shadow 자동 purge 배선 (D67)

**정책**(설계 확정): shadow 귀속 **3일** 보존, 서버 **기동 시 1회**, **VACUUM 없음**. stdio 서버는 종료가 보장되지 않아 종료 훅은 신뢰할 수 없다.

**Files:**
- Modify: `cmd/context-router/main.go:455` 근처 (`sandbox.SweepStale` 호출부)
- Modify: `cmd/context-router/main_test.go`

**Interfaces:**
- Consumes: `store.Open(projDir string, readOnly bool) (*Store, error)`, `(*Store).PurgeHookOnlyOlderThan`(T11).
- Produces: `shadowRetention` 상수 — 값과 환경변수 오버라이드 규약.

- [ ] **Step 1: 실패 테스트를 쓴다**

`cmd/context-router/main_test.go`:

```go
// TestShadowRetentionDefault: 기본 보존 기간이 3일이고, 환경변수로만 조정된다.
// 임의 상향으로 정책이 조용히 무력화되지 않도록 파싱 실패·비양수는 기본값을 쓴다
// (storeWarnBytes와 같은 규율).
func TestShadowRetentionDefault(t *testing.T) {
	cases := []struct {
		env  string
		want time.Duration
	}{
		{"", 72 * time.Hour},
		{"24h", 24 * time.Hour},
		{"0", 72 * time.Hour},
		{"-5h", 72 * time.Hour},
		{"garbage", 72 * time.Hour},
	}
	for _, c := range cases {
		got := shadowRetention(func(string) string { return c.env })
		if got != c.want {
			t.Errorf("shadowRetention(%q)=%v want %v", c.env, got, c.want)
		}
	}
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `go test -p 1 -count=1 -run TestShadowRetentionDefault ./cmd/context-router/`
Expected: FAIL — `undefined: shadowRetention`

- [ ] **Step 3: 구현한다**

`main.go`에 추가:

```go
// defaultShadowRetention — shadow 귀속 아티팩트 보존 기간(설계 v0.12 D67). 훅이 가로챈
// 출력은 대개 해당 세션 안에서 소비되므로 짧게 잡는다.
const defaultShadowRetention = 72 * time.Hour

// shadowRetention — CTR_SHADOW_RETENTION(time.ParseDuration 형식) 양수만 채택한다.
// 파싱 실패·비양수는 기본값(storeWarnBytes와 같은 규율 — 잘못된 값이 정책을 무력화하지
// 않게 한다).
func shadowRetention(getenv func(string) string) time.Duration {
	if d, err := time.ParseDuration(getenv("CTR_SHADOW_RETENTION")); err == nil && d > 0 {
		return d
	}
	return defaultShadowRetention
}
```

`main.go:454-456`의 `SweepStale`은 `if slices.Contains(f.Enable, "exec")` 블록 **안**에 있다. shadow 회수는 exec 프로필과 무관하므로 **그 블록 밖**, `return mcp.Serve(...)`(457행) 직전에 넣는다. `st`는 이 지점에서 이미 열려 있다 — 458행이 `Store: st`로 넘긴다.

```go
	if slices.Contains(f.Enable, "exec") {
		sandbox.SweepStale(scratchRoot, 24*time.Hour) // D61: 기동 시 24h+ 스테일 스윕
	}
	// D67: shadow 귀속 보존 기간 초과분을 기동 시 1회 회수한다. exec 프로필과 무관하다.
	// VACUUM은 하지 않는다 — free page 재사용으로 파일이 정상상태에 머무는 것이 목표이고,
	// 168MiB VACUUM은 기동 지연이 크다. 실패는 기동을 막지 않는다(다음 기동에서 재시도).
	if rep, err := st.PurgeHookOnlyOlderThan(ctx, time.Now().Add(-shadowRetention(os.Getenv)).Unix()); err != nil {
		slog.Warn("기동 shadow 회수 실패 — 다음 기동에서 재시도", "error", err)
	} else if rep.Hashes > 0 {
		slog.Info("기동 shadow 회수", "hashes", rep.Hashes, "bytes", rep.ReclaimedB)
	}
	return mcp.Serve(ctx, mcp.Config{
```

- [ ] **Step 4: 통과를 확인한다**

Run: `go test -p 1 -count=1 ./cmd/context-router/`
Expected: PASS

- [ ] **Step 5: 전체 회귀와 실환경 확인**

```bash
go test -p 1 -count=1 ./...
```

그다음 서버를 재기동하고 `context-router doctor`의 `[14]`·`[15]` 값을 기록한다. 계획 작성 시점 기준값: `[14]` 176,340,992B, `[15]` 42,950,019B / 992 해시. 3일 초과 shadow가 있으면 `[15]`가 줄어야 한다.

- [ ] **Step 6: 커밋한다**

```bash
gofumpt -l -w cmd/context-router/main.go cmd/context-router/main_test.go
git add cmd/context-router/main.go cmd/context-router/main_test.go
git commit -m "feat(v0.12): 기동 시 shadow 보존 기간 회수 (D67)"
```

---

## 설계 커버리지

| 설계 항목 | 태스크 |
| --- | --- |
| D63 ① 계측 교정(네임스페이스·세션 분모) | T1 |
| D63 ② 서버 등록 단일화 | T2(`ctrMCPServerName` 상수), T6(배선) |
| D63 ③ 상시 로드 적용 | T2(`AlwaysLoad`), T6(배선) |
| D63 ④ SessionStart 안내 | **범위 밖** — 설계가 ①③ 결과에 따른 조건부로 규정했다. 관측 후 별도 계획으로 다룬다 |
| D64 원커맨드 멱등 병합 | T2, T6 |
| D64 스코프 규칙(F9) | T3, T6 |
| D64 승인 정책(ask 미생성) | T4 |
| D64 ask/allow 충돌 진단 | T5 |
| D65 go `GOENV` | T7 |
| D65 node/bun·python | T8 |
| D65 `PSModulePath` | T9 |
| D65 적용 실증 테스트 | T7(`TestRunGoIgnoresHostGoEnv`), T8(`TestRunPyIgnoresUserSite`), T9(`TestRunShellWorksWithoutHostPSModulePath`) |
| D66 종료코드 계약 | T10 |
| D67 나이 필터·VACUUM 분리 | T11 |
| D67 기동 시 자동 실행 | T12 |
| D67 임계값 재설정 | **범위 밖** — 설계가 "정상상태 관측 후"로 규정했다. T12 적용 후 관측 데이터가 쌓여야 정할 수 있다 |
| 이월 Minor: `rows.Err()` 누락, 음수 `days` | T1 Step 8 |
| 이월 Minor 나머지 7건 | **범위 밖** — 이 계획의 태스크와 접점이 없다. 별도 triage |

## 실행 순서와 의존

```
T1 (독립, 선행 게이트)
T2 → T3 → T6        (T4·T5는 T2·T3와 무관하게 병렬 가능)
T4, T5 (독립)
T7, T8, T9 (서로 독립)
T10 (독립)
T11 → T12
```

**T1을 가장 먼저 완료한다** — 설계서 §1.3이 계측 교정을 상시 로드보다 선행으로 못박았다. T6이 `alwaysLoad`를 켜기 전에 T1이 병합돼 있어야 전후 비교가 성립한다.

**T12 이후 관측 구간**을 둔다. 상시 로드(T6)와 자동 회수(T12)가 적용된 뒤 며칠간 `usage --adoption`과 `doctor [14][15]`를 기록해야 D63 ④(안내 도입 여부)와 D67 임계값을 정할 수 있다.
