# v0.11 (D58–D62) Implementation Plan — exec 2종·OS 격리·러너 6언어·출력 ephemeral·채택 개선

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `ctr_execute`·`ctr_execute_file`(exec 프로필 opt-in, fail-closed 격리 프로브)를 OS별 샌드박스(Job Object / setpgid+landlock / setpgid+sandbox-exec) 위 러너 6언어로 구현하고, 채택 계측(`usage --adoption`)·doctor [18]·도구 설명 개정을 함께 릴리스한다.

**Architecture:** `internal/sandbox`(스크래치 수명·env 닫힌 표·per-OS 실행, build tag 3파일, 자기정의 인터페이스 0) 위에 `internal/exec`(러너 테이블 — 감지·버전 게이트·캐시 재지정·명령 조립)를 얹고, `internal/mcp`가 exec 프로필 게이트+프로브 뒤에 2도구를 등록한다. Unix FS 제한은 transform의 SelfExe 워커 선례를 따라 `__exec-launcher` 재실행 모드로 자식에 landlock/sandbox-exec를 적용한다. 오류는 exec/sandbox sentinel → `toToolError` 단일 지점 배선.

**Tech Stack:** Go stdlib(os/exec·flag·slog) + golang.org/x/sys/windows(Job Object) + landlock-lsm/go-landlock v0.9.0(기존 의존) + modernc SQLite(기존). 신규 외부 의존 0.

**정본 스펙:** `docs/context-router-design-v0.11-ko.md` (D58–D62, §5 검수 기록 포함, 커밋 f3ae92d). 이 계획과 스펙이 충돌하면 스펙이 이긴다.

## Global Constraints

- 테스트는 항상 `go test -p 1 ./...` (메모리 캡 규칙 — 절대 병렬 금지). 개별 패키지는 `go test -p 1 ./internal/sandbox/...` 식.
- 커밋 전 `gofumpt -l .` 로컬 확인(출력 0줄이어야 함 — CI 전용 게이트라 로컬서 안 잡히면 lint 인시던트 재발). `git add -A` 금지 — 파일 명시 add.
- 오류 메시지에 절대경로·사용자 입력 원문 금지(§6/§12 canary 관례). 자기정의 인터페이스 금지(아키텍처 문서 계약).
- **의존**: 신규 외부 의존은 **go-landlock v0.9.0 1건만 허용**(vision §8.3 사전 지명 — Linux FS 제한). 그 외 신규 의존 금지. Windows Job Object·setpgid는 기존 `golang.org/x/sys` v0.47.0으로 충족.
- timeout: 기본 120,000ms·상한 1,800,000ms. 출력 캡: stdout 32KiB(32768)·stderr 8KiB(8192). 스크래치 스윕 TTL 24h.
- env 닫힌 표(D59): Windows = PATH·PATHEXT·COMSPEC·SystemRoot·SystemDrive·TEMP·TMP·USERPROFILE·HOMEDRIVE·HOMEPATH·LOCALAPPDATA·APPDATA·ProgramFiles·ProgramData·PSModulePath / Unix = PATH·HOME·TMPDIR·LANG·LC_ALL. + D60 러너별 재지정(값은 스크래치 하위로 생성): go=GOCACHE·GOPATH·GOTMPDIR / csharp=DOTNET_CLI_HOME·NUGET_PACKAGES + `DOTNET_CLI_DO_NOT_USE_MSBUILD_SERVER=1`·`MSBUILDDISABLENODEREUSE=1`·`UseSharedCompilation=false` / python=PYTHONPYCACHEPREFIX.
- 대형 테스트 데이터는 `strings.Repeat`/testdata로 조립(응답 분할 규율 — 통파일 재작성 금지).
- toolchain 부재 환경 대비: 러너 의존 테스트는 감지 실패 시 `t.Skip`.

## File Structure

- Create: `internal/sandbox/sandbox.go` — Spec/Result/ErrSetup·스크래치 수명·BaseEnv 닫힌 표·Probe (OS 무관 공통)
- Create: `internal/sandbox/run_windows.go` — Job Object 실행(CREATE_SUSPENDED→assign→resume·kill-on-job-close·캡)
- Create: `internal/sandbox/run_unix.go` (`//go:build !windows`) — setpgid 그룹 킬 + `__exec-launcher` 경유 실행
- Create: `internal/sandbox/launcher_linux.go` / `launcher_darwin.go` / `launcher_other.go` — 런처의 FS 제한 적용(landlock / sandbox-exec / no-op)
- Create: `internal/exec/exec.go` — Request/Response/sentinel·러너 테이블·감지·버전 게이트·env 조립·Run/RunnerStatus
- Modify: `internal/mcp/mcp.go` — exec 프로필 게이트+프로브 등록·ExecuteInput/Output·toToolError sentinel 2코드 신설·D62 설명 개정(search/fetch)
- Modify: `cmd/context-router/main.go` — `--enable` 도움말에 exec·`__exec-launcher` 모드 분기(transformWorkerArg 선례)
- Modify: `internal/cli/cli.go` — usage `--adoption`·doctor [18]·hostSnippet 개정
- Modify: `internal/mcp/mcp_test.go` — 프로필 테이블에 exec 행·직렬화 상한 상향·설명 가드
- Modify: `docs/context-router-code-architecture-ko.md` — 신규 패키지 2개(의존 그래프)·§6 sentinel 추가
- Modify: `.github/workflows/ci.yml` — setup-dotnet(이미지에 .NET 10 부재 시)
- Test: 각 패키지 `*_test.go` 병치(기존 관례)

---

### Task 1: internal/sandbox 공통 — Spec/Result·스크래치 수명·env 닫힌 표·Probe

**Files:**
- Create: `internal/sandbox/sandbox.go`
- Test: `internal/sandbox/sandbox_test.go`

**Interfaces:**
- Consumes: 없음(stdlib만)
- Produces (T2·T3·T4가 사용):
  - `type Spec struct { Argv []string; Dir string; Env []string; Timeout time.Duration; StdoutCap, StderrCap int }`
  - `type Result struct { Stdout, Stderr []byte; StdoutTrunc, StderrTrunc bool; ExitCode int; TimedOut bool; Duration time.Duration }` (TimedOut=true면 ExitCode=-1)
  - `var ErrSetup = errors.New("sandbox: 격리 준비 실패")` — Job/landlock/스크래치 실패의 sentinel(fail-closed)
  - `func NewScratch(parent string) (string, error)` — 0700 고유 디렉터리
  - `func SweepStale(parent string, ttl time.Duration)` — best-effort, 오류 무시
  - `func BaseEnv() []string` — OS 닫힌 표만 통과(Global Constraints의 목록 그대로)
  - `func Probe(ctx context.Context) error` — T2가 per-OS로 구현(윈도: Job 생성/해제, 유닉스: 항상 nil — landlock은 BestEffort라 프로브 비대상). **T1에서는 선언하지 않는다**(per-OS 파일 소관).

- [ ] **Step 1: 실패 테스트 작성** — `internal/sandbox/sandbox_test.go`

```go
package sandbox

import (
	"os"
	"path/filepath"
	"runtime"
	"strings"
	"testing"
	"time"
)

func TestNewScratchCreatesPrivateDir(t *testing.T) {
	parent := t.TempDir()
	dir, err := NewScratch(parent)
	if err != nil {
		t.Fatalf("NewScratch: %v", err)
	}
	fi, err := os.Stat(dir)
	if err != nil || !fi.IsDir() {
		t.Fatalf("스크래치 미생성: %v", err)
	}
	if filepath.Dir(dir) != parent {
		t.Fatalf("부모 불일치: %s", dir)
	}
	if runtime.GOOS != "windows" && fi.Mode().Perm() != 0o700 {
		t.Fatalf("owner-only 아님: %v", fi.Mode().Perm())
	}
}

func TestSweepStaleRemovesOldKeepsFresh(t *testing.T) {
	parent := t.TempDir()
	old, _ := NewScratch(parent)
	fresh, _ := NewScratch(parent)
	past := time.Now().Add(-48 * time.Hour)
	if err := os.Chtimes(old, past, past); err != nil {
		t.Fatal(err)
	}
	SweepStale(parent, 24*time.Hour)
	if _, err := os.Stat(old); !os.IsNotExist(err) {
		t.Fatalf("스테일 미제거")
	}
	if _, err := os.Stat(fresh); err != nil {
		t.Fatalf("신선분 오삭제: %v", err)
	}
}

func TestBaseEnvClosedTable(t *testing.T) {
	t.Setenv("CTR_TEST_EXCLUDED_VAR", "marker")
	t.Setenv("PATH", os.Getenv("PATH")) // 존재 보장
	for _, kv := range BaseEnv() {
		if strings.HasPrefix(kv, "CTR_TEST_EXCLUDED_VAR=") {
			t.Fatalf("닫힌 표 위반: 표 밖 변수 통과")
		}
	}
	found := false
	for _, kv := range BaseEnv() {
		if strings.HasPrefix(kv, "PATH=") {
			found = true
		}
	}
	if !found {
		t.Fatalf("PATH 누락")
	}
}
```

- [ ] **Step 2: 실패 확인**

Run: `go test -p 1 ./internal/sandbox/`
Expected: FAIL (패키지 미존재 → 컴파일 오류)

- [ ] **Step 3: 최소 구현** — `internal/sandbox/sandbox.go`

```go
// Package sandbox — exec 3종의 OS 격리 실행 기반(설계 v0.11 D59).
// 자기정의 인터페이스 0: Run/Probe는 per-OS 파일(build tag)이 동일 시그니처로 구현.
package sandbox

import (
	"errors"
	"fmt"
	"os"
	"path/filepath"
	"runtime"
	"time"
)

// ErrSetup — Job/landlock/스크래치 등 격리 준비 실패 sentinel(fail-closed, D61:
// 사용자 코드 실패와 구분되어 mcp toToolError에서 SANDBOX_UNAVAILABLE로 매핑).
var ErrSetup = errors.New("sandbox: 격리 준비 실패")

type Spec struct {
	Argv      []string      // argv[0]은 LookPath 완료된 실행 파일
	Dir       string        // cwd = 스크래치(D58)
	Env       []string      // 완성 env(BaseEnv+러너 재지정 — internal/exec가 조립)
	Timeout   time.Duration // 호출 계층에서 클램프 완료(120s 기본·1800s 상한)
	StdoutCap int           // 32768 (D61)
	StderrCap int           // 8192 (D61)
}

type Result struct {
	Stdout, Stderr           []byte
	StdoutTrunc, StderrTrunc bool
	ExitCode                 int // TimedOut=true면 -1(무의미 — MCP 계층이 null로 변환)
	TimedOut                 bool
	Duration                 time.Duration
}

// NewScratch: 실행별 고유 스크래치(0700). 이름 충돌은 MkdirTemp가 회피.
func NewScratch(parent string) (string, error) {
	if err := os.MkdirAll(parent, 0o700); err != nil {
		return "", fmt.Errorf("%w: 스크래치 부모 생성 실패", ErrSetup)
	}
	dir, err := os.MkdirTemp(parent, "run-")
	if err != nil {
		return "", fmt.Errorf("%w: 스크래치 생성 실패", ErrSetup)
	}
	return dir, nil
}

// SweepStale: 기동 시 24h+ 스테일 스윕(D61 — best-effort, 실패 무시).
func SweepStale(parent string, ttl time.Duration) {
	ents, err := os.ReadDir(parent)
	if err != nil {
		return
	}
	cut := time.Now().Add(-ttl)
	for _, e := range ents {
		info, err := e.Info()
		if err != nil || !e.IsDir() {
			continue
		}
		if info.ModTime().Before(cut) {
			os.RemoveAll(filepath.Join(parent, e.Name()))
		}
	}
}

// baseKeys: D59 env allowlist 닫힌 표 — 열거 외 전부 차단.
func baseKeys() []string {
	if runtime.GOOS == "windows" {
		return []string{"PATH", "PATHEXT", "COMSPEC", "SystemRoot", "SystemDrive",
			"TEMP", "TMP", "USERPROFILE", "HOMEDRIVE", "HOMEPATH",
			"LOCALAPPDATA", "APPDATA", "ProgramFiles", "ProgramData", "PSModulePath"}
	}
	return []string{"PATH", "HOME", "TMPDIR", "LANG", "LC_ALL"}
}

// BaseEnv: 닫힌 표에 있는 변수만 현재 프로세스에서 복사(값 존재분만).
func BaseEnv() []string {
	var out []string
	for _, k := range baseKeys() {
		if v, ok := os.LookupEnv(k); ok {
			out = append(out, k+"="+v)
		}
	}
	return out
}
```

- [ ] **Step 4: 통과 확인**

Run: `go test -p 1 ./internal/sandbox/`
Expected: PASS (3 테스트)

- [ ] **Step 5: gofumpt·커밋**

```bash
gofumpt -l .
git add internal/sandbox/sandbox.go internal/sandbox/sandbox_test.go
git commit -m "feat(v0.11): internal/sandbox 공통 — 스크래치 수명·env 닫힌 표·ErrSetup (D59/D61)"
```

---

### Task 2: sandbox Windows — Job Object 실행·Probe

**Files:**
- Create: `internal/sandbox/run_windows.go` (`//go:build windows`)
- Test: `internal/sandbox/run_windows_test.go` (`//go:build windows`)

**Interfaces:**
- Consumes: `Spec`, `Result`, `ErrSetup` (T1)
- Produces (T4·T5가 사용): `func Run(ctx context.Context, s Spec) (Result, error)` · `func Probe(ctx context.Context) error`

**설계 노트:** Job Object에 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`를 걸고 자식을 `CREATE_SUSPENDED`로 만든 뒤 AssignProcessToJobObject → ResumeThread 순서(자식이 잡 배정 전에 손자를 만들지 못하게). Job 핸들을 부모가 쥔 채 프로세스가 끝나면 잡을 닫아 트리째 종료. Assign 실패 시 자식을 죽이고 `ErrSetup`. 표준 라이브러리 `os/exec` + `golang.org/x/sys/windows` 사용. stdout/stderr는 상한 버퍼(capWriter)로 읽어 초과분을 버린다.

- [ ] **Step 1: 실패 테스트 작성** — `run_windows_test.go`

```go
//go:build windows

package sandbox

import (
	"context"
	"testing"
	"time"
)

func TestRunWindowsEchoAndExit(t *testing.T) {
	dir := t.TempDir()
	s := Spec{
		Argv:      []string{"cmd.exe", "/c", "echo hi& exit 3"},
		Dir:       dir,
		Env:       BaseEnv(),
		Timeout:   30 * time.Second,
		StdoutCap: 32768, StderrCap: 8192,
	}
	r, err := Run(context.Background(), s)
	if err != nil {
		t.Fatalf("Run: %v", err)
	}
	if r.ExitCode != 3 {
		t.Fatalf("exit=%d want 3", r.ExitCode)
	}
	if got := string(r.Stdout); got == "" || got[:2] != "hi" {
		t.Fatalf("stdout=%q", got)
	}
}

func TestRunWindowsTimeoutKillsTree(t *testing.T) {
	dir := t.TempDir()
	s := Spec{
		Argv:      []string{"cmd.exe", "/c", "ping -n 30 127.0.0.1 >NUL"},
		Dir:       dir,
		Env:       BaseEnv(),
		Timeout:   1 * time.Second,
		StdoutCap: 32768, StderrCap: 8192,
	}
	start := time.Now()
	r, err := Run(context.Background(), s)
	if err != nil {
		t.Fatalf("Run: %v", err)
	}
	if !r.TimedOut {
		t.Fatalf("TimedOut=false")
	}
	if time.Since(start) > 10*time.Second {
		t.Fatalf("트리 미종료 — 지연 %v", time.Since(start))
	}
}

func TestProbeWindows(t *testing.T) {
	if err := Probe(context.Background()); err != nil {
		t.Fatalf("Probe: %v", err)
	}
}
```

- [ ] **Step 2: 실패 확인**

Run: `go test -p 1 ./internal/sandbox/`
Expected: FAIL ("Run/Probe not defined")

- [ ] **Step 3: 구현** — `run_windows.go`

```go
//go:build windows

package sandbox

import (
	"bytes"
	"context"
	"fmt"
	"os/exec"
	"time"

	"golang.org/x/sys/windows"
)

// capWriter: 상한까지만 담고 이후는 버린다(트렁케이션 표시).
type capWriter struct {
	buf   bytes.Buffer
	cap   int
	trunc bool
}

func (c *capWriter) Write(p []byte) (int, error) {
	if room := c.cap - c.buf.Len(); room > 0 {
		if len(p) > room {
			c.buf.Write(p[:room])
			c.trunc = true
		} else {
			c.buf.Write(p)
		}
	} else if len(p) > 0 {
		c.trunc = true
	}
	return len(p), nil // 파이프는 계속 소비(러너가 EPIPE로 죽지 않게)
}

func newJob() (windows.Handle, error) {
	h, err := windows.CreateJobObject(nil, nil)
	if err != nil {
		return 0, err
	}
	var info windows.JOBOBJECT_EXTENDED_LIMIT_INFORMATION
	info.BasicLimitInformation.LimitFlags = windows.JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE
	if _, err := windows.SetInformationJobObject(h,
		windows.JobObjectExtendedLimitInformation,
		uintptr(unsafePtr(&info)), uint32(unsafeSize(&info))); err != nil {
		windows.CloseHandle(h)
		return 0, err
	}
	return h, nil
}

// Probe: Job 생성/해제가 이 환경에서 가능한지 최소 확인(fail-closed 등록용).
func Probe(_ context.Context) error {
	h, err := newJob()
	if err != nil {
		return fmt.Errorf("%w: Job Object 생성 불가: %v", ErrSetup, err)
	}
	windows.CloseHandle(h)
	return nil
}

func Run(ctx context.Context, s Spec) (Result, error) {
	job, err := newJob()
	if err != nil {
		return Result{}, fmt.Errorf("%w: Job 생성: %v", ErrSetup, err)
	}
	defer windows.CloseHandle(job) // 닫으면 kill-on-job-close로 트리 종료

	cmd := exec.Command(s.Argv[0], s.Argv[1:]...)
	cmd.Dir = s.Dir
	cmd.Env = s.Env
	cmd.SysProcAttr = &windows.SysProcAttr{CreationFlags: windows.CREATE_SUSPENDED}
	out := &capWriter{cap: s.StdoutCap}
	errw := &capWriter{cap: s.StderrCap}
	cmd.Stdout, cmd.Stderr = out, errw

	if err := cmd.Start(); err != nil {
		return Result{}, fmt.Errorf("%w: start: %v", ErrSetup, err)
	}
	ph, err := windows.OpenProcess(windows.PROCESS_ALL_ACCESS, false, uint32(cmd.Process.Pid))
	if err == nil {
		err = windows.AssignProcessToJobObject(job, ph)
		windows.CloseHandle(ph)
	}
	if err != nil {
		cmd.Process.Kill()
		return Result{}, fmt.Errorf("%w: assign: %v", ErrSetup, err)
	}
	if err := resumeMainThread(cmd.Process.Pid); err != nil {
		cmd.Process.Kill()
		return Result{}, fmt.Errorf("%w: resume: %v", ErrSetup, err)
	}

	start := time.Now()
	done := make(chan error, 1)
	go func() { done <- cmd.Wait() }()
	timer := time.NewTimer(s.Timeout)
	defer timer.Stop()

	res := Result{StdoutCap: 0} //nolint — 필드 없음, 아래에서 채움
	timedOut := false
	select {
	case werr := <-done:
		res.ExitCode = exitCodeOf(werr, cmd)
	case <-timer.C:
		timedOut = true
		windows.CloseHandle(job) // 트리 즉시 종료
		<-done                   // Wait 회수
		res.ExitCode = -1
	case <-ctx.Done():
		windows.CloseHandle(job)
		<-done
		res.ExitCode = -1
	}
	res.Stdout, res.StdoutTrunc = out.buf.Bytes(), out.trunc
	res.Stderr, res.StderrTrunc = errw.buf.Bytes(), errw.trunc
	res.TimedOut = timedOut
	res.Duration = time.Since(start)
	return res, nil
}
```

> **구현 주의(implementer):** `unsafePtr`/`unsafeSize`는 `unsafe.Pointer(&info)`·`unsafe.Sizeof(info)`를 뜻하는 자리표시 — 실제로는 `import "unsafe"` 후 `uintptr(unsafe.Pointer(&info))`, `uint32(unsafe.Sizeof(info))`로 인라인하라. `resumeMainThread`는 `CreateToolhelp32Snapshot`로 프로세스의 첫 스레드를 찾아 `ResumeThread`한다(x/sys/windows 제공). `exitCodeOf(werr, cmd)`는 `cmd.ProcessState.ExitCode()`를 반환하되 werr가 `*exec.ExitError`가 아니고 non-nil이면 -1. `res := Result{StdoutCap:0}` 라인의 주석 표기는 잘못됨 — `var res Result`로 시작하라.

- [ ] **Step 4: 통과 확인** (Windows 러너)

Run: `go test -p 1 ./internal/sandbox/`
Expected: PASS (Windows 3 테스트 — echo/timeout/probe)

- [ ] **Step 5: gofumpt·커밋**

```bash
gofumpt -l .
git add internal/sandbox/run_windows.go internal/sandbox/run_windows_test.go
git commit -m "feat(v0.11): sandbox Windows Job Object 실행·Probe — 트리 종료·출력 캡 (D59)"
```

---

### Task 3: sandbox Unix — setpgid 실행 + 런처 FS 제한(landlock/sandbox-exec)

**Files:**
- Create: `internal/sandbox/run_unix.go` (`//go:build !windows`)
- Create: `internal/sandbox/launcher_linux.go` (`//go:build linux`)
- Create: `internal/sandbox/launcher_darwin.go` (`//go:build darwin`)
- Create: `internal/sandbox/launcher_other.go` (`//go:build !windows && !linux && !darwin`)
- Test: `internal/sandbox/run_unix_test.go` (`//go:build !windows`)

**Interfaces:**
- Consumes: `Spec`, `Result`, `ErrSetup`, `capWriter`(T2가 windows 전용이므로 **T3에서 unix용 capWriter를 별도 파일에 두지 말고 sandbox.go 공통으로 승격** — T1 sandbox.go에 capWriter를 옮기고 run_windows.go의 정의는 제거. 이 리팩터는 T3 Step 0으로 수행)
- Produces:
  - run_unix.go: `func Run(ctx context.Context, s Spec) (Result, error)` · `func Probe(context.Context) error`(unix는 항상 nil — landlock BestEffort라 프로브 비대상) · `func RunLauncher(args []string) error`
  - launcher_*.go: `func wrapArgv(scratch string, argv []string, selfExe string) []string`(FS 제한을 입히는 argv 변형) · `func applyLauncherRestriction(scratch string) error`(Linux만 실제 동작)
- SelfExe: sandbox는 자기 바이너리 경로가 필요하다 — `Spec`에 `SelfExe string` 필드 추가(T1 Spec 확장, T2 windows는 미사용)

**설계 노트:** Linux는 landlock을 **자식 프로세스 자신**이 걸어야 하므로 `selfExe __exec-launcher <scratch> -- <argv...>`로 재실행(`__transform-worker` 선례). 런처가 `applyLauncherRestriction`(landlock: 쓰기=scratch·읽기=전역)을 건 뒤 `syscall.Exec`로 실제 argv 대체. macOS는 `sandbox-exec -p <profile> <argv...>`로 argv를 감싸므로 재실행 불필요(런처 미사용). 기타 유닉스는 제한 없이 그대로(BestEffort 통과). 부모는 `Setpgid:true` + 타임아웃 시 `syscall.Kill(-pgid, SIGKILL)`로 그룹 종료.

- [ ] **Step 0: capWriter 공통 승격 리팩터**

`internal/sandbox/sandbox.go`에 `capWriter`(T2 Step3의 정의 그대로)를 옮기고, `run_windows.go`에서 그 정의를 삭제(Windows 빌드는 공통 것을 사용). `Spec`에 `SelfExe string` 필드 추가.

Run: `go build ./internal/sandbox/` → 성공 확인 후 진행.

- [ ] **Step 1: 실패 테스트 작성** — `run_unix_test.go`

```go
//go:build !windows

package sandbox

import (
	"context"
	"os"
	"path/filepath"
	"testing"
	"time"
)

func selfExe(t *testing.T) string {
	t.Helper()
	p, err := os.Executable()
	if err != nil {
		t.Fatal(err)
	}
	return p
}

func TestRunUnixEchoExit(t *testing.T) {
	dir := t.TempDir()
	s := Spec{
		Argv: []string{"/bin/sh", "-c", "printf hi; exit 5"},
		Dir:  dir, Env: BaseEnv(), SelfExe: selfExe(t),
		Timeout: 30 * time.Second, StdoutCap: 32768, StderrCap: 8192,
	}
	r, err := Run(context.Background(), s)
	if err != nil {
		t.Fatalf("Run: %v", err)
	}
	if r.ExitCode != 5 || string(r.Stdout) != "hi" {
		t.Fatalf("exit=%d out=%q", r.ExitCode, r.Stdout)
	}
}

func TestRunUnixTimeoutKillsGroup(t *testing.T) {
	dir := t.TempDir()
	s := Spec{
		Argv: []string{"/bin/sh", "-c", "sleep 30"},
		Dir:  dir, Env: BaseEnv(), SelfExe: selfExe(t),
		Timeout: 1 * time.Second, StdoutCap: 32768, StderrCap: 8192,
	}
	start := time.Now()
	r, err := Run(context.Background(), s)
	if err != nil {
		t.Fatalf("Run: %v", err)
	}
	if !r.TimedOut || time.Since(start) > 10*time.Second {
		t.Fatalf("그룹 미종료 TimedOut=%v dur=%v", r.TimedOut, time.Since(start))
	}
}

func TestStdoutCapTruncates(t *testing.T) {
	dir := t.TempDir()
	s := Spec{
		Argv: []string{"/bin/sh", "-c", "yes x | head -c 100000"},
		Dir:  dir, Env: BaseEnv(), SelfExe: selfExe(t),
		Timeout: 30 * time.Second, StdoutCap: 32768, StderrCap: 8192,
	}
	r, err := Run(context.Background(), s)
	if err != nil {
		t.Fatalf("Run: %v", err)
	}
	if !r.StdoutTrunc || len(r.Stdout) != 32768 {
		t.Fatalf("cap 미적용 trunc=%v len=%d", r.StdoutTrunc, len(r.Stdout))
	}
	_ = filepath.Separator
}
```

- [ ] **Step 2: 실패 확인**

Run: `go test -p 1 ./internal/sandbox/`
Expected: FAIL (Run/RunLauncher/wrapArgv 미정의)

- [ ] **Step 3: 구현** — `run_unix.go`

```go
//go:build !windows

package sandbox

import (
	"context"
	"fmt"
	"os/exec"
	"syscall"
	"time"
)

// Probe: unix는 landlock/sandbox-exec가 BestEffort라 등록 프로브 비대상(항상 nil).
func Probe(context.Context) error { return nil }

func Run(ctx context.Context, s Spec) (Result, error) {
	argv := wrapArgv(s.Dir, s.Argv, s.SelfExe) // OS별 FS 제한 입힘
	cmd := exec.Command(argv[0], argv[1:]...)
	cmd.Dir = s.Dir
	cmd.Env = s.Env
	cmd.SysProcAttr = &syscall.SysProcAttr{Setpgid: true}
	out := &capWriter{cap: s.StdoutCap}
	errw := &capWriter{cap: s.StderrCap}
	cmd.Stdout, cmd.Stderr = out, errw

	if err := cmd.Start(); err != nil {
		return Result{}, fmt.Errorf("%w: start: %v", ErrSetup, err)
	}
	pgid := cmd.Process.Pid // Setpgid로 pgid == pid
	kill := func() { syscall.Kill(-pgid, syscall.SIGKILL) }

	start := time.Now()
	done := make(chan error, 1)
	go func() { done <- cmd.Wait() }()
	timer := time.NewTimer(s.Timeout)
	defer timer.Stop()

	var res Result
	select {
	case werr := <-done:
		res.ExitCode = exitCodeOf(werr, cmd)
	case <-timer.C:
		res.TimedOut = true
		kill()
		<-done
		res.ExitCode = -1
	case <-ctx.Done():
		kill()
		<-done
		res.ExitCode = -1
	}
	res.Stdout, res.StdoutTrunc = out.buf.Bytes(), out.trunc
	res.Stderr, res.StderrTrunc = errw.buf.Bytes(), errw.trunc
	res.Duration = time.Since(start)
	return res, nil
}

// exitCodeOf: 정상 종료면 exit code, 그 외 -1.
func exitCodeOf(werr error, cmd *exec.Cmd) int {
	if cmd.ProcessState != nil {
		return cmd.ProcessState.ExitCode()
	}
	if werr != nil {
		return -1
	}
	return 0
}
```

`launcher_linux.go`:

```go
//go:build linux

package sandbox

import (
	"fmt"
	"os"
	"syscall"

	"github.com/landlock-lsm/go-landlock/landlock"
)

// wrapArgv: 자기 재실행 런처로 감싼다 — 런처가 landlock을 건 뒤 실제 argv로 대체.
func wrapArgv(scratch string, argv []string, selfExe string) []string {
	out := []string{selfExe, "__exec-launcher", scratch, "--"}
	return append(out, argv...)
}

// applyLauncherRestriction: 쓰기=scratch·읽기=전역(BestEffort). 미지원 커널은 통과.
func applyLauncherRestriction(scratch string) error {
	return landlock.V5.BestEffort().RestrictPaths(
		landlock.RWDirs(scratch),
		landlock.RODirs("/"),
	)
}

// RunLauncher: `__exec-launcher <scratch> -- <argv...>` 진입점(main.go가 분기).
func RunLauncher(args []string) error {
	if len(args) < 2 || args[1] != "--" {
		return fmt.Errorf("exec-launcher: 잘못된 인자")
	}
	scratch, argv := args[0], args[2:]
	if err := applyLauncherRestriction(scratch); err != nil {
		return fmt.Errorf("exec-launcher: 제한 적용 실패: %w", err)
	}
	if len(argv) == 0 {
		return fmt.Errorf("exec-launcher: 빈 argv")
	}
	return syscall.Exec(argv[0], argv, os.Environ()) // 성공 시 반환 없음
}
```

`launcher_darwin.go`:

```go
//go:build darwin

package sandbox

import "fmt"

// wrapArgv: sandbox-exec 프로필로 argv를 감싼다(쓰기=scratch·읽기 허용). 재실행 불필요.
func wrapArgv(scratch string, argv []string, _ string) []string {
	profile := fmt.Sprintf(`(version 1)(allow default)(deny file-write*)(allow file-write* (subpath %q))`, scratch)
	return append([]string{"/usr/bin/sandbox-exec", "-p", profile}, argv...)
}

// RunLauncher: darwin은 런처 재실행 경로 미사용.
func RunLauncher([]string) error { return fmt.Errorf("exec-launcher: darwin 미사용") }
```

`launcher_other.go`:

```go
//go:build !windows && !linux && !darwin

package sandbox

import "fmt"

// wrapArgv: FS 제한 미지원 OS — BestEffort 통과(제한 없이 그대로).
func wrapArgv(_ string, argv []string, _ string) []string { return argv }

func RunLauncher([]string) error { return fmt.Errorf("exec-launcher: 미지원 OS") }
```

- [ ] **Step 4: 통과 확인**

Run: `go test -p 1 ./internal/sandbox/`
Expected: PASS (unix 3 테스트). Linux면 landlock 경로도 자기재실행으로 실행됨(테스트 바이너리가 `__exec-launcher`를 모르면 실패하므로 — **이 테스트는 T6의 main.go 분기 전에는 landlock 경로가 안 도므로, T3에서는 `wrapArgv`가 self 재실행이어도 실제 exec 대상이 테스트 바이너리라 무한재귀 위험**. 회피: T3 테스트는 `SelfExe`를 실제 ctr 바이너리가 아닌 값으로 두지 말고, **Linux에서 `wrapArgv` 단위 테스트로 argv 형태만 검증**하고 통합 실행은 T6 이후 exec 패키지 통합테스트에서 수행. run_unix_test.go의 3개 실행 테스트는 `wrapArgv`를 우회하도록 Run에 `s.SelfExe==""`면 제한 생략하는 분기를 넣는다).

> **implementer 주의:** landlock 자기재실행이 테스트 바이너리를 재귀 호출하지 않도록, `Run`은 `s.SelfExe == ""`일 때 `wrapArgv`를 건너뛰고 원 argv로 실행한다(단위 테스트·제한 불요 경로). 통합 실행 검증은 T6(main.go 분기) 뒤 exec 패키지에서. `wrapArgv` 자체는 별도 단위 테스트(`TestWrapArgvLinux`)로 argv 형태를 검증.

- [ ] **Step 5: go.mod에 landlock 추가 + gofumpt·커밋**

```bash
go get github.com/landlock-lsm/go-landlock/landlock@v0.9.0
go mod tidy
gofumpt -l .
git add internal/sandbox/ go.mod go.sum
git commit -m "feat(v0.11): sandbox Unix setpgid 실행 + 런처 FS 제한(landlock/sandbox-exec) (D59)"
```

---

### Task 4: internal/exec — 러너 테이블·감지·버전 게이트·env 재지정

**Files:**
- Create: `internal/exec/exec.go`
- Test: `internal/exec/exec_test.go`

**Interfaces:**
- Consumes: `sandbox.Spec/Result/Run/ErrSetup` (T1–T3)
- Produces (T5가 사용):
  - `type Request struct { Language, Code, FilePath string; TimeoutMS int }`
  - `type Response struct { Stdout, Stderr string; ExitCode *int; TimedOut, StdoutTrunc, StderrTrunc bool; DurationMS int64; Runner string }` (ExitCode는 timed_out 시 nil — D61)
  - `var ErrUnsupportedLang, ErrToolchainMissing, ErrVersionGate = errors.New(...)` (도구 오류 sentinel — mcp가 매핑)
  - `func Run(ctx context.Context, scratchParent, selfExe string, req Request) (Response, error)`
  - `func RunnerStatus() []LangStatus` (doctor [18]용 — `type LangStatus struct { Lang, Runner, Version string; OK bool }`)
- 상한: `clampTimeout(ms int) time.Duration` — 기본 120000·상한 1800000.

**설계 노트:** 언어별 `runner` 구조(감지 우선순위·버전 게이트·argv 조립·env 재지정)를 테이블로. 스니펫은 스크래치에 언어별 파일명으로 기록(예: `snippet.go`는 `go run`이 요구하는 확장자). C#은 `dotnet run <file.cs>` + in-job env 3종. env는 `sandbox.BaseEnv()` + 러너 재지정(스크래치 하위 캐시 경로 생성)을 합쳐 `Spec.Env`로.

- [ ] **Step 1: 실패 테스트** — `exec_test.go`

```go
package exec

import (
	"context"
	"os"
	"os/exec"
	"strings"
	"testing"
)

func selfExe(t *testing.T) string {
	t.Helper()
	p, _ := os.Executable()
	return p
}

func TestRunShellEcho(t *testing.T) {
	if _, err := exec.LookPath(shellName()); err != nil {
		t.Skip("shell 미설치")
	}
	resp, err := Run(context.Background(), t.TempDir(), selfExe(t),
		Request{Language: "shell", Code: "echo hello"})
	if err != nil {
		t.Fatalf("Run: %v", err)
	}
	if !strings.Contains(resp.Stdout, "hello") {
		t.Fatalf("stdout=%q", resp.Stdout)
	}
	if resp.ExitCode == nil || *resp.ExitCode != 0 {
		t.Fatalf("exit=%v", resp.ExitCode)
	}
	if resp.Runner == "" {
		t.Fatalf("runner 미기록")
	}
}

func TestUnsupportedLang(t *testing.T) {
	_, err := Run(context.Background(), t.TempDir(), selfExe(t),
		Request{Language: "brainfuck", Code: "+"})
	if err == nil || !strings.Contains(err.Error(), "지원") {
		t.Fatalf("미지원 언어 오류 아님: %v", err)
	}
}

func TestClampTimeout(t *testing.T) {
	if clampTimeout(0).Milliseconds() != 120000 {
		t.Fatalf("기본 120s 아님")
	}
	if clampTimeout(9_000_000).Milliseconds() != 1_800_000 {
		t.Fatalf("상한 1800s 아님")
	}
}
```

> `shellName()`는 exec.go의 미노출 헬퍼(Win `pwsh`/Unix `sh`) — 테스트가 같은 패키지라 접근 가능.

- [ ] **Step 2: 실패 확인** — `go test -p 1 ./internal/exec/` → FAIL(미정의)

- [ ] **Step 3: 구현** — `exec.go`

핵심 골격(전체 러너 테이블). 각 러너는 `detect()`(LookPath+버전 게이트)·`argv(file)`·`redirectEnv(scratch)`를 제공:

```go
// Package exec — ctr_execute/execute_file의 러너 테이블(설계 v0.11 D60).
// sandbox 위에서 언어별 toolchain을 감지·실행한다. 내장 인터프리터 없음.
package exec

import (
	"context"
	"errors"
	"fmt"
	"os"
	"os/exec"
	"path/filepath"
	"runtime"
	"strconv"
	"strings"
	"time"

	"github.com/wotjr1649/context-router/internal/sandbox"
)

var (
	ErrUnsupportedLang  = errors.New("exec: 지원하지 않는 언어")
	ErrToolchainMissing = errors.New("exec: toolchain 미설치")
	ErrVersionGate      = errors.New("exec: toolchain 버전 미달")
)

const (
	defaultTimeout = 120_000
	maxTimeout     = 1_800_000
	stdoutCap      = 32768
	stderrCap      = 8192
)

type Request struct {
	Language, Code, FilePath string
	TimeoutMS                int
}

type Response struct {
	Stdout, Stderr string
	ExitCode       *int // timed_out 시 nil (D61)
	TimedOut       bool
	StdoutTrunc    bool
	StderrTrunc    bool
	DurationMS     int64
	Runner         string
}

func clampTimeout(ms int) time.Duration {
	if ms <= 0 {
		ms = defaultTimeout
	} else if ms > maxTimeout {
		ms = maxTimeout
	}
	return time.Duration(ms) * time.Millisecond
}

// runner: 한 언어의 실행 계약.
type runner struct {
	file    string                              // 스니펫 파일명
	detect  func() (bin, version string, err error)
	argv    func(bin, file string) []string
	extra   func(scratch string) []string       // 러너별 재지정/강제 env (nil 가능)
}

func shellName() string {
	if runtime.GOOS == "windows" {
		return "pwsh"
	}
	return "sh"
}

// lookVersioned: bins를 우선순위로 찾고 minGate(있으면)로 버전 게이트.
func lookVersioned(bins []string) (string, error) {
	for _, b := range bins {
		if p, err := exec.LookPath(b); err == nil {
			return p, nil
		}
	}
	return "", ErrToolchainMissing
}

func table() map[string]runner {
	return map[string]runner{
		"shell":      shellRunner(),
		"javascript": {file: "snippet.js", detect: detectJS, argv: fileArgv},
		"typescript": tsRunner(),
		"python":     {file: "snippet.py", detect: detectPy, argv: fileArgv, extra: pyEnv},
		"go":         {file: "snippet.go", detect: detectGo, argv: goArgv, extra: goEnv},
		"csharp":     {file: "snippet.cs", detect: detectCS, argv: csArgv, extra: csEnv},
	}
}

func Run(ctx context.Context, scratchParent, selfExe string, req Request) (Response, error) {
	r, ok := table()[req.Language]
	if !ok {
		return Response{}, fmt.Errorf("%w: %s", ErrUnsupportedLang, req.Language)
	}
	bin, version, err := r.detect()
	if err != nil {
		return Response{}, err // ErrToolchainMissing / ErrVersionGate
	}
	scratch, err := sandbox.NewScratch(scratchParent)
	if err != nil {
		return Response{}, err // ErrSetup
	}
	defer os.RemoveAll(scratch)

	code := req.Code
	env := sandbox.BaseEnv()
	if r.extra != nil {
		env = append(env, r.extra(scratch)...)
	}
	// execute_file: 파일 경로를 CTR_FILE로 노출(스니펫이 읽음)
	if req.FilePath != "" {
		env = append(env, "CTR_FILE="+req.FilePath)
	}
	env = append(env, "CTR_SCRATCH="+scratch)

	file := filepath.Join(scratch, r.file)
	if err := os.WriteFile(file, []byte(code), 0o600); err != nil {
		return Response{}, fmt.Errorf("%w: 스니펫 기록 실패", sandbox.ErrSetup)
	}
	spec := sandbox.Spec{
		Argv: r.argv(bin, file), Dir: scratch, Env: env, SelfExe: selfExe,
		Timeout: clampTimeout(req.TimeoutMS), StdoutCap: stdoutCap, StderrCap: stderrCap,
	}
	res, err := sandbox.Run(ctx, spec)
	if err != nil {
		return Response{}, err // ErrSetup 계열
	}
	resp := Response{
		Stdout: string(res.Stdout), Stderr: string(res.Stderr),
		TimedOut: res.TimedOut, StdoutTrunc: res.StdoutTrunc, StderrTrunc: res.StderrTrunc,
		DurationMS: res.Duration.Milliseconds(), Runner: runnerLabel(req.Language, bin, version),
	}
	if !res.TimedOut {
		ec := res.ExitCode
		resp.ExitCode = &ec
	}
	return resp, nil
}

func runnerLabel(lang, bin, version string) string {
	name := filepath.Base(bin)
	if version != "" {
		return name + " " + version
	}
	return name
}

func fileArgv(bin, file string) []string { return []string{bin, file} }
func goArgv(bin, file string) []string   { return []string{bin, "run", file} }
func csArgv(bin, file string) []string   { return []string{bin, "run", file} }
```

이어서 언어별 detect/env 헬퍼(같은 파일):

```go
func shellRunner() runner {
	if runtime.GOOS == "windows" {
		return runner{file: "snippet.ps1",
			detect: func() (string, string, error) {
				if p, err := exec.LookPath("pwsh"); err == nil {
					return p, "7", nil
				}
				if p, err := exec.LookPath("powershell"); err == nil {
					return p, "5.1", nil // runner 필드로 5.1 가시화 (D60)
				}
				return "", "", ErrToolchainMissing
			},
			argv: func(bin, file string) []string {
				return []string{bin, "-NoProfile", "-NonInteractive", "-File", file}
			}}
	}
	return runner{file: "snippet.sh",
		detect: func() (string, string, error) {
			p, err := exec.LookPath("sh")
			return p, "", err
		},
		argv: func(bin, file string) []string { return []string{bin, file} }}
}

func detectJS() (string, string, error) {
	p, err := lookVersioned([]string{"bun", "node"})
	return p, "", err
}

func tsRunner() runner {
	return runner{file: "snippet.ts",
		detect: detectTS,
		argv: func(bin, file string) []string {
			if filepath.Base(bin) == "bun" || strings.HasPrefix(filepath.Base(bin), "bun") {
				return []string{bin, file}
			}
			return []string{bin, "--experimental-transform-types", file}
		}}
}

// detectTS: bun 우선, 없으면 node ≥22.7 게이트.
func detectTS() (string, string, error) {
	if p, err := exec.LookPath("bun"); err == nil {
		return p, "", nil
	}
	p, err := exec.LookPath("node")
	if err != nil {
		return "", "", ErrToolchainMissing
	}
	out, err := exec.Command(p, "--version").Output() // "v22.7.0\n"
	if err != nil {
		return "", "", ErrToolchainMissing
	}
	if !nodeAtLeast(strings.TrimSpace(string(out)), 22, 7) {
		return "", "", fmt.Errorf("%w: node ≥22.7 필요", ErrVersionGate)
	}
	return p, "", nil
}

// nodeAtLeast: "vMAJOR.MINOR.PATCH"가 (major,minor) 이상인가.
func nodeAtLeast(v string, major, minor int) bool {
	v = strings.TrimPrefix(v, "v")
	parts := strings.SplitN(v, ".", 3)
	if len(parts) < 2 {
		return false
	}
	maj, _ := strconv.Atoi(parts[0])
	min, _ := strconv.Atoi(parts[1])
	if maj != major {
		return maj > major
	}
	return min >= minor
}

func detectPy() (string, string, error) {
	p, err := lookVersioned([]string{"python3", "python"})
	return p, "", err
}
func pyEnv(scratch string) []string {
	return []string{"PYTHONPYCACHEPREFIX=" + filepath.Join(scratch, "pycache")}
}

func detectGo() (string, string, error) {
	p, err := exec.LookPath("go")
	return p, "", err
}
func goEnv(scratch string) []string {
	return []string{
		"GOCACHE=" + filepath.Join(scratch, "go-build"),
		"GOPATH=" + filepath.Join(scratch, "go"),
		"GOTMPDIR=" + filepath.Join(scratch, "go-tmp"),
	}
}

// detectCS: dotnet + SDK ≥10 게이트.
func detectCS() (string, string, error) {
	p, err := exec.LookPath("dotnet")
	if err != nil {
		return "", "", ErrToolchainMissing
	}
	out, err := exec.Command(p, "--list-sdks").Output()
	if err != nil {
		return "", "", ErrToolchainMissing
	}
	if !dotnetHasMajor(string(out), 10) {
		return "", "", fmt.Errorf("%w: .NET SDK ≥10 필요", ErrVersionGate)
	}
	return p, "10+", nil
}

// dotnetHasMajor: --list-sdks 출력에 major 이상 SDK가 하나라도 있는가.
func dotnetHasMajor(list string, major int) bool {
	for _, line := range strings.Split(list, "\n") {
		line = strings.TrimSpace(line)
		if line == "" {
			continue
		}
		maj, _ := strconv.Atoi(strings.SplitN(line, ".", 2)[0])
		if maj >= major {
			return true
		}
	}
	return false
}
func csEnv(scratch string) []string {
	return []string{
		"DOTNET_CLI_HOME=" + filepath.Join(scratch, "dotnet"),
		"NUGET_PACKAGES=" + filepath.Join(scratch, "nuget"),
		"DOTNET_CLI_DO_NOT_USE_MSBUILD_SERVER=1",
		"MSBUILDDISABLENODEREUSE=1",
		"UseSharedCompilation=false",
	}
}

// RunnerStatus: doctor [18]용 — 각 언어의 감지 결과(실행 없음).
type LangStatus struct {
	Lang, Runner, Version string
	OK                    bool
}

func RunnerStatus() []LangStatus {
	langs := []string{"shell", "javascript", "typescript", "python", "go", "csharp"}
	var out []LangStatus
	for _, l := range langs {
		r := table()[l]
		bin, ver, err := r.detect()
		st := LangStatus{Lang: l, OK: err == nil, Version: ver}
		if err == nil {
			st.Runner = filepath.Base(bin)
		}
		out = append(out, st)
	}
	return out
}
```

- [ ] **Step 4: 통과 확인** — `go test -p 1 ./internal/exec/` → PASS(shell·미지원·clamp). CI가 제공하는 언어는 각 러너 실행 테스트 추가(감지 실패 시 Skip).

- [ ] **Step 5: gofumpt·커밋**

```bash
gofumpt -l .
git add internal/exec/exec.go internal/exec/exec_test.go
git commit -m "feat(v0.11): internal/exec 러너 테이블 6언어 — 감지·버전 게이트·캐시 재지정 (D60)"
```

---

### Task 5: mcp 등록 — exec 프로필 게이트 + 프로브 + 도구 2종 + sentinel 배선

**Files:**
- Modify: `internal/mcp/mcp.go` (NewServer 등록부·toToolError·신규 registerExecute)
- Test: `internal/mcp/mcp_execute_test.go`

**Interfaces:**
- Consumes: `exec.Run/Request/Response`, sentinel `exec.Err*`, `sandbox.ErrSetup`, `sandbox.Probe`
- Produces: `ctr_execute`·`ctr_execute_file` 도구; `Config`에 `ScratchRoot string`·`SelfExe`(기존) 사용
- `Config`에 필드 추가: `ScratchRoot string`(store-root 하위 exec 스크래치 부모)

**설계 노트:** `slices.Contains(cfg.Enable, "exec")`일 때만, 그리고 `sandbox.Probe(ctx)` 성공 시에만 등록(삼중 게이트 중 서버측 2개 — transform 프로브 선례). 실패는 slog.Warn + 미등록. toToolError에 exec sentinel 3종 + sandbox.ErrSetup 매핑 추가.

- [ ] **Step 1: 실패 테스트** — `mcp_execute_test.go`

```go
package mcp

import (
	"context"
	"strings"
	"testing"

	"github.com/modelcontextprotocol/go-sdk/mcp"
)

// exec 프로필 미활성 시 도구 미등록.
func TestExecuteNotRegisteredWithoutProfile(t *testing.T) {
	names := toolNamesFor(t, nil) // Enable 없음
	for _, n := range names {
		if n == "ctr_execute" || n == "ctr_execute_file" {
			t.Fatalf("exec 프로필 없이 %s 등록됨", n)
		}
	}
}

// exec 프로필 활성 시 2종 등록 + 실행 왕복.
func TestExecuteRegisteredAndRuns(t *testing.T) {
	cs := newTestServerWithExec(t) // Enable=["exec"], 프로브 통과 환경
	res, err := cs.CallTool(context.Background(), &mcp.CallToolParams{
		Name: "ctr_execute", Arguments: ExecuteInput{Language: "shell", Code: "echo ok"},
	})
	if err != nil {
		t.Fatalf("CallTool: %v", err)
	}
	var out ExecuteOutput
	decodeResult(t, res, &out)
	if !strings.Contains(out.Stdout, "ok") {
		t.Fatalf("stdout=%q", out.Stdout)
	}
}
```

> `toolNamesFor`/`newTestServerWithExec`/`decodeResult`는 mcp_test.go의 기존 헬퍼 패턴을 따른다(구현자: 기존 `TestProfileToolExposure`의 서버 생성 헬퍼 재사용, exec Enable만 추가). shell 미설치 CI에서는 `t.Skip`.

- [ ] **Step 2: 실패 확인** — `go test -p 1 ./internal/mcp/` → FAIL(ExecuteInput 미정의)

- [ ] **Step 3: 구현** — mcp.go에 추가

```go
// --- ctr_execute / ctr_execute_file (설계 v0.11 D58, Enable "exec" + 프로브 게이트) ---

type ExecuteInput struct {
	Language  string `json:"language" jsonschema:"실행 언어: shell|javascript|typescript|python|go|csharp"`
	Code      string `json:"code" jsonschema:"실행할 코드 — 파생 답만 print(대형 원문은 코드 내에서 집계·필터). 저장본의 무 I/O 변환은 ctr_transform"`
	TimeoutMS int    `json:"timeout_ms,omitempty" jsonschema:"실행 시간 상한(ms), 기본 120000, 최대 1800000"`
}

type ExecuteFileInput struct {
	Language  string `json:"language" jsonschema:"실행 언어: shell|javascript|typescript|python|go|csharp"`
	Path      string `json:"path" jsonschema:"CTR_FILE로 스니펫에 전달할 파일 경로(절대·존재)"`
	Code      string `json:"code" jsonschema:"CTR_FILE을 처리하는 코드"`
	TimeoutMS int    `json:"timeout_ms,omitempty" jsonschema:"실행 시간 상한(ms), 기본 120000, 최대 1800000"`
}

type ExecuteOutput struct {
	Stdout      string `json:"stdout"`
	Stderr      string `json:"stderr"`
	ExitCode    *int   `json:"exit_code"` // timed_out 시 null (D61)
	TimedOut    bool   `json:"timed_out"`
	StdoutTrunc bool   `json:"stdout_truncated"`
	StderrTrunc bool   `json:"stderr_truncated"`
	DurationMS  int64  `json:"duration_ms"`
	Runner      string `json:"runner"`
}

func toExecuteOutput(r exec.Response) ExecuteOutput {
	return ExecuteOutput{
		Stdout: r.Stdout, Stderr: r.Stderr, ExitCode: r.ExitCode, TimedOut: r.TimedOut,
		StdoutTrunc: r.StdoutTrunc, StderrTrunc: r.StderrTrunc,
		DurationMS: r.DurationMS, Runner: r.Runner,
	}
}

func registerExecute(srv *mcp.Server, scratchRoot, selfExe string) {
	mcp.AddTool(srv, &mcp.Tool{
		Name: "ctr_execute",
		Description: "샌드박스에서 코드를 실행하고 stdout만 반환한다(think-in-code — 큰 데이터는 " +
			"코드 안에서 집계해 파생 답만 print; 출력 초과 시 ctr_search 연계). 저장된 artifact의 " +
			"무 I/O·결정적 변환은 ctr_transform, 툴체인·파일시스템이 필요한 실행은 이 도구.",
	}, func(ctx context.Context, _ *mcp.CallToolRequest, in ExecuteInput) (*mcp.CallToolResult, ExecuteOutput, error) {
		start := time.Now()
		resp, err := exec.Run(ctx, scratchRoot, selfExe,
			exec.Request{Language: in.Language, Code: in.Code, TimeoutMS: in.TimeoutMS})
		if err != nil {
			return nil, ExecuteOutput{}, toToolError(err)
		}
		out := toExecuteOutput(resp)
		st.LedgerAppend("ctr_execute", 0, jsonLen(out), time.Since(start).Milliseconds())
		return nil, out, nil
	})
	// ctr_execute_file: path 검증 후 CTR_FILE로 전달(동일 계약)
	mcp.AddTool(srv, &mcp.Tool{
		Name: "ctr_execute_file",
		Description: "ctr_execute와 동일하되 CTR_FILE 환경변수로 파일 경로를 스니펫에 전달한다.",
	}, func(ctx context.Context, _ *mcp.CallToolRequest, in ExecuteFileInput) (*mcp.CallToolResult, ExecuteOutput, error) {
		start := time.Now()
		abs, verr := validateExecPath(in.Path)
		if verr != nil {
			return nil, ExecuteOutput{}, toToolError(verr)
		}
		resp, err := exec.Run(ctx, scratchRoot, selfExe,
			exec.Request{Language: in.Language, Code: in.Code, FilePath: abs, TimeoutMS: in.TimeoutMS})
		if err != nil {
			return nil, ExecuteOutput{}, toToolError(err)
		}
		out := toExecuteOutput(resp)
		st.LedgerAppend("ctr_execute_file", 0, jsonLen(out), time.Since(start).Milliseconds())
		return nil, out, nil
	})
}

// validateExecPath: 절대·존재·일반 파일 검증(원문 에코 없음, §6).
func validateExecPath(p string) (string, error) {
	if !filepath.IsAbs(p) {
		return "", exec.ErrInvalidPath
	}
	fi, err := os.Stat(p)
	if err != nil || fi.IsDir() {
		return "", exec.ErrInvalidPath
	}
	return p, nil
}
```

> **implementer 주의:** `registerExecute`는 store 참조(`st`)가 클로저에 필요하므로 실제 시그니처는 `registerExecute(srv, st, scratchRoot, selfExe)`로 하라(위 registerFetch 패턴과 동일 — `st.LedgerAppend` 사용). `exec.ErrInvalidPath`를 T4 exec.go sentinel에 추가(`errors.New("exec: 잘못된 파일 경로")`).

NewServer 등록부(mcp.go:79 부근)에 추가:

```go
	if slices.Contains(cfg.Enable, "exec") {
		if err := sandbox.Probe(context.Background()); err != nil {
			slog.Warn("mcp: exec 격리 프로브 실패 — ctr_execute 비활성화", "error", err)
		} else {
			registerExecute(srv, cfg.Store, cfg.ScratchRoot, cfg.SelfExe)
		}
	}
```

toToolError에 매핑 추가(mcp.go:141 부근, default 앞):

```go
	case errors.Is(err, exec.ErrUnsupportedLang), errors.Is(err, exec.ErrInvalidPath):
		return toolErr(codeInvalidArgument, "잘못된 실행 요청입니다")
	case errors.Is(err, exec.ErrToolchainMissing), errors.Is(err, exec.ErrVersionGate):
		return toolErr(codeUnsupportedFile, "요청한 실행 환경을 사용할 수 없습니다")
	case errors.Is(err, sandbox.ErrSetup):
		return toolErr(codeStorageUnavailable, "격리 실행을 준비할 수 없습니다")
```

- [ ] **Step 4: 통과 확인** — `go test -p 1 ./internal/mcp/` → PASS

- [ ] **Step 5: gofumpt·커밋**

```bash
gofumpt -l .
git add internal/mcp/mcp.go internal/mcp/mcp_execute_test.go
git commit -m "feat(v0.11): mcp ctr_execute/execute_file — exec 프로필+프로브 게이트·sentinel 배선 (D58/D61)"
```

---

### Task 6: main.go — `__exec-launcher` 분기 + `--enable exec` 배선

**Files:**
- Modify: `cmd/context-router/main.go` (worker 분기·enable 도움말·ScratchRoot/SelfExe 전달)
- Test: `cmd/context-router/main_test.go`(있으면 확장, 없으면 sandbox 통합테스트로 대체)

**Interfaces:**
- Consumes: `sandbox.RunLauncher`, `mcp.Config.ScratchRoot`
- Produces: exec 실행 경로 완결(Linux landlock 자기재실행이 실제로 돎)

**설계 노트:** `transformWorkerArg` 분기(main.go:586) 바로 옆에 `__exec-launcher` 분기 추가 — 플래그 파싱보다 먼저. `ScratchRoot`는 store-root 하위 `exec-scratch`로 유도하고 기동 시 `sandbox.SweepStale` 호출.

- [ ] **Step 1: 실패 테스트** — 통합: exec로 파일 쓰기가 스크래치 밖(Linux)에서 거부되는지(landlock 실증). CI Linux 전용.

```go
//go:build linux

package main_test

// TestExecLauncherLandlockBlocksOutsideWrite: ctr_execute go 스니펫이 워크트리 밖
// (예: $HOME/should-not-exist)에 쓰기를 시도하면 실패해야 한다(landlock 쓰기=스크래치).
// (실행: 빌드된 ctr 바이너리를 exec 프로필로 기동 — 세부는 기존 통합테스트 하네스 관례를 따름)
```

> 통합테스트 하네스가 무겁다면, T6은 **단위로 축소**: `main.go`가 `__exec-launcher` 인자를 받으면 `sandbox.RunLauncher`로 보내는지만 검증(플래그 파싱 이전 분기). landlock 실증은 §2 릴리스 게이트의 도그푸딩/CI job에서 수행.

- [ ] **Step 2: 실패 확인** — 해당 테스트 FAIL 또는 분기 부재 확인.

- [ ] **Step 3: 구현** — main.go

```go
const execLauncherArg = "__exec-launcher"
```

main() 진입부(transformWorkerArg 분기 옆):

```go
	if len(os.Args) > 1 && os.Args[1] == execLauncherArg {
		if err := sandbox.RunLauncher(os.Args[2:]); err != nil {
			fmt.Fprintln(os.Stderr, "ctr:", err)
			os.Exit(1)
		}
		return // RunLauncher 성공 시 syscall.Exec로 대체되어 도달 안 함(darwin/other는 오류)
	}
```

`--enable` 도움말 문자열(main.go:49)에 exec 추가: `"opt-in profiles: ingest,net,exec"`. run()이 mcp.Config를 만드는 지점에 `ScratchRoot: filepath.Join(storeRoot, projectID, "exec-scratch")` 설정 + 기동 시 `sandbox.SweepStale(scratchRoot, 24*time.Hour)`.

> **implementer:** ScratchRoot 정확 위치는 store가 프로젝트 디렉터리를 유도하는 기존 코드(`main.go`의 store.Open 경로)를 따른다 — worktree 스토어 디렉터리 하위에 둔다(session.db와 형제). `SelfExe`는 이미 transform용으로 Config에 세팅되어 있으면 재사용.

- [ ] **Step 4: 통과 확인** — `go build ./... && go test -p 1 ./...`(전체) GREEN. Linux면 landlock 통합테스트 PASS(또는 축소 단위 PASS).

- [ ] **Step 5: gofumpt·커밋**

```bash
gofumpt -l .
git add cmd/context-router/main.go
git commit -m "feat(v0.11): main __exec-launcher 분기 + --enable exec 배선·스크래치 스윕 (D58/D59)"
```

---

### Task 7: usage --adoption — 채택 계측

**Files:**
- Modify: `internal/cli/cli.go` (runUsage에 --adoption 분기 + runUsageAdoption)
- Test: `internal/cli/cli_test.go`(확장)

**Interfaces:**
- Consumes: session.db(loadCCSessions 선례의 store 접근) tool_call 이벤트
- Produces: `--adoption [--days N]` 출력 — ctr_* / ctx_* 절대 호출 수(1차) + 비율(참고)

**설계 노트:** session.db `session_events`에서 `event_type='tool_call'`을 summary별로 집계, `ctr_`/`mcp__ctr__` 접두 = ctr, `ctx_`/`ctxscribe` 포함 = ctxscribe로 분류. 1차 지표는 절대 호출 수(비율은 분모 붕괴 주의 문면과 함께 참고 병기 — 스펙 D62).

- [ ] **Step 1: 실패 테스트** — cli_test.go에 `TestUsageAdoptionCounts`: 임시 session.db에 tool_call 이벤트(ctr_search 2·ctx_execute 5) 주입 후 `--adoption` 출력에 `ctr\t2`·`ctxscribe\t5` 라인과 ratio 문자열이 있는지.

```go
func TestUsageAdoptionCounts(t *testing.T) {
	// 기존 session.db 픽스처 헬퍼(cli_test.go의 seedSessionDB 관례)로 이벤트 주입
	dir := seedAdoptionEvents(t, map[string]int{
		"mcp__ctr__ctr_search": 2, "mcp__plugin_ctxscribe_mcp__ctx_execute": 5,
	})
	var buf bytes.Buffer
	if err := runUsage(context.Background(), &buf, []string{"--adoption"}, dir.storeRoot, dir.projRoot); err != nil {
		t.Fatalf("runUsage: %v", err)
	}
	got := buf.String()
	if !strings.Contains(got, "ctr\t2") || !strings.Contains(got, "ctxscribe\t5") {
		t.Fatalf("집계 누락:\n%s", got)
	}
}
```

> `seedAdoptionEvents`는 cli_test.go의 기존 session.db 시딩 헬퍼를 따른다(구현자: `loadCCSessions` 테스트가 쓰는 픽스처 방식 재사용).

- [ ] **Step 2: 실패 확인** — FAIL(--adoption 미인식)

- [ ] **Step 3: 구현** — runUsage에 플래그·분기 추가

```go
	adoption := fs.Bool("adoption", false, "ctr vs ctxscribe MCP 호출 채택 계측(session.db 집계 — transcript 토큰 집계와 별개, 설계 v0.11 D62)")
	days := fs.Int("days", 0, "최근 N일로 제한(--adoption 전용, 0=전체)")
	// ... Parse 후:
	if *adoption {
		return runUsageAdoption(ctx, w, storeRoot, projectRoot, *days, time.Now())
	}
```

`runUsageAdoption`(새 함수) — session.db 열어 tool_call 집계:

```go
// runUsageAdoption: session_events(tool_call)를 ctr/ctxscribe로 분류 집계(설계 v0.11 D62).
// 1차 지표 = 절대 호출 수. 비율은 참고(브리지 종료 후 분모 붕괴 — 스펙 명시).
func runUsageAdoption(ctx context.Context, w io.Writer, storeRoot, projectRoot string, days int, now time.Time) error {
	db, err := openSessionDBReadOnly(storeRoot, projectRoot) // 기존 loadCCSessions의 열기 관례 재사용
	if err != nil {
		return errors.New("usage: 세션 저장소를 열 수 없습니다")
	}
	defer db.Close()
	q := "SELECT summary, COUNT(*) FROM session_events WHERE event_type='tool_call'"
	var args []any
	if days > 0 {
		q += " AND ts >= ?"
		args = append(args, now.AddDate(0, 0, -days).Unix())
	}
	q += " GROUP BY summary"
	rows, err := db.QueryContext(ctx, q, args...)
	if err != nil {
		return errors.New("usage: 집계 조회 실패")
	}
	defer rows.Close()
	var ctr, ctx int64
	for rows.Next() {
		var s string
		var n int64
		if err := rows.Scan(&s, &n); err != nil {
			return errors.New("usage: 행 스캔 실패")
		}
		switch {
		case strings.Contains(s, "ctr_") || strings.Contains(s, "mcp__ctr__"):
			ctr += n
		case strings.Contains(s, "ctx_") || strings.Contains(s, "ctxscribe"):
			ctx += n
		}
	}
	fmt.Fprintln(w, "tool\tcalls")
	fmt.Fprintf(w, "ctr\t%d\n", ctr)
	fmt.Fprintf(w, "ctxscribe\t%d\n", ctx)
	if ctr+ctx > 0 {
		fmt.Fprintf(w, "# ratio(참고, 분모 붕괴 주의): %.1f%%\n", 100*float64(ctr)/float64(ctr+ctx))
	}
	return nil
}
```

> **implementer:** `openSessionDBReadOnly`는 신규 헬퍼가 아니라 `loadCCSessions`가 내부에서 여는 방식을 함수로 추출하거나 그 경로를 재사용하라(session.db 경로 유도는 이미 cli.go에 존재). `--adoption`은 `--compare`/`--totals`와 배타(파싱 후 검증 추가).

- [ ] **Step 4: 통과 확인** — `go test -p 1 ./internal/cli/` → PASS

- [ ] **Step 5: gofumpt·커밋**

```bash
gofumpt -l .
git add internal/cli/cli.go internal/cli/cli_test.go
git commit -m "feat(v0.11): usage --adoption — ctr/ctxscribe 채택 계측(절대 호출 수 1차) (D62)"
```

---

### Task 8: doctor [18] 러너 감지 + hostSnippet 개정

**Files:**
- Modify: `internal/cli/cli.go` (runDoctor에 [18]·hostSnippet)
- Test: `internal/cli/cli_test.go`(doctor 출력 확장 검증)

**Interfaces:**
- Consumes: `exec.RunnerStatus()`
- Produces: doctor [18] 라인 + 스니펫에 `--enable exec`·exec 2종 ask

- [ ] **Step 1: 실패 테스트** — doctor 출력에 `[18] exec runners:`가 있고 6언어가 라벨링되는지(감지 결과는 환경 의존이라 접두만 검증).

```go
func TestDoctorShowsRunnerLine(t *testing.T) {
	var buf bytes.Buffer
	_ = runDoctor(context.Background(), &buf, t.TempDir(), t.TempDir(), "0.11.0")
	if !strings.Contains(buf.String(), "[18] exec runners:") {
		t.Fatalf("[18] 누락:\n%s", buf.String())
	}
}
```

- [ ] **Step 2: 실패 확인** — FAIL

- [ ] **Step 3: 구현** — runDoctor의 [17] build 라인 다음에:

```go
	// [18] exec 러너 감지(설계 v0.11 D58 — 실행 없음, LookPath/버전 게이트만).
	var parts []string
	for _, s := range exec.RunnerStatus() {
		if s.OK {
			label := s.Runner
			if s.Version != "" {
				label += "/" + s.Version
			}
			parts = append(parts, s.Lang+"="+label)
		} else {
			parts = append(parts, s.Lang+"=미검출")
		}
	}
	fmt.Fprintf(w, "[18] exec runners: %s\n", strings.Join(parts, " "))
```

hostSnippet 개정(exec는 기본 OFF·프로필 opt-in):

```go
// Claude Code .mcp.json에 exec 활성 예시 추가:
//   "ctr-exec": { "command": "context-router", "args": ["--enable", "exec"] }
// permissions ask에 mcp__ctr__ctr_execute, mcp__ctr__ctr_execute_file 추가.
// Codex enabled_tools에 "ctr_execute","ctr_execute_file" 추가(활성 시 approval prompt 권장).
```

> hostSnippet 상수 문자열을 위 내용으로 확장(기존 ingest/net ask 라인 옆). 정확한 문면은 기존 스니펫 스타일을 따른다.

- [ ] **Step 4: 통과 확인** — `go test -p 1 ./internal/cli/` → PASS

- [ ] **Step 5: gofumpt·커밋**

```bash
gofumpt -l .
git add internal/cli/cli.go internal/cli/cli_test.go
git commit -m "feat(v0.11): doctor [18] 러너 감지 라인 + hostSnippet exec 프로필 안내 (D58)"
```

---

### Task 9: D62 도구 설명 개정 + mcp_test 프로필 테이블·직렬화 상한

**Files:**
- Modify: `internal/mcp/mcp.go` (ctr_search·ctr_fetch 설명 개정)
- Modify: `internal/mcp/mcp_test.go` (프로필 테이블에 exec 행·직렬화 상한 상향·설명 가드)

**설계 노트:** §3.5 비혼동 계약 보존 — ctr_fetch의 "byte-exact·웹 fetch 아님·ctr_fetch_and_index" 3문구는 유지하고(가드 테스트 불변), 회수 유도 문구만 보강. ctr_search 설명에 "재읽기 전 검색" 트리거 보강. 직렬화 상한 `maxToolSchemaBytes`는 2도구+설명 확장분 반영해 상향(의도된 상향 — 커밋 메시지 명시).

- [ ] **Step 1: 실패 테스트** — mcp_test.go의 프로필 노출 테이블에 exec 행 추가(Enable=["exec"] → ctr_execute·ctr_execute_file 포함), 직렬화 상한 테스트는 현재 값으로 FAIL 유도(2도구 추가로 초과).

- [ ] **Step 2: 실패 확인** — `go test -p 1 ./internal/mcp/` → FAIL(상한 초과·테이블 불일치)

- [ ] **Step 3: 구현** — ctr_search 설명 보강(registerSearch), 직렬화 상한 상향(`maxToolSchemaBytes` 새 값 — 실측 후 여유분), 프로필 테이블 exec 행. ctr_fetch 3문구 불변 확인(TestFetchDescriptionMentionsByteExactNotWebFetch 계속 PASS).

> **implementer:** 상한 새 값은 `go test`가 실측 바이트를 로그로 뱉게 하거나, 기존 12029에서 실제 초과분만큼만 올린다(과대 상향 금지 — 회귀 감지력 유지). D62 설명 개정은 §3.5 가드 문구를 절대 건드리지 않는다.

- [ ] **Step 4: 통과 확인** — `go test -p 1 ./internal/mcp/` → PASS(가드 포함 전체)

- [ ] **Step 5: gofumpt·커밋**

```bash
gofumpt -l .
git add internal/mcp/mcp.go internal/mcp/mcp_test.go
git commit -m "feat(v0.11): D62 도구 설명 개정(§3.5 비혼동 보존) + 프로필 테이블 exec·직렬화 상한 상향"
```

---

### Task 10: 아키텍처 문서 갱신 + CI setup-dotnet

**Files:**
- Modify: `docs/context-router-code-architecture-ko.md` (의존 그래프에 internal/sandbox·internal/exec, §6 sentinel)
- Modify: `.github/workflows/ci.yml` (setup-dotnet — .NET 10 부재 시)

**설계 노트:** 아키텍처 문서의 의존 그래프에 신규 패키지 2개(sandbox←exec←mcp, sandbox leaf)와 §6 단일 지점 매핑에 exec/sandbox sentinel 3+1을 추기. CI test job에 `actions/setup-dotnet@v4`(10.0.x) 추가 — ubuntu/macos 이미지에 .NET 10 부재 대비(windows-latest는 프리인스톨 확인 후 조건부).

- [ ] **Step 1**: 아키텍처 문서에 의존 그래프·sentinel 추가(정합성 검증 — mcp가 os/exec를 internal/exec 경유로만 쓰는 규칙 명시).
- [ ] **Step 2**: ci.yml test job에 setup-dotnet 스텝 추가(go setup 다음, test 실행 전).
- [ ] **Step 3**: 로컬 `go build ./...` GREEN 확인(문서·CI는 빌드 무관이나 정합 확인).
- [ ] **Step 4: 커밋**

```bash
git add docs/context-router-code-architecture-ko.md .github/workflows/ci.yml
git commit -m "docs(v0.11): 아키텍처 의존 그래프에 sandbox·exec + §6 sentinel; CI setup-dotnet"
```

---

### Task 11: 릴리스 체크포인트 v0.11.0

**Files:**
- Modify: `internal/buildinfo/*`(버전 범프 단일 지점 — D56)
- Modify: `docs/prompts/2026-07-24-session-28-*.md`(핸드오프 기록)

**설계 노트:** D56 단일 지점 버전 범프 0.11.0 → PR → CI 3-OS GREEN(격리 실증·cold-cache·env 닫힌 표 fixture 포함) → tag v0.11.0 → 도그푸딩(설치 후 execute·execute_file 각 1회 + `usage --adoption` 베이스라인 기록) → doctor [18] 실관측.

- [ ] **Step 1**: 최종 검증 — `go build ./... && go vet ./... && go test -p 1 ./...` GREEN, `gofumpt -l .` 0줄, 잔존 "0.10.0" 비테스트 0.
- [ ] **Step 2**: 버전 범프 0.11.0(단일 지점) 커밋.
- [ ] **Step 3**: PR 생성 → `gh pr checks` **전체 출력** 확인(tail 잘림 금지 — lint 게이트 포함 전 job GREEN). lint 실패 시 gofumpt 로컬 재확인 후 수정.
- [ ] **Step 4**: main 머지 후 tag v0.11.0 → tag-version 잡 success 확인.
- [ ] **Step 5**: 도그푸딩 — `context-router hook install`(+codex --user 별도) → cc/cx marker 0.11.0 → `--enable exec` 기동으로 execute·execute_file 각 1회 실사용 → `usage --adoption` 베이스라인 기록 → doctor [18] 실관측.
- [ ] **Step 6**: 세션 핸드오프 기록 작성·커밋·푸시(docs/prompts 규약 7항).

---

## Self-Review

- **스펙 커버리지**: D58(T5·T8 노출·doctor) · D59(T1–T3 격리) · D60(T4 러너) · D61(T4·T5 반환·ephemeral) · D62(T7·T9 채택). §2 릴리스 게이트 4항 = T11. §5 검수 수용 10건 전부 태스크에 반영(캐시 재지정 T4·per-OS 보장 T2/T3·반환 정밀화 T4/T5·프로필 게이트 T5·adoption 절대수 T7·runner 필드 T4·transform 안내 T9·cwd 스크래치 T4·env 닫힌 표 T1·임시 저장 문면 스펙). ✅
- **플레이스홀더**: 각 태스크에 실제 코드·명령·기대출력 존재. 헬퍼 재사용 지점은 기존 관례 명시(추상 "TBD" 없음). ✅
- **타입 정합**: `sandbox.Spec{Argv,Dir,Env,SelfExe,Timeout,StdoutCap,StderrCap}`·`sandbox.Result{...ExitCode,TimedOut...}`·`exec.Request/Response(ExitCode *int)`·`mcp.ExecuteInput/Output`·`exec.RunnerStatus()[]LangStatus`가 T1→T4→T5→T8 사이 일관. `Run(ctx,scratchParent,selfExe,req)` 시그니처 T4↔T5 일치. ✅

## Execution Handoff

이 계획은 `docs/superpowers/plans/2026-07-24-v0.11-d58-d62.md`에 저장됨. 실행은 프로젝트 표준(superpowers:subagent-driven-development) — 태스크별 fresh 서브에이전트(general-purpose는 `model: "opus"` 명시) + 2단계 리뷰(스펙+품질), BASE는 원장 기록. 태스크/통합/최종 리뷰 체크포인트마다 서브에이전트 리뷰어 + Codex `review --base <BASE>` 병렬 1패스(재리뷰는 서브 단독).
