# v0.9 (D51·D52) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 미지 세션 이벤트를 drop 대신 자동 등록(D51)하고, doctor에 Codex MCP 등록 상태 검사 [16](D52)을 추가한 뒤 0.9.0을 릴리스한다.

**Architecture:** D51은 `internal/hook/hook.go` dispatch의 `!exists` 분기 1곳을 `EnsureSession(ctx, "first-event", worktreeRoot)`+계속 처리로 교체(기존 멱등·단일 트랜잭션 계약 재사용), compare는 합성 세션을 기본 제외하고 `synthetic=N`을 보고한다. D52는 install의 순수 라인 분류 헬퍼를 재사용하는 읽기 전용 판별 헬퍼 2건을 신설해 doctor [16] 5분기를 출력한다.

**Tech Stack:** Go, modernc.org/sqlite(JSON1 `json_extract` 내장), 기존 테스트 하네스(fixtureWith·runHook·session.OpenReadOnly).

**Spec:** `docs/context-router-design-v0.9-ko.md` (a475ea8) — 모든 계약 문면의 원본.

## Global Constraints

- **테스트는 반드시 `go test -p 1`** (메모리 캡 규칙 — 병렬 패키지 금지). 예: `go test -p 1 ./internal/hook/`.
- `git add`는 파일 명시(`git add -A` 금지). 커밋 트레일러: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- 파일 출력 UTF-8(no BOM)·LF. 주석은 기존 한국어 관례·밀도를 따른다.
- §12 canary: 오류 문자열에 절대경로·비밀 금지. 테스트 비밀 리터럴은 런타임 분해(`"xox"+"b-..."`).
- 긴 테스트 데이터는 `strings.Repeat`/testdata로 조립(전체 파일 재작성 금지 — 응답 분할 규율).
- 실사용자 상태 접촉 0: cli 패키지 TestMain이 CODEX_HOME·홈을 전역 중화한다(기왕, e4fc70d) — 새 테스트도 그 안에서만 동작해야 한다.
- compare(`usage --compare`) 출력은 변경 가능. **무플래그 usage 본표·`--totals`는 byte-for-byte 게이트** — `loadCCSessions`(cli.go:356, 본표 공유)를 변경하지 말 것.

---

### Task 1: D51 — dispatch 미지 세션 자동 등록 + 테스트 반전/이관

**Files:**
- Modify: `internal/hook/hook.go` (57-61 const 블록, 142-150 dispatch 분기)
- Modify: `internal/hook/hook_test.go` (TestHookHostIsolation 58-96, TestHookUnknownSession 342-367, TestDropLineDiagnosticFields 369-402)
- Test: `internal/hook/hook_test.go`

**Interfaces:**
- Consumes: `(*session.AppendDB).EnsureSession(ctx, source, worktreeRoot string) (created bool, err error)` — 기존(session.go:860), INSERT OR IGNORE 멱등.
- Produces: hook.go 상수 `sourceFirstEvent = "first-event"` (Task 2의 SQL 리터럴과 일치해야 함 — session_start payload `{"source":"first-event",...}`).

- [ ] **Step 1: 실패 테스트 작성 — TestHookUnknownSession 반전**

기존 단정(events=0 + unknown-session drop)을 등록+기록 단정으로 교체한다. 파일의 기존 헬퍼(`fixtureWith`·`runHook`·`sessDir`·`readDrops`)와 임포트를 그대로 쓴다.

```go
// ③ 비-SessionStart 이벤트를 미지 세션으로 → D51: drop 대신 합성 등록(source="first-event") 후 계속.
// PreToolUse(Read)는 가드 통과 시 이벤트를 만들지 않으므로 session_start 1건만 남는다.
func TestHookUnknownSession(t *testing.T) {
	storeRoot := filepath.Join(t.TempDir(), "storeroot")
	cwd := t.TempDir()
	in := fixtureWith(t, "pretooluse-read.json", map[string]any{"cwd": cwd})
	if rc := runHook(t, storeRoot, in, nil); rc != 0 {
		t.Fatalf("rc=%d want 0", rc)
	}

	dir := sessDir(t, storeRoot, cwd)
	reader, err := session.OpenReadOnly(dir)
	if err != nil {
		t.Fatalf("open session.db: %v", err)
	}
	var n int
	var payload string
	qErr := reader.QueryRow("SELECT count(*) FROM session_events").Scan(&n)
	pErr := reader.QueryRow("SELECT payload FROM session_events WHERE event_type='session_start'").Scan(&payload)
	_ = reader.Close()
	if qErr != nil || pErr != nil {
		t.Fatalf("query: count=%v payload=%v", qErr, pErr)
	}
	if n != 1 {
		t.Fatalf("events=%d want 1 (session_start 합성 등록만 — PreToolUse 통과는 무이벤트)", n)
	}
	if !strings.Contains(payload, `"source":"first-event"`) {
		t.Fatalf("payload=%q want source=first-event(합성 마커)", payload)
	}
	if got := readDrops(t, dir); strings.Contains(got, "unknown-session") {
		t.Fatalf("drops=%q — D51 후 unknown-session은 신규 발생 소멸", got)
	}
}
```

- [ ] **Step 2: 실패 테스트 작성 — PostToolUse 2이벤트 신규 테스트**

```go
// D51 — 미지 세션의 PostToolUse: 합성 등록(session_start) + tool_call까지 총 2이벤트가
// 기록된다(트리거 이벤트도 정상 처리 경로 — 스펙 §2).
func TestHookFirstEventRegistrationPostToolUse(t *testing.T) {
	storeRoot := filepath.Join(t.TempDir(), "storeroot")
	cwd := t.TempDir()
	in := fixtureWith(t, "pretooluse-read.json", map[string]any{
		"cwd":             cwd,
		"hook_event_name": "PostToolUse",
		"tool_name":       "Read",
	})
	env := map[string]string{"CTR_HOOK_DEADLINE_MS": "60000"} // 느린 러너 fail-open drop 방지
	if rc := runHook(t, storeRoot, in, env); rc != 0 {
		t.Fatalf("rc=%d want 0", rc)
	}
	dir := sessDir(t, storeRoot, cwd)
	reader, err := session.OpenReadOnly(dir)
	if err != nil {
		t.Fatalf("open session.db: %v", err)
	}
	var starts, calls int
	e1 := reader.QueryRow("SELECT count(*) FROM session_events WHERE event_type='session_start'").Scan(&starts)
	e2 := reader.QueryRow("SELECT count(*) FROM session_events WHERE event_type='tool_call'").Scan(&calls)
	_ = reader.Close()
	if e1 != nil || e2 != nil {
		t.Fatalf("query: %v %v", e1, e2)
	}
	if starts != 1 || calls != 1 {
		t.Fatalf("session_start=%d tool_call=%d want 1/1", starts, calls)
	}
}
```

- [ ] **Step 3: 실패 테스트 작성 — TestHookHostIsolation 반전(D35 격리 속성이 단정의 본체)**

기존 60-96행 테스트 본문을 교체한다(주석 포함). cx 이벤트가 **cx: 세션으로** 자동 등록되고 cc: 세션이 오염되지 않음을 단정한다.

```go
// D35×D51 — 동일 UUID의 cc/cx 격리: cc SessionStart 후 같은 UUID의 cx 이벤트는 (drop이 아니라)
// cx: 네임스페이스 세션으로 자동 등록·기록된다. cc: 세션으로의 오귀속 0이 격리 단정의 본체.
func TestHookHostIsolation(t *testing.T) {
	storeRoot := t.TempDir()
	cwd := evalLong(t, t.TempDir())
	env := map[string]string{"CTR_HOOK_DEADLINE_MS": "60000"}
	start := fixtureWith(t, "sessionstart.json", map[string]any{"cwd": cwd})
	post := fixtureWith(t, "posttooluse-codex-bash.json", map[string]any{"cwd": cwd})

	if rc := runHook(t, storeRoot, start, env); rc != 0 { // cc 세션 등록
		t.Fatalf("cc SessionStart rc=%d", rc)
	}
	if rc := runHookHost(t, HostCodex, storeRoot, post, env); rc != 0 { // 같은 UUID의 cx 이벤트
		t.Fatalf("cx PostToolUse rc=%d", rc)
	}
	sdir := sessDir(t, storeRoot, cwd)
	reader, err := session.OpenReadOnly(sdir)
	if err != nil {
		t.Fatalf("open session.db: %v", err)
	}
	var cxSessions, ccEvents int
	e1 := reader.QueryRow("SELECT count(*) FROM sessions WHERE session_id LIKE 'cx:%'").Scan(&cxSessions)
	e2 := reader.QueryRow("SELECT count(*) FROM session_events e JOIN sessions s ON s.session_id=e.session_id WHERE s.session_id LIKE 'cc:%' AND e.event_type<>'session_start'").Scan(&ccEvents)
	_ = reader.Close()
	if e1 != nil || e2 != nil {
		t.Fatalf("query: %v %v", e1, e2)
	}
	if cxSessions != 1 {
		t.Fatalf("cx sessions=%d want 1 (D51 합성 등록)", cxSessions)
	}
	if ccEvents != 0 {
		t.Fatalf("cc 비-session_start 이벤트=%d want 0 (cx→cc 오귀속 금지)", ccEvents)
	}
	if got := readDrops(t, sdir); strings.Contains(got, "unknown-session") {
		t.Fatalf("drops=%q — D51 후 unknown-session 미발생", got)
	}
}
```

주의: 기존 테스트 78행 이후의 "cx SessionStart를 거쳐야 기록" 후반부는 삭제한다(자동 등록으로 전제가 사라짐 — EnsureSession 멱등이 후속 SessionStart를 흡수).

- [ ] **Step 4: 실패 테스트 작성 — D43 5필드 포맷 핀을 appendDrop 직접 단위 테스트로 이관**

`TestDropLineDiagnosticFields`(369-402행) 전체를 아래로 교체한다(같은 위치).

```go
// TestAppendDropLineFormat — D43 5필드 포맷 핀(v0.9 이관): unknown-session 소멸로 결정적
// 발화 경로가 사라져 appendDrop을 직접 호출해 라인 계약을 고정한다(스펙 §2).
// "<unix-ts>\t<reason>\t<sid8>\t<hook_event>\t<tool>\n", sid8=앞 8자, 미상="-", 탭·개행 sanitize.
func TestAppendDropLineFormat(t *testing.T) {
	dir := t.TempDir()
	appendDrop(dir, "format-probe", "cc:99999999-0000-7000-8000-000000000000", "PostToolUse", "Read")
	appendDrop(dir, "tab\tnewline\n", "", "", "")
	data, err := os.ReadFile(filepath.Join(dir, "session.drops.log"))
	if err != nil {
		t.Fatalf("read drops: %v", err)
	}
	lines := strings.Split(strings.TrimSpace(string(data)), "\n")
	if len(lines) != 2 {
		t.Fatalf("lines=%d want 2", len(lines))
	}
	f := strings.Split(lines[0], "\t")
	if len(f) != 5 {
		t.Fatalf("fields=%d want 5 (line=%q)", len(f), lines[0])
	}
	if f[1] != "format-probe" || f[2] != "cc:99999" || f[3] != "PostToolUse" || f[4] != "Read" {
		t.Fatalf("fields=%v want [ts format-probe cc:99999 PostToolUse Read]", f)
	}
	g := strings.Split(lines[1], "\t")
	if len(g) != 5 || g[1] != "tab newline " || g[2] != "-" || g[3] != "-" || g[4] != "-" {
		t.Fatalf("sanitize/미상 필드 위반: %v", g)
	}
}
```

(sanitize 결과 리터럴 `"tab newline "`은 구현의 Replacer가 탭·개행을 공백 치환하는 현행 계약(hook.go:630) 그대로다 — 구현을 바꾸지 말고 테스트 실행 결과와 다르면 리터럴을 실측에 맞춘다.)

- [ ] **Step 4b: 실패 테스트 작성 — 멱등(2연속) + 가드 deny 경로 등록(스펙 §2)**

```go
// D51 — 동일 미지 세션 이벤트 2연속: 세션 1행·session_start 1건(EnsureSession 멱등의
// 신규 호출 지점 재확인 — 스펙 §2).
func TestHookFirstEventIdempotent(t *testing.T) {
	storeRoot := filepath.Join(t.TempDir(), "storeroot")
	cwd := t.TempDir()
	in := fixtureWith(t, "pretooluse-read.json", map[string]any{
		"cwd": cwd, "hook_event_name": "PostToolUse", "tool_name": "Read",
	})
	env := map[string]string{"CTR_HOOK_DEADLINE_MS": "60000"}
	for i := 0; i < 2; i++ {
		if rc := runHook(t, storeRoot, in, env); rc != 0 {
			t.Fatalf("run %d rc=%d want 0", i, rc)
		}
	}
	dir := sessDir(t, storeRoot, cwd)
	reader, err := session.OpenReadOnly(dir)
	if err != nil {
		t.Fatalf("open session.db: %v", err)
	}
	var sessions, starts int
	e1 := reader.QueryRow("SELECT count(*) FROM sessions").Scan(&sessions)
	e2 := reader.QueryRow("SELECT count(*) FROM session_events WHERE event_type='session_start'").Scan(&starts)
	_ = reader.Close()
	if e1 != nil || e2 != nil {
		t.Fatalf("query: %v %v", e1, e2)
	}
	if sessions != 1 || starts != 1 {
		t.Fatalf("sessions=%d starts=%d want 1/1 (멱등)", sessions, starts)
	}
}
```

가드 deny 경로(스펙 §2 — "가드가 ad를 쓰는 유일 경로 회귀"): 기존 가드 deny 테스트(대형 in-boundary 파일로 guardRead deny를 유도하는 테스트 — `internal/hook` 내 `permissionDecision` 또는 `warning` 단정 테스트의 픽스처 조립 관례)를 복제해 **SessionStart 없이** 실행하고, deny JSON 출력 + 합성 등록 세션에 warning 이벤트 1건이 기록됨을 단정하는 `TestHookFirstEventGuardDenyWarning`을 추가한다(대형 파일은 `strings.Repeat`로 임계 초과 조립 — 응답 분할 규율).

- [ ] **Step 5: 테스트 실행 — 실패 확인**

Run: `go test -p 1 -run "TestHookUnknownSession|TestHookFirstEvent|TestHookHostIsolation|TestAppendDropLineFormat" ./internal/hook/`
Expected: FAIL — TestHookUnknownSession(events=0·drop 잔존), TestHookFirstEventRegistrationPostToolUse(이벤트 0), TestHookHostIsolation(cx sessions=0). TestAppendDropLineFormat은 PASS일 수 있다(순수 이관 — 그 자체로 정상).

- [ ] **Step 6: 구현 — hook.go 분기 교체**

`internal/hook/hook.go` 57행 const 블록에 상수 추가:

```go
	sourceFirstEvent = "first-event" // D51 합성 등록 마커 — session_start payload source(스펙 v0.9 §0)
```

147-150행의 분기를 교체:

```go
	if !exists {
		// D51 register-on-first-event(v0.9 §0): 미지 세션은 drop 대신 합성 등록 후 그대로 계속
		// 처리한다(트리거 이벤트 포함). EnsureSession은 INSERT OR IGNORE 멱등이라 동시 경쟁 무해.
		// 등록 커밋~append 사이 실패는 기존 append-failed 계약과 동일(원자 결합 비도입 — §5).
		if _, ensErr := ad.EnsureSession(ctx, sourceFirstEvent, worktreeRoot); ensErr != nil {
			appendDrop(dir, "ensure-failed", external, in.HookEventName, in.ToolName)
			return
		}
	}
```

- [ ] **Step 7: 테스트 실행 — 통과 확인 + 패키지 회귀**

Run: `go test -p 1 ./internal/hook/`
Expected: PASS 전부. (다른 테스트가 unknown-session에 기대 걸었다면 — grep으로 확인: `internal/hook` 내 `unknown-session` 검색 — 반전 3건 외 잔존 참조는 주석뿐이어야 한다.)

- [ ] **Step 8: Commit**

```bash
git add internal/hook/hook.go internal/hook/hook_test.go
git commit -m "feat(v0.9): D51 register-on-first-event — 미지 세션 합성 등록(source=first-event)·테스트 반전 3건·D43 포맷 핀 appendDrop 단위 이관

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: D51 — compare 합성 세션 기본 제외 + `synthetic=N` 보고

**Files:**
- Modify: `internal/cli/compare.go` (63행 인근 cc 분류·95행 excluded 라인·99행 cx 로더 호출·144-145행 coverage 라인)
- Modify: `internal/cli/rollout.go` (`loadCXSessions` 193행 — synthetic 동시 수집, `scanRollouts` 257행 — synthetic 파라미터)
- Modify: `internal/cli/rollout_test.go` (256·278·287행 — 시그니처 갱신)
- Test: `internal/cli/compare_test.go`

**Interfaces:**
- Consumes: Task 1의 payload 마커 `{"source":"first-event"}`(session.go sessionStartPayload json 태그 `source`), `session.OpenReadOnly`.
- Produces: `loadCCSynthetic(ctx context.Context, storeRoot, projectRoot string) map[string]bool` (compare.go 신설, compare 전용), `loadCXSessions(ctx, storeRoot, projectRoot) (set, synthetic map[string]bool, incomplete bool)` (반환 3개로 확장 — set에서 synthetic 제거됨), `scanRollouts(ctx, rolloutRoot, projectRoot, cxSet, synthetic map[string]bool, incomplete bool, now time.Time) (cxScan, error)` + `cxScan.syntheticN int64`.
- **금지**: `loadCCSessions`(cli.go:356)는 본표(cli.go:456) 공유 — 변경 금지.

- [ ] **Step 1: 실패 테스트 작성 — cc synthetic 제외**

`compare_test.go`의 기존 픽스처 관례(세션 등록 헬퍼 — 파일 상단의 기존 테스트가 session.OpenAppend/EnsureSession 또는 등록 헬퍼를 쓰는 방식을 그대로 따른다; 기존 `runUsageString` 헬퍼 재사용)로 작성:

```go
// D51 — source="first-event" 합성 등록 세션은 --compare 집계에서 기본 제외되고
// synthetic=N으로 보고된다(스펙 v0.9 §0 A/B 측정 무결성).
func TestCompareSyntheticExcluded(t *testing.T) {
	// 1) storeRoot/projectRoot 준비 + 세션 2개 등록:
	//    - "cc:<uuidA>" — EnsureSession(ctx, "startup", root)  (정상)
	//    - "cc:<uuidB>" — EnsureSession(ctx, "first-event", root) (합성)
	//    등록은 기존 테스트의 세션 생성 관례를 그대로 재사용한다(session.OpenAppend + EnsureSession).
	// 2) transcript 디렉터리에 uuidA.jsonl·uuidB.jsonl 각 1레코드(파일 상단 기존 픽스처 리터럴 재사용).
	// 3) out := runUsageString(t, storeRoot, projectRoot, tdir, "--compare")
	// 4) 단정:
	//    - "hooks:on\t1\t" — uuidA만 on(합성 제외로 1세션)
	//    - strings.Contains(out, "synthetic=1")
	//    - uuidB가 off로도 계상되지 않음(off 세션 수가 uuidB 포함 전 기대값과 일치)
}
```

위 주석 골격을 실제 코드로 완성한다 — 파일 상단의 기존 테스트(예: compare_test.go:35 부근 픽스처)와 동일한 등록·transcript 조립 코드를 복제해 source 값만 바꾼다. **세션 등록 헬퍼가 이미 있으면 그것을 쓰고 새 헬퍼를 만들지 않는다.**

- [ ] **Step 2: 실패 테스트 작성 — cx synthetic 제외**

기존 cx compare 테스트(등록 세션+rollout 픽스처 — compare_test.go:133 부근 `runUsageString(..., "--compare", "--rollouts", rolloutRoot)` 관례)를 복제해, cx: 세션 1개를 `EnsureSession(ctx, "first-event", root)`로 등록하고 그 UUID의 rollout 파일을 두면: on/off/unknown 어디에도 계상되지 않고 coverage 라인에 `synthetic=1`이 나타남을 단정한다.

- [ ] **Step 3: 테스트 실행 — 실패 확인**

Run: `go test -p 1 -run "TestCompareSynthetic" ./internal/cli/`
Expected: FAIL — `synthetic=` 문자열 부재, on 세션 수 불일치.

- [ ] **Step 4: 구현 — cc 측**

`compare.go`에 신설(loadCCSessions 본문(cli.go:356-383)을 복제하되 SQL만 교체 — 재사용 금지 사유: 본표 byte-for-byte 게이트):

```go
// loadCCSynthetic — D51(v0.9): source="first-event" 합성 등록 cc: 세션 집합(--compare 전용
// 기본 제외 — 구성상 불완전 표본). 실패는 빈 집합(fail-soft, loadCCSessions와 동형).
func loadCCSynthetic(ctx context.Context, storeRoot, projectRoot string) map[string]bool {
	canon, err := ident.Canonicalize(projectRoot)
	if err != nil || canon.ProjectID == "" {
		return nil
	}
	sessDir := filepath.Join(storeRoot, "projects", canon.ProjectID, "worktrees", canon.WorktreeID)
	if fi, statErr := os.Stat(filepath.Join(sessDir, "session.db")); statErr != nil || fi.IsDir() {
		return nil
	}
	db, err := session.OpenReadOnly(sessDir)
	if err != nil {
		return nil
	}
	defer func() { _ = db.Close() }()
	rows, err := db.QueryContext(ctx, `SELECT s.session_id FROM sessions s JOIN session_events e
		ON e.session_id = s.session_id WHERE s.session_id LIKE 'cc:%'
		AND e.event_type='session_start' AND json_extract(e.payload,'$.source')='first-event'`)
	if err != nil {
		return nil
	}
	defer func() { _ = rows.Close() }()
	set := make(map[string]bool)
	for rows.Next() {
		var id string
		if rows.Scan(&id) == nil {
			set[id] = true
		}
	}
	return set
}
```

cc 분류 루프(compare.go:63-87)에 편입:

```go
	ccSet := loadCCSessions(ctx, storeRoot, projectRoot)
	ccSyn := loadCCSynthetic(ctx, storeRoot, projectRoot)
	var on, off ccCompareArm
	var ccExcluded, ccSynthetic int64
	for _, e := range entries {
		...
		id := "cc:" + strings.TrimSuffix(e.Name(), ".jsonl")
		if ccSyn[id] {
			ccSynthetic++ // D51 — 합성 등록 세션은 양 arm 모두에서 제외(불완전 표본)
			continue
		}
		grp := &off
		if ccSet[id] {
			grp = &on
		}
		...
	}
```

95행 출력을 교체: `fmt.Fprintf(w, "excluded(--min-records): %d | synthetic=%d\n", ccExcluded, ccSynthetic)`

- [ ] **Step 5: 구현 — cx 측**

`rollout.go` `loadCXSessions`: 순회 내부 쿼리를 확장해 synthetic을 동시 수집하고 **set에서 제외**한다. 기존 쿼리(230행)를 다음으로 교체:

```go
	rows, qErr := db.QueryContext(ctx, `SELECT s.session_id, EXISTS(
		SELECT 1 FROM session_events e WHERE e.session_id = s.session_id
		AND e.event_type='session_start' AND json_extract(e.payload,'$.source')='first-event')
		FROM sessions s WHERE s.session_id LIKE 'cx:%'`)
```

스캔 루프에서 `(id string, syn bool)` 스캔 → `syn`이면 synthetic set에, 아니면 set에 넣는다. 시그니처: `func loadCXSessions(ctx context.Context, storeRoot, projectRoot string) (map[string]bool, map[string]bool, bool)` — 반환 (set, synthetic, incomplete). doc comment에 D51 사유 1줄 추가.

`scanRollouts`(257행): `synthetic map[string]bool` 파라미터를 `cxSet` 뒤에 추가. rollout의 세션 ID 판정 지점(cxSet 조회 위치)에서 **synthetic 선차 검사**: `if synthetic[id] { sc.syntheticN++; continue }` (on/off/unknown 어디에도 미계상). `cxScan` 구조체에 `syntheticN int64` 필드 추가.

`compare.go:99`: `cxSet, cxSyn, incomplete := loadCXSessions(...)` → `scanRollouts(ctx, rolloutRoot, projectRoot, cxSet, cxSyn, incomplete, now)`.

coverage 라인(144-145행) 교체:

```go
		fmt.Fprintf(w, "coverage: on 등록=%d 매칭=%d n/a=%d | off 창내미등록=%d | unknown=%d%s | excluded(--min-records)=%d | synthetic=%d\n",
			sc.registered, sc.matched, sc.registered-sc.matched, len(sc.off), unknownN, inc, cxExcluded, sc.syntheticN)
```

`rollout_test.go` 256·278·287행: 반환값 3개로 갱신(기존 단정 유지 + 새 synthetic 반환은 `_` 또는 빈 집합 단정).

- [ ] **Step 6: 테스트 실행 — 통과 + 패키지 회귀**

Run: `go test -p 1 ./internal/cli/`
Expected: PASS 전부(기존 compare/rollout 테스트 포함 — synthetic 0이면 출력 접미만 `synthetic=0`으로 변한다: 기존 테스트가 coverage/excluded 라인을 정확 일치 단정하면 그 리터럴에 ` | synthetic=0`을 추가).

- [ ] **Step 7: Commit**

```bash
git add internal/cli/compare.go internal/cli/rollout.go internal/cli/compare_test.go internal/cli/rollout_test.go
git commit -m "feat(v0.9): D51 compare 합성 세션 기본 제외 — loadCCSynthetic·loadCXSessions synthetic 동시 수집·scanRollouts 선차 제외·synthetic=N 보고

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: D52 — 판별 헬퍼 2건 + doctor [16] 5분기

**Files:**
- Modify: `internal/cli/codex_toml.go` (probeCodexMCPBlock 신설 — installCodexConfigBlock 161행 인근)
- Modify: `internal/cli/hook_install.go` (scanCodexRegisteredHooks 신설 — scanRegisteredHooks 661행 인근)
- Modify: `internal/cli/cli.go` (doctor — [15] 블록 뒤 1622행 인근에 [16] 삽입, hostSnippet 출력 1624행 앞)
- Test: `internal/cli/codex_toml_test.go`, `internal/cli/hook_install_test.go`, `internal/cli/cli_test.go`

**Interfaces:**
- Consumes: `classifyMarkers(lines) (markerClass, int, int)`·`scanOutside(lines, class, begin, end) (bool, bool)`·`splitLinesKeepEnds`(codex_toml.go 순수 헬퍼), `codexConfigPath() (string, error)`(233행), `codexHooksPath(user bool, projectRoot string) (string, error)`(271행), `isOurCodexGroup(raw json.RawMessage) bool`(235행), `hookMarkerPrefix() string`(61행), doctor의 `version` 변수·`w io.Writer` 관례.
- Produces: `probeCodexMCPBlock(existing []byte) (present bool, anomaly bool)`, `scanCodexRegisteredHooks(path string) (count int, marker string, err error)`, doctor 라인 `[16] codex: ...`.

- [ ] **Step 1: 실패 테스트 작성 — probeCodexMCPBlock 4상태**

`codex_toml_test.go`에 추가(기존 테스트의 픽스처 리터럴 관례 재사용 — 관리 블록은 `codexBlockBody` 상수로 조립):

```go
// D52 — doctor [16] 존재 판별(v0.9 §0): install 상태기계(classReplace/classAppend→동일
// mcpWritten)는 존재/부재를 구분하지 못하므로 별도 판별 헬퍼가 필요하다(적대 검수 P1).
func TestProbeCodexMCPBlock(t *testing.T) {
	cases := []struct {
		name    string
		in      string
		present bool
		anomaly bool
	}{
		{"빈 파일 — 부재", "", false, false},
		{"관리 블록 존재", codexBlockBody, true, false},
		{"블록 밖 맨 헤더", "[mcp_servers.ctr]\ncommand = \"x\"\n", true, false},
		{"마커 이상 — BEGIN만", codexBlockBegin + "\n[mcp_servers.ctr]\n", false, true},
		{"무관 내용 — 부재", "[model]\nname = \"gpt\"\n", false, false},
	}
	for _, c := range cases {
		p, a := probeCodexMCPBlock([]byte(c.in))
		if p != c.present || a != c.anomaly {
			t.Errorf("%s: present=%v anomaly=%v want %v/%v", c.name, p, a, c.present, c.anomaly)
		}
	}
}
```

- [ ] **Step 2: 실패 테스트 작성 — scanCodexRegisteredHooks**

`hook_install_test.go`에 추가. Codex hooks.json 픽스처는 기존 codex install 테스트의 JSON 조립 관례를 재사용한다(mergeCodexHooks가 실제로 쓰는 최상위 구조 그대로 — 파일 내 기존 codex hooks.json 테스트 리터럴을 복제해 statusMessage만 조정):

```go
// D52 — Codex hooks.json 자기 그룹 marker 버전 추출(v0.9 §0): isOurCodexGroup은 bool만
// 반환하므로 신설(적대 검수 P1). 파일 부재는 (0,"",nil) — 미설치 정보 분기.
func TestScanCodexRegisteredHooks(t *testing.T) {
	// ① 부재: count=0 marker="" err=nil
	if n, m, err := scanCodexRegisteredHooks(filepath.Join(t.TempDir(), "hooks.json")); n != 0 || m != "" || err != nil {
		t.Fatalf("부재: n=%d m=%q err=%v want 0/\"\"/nil", n, m, err)
	}
	// ② 자기 그룹 존재: 기존 codex install 테스트의 hooks.json 산출(mergeCodexHooks(nil, cmd,
	//    hookMarker("0.9.0"), true, true))을 임시 파일로 쓰고 → count>0, marker="0.9.0"
	// ③ 타인 그룹만: statusMessage 마커 접두 없는 그룹 → count=0, marker=""
	// (②③은 파일 내 기존 codex hooks 픽스처 조립 코드를 복제해 완성한다)
}
```

②③ 주석 골격을 기존 픽스처 관례로 실코드화한다.

- [ ] **Step 3: 실패 테스트 작성 — doctor [16] 5분기**

`cli_test.go`의 기존 doctor 테스트 관례(`strings.Contains` 단정·CODEX_HOME 격리)로 5케이스를 추가한다. 각 케이스는 `t.Setenv("CODEX_HOME", tmp)`로 격리된 tmp에 픽스처를 배치하고 doctor 출력을 단정:

```go
// D52 — doctor [16] 5분기(v0.9 §0). CODEX_HOME 격리 하에 각 분기 출력 단정.
func TestDoctorCodexMCPLine(t *testing.T) {
	// 분기① config.toml 부재 → "[16] codex: config.toml 없음 — 미사용/미설치" 포함, "warning" 미포함
	// 분기② marker 존재(hooks.json에 자기 그룹) + config.toml에 블록 부재
	//        → "[16] warning:" + "hook install --codex" 포함(§9-2 소멸 시그니처)
	// 분기③ config.toml 존재·블록 부재·marker 부재 → 정보 라인("hook install --codex" 안내), warning 미포함
	// 분기④ 블록 존재 + marker 존재 → "블록=존재" + "marker 0.9.0" 포함, warning 미포함
	// 분기⑤ 마커 이상(BEGIN만) → "[16] warning:" + "수동 확인" 포함
	// 각 케이스: CODEX_HOME/config.toml·CODEX_HOME/hooks.json(사용자 레벨)을 조립해 runDoctorString(기존
	// doctor 테스트 헬퍼) 출력을 단정. 프로젝트 레벨 hooks.json(<projectRoot>/.codex/hooks.json) 병기
	// 단정 1건 포함(분기④에서 project=미등록 user=등록 형태).
}
```

주석 골격을 기존 doctor 테스트 헬퍼로 실코드화한다(테이블 주도 5케이스 권장).

- [ ] **Step 4: 테스트 실행 — 실패 확인**

Run: `go test -p 1 -run "TestProbeCodexMCPBlock|TestScanCodexRegisteredHooks|TestDoctorCodexMCPLine" ./internal/cli/`
Expected: FAIL — 미정의 함수(컴파일 에러) 또는 [16] 라인 부재.

- [ ] **Step 5: 구현 — 헬퍼 2건**

`codex_toml.go`(installCodexConfigBlock 아래):

```go
// probeCodexMCPBlock — doctor [16] 존재 판별(D52, 스펙 v0.9 §0). install 상태기계는 "무엇을
// 쓸지"의 분류(classReplace/classAppend→동일 mcpWritten)라 존재/부재 판별에 부적합 — 같은
// 순수 라인 헬퍼를 재사용해 읽기 전용으로 판정한다. present: 소유 블록(classReplace) 또는
// 블록 밖 맨 헤더. anomaly: 마커 무결성 이상 또는 키-경계 충돌(→ [16] warning ⑤).
func probeCodexMCPBlock(existing []byte) (present bool, anomaly bool) {
	lines := splitLinesKeepEnds(existing)
	class, begin, end := classifyMarkers(lines)
	if class == classAnomaly {
		return false, true
	}
	hasHeader, conflict := scanOutside(lines, class, begin, end)
	if conflict {
		return false, true
	}
	return class == classReplace || hasHeader, false
}
```

`hook_install.go`(scanRegisteredHooks 아래 — Codex 형제, 최상위 JSON 구조는 mergeCodexHooks가 파싱하는 구조와 동일하게):

```go
// scanCodexRegisteredHooks — scanRegisteredHooks의 Codex 형제(D52, 스펙 v0.9 §0): hooks.json
// 자기 그룹 수와 marker 버전(statusMessage의 hookMarkerPrefix 뒤)을 읽는다. 파일 부재는
// (0,"",nil) — 미설치 정보 분기. isOurCodexGroup(전건 판정)을 그대로 재사용한다.
func scanCodexRegisteredHooks(path string) (count int, marker string, err error) {
	data, readErr := os.ReadFile(path)
	if readErr != nil {
		if os.IsNotExist(readErr) {
			return 0, "", nil
		}
		return 0, "", readErr
	}
	var doc map[string]json.RawMessage // 최상위: hook_event → 그룹 배열(mergeCodexHooks 파싱 구조와 동일 계층)
	if err := json.Unmarshal(data, &doc); err != nil {
		return 0, "", err
	}
	for _, rawGroups := range doc {
		var groups []json.RawMessage
		if json.Unmarshal(rawGroups, &groups) != nil {
			continue
		}
		for _, g := range groups {
			if !isOurCodexGroup(g) {
				continue
			}
			count++
			if marker == "" {
				var p codexGroupProbe
				if json.Unmarshal(g, &p) == nil && len(p.Hooks) > 0 {
					marker = strings.TrimPrefix(p.Hooks[0].StatusMessage, hookMarkerPrefix())
				}
			}
		}
	}
	return count, marker, nil
}
```

**주의**: 최상위 구조가 `map[string]json.RawMessage`가 아니라면(예: `{"hooks": {...}}` 래퍼) mergeCodexHooks의 실제 파싱 타입을 열어 그대로 미러링한다 — 구조 불일치 시 테스트 ②가 잡는다.

- [ ] **Step 6: 구현 — doctor [16]**

`cli.go` 1622행([15] 블록 종료) 뒤·1624행(`fmt.Fprintln(w)` + hostSnippet) 앞에 삽입:

```go
	// [16] codex 등록 상태(D52, 스펙 v0.9 §0) — 읽기 전용 5분기. 경고는 [14]와 동일하게
	// failed에 미계상(doctor exit 계약 무변경). trust 해시 검증은 비범위(스펙 §1.2).
	codexHooksScope := func(path string, pathErr error) (string, bool) { // (표시, marker존재)
		if pathErr != nil {
			return "확인불가", false
		}
		n, marker, scanErr := scanCodexRegisteredHooks(path)
		switch {
		case scanErr != nil:
			return "파싱실패", false
		case n == 0:
			return "미등록", false
		case marker != "" && marker != version:
			return fmt.Sprintf("등록됨(%d개, marker %s≠%s — hook install --codex 재실행)", n, marker, version), true
		default:
			return fmt.Sprintf("등록됨(%d개, marker %s)", n, marker), true
		}
	}
	projCodexHooks, _ := codexHooksPath(false, projectRoot)
	userCodexHooks, userCodexHooksErr := codexHooksPath(true, projectRoot)
	projScope, projMarker := codexHooksScope(projCodexHooks, nil)
	userScope, userMarker := codexHooksScope(userCodexHooks, userCodexHooksErr)
	markerPresent := projMarker || userMarker
	cfgPath, cfgPathErr := codexConfigPath()
	switch {
	case cfgPathErr != nil:
		fmt.Fprintln(w, "[16] codex: config.toml 경로 확인불가")
	default:
		cfgData, readErr := os.ReadFile(cfgPath)
		switch {
		case os.IsNotExist(readErr): // 분기① — 마커 여부 무관(상태 루트 부재=미사용, 소멸 시그니처 아님)
			fmt.Fprintf(w, "[16] codex: config.toml 없음 — 미사용/미설치 (hooks: project=%s user=%s)\n", projScope, userScope)
		case readErr != nil:
			fmt.Fprintln(w, "[16] codex: config.toml 읽기 실패")
		default:
			present, anomaly := probeCodexMCPBlock(cfgData)
			switch {
			case anomaly: // 분기⑤
				fmt.Fprintf(w, "[16] codex: [mcp_servers.ctr] 블록=이상 (hooks: project=%s user=%s)\n", projScope, userScope)
				fmt.Fprintln(w, "[16] warning: config.toml 관리 마커 이상/키-경계 충돌 — 수동 확인 필요(hook install --codex 안내 참조)")
			case present: // 분기④
				fmt.Fprintf(w, "[16] codex: [mcp_servers.ctr] 블록=존재 (hooks: project=%s user=%s)\n", projScope, userScope)
			case markerPresent: // 분기② — §9-2 소멸 시그니처
				fmt.Fprintf(w, "[16] codex: [mcp_servers.ctr] 블록=부재 (hooks: project=%s user=%s)\n", projScope, userScope)
				fmt.Fprintln(w, "[16] warning: 훅은 설치됐으나 MCP 블록 부재 — deny 안내가 가리키는 ctr_search/ctr_fetch를 Codex가 볼 수 없음. hook install --codex 재기입 권장")
			default: // 분기③
				fmt.Fprintf(w, "[16] codex: [mcp_servers.ctr] 블록=부재·훅 미설치 — hook install --codex (hooks: project=%s user=%s)\n", projScope, userScope)
			}
		}
	}
```

- [ ] **Step 7: 테스트 실행 — 통과 + 패키지 회귀**

Run: `go test -p 1 ./internal/cli/`
Expected: PASS 전부(기존 doctor 테스트는 Contains 단정이라 [16] 추가에 안전 — TestMain 격리로 기본 분기①).

- [ ] **Step 8: Commit**

```bash
git add internal/cli/codex_toml.go internal/cli/hook_install.go internal/cli/cli.go internal/cli/codex_toml_test.go internal/cli/hook_install_test.go internal/cli/cli_test.go
git commit -m "feat(v0.9): D52 doctor [16] codex 등록 상태 검사 — probeCodexMCPBlock·scanCodexRegisteredHooks 신설·5분기 출력·양 레벨 hooks.json 병기

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 0.9.0 범프(2지점) + 전체 회귀

**Files:**
- Modify: `cmd/context-router/main.go:29` (`const version = "0.8.0"` → `"0.9.0"`)
- Modify: `internal/mcp/mcp.go:30` (`const ServerVersion = "0.8.0"` → `"0.9.0"`)

**Interfaces:**
- Consumes: 없음. Produces: doctor [16]·[9] marker 대조 기준 버전 0.9.0.

- [ ] **Step 1: 2지점 동시 갱신**

두 상수를 `"0.9.0"`으로 편집(다른 라인 무변경).

- [ ] **Step 2: 전체 회귀 + 빌드**

Run: `go build ./... && go vet ./... && go test -p 1 ./...`
Expected: 빌드·vet 클린, 전 패키지 ok(버전 리터럴을 단정하는 테스트가 있으면 0.9.0으로 갱신 — grep `"0.8.0"` 로 잔존 0 확인).

- [ ] **Step 3: Commit**

```bash
git add cmd/context-router/main.go internal/mcp/mcp.go
git commit -m "chore(v0.9.0): 버전 범프 (2지점 동시 갱신)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## 릴리스 후속(계획 밖 — 컨트롤러 수행)

최종 이중 리뷰(서브 + Codex `review --base <plan-BASE>`) → PR → CI 3-OS → 머지 → tag v0.9.0 → 도그푸딩(`go install` + `hook install` 양 호스트 재설치 + Codex `/hooks` 재신뢰 안내 + doctor [16] 분기④ 실관측 + 교차 프로젝트 unknown-session 신규 발생 정지 관측 — 스펙 §1.1).
