# v0.10 (D53–D57) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 서브에이전트 생애주기 캡처(D53)·Grep 가드(D54)·hook-only VACUUM 소급(D55)·버전 중앙화(D56)·수렴 로드맵 문서화(D57)를 스펙 `docs/context-router-design-v0.10-ko.md`(커밋 6f6c883) 그대로 구현한다.

**Architecture:** cc 훅 표면 4→6이벤트 + hook dispatch/classify 확장(신규 event_type 2종·RawMessage 지연 파싱 표식), 기존 가드 파이프라인에 Grep 정적 deny 합류(denyTool reason 파라미터화), 기존 `vacuumReclaim` 헬퍼를 hook-only 경로에 합류, 신규 무의존 leaf 패키지 `internal/buildinfo`로 버전 단일화.

**Tech Stack:** Go 1.26(모듈 기존), SQLite(mattn 기존), GitHub Actions. 신규 외부 의존 0.

## Global Constraints

- **스펙이 계약**: `docs/context-router-design-v0.10-ko.md`(6f6c883) §0–§2 문면 우선. 충돌 발견 시 BLOCKED 보고(임의 해석 금지).
- **메모리 캡 테스트 규율**: 모든 테스트는 `go test -p 1 ./...` 또는 패키지 단위 `go test -p 1 -run <name> ./internal/<pkg>/`. 절대 무캡 전체 실행 금지.
- **응답 분할 규율**: 긴 테스트 데이터는 `strings.Repeat`/기존 testdata 재사용. 파일 전체 재작성 금지 — Edit 최소 diff.
- **§12 canary**: 테스트에 비밀 모양 리터럴 신규 도입 금지(기존 분해 관례 유지).
- **byte-for-byte 게이트**: 무플래그 `usage` 본표 등 기존 출력 계약 무변경. doctor는 라인 **추가**만.
- 커밋 트레일러: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- 도그푸딩 재설치는 **전 태스크 완료 후 릴리스 시점 1회**(스펙 §1.3) — 태스크 중 `go install`/`hook install` 실행 금지.

---

### Task 1: D53 생애주기 — subagent_start/stop 이벤트 + cc 6이벤트 설치

**Files:**
- Modify: `internal/hook/hook.go` (hookInput 필드 2·classify 최상단 분기·dispatch 분기)
- Modify: `internal/cli/hook_install.go` (hookRegistrations 배열 +2)
- Test: `internal/hook/hook_test.go`, `internal/cli/hook_install_test.go`

**Interfaces:**
- Consumes: 기존 `buildEvent(in hookInput) (session.Event, bool)`, `classify(in hookInput) (string, string, map[string]string)`, `appendDrop`, `ad.Append`, hook_test 헬퍼(`runHook`, `sessDir`, 픽스처 관례 — 파일 상단 참조).
- Produces: hookInput 신규 필드 `AgentID`/`AgentType json.RawMessage`(Task 2가 사용), 헬퍼 `agentStrings(in hookInput) (id, typ string)`(관대 추출 — Task 2의 `agentFields`와 별개), event_type `subagent_start`/`subagent_stop`.

- [ ] **Step 1: 실패 테스트 작성** — `internal/hook/hook_test.go`에 추가(기존 픽스처 관례 재사용 — 파일 상단 헬퍼로 stdin JSON을 직접 조립하는 기존 테스트와 동형으로 작성):

```go
// D53 — SubagentStart/Stop 생애주기 이벤트(스펙 v0.10 §0·§2). 등록된 세션에 각 1이벤트,
// summary는 "subagent started: <type>"·빈 type이면 접미 생략, attrs는 빈 값 포함 2키 기록.
func TestHookSubagentLifecycle(t *testing.T) {
	storeRoot := filepath.Join(t.TempDir(), "storeroot")
	cwd := t.TempDir()
	sid := "3f2504e0-4f89-41d3-9a0c-0305e82c3301"
	// SessionStart로 세션 등록(기존 관례)
	start := mustJSON(t, map[string]any{"hook_event_name": "SessionStart", "session_id": sid, "cwd": cwd, "source": "startup"})
	if rc := runHook(t, storeRoot, start, nil); rc != 0 {
		t.Fatalf("SessionStart rc=%d", rc)
	}
	cases := []struct {
		event, agentType, wantType, wantSummary string
	}{
		{"SubagentStart", "Explore", "subagent_start", "subagent started: Explore"},
		{"SubagentStop", "Explore", "subagent_stop", "subagent stopped: Explore"},
		{"SubagentStart", "", "subagent_start", "subagent started"}, // 빈 type — 접미 생략(§3 실측 근거)
	}
	for _, c := range cases {
		in := mustJSON(t, map[string]any{
			"hook_event_name": c.event, "session_id": sid, "cwd": cwd,
			"agent_id": "a1b2c3d4e5f6", "agent_type": c.agentType,
		})
		if rc := runHook(t, storeRoot, in, nil); rc != 0 {
			t.Fatalf("%s rc=%d", c.event, rc)
		}
		ev := lastEvent(t, sessDir(t, storeRoot, cwd), sid) // (type, summary, attrs) 조회 헬퍼 — 없으면 신설(아래 Step 1b)
		if ev.Type != c.wantType || ev.Summary != c.wantSummary {
			t.Fatalf("got (%s,%q) want (%s,%q)", ev.Type, ev.Summary, c.wantType, c.wantSummary)
		}
		if ev.Attrs["agent_id"] != "a1b2c3d4e5f6" || ev.Attrs["agent_type"] != c.agentType {
			t.Fatalf("attrs=%v want agent_id/agent_type 병기(빈 값 포함)", ev.Attrs)
		}
	}
}

// D53 — 미등록 세션의 SubagentStart는 D51 합성 등록 경유 후 기록(파이프라인 합류 회귀).
func TestHookSubagentUnknownSessionSynthesizes(t *testing.T) {
	storeRoot := filepath.Join(t.TempDir(), "storeroot")
	cwd := t.TempDir()
	sid := "4a2504e0-4f89-41d3-9a0c-0305e82c3302"
	in := mustJSON(t, map[string]any{
		"hook_event_name": "SubagentStart", "session_id": sid, "cwd": cwd,
		"agent_id": "x1", "agent_type": "Explore",
	})
	if rc := runHook(t, storeRoot, in, nil); rc != 0 {
		t.Fatalf("rc=%d", rc)
	}
	// session_start(Source="first-event") + subagent_start = 2이벤트
	n := countEvents(t, sessDir(t, storeRoot, cwd), sid)
	if n != 2 {
		t.Fatalf("events=%d want 2 (합성 등록 + subagent_start)", n)
	}
}

// D53 비수용 부정(§2): SubagentStop의 last_assistant_message·agent_transcript_path는
// summary·attrs 어디에도 미출현.
func TestHookSubagentStopRejectsBodyFields(t *testing.T) {
	storeRoot := filepath.Join(t.TempDir(), "storeroot")
	cwd := t.TempDir()
	sid := "5b2504e0-4f89-41d3-9a0c-0305e82c3303"
	start := mustJSON(t, map[string]any{"hook_event_name": "SessionStart", "session_id": sid, "cwd": cwd, "source": "startup"})
	if rc := runHook(t, storeRoot, start, nil); rc != 0 {
		t.Fatalf("SessionStart rc=%d", rc)
	}
	secret := "CANARY" + "SECRET" + "BODY" // 분해 조립(§12)
	in := mustJSON(t, map[string]any{
		"hook_event_name": "SubagentStop", "session_id": sid, "cwd": cwd,
		"agent_id": "x2", "agent_type": "claude",
		"last_assistant_message": secret, "agent_transcript_path": "C:/tmp/" + secret + ".jsonl",
	})
	if rc := runHook(t, storeRoot, in, nil); rc != 0 {
		t.Fatalf("rc=%d", rc)
	}
	ev := lastEvent(t, sessDir(t, storeRoot, cwd), sid)
	joined := ev.Summary + fmt.Sprint(ev.Attrs)
	if strings.Contains(joined, secret) {
		t.Fatalf("본문 필드 유출: %q", joined)
	}
}
```

`mustJSON`/`lastEvent`/`countEvents` 헬퍼가 파일에 없으면 테스트 파일 하단에 신설(각각 `json.Marshal` 래퍼 / session.db를 `sql.Open`으로 열어 마지막 이벤트의 event_type·summary·payload attrs를 읽는 소형 함수 / COUNT 쿼리). 기존에 동등 헬퍼가 있으면 그것을 재사용하고 신설하지 않는다(DRY — 파일을 먼저 훑을 것).

- [ ] **Step 2: RED 확인** — Run: `go test -p 1 -run "TestHookSubagent" ./internal/hook/`
  Expected: FAIL (subagent 이벤트 미기록 — 현행 dispatch가 해당 HookEventName을 무시)

- [ ] **Step 3: 구현** — 세 지점:

① `internal/hook/hook.go` hookInput(31-41행)에 필드 2개 추가:

```go
	AgentID   json.RawMessage `json:"agent_id"`   // D53 — RawMessage 지연 관대 파싱(스펙 v0.10 §0: string 필드 금지)
	AgentType json.RawMessage `json:"agent_type"` // D53 — 〃
```

② 같은 파일에 관대 추출 헬퍼 + classify 최상단 분기(classify 함수 시작부, PostToolUseFailure 검사보다 앞):

```go
// agentStrings — D53 관대 추출: RawMessage에서 문자열 언마샬 성공분만, 실패·부재는 빈 문자열.
// 생애주기 이벤트의 attrs는 빈 값도 기록하므로 ok 게이트가 없다(표식 게이트는 agentFields — T2).
func agentStrings(in hookInput) (id, typ string) {
	_ = json.Unmarshal(in.AgentID, &id)
	_ = json.Unmarshal(in.AgentType, &typ)
	return id, typ
}
```

classify 최상단(함수 첫 문장으로):

```go
	if in.HookEventName == "SubagentStart" || in.HookEventName == "SubagentStop" {
		et, verb := "subagent_start", "started"
		if in.HookEventName == "SubagentStop" {
			et, verb = "subagent_stop", "stopped"
		}
		id, typ := agentStrings(in)
		summary := "subagent " + verb
		if typ != "" {
			summary += ": " + typ // 빈 type = 접미 생략(§3 실측 — 빈 summary 드롭 회피)
		}
		return et, summary, map[string]string{"agent_id": id, "agent_type": typ}
	}
```

③ dispatch(PreToolUse 분기 `return` 직후·PostToolUse 분기 앞)에 동렬 분기:

```go
	// D53 — 서브에이전트 생애주기(스펙 v0.10 §0): buildEvent→Append 경유, 1 호출 = 1 이벤트.
	if in.HookEventName == "SubagentStart" || in.HookEventName == "SubagentStop" {
		if ev, ok := buildEvent(in); ok {
			if _, _, _, err := ad.Append(ctx, ev); err != nil {
				appendDrop(dir, "append-failed", external, in.HookEventName, in.ToolName)
			}
		}
		return
	}
```

④ `internal/cli/hook_install.go` hookRegistrations(33-42행)에 2행 추가:

```go
	{"SubagentStart", ""},
	{"SubagentStop", ""},
```

- [ ] **Step 4: GREEN 확인** — Run: `go test -p 1 -run "TestHookSubagent" ./internal/hook/` → PASS. 이어서 `go test -p 1 ./internal/hook/ ./internal/cli/` 전체 회귀(기존 install 테스트가 4이벤트를 단정하고 있으면 6이벤트로 갱신 — 멱등 단정 유지). `go vet ./internal/hook/ ./internal/cli/`.

- [ ] **Step 5: Commit** — `git add internal/hook/ internal/cli/ && git commit -m "feat(v0.10): D53 서브에이전트 생애주기 — subagent_start/stop 이벤트·cc 6이벤트 설치"`

---

### Task 2: D53 표식 병기 + empty/GC 경계 테스트

**Files:**
- Modify: `internal/hook/hook.go` (buildEvent에 표식 병합·`agentFields` 헬퍼)
- Test: `internal/hook/hook_test.go`, `internal/cli/cli_test.go`(doctor empty), `internal/session/retention_test.go`(GC 경계)

**Interfaces:**
- Consumes: Task 1의 `AgentID`/`AgentType RawMessage` 필드·`agentStrings`. retention_test 기존 헬퍼 `seedSession`/`seedSessionStart`/`insertRawEvent`·`Sweep(ctx, d, now)` 시계 주입. cli_test 기존 `runDoctor` 관례.
- Produces: `agentFields(in hookInput) (id, typ string, ok bool)` — ok는 agent_id 비어있지 않을 때만 true.

- [ ] **Step 1: 실패 테스트 작성** — `internal/hook/hook_test.go`:

```go
// D53 표식(스펙 §0·§2): agent_id 실린 PostToolUse → attrs 병기, 부재 → 두 키 부재,
// 빈 값 → 표식 생략, wrong-type → 기본 이벤트 미드롭(RawMessage 기전이 계약 — 재검수 P2).
func TestHookAgentAttribution(t *testing.T) {
	cases := []struct {
		name      string
		agentID   any  // nil=필드 자체 생략
		agentType any
		wantMark  bool
		wantDrop  bool
	}{
		{"present", "a1b2", "Explore", true, false},
		{"absent", nil, nil, false, false},
		{"empty_id", "", "Explore", false, false},           // best-effort — 표식 생략
		{"wrong_type", 123, map[string]int{"x": 1}, false, false}, // 기본 이벤트 미드롭
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			storeRoot := filepath.Join(t.TempDir(), "storeroot")
			cwd := t.TempDir()
			sid := "6c2504e0-4f89-41d3-9a0c-0305e82c3304"
			start := mustJSON(t, map[string]any{"hook_event_name": "SessionStart", "session_id": sid, "cwd": cwd, "source": "startup"})
			if rc := runHook(t, storeRoot, start, nil); rc != 0 {
				t.Fatalf("SessionStart rc=%d", rc)
			}
			m := map[string]any{
				"hook_event_name": "PostToolUse", "session_id": sid, "cwd": cwd,
				"tool_name": "Glob", "tool_input": map[string]string{"pattern": "*.go"},
				"tool_response": map[string]string{"result": "ok"},
			}
			if c.agentID != nil {
				m["agent_id"] = c.agentID
			}
			if c.agentType != nil {
				m["agent_type"] = c.agentType
			}
			if rc := runHook(t, storeRoot, mustJSON(t, m), nil); rc != 0 {
				t.Fatalf("PostToolUse rc=%d", rc)
			}
			ev := lastEvent(t, sessDir(t, storeRoot, cwd), sid)
			if ev.Type != "tool_call" {
				t.Fatalf("기본 이벤트 소실 — type=%s (드롭 회귀)", ev.Type)
			}
			_, hasID := ev.Attrs["agent_id"]
			if hasID != c.wantMark {
				t.Fatalf("표식 존재=%v want %v (attrs=%v)", hasID, c.wantMark, ev.Attrs)
			}
		})
	}
}
```

`internal/session/retention_test.go`(기존 헬퍼·Sweep(ctx,d,now) 관례):

```go
// D53 empty/GC 경계(스펙 §0 상호작용 ①·§2): 생애주기 이벤트만 있는 세션은 empty GC 비대상,
// retention 만료 시 같은 Sweep 호출에서 수거(추가 7일 유예 없음 — 재검수 정정 계약).
func TestSweepSubagentLifecycleSession(t *testing.T) {
	d := openTestDB(t) // 파일의 기존 DB 오픈 관례 재사용(이름이 다르면 그것을 사용)
	now := time.Now()
	old := now.Add(-40 * 24 * time.Hour) // started_at 40일 전(7일 게이트 통과·retention 30일 초과)
	sid := "cc:7d2504e0-4f89-41d3-9a0c-0305e82c3305"
	seedSession(t, d, sid, old, 2592000)                       // retention 30일
	seedSessionStart(t, d, sid, old)                            // session_start(old)
	insertRawEvent(t, d, sid, "subagent_start", now.Unix())     // 생애주기 이벤트(신선)
	// 1차 스윕: subagent_start가 신선 → 세션은 non-empty → GC 비대상
	if err := Sweep(context.Background(), d, now); err != nil {
		t.Fatal(err)
	}
	if !sessionExists(t, d, sid) {
		t.Fatal("신선한 subagent_start 보유 세션이 GC됨 — empty 술어 회귀")
	}
	// 2차 스윕: retention 경과 시점 — 이벤트 만료와 같은 호출에서 세션 행 수거
	later := now.Add(31 * 24 * time.Hour)
	if err := Sweep(context.Background(), d, later); err != nil {
		t.Fatal(err)
	}
	if sessionExists(t, d, sid) {
		t.Fatal("retention 만료 후 같은 Sweep에서 미수거 — 상한 닫힘 회귀")
	}
}
```

(`openTestDB`/`sessionExists`가 없으면 파일의 기존 테스트가 쓰는 동등 관례를 그대로 따르고, 정말 없을 때만 소형 신설.)

`internal/cli/cli_test.go` — 기존 doctor 테스트 관례(`runDoctor` + session.db seed)로:

```go
// D53 — subagent_start만 있는 세션은 doctor [6] empty 카운트에서 제외(스펙 §0 상호작용 ①).
func TestDoctorEmptyExcludesSubagentLifecycle(t *testing.T) {
	// 기존 doctor 테스트의 storeRoot/session.db seed 관례를 재사용해 세션 2개 구성:
	// A = session_start 단독(empty), B = session_start + subagent_start(non-empty).
	// runDoctor 출력의 "[6] ... sessions=2 (empty=1)" 단정.
}
```

(위 골격은 파일의 기존 doctor seed 테스트를 먼저 읽고 그 관례 — 헬퍼명·seed 방식 — 에 정확히 맞춰 채운다. 단정 대상은 `sessions=2 (empty=1)` 부분 문자열.)

- [ ] **Step 2: RED 확인** — Run: `go test -p 1 -run "TestHookAgentAttribution|TestSweepSubagentLifecycle|TestDoctorEmptyExcludesSubagent" ./internal/hook/ ./internal/session/ ./internal/cli/`
  Expected: hook은 표식 미병기로 FAIL, session/cli는 신규 타입 자체는 기존 술어로 이미 PASS일 수 있음(그 경우 이 둘은 회귀 핀으로 유지 — RED 필수 아님, 계약 고정이 목적)

- [ ] **Step 3: 구현** — `internal/hook/hook.go`:

① 표식 게이트 헬퍼(agentStrings 옆):

```go
// agentFields — D53 표식 게이트(스펙 v0.10 §0): agent_id가 비어있지 않을 때만 ok. 결손·빈 값·
// 타입 이상은 표식 생략일 뿐 기본 이벤트 처리 불변(best-effort — 재검수 P2 기전).
func agentFields(in hookInput) (id, typ string, ok bool) {
	id, typ = agentStrings(in)
	if id == "" {
		return "", "", false
	}
	return id, typ, true
}
```

② buildEvent의 `eventType, summary, attrs := classify(in)` 직후에 병합:

```go
	if id, typ, ok := agentFields(in); ok { // D53 표식 — PostToolUse/Failure·생애주기 공통(동일 키 덮어쓰기 무해)
		if attrs == nil {
			attrs = map[string]string{}
		}
		attrs["agent_id"], attrs["agent_type"] = id, typ
	}
```

- [ ] **Step 4: GREEN 확인** — Run: `go test -p 1 -run "TestHookAgentAttribution|TestSweepSubagentLifecycle|TestDoctorEmptyExcludesSubagent" ./internal/hook/ ./internal/session/ ./internal/cli/` → PASS. 패키지 회귀: `go test -p 1 ./internal/hook/ ./internal/session/ ./internal/cli/`.

- [ ] **Step 5: Commit** — `git commit -m "feat(v0.10): D53 표식 병기(RawMessage best-effort) + empty/GC 경계 핀"`

---

### Task 3: D54 Grep 가드 — denyTool reason 파라미터화 + 정적 deny

**Files:**
- Modify: `internal/hook/hook.go` (denyTool 시그니처·guardGrep·dispatch case·reason 상수)
- Modify: `internal/cli/hook_install.go` (PreToolUse matcher `|Grep`)
- Test: `internal/hook/hook_test.go`, `internal/cli/hook_install_test.go`

**Interfaces:**
- Consumes: 기존 `denyTool(ctx, ad, in, dir, toolName, detail, stdout)`(호출부 3곳: guardRead/guardBash/guardPowerShell), `readGuardInput`의 `*int64` 포인터 관례.
- Produces: `denyTool(..., detail, reason string, stdout)` 신 시그니처, `guardGrep`, 상수 `denyReasonIndexed`(기존 문구)·`denyReasonGrep`.

- [ ] **Step 1: 실패 테스트 작성** — `internal/hook/hook_test.go`(기존 guard 테스트의 세션 등록·stdout 캡처 관례 재사용):

```go
// D54 — Grep 가드 5분기(스펙 §2 ①~⑤) + 전용 reason. deny는 content+head_limit 0 단 하나.
func TestGuardGrep(t *testing.T) {
	cases := []struct {
		name string
		ti   map[string]any
		deny bool
	}{
		{"content_unlimited", map[string]any{"pattern": "x", "output_mode": "content", "head_limit": 0}, true},
		{"content_default", map[string]any{"pattern": "x", "output_mode": "content"}, false},          // 부재=250 캡
		{"content_capped", map[string]any{"pattern": "x", "output_mode": "content", "head_limit": 50}, false},
		{"files_unlimited", map[string]any{"pattern": "x", "output_mode": "files_with_matches", "head_limit": 0}, false},
		{"unparsable", map[string]any{"head_limit": "zero"}, false}, // 파싱 불가 → 통과
	}
	for _, c := range cases {
		t.Run(c.name, func(t *testing.T) {
			storeRoot := filepath.Join(t.TempDir(), "storeroot")
			cwd := t.TempDir()
			sid := "8e2504e0-4f89-41d3-9a0c-0305e82c3306"
			start := mustJSON(t, map[string]any{"hook_event_name": "SessionStart", "session_id": sid, "cwd": cwd, "source": "startup"})
			if rc := runHook(t, storeRoot, start, nil); rc != 0 {
				t.Fatalf("SessionStart rc=%d", rc)
			}
			in := mustJSON(t, map[string]any{
				"hook_event_name": "PreToolUse", "session_id": sid, "cwd": cwd,
				"tool_name": "Grep", "tool_input": c.ti,
			})
			out := runHookCaptureStdout(t, storeRoot, in) // stdout 캡처 관례 — 기존 guard 테스트와 동형
			if c.deny {
				if !strings.Contains(out, `"permissionDecision":"deny"`) {
					t.Fatalf("deny 미발화: %q", out)
				}
				if !strings.Contains(out, "head_limit") || !strings.Contains(out, "ctr_search") {
					t.Fatalf("Grep 전용 reason 아님(§2 ① — 하드코딩 회귀): %q", out)
				}
			} else if out != "" {
				t.Fatalf("통과 케이스에 출력: %q", out)
			}
		})
	}
}

// D54 §2 ⑥ — denyTool 파라미터화 후 기존 3가드 reason 문구 불변 회귀.
// 기존 guardRead deny 테스트(TestGuardRead* 계열)의 deny 출력 단정에
// `"이미 인덱스됨"` 부분 문자열 단정을 추가 확인(이미 있으면 그대로 — 이 항목은 확인만).
```

(`runHookCaptureStdout`이 없으면 기존 guard 테스트가 stdout을 검증하는 방식을 그대로 재사용.)

- [ ] **Step 2: RED 확인** — Run: `go test -p 1 -run "TestGuardGrep" ./internal/hook/`
  Expected: FAIL (Grep 미가드 — deny 무발화)

- [ ] **Step 3: 구현** — 네 지점:

① reason 상수 + denyTool 시그니처(하드코딩 문구를 상수로 승격, reason 파라미터 추가):

```go
const (
	denyReasonIndexed = "이미 인덱스됨 — ctr_search로 검색, ctr_fetch로 바이트 정확 조회"
	denyReasonGrep    = "무제한 content 검색 — head_limit을 지정하거나 ctr_search로 검색"
)
```

`denyTool` 시그니처를 `func denyTool(ctx context.Context, ad *session.AppendDB, in hookInput, dir, toolName, detail, reason string, stdout io.Writer)`로 바꾸고 `"permissionDecisionReason": reason`으로 치환. 기존 호출부 3곳(guardRead/guardBash/guardPowerShell)에 `denyReasonIndexed` 전달.

② grepGuardInput + guardGrep(readGuardInput 옆):

```go
// grepGuardInput — Grep tool_input 중 가드가 보는 필드(스펙 v0.10 D54). head_limit은 *int64 —
// 부재(호스트 기본 250 캡)와 명시 0(무제한)을 구분한다(실측 §3: 미지정 시 필드 자체 부재).
type grepGuardInput struct {
	OutputMode string `json:"output_mode"`
	HeadLimit  *int64 `json:"head_limit"`
}

// guardGrep — PreToolUse(Grep) 가드(D54). deny 조건은 정확히 하나: output_mode=="content" &&
// head_limit 명시 0. 색인 단계 없음 — 정적 판정 → denyTool 직행(guard-store 비대상, drop은
// guard-append만). 그 외 전 조합 통과(allow-bias).
func guardGrep(ctx context.Context, ad *session.AppendDB, in hookInput, dir string, stdout io.Writer) {
	var f grepGuardInput
	if json.Unmarshal(in.ToolInput, &f) != nil {
		return // 파싱 불가 → 통과
	}
	if f.OutputMode != "content" || f.HeadLimit == nil || *f.HeadLimit != 0 {
		return
	}
	denyTool(ctx, ad, in, dir, "Grep", "head_limit=0 content", denyReasonGrep, stdout)
}
```

③ dispatch PreToolUse 스위치에 `case "Grep": guardGrep(ctx, ad, in, dir, stdout)` 추가.

④ `internal/cli/hook_install.go`: `{"PreToolUse", "Read|Bash|PowerShell"}` → `{"PreToolUse", "Read|Bash|PowerShell|Grep"}`. (install 테스트의 matcher 단정도 갱신.)

- [ ] **Step 4: GREEN 확인** — Run: `go test -p 1 -run "TestGuardGrep" ./internal/hook/` → PASS. 회귀: `go test -p 1 ./internal/hook/ ./internal/cli/`(기존 guard deny 테스트 전건 PASS = ⑥ 회귀 확인). `go vet ./internal/hook/`.

- [ ] **Step 5: Commit** — `git commit -m "feat(v0.10): D54 Grep 가드 — denyTool reason 파라미터화·content+head_limit0 정적 deny·matcher 확장"`

---

### Task 4: D55 hook-only VACUUM 소급

**Files:**
- Modify: `internal/cli/cli.go` (runPurgeHookOnly ⑤ 교체)
- Test: `internal/cli/cli_test.go` (TestPurgeHookOnlyVacuumFailureContinues 반전·리네임 + 성공 보고 단정)

**Interfaces:**
- Consumes: 기존 `vacuumReclaim(ctx, st, projDir, beforeB, w)`, `contentFootprint(projDir)`, `store.Open`, `st.PurgeHookOnly`.
- Produces: (없음 — 동작 계약 변경만: hook-only VACUUM 실패 시 rc≠0 + 총합 보고 라인)

- [ ] **Step 1: 테스트 반전** — `internal/cli/cli_test.go`의 `TestPurgeHookOnlyVacuumFailureContinues`(2425행 부근, BEGIN IMMEDIATE 선점 게이트 구조 유지)를 `TestPurgeHookOnlyVacuumFailurePropagates`로 리네임하고 최종 단정을 반전:

```go
	// 반전(D55): VACUUM 실패는 rc≠0로 전파 — 단 이미 커밋된 hook-only 삭제분은 유지.
	if rc := <-done; rc == nil {
		t.Fatalf("VACUUM BUSY인데 rc=nil — D55 rc≠0 계약 회귀:\n%s", gw.buf.String())
	}
	// 삭제분 유지 단정: 기존 테스트의 사후 검증(quick_check/행 수) 그대로 유지.
```

성공 경로 단정 추가 — 기존 hook-only 성공 테스트(무경합 경로)에 총합 보고 라인 단정:

```go
	if !strings.Contains(buf.String(), "purge: content.db(+wal/shm) ") {
		t.Fatalf("D55 총합 보고 라인 없음:\n%s", buf.String())
	}
```

- [ ] **Step 2: RED 확인** — Run: `go test -p 1 -run "TestPurgeHookOnly" ./internal/cli/`
  Expected: FAIL (현행 log-and-continue라 rc=nil / 총합 보고 라인 부재)

- [ ] **Step 3: 구현** — `runPurgeHookOnly`에서 `store.Open` 직후 before 측정, ⑤ 블록 교체:

```go
	st, err := store.Open(projDir, false)
	if err != nil {
		return err
	}
	beforeB := contentFootprint(projDir) // D55: open 후·PurgeHookOnly 전 — 삭제+VACUUM 효과 격리(스펙 §0)
	rep, purgeErr := st.PurgeHookOnly(ctx)
	var vacErr error
	if purgeErr == nil {
		// ④ 실회수 보고 먼저(스펙 §3 순서) — VACUUM 성패와 무관하게 부분 성공을 즉시 노출한다.
		fmt.Fprintf(w, "hook-only purge: 실회수 %dB(%d hashes), 유예 %d건, 실패 %d건\n",
			rep.ReclaimedB, rep.Hashes, rep.DeferredFiles, rep.FailedFiles)
		// ⑤ D55: vacuumReclaim 합류 — checkpoint busy 검증·총합 보고, 실패는 rc≠0(본경로 동일).
		// 이미 커밋된 삭제분은 유지된다(vacuumReclaim 계약 — 호출자 미롤백).
		vacErr = vacuumReclaim(ctx, st, projDir, beforeB, w)
	}
	closeErr := st.Close()
	if purgeErr != nil {
		return purgeErr
	}
	if vacErr != nil {
		return vacErr
	}
	return closeErr
```

기존 stderr VACUUM 로그 라인은 제거(stderr 파라미터 자체는 confirm 관례 유지 여부에 따라 그대로 둠 — 미사용이 되면 `_ = stderr` 없이 Go는 무사용 파라미터 허용). 함수 상단 주석의 "⑤ VACUUM 후행(실패는 stderr log-and-continue·rc=0…)" 문구를 D55 계약으로 갱신.

- [ ] **Step 4: GREEN 확인** — Run: `go test -p 1 -run "TestPurgeHookOnly" ./internal/cli/` → PASS. 회귀: `go test -p 1 ./internal/cli/`.

- [ ] **Step 5: Commit** — `git commit -m "feat(v0.10): D55 hook-only VACUUM 소급 — vacuumReclaim 합류·rc 전파·총합 보고"`

---

### Task 5: D56 버전 중앙화 — internal/buildinfo + version 서브커맨드

**Files:**
- Create: `internal/buildinfo/buildinfo.go`
- Modify: `cmd/context-router/main.go` (`const version` 제거·cliSubcommands에 version), `internal/mcp/mcp.go` (`ServerVersion` 제거·참조 치환), `internal/cli/cli.go` (Run switch에 case "version")
- Test: `cmd/context-router/main_test.go` (핀 테스트 삭제), `internal/cli/cli_test.go` (version 서브커맨드 출력)

**Interfaces:**
- Produces: `buildinfo.ProductVersion() string`(초기값 `"0.10.0-dev"`), CLI `version` 서브커맨드(ProductVersion 1줄 stdout). Task 6이 doctor build 라인에서 사용.

- [ ] **Step 1: 실패 테스트 작성** — `internal/cli/cli_test.go`:

```go
// D56 — version 서브커맨드: ProductVersion 1줄만 출력(CI 추출 표면, 스펙 §0).
func TestRunVersionSubcommand(t *testing.T) {
	var out, errOut bytes.Buffer
	err := Run(context.Background(), "version", nil, t.TempDir(), t.TempDir(), "9.9.9-test", false, "", &out, &errOut)
	if err != nil {
		t.Fatalf("err=%v", err)
	}
	if out.String() != "9.9.9-test\n" {
		t.Fatalf("out=%q want %q", out.String(), "9.9.9-test\n")
	}
	if err := Run(context.Background(), "version", []string{"x"}, t.TempDir(), t.TempDir(), "v", false, "", &out, &errOut); err == nil {
		t.Fatal("잉여 인자 미거부")
	}
}
```

- [ ] **Step 2: RED 확인** — Run: `go test -p 1 -run "TestRunVersionSubcommand" ./internal/cli/`
  Expected: FAIL — "미지 서브커맨드: version"

- [ ] **Step 3: 구현** —

① Create `internal/buildinfo/buildinfo.go`:

```go
// Package buildinfo — 제품 버전 단일 소스(스펙 v0.10 D56). 무의존 leaf — 아키텍처 문서 의존
// 그래프의 D13 예외(파편화가 아닌 단일 상수 leaf, store.OpenContext 선례와 동형 — Task 6에서
// 문서 갱신). commit/dirty/pseudo-version은 여기 절대 불포함(marker 등호 비교 보호 — D56).
package buildinfo

// productVersion — 릴리스 수동 지점 유일 1곳. dev 사이클 중 "-dev" 접미, 정식 릴리스 커밋에서
// 제거(마커 경계·도그푸딩 절차는 스펙 v0.10 D56·§1.3). var(const 아님) — 향후 -ldflags -X
// 주입 전환 여지(unexported + accessor = 외부 변경 차단).
var productVersion = "0.10.0-dev"

// ProductVersion — 전 소비처(CLI 배너·hook Producer·marker·doctor·MCP serverInfo·version
// 서브커맨드)의 유일 입구.
func ProductVersion() string { return productVersion }
```

② `cmd/context-router/main.go`: `const version = "0.9.0"`(29행) → `var version = buildinfo.ProductVersion()` + import 추가. (하류 소비처 — 배너·Producer·cli.Run 인자 — 는 기존 `version` 심볼 그대로라 무변경.)

③ `internal/mcp/mcp.go`: `const ServerVersion = "0.9.0"`(30행) 및 그 주석 삭제 → 패키지 내 `ServerVersion` 전 참조(`mcp.go:69`·`mcp.go:835` Implementation.Version 등 — grep으로 전수)를 `buildinfo.ProductVersion()`으로 치환 + import 추가. 컴파일 에러가 잔존 참조를 전부 드러낸다.

④ `internal/cli/cli.go` Run switch(`case "doctor"` 위)에:

```go
	case "version":
		// D56 — ProductVersion 1줄(CI 태그 검증·사용자 확인용 추출 표면). 부가 정보 없음 —
		// 상세 commit/dirty는 doctor build 라인 전담(스펙 §0 분리).
		if len(args) > 0 {
			return fmt.Errorf("cli: version: 예상치 않은 인자 %d개", len(args))
		}
		fmt.Fprintln(stdout, version)
		return nil
```

⑤ `cmd/context-router/main.go` cliSubcommands(466행)에 `"version": true` 추가.

⑥ `cmd/context-router/main_test.go`의 `TestVersionPinnedToServerVersion`(33-35행) 삭제(심볼 소멸 — 단일 소스라 불일치 표현 불가).

- [ ] **Step 4: GREEN 확인** — Run: `go build ./...` (잔존 참조 컴파일 검증) → `go test -p 1 -run "TestRunVersionSubcommand" ./internal/cli/` → PASS → `go test -p 1 ./cmd/... ./internal/mcp/ ./internal/cli/`. 잔존 리터럴 감사: `grep -rn '"0\.9\.0"\|"0\.10\.0"' cmd/ internal/ --include="*.go"` — 비테스트 히트 0 확인(테스트 픽스처는 버전 주입형이라 제외 — 스펙 §2 제외 목록).

- [ ] **Step 5: Commit** — `git commit -m "feat(v0.10): D56 버전 중앙화 — internal/buildinfo 단일 소스·version 서브커맨드·핀 테스트 삭제"`

---

### Task 6: D56 doctor build 라인 + CI 태그 검증 + 아키텍처 문서

**Files:**
- Modify: `internal/cli/cli.go` (doctor 말미 build 라인), `.github/workflows/ci.yml` (태그 검증 잡), `docs/context-router-code-architecture-ko.md` (그래프 buildinfo·D13 예외)
- Test: `internal/cli/cli_test.go` (build 라인 존재 단정)

**Interfaces:**
- Consumes: Task 5의 `version`(= ProductVersion 하류)·`version` 서브커맨드. `runtime/debug.ReadBuildInfo`.

- [ ] **Step 1: 실패 테스트 작성** — `internal/cli/cli_test.go`의 기존 doctor 테스트에 단정 추가(또는 소형 신규):

```go
// D56 — doctor build 정보 라인: ProductVersion + (성공 시) vcs 진단. 경고 아님·실패 시 생략.
// 테스트 빌드는 ReadBuildInfo가 대체로 성공하나 vcs 필드는 환경 의존 — 라인 접두만 단정한다.
	if !strings.Contains(out, "[17] build: "+testVersion) {
		t.Fatalf("[17] build 라인 없음:\n%s", out)
	}
```

(`testVersion`은 해당 doctor 테스트가 runDoctor에 주입하는 버전 문자열 변수를 그대로 사용.)

- [ ] **Step 2: RED 확인** — Run: `go test -p 1 -run "TestDoctor" ./internal/cli/` → 신규 단정 FAIL

- [ ] **Step 3: 구현** — ① `internal/cli/cli.go` doctor의 [16] 출력 직후:

```go
	// [17] build(D56) — VCS 진단 정보 라인. marker·stale 비교 비관여(안정 SemVer 계약 — 스펙 §0),
	// ReadBuildInfo 실패·필드 부재는 해당 요소 생략(정보 라인 — 경고 아님·failed 미계상).
	{
		parts := []string{}
		if bi, biOK := debug.ReadBuildInfo(); biOK {
			parts = append(parts, "go="+bi.GoVersion)
			for _, s := range bi.Settings {
				switch s.Key {
				case "vcs.revision":
					r := s.Value
					if len(r) > 12 {
						r = r[:12]
					}
					parts = append(parts, "commit="+r)
				case "vcs.time":
					parts = append(parts, "time="+s.Value)
				case "vcs.modified":
					if s.Value == "true" {
						parts = append(parts, "dirty")
					}
				}
			}
		}
		fmt.Fprintf(w, "[17] build: %s (%s)\n", version, strings.Join(parts, " "))
	}
```

② `.github/workflows/ci.yml`에 잡 추가(기존 잡 뒤, 동일 들여쓰기):

```yaml
  # D56(스펙 v0.10): 태그와 바이너리 ProductVersion 일치 검증 — -dev 잔존 태그 차단.
  tag-version:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
      - name: verify tag matches ProductVersion
        run: |
          got="$(go run ./cmd/context-router version)"
          want="${GITHUB_REF_NAME#v}"
          test "$got" = "$want" || { echo "version mismatch: binary=$got tag=$want"; exit 1; }
```

③ `docs/context-router-code-architecture-ko.md`: 의존 그래프 표(§ "패키지 의존 그래프 (확정)")에 `buildinfo` 행(무의존 leaf, 소비자 `cmd`·`mcp`·(doctor 경유) `cli`는 version 인자 하류라 직접 import는 cmd·mcp 2곳) 추가 + D13 절의 "패키지 수 증가는 D13 역행" 문단 옆에 예외 명문 1문장: "`internal/buildinfo`는 D13 예외 — 파편화가 아닌 단일 상수 leaf(`store.OpenContext` 예외 선례와 동형, 스펙 v0.10 D56·§5)." 표의 정확한 삽입 위치·형식은 문서를 열어 기존 행 형식에 맞춘다.

- [ ] **Step 4: GREEN 확인** — `go test -p 1 -run "TestDoctor" ./internal/cli/` → PASS. `go test -p 1 ./internal/cli/` 회귀. ci.yml은 `git diff`로 문면 리뷰(로컬 실행 불가 — 실태그에서 검증, 스펙 §2).

- [ ] **Step 5: Commit** — `git commit -m "feat(v0.10): D56 doctor [17] build 라인·CI 태그 검증 잡·아키텍처 그래프 buildinfo 반영"`

---

### Task 7: D57 수렴 로드맵 — vision 문서 갱신

**Files:**
- Modify: `docs/context-router-vision-proposal-ko.md` (로드맵 절 추가)

**Interfaces:** (없음 — 문서 태스크. 결정 원본은 스펙 §0 D57 — 이 절은 참조·요약이지 사본이 아니다.)

- [ ] **Step 1: 문서 절 추가** — vision 문서 말미(기존 로드맵/후속 절이 있으면 그 옆)에:

```markdown
## 수렴 로드맵 (D57, 2026-07-23 — 결정 원본: design-v0.10 §0)

- **종착지 = context-router.** ctxscribe(사용자의 mksglu/context-mode
  하드 포크)는 대체 시까지의 브리지 — 대체 후 포크 유지보수(업스트림
  선택 포팅 노동)를 종료한다.
- **대체 게이트 = exec 3종(D21 트랙).** exec가 ctr에 이식되면
  ctxscribe의 잔여 고유 가치(think-in-code 실행 샌드박스)가 소멸한다.
  v0.11+ 주력 후보로 격상.
- **근거 실측(2026-07-23, session-26)**: ctr MCP 도구 호출 8회 vs
  ctxscribe 422회(본 프로젝트 전 기간) — ctr의 고유 가치는 능동
  도구가 아니라 배경 레이어(zero-reliance 훅 캡처·A/B 계측·가드)에
  있고, 이는 ctxscribe가 구조적으로 갖지 못하는 축.
- **병행 축**: 회수 경로 채택 개선(ctr_search/ctr_fetch 사용 유도 —
  캡처의 가치는 소비가 만든다).
```

- [ ] **Step 2: 확인** — `git diff docs/context-router-vision-proposal-ko.md` 문면 리뷰(스펙 D57과 상충 없는지 — 특히 "결정 원본은 스펙" 관계 유지).

- [ ] **Step 3: Commit** — `git commit -m "docs(v0.10): D57 수렴 로드맵 — vision 문서 갱신"`

---

## 최종 검증 (전 태스크 완료 후)

- [ ] `go build ./...` && `go vet ./...` && `go test -p 1 ./...` (메모리 캡 준수) 전체 GREEN
- [ ] `grep -rn '"0\.9\.0"' cmd/ internal/ --include="*.go"` 비테스트 히트 0
- [ ] 스펙 §1.1 범위 항목별 대조(D53~D57 전부 커밋 존재)
- [ ] 도그푸딩 설치·`hook install`·릴리스 범프(0.10.0-dev→0.10.0)·태그는 **계획 밖**(릴리스 절차 — 스펙 §1.3 표준 절차)
