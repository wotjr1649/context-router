# 세션 54 스펙 — 호스트 겹침과 도구 표면 측정

**작성**: 2026-08-08 (session-53 말미, v0.19.0 배송 직후)
**성격**: 측정 세션. **코드 변경 없음이 기본값**이고, 변경은 측정이 요구할 때만.
**산출**: 이 파일에 수치와 등급을 채우고, 그것이 지지하는 방향 판단을 적는다. 방향이
정해지면 그때 설계서(`docs/context-router-design-v0.20-ko.md`)로 올라간다.

---

## 0. 왜 이 세션이 필요한가

v0.19.0을 배송한 뒤 두 가지가 드러났다.

**하나 — 호스트가 이 제품 논지의 절반을 자체 구현했다.** Claude Code는 도구 결과가 임계를
넘으면 전체를 디스크에 넣고 미리보기와 경로만 컨텍스트에 남긴다. microcompaction이 큰 것부터
먼저 밀어낸다. **이 서술은 소스 분석 2차 출처에서 왔다** `[문서·2차]` — 공식 문서가 아니다.
이 머신에서 재서 등급을 확정하는 것이 M1이다.

**둘 — 도구 열 개의 비용과 실사용이 크게 어긋난다.** 아래 §2의 표가 그것이다.

두 관측이 한 자리에서 만난다. MCP 사양 `2026-07-28`이 `tools/list` 결과에 `ttlMs`·`cacheScope`를
**필수**로 얹는데, 현재 스키마 예산 여유는 **33 B**다. **쓰지 않는 도구를 정리하는 것이
사양 대응의 예산을 만드는 일**이다.

**이 세션의 규율**: 방향을 먼저 정하고 측정으로 확인하는 순서를 피한다. 소유자가 제품 방향을
"측정이 답하게 한다"로 정했다 — 측정이 먼저다.

---

## 1. 이미 잰 것 — 다음 세션은 이것을 다시 재지 않는다

전부 2026-08-08, 이 머신 `[실측]`.

### 1.1 저장 규모

```
context-router 스토어   1,343.7 MB   (.db 92개 = 1,230.2 MB, -wal 26.7 MB)
Claude Code 전사본        566.5 MB   (jsonl 917개, 이 프로젝트 것 93개)
                        ─────────
같은 부류 데이터 합계     ~1.9 GB

이 프로젝트 스토어: 블롭 65,564,780B → DB 파일 709,890,048B  (약 10.8배)
  doctor [14]가 CTR_CONTENT_FILE_WARN_BYTES(104,857,600B) 초과 경고 중.
  회수는 VACUUM 뿐이고 라이브 서버 제약으로 서버 비가동 시에만 가능. 자동 삭제 없음.
```

**두 수치의 단위가 같지 않다** — 양쪽 다 전 프로젝트 범위지만, 우리 쪽은 SQLite 파일(FTS 축·
청크·free page 포함)이고 호스트 쪽은 원본 jsonl이다. 위의 10.8배가 그 차이의 크기를 말한다.
**"우리가 2.3배 더 쓴다"로 읽으면 안 된다** — 같은 원본 바이트를 담는 비용이 형식마다 다르다.
M2·M3이 재는 것은 **원본 바이트의 겹침**이지 파일 크기의 비가 아니다.

### 1.2 도구 호출 실적 (`stats`, 2026-07-19 ~ 08-01)

배포본은 **0.17.0**이었다 — **D99 지연 적재가 없던 구간이라 열 개가 전부 상시 로드였다.**
이 사실이 아래 해석의 전제다.

| 도구 | 호출 |
|---|---:|
| `ctr_search` | 58 |
| `ctr_fetch` | 49 |
| `ctr_execute` | 49 |
| `ctr_execute_file` | 16 |
| `ctr_transform` | 8 |
| `ctr_session_summary` | 5 |
| `ctr_export_events` | 3 |
| `ctr_index` | 1 |
| `ctr_record_event` | 1 |
| `ctr_fetch_and_index` | **0** |
| **합계** | **190** (반환 2,648,277B) |

### 1.3 도구별 스키마 바이트 (영구 컨텍스트 비용)

`CTR_ENABLE=ingest,net,exec`로 띄운 서버의 `tools/list` 왕복 실측.

| 도구 | 전체 | description | inputSchema | alwaysLoad |
|---|---:|---:|---:|---|
| `ctr_search` | 2,826 | 928 | 530 | **YES** |
| `ctr_fetch` | 1,959 | 283 | 648 | |
| `ctr_session_summary` | 1,911 | 132 | 360 | |
| `ctr_export_events` | 1,751 | 182 | 441 | |
| `ctr_index` | 1,744 | 637 | 602 | **YES** |
| `ctr_record_event` | 1,420 | 356 | 762 | |
| `ctr_execute` | 1,325 | 313 | 504 | |
| `ctr_transform` | 1,189 | 387 | 496 | |
| `ctr_execute_file` | 1,121 | 96 | 512 | |
| `ctr_fetch_and_index` | 800 | 65 | 237 | |
| **합계** | **16,067** | | | 상한 16,100 (여유 33) |

### 1.4 비교 대상 — `mksglu/context-mode`

같은 문제를 푸는 프로젝트. `[문서]` — 2026-08-08에 README를 **두 경로로 각각 읽어** 도구 이름
열한 개·세션 기록 담당(훅)·임계 5 KB·훅 이벤트 여섯·라이선스가 서로 일치하는 것을 확인했다.
남의 소프트웨어에 대한 서술이므로 한 출처로 적지 않는다.

| | context-router (10) | context-mode (11) |
|---|---|---|
| 검색·색인·웹·실행 | `search` `index` `fetch_and_index` `execute` `execute_file` | 같은 다섯 (`ctx_` 접두) |
| byte-exact 회수 | **`ctr_fetch`** (chunk/line/byte 선택자) | 없음 |
| 밀폐 변환 | **`ctr_transform`** (starlark) | `ctx_batch_execute` |
| 세션 | 도구 셋 | 훅이 자동, 도구 없음 |
| 운영 | **CLI 전용** | 도구 다섯 (`doctor` `upgrade` `purge` `stats` `insight`) |
| 호스트 | 2 | 17 |
| 훅 이벤트 | 3 | 6 (`PreCompact`·`UserPromptSubmit` 포함) |
| 임계 | 16 KiB | 5 KB + intent |
| 분석 | 없음 | 호스트형 대시보드 |
| 라이선스 | Elastic-2.0 | Elastic-2.0 |

**이 비교가 근거를 준 우리 판단 둘.**

1. **운영을 CLI에 둔 것.** 저쪽은 11개 중 다섯이 운영 도구다(`stats`·`doctor`·`upgrade`·
   `purge`·`insight`). 영구 스키마 비용도 비용이지만, 더 무거운 것은 **그 다섯이 모델이 호출
   가능한 표면에 있다**는 점이다 — MCP 도구라는 사실에서 곧바로 따라온다. 공개 문서가 적는
   각각의 용도가 사실이라면(`purge`는 색인된 지식 베이스 전체 삭제, `upgrade`는 원격에서 받아
   재빌드하고 훅 재구성 `[문서]`), **`upgrade`는 호스트 설정을 쓰는 자리이고 D96 계약 1이
   금한 바로 그 형태다.**
   **이것은 설계 차이이지 결함 주장이 아니다** — 저쪽의 위협 모델이 우리와 다를 수 있고,
   우리는 그것을 읽지 않았다 `[미실측]`. 여기 적는 값은 **우리 D96 판단에 비교 근거가
   생겼다**는 것까지다.
2. **로컬 전용.** 호스트형 대시보드가 없는 것이 지금은 기능 부족이 아니라 신뢰 경계다.

**저쪽이 앞선 셋.** 호스트 17개. `PreCompact` 훅 — 호스트가 압축하기 **전에** 스냅샷을
만드는 것으로, M1이 재는 바로 그 문제에 대한 직접적 답이다. 5 KB 임계.

**우리만 있는 둘.** `ctr_fetch`의 byte-exact 선택자, `ctr_transform`의 밀폐 변환.

---

## 2. 재는 것 여섯

### M1 — 호스트 오프로딩 실측 `[문서·2차]` → `[실측]`

`~/.claude/projects/**/*.jsonl`에서 오프로딩 레코드를 뽑아 **임계와 미리보기 크기를 확정**한다.

- 표본 하나(이 프로젝트 최대 파일, 6,898,169B)에 마커가 실재한다 `[실측]`:
  `tool_result` 327 · `toolUseResult` 325 · `stored` 124 · `preview` 61 · `truncated` 49 ·
  `offload` 28.
- 확인할 값: 오프로딩 임계(2차 출처는 50,000자), 남는 미리보기 크기(2차 출처는 2KB),
  오프로딩된 본문의 실제 저장 위치.
- **답**: 2차 출처의 두 수치가 이 머신에서 사실인가.

### M2 — 임계 구간 분포 ★ 이 세션의 핵심 수치

도구 결과 크기 히스토그램을 세 구간으로 낸다. 각 구간의 **건수와 바이트**를 낸다.

| 구간 | 누가 잡는가 |
|---|---|
| `< 16 KiB` | 아무도 안 잡는다 |
| `16 KiB ~ M1이 확정한 호스트 임계` | **우리만** |
| `> 호스트 임계` | 둘 다 |

- **답**: 가운데 구간이 이 제품의 고유 영역이다. 그 크기가 방향을 정한다.
- 세 번째 구간이 압도적이면 중복 저장이고, 가운데가 크면 우리 임계가 값을 낸다.

### M3 — 중복률

우리 shadow artifact와 호스트 전사본의 도구 결과를 **해시로 대조**한다.

- 우리는 artifact 해시를 갖고 있다(`doctor [15]` hashes=922). 전사본 쪽 도구 결과 본문을
  같은 방식으로 해시해 교집합을 센다.
- **답**: 같은 바이트를 둘 다 갖고 있는 비율.
- **비싸지면 멈춘다.** M2가 이미 구간별 추정을 주므로 M3은 정밀화다.

### M4 — Codex 쪽 오프로딩 유무 ★ 갈림길

Codex CLI가 자체 오프로딩을 하는가.

- **답이 "안 한다"면 양 호스트 지원이 차별점의 중심이 된다** — Claude에서 겹치더라도 Codex에서
  온전하다. 답이 "한다"면 겹침이 양쪽이다.
- 재는 법: Codex 세션 전사본(있다면) 또는 `codex` 설정·문서에서 오프로딩 기제를 찾고,
  없으면 큰 출력 하나를 내고 세션 상태를 관찰한다.

### M5 — 회수 실적

저장된 artifact 중 실제로 `ctr_search`·`ctr_fetch`가 건드린 비율.

- **먼저 확인할 것: 지금 데이터로 이 질문에 얼마나 답할 수 있는가.** `stats`는 도구별 호출
  수만 주고 artifact 단위 접근 기록이 있는지는 확인되지 않았다.
- 부족하면 **그 사실을 기록하고 계측을 v0.20 후보로 남긴다.** 없는 데이터를 만들려고 이
  세션을 늘리지 않는다.
- 참고 상계: 2주간 저장 ~1.3 GB 대 도구 반환 2.6 MB.

### M6 — 도구 표면 재판정 ★ 새로 더한 것

§1.2·§1.3의 표를 **v0.19 실사용 구간**으로 갱신하고, 아래 가설 셋을 확인하거나 뒤집는다.

**전제**: §1.2의 구간은 0.17.0(전부 상시 로드)이었다. D99 지연 적재가 사용을 어떻게 바꾸는지가
판정의 전제이므로, v0.19를 쓴 구간이 생긴 뒤에 판정한다.

**착수 조건(판정 가능한 형태)**: `stats`의 도구별 span 중 **2026-08-08 이후 호출이 열 건
이상**일 것. 그 미만이면 표본이 가설을 뒤집을 힘이 없으므로 M6은 **가설 셋을 그대로 둔 채
닫고**, `stats`의 그 시점 수치만 이 문서에 덧붙인다. 열 건은 임의의 하한이다 — 이 세션에서
"판정할 수 있다/없다"를 사람이 다시 판단하지 않게 하려고 미리 고정한 것이고, 표본이 그
언저리면 판정 대신 관측만 적는다.

#### H1 — `ctr_fetch_and_index`는 뺄 후보다

800 B, 2주간 **0회**.

- 확인할 것: `ctr_index` + 외부 fetch로 같은 일이 되는가, 아니면 SSRF 안전 fetch가 이 도구
  안에만 있어 대체 불가인가.
- 뒤집는 증거: v0.19 구간에서 호출이 붙는다 / 대체 경로가 없다.
- 닫는 방식: 남기면 근거를 적고, 빼면 `net` 프로필의 의미가 무엇이 되는지 함께 판정한다.

#### H2 — alwaysLoad 쌍이 틀렸다

D99는 `ctr_search` + `ctr_index`를 상시로 골랐다. 사용은 `ctr_search`(58) + `ctr_fetch`(49)를
가리키고, `ctr_index`는 **1회**다.

- **`ctr_index`의 저사용에는 선의의 해석이 있다**: 훅 shadow 포착이 색인을 자동으로 하므로
  수동 색인이 필요 없었다. 그것은 **설계가 의도대로 도는 증거**이지 상시 적재의 근거는 아니다.
- 확인할 것: 지연 적재 아래에서 `ctr_fetch` 호출이 유지되는가(유지되면 지연이 장벽이 아니라는
  뜻), `ctr_index`가 상시인데도 계속 안 쓰이는가.
- 뒤집는 증거: 지연된 `ctr_fetch`의 호출이 급감한다 → 지연이 실제 장벽이고 상시 자리는 사용
  빈도가 아니라 **진입 가능성**으로 정해야 한다는 뜻이 된다.
- D99의 근거 문장(색인에 없는 지연 도구는 모델이 호출하지 않는다)이 이 관측과 어떻게 맞물리는지
  함께 적는다.

#### H3 — 세션 트리오가 가장 큰 회수 후보다

`ctr_record_event` + `ctr_export_events` + `ctr_session_summary` = **5,082 B, 예산의 32%**,
2주간 **9회**.

- context-mode는 같은 일(세션 기록)을 **훅으로만** 하고 도구를 두지 않는다.
- 확인할 것: 이 셋이 여는 것 중 훅과 CLI로 대체되지 않는 것이 무엇인가. 모델이 세션 이벤트를
  **쓰는 것**(`record_event`)과 **읽는 것**(`export_events`·`session_summary`)은 성격이 다르다 —
  나눠서 판정한다.
- **이것은 제품 정체성 질문이다.** M2·M4의 결과와 묶어 판단하고, 단독으로 결정하지 않는다.
- 뒤집는 증거: v0.19 구간에서 세션 도구 사용이 붙는다 / 교차 세션 연속성이 이 셋에 의존한다.

---

## 3. 방법

**이 프로젝트 자신의 도구로 잰다.** 전사본 566 MB를 컨텍스트에 넣지 않고 `ctr_index`로 색인한
뒤 `ctr_search`·`ctr_transform`으로 집계한다. 측정 자체가 도그푸딩이 되고, 이 규모를 다루면서
제품이 제 값을 하는지가 부수적으로 드러난다 — **그 관측도 기록한다**(M5의 간접 증거가 된다).

**전사본은 대화 내용이다.** 집계는 **크기·해시·마커까지만** 다루고 내용은 산출물에 담지
않는다. 잘라 낸 조각을 예시로 싣지 않는다.

---

## 4. 멈춤 조건

**M1·M2·M4가 나오면 방향 판단이 선다.** 다섯을 다 채우는 것이 목표가 아니다.

- **M3**가 비싸지면 멈추고 미실측으로 기록한다 — M2가 구간별 추정을 이미 준다.
- **M5**는 지금 데이터로 답할 수 있는 만큼만 답하고, 계측이 필요하면 v0.20 후보로 넘긴다.
- **M6**은 v0.19 사용 구간이 없으면 **가설 셋을 그대로 둔 채 닫는다.** 데이터 없이 도구를
  빼지 않는다.

---

## 5. 이 세션 이후의 후보 (여기서 결정하지 않는다)

- **MCP `2026-07-28` 대응.** `server/discover`가 **MUST**, `tools/list`에 `ttlMs`·`cacheScope`
  필수, stateless(`initialize` 제거), 모든 결과에 `resultType`. `go-sdk`는 **v1.7.0**이
  지원하고 우리는 **v1.6.1**이다. 폐기 예정인 roots·sampling·logging은 **우리가 셋 다 쓰지
  않고 로그는 이미 stderr**라 영향이 없다 `[실측]`.
  **착수 전 선행 측정 하나**: 우리 두 호스트가 지금 협상하는 `protocolVersion`. 구 버전이면
  기록하고 대기한다.
- **저장 비용.** VACUUM 경로(라이브 서버 제약), 보존 정책, 스토어 92개, 임계 재조정.
- **Codex 거부 의미** (설계서 §5-1의 남은 미확정). 웹의 2차 출처들이 서로 엇갈린다 —
  `exit 2`만 본다는 쪽과 `permissionDecision`의 `ask`까지 본다는 쪽. 프로브 절차가
  `C:\Users\js\ctr-codex-migration\`에 서 있으니 잰다.
- **`CHANGELOG` 훅 신뢰 문장 정정** — 다음 릴리스 항목(session-53 §5.1).

---

## 6. 기록 규율

- 모든 수치에 등급을 붙인다. `[실측]`은 이 머신에서 명령을 돌리거나 파일을 연 것이다.
- **주장이 측정을 넘지 않게 한다.** session-53이 이 형태의 실패를 여섯 번 했다(§3.7·§3.10).
  인과 설명은 `[추정]`으로 내리고, 구간에 여러 원인이 들어갈 수 있으면 `[미실측]`으로 남긴다.
- 하류 사본은 인용하게 하고, 등급은 주장이 처음 쓰이는 자리에만 붙인다.

---

# 7. 측정 결과 — 세션 54 (2026-08-08)

§0–§6은 측정 **전에** 쓴 것이고 그대로 둔다. 이 절이 답이다. 어긋나는 곳은 이 절이 이긴다.

**코퍼스는 살아 있다.** 전사본은 이 세션이 도는 동안에도 늘어난다 — 같은 스캐너를 두 번
돌리면 도구 결과 수가 32,761 → 32,798로 움직였다. 아래 수치는 **마지막 실행**의 것이고,
백분율은 그 사이 0.1%p 안에서만 변했다.

## 7.1 방법 — §3을 쓰지 않았다

§3은 전사본을 `ctr_index`로 색인한 뒤 `ctr_search`·`ctr_transform`으로 집계하자고 했다.
**쓰지 않았다.** 두 이유이고, 둘 다 다음에도 유효하다.

1. **S4.** 전사본 567 MB는 과거 대화 전문이고 비밀값이 섞여 있을 수 있다. 크기 히스토그램
   하나를 얻자고 그것을 스토어에 색인하는 것은 비례하지 않는다.
2. **묻는 것이 검색이 아니라 집계다.** `ctr_search`는 BM25라 히스토그램을 못 내고,
   `ctr_transform`은 artifact 하나 단위여서 918개면 호출이 918회다.

대신 스트리밍 스캐너 여덟 개를 썼다(§7.9). **문자열 값은 길이만 내고 내용은 어떤 산출물에도
담지 않는다** — §3의 그 규율은 지켰다.

**이 이탈 자체가 관측이다** `[실측]`. 이 제품은 **회수**에 답하고 **집계**에는 답하지 않는다.
`stats`가 도구별 합계를 주지만 artifact 단위 질문(어느 것이 얼마나 컸나, 무엇이 읽혔나)에
닿는 표면이 없다. M5가 계측을 v0.20 후보로 남기는 이유가 여기서 한 번 더 나온다.
비교 대상 `context-mode`가 `insight` 도구를 둔 자리이기도 하다.

## 7.2 M1 — 호스트 오프로딩 `[문서·2차]` → `[실측]`

**오프로딩은 실재한다.** 저장 위치는
`~/.claude/projects/<프로젝트>/<세션ID>/tool-results/` 이고 파일 이름이 **`<tool_use_id>.txt`**
다 — 이름이 곧 귀속이라 분류가 자기검증된다.

```
tool-results 파일 173개 / 62,237,635 B / 최소 10,367 B / 최대 6,928,664 B
  toolu_<id>.txt    95   도구 결과 오프로딩  (그중 90개가 스캔한 전사본의 tool_use_id와 일치)
  hook-<uuid>-*.txt 38   훅 systemMessage·additionalContext
  webfetch-*.pdf    29   WebFetch가 받은 PDF 원본
  기타              11
```

**2차 출처의 두 수치 중 하나는 맞고 하나는 틀렸다.**

- **미리보기 ~2 KB — 맞다** `[실측]`. 오프로딩된 90건이 컨텍스트에 남긴 문자 수:
  최소 1,732 · p10 1,987 · p25 2,065 · **p50 2,162** · p75 2,228 · p90 2,320 · 최대 3,431.
  90건 중 81건(90%)이 [1,900, 2,600] 안이다. 오프로딩은 5,517,066자 중 194,982자만 남긴다
  (**3.5% 잔존**).
- **임계 50,000자 — 규칙으로서 틀렸다** `[실측]`. 오프로딩된 것의 최소가 21,404자인데,
  **그보다 크면서 컨텍스트에 그대로 남은 결과가 730건**이다. 남은 것의 최대는 115,121자로
  오프로딩된 것의 p50(60,850)보다 크다. **단일 크기 임계가 아니다.**

**그러면 무엇이 정하는가 — 크기도 압력도 아니다.**

- 압력 가설도 성립하지 않는다 `[실측]`. 16 KiB 이상 결과의 호출 시점 컨텍스트 토큰
  (호스트가 기록한 cache_read+cache_creation): 오프로딩된 것 p10/p50/p90 =
  27,602 / 70,398 / 254,931, 남은 것 = 34,237 / 67,611 / 253,986. **사실상 같은 분포다.**
- **도구가 정한다** `[실측]`. 16 KiB 이상 결과의 오프로딩 비율:

  | 도구 | 오프로딩 / 16 KiB 이상 |
  |---|---|
  | `Read` | **0 / 736** |
  | `Write` | **0 / 129** |
  | `Edit` | **0 / 19** |
  | `WebFetch` | 53 / 89 |
  | `Bash` | 16 / 98 |
  | `Grep` | 14 / 34 |
  | `PowerShell` | 7 / 23 |

  **왜 도구마다 다른지는 `[미실측]`이다.** 파일 내용을 돌려주는 도구(Read·Write·Edit)가
  통째로 면제라는 것까지가 관측이고, 그 규칙의 문면은 우리가 읽지 않았다.

**★ 오프로딩은 디스크가 아니라 컨텍스트에서만 뺀다** `[실측]`. 오프로딩된 90건 **전부**,
전사본의 `toolUseResult`가 저장 파일의 95% 이상을 여전히 담고 있다(90/90, 부분 0). 표본
하나의 형태: `stdout` 길이 73,439 = 저장 파일 73,439 B, 그런데 모델이 본 것은 ~2 KB 문자열
하나. **호스트의 오프로딩은 보존 기능이 아니라 컨텍스트 창 기능이다.** M3의 해석이 여기서
갈린다(§7.4).

**마커 문자열 세기는 오프로딩의 증거가 아니다.** 이 기제가 남기는 것은 **파일 참조**이지
`offload`·`preview`·`stored`라는 이름의 JSON 키가 아니다 — 실제 오프로딩 레코드 둘의
`toolUseResult` 키는 각각 `interrupted·isImage·noOutputExpected·stderr·stdout`(Bash)와
`appliedLimit·content·mode·numFiles·numLines·totalLines`(Grep)로, 그 셋 중 아무것도 없다
`[실측]`. 전사본 파일 셋의 전체 키 경로 목록에도 없다 `[실측 — 표본 3파일]`.
§2 M1이 인용한 부분일치 수가 **무엇을 셌는지는 우리가 재지 않았다** `[미실측]` — 확실한
것은 그 수가 오프로딩 건수의 근거가 될 수 없다는 것까지다. **분류는 파일 이름으로 한다.**

## 7.3 M2 — 임계 구간 분포 ★ `[실측]`

**임계를 가정하지 않았다.** §2의 세 구간은 "호스트 임계"를 알아야 그을 수 있는데 §7.2가 그런
임계가 없음을 보였다. 대신 **호스트 자신의 오프로딩 판정을 분류기로** 썼다 — 더 강한 형태다.

전 프로젝트, 도구 결과 **32,798건 / 111,483,670자**:

| 구간 | 누가 잡는가 | 건수 | 자 | 비중 |
|---|---|---:|---:|---:|
| A `<16 KiB`, 호스트 유지 | **아무도** | 31,659 | 70,477,612 | **63.2%** |
| A′ `<16 KiB`, 호스트 오프로딩 | 호스트만 | **0** | 0 | 0% |
| B `≥16 KiB`, **호스트가 컨텍스트에 그대로 둠** | **우리만** | 1,049 | 35,488,992 | **31.8%** |
| C `≥16 KiB`, 호스트 오프로딩 | 둘 다 | 90 | 5,517,066 | **4.9%** |

**16 KiB 이상 결과 중 호스트가 오프로딩하는 것은 90/1,139 = 7.9%다.**

이 프로젝트 전사본만: A 20,641건 / 47,500,775자(66.7%) · **B 705건 / 22,312,753자(31.3%)** ·
C 26건 / 1,448,456자(2.0%).

**측정 단위의 편향 방향** `[실측]`. 우리 임계는 `tool_response`의 **직렬화 JSON 바이트**에
걸린다(`internal/hook/shadow.go:37-38`, `size := len(body)`). 위 표의 단위는 이스케이프를 푼
문자 수라 항상 그보다 작다. **따라서 구간 B는 우리가 실제로 잡는 것의 하한이다** — 과장이
아니라 과소다. 상한 쪽은 문제없다: 구간 B의 최대가 115,121자로 `CTR_SHADOW_MAX`(1 MiB)
안에 전부 들어간다.

**제외한 것 하나** — `webfetch-*.pdf` 29건(~50 MB)은 `<tool_use_id>.txt` 규칙에 걸리지 않아
구간 C에 없다. 바이너리 다운로드라 우리 훅도 잡지 않는다(바이너리 판정에서 스킵).

## 7.4 M3 — 중복률 `[M2가 대체]`

**해시 대조를 하지 않았다.** §4가 "비싸지면 멈춘다"고 했고, §7.2의 분류가 파일 이름으로
자기검증되므로 해시가 더 줄 정보가 없다. 답은 두 겹이다.

1. **호스트의 오프로딩 저장소와의 중복**: 90건 / 5,517,066자. 우리가 잡을 것(구간 B+C =
   1,139건 / 41,006,058자) 대비 **건수 7.9% · 바이트 13.5%** `[실측]`.
2. **★ 호스트의 전사본과의 중복은 사실상 전부다** `[실측]`. `toolUseResult`가 모든 도구
   결과의 전문을 담고(오프로딩된 것 포함 — 90/90), 그 파일은 `~/.claude/projects`에 그대로
   남는다. **우리가 shadow로 저장하는 바이트는 Claude에서는 이미 디스크에 있다.**
   완전한 사본은 아니다: 824건(약 2.5%)이 잘림 표식을 달고 있어 그만큼은 호스트도 원본을
   잃는다 `[실측]`. 다만 고정 상한은 없다 — 16 KiB 이상에서 같은 크기가 반복되는 최대
   빈도가 9회에 그친다(고정 캡이 있으면 여기 뭉친다).
   **잘림 표식 수는 정규식 의존이다** — 패턴을 넓히거나 좁히면 값이 10% 안팎으로 움직인다
   (Codex 쪽에서 같은 두 패턴이 842와 932를 냈다). **오프로딩 분류(파일 이름)와 달리 이
   수치는 자기검증되지 않으므로 "약"을 붙인다.**

**이것이 이 세션이 바꾼 서술이다.** Claude에서 우리 값은 "밖으로 뺀다"도 "보존한다"도 아니다
— 바이트는 이미 append-only jsonl에 있다. **값은 그것이 질의 가능하지 않다는 데 있다.**

## 7.5 M4 — Codex 쪽 오프로딩 ★ `[실측]`

**Codex는 오프로딩하지 않는다. 대신 잘라내고, 잘린 바이트는 어디에도 없다.**

```
~/.codex/sessions   rollout-*.jsonl 272개 / 376.0 MB / 도구 출력 16,253건
  오프로딩 저장소            없음 (attachments 14파일 0.31 MB — 사용자 첨부)
  출력 크기 p50 1,278 · p90 29,142 · p99 40,176 · 최대 134,868
  ≥16 KiB 2,913 · ≥32 KiB 1,247 · ≥64 KiB 16 · ≥1 MiB 0
```

**절단 캡이 ~40,000자에 있다** `[실측]`. **40,173~40,177 다섯 값에만 387건이 뭉쳐 있다** —
크기 분포에 자연히 생길 수 없는 모양이고, 이것이 캡의 직접 증거다(캡 40,000 + 고지 문구
~175자로 읽힌다 `[추정]`). [39,500, 41,000] 구간 586건 중 **95%(556건)가 잘림 표식**을
달고 있다. 전체로는 **약 932건(5.7%)이 잘린 출력**이다 — 이 수는 §7.4의 단서대로 정규식
의존이라 "약"을 붙인다(같은 코퍼스에서 두 패턴이 842와 932를 냈다). **뭉침 387건 쪽은
정규식과 무관하므로 캡의 존재 자체는 흔들리지 않는다.**

**갈림길의 답**: §2가 "안 한다면 양 호스트 지원이 차별점의 중심이 된다"고 했다. 답은 "안
한다"보다 강하다 — **파괴한다.** 다만 그 강한 형태는 조건부다(§7.8).

## 7.6 M5 — 회수 실적 `[실측 상계]`

```
content.db  artifacts=920  전부 16 KiB 이상 (최소 16,431 B)
            p50 50,208 · p75 90,801 · p90 121,025 · p99 189,488 · 최대 1,768,890
  sources: hook 918 · file 2
  CTR_SHADOW_MAX(1 MiB) 초과는 file 소스 1건뿐 — 훅 포착은 전부 [16 KiB, 1 MiB] 안이다
```

**상계: 저장된 920개 중 실제로 건드려진 것은 최대 11.6%다** `[실측]`. `ctr_fetch` 49회 +
`ctr_search` 58회 = 107회가 이 스토어 생애 전부이고, 그 전부가 서로 다른 artifact를 건드렸다고
가정한 값이다. 실제 값은 이보다 낮다.

**정확한 값은 지금 데이터로 낼 수 없다** `[실측]` — 스키마에 artifact 단위 접근 기록이 없다
(`artifacts(id, content_hash, media_type, byte_length, redaction, created_at)`, `ledger`는
도구별 합계만). **계측을 v0.20 후보로 남긴다.** §4가 지시한 그대로다.

호스트 귀속 분해 `[실측]`: `cc:` 906 artifact / 57,666,452 B / 최대 206,364 B / 40,177 B 초과
557건, `cx:` **12 artifact / 271,150 B / p50 17,520 / 최대 33,213 / 40,177 B 초과 0건**.

## 7.7 M6 — 도구 표면 재판정 `[닫힘 — 가설 셋 그대로]`

**착수 조건 미달이다.** `stats`의 span이 **2026-08-01에서 끝나고 그 뒤 호출이 0건**이다
(합계는 §1.2와 완전히 동일 — 190회 / 2,648,277 B). §2가 미리 박은 조건은 "2026-08-08 이후
열 건 이상"이었다.

**따라서 H1·H2·H3은 손대지 않고 닫는다. 도구를 빼지 않는다.** 이것은 판단이 아니라 사전
등록된 규칙의 적용이다 — 그 규칙을 넣은 이유가 지금 이 자리에서 사람이 다시 판단하지 않게
하려는 것이었다.

**따라오는 결과 하나**: MCP `2026-07-28`이 요구하는 `ttlMs`·`cacheScope`를 넣을 예산(여유
33 B)이 이번에 생기지 않았다. **v0.19를 쓰는 구간을 만드는 것이 그 대응의 선행 조건이다.**

## 7.8 방향 판단 — M1·M2·M4가 지지하는 것

멈춤 조건(M1·M2·M4)이 채워졌다. 이 넷이 근거다.

1. **겹침은 절반이 아니라 4.9%다.** 세션 53이 "호스트가 이 제품 논지의 절반을 자체
   구현했다"고 적은 것은 `[문서·2차]`였고, 실측은 훨씬 좁다. 호스트는 16 KiB 이상 결과의
   **7.9%만** 오프로딩하고, 그것도 도구에 따라 통째로 면제한다.
2. **구간 B(31.8%, 이 프로젝트 31.3%)가 우리 고유 영역이고 전부 우리 포착 창 안에 있다.**
   §2가 "가운데가 크면 우리 임계가 값을 낸다"고 했다 — 가운데가 크다.
3. **논지가 이동한다.** Claude에서 우리 값은 "밖으로 뺀다"가 아니다. 바이트는 호스트
   전사본에 이미 있다(§7.4). **값은 질의 가능성이다** — FTS, byte-exact 선택자, 교차 세션.
   세션 53의 브레인스토밍이 같은 방향을 가리켰고, 이제 그 서술이 실측 위에 선다.
4. **Codex에서는 논지가 여전히 보존이다** — 단, §7.9의 열린 측정 하나에 달렸다.
5. **도구 표면은 데이터가 없어 그대로다.** 다음에 할 일은 도구를 빼는 것이 아니라
   **v0.19를 쓰는 것**이다.

**설계서(v0.20)로 올릴 것은 1·2·3이다.** 4는 §7.9가 닫힌 뒤에 올린다.

## 7.9 남은 `[미실측]` 하나 — 절차를 박아 둔다

**우리 Codex 훅은 절단 전 출력을 보는가, 후를 보는가.** 이 답이 §7.8-4를 정한다 — 후라면
"Codex에서 우리가 유일한 사본"이 아니라 "같은 잘린 바이트를 질의 가능하게 둔다"로 약해진다.

**지금 표본으로는 갈리지 않는다** `[실측]`: `cx:` artifact 12개, 최대 33,213 B — 캡(~40,177 B)
아래다. 플러그인이 2026-08-08에 설치돼 구간이 몇 시간뿐이다.

**절차** (소유자가 Codex 세션에서 돌린다):

```
선행 조건 관문 — 통과 못 하면 무엇을 먼저 할지만 알리고 멈춘다:
  codex plugin list       → context-router installed, enabled 0.19.0
  저장소를 CWD로 연 세션  → context-router doctor 의 [2] ProjectID=context-router-4d307dacd75b

기준값 (2026-08-08 측정):
  [15] cx:=271,150B, cx: artifact 12개, 최대 33,213B

유발: 한 번의 도구 호출로 45,000자 이상 출력 하나를 낸다.
     (Codex의 캡이 ~40,000자이므로 캡을 확실히 넘겨야 한다)

판정: context-router doctor 의 [15] cx: 증가분과, cx: artifact 최대 크기.
  최대 > 40,177 B  → 훅이 절단 전 출력을 본다.  §7.8-4가 선다.
  최대 ≈ 40,17x B  → 훅이 절단 후를 본다.       §7.8-4를 약한 형태로 고친다.
  cx: 무변화       → 훅이 안 돌았다. 발화 문제이지 이 질문의 답이 아니다.
```

세션 53이 배운 것을 그대로 쓴다: 축을 `doctor [15]`의 `cx:`로 잡고(Claude 활동이 못
움직이므로 귀속이 깨끗하다), `CTR_STORE_ROOT`를 임시 경로로 미는 방식은 **쓰지 않는다**
(그 변수가 훅 자식까지 전파되는지가 미실측이라 거짓 음성이 나온다).

## 7.10 하지 않은 선행 측정 하나

§5가 MCP `2026-07-28` 대응의 선행 조건으로 "두 호스트가 협상하는 `protocolVersion`"을
적었다. **재지 않았다** — 멈춤 조건이 이미 채워졌고, §7.7이 그 대응 자체를 v0.19 사용
구간 뒤로 미뤘기 때문이다. 시도한 값싼 경로 둘은 실패했다 `[실측]`: Codex rollout 전사본에
`protocolVersion` 문자열이 **없고**, `~/.claude/debug`의 파일 셋은 2026-08-07자로 이 측정
구간 밖이다. **호스트의 `initialize` 요청을 서버 쪽에서 관측하는 경로가 따로 필요하다.**

## 7.11 측정 도구

스크래치패드에 스캐너 여덟 개와 스토어 조회기 하나를 뒀다(전부 Go, stdlib + 이 저장소가
이미 쓰는 `modernc.org/sqlite`). **저장소에 남기지 않는다** — 일회성 측정기이고, 남기면
계약 없는 코드가 된다. 재현이 필요하면 이 절의 서술로 다시 쓴다.

| 이름 | 무엇을 | 어떻게 |
|---|---|---|
| scan1 | 전사본 레코드 구조 | 키 경로 + 값 **타입**, 문자열은 길이만 |
| scan2–4 | 크기 분포 초안 | 오프로딩 판정에 거짓 양성 — 폐기 |
| **scan5** | M1·M2 정본 | 분류 = `tool-results/<자기 tool_use_id>.txt` 존재 |
| **scan6·7** | M4 | Codex 출력 크기 분포, 절단 캡 확정 |
| scan8 | 오프로딩 레코드 형태 | 키 경로 + 길이만 |
| **q** | M5 | `content.db`·`session.db`를 `mode=ro`로 열어 크기·귀속 |

**scan2–4를 표에 남긴 이유**: 셋 다 "경로 문자열이 레코드 어딘가에 있으면 오프로딩"으로
판정해 거짓 양성을 냈고(오프로딩 파일을 `Read`한 레코드가 오프로딩으로 잡힌다), 그 상태로
읽은 임계가 "min 268 B"였다. **자기검증되는 분류(파일 이름 = 자기 id)로 바꾸고 나서야 숫자가
섰다.** 다음 측정에서 같은 함정을 다시 파지 않도록 남긴다.
