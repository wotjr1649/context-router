# Changelog

이 파일은 **v0.12.0부터** 기록한다. 그 이전 릴리스는 git 태그와 설계 문서
(`docs/context-router-design-v0.X-ko.md` — D 번호 결정 이력)를 참조한다. 소급
작성은 하지 않았다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/), 버전은
[Semantic Versioning](https://semver.org/lang/ko/)을 따른다. 각 항목의 D 번호는
그 결정의 근거가 있는 설계 문서 절을 가리킨다.

## [Unreleased]

## [0.20.0] — 2026-08-10

`ctr_fetch`의 회수 성공·실패가 지금까지 원장에 남지 않아 72시간 보존 창을 늘릴지 줄일지
판단할 데이터가 없었다. 이 릴리스는 그 실적을 `ledger.db`에 남겨 창을 데이터로 판단할 길을
연다(D103). 함께, v0.19.1이 "업그레이드 주의"에 적어 두고 실제 스토어에서 재기 전까지 미뤄
둔 D102의 항목 둘 — 병합 ctx에 deadline을 둘지, 병합 뒤 부푼 WAL을 어떻게 되돌릴지 — 을
배포 뒤 실측으로 닫는다. 코드가 새로 하는 일은 원장 계측과 DSN 파라미터 하나뿐이고, **사용자가
보는 변화는 `stats` 출력에 몰려 있다** — 회수 실적 줄이 하나 붙고 `total` 행의 합산 범위가
`ctr_*`로 좁아진다. 그 밖의 동작은 바뀌지 않는다.

### Added

- **회수 실적 계측** (D103). `ledger.db`에 `artifact_id`·`artifact_age_s`·`shadow_owned` 세
  열이 붙고, `ctr_fetch`는 **해소했을 때와 artifact가 없어 실패했을 때 모두** 행을 남긴다 —
  앞의 것은 "창이 충분했다", 뒤의 것은 "창이 짧았다"의 증거다. 훅 포착마다 활동량 행
  (`hook:shadow`)이 하나 남는다 — 그 행은 세션 이벤트 둘을 낸 **뒤에** 쓴다. 셋이 훅의
  2000 ms 예산을 나눠 쓰는데 쪼들릴 때 굶어야 하는 쪽은 모델이 보는 산출이 아니라 진단이기
  때문이고, 대가는 **바쁠 때 그 활동량이 과소 계상된다**는 것이다. 나이는 회수 시점에 박고
  시계·범위는 D67 퍼지와 같은
  `max(sources.indexed_at)` / `content_hash`이므로, 아티팩트가 지워진 뒤에도 분포가 남는다.
  `shadow_owned`는 **그 회수가 퍼지 대상 아티팩트였는지**를 같은 순간에 박는다 — explicit
  소스가 붙은 아티팩트는 창이 아예 건드리지 않아 그 회수 나이가 창의 길이에 답하지 않는다.
  나이를 못 읽은 해소는 0이 아니라 **NULL**로 남아 분위수에서 빠진다(같은 초에 회수한 행과
  섞이면 분포가 내려간다).
- `stats`에 `fetch` 줄이 하나 붙는다:
  `calls=N legacy=N legacy_after_migrate=N resolved=N resolved_artifacts=N missed=N shadow_rows=N shadow_artifacts=N age_s p50=N p90=N max=N`.
  칸마다 세는 것이 다르다 — `legacy`는 이관 전 행(해소에도 미해소에도 안 든다),
  `shadow_rows`는 나이 분위수가 선 모집단의 행 수(`resolved`의 **부분집합**이지 더하는 수가
  아니다), `shadow_artifacts`는 그 모집단의 distinct artifact 수다. **행을 세는 칸 바로 옆에 그
  부분집합을 놓는 것**이 배치 규칙이다: `ctr_fetch`가 기본 16 KiB까지만 돌려주므로 아티팩트
  하나가 여러 행이 되는데, 둘을 나란히 봐야 그 집중이 보인다. **D104의 채택 문턱이 읽는 것은
  행이 아니라 `resolved_artifacts + missed`**이고, 나이 분위수 셋도 행이 아니라 **아티팩트당 가장
  늦은 회수** 하나씩의 분포다 — 행으로 재면 페이징 한 번이 문턱을 채우고 분위수를 아래로 끈다.
  `legacy_after_migrate`는 그 `legacy` 중 **이관 뒤에 쓰인** 행이다(아래 "업그레이드 주의" 3).
- **못 잰 수는 0으로 찍지 않는다.** `ledger.db`는 스키마 버전을 두지 않는 best-effort 보조 DB라
  세 `ALTER`가 아직 안 돈 원장을 `stats`가 먼저 만나는 상태가 정상적으로 도달 가능하고, 그때의
  0은 측정값이 아니다. 그런 칸은 **`미이관`**(그 열이 아직 없다 — 새 바이너리로 스토어를 한 번
  열면 채워진다) 또는 **`없음`**(잴 것이 아직 없다 — 원장 파일 자체가 없거나 읽지 못했다 /
  분위수 표본이 0건)으로 찍힌다. 숫자가
  아닌 그 칸은 **0이 아니며, D104의 착수 조건을 충족시키지도 불충족시키지도 않는다.**
  `doctor [14]`가 `free`/`live`에서 쓰는 `측정실패`와 말이 다른 이유는 여기서 **실패한 것이
  없기** 때문이다 — 업그레이드 전의 정상 상태다.
- **원장 이관이 조건부가 되고 흔적을 남긴다.** 세 `ALTER`는 **없는 열에만** 걸리고(전에는
  조건 없이 던져 하루 약 295회의 훅 포착마다 `duplicate column name`을 냈다), 실패는 열마다
  경고 한 줄로 남으며, 스키마를 못 읽으면 아무것도 던지지 않는다. 열을 실제로 붙였을 때만
  `ledger.db`의 `user_version`에 **이관 워터마크**를 한 번 찍는다 — 그 값이
  `legacy_after_migrate`가 서는 앵커다.
- **`stats`는 원장을 못 읽어도 죽지 않는다.** `ledger.db`는 있는데 `ledger` 테이블이 없는
  상태에서 전에는 **헤더 한 줄도 안 찍히고** 명령이 끝났다. 이제는 표와 회수 줄을 찍되 못 읽은
  칸에 표식을 세우고 **종료 코드는 여전히 0이 아니다** — 출력이 나왔다고 성공이 아니라는 뜻이다.
  같은 이유로 `total` 행의 세 칸도 표식을 탈 수 있다: 집계가 실패했는데 0을 찍으면 "못 읽은
  원장"이 "호출 0회"로 읽힌다. **원장 파일이 아예 없는 정상 상태에서는 그 칸이 숫자 0으로
  남으므로**, `total`이 파일 부재와 읽기 실패를 가르는 자리다.

### Changed

- **`stats` 표의 `ctr_fetch` calls가 뜻을 바꾼다.** 전에는 성공한 회수만 셌고, 이제
  **성공 + artifact 부재**를 센다(잘못된 선택자나 잘못된 chunk id는 여전히 세지 않는다 —
  그것들은 창의 길이에 대해 아무 말도 하지 않는다). 같은 원장의 옛 행은 그대로 남고 새 세
  열이 NULL이라 해소·미해소 어느 쪽에도 들지 않는다.
- **`stats` 표의 `total` 행이 `ctr_*` 도구만 합산한다.** 새로 생긴 `hook:shadow` 행은 하루
  약 295건이라 그대로 두면 총계를 지배하는데, **이 릴리스 전 원장의 도구는 전부 `ctr_*`였으므로
  이 제외는 총계의 뜻을 바꾸는 것이 아니라 유지한다.** `hook:shadow` 행 자체는 표에 그대로
  찍힌다 — 관측 채널은 잃지 않는다. 그 행이 세는 것은 **포착 사건**이지 저장된 객체가 아니다
  (내용 주소 중복 재포착도 한 행을 남긴다).

### Fixed

- **`content.db` DSN에 `journal_size_limit(33554432)`(32 MiB)를 추가한다** (D102). v0.19.1의
  "업그레이드 주의"가 적었듯, 병합(`optimize`)이 도는 동안 `content.db-wal`이 부풀고 이
  pragma가 없어 체크포인트가 WAL을 재사용할 뿐 줄이지 않아 그 몫이 서버가 살아 있는 내내
  디스크에 남았다. 32 MiB는 **바닥이지 0이 아니다** — 절단은 체크포인트 호출 그 자체가
  아니라 그 뒤 빈 WAL에 첫 프레임을 쓰는 다음 커밋에서 일어난다(자기 치유이되 즉시는
  아니다). 병합 뒤 `Store.CheckpointTruncate`를 직접 부르는 안은 기각했다 — 열린 reader와
  공존하는 TRUNCATE 체크포인트는 `busy_timeout`(5000 ms) 소진까지 최대 약 5초 쓰기 락을
  늘리는데, 그 창을 줄이는 것이 D102 계약 9의 목적이기 때문이다.

### Unchanged (의도)

- **병합 ctx에는 여전히 deadline을 두지 않는다** (D102). v0.19.1의 "업그레이드 주의"가 이미
  "deadline은 없고 있어서도 안 된다"고 적었다 — `optimize`가 단일 암시 트랜잭션이라
  중단되면 통째 롤백돼 매번 진전 0으로 끝나기 때문이다. 이번 배포 뒤 실측이 그 판단을 코드
  변경 없이 수로 확정한다: 가득 찬 backlog에 리더 12개가 동시 접속한 상태의 첫 병합이 최악
  **7.4166838s**, 이미 병합된 인덱스에 건 재실행은 **643.1µs**다 — 전자는 그동안 쌓인
  세그먼트를 한 번에 치우는 일회성 비용이지 정상상태의 하루치 재작성 비용이 아니며, 그
  정상상태는 여전히 미실측이다. 같은 병합에서 `content.db-wal` 피크가
  **100,812,312 B(96.1 MiB)**로 잡혔고, `doctor [14]`의 live 축은 병합 전
  **592,732,160 B**에서 병합 후 **160,727,040 B**로(−72.9%) 떨어진 반면 file 축은 병합
  전후 모두 경고 상태로 남았다 — 죽은 신호가 아니라 자동 경로가 VACUUM을 하지 않아(D102
  계약 5) 남는 회수 가능 페이지를 그대로 보고하는 것이다.
- **72시간 보존 창의 기본값은 그대로다** (D104). 늘리거나 줄이는 판단은 이 계측이 도는
  **14일 뒤**, 그리고 회수 실적 줄의 **`resolved_artifacts + missed` 10건 이상 +
  (`shadow_artifacts` 30건 이상 또는 `missed` 5건 이상)**이 찼을 때 한다. 설계서에 판정 규칙을
  미리 박아 뒀다.
  **미달이면 "늘리지 않는다"가 결론이되, 그것은 보류이지 확정이 아니다** — 72시간을 넘겨
  해소된 회수가 원장에 하나라도 있으면(`age_s max` > 259,200) 그것은 "신호 없음"이 아니라
  **창이 짧았다는 직접 증거**라서, 미달이어도 질문을 닫지 않는다. **측정을 실제로 시작하려면
  아래 "업그레이드 주의"의 셋을 해야 한다.**

### 업그레이드 주의 — 측정 구간을 시작하려면 세 가지를 해야 한다

이 릴리스는 눈금을 만들 뿐 스스로 재기 시작하지 않는다. 아래 셋을 하지 않으면 **14일이 흘러도
원장에 아무것도 쌓이지 않고**, D104의 착수 조건은 판정 불가 상태로 남는다. 앞의 둘은 시작하기
위한 것이고, 셋째는 **시작한 줄 알았는데 아니었던 경우**를 잡는다.

1. **설치본을 새 바이너리로 간다 — 이것이 첫 걸음이다.** 원장에 열 셋을 붙이는 `ALTER`는
   스토어를 **writable로 열 때만** 돌고 `stats`는 원장을 read-only로만 여므로, 옛 바이너리가
   계속 서빙하는 동안 회수 줄은 `calls`만 숫자이고 나머지는 `미이관`으로 남는다(2026-08-10
   실측에서 실제로 그 모양이었다). **그 상태로 흐르는 14일은 아무것도 재지 않는다.**
2. **측정 구간 동안 창을 넓혀 띄운다 — `CTR_SHADOW_RETENTION=336h`(14일).** 이것은 선택이
   아니라 **측정의 전제**다: 72시간에서 잘린 나이 분포로는 "72시간이 짧은가"에 구조적으로 답할
   수 없다(성공 나이가 72시간을 넘을 수 없으므로 규칙이 창을 유지하거나 줄일 수만 있다).
   **그 구간 내내 `doctor [14]`의 live 축 경고가 뜨는 것이 정상이다** — 보관량이 72시간분의
   약 4.7배(약 800 MB 추정)로 올라 임계 256 MiB를 넘기 때문이고, 경고가 그 상태를 정확히
   보고하는 것이 그 축을 그 값으로 잡은 이유다.
   **절단선은 즉시 336시간으로 올라가지 않는다**: 변수를 걸기 전 마지막 기동의 퍼지가 이미
   72시간을 넘는 것을 다 지웠으므로 경과 t에서 관측 가능한 최대 나이는 `72h + t`이고,
   336시간에 닿는 것은 구간의 마지막 사흘뿐이다 — 그래서 이 구간의 p90은 **하한**으로 읽는다.

3. **경보 칸을 한 번 본다 — `legacy_after_migrate`가 0이 아니면 서버를 다시 띄운다.** 세션이
   열린 채 새 바이너리를 깔면 훅이 원장을 이관하는 동안 **옛 서버가 `ctr_fetch`의 유일한
   기록자**로 남는다. 그 행들은 레거시로 읽히므로 **표식은 하나도 서지 않고**(열은 다 있다)
   나머지 칸이 전부 숫자인데, 해소도 미해소도 0이라 결정표가 "채택의 문제"로 오독한다 — 할 일은
   채택을 늘리는 것이 아니라 **서버를 다시 띄우는 것**이다. 설계서 결정표 **행 0b**가 이 상태만
   받는다.
   **이 수는 누적이고 지워지지 않는다**: 서버를 다시 띄워도 더 늘지 않을 뿐 0으로 돌아가지
   않는다(그 구간의 회수는 실제로 안 재졌으므로 그것이 옳다). **내일 같은 수를 다시 보고
   "안 고쳐졌다"로 읽으면 안 된다 — 늘어나는지를 봐야 한다.**
   v0.19.1로 되돌린 뒤에 이 경보가 뜨는 것도 정확한 진술이다.

**구간이 끝나면 변수를 지우는가**: **판정이 질문을 닫았을 때만이다.** 판정이 미달로 끝나는
경우(설계서 결정표의 행 0·0b·2·3·3b — 첫 구간에서는 그쪽이 더 그럴듯하다) **변수를 지우면 다음
구간의 나이 관측이 다시 72시간에서 잘려 열어 둔 질문을 영영 답할 수 없다.** 유지하면 약
800 MB와 상시 경고가 계속되고, 지우면 다음 구간이 절단된다 — **양쪽 다 대가가 있어 설계서가
이것을 소유자 판단으로 남겼다**(D104 판정 규칙 끝의 ★, 무행동의 기본값은 "유지"다). 질문이
닫힌 뒤에는 변수를 지우고, 그 다음 기동의 퍼지가 초과분을 걷는다.

### Fixed — 문서

- **0.19.0 Removed 절의 Codex 훅 신뢰 문장을 정정한다.** 그 절은 *"Codex에서는 훅 신뢰를 준
  뒤부터다"* 라고만 적었는데, 이는 **플러그인으로 설치한 사용자에게 할 일이 남아 있는 것처럼
  읽힌다 — 남아 있지 않다.** 태그 이후의 실측으로 `codex plugin add`가 우리 훅 파일의
  항목별 `trusted_hash`를 `config.toml`에 함께 기록하는 것이 확인됐다(설치 직전
  `[hooks.state.*]` 15개·우리 것 0 → 직후 18개·우리 것 3). 즉 **신뢰를 주는 것은 설치이고,
  별도 승인 걸음은 없다.** 그 절이 인용한 부정 결과(빈 store로 끝난 프로브)와 어긋나지
  않는다 — 그 프로브는 마켓플레이스·플러그인 등록 없이 훅 파일만 놓아 `hooks.state` 항목이
  아예 생기지 않은 경우였다. 옛 방식으로 훅 파일을 직접 놓은 사용자에게만 원래 문장이
  적용된다. 근거는 설계서 §5-1·§5-8. **태그된 0.19.0 절은 고치지 않았다** — 릴리스 노트는
  배송 시점의 기록이고, 정정은 이 항목의 자리다.

## [0.19.1] — 2026-08-09

FTS5 세그먼트가 병합되지 않아 쌓인 저장 파일 증가분을 회수하고, `doctor [14]`의 판정 눈금을
**둘로 나눈다** — 그 결함이 실제로 움직이는 값(live 바이트)과 디스크 총점유(파일 축)다(D102).
코드가 새로 하는 일은 병합 하나이고, 나머지 변경은 전부 그 병합에 맞춰 관측 눈금을 다시 그은
것이다.

### Fixed

- **FTS 세그먼트가 병합되지 않아 저장 파일의 76%가 회수되지 않고 쌓였다** (D102). 외부 콘텐츠
  FTS5의 삭제는 tombstone을 새 세그먼트에 쌓을 뿐이고, 이 저장소에는 병합을 부르는 코드가
  없었다 — D67의 72시간 퍼지가 지운 몫이 파일에서 돌아오지 않았다. 실측: 파일
  709,890,048 B 중 539,099,136 B가 회수 가능분이었고, `optimize`+VACUUM 뒤 170,790,912 B가
  됐다(`optimize` 단독은 파일 크기를 줄이지 않는다 — live 페이지만 준다). **서버가 사는 동안
  도는 고루틴이 하루 한 번 병합**하고, 수동 회수 경로 둘은 VACUUM 앞에 병합한다. 정상 종료로
  진행 중이던 병합이 취소되면 `WARN`이 아니라 `DEBUG`로 남고(깨끗한 종료가 "병합 실패"로
  보이지 않는다), 완료 로그는 소요 시간(`took`)을 싣는다.
- **`purge --vacuum`·`--hook-only`가 회수 가능분의 약 30%만 되돌리던 것**을 고쳤다 — VACUUM은
  free page만 되돌리고 tombstone은 병합해야 free page가 된다. 병합 실패는 이제 **명령의 종료
  상태에 반영된다**(VACUUM은 그대로 진행한다 — 부분 회수가 무회수보다 낫다).

### Changed

- `doctor [14]`의 **경고가 둘로 갈린다 — `live` 축과 `file` 축이고, 둘 다 뜰 수 있다.** 서로
  다른 질문에 답하기 때문이다.
  - **`live` 축 — "쓰레기가 쌓였나"**(신규 키 `CTR_CONTENT_LIVE_WARN_BYTES`, 기본 256 MiB).
    판정값은 `(page_count-freelist_count) × page_size`이고 **셋 다 한 스냅샷(한 문장)에서**
    읽는다. 자동 경로가 VACUUM을 하지 않아 파일은 고수위에 머물므로 파일 기준 임계는 정리
    뒤에도 상시 초과였다 — 15일간 죽은 신호였던 이유다. **`os.Stat`의 파일 크기를 섞어 빼지
    않는다**: `PRAGMA freelist_count`는 WAL 프레임까지 반영한 커밋 스냅샷이라, 병합 직후처럼
    체크포인트가 없는 구간에서는 `file-free`가 음수가 되고 0으로 클램프하면 **스토어가 가장 클
    때 이 경고가 영영 안 뜬다.** 이 축을 줄이는 것은 VACUUM이 아니라 삭제+병합이라, 문면에
    훅 아티팩트 보존 창의 실효값을 함께 적는다.
  - **`file` 축 — "디스크를 얼마나 먹나"**(기존 키 `CTR_CONTENT_FILE_WARN_BYTES`, 기본
    **100 MiB → 512 MiB**). 재는 값이 `content.db` 본체가 아니라 **총점유(본체+`-wal`+`-shm`)로
    확정**됐다 — 아래 "업그레이드 주의"가 적은 `-wal` 팽창도 이 축이 덮는다. 512 MiB는 정리 후
    정상상태(본체 실측 171 MB + 그날 병합이 다시 쓰는 몫)에서는 조용하고, 실측된 부푼
    상태(본체만 709,890,048 B)에서는 발화하도록 고른 값이다. 이 축의 구제 경로가 VACUUM이다.
- **`[14]` 정보 줄에 두 판정값을 병기한다**: `file=<총점유>B(db+wal+shm) free=<n>B live=<n>B` —
  경고가 없는 상태에서 판정을 재현하려고 손으로 빼지 않아도 된다. `free`는 **live 산출에서
  차감할 뿐 독립된 경고 신호로 쓰지 않는다**(병합되지 않은 세그먼트는 free page가 아니라 live
  page라 freelist는 결함이 있을 때 오히려 낮게 읽힌다) — 대신 `file` 축 경고가 "VACUUM이 되돌릴
  몫"으로 그 값을 인용한다. pragma를 못 읽으면 두 값은 `0`이 아니라 `측정실패`로 찍히고 live 축
  판정을 하지 않는다.
- `doctor [14]` 경고 문면을 고친다. *"자동 삭제 없음"* → *"자동 VACUUM 없음"*(바로 위에서
  보고하는 `artifacts=`는 D67 퍼지로 사용자 조작 없이 줄어든다 — 옛 문면은 평문으로 읽으면
  어긋났다). 이 문장은 VACUUM이 구제 경로인 **`file` 축** 문면에 있다. **훅 아티팩트 보존 창의
  실효값**은 삭제가 움직이는 **`live` 축** 문면에 적는다 — 기본은 72시간이지만
  `CTR_SHADOW_RETENTION`이 걸리면 그 값이 찍힌다(해석기를 `internal/store`로 옮겨 `doctor`가
  인쇄하는 값과 실제 적용 값이 갈라지지 않게 했다 — D13).
  **blob 축**(`CTR_STORE_WARN_BYTES`)의 *"자동 삭제 없음"*도 고친다 — D67 기동
  퍼지가 shadow 귀속 blob을 보존 창 밖에서 자동 회수하고, 그 퍼지가 지우는 것이 바로 이 축이
  합산하는 물리 CAS 파일이다. 이제 그 사실을 적고, *"자동 삭제 없음"*은 explicit 소스로
  범위가 좁혀졌다.

### 업그레이드 주의 — 첫 기동의 일회성 대가

**이 버전으로 처음 뜨는 서버는 기동 30초 뒤에 그동안 쌓인 세그먼트 전부를 한 번에 병합한다.**
도그푸딩 저장소 기준으로 그 몫이 약 490 MB였다. 정상상태의 하루치 병합은 쓰기 락을 약 1.2초
잡지만 **이 첫 회는 그보다 훨씬 오래 잡는다** — 그동안 이 프로세스의 모든 쓰기가 줄을 서고
(`writer`가 `SetMaxOpenConns(1)`), **그 사이에 도착한 훅은 총예산 2000 ms 안에 락을 잡지 못해
포착이 버려질 수 있다.** 버려진 것은 `doctor [12]`의 `shadow-store` drop으로 남으니 사후에
확인할 수 있다. 이 자리에 deadline은 없고 있어서도 안 된다 — `optimize`는 단일 암시
트랜잭션이라 중단되면 통째 롤백돼 매번 0의 진전을 낸다.

**통제된 창에서 미리 치우려면**, 업그레이드 직후 **서버를 내린 상태로**
`purge --project <id> --older-than <기간> --vacuum`(shadow만 치울 거면
`purge --project <id> --hook-only` — 이쪽은 상시 VACUUM이라 `--vacuum`을 붙이면 거부된다)을
**한 번** 돌리면 된다. 두 경로 모두 VACUUM 앞에서
병합하므로, 그 뒤에 뜬 서버의 첫 자동 병합은 이미 병합된 인덱스에 걸려 일 없이 반환한다.

**그리고 병합이 도는 동안 `content.db-wal`이 병합 크기만큼 커진다.** DSN에
`journal_size_limit`이 없어 번들 SQLite 기본 −1이 걸리고, 그러면 체크포인트는 WAL을 *재사용*할
뿐 파일을 줄이지 않는다 — 실제로 접히는 것은 서버가 내려갈 때(`Store.Close()`의
`wal_checkpoint(TRUNCATE)`)이므로 **그 몫은 세션 내내 디스크에 남는다.** `doctor [14]`의
`file=`이 총점유(본체+`-wal`+`-shm`)라 **이 증가분도 거기에 잡히고**, 그래서 `file` 축 경고가
이 상태를 보고한다. 매일 전체 인덱스를 다시 쓰는 것이라 첫 실행만의 성질도 아니다 — 디스크
여유는 `content.db` 크기만큼을 더 본다.

## [0.19.0] — 2026-08-08

호스트 설정 파일에 등록물을 손으로 써 넣는 일을 그만둔다(D96–D101). Claude Code와 Codex가
플러그인 매니페스트를 읽어 MCP 서버를 스스로 등록하고, 이 프로그램이 호스트 설정 파일에 쓰는
자리는 `hook uninstall`이 자기 훅 그룹을 지우는 방향 하나뿐이다(D96 계약 1의 유일한 예외 —
아래 Changed 참조). 열여덟 릴리스에 걸쳐 손편집 등록이 낸 파일 파괴 결함 다섯이 그 기입
경로와 함께 사라진다(v0.18 §3.5·§3.7·§3.8).

### Added

- **`CTR_ENABLE` 환경 변수**(D101) — `--enable`과 같은 쉼표 목록(`ingest`/`net`/`exec`)을
  받는다. 값 전체가 `none`이면 opt-in을 0개로 낮춘다(search/fetch/transform만 남는 자세) —
  `none`은 값 전체일 때만 유효하고 다른 이름과 섞으면 오류다. 우선순위는 `--enable` 플래그 >
  `CTR_ENABLE` > 내장 기본값 `ingest,net` 순이다. 플러그인 매니페스트는 `args`를 고정하지
  않으므로, 플러그인으로 설치한 서버의 프로필을 바꾸는 방법은 이 환경 변수뿐이다.
- **`doctor`가 옛 등록물을 순수 줄 스캐너로 찾는다**(`internal/cli/codex_scan.go`) —
  `codex mcp get`은 무효 TOML에서 실패하고 `--json`은 자격증명을 평문으로 내므로 둘 다
  쓰지 않는다(설계서 §5-13).

### Changed — 사용자 관측 가능한 동작 변경

- **도구 이름 접두가 `mcp__ctr-exec__`에서 `mcp__plugin_context-router_ctr__`로 바뀐다**
  (D98). 손으로 붙여 넣은 권한 규칙(`.claude/settings.json`의 `permissions.ask`/`allow`)은
  옛 접두를 참조하므로 더는 매치하지 않는다 — 이 프로그램은 권한 규칙을 쓰지도 고치지도
  않으므로(D96 계약 1) 새 접두로 직접 옮겨야 한다. 같은 파일을 만지는 `hook uninstall`도
  훅 그룹만 건드리고 `permissions`는 지나친다.
- **`hook uninstall`의 범위가 훅 그룹 제거 하나로 좁아진다** — `.claude/settings.json`과
  Codex `hooks.json`의 우리 훅 그룹만 지운다. `.mcp.json` 항목·MCP 승인 키·`config.toml`
  관리 블록 제거는 더 이상 하지 않는다 — 그 자리는 호스트 자신의 `mcp remove`가 맡는다.
- **Claude Code의 플러그인 설치·삭제는 `settings.json`을 통째로 재직렬화한다** — 사용자
  값은 보존되지만 들여쓰기·줄바꿈·키 순서가 바뀌고, `plugin uninstall`은 빈
  `"enabledPlugins": {}`를 남겨 왕복이 원래 바이트로 돌아오지 않는다. `settings.json`에
  주석이 있으면 `plugin marketplace add`가 `Invalid JSON syntax`로 실패하고 파일은 손대지
  않는다 — Claude의 설정 파일은 JSONC가 아니라 엄격 JSON이다. Codex의 같은 왕복은
  `config.toml`을 원본과 바이트 단위로 동일하게 되돌린다(설계서 §5-10).
- **인자·환경 변수 없이 맨 `context-router`를 실행하면 기본 프로필 `ingest,net`이 켜진다**
  (D101) — 이전에는 `--enable`/`CTR_ENABLE` 없이는 `ctr_index`·`ctr_fetch_and_index` 둘 다
  꺼져 있었다. `net`은 아웃바운드 HTTP를 여는 프로필이다. 플러그인 매니페스트도 `args`를
  넘기지 않으므로, 플러그인으로 설치한 서버가 켜는 프로필이 바로 이 기본값이다.

### Removed

- **`hook install`이 사라진다.** 실행하면 파일을 하나도 만들지 않고 플러그인 설치 절차를
  안내한 뒤 종료 코드 1로 끝난다. 등록은 `claude plugin install` / `codex plugin add`가
  한다(D96·D97). `.mcp.json`·`config.toml` 기입 코드 전부(`codex_toml.go` 포함)가 함께
  사라진다. 훅 정의 자체는 플러그인 매니페스트(`hooks/hooks.json` ·
  `hooks/codex-hooks.json`)가 나르므로 수동 포착은 Claude Code에서 플러그인 설치만으로 그대로
  돈다. **Codex에서는 훅 신뢰를 준 뒤부터다** — Codex의 훅은 지속되는 신뢰를 요구하고
  (`codex --help`의 `--dangerously-bypass-hook-trust`), 신뢰를 주지 않은 홈에서 돌린 프로브는
  `SessionStart`를 포함해 훅이 하나도 발화하지 않았다(설계서 §5-8). 옛 설치기가 남긴 훅 그룹은
  `hook uninstall`로 지운다 — 매니페스트 훅과 겹쳐 두면 같은 포착이 두 번 일어나고,
  `doctor [9]`·`[16]`이 그 잔존을 짚는다.
- **위 두 문장이 닿지 않던 형태 하나에 이제 진단이 닿는다**: 옛 훅 그룹에 **사용자가 자기
  항목을 더해 놓았으면** `hook uninstall`은 여전히 그 그룹을 그대로 둔다(혼합 그룹 불가침 —
  남의 항목을 함께 지우는 것보다 우리 항목이 남는 편을 고른다). `doctor`도 같은 소유 판정을
  쓰므로 `[9]`·`[16]`의 제거 가능 개수는 그 그룹을 `옛 그룹 없음`으로 센다 — 다만 이제 그
  뒤에 **동거 부류를 따로 세어** 병기한다: `사용자 항목과 함께 있는 우리 항목 N그룹 — 호스트의
  /hooks에서 그 항목을 지우세요(플러그인 훅과 겹쳐 같은 포착이 두 번 일어납니다)`. 정리
  목적지는 `hook uninstall`이 아니라 호스트의 `/hooks`다 — 그 명령으로는 지워지지 않는다.
  Codex 쪽은 v0.4부터 혼합 그룹을 불가침으로 다뤘고 이번 릴리스에서 Claude 쪽 판정이
  같아졌다.

## [0.18.0] — 2026-08-06

v0.17이 "뿌리 A"라 부른 것 — **판독기들이 같은 바이트를 서로 다르게 본다는 사실 자체** —
를 닫는다. 인라인 `env` 테이블의 키·값 경계를 찾는 걸음이 자리마다 따로 있었고, 판독과
되쓰기가 어긋나는 자리마다 조용한 덮어쓰기나 영구 무변경이 하나씩 서 있었다. 이제 한 패스
열거형 하나가 그 경계를 정하고 소비자 여섯이 그 하나를 지난다(D92). 함께 `hook install`이
`.mcp.json`을 실행마다 되쓰던 것을 멈춘다.

**판독이 통일되면서 소유 판정이 움직인 파일 형태가 여럿이다** — 0.17.2가 손대지 않던
등록물을 이제 관리하고, 되쓰던 등록물을 이제 남긴다. 아래 Changed가 그 집합이고, 설계서
§0의 재기준선 표(열여섯 행)가 전/후를 형태별로 적는다.

### Changed — 사용자 관측 가능한 동작 변경

0.17.2에서 올라오는 사용자가 자기 `~/.codex/config.toml`에서 실제로 겪는 차이다 — 앞의 셋은
판정과 산출이 움직인 것이고, 마지막 하나는 판정은 그대로인 채 문면만 넓어진 것이다.

- **인라인 `env`의 둘째 줄 이하에 표식이 앉아 있어도 그 등록물은 우리 것이다 — `command`가
  남의 값이어도 그렇다** (D92 — 설계서 §0 재기준선 행 15). 0.17.2는 같은 파일을 손대지
  않았다: `install`은 `표식도 command도 우리 것이 아닌 [mcp_servers.ctr] 감지 — 기입 생략`을
  냈고 `uninstall`은 "사용자 등록으로 보고 건드리지 않았습니다"였다. 이제 `install`이
  **`command`를 우리 것으로 되쓰고** `enabled_tools`를 넣고 표식을 갱신하며,
  **`hook uninstall --codex`가 그 테이블을 통째로 지운다.** 소유는 D84의 세 절
  disjunction이고 그 첫 절이 표식 값 하나다(D82) — `command` 절은 그것과 **또는**로 묶이므로
  남의 `command`는 첫 절의 반증이 아니다. 0.17.2가 같은 계약 아래 두 파일을 다르게 판정한
  것은 계약이 아니라 판독기가 물리 라인 하나만 보던 결함이었다(실측: 같은 파일에서 표식을
  엔트리 첫 물리 라인으로 옮기면 0.17.2도 기입하고 제거한다). **표식 줄을 남긴 채 `command`를
  손으로 고쳐 둔 사용자는 그 편집이 되돌려지는 것을 보고, `uninstall`은 0.17.2가 남겨 두던
  테이블을 지운다.** 관리에서 빼려면 표식 키(`CTR_MANAGED`) 자체를 지워라.
- **따옴표를 포함한 키 이름을 TOML이 읽는 대로 읽는다 — 소유가 두 방향으로 움직인다**
  (D92 — 설계서 §0 재기준선 행 9·10). `'"env"'.CTR_MANAGED`나 `env.'"CTR_MANAGED"'`가 TOML
  에서 갖는 키 이름에는 **따옴표가 들어 있어** 우리 `env`도 우리 표식 키도 아닌데, 0.17.2는
  따옴표를 벗기고 그 사용자 값을 우리 표식으로 읽었다.
  - 남의 `command`에 그런 키가 현재 표식 값을 담고 있으면 0.17.2는 **남의 테이블을 되썼고
    `uninstall`이 그것을 지웠다.** 이제 `install`은 기입을 생략하고 `uninstall`은 "사용자
    등록"으로 남긴다. **이 릴리스가 고친 것 중 사용자 파일에 가장 위험했던 자리다.**
  - 반대로 우리 `command` + `env.'"CTR_MANAGED"' = "<현재 표식>"이면 표식은 **없고**, 점 표기
    `env.` 키가 실재해 헤더를 붙일 자리도 없다. 0.17.2는 기입했고 이제 무변경 + 사유다.
- **구간의 끝이나 그 안의 구조를 확정할 수 없으면 되쓰지 않는다** (D92 계약 4·7 — 설계서
  §0 재기준선 행 11~13). 0.17.2는 아래 세 형태에 모두 기입했다.
  - `env = { CTR_MANAGED = "…", A = }`처럼 중괄호는 열렸는데 그 안을 확정하지 못하는 형태
    에서 표식의 유무는 **모르는** 것이지 없는 것이 아니다. `install`·`uninstall` 둘 다
    무변경 + 사유로 빠진다.
  - 같은 형태에 `command`까지 남의 것이면 0.17.2는 `mcpExistingHeader`로 **MCP를 확정해
    가드를 등록**했다. 이제 MCP 미확정이므로 **가드 등록도 함께 빠진다.**
  - 인라인 `env`가 닫히지 않은 채 뒤따르는 `[other]` 헤더가 그 값 안으로 들어가는 형태에서
    0.17.2는 기입하면서 사용자 테이블을 바이트째 보존했고 `uninstall`은 우리 두 줄만 지웠다.
    이제 둘 다 무변경 + 사유다. **무변경은 0.17.2보다 좁다** — 그래도 끝을 확정할 수 없는
    구간을 되쓰지 않는다는 규칙이 이긴다.
- **사유 문면 둘이 넓어졌다.** 닫히지 않음 사유는 "파일 끝에서 여러 줄 문자열 또는 배열"만
  말했으나 이제 인라인 테이블과 **다음 테이블 헤더 앞**이라는 자리를 함께 댄다. 점 표기
  사유는 `env.NAME`만 말했으나 인라인 `env` 안과 `[mcp_servers.ctr.env]` 테이블 안의
  `CTR_MANAGED.NAME`을 함께 댄다. **사유가 대지 않는 형태의 사용자는 자기 파일에 없는
  문자열을 찾다가 install이 왜 영구 무변경인지 끝내 알 수 없다** — 이 릴리스가 새로 내는
  "env가 인라인 테이블이 아니다" 사유도 그래서 **닿는 두 부류 모두에 참인** 문면으로 쓴다
  (우변이 애초에 테이블이 아닌 것 · 중괄호는 열렸으나 구조를 읽지 못한 것). 뒤엣것에
  "인라인 테이블로 고치라"만 내면 사용자는 이미 `env = { … }`인 자기 파일을 보고 고칠 것을
  찾지 못한 채 제거가 끝난 줄 안다.

### Fixed

- **중첩 인라인 안쪽의 `CTR_MANAGED`를 우리 표식으로 읽어 사용자 값을 덮어쓰던 것을
  멈춘다** (D92 — 설계서 §0 재기준선 행 1).
  `env = { A = { CTR_MANAGED = "inner" }, B = "1" }`에서 안쪽 마디를 바깥 표식으로 읽었고,
  **산출이 유효 TOML이라 게이트가 통과시켰으며 doctor도 아무 말을 하지 않았다** — 사용자는
  자기 값이 바뀐 것을 알 수 없었다. 이제 표식 부재로 읽고 삽입한다.
- **인라인 `env`의 문자열 값 *안*에 표식 모양이 있으면 표식이 영영 서지 않던 것을 고친다**
  (D92 — 설계서 §0 재기준선 행 7). `env = { A = "x, CTR_MANAGED = 0, y" }`에서 판독은 그
  모양을 키가 있는 것으로 읽고 되쓰기는 자리를 찾지 못해 포기했다 — 두 판독기가 같은
  바이트를 다르게 본 그 자체다.
  그 파일은 install을 몇 번 돌려도 표식이 서지 않았고 `doctor`가 계속 표식없음을 냈다.
  이제 양쪽이 "부재"로 일치해 삽입되며, **사용자 값 바이트는 하나도 바뀌지 않는다.**
- **인라인 `env`의 문자열 값 *안*에 든 키 모양을 "이스케이프 표기 키"로 오탐해 얼어붙던 것을
  고친다** (D92 — 설계서 §0 재기준선 행 14). `env = { A = ', "C:\t" = 2', CTR_MANAGED = "…" }`
  는 유효한 TOML이고 그 키 모양은 문자열 값 **안**에 사는 내용이다. D87의 이스케이프 키 검사가
  그것을 우리 구간의 키로 읽어 `install`·`uninstall`·`doctor --fix`가 **셋 다** 영구 무변경
  이었고, 사유가 대는 이스케이프 키는 그 파일에 존재하지 않아 사용자가 고칠 것을 찾을 수
  없었다. 이제 키 검사가 문자열 값을 건너뛰므로 평소대로 기입한다.
- **여러 줄로 이어진 인라인 `env`를 다룬다** (D92 — 설계서 §0 재기준선 행 6). 되쓰기 스팬이
  물리 라인 하나에 갇혀 있어, 표식이 두 줄에 걸치면 갱신을 포기하고 표식이 없으면 삽입도
  하지 못했다.
  스캐너가 `{`·`}`를 세게 되어 그 형태가 한 논리 엔트리로 묶이고, 치환이 파일 좌표로
  일어나 갱신·삽입 두 갈래가 모두 제자리다. 줄 종결자(CRLF 포함)는 원문 그대로 옮긴다.
- **따옴표 안에 공백이 있는 남의 키를 우리 `env`로 읽던 것을 멈춘다** (D92 — v0.17 §1.2
  잔여 ② · 설계서 §0 재기준선 행 3). 키 판독이 `strings.Fields` 정규화를 지나
  `"e n v" = { … }`가 `env`로 읽혔고, **남의 테이블에 우리 표식이 섰다.** 판독을 원문
  좌변으로 옮기고 문자열 밖 공백만 무시한다.
- **남의 키 하나에 우리 등록물의 install이 영구히 얼어붙던 것을 고친다** (D92 — 설계서 §0
  재기준선 행 8). `env = { '"CTR_MANAGED"'.x = 1, … }`의 키 이름은 TOML이 **따옴표를 포함해**
  읽으므로 우리 표식 키가 아닌데, 판정이 따옴표를 벗기고 그것을 우리 점 표기 표식 키로 읽어
  차단을 세웠다 — 우리 `command`가 박힌 정상 등록물에 `install`이 무변경으로 이탈했고, MCP가
  확정되지 않아 가드 등록도 빠졌으며, 사유는 그 파일에 없는 점 표기 `env`를 대고 있었다.
  키 벗기기가 한 자리(`tomlUnquoteKey`)를 지나 판독기와 판정이 같은 바이트를 같게 읽는다.
- **점 표기 표식 키가 "우리 결함" 보고로 새어 나오던 것을 고친다 — 인라인과
  `[mcp_servers.ctr.env]` 헤더 두 형태 모두** (D92 — 설계서 §0 재기준선 행 2·16).
  `env = { CTR_MANAGED.sub = "x" }`는 중첩이 아니라 깊이 1 마디라 깊이 한정이 걸러 내지
  못했고, 삽입이 같은 키를 두 번 만들어 게이트가 산출을 거부했다 — 사용자는 `--fix`가
  가리키는 곳 없이 `mcpOutputInvalid`만 받았다. 같은 차단이 `[mcp_servers.ctr.env]` 안의
  `CTR_MANAGED.sub = "x"`에는 걸려 있지 않아 그 형태도 같은 "제품 결함이니 알려 주세요"로
  굳어 있었는데, **하필 그것이 우리 설치기 자신이 기입하는 헤더 형태다** — 사용자는 할 일이
  없고 install은 영구 무변경이었다. 이제 두 형태를 술어 하나가 답하고, 무변경으로 멈추며
  **수행 가능한 사유**를 낸다.
- **`env` 우변이 인라인 테이블이 아니면 사유를 낸다** (D92 — 설계서 §0 재기준선 행 4).
  `env = []` · `env = "x"` · `env = 1`에서 헤더도 표식도 붙일 자리가 없는데 `install`은
  성공을 보고했고, 표식만 빠진 채 `command`·`args`·`enabled_tools`가 기입됐다. 이제
  무변경으로 멈추며, MCP가 확정되지 않으므로 **가드 등록도 함께 멈춘다.**
- **닫히지 않은 인라인 테이블에 사유가 붙는다** (D92 — 설계서 §0 재기준선 행 5). 여는 `{`가
  EOF까지 닫히지 않는 파일에 `install`이 성공을 보고했다. **그 형태에서는** 산출 바이트가
  전과 같고 보고만 바뀐다 — 여는 `{`가 뒤따르는 테이블 헤더를 삼키는 형태는 판정과 산출이
  함께 바뀐다(위 Changed). 사유 문면도 "여러 줄 문자열 또는 배열"에 인라인 테이블을
  더한다. **"그런 입력은 이미 무효 TOML"이라는 전제에는 기대지 않는다** — 그 전제를 깨는
  **유효한** 파일이 태그 직전 리뷰에서 실제로 나왔다(아래 여러 줄 문자열 항목). 뿌리를
  고쳐 그 반례는 사라졌지만,
  스캐너는 TOML 파서가 아니므로 전제 자체를 세우지 않는다.
- **여러 줄 문자열의 닫기가 TOML의 실제 규칙을 따른다.** 닫는 구분자는 "첫 삼중 따옴표"가
  아니라 **뒤에 같은 따옴표가 더 붙지 않은 첫 삼중 따옴표**다 — `"""x""""`는 내용이 따옴표
  하나인 8바이트 토큰이고 삼중 홑따옴표 갈래도 같다. 종전의 탐욕적 닫기는 따옴표 하나를
  문자열 **밖**에 남겨 한 줄 문자열을 열었고, 그 문자열이 줄 뒤쪽을 삼켰다. 이 릴리스가
  `{`·`}`를 세기 시작하면서 그 잔여가 두 경로로 자랐다 — **`uninstall`이 사용자
  `config.toml`을 통째로 비웠고**(삼켜진 닫는 중괄호로 깊이가 풀리지 않아 뒤따르는 사용자
  테이블이 테이블 경계로 잡히지 않는다. 짝 없는 중괄호가 깊이를 도로 내려 EOF 열림 감지도
  서지 않아 **사유 없이** 제거 완료를 보고했다), 유효한 파일이 "env가 인라인 테이블이
  아니다"로 굳어 `install`이 무변경이 됐다. 둘 다 태그 전에 닫았고, 실측으로 v0.17.2와
  같은 판정으로 돌아왔다.
- **`hook install`이 실행마다 `.mcp.json`을 되쓰던 것을 멈춘다.** 그 자리에 무변경 판정이
  없어 병합 결과가 디스크와 같아도 기입했다(`mergeMCPServers`의 `changed`를 버렸고, 그
  함수는 install에서 그 값을 참으로 고정한다). 이제 **바이트가 다를 때만** 쓰므로 2회차
  install이 진짜 무동작이고, 무변경 실행은 그렇다고 보고한다. 등록물에 프로필이 없을 때의
  안내는 기입·무변경 **두 갈래 모두**에서 나온다 — 기입 갈래에만 두면 정상 상태의 사용자가
  `ctr_index`·`ctr_fetch_and_index`가 꺼진 이유를 다시 듣지 못한다.

### 알려진 잔여 (이 릴리스가 닫지 않은 것)

- **여러 줄 배열이 뒤따르는 테이블 헤더를 삼키면 `hook uninstall --codex`가 그 사용자 테이블을
  우리 테이블과 함께 지운다 — 삼킨 것이 파일의 나머지 전부면 `config.toml`이 0바이트가 된다.**
  `args = ["a"`로 열린 배열의 다음 줄이 `[other]`이고 그 뒤 어딘가에서 `]`가 닫히는 형태다.
  실측: `[mcp_servers.ctr]`·`command`·`args = ["a"`⏎`[other]`⏎`uKEY = "uV"]`만 있는 파일에서
  uninstall 산출이 0바이트다. 닫는 `]` **밖**의 테이블은 앞뒤 모두 바이트째 남는다 — 손실은 그
  구간에 갇히지만, 그 구간이 파일 전부이면 남는 것이 없다. **사유는 나오지 않는다** — 제거 완료
  문면과 exit 0뿐이고, 되돌릴 수단은 그 실행이 남긴 `config.toml.bak` 단일 슬롯이 유일하다(D84).
  그런 입력은 우리 게이트에서 이미 무효 TOML이다(실측: `codexTOMLParses`가 false).
  **이 릴리스가 들인 것이 아니다** — base `128a727`과 v0.18이 같은 입력에서 `install`·
  `uninstall` 산출을 바이트째 같게 낸다(실측). 같은 부류의 **인라인 테이블** 형태는 이 릴리스가
  닫았지만(위 Changed) 배열은 열어 두었다: `["a"]`는 유효한 테이블 헤더이자 유효한 배열
  원소여서 TOML 문법으로 갈리지 않고, 같은 규칙을 배열에 걸면 `V4 = [`⏎`  ["a"]`⏎`]` 같은
  정상 파일이 얼어붙는다(실측: 유효 TOML이고 지금 기입한다). v0.19의 설계 항목이다(설계서 §4).
- **`.mcp.json`의 백업(D95)은 배송하지 않는다.** 계약(단일 슬롯 · 기입한 반환에서만)은
  옳지만 **슬롯의 자리**가 정해지지 않았다: `<projectRoot>/.mcp.json.bak`은 버전 관리
  아래에 떨어지고, MCP 설정의 서버 `env` 블록에는 자격증명이 흔히 실리며, 그 이름에는
  대개 어느 저장소에도 무시 규칙이 없다 — `git add -A` 한 번이 그것을 공유 저장소에
  올린다. 자리는 v0.19의 설계 항목이다(설계서 §4). `config.toml`의 백업은 그대로다(D84).

## [0.17.2] — 2026-08-01

session-48이 "판독으로만 확인했고 실행하지 않았다"고 남긴 연쇄를 실측했고 재현됐다.
**우리가 만든 파일이, 편집기가 붙인 세 바이트 때문에 우리 손에서 막히는 상태다.**
세 항목이 한 뿌리다 — 판정용 라인 정규화가 인지하지 못하는 바이트가 있으면 그 줄은
헤더도 마커도 아니게 되고, 그 뒤의 모든 판정이 같은 파일을 다른 파일로 읽는다.

### Fixed

- **선두 UTF-8 BOM이 우리 테이블 헤더 줄에 붙어도 그 테이블을 찾는다** (D80·D89).
  `hook install --codex`가 만드는 `config.toml`의 첫 줄이 `[mcp_servers.ctr]`이고, Windows
  편집기(PowerShell 5.1 `Out-File -Encoding utf8`, 구버전 메모장)가 그 줄에 BOM을 붙인다.
  U+FEFF는 Go의 `strings.Fields`가 보는 공백이 아니라 판정 정규화가 그대로 통과시켰고,
  그러면 헤더로 인식되지 않아 **우리 구간을 잃는다** — 그 구간의 `command`·`args`가 "관리
  테이블 밖의 ctr 정의"로 잡혀 `codex=충돌`이 서고, `hook install --codex`도 `doctor --fix`도
  거절하며, 사용자는 **멀쩡한 자기 파일을 손으로 정리하라**는 안내를 받았다. v0.17.0이 이
  형태를 닫을 때 파서 축만 닫고 라인 정규화 축을 남긴 자리다.
  **되쓰기는 그 세 바이트를 보존한다** — 판정에서만 뗀다.
- **구 블록의 `# BEGIN` 마커가 파일 첫 줄일 때도 마이그레이션이 선다** (D84). 같은 뿌리다.
  마커 정확 매치가 BOM을 보면 분류가 append로 떨어져 install이 관리 테이블을 새로 이어
  붙이고, 같은 논리 테이블이 두 번 정의된다 — 게이트가 기입을 막으므로 파손은 없지만 그
  파일은 어떤 명령으로도 풀리지 않는 막다른 갈래가 된다.
- **구 블록의 소유 판정이 헤더의 후행 주석을 인지한다** (D84). 정규화 문자열을 `==`로
  비교해 `[mcp_servers.ctr] # 메모` 형태에서 소유가 서지 않았고, 그 파일의 마이그레이션이
  영영 닫혔다(방향은 fail-safe였다). 헤더 이름을 읽는 자리를 하나로 모아 닫는다 — 값 판독이
  v0.17.0에서 주석을 인지하게 된 것과 같은 부류의 자리이고, 헤더 쪽은 그때 함께 오지 않았다.

## [0.17.1] — 2026-08-01

v0.17.0의 `/code-review` 워크플로가 주간 한도로 끊겨 검증되지 않은 채 남아 있던 후보
다섯을 재판정한 결과다. 셋은 확정, 둘은 현행 설계 계약이 그 동작을 명시하고 있어
v0.18 설계로 넘겼다(§4). **동작 변경은 없다 — 보고가 파일과 어긋나던 두 자리를 맞춘다.**

### Fixed

- **`hook install --codex`가 `config.toml`을 통째로 읽지 못하는 상태를 알린다** (D89).
  입력이 TOML로 파스되지 않으면 게이트는 계약상 작동하지 않고(비대칭) 기입은 그대로
  일어나는데, 그 사실을 인쇄하는 자리는 `doctor [16]` 하나였다 — **파일을 바꾸는 것은
  설치 경로다.** 그래서 파스되지 않는 파일에 기입하면서
  `MCP 관리 테이블 기입 완료 — Codex 재시작 시 반영`만 나갔고, Codex는 그 파일의 **어떤**
  설정도 읽지 못하므로 재시작해도 반영되지 않는다. 이제 상태 보고 **앞**에 그 사실이
  나가 뒤따르는 문면 전체를 한정한다. **기입 정책은 무변경이다** — 알리기만 한다.
- **MCP 미확정 실행의 등록 개수와 가드 문면이 `hooks.json`과 일치한다** (D47·D32).
  설치 결합은 **등록 방향에만** 걸려, MCP가 확정되지 않은 실행은 가드를 새로 쓰지 않지만
  앞선 설치가 등록한 `PreToolUse` 가드 그룹을 **지우지도 않는다.** 그런데 문면은
  `가드 등록 보류`와 **의도한** 개수(`2개 이벤트 등록 완료`)만 말해, 실제로는 셋이
  등록된 파일을 두고 사용자가 거부 표면이 없다고 읽었다. 이제 개수는 파일에 실제로 남은
  우리 그룹 수이고, 앞선 가드가 남아 있으면 그 사실을 따로 알린다. **동작은 무변경이다** —
  가드를 지우는 방향은 정상 동작 중인 등록물의 보호를 조용히 끄므로 설계 결정이 필요하다(§4).

### Internal

- D91 부족 감지의 `ToolsPresent`(`enabled_tools` 키의 **물리 라인** 존재)에 감시선이
  없었다. 키를 값의 개수로 재는 회귀가 기존 네 행 전부에서 같은 판정을 내 스위트 전체를
  통과한다 — 돌연변이 실측으로 확인했다. `빈 배열` 행을 더해 그 회귀가 물게 했다. 그
  회귀 뒤에는 **도구 0개 allowlist**(D91이 근거로 든 바로 그 파일)에서 부족 경고가 조용히
  사라진다.

## [0.17.0] — 2026-08-01

### Added

- **`config.toml`에 기입하기 전에 우리가 만든 바이트가 TOML로 파스되는지 검증한다** (D89).
  파스되지 않으면 **한 바이트도 쓰지 않고** 무변경으로 빠지며, 그 사유는 다른 이상과 성격이
  반대다 — 사용자 파일을 고치라는 지시가 아니라 **우리 결함의 자수**다:
  `이 도구가 만든 결과가 유효한 TOML이 아니어서 기입하지 않았습니다 — 제품 결함이니 파일
  형태와 함께 알려 주세요`. `hook install --codex`는 그 상태에서 가드 훅 등록도 보류한다.
  - `doctor`는 그 파일을 `[16] codex: [mcp_servers.ctr] 테이블=기입불가` ·
    `[20] mcp markers: … codex=기입불가`로 부르고 `--fix`를 권하지 않는다.
  - **계약은 비대칭이다**: 우리 파서 기준으로 **입력이 이미 파스되지 않는** 파일에서는 게이트가
    작동하지 않는다 — 그 산출물의 무효는 우리가 들인 것이 아니다.
  - **게이트는 구문만 본다.** 사용자 값·키·주석이 그대로인가(의미 보존)는 게이트의 대상이 아니다.
  - 검증에 **새 직접 의존 하나**가 들어왔다 — `github.com/pelletier/go-toml/v2` v2.4.3(MIT).
    **검증 전용**이며 판정·기입은 여전히 파서 비의존이다(D80의 왕복 보존 근거는 유지된다).
    D8의 "의존성 7개"를 실측에 맞춰 개정하고 `NOTICE`(28→29) · `THIRD-PARTY-NOTICES`를 갱신했다.
- **`doctor [16]`이 `config.toml`을 통째로 읽지 못하는 상태를 알린다** (D89). 입력이 TOML로
  파스되지 않으면 `[16] codex: config.toml이 TOML로 파스되지 않습니다 — Codex가 이 파일의
  모든 설정을 읽지 못합니다`가 정보 줄로 나온다. **`failed`에 계상하지 않는다**(종료코드 계약
  무변경) — 기존 `[16]`·`[20]` 경고와 같은 취급이다.
- **`doctor [20]`이 등록물의 도구 목록이 프로필보다 모자란 것을 알린다** (D91). `--fix`가
  `args`·`enabled_tools`를 보존하는 계약(D86) 때문에, 릴리스가 프로필에 도구를 더해도 사용자가
  알 길이 없었다. 새 줄은 `[20]` 접두를 쓰고 codex 사유 줄 뒤·기존 warning 줄 앞에 오며
  **`failed`에 계상하지 않는다.**
  - **넓힌 것은 통과한다** — 손으로 더한 도구는 D86의 보호 대상이라 나그가 되면 안 된다.
    모자란 것만 본다. `enabled_tools` 키가 **물리 라인으로 실재할 때만** 판정한다(부재와 빈
    배열은 의미가 반대다).
  - **기입 가능한 등록물만 본다** — 사용자 소유 테이블·구간 밖 충돌·구간 판정 불가·게이트
    이탈에는 권하지 않는다. 실행해도 바뀌지 않는 명령을 안내하게 되기 때문이다.
  - **권고 문면이 `args` 되읽기 성공 여부로 갈린다.** 되읽은 갈래는 `hook install --codex로
    반영하세요`이고, 되읽지 못한 갈래는 그 명령이 목록을 **보존하므로** 조치가 되지 않는다 —
    `hook install --codex --enable <프로필>`로 프로필을 명시하라고 안내하며(`<프로필>`은 리터럴
    자리표시자다 — 되읽지 못한 값을 추측해 제시하지 않는다), 그 명령이 **해석하지 못한 기존
    `args`도 프로필 값으로 함께 교체한다는 것**을 예고한다. 두 문면 모두 도구 목록이 프로필
    기본값으로 다시 쓰인다는 것을 함께 예고한다.
  - **`--fix`는 목록을 건드리지 않는다.** D86 계약은 무손상이며 이 항목은 감지만 더한다.

### Changed — 사용자 관측 가능한 동작 변경

- **`doctor [16]`이 사용자 소유 테이블을 구별하고 그 막다른 갈래에 조치를 붙인다** (D89).
  종전에는 `테이블=존재`만 인쇄했는데 `--fix`의 거절 문면이 사용자를 `[16]`으로 보내므로 도착한
  자리에 할 일이 없었다. 이제 `테이블=존재(사용자 소유)`와 함께 `[16] warning: 표식도 command도
  우리 것이 아닙니다 — 그 테이블의 이름을 바꾸거나 수동으로 정리한 뒤 hook install --codex를
  재실행하세요`가 나온다.
- **`hook uninstall --codex`가 점 표기 `env.CTR_MANAGED`로 접힌 우리 등록물을 이제 실제로
  제거한다** (D90). 표식 판독기가 그 형태를 읽으므로 표식이 소유를 세운다 — **우리 표식이 우리
  소유의 정의**이므로 제거가 옳다. 종전에는 그 형태를 읽지 못해 `command`가 우리 이름이 아닌
  등록물(절대경로·래퍼로 고쳐 둔 값)이 남의 것으로 서서 제거에서 빠졌고, 훅만 지운 채 관리
  테이블이 남아 Codex가 그 MCP 서버를 계속 띄웠다. 같은 근거로 `hook install --codex`가 그
  등록물을 인수하고 `[20]`이 그 버전 표식을 읽는다.

### Fixed

- **선두 UTF-8 BOM이 붙은 `config.toml`에서 게이트가 스스로 꺼지지 않는다** (D89). 검증에 쓰는
  파서가 그 세 바이트를 키의 첫 글자로 읽고 거부해 **입력이 무효로 섰고**, 그러면 비대칭 계약이
  게이트를 통째로 건너뛰어 검증되지 않은 산출물이 그대로 기입되면서 "기입 완료"가 인쇄됐다 —
  되돌림이 가장 필요한 자리에서 되돌림이 꺼져 있었다. 같은 이유로 `doctor [16]`이 멀쩡한 파일에
  `config.toml이 TOML로 파스되지 않습니다 — Codex가 이 파일의 모든 설정을 읽지 못합니다`를
  오보했다. BOM은 Windows 편집기(PowerShell 5.1의 `Out-File -Encoding utf8`, 구버전 메모장)가
  붙이는 바이트이고 **Codex 자신은 그 파일을 정상으로 읽는다**(실측). 이제 파스 판정 전에 선두
  BOM만 뗀다 — 우리 산출물이 원문 줄을 보존해 그 BOM을 그대로 남기므로 왕복도 같은 함수에서
  함께 닫힌다. 비-UTF-8 바이트는 Codex도 거부하므로 무효 판정이 사실이며 범위 밖이다.
- **`[mcp_servers.ctr]` 안의 점 표기 `env.FOO = "bar"`를 env 서브테이블 정의로 인정한다**
  (D90). 종전에는 그 형태를 알아보지 못해 `[mcp_servers.ctr.env]` 헤더를 덧붙였고, TOML은 점
  표기가 만든 서브테이블을 뒤 헤더로 재정의하는 것을 금지하므로 **사용자의 Codex가 그 파일
  전체를 읽지 못하게** 됐다. `CTR_MANAGED`가 아닌 아무 env 키에서나 나던 파손이다.
  - 표식이 이미 현재 값이면 평소대로 기입하고 **점 표기 줄은 원문 그대로 보존된다.**
  - 표식을 새로 넣거나 갱신해야 하는데 쓸 자리가 점 표기뿐이면 **무변경 + 수행 가능한 안내**로
    빠진다: `env 키가 점 표기(env.NAME)로 적혀 있습니다 — [mcp_servers.ctr.env] 테이블로 옮긴
    뒤 재실행하세요`.
  - 같은 상태를 `doctor [16]`도 다르게 부른다 — 종전 `테이블=존재`가 `테이블=이상`이 되고 위
    사유가 `[16] warning:` 줄로 함께 나온다.
  - 점 표기 `env.CTR_MANAGED`는 **표식 판독기가 소유 표식으로 읽는다** — env 서브테이블을
    정의하는 셋째 형태다(`[mcp_servers.ctr.env]` 헤더 구간 · 관리 테이블 안의 인라인 `env`
    대입 · 점 표기). 소유 판정·버전 라벨·제거가 그 한 함수를 지나므로 함께 옳아진다. 다만
    **그 형태로 기입하지는 않는다** — 새 기입 형태를 들이면 이전 릴리스 바이너리가 그 파일을
    만나 헤더를 덧붙이고 같은 파손을 만든다.
  - 그래서 **표식만이 소유 근거인 점 표기 등록물**(`command`를 절대경로·래퍼로 고쳐 둔 등록물)도
    우리 것으로 인식된다. 인식하지 못하면 이 릴리스가 위에 새로 만든 `테이블=존재(사용자 소유)`
    갈래와 그 정리 안내가 **정상 현재 버전 등록물**에 나가고, **그 안내를 따르면 사용자가 동작
    중인 자기 등록물을 지운다.** `[20]`도 그 등록물을 `미등록`으로 부르지 않고 버전 표식을
    읽는다. 이 형태는 손편집이 아니라 **Codex가 파일을 재직렬화하며 단일 키 서브테이블을 접을
    때** 생기므로 정상 설치 뒤 정상 사용으로 도달한다.
  - 표식이 **현재 값이 아닌** 점 표기 파일은 소유가 선 뒤에도 그대로 무변경으로 이탈한다 —
    기입 형태를 바꾸지 않기 때문이고, 사용자가 받는 것은 위의 수행 가능한 안내다. 그 상태에서
    `hook install --codex`는 `config.toml 무변경·가드 등록 보류`를 인쇄한다 — 표식을 쓰지 못한
    파일을 MCP 확정으로 보지 않으므로 가드 훅을 **새로 등록하지 않는다**(이미 등록된 가드를
    회수하지는 않는다).
  - **이스케이프를 담은 마디**(`"env".X` 류)는 이 릴리스가 다루지 않는다(v0.18 D93). 그 형태가
    만드는 중복 정의는 위 게이트가 막는다.
- **후행 주석에 적어 둔 문자열이 값으로 읽히던 것을 고쳤다.** 값 판독이 주석을 지나 스캔해서
  주석 안 문자열이 표식·`command`의 값이 됐다 — 소유 판정은 표식 **값**을 기준으로 하므로
  **주석 한 줄로 관리 테이블 소유가 설 수 있는 상태**였다. 이제 값 판독이 **문자열 밖 `#`에서
  멈춘다**(홑따옴표 리터럴 안의 `#`은 주석이 아니므로 건너뛴다). 판독 전용이라 되쓰기는 그대로
  원문 바이트를 옮긴다 — 보존 라인의 주석은 사라지지 않는다.
  - 같은 결함이 `enabled_tools`에도 있어 **주석으로 꺼 둔 도구 이름이 활성으로 계수됐다** —
    D91의 도구 부족 감지와 `hook install --codex`의 exec 노출 안내가 함께 사실과 어긋났다.
  - 절단은 **물리 라인마다** 한다 — 주석은 그 물리 라인의 끝까지이므로, 이은 문자열에서 한 번만
    자르면 여러 줄 배열의 첫 주석 뒤에 있는 진짜 원소가 통째로 사라진다.
- **`hook uninstall --codex`가 관리 테이블을 소유 실패로 남겼으면 그 사실과 사유를 알린다.**
  종전에는 소유 판정 실패와 관리 테이블 부재가 같은 반환값으로 빠져 앞의 경우에 **어떤 문면도
  나오지 않았다** — 훅 제거 문면과 종료코드 0만 보이는데 관리 테이블은 남아 Codex가 그 MCP
  서버를 계속 띄웠다. 이제 `관리 테이블이 남아 있으나 표식도 command도 우리 것이 아닙니다 —
  사용자 등록으로 보고 건드리지 않았습니다. 직접 정리하세요`가 나온다. **테이블 부재 갈래는
  그대로 무문면이다** — 캐치올로 접으면 미설치 사용자의 흔한 실행에 잔존 문면이 나간다.
- **`[20]` 경고가 없는 파일의 수리 동작을 약속하지 않는다.** 종전에는 한 문면이 두 파일의
  조치를 늘 함께 예고했다 — `config.toml`이 없는 사용자에게 "백업을 남깁니다"가, `.mcp.json`이
  없는 사용자에게 "옛 이름의 항목을 함께 정리합니다"가 나갔다. 경고 블록의 진입 조건과 파일별
  두 절을 모두 **그 파일에 `--fix`가 실제로 기입하는 조건**에 건다. **둘 다 드리프트인 출력은
  바이트 그대로다** — 판정과 `--fix`의 기입 동작은 바뀌지 않는다.
  - **표식이 현재 버전이어도 경고가 나간다.** `--fix`가 `.mcp.json`을 다시 쓸 상태 — 대체된 옛
    이름의 항목 정리 · `command` 되쓰기 · `alwaysLoad` self-heal · 재직렬화 정규화 — 가 그것이다.
    종전에는 그 절도 경고 블록 자체도 **표식 드리프트**에 걸려 있어 이 상태에서 예고가 통째로
    빠졌고, 이어지는 `--fix`는 되돌릴 기회 없이 사용자의 옛 이름 항목을 지웠다. 예고와 기입이
    이제 같은 계산 하나를 쓴다 — 조건을 두 자리에 문장으로 적은 것이 그 어긋남의 원인이었다.
- **v0.14 구 형식 마이그레이션이 짝 없는 종료 마커를 남기지 않는다.** 구 `# BEGIN/END` 블록이
  관리 테이블과 env 테이블을 **모두** 감싸면 마커 한 줄이 env 구간 안으로 들어오는데, 구간
  편집은 편집 범위 안에서 삭제 목록을 보지 않으므로 그 줄이 보존 라인으로 옮겨졌다. 남은
  마커 때문에 **다음 실행의 마커 분류가 이상으로 떨어져 그 파일의 마이그레이션 경로가
  닫혔다.** 거울 배치(시작 마커가 env 구간 안)도 같은 경로가 받는다. 우리가 배포한 산출물은
  그 배치를 만들지 않으므로 근거는 손편집 파일이다.

### Internal

- `[16]`의 판정원을 `codexRegistrationVerdict`로 옮겨 **`[16]`과 `[20]`이 한 번 계산한 판정을
  공유한다**(D89). 각자 판정하면 같은 파일을 `[16]`은 `테이블=존재`로 `[20]`은 기입불가로
  불러 v0.16이 닫으려던 "판정원 둘"이 재생산되고, doctor 1회가 같은 바이트를 두 번 읽는다.
- 이탈 사유의 전달 채널을 세웠다(D89) — 결과 구조체가 실은 사유가 `probeCodexMCPBlock`의
  사유보다 **우선한다**. install만 아는 이탈(게이트·점 표기)은 probe가 알지 못한다.
- 게이트 결과를 결과 필드가 아니라 **상태 값**으로 냈다(D89). 상태로 분기하는 소비자 다섯 중
  셋이 `Changed`를 보지 않으므로, 필드로 두면 기입을 건너뛴 실행이 "기입 완료"를 인쇄하고 MCP
  확정으로 가드 훅까지 등록한다.
- 도구 부족 판정에 **별도 판독기를 만들지 않았다**(D91) — 진단 전용 필드 넷을 기존 결과·판정
  구조체에 같은 이름·같은 타입으로 실어 나른다. 넷째 판정원이 생기면 그 넷이 갈리는 것이 D85가
  닫은 어긋남이다.
- `marker X≠Y` 라벨 조각의 소유자를 `markerDriftLabel` 하나로 모았다. **문자열 조립만
  공유하고 판정은 각 자리가 그대로 갖는다** — D85가 남긴 분리를 되돌리지 않는다. 라벨 문면은
  불변이다(Codex 16상태 × `.mcp.json` 13상태 × `fix{F,T}`의 `[20]` 출력 342줄 전후 바이트 동일).
- `doctor` 테스트의 호스트 의존을 공통 픽스처로 격리했다 — 환경변수를 세우지 않고 진단을 부르는
  테스트가 여럿이었고 모두 호스트의 실제 파일을 읽어 공유 임시 홈을 통해 서로를 오염시켰다.
- CI가 게이트로 돌리는 `golangci-lint` v2 소견 둘(ineffassign · staticcheck QF1001)과 `gofumpt`
  포맷 하나를 닫았다 — 셋 다 의미 보존 변환이다. 계획의 검증 절차에 lint·포맷 게이트가 없어
  태스크별 리뷰가 구조적으로 놓치던 축이다.
- 줄 번호 인용 위생(§1.4-마): 이 릴리스가 만지는 파일 전체에서 `file.go:123` 형태를 훑어
  남은 둘을 걷어냈다. **둘 다 이미 다른 줄을 가리키고 있었다** — 이 규칙이 있는 이유 그대로다.

### 알려진 잔여 (이 릴리스가 닫지 않은 것)

- **선두 BOM은 파스 판정에서만 떼진다 — 구간 판독은 원바이트를 본다.** 그래서 BOM이
  `[mcp_servers.ctr]` 줄 바로 앞에 붙으면 그 줄이 헤더로 잡히지 않고, 구간 밖에 `mcp_servers`와
  ctr 신호가 함께 있는 상태가 되어 `[20]`의 codex 라벨이 `충돌`이 되고 `hook install --codex`가
  `MCP 기입·가드 등록 보류`로 빠진다. **파일은 손상되지 않지만 재등록이 막히는 막다른 갈래**이며,
  `hook install --codex`가 `config.toml`을 새로 만들 때 그 헤더가 **첫 줄**이므로 Windows
  편집기로 한 번 저장하는 것만으로 도달한다.
- **재직렬화된 점 표기 파일에서 표식이 옛 버전이면 자동 해소가 없다.** 기입 형태를 점 표기로
  바꾸지 않는 것이 위 D90 항목의 결정이므로, 그 파일은 무변경 + 수동 이전 안내로 남는다 —
  **버전을 올릴 때마다** 그 상태에 도달한다.
- **`.mcp.json`에는 `config.toml`과 달리 백업 조항이 없다.** 위 `[20]` 항목이 실행 전 예고를
  세웠지만, `--fix`가 그 파일을 다시 쓴 뒤의 복구 수단은 없다(`config.toml`만 `.bak` 단일
  슬롯을 남긴다).
- **`--fix`의 예고와 기입은 기입 내용 계산 하나를 공유하지만, 그 앞의 가드 술어(읽기 성공 ·
  우리 소유)는 여전히 두 자리에 미러링돼 있다.** 갈리면 같은 부류의 어긋남이 다시 생긴다.

## [0.16.0] — 2026-07-31

### Changed — 사용자 관측 가능한 동작 변경

- **`doctor [20]`의 경고가 `doctor --fix`의 행동과 일치한다** (D85). 경고는 `--fix`가 실제로
  파일을 바꾸는 상태에서만 나온다 — 관리 테이블이 없는 파일에는 경고하지 않고(등록을 만들지
  않으므로) 구간 밖 충돌·구간 판정 불가 상태에서도 `--fix`를 권하지 않는다.
  - **`config.toml`에 대한 `[20]` 판정이 네 상태에서 달라진다** — 같은 파일에 `doctor`를 돌리면
    출력이 바뀌는 것을 본다:
    - 구 `# BEGIN/END` 블록 안에 우리 `command`와 **현재 버전 표식**이 있는 파일:
      `codex=marker <버전>` **무경고**에서 `codex=marker <버전>(형식)` **경고**로. 형식
      드리프트로 `--fix`가 파일을 바꾸는 상태인데 종전에는 경고하지 않았다.
    - 우리 두 구간 **밖**에 충돌 정의가 있고 우리 표식도 있는 파일: `codex=marker <옛>≠<현재>`
      **경고**에서 `codex=충돌` **경고 없음**으로. `--fix`가 그 상태를 고치지 못하므로 권하지
      않는 것이 옳다 — 이 릴리스가 닫는 어긋남이다.
    - 우리 관리 테이블이 없는데 구간 판정에 이상이 있는 `config.toml`: `codex=미등록`에서
      `codex=이상` **+ 사유 줄**로.
    - 구 블록 안에 우리 테이블이 있으나 표식도 없고 `command`도 우리 것이 아닌 파일:
      `codex=미등록` **무경고**에서 `codex=구형식` **경고**로. `--fix`가 인수하는 대상이므로
      권고가 옳다.
  - **라벨 셋이 늘었다**: `구형식`(구 `# BEGIN/END` 블록 안에서 `command`를 직접 고쳐 둔
    등록물 — `--fix`가 마이그레이션한다) · `충돌`(우리 두 구간 밖의 중복 정의 신호) ·
    `이상`(구간 판정 불가). **`이상`은 사유를 함께 인쇄한다** — 중복 헤더인지, 닫히지 않은
    여러 줄 값인지, 이스케이프 표기 키인지가 `[16]`·설치기 안내에 나온다.
  - 경고 문면이 사유 중립으로 바뀌었다 — 표식은 현재인데 형식이 어긋난 상태에서 "버전 표식이
    다르다"고 말하지 않는다.
- **`doctor --fix`가 `enabled_tools`와 `args`를 보존한다** (D86). 손으로 넓힌 도구 목록이
  프로필 기본값으로 줄어들지 않는다. `--fix`가 맞추는 것은 **소유 표식과 `command`**이며,
  `command`를 절대경로나 래퍼로 고쳐 두었다면 그 값은 우리 값으로 되돌아간다.
  - `--fix`는 등록물의 도구 목록을 **늘리지도 않는다.** 릴리스가 프로필에 새 도구를 더하면
    그 반영은 `hook install --codex`가 한다.
  - `--fix`의 안내가 `.mcp.json`에서 **대체된 옛 이름의 항목을 함께 정리한다**는 사실을
    예고한다.
  - **`[20]` 경고가 `command` 되쓰기를 함께 예고한다.** 보존되는 것만 열거하면 절대경로·래퍼로
    고쳐 둔 실행 경로가 예고 없이 우리 이름으로 돌아가고, `PATH`에 그 이름이 없는 호스트에서는
    그 등록이 더 이상 기동하지 않는다.
- **소유 표식의 값이 문자열이 아닌 등록물이 드리프트로 정리된다.** `env.CTR_MANAGED`에 숫자
  같은 값이 들어 있으면 그 표식은 판독되지 않는데, 종전에는 `doctor`가 그 상태를 경고 없이
  `표식없음`으로 보고하면서 같은 실행의 `--fix`가 "이미 현재 형식·버전입니다"라고 말했다 —
  서로 모순된 두 줄이고 사용자에게 다음 조치가 없었다. 이제 `--fix`가 **그 값 토큰만** 관리
  표식 문자열로 갈아 끼운다(같은 줄의 다른 환경변수는 **그 값 안의 텍스트까지** 원문 그대로다 —
  값 안에 표식 키와 같은 모양이 들어 있어도 그 자리는 값이지 키가 아니다). 값이 다음 줄로
  이어지는 형태는 여전히 손대지 않는다.
- **`hook uninstall --codex`가 `config.toml`을 손대지 못했으면 그 사실과 사유를 알린다.** 구간
  판정에 이상이 있는 파일(중복 헤더 · 닫히지 않은 여러 줄 값 · 이스케이프 표기 키)에서는 관리
  테이블을 지우지 않는데, 종전에는 그 갈래에서 아무 줄도 내지 않아 훅 항목 제거 문면만 보였다 —
  두 관리 테이블이 남아 Codex가 그 MCP 서버를 계속 띄우는데도 성공으로 읽혔다. 종료코드 계약은
  바뀌지 않는다.
- **관리 테이블 안의 키가 이스케이프 표기로 적혀 있으면 `config.toml`을 손대지 않는다**
  (D87). TOML은 그 표기를 보통 표기와 같은 키로 읽는데 우리는 이스케이프를 해석하지 않으므로,
  기입하면 같은 키가 두 번 정의된 파일이 된다. `doctor`가 그 상태를 사유와 함께 알린다.
  값에 든 역슬래시(Windows 경로 등)는 이 판정의 대상이 아니다. **키 줄의 후행 주석도 대상이
  아니다** — 주석 안에 이스케이프 표기의 키 모양이 있어도 정상 파일로 본다.

### Internal

- `doctor [20]`과 `doctor --fix`가 요청 조립까지 공유하는 한 판정을 쓴다 — 감지와 고침이 다른
  판독기에서 나오던 구조를 닫았다(D85).
- 구간 판정 실패가 불리언에서 사유 열거형으로 올라갔다(D85·D87, 이월 X3).
- **키 줄의 후행 주석은 왕복 보존 대상이 아니다**를 계약으로 명시했다(D88). 인라인 `env`
  경로는 값을 바꾸거나 표식을 끼워 넣을 뿐 그 줄을 다시 쓰지는 않으므로 주석은 남는다.
- v0.14 구 형식 마이그레이션의 **배선**(`hook install --codex` · `doctor --fix`)에 테스트가
  생겼다 — 그전까지 구 형식 픽스처는 순수 함수 수준에만 있었다.
- `[0.15.0]` 절에서 빠져 있던 사용자 관측 문면 변경을 보완했다.

## [0.15.0] — 2026-07-30

### Changed — 사용자 관측 가능한 동작 변경

- **Codex `config.toml`의 관리 단위가 주석 마커에서 TOML 테이블 경계로 바뀌었다** (D80).
  `# BEGIN/END context-router`를 더 쓰지 않는다 — Codex는 다른 서버를 추가하는 것만으로
  파일 전체를 재직렬화하며 주석을 지우므로, 마커로는 관리 단위를 유지할 수 없다. 관리 범위는
  `[mcp_servers.ctr]`와 `[mcp_servers.ctr.env]` **두 독립 구간**이고, 소유 표식은 재직렬화를
  견디는 `env.CTR_MANAGED`로 옮겼다(서버는 이 환경변수를 읽지 않는다 — 표식일 뿐이다).
  - **재설치가 우리 테이블 안의 사용자 키를 더 이상 버리지 않는다.** install이 소유하는 키는
    `command`·`args`·`enabled_tools`·`env.CTR_MANAGED` 넷이고, 그 밖의 키(예: 직접 넣은
    `default_tools_approval_mode`)와 `env`의 다른 환경변수는 원문 그대로 왕복 보존된다.
  - **구 형식 파일은 1회 변환된다.** 마커 두 줄만 지우고 테이블은 그대로 둔다. 마커가 이미
    소멸한 파일도 `command`가 우리 것이면 인수해 표식을 다시 기입한다(self-heal).
  - `hook uninstall --codex`는 우리 두 테이블만 지운다 — 마커가 밀려 그 사이에 사용자 테이블이
    들어간 파일에서도 그 테이블이 살아남는다.
  - **`[mcp_servers.ctr-exec]`는 읽지도 쓰지도 않는다.** 그 이름은 `.mcp.json` 등록의 제품
    표준 이름이지만 `config.toml`에서 같은 이름의 테이블은 사용자가 만든 별도 등록이다.
  - 내용을 바꾸기 직전 `config.toml.bak` 단일 슬롯 백업을 남긴다(누적하지 않는다). 기입 바이트가
    기존과 같으면 쓰기와 백업을 모두 생략한다.
- **설치기 기본 프로필이 `ingest,net`이 됐다** (D81). 플래그 없는 **첫** 설치가
  `ctr_index`·`ctr_fetch_and_index`를 등록물에 싣는다 — 그 둘의 미등록이 세 세션 이월된 원인이
  기본값이었다. 이미 설치된 항목이 있으면 그 프로필을 그대로 유지한다(재설치가 켜 둔 프로필을
  끄지 않는다). **exec는 `--enable-exec` 명시 opt-in 그대로다.**
  - **`hook install --enable <프로필 목록>`**(쉼표 구분)을 새로 받는다. `--enable-exec`과 함께
    지정하면 결과는 합집합이고 지정 순서가 결과를 바꾸지 않는다. 모르는 이름은 오류다.
  - **`--codex`와 `--enable-exec`을 함께 쓸 수 있다.** 프로필이 Codex 관리 테이블에도 실리고
    `enabled_tools`가 `args`와 **함께** 조립되므로, 그 조합을 막던 두 사유가 모두 사라졌다.
  - 설치기는 승인 모드 키(`default_tools_approval_mode`·`tools.<도구>.approval_mode`)를
    **기입하지 않는다.** 관리 블록의 권장 주석은 재직렬화가 지우므로 같은 안내를 stdout으로 낸다.
- **훅 등록물에서 버전이 사라졌다** (D82). `.claude/settings.json` 훅 그룹의 `__ctrManaged`와
  Codex `hooks.json`의 `statusMessage`가 무버전 `context-router`가 된다. 버전은 MCP 등록물
  (`.mcp.json`의 `__ctrManaged`, `config.toml`의 `env.CTR_MANAGED`)에만 남는다. 훅 정의가 릴리스
  간 바이트 동일해지므로 **같은 설치 옵션·같은 MCP 상태에서는 버전만 이유로 Codex 재신뢰가
  강요되지 않는다**(`trusted_hash`가 무엇을 덮는지는 Codex 내부라 미검증이며, 이 주장은
  필요조건까지다). 구 버전 마커(`context-router/0.14.0`)도 계속 소유로 인정되고 대칭 제거된다.
  `doctor`의 훅 스코프는 무버전 마커에 버전 불일치 경고를 내지 않는다.
- **`doctor`에 `[20] mcp markers`와 `doctor --fix`가 생겼다** (D83). 두 MCP 등록물의 버전 표식을
  읽어 드리프트를 알리고, `--fix`가 현재 버전으로 다시 기입한다. **파일이 없으면 만들지 않고,
  파일이 있어도 우리 소유로 확인된 등록물이 없으면 만들지 않으며**, 종료코드 계약도 바뀌지
  않는다(드리프트와 그 고침은 실패 항목 수에 들지 않는다). 기입은 install이 쓰는 경로를 그대로
  재사용하고, config.toml 갈래는 D84의 백업을 남긴다.
- **`doctor`의 호스트 등록 안내가 두 절 모두 바뀌었다.** Codex 절은 새 형식을 인쇄한다 —
  `[mcp_servers.ctr.env]`와 무버전 `CTR_MANAGED`를 담으므로 붙여 넣은 등록도 소유 판정에
  걸린다. **Claude Code 절도 함께 바뀌었다**: `.mcp.json` 예시와 사용자 스코프
  `claude mcp add` 명령줄의 프로필이 `exec`에서 기본 프로필 `ingest,net`으로 바뀌었고,
  `permissions` 예시에 붙은 설명이 "그 등록에는 두 도구 자체가 없어 규칙이 아무것도 매치하지
  않는다"에서 "두 도구가 모두 있어 규칙이 그대로 매치한다"로 바뀌었다 — 붙여 넣은 `ask`가
  이제 실제로 물린다. 두 절의 `args`·`enabled_tools`도 같은 기본 프로필을 담는다.
- **승인 프롬프트 안내에 비대화형 경고가 붙었다.** `default_tools_approval_mode = "prompt"`는
  대화형 세션 전용이다 — 프로그램이 Codex를 비대화형으로 몰면 그 프롬프트에 답할 수단이 없어
  그 서버의 도구 호출이 응답 없이 매달린다. 설치기 stdout과 `doctor`의 Codex 스니펫 **두 곳**
  모두에 그 문장이 있다.
- **`hook install`이 빈 프로필 등록물에 명시 경로를 안내한다.** v0.14 이전 등록물은 `--enable`이
  없어 전부 빈 프로필이고 보존 규칙이 이겨 기본 프로필로 넓어지지 않으므로, 그 상태를 감지하면
  `hook install --enable ingest,net`을 알린다. 조용히 넓히면 쓰기·네트워크 도구를 동의 없이
  노출하므로 동작이 아니라 안내로 닫았다.
- **`hook install --codex`가 exec 노출 경로를 알린다.** 관리 테이블의 `enabled_tools`에 exec
  도구가 실리면 그 사실을 stdout으로 낸다 — 별도 `[mcp_servers.ctr-exec]`에 걸어 둔 승인 모드는
  서버 테이블 단위라 이 경로에는 걸리지 않는다. **문면이 두 톤이다**: 이번 실행이 켠 경우와,
  손대지 않은 값에 이미 들어 있던 경우를 구분한다.
- **기존 `args`를 프로필로 되읽지 못하면 그 사실을 알린다.** 그때 `args`와 `enabled_tools`는
  **둘 다** 손대지 않고 `command`와 소유 표식만 갱신하며, 프로필을 바꾸려면 `--enable`로
  명시해야 한다.
- **`doctor [16]`과 `hook install --codex`의 문면에서 "블록"이 "테이블"이 됐다** (D80). `[16]`의
  상태 표시가 `[mcp_servers.ctr] 테이블=존재/부재/이상`이고, 이상 경고는 `config.toml 관리 마커
  이상/키-경계 충돌`에서 `관리 테이블 중복 정의/키-경계 충돌`로, 설치기 보류 안내는 `관리 블록
  마커 이상 … 블록 수동 정리`에서 `관리 테이블 중복 정의 … 중복 헤더 정리`로 바뀌었다.
- **`hook install --codex`의 기존 등록 감지 안내가 조건을 좁혀 다시 쓰였다.** 그 문면은 이제
  **표식도 없고 `command`도 우리 것도 아닌** `[mcp_servers.ctr]`에만 나온다(사용자 등록으로
  본다) — 표식이 없어도 `command`가 우리 것이면 D80의 인수 대상이라 기입을 생략하지 않는다.
- **`doctor`의 훅 스코프 라벨에서 무버전 마커의 `marker` 절이 사라졌다** (D82). `[9]`와 `[16]`이
  `등록됨(N개, marker context-router)`이 아니라 `등록됨(N개)`을 인쇄한다 — 버전 비교는 `[20]`이
  맡는다.
- **`doctor`의 미지 플래그 오류 문면이 바뀌었다.** 모르는 플래그를 주면 `예상치 않은 인자 N개`가
  아니라 `인식할 수 없는 플래그(사용 가능: --fix)`가 나온다. 플래그가 아닌 잉여 인자는 종전
  문면 그대로다.
- **`config.toml.bak` 단일 슬롯 백업이 `hook uninstall --codex`에도 선다** (D84). 세 경로
  (`hook install --codex` · `doctor --fix` · `hook uninstall --codex`) 모두 내용을 바꾸기 직전에
  같은 슬롯을 덮어쓰고, 백업이 실패하면 파일을 바꾸지 않고 멈춘다.

### Internal

- 게이트 11(`TestSchemaTokenBudget`)의 측정 대상을 **설치기가 만들 수 있는 최대 프로필**
  (`ingest`·`net`·`exec` 10도구)로 옮기고 상한을 실측 + 최소 여유로 재기준화했다. 이전 형태는
  exec만 켠 8도구를 재서 D81이 여는 표면이 옛 상한을 넘겨도 통과했다 — 물리지 않는 검사였다.
- 설계 문서와 `internal/exec` 주석이 `exec.go`를 줄 번호로 가리키던 인용을 심볼 인용으로
  바꿨다. v0.14 한 릴리스에 인용이 두 번 밀린 부류가 구조적으로 사라진다.
- `TestD78Limits`의 `fallback-throw-no-signal`이 파싱 실패를 가르게 됐다 — PowerShell은 미처리
  `throw`와 ParserError에 모두 exit 1을 내므로, 본문이 실제로 실행됐음을 stdout 표지로 함께
  단정한다.

## [0.14.0] — 2026-07-29

### Changed — 사용자 관측 가능한 동작 변경

- **shell 러너(windows)의 `.ps1` 종료 신호를 `Register-EngineEvent PowerShell.Exiting`으로
  승격했다** (D78). `exit`·인수 없는 `exit`·`throw`로 끝나는 스니펫에서도 마지막 외부
  명령의 종료 코드가 기록되므로, v0.13이 적은 "스니펫이 `exit`으로 끝나면 이 보강은
  동작하지 않는다"는 더 이상 맞지 않는다. 보강 줄의 조건은 D76 그대로다 — `exit_code`가
  0이고 `stderr`가 빈 경우에만 들어가고, `exit_code`의 의미도 바뀌지 않는다. 그래서 줄이
  **새로 붙는 것은 종료 코드 0으로 끝나는 `exit`**(인수 없는 `exit`과 `exit 0`)이고,
  `exit <비 0>`과 `throw`는 종료 코드가 기록돼도 그 조건에서 걸러진다.
  - 등록 문은 스니펫을 **감싸지 않고 첫 줄에 이어 붙는다** — 문 종료 오류가 나도 그 뒤의
    나머지 스니펫이 그대로 실행된다. 대신 **오류가 1행뿐인 스니펫에서는 stderr의 열
    번호가 밀리고 소스 줄 에코에 등록 문 꼬리가 실린다**(줄 번호는 정확하다). 2행 이상
    오류의 stderr는 바이트 그대로다.
  - 최상단 전용 구문(`param`·`using`·`dynamicparam`·명명 블록·속성 선언·줄 연속)으로
    시작하는 스니펫은 승격하지 않고 v0.13 형태로 폴백한다 — 그 경로의 알려진 공백도
    함께 유지된다.

### Internal

- 기동 purge의 실 잠금 보유 시간에 자동 게이트를 세웠다 (D77). 임계는 훅 예산 2000ms의
  50%다. 픽스처는 나이 게이트를 넘겨 **실제 회수 경로를** 재며, 그 사실을 테스트가
  단정한다(`ReclaimedB`·`DeferredFiles`·`FailedFiles`). 잡는 것은 해시당 비용의
  회귀이며, 100해시 배치의 절대 보유 시간은 합성 픽스처로 검증하지 않는다.
- lint에 `GOOS=darwin`·`GOOS=netbsd`·`--build-tags perf` 세 축을 더해 D69가 남긴 미검출
  파일 셋(`launcher_darwin.go`·`launcher_other.go`·`perf_test.go`)을 비웠다 (D79). 축마다
  컴파일을 먼저 확인한다 — 빌드가 깨진 축에서 lint는 `0 issues.`를 내며 종료 코드만
  실패해 스코프 확인이 거짓 초록이 되기 때문이다. `perf`는 태그가 걸린 파일이 테스트
  파일뿐이라 `go build`가 아니라 **테스트 컴파일**로 판정한다. `freebsd`는 그 빌드 조건을
  만족하지 않아 대표에서 제외했다.

## [0.13.0] — 2026-07-27

### Changed — 사용자 관측 가능한 동작 변경

- **shell 러너(unix)가 새로 홈 유도 격리를 적용받는다** (D75). `~`와 `$HOME`이 매 실행
  새 스크래치를 가리키므로 그 안에서 부르는 툴체인이 호스트 사용자 구성을 읽지 않는다 —
  javascript·typescript 러너와 같은 공유 헬퍼다(그 둘은 v0.12/D65부터 이미 스크래치
  홈을 썼고, 이번에 `CFFIXED_USER_HOME`·`XDG_DATA_HOME` 앵커를 추가로 얻는다). **홈
  유도 툴체인 루트와 캐시도 함께 옮겨가 스니펫이 의존물을 다시 받는다** — 실행 시간이
  늘고 네트워크 접근이 발생할 수 있다(dotnet 첫 실행 워크로드 무결성 검사망은 이 배송이
  억제하지만, `npm install`·`dotnet restore` 같은 일반 의존성 복원까지 막지는 않는다).
- **javascript·typescript 러너에서 홈 준비 실패가 실행 거부로 바뀌었다** (D75).
  이전에는 경고만 남기고 실행했다. 홈이 없으면 재지정이 성립하지 않고 그 상태로 실행하면
  호스트 홈으로 되돌아갈 수 있다.
- **shell 러너(windows)가 마지막 외부 명령의 종료 상황을 `stderr`에 한 줄로 남긴다** (D76).
  `exit_code`가 0이고 `stderr`가 빈 경우에만 들어가며 `exit_code`의 의미는 바뀌지 않는다.
  이 줄은 "스니펫이 실패했다"가 아니라 "마지막 외부 명령(네이티브 프로그램 또는 호출된 스크립트)이 비 0으로 끝났다"를
  뜻한다. 스니펫이 `exit`으로 끝나면 이 보강은 동작하지 않는다 — 종료 코드를 명시하는 것이
  그 경우의 대응이다.
- **doctor `[3]`에 대상 색인의 실재 수가 병기된다** (D73): `indexes=<실재>/3`.

### Internal

- shadow 보존 술어에 복합 색인 3개를 더했다 (D73). **스키마 버전은 그대로 1이다** — 색인은
  가산적 변경이라 이 릴리스의 스토어를 이전 버전 바이너리도 계속 열 수 있다. 색인이 있으면
  shadow 아티팩트가 늘어도(2천→2만 행) 술어 소요 증가가 완만하다 — 없으면 거의 제곱으로
  자란다(수치는 설계 §3 실측 참조).
- 기동 purge의 파일 회수 루프가 종료 신호를 관측한다 (D74).

## [0.12.1] — 2026-07-27

### Changed

- **doctor `[12]` drops 진단에 사유별 마지막 발생 시각이 병기된다** (D71).
  형태는 `<사유>=<건수>@<YYYY-MM-DD>`(UTC)다. 누적 집계만 보이던 이전 출력은 이미 해결된
  문제를 현재 발생 중인 것처럼 읽히게 했다 — 시각이 있으면 그 구분이 한눈에 보인다.
  정수 변환에 실패한 **줄만** 병기에서 빠지고, 같은 사유의 다른 줄이 만든 병기는
  남는다(집계는 유지된다). `unparsed`는 사유 단위 버킷이라 애초에 병기 대상이
  아니다.

### Internal

- CI 액션 참조를 커밋 SHA로 고정하고 워크플로 토큰 권한을 `contents: read`로 좁혔다 (D68).
- `GOOS=windows` lint 스텝을 추가해 windows 전용 파일의 미검출 정적 오류를 닫았다 (D69).
- bun을 설치하지 않는 잡을 추가해 node 인터프리터 레그를 CI에서 실행한다 (D70).
- `buildinfo` 주석을 실제 릴리스 절차에 맞췄다 (D72).

## [0.12.0] — 2026-07-26

### Changed — 사용자 관측 가능한 동작 변경

- **shadow 데이터가 3일 후 자동 삭제된다** (D67). 서버 기동 시 1회 실행되며,
  훅이 가로챈 출력(shadow 귀속)에만 적용된다. 나이 기준은 **마지막 포착
  시각**이므로 같은 내용이 다시 포착되면 보존 기간이 갱신된다. `ctr_index` ·
  `ctr_fetch_and_index`로 직접 넣은 explicit 소스는 **무기한 보존**한다.
  보존 기간은 `CTR_SHADOW_RETENTION`(`time.ParseDuration` 형식, 양수만 채택)으로
  조정할 수 있다.
  - **파일 크기는 즉시 줄지 않는다.** 삭제로 생긴 여유 공간은 이후 기록이
    재사용하고, 자동 경로에서 VACUUM은 하지 않는다 — 목표는 파일 축소가 아니라
    성장 억제다. 디스크를 되돌려야 하면 서버 비가동 상태에서
    `purge --project <id> --older-than <dur> --vacuum`.
  - doctor `[14]`의 "자동 삭제 없음"은 **임계 초과 경고**에 붙은 문면이다 —
    임계를 넘겨도 그 경고 자체가 삭제를 유발하지 않는다는 뜻이며, 위의 3일 보존과
    별개 경로다.
- **python 러너에서 호스트 `pip install --user` 패키지가 보이지 않는다** (D65).
  `PYTHONNOUSERSITE=1`로 실행하고 `PIP_CONFIG_FILE`을 스크래치로 돌린다.
- **javascript·typescript 러너가 스크래치 홈을 쓴다** (D65). `HOME`과
  `XDG_CONFIG_HOME`을 스크래치로 돌리므로 호스트 홈의 도구 구성이 스니펫에
  반영되지 않는다.
- **go 러너가 `GOENV`를 스크래치로 돌린다** (D65). 호스트 `go env -w` 설정이
  스니펫 실행에 상속되지 않는다.
- **shell 러너(windows)가 모듈 경로와 사용자 프로필을 고정한다** (D65).
  `PSModulePath`를 스크래치·인터프리터 설치본·머신 전역 세 항목으로 명시하고
  `USERPROFILE`을 스크래치로 돌리므로, 호스트에 설치된 사용자 모듈이 스니펫에서
  자동 로드되지 않는다.
- **격리 구성 쓰기가 실패하면 실행을 거부한다**(fail-closed). 이전에는 경고만
  남기고 그대로 실행했다. 판정 기준은 "그 파일이 없을 때 호스트와 무관한 기본으로
  안전하게 퇴화하는가"이며, 퇴화하지 않는 지점(pip.conf · nuget.config ·
  psmodules · GOTMPDIR · macOS Application Support · NuGet sentinel)이 시행
  대상이다.
- **MCP 서버 등록이 단일화되고 상시 로드가 붙는다** (D63). 설치기가
  `alwaysLoad: true`를 쓴 단일 `ctr-exec` 항목을 만들고, 같은 도구를 중복 노출하던
  옛 `ctr` 등록은 제거한다. `alwaysLoad`는 **Claude Code v2.1.121 이상**에서만
  동작하며 그 이하 호스트는 이 키를 조용히 무시한다.
- **`usage --adoption` 집계 단위가 MCP 서버 네임스페이스로 바뀌었다** (D63).
  이전의 `ctr_` 부분 문자열 버킷팅은 서로 다른 서버 등록을 한 카운터에 합쳤다.
  이제 서버별 호출 수와 그 서버를 부른 고유 세션 수를 tool_call 세션 분모와 함께
  낸다. 옛 2행 출력과 ratio 문면은 없어졌다.

### Added

- **`hook install`이 `.mcp.json` 등록과 호스트 승인 키까지 관리한다** (D64).
  이전에는 훅만 설치했고 MCP 등록은 수동 편집이었다. 플래그 없는 재실행은 기존
  `args`를 보존하고(활성 프로필을 끄지 않는다), 재실행 결과는 바이트 단위로
  멱등이다. Codex 훅은 별도 갈래이므로 전체 설치는 **세 번**이다 —
  `hook install` · `hook install --codex` · `hook install --codex --user`.
- **`hook uninstall`이 설치기가 넣은 것을 되돌린다** (D64). `.mcp.json`의 관리
  항목과 승인 키에서 그 이름을 함께 제거한다.
- **doctor `[19]` 승인 규칙 정합 진단** (D64). `ask`와 `allow`가 같은 도구를
  덮어 한쪽이 무력화되는 상태를 보고한다.
- **doctor 호스트 스니펫이 권한 모드를 기준으로 안내한다** (D64). 승인 강도가
  호스트 권한 모드와 규칙 평가 순서에 따라 달라지는 점을 문면에 반영했다.

[0.19.0]: https://github.com/wotjr1649/context-router/compare/v0.18.0...v0.19.0
[0.18.0]: https://github.com/wotjr1649/context-router/compare/v0.17.2...v0.18.0
[0.17.2]: https://github.com/wotjr1649/context-router/compare/v0.17.1...v0.17.2
[0.17.1]: https://github.com/wotjr1649/context-router/compare/v0.17.0...v0.17.1
[0.17.0]: https://github.com/wotjr1649/context-router/compare/v0.16.0...v0.17.0
[0.16.0]: https://github.com/wotjr1649/context-router/compare/v0.15.0...v0.16.0
[0.15.0]: https://github.com/wotjr1649/context-router/compare/v0.14.0...v0.15.0
[0.14.0]: https://github.com/wotjr1649/context-router/compare/v0.13.0...v0.14.0
[0.13.0]: https://github.com/wotjr1649/context-router/compare/v0.12.1...v0.13.0
[0.12.1]: https://github.com/wotjr1649/context-router/compare/v0.12.0...v0.12.1
[0.12.0]: https://github.com/wotjr1649/context-router/compare/v0.11.1...v0.12.0
